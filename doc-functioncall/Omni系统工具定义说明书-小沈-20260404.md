# Omni 系统工具定义说明书

**创建时间**: 2026-04-04 06:15:11
**编写人**: 小沈
**版本**: v1.0

---

## 一、工具定义核心（精要）

### 1.1 工具定义四要素

每个工具定义包含以下4个核心要素：

| 要素 | 来源 | 说明 |
|------|------|------|
| **name** | 手动指定 | 工具名称，LLM 通过此名称调用 |
| **description** | 手动指定 | 工具用途说明，告诉 LLM 何时使用 |
| **parameters** | Pydantic模型自动生成 | 参数类型、必选/可选、参数描述 |
| **examples** | 手动指定 | 使用示例（可选，帮助 LLM 理解） |

### 1.2 一个函数的完整定义包含

| 序号 | 字段 | 来源 | 说明 |
|------|------|------|------|
| 1 | name | @register_tool 手动指定 | 工具名称 |
| 2 | description | @register_tool 手动指定 | 工具用途说明 |
| 3 | input_schema | Pydantic 模型自动生成 | 参数类型、必选/可选、描述 |
| 4 | input_examples | @register_tool 手动指定 | 使用示例（可选） |

### 1.2 工具定义代码结构

```python
@register_tool(
    name="工具名",                                    # ← 1. name
    description="工具用途说明...",                     # ← 2. description
    input_model=工具参数模型,                         # ← 3. Pydantic模型 → 自动生成parameters
    examples=[{...}]                                  # ← 4. examples（可选）
)
async def 工具函数(self, 参数...):
    ...
```

### 1.3 Pydantic 模型定义参数

```python
class 工具名Input(BaseModel):
    参数名: 类型 = Field(..., description="参数说明")           # 必填参数
    参数名: 类型 = Field(default=默认值, description="参数说明")  # 可选参数
```

### 1.4 最终输出格式（OpenAI兼容）

```python
{
    "type": "function",
    "function": {
        "name": "工具名",
        "description": "工具用途...",
        "parameters": {
            "type": "object",
            "properties": {
                "参数名": {
                    "type": "类型",
                    "description": "参数说明"
                }
            },
            "required": ["必填参数"]
        }
    }
}
```

---

## 二、详细说明与示例

### 2.1 @register_tool 装饰器

**作用**：注册工具，将其添加到工具注册表

**参数**：
- `name`: 工具名称（必填）
- `description`: 工具描述，说明使用场景和参数说明（必填）
- `input_model`: Pydantic 模型类，用于生成参数 Schema（必填）
- `examples`: 使用示例列表，可选，帮助 LLM 理解参数格式

### 2.2 Pydantic 模型

**作用**：用 Pydantic 定义工具参数的结构、类型、验证规则
 
类型是什么？（str、int、bool）
必填还是可选？（... 必填，default=xxx 可选）
参数描述是什么？

**关键特性**：
- `...` 表示必填参数
- `default=xxx` 表示可选参数
- `Field(description="xxx")` 添加参数描述
- `ge`、`le`、`gt`、`lt` 用于数值范围限制

### 2.3 自动 Schema 生成- Pydantic 自动转换

**原理**：通过 `model_json_schema()` 将 Pydantic 模型转换为 JSON Schema

```
Pydantic 模型                          JSON Schema
─────────────────────────────────────────────────────────
str                                  → "type": "string"
int                                  → "type": "integer"
bool                                 → "type": "boolean"
... 必填                             → "required": ["参数名"]
Field(description="xxx")              → "description": "xxx"
Field(default=1)                      → 省略（默认值的参数不在 required 中）
```

### 2.4 最终 Schema 格式 —— 给 LLM 看的"说明书"
```python
{
    "type": "function",
    "function": {
        "name": "read_file",
        "description": "读取文件的内容。使用场景：...",
        "parameters": {
            "type": "object",
            "properties": {
                "file_path": {
                    "type": "string",
                    "description": "文件的完整路径"
                },
                "offset": {
                    "type": "integer",
                    "description": "起始行号，从1开始"
                },
                "limit": {
                    "type": "integer", 
                    "description": "最大读取行数"
                },
                "encoding": {
                    "type": "string",
                    "description": "文件编码"
                }
            },
            "required": ["file_path"]   # 只file_path必填
        }
    }
}
```
LLM 看到后就知道：
- "有个 read_file 工具"
- "必须传 file_path 参数"
- "可以选填 offset、limit、encoding"

### 2.5 一句话总结

| 步骤 | 比喻 |
|------|------|
| 1. 工具注册 | 告诉系统"我有一个工具" |
| 2. Pydantic模型 | 定义工具的参数长什么样 |
| 3. 自动生成Schema | 自动转换成LLM能看懂的格式 |
| 4. 最终Schema | 给 LLM 的完整说明书 |

## 三、示例：list_directory 工具

### 3.1 Pydantic 模型（file_schema.py）

```python
class ListDirectoryInput(BaseModel):
    """list_directory 工具的输入参数"""
    dir_path: str = Field(
        description="目录的完整路径（必须是绝对路径，如 D:/项目代码）"
    )
    recursive: bool = Field(
        default=False,
        description="是否递归列出所有子目录，默认为False（不递归）"
    )
    max_depth: int = Field(
        default=10,
        ge=1,
        le=50,
        description="最大递归深度，仅当 recursive=True 时有效，默认为10"
    )
```

### 3.2 工具注册（file_tools.py）

```python
@register_tool(
    name="list_directory",
    description="""列出指定目录中的所有文件和子目录。

使用场景：
- 当用户想要查看某个文件夹里有什么文件时使用此工具
- 当需要了解目录结构时使用
- 当需要获取文件列表进行进一步操作时使用

参数说明：
- dir_path: 目录的完整路径（必须是绝对路径，如 D:/项目代码）
- recursive: 是否递归列出子目录内容，默认为False
- max_depth: 最大递归深度，仅当 recursive=True 时有效

【重要】必须使用 dir_path 作为参数名""",
    input_model=ListDirectoryInput,
    examples=[
        {"dir_path": "C:/Users/用户名/Documents", "recursive": False},
        {"dir_path": "D:/项目代码", "recursive": True, "max_depth": 3}
    ]
)
async def list_directory(
    self,
    dir_path: str,
    recursive: bool = False,
    max_depth: int = 100000,
) -> Dict[str, Any]:
    """列出目录内容"""
    ...
```

### 3.3 最终生成的 Schema

```python
{
    "type": "function",
    "function": {
        "name": "list_directory",
        "description": "列出指定目录中的所有文件和子目录。\n\n使用场景：...\n\n参数说明：\n- dir_path: 目录的完整路径...\n- recursive: 是否递归...\n- max_depth: 最大递归深度...",
        "parameters": {
            "type": "object",
            "properties": {
                "dir_path": {
                    "type": "string",
                    "description": "目录的完整路径（必须是绝对路径，如 D:/项目代码）"
                },
                "recursive": {
                    "type": "boolean",
                    "description": "是否递归列出所有子目录，默认为False（不递归）",
                    "default": False
                },
                "max_depth": {
                    "type": "integer",
                    "description": "最大递归深度，仅当 recursive=True 时有效，默认为10",
                    "default": 10
                }
            },
            "required": ["dir_path"]
        }
    }
}
```

---

### 3.4 Tool Examples 编写指南

**examples 的作用**：
- 帮助 LLM 理解参数的格式和组合方式
- 展示必填参数和可选参数的典型用法
- 覆盖不同的使用场景
- 提高 LLM 调用工具的准确率

**编写原则**：
1. **覆盖典型场景**：至少包含 2-3 个不同场景的示例
2. **展示参数组合**：展示必填参数单独使用、与可选参数组合使用
3. **真实路径**：使用真实的路径格式（Windows 绝对路径）
4. **简洁明了**：每个示例只展示一种用法，不要过于复杂

#### 示例 1: read_file 工具（复杂参数组合）

```python
@register_tool(
    name="read_file",
    description="从文件系统读取文件，支持文本、图片、PDF、Jupyter notebook，自动处理 UTF-8/GBK 编码。",
    input_model=ReadFileInput,
    examples=[
        # 示例1：只传必填参数（最常用场景）
        {"file_path": "D:/OmniAgentAs-desk/backend/app/main.py"},
        # 示例2：读取大文件的前 N 行
        {"file_path": "D:/OmniAgentAs-desk/backend/app/services/agent.py", "offset": 1, "limit": 100},
        # 示例3：读取文件的中间部分
        {"file_path": "D:/OmniAgentAs-desk/doc/Function-Call参考和新设计-小沈-2026-04-03.md", "offset": 500, "limit": 50}
    ]
)
```

#### 示例 2: grep_file_content 工具（多可选参数）

```python
@register_tool(
    name="grep_file_content",
    description="基于 ripgrep 的强大内容搜索，支持正则表达式和多选项，支持 Unicode 中文字符搜索。",
    input_model=GrepFileContentInput,
    examples=[
        # 示例1：只传必填参数（简单搜索）
        {"pattern": "def read_file"},
        # 示例2：指定搜索目录和文件类型
        {"pattern": "class.*Agent", "search_dir": "D:/OmniAgentAs-desk/backend", "glob": "*.py"},
        # 示例3：带上下文的复杂搜索
        {"pattern": "async def.*tool", "search_dir": "D:/OmniAgentAs-desk/backend", "context_lines": 3, "ignore_case": True},
        # 示例4：限制返回数量
        {"pattern": "import", "search_dir": "D:/OmniAgentAs-desk/frontend", "head_limit": 20, "output_mode": "files_with_matches"}
    ]
)
```

#### 示例 3: edit_file 工具（复杂嵌套参数）

```python
@register_tool(
    name="edit_file",
    description="使用高级模式匹配进行选择性编辑，支持多同时编辑、缩进保留、dryRun 预览。",
    input_model=EditFileInput,
    examples=[
        # 示例1：单次编辑
        {
            "file_path": "D:/OmniAgentAs-desk/backend/app/main.py",
            "edits": [{"oldText": "def old_function():", "newText": "def new_function():"}]
        },
        # 示例2：多次编辑
        {
            "file_path": "D:/OmniAgentAs-desk/backend/app/services/agent.py",
            "edits": [
                {"oldText": "class OldAgent:", "newText": "class NewAgent:"},
                {"oldText": "self.name = 'old'", "newText": "self.name = 'new'"}
            ]
        },
        # 示例3：预览模式
        {
            "file_path": "D:/OmniAgentAs-desk/frontend/src/components/Chat.tsx",
            "edits": [{"oldText": "const oldVar = 1;", "newText": "const newVar = 2;"}],
            "dryRun": True
        }
    ]
)
```

#### 示例 4: execute_shell_command 工具（不同类型参数）

```python
@register_tool(
    name="execute_shell_command",
    description="在指定 shell 环境中执行命令。Windows 原生默认 PowerShell，可选 CMD。",
    input_model=ExecuteShellCommandInput,
    examples=[
        # 示例1：只传必填参数
        {"command": "dir"},
        # 示例2：指定 shell 类型
        {"command": "ls -la", "shell_type": "powershell"},
        # 示例3：带超时设置
        {"command": "python -m pytest", "timeout": 300000},
        # 示例4：后台运行
        {"command": "npm run dev", "run_in_background": True}
    ]
)
```

#### 示例 5: search_web 工具（数组参数）

```python
@register_tool(
    name="search_web",
    description="搜索网络获取最新信息。",
    input_model=SearchWebInput,
    examples=[
        # 示例1：只传必填参数
        {"query": "OpenAI function calling"},
        # 示例2：限定搜索域名
        {"query": "React 19 新特性", "allowed_domains": ["github.com", "react.dev"]},
        # 示例3：排除广告域名
        {"query": "Python 最佳实践", "blocked_domains": ["ads.example.com", "spam.com"]}
    ]
)
```

#### 示例 6: compress_archive 工具（枚举类型参数）

```python
@register_tool(
    name="compress_archive",
    description="将文件或目录压缩为 zip 或 tar 格式。",
    input_model=CompressArchiveInput,
    examples=[
        # 示例1：只传必填参数（默认 zip 格式）
        {"source_path": "D:/OmniAgentAs-desk/backend", "output_path": "D:/backup/backend.zip"},
        # 示例2：指定压缩格式
        {"source_path": "D:/OmniAgentAs-desk/doc", "output_path": "D:/backup/doc.tar.gz", "format": "tar"}
    ]
)
```

#### 示例 7: get_current_time 工具（无必填参数）

```python
@register_tool(
    name="get_current_time",
    description="获取当前系统时间，支持多种格式输出。",
    input_model=GetCurrentTimeInput,
    examples=[
        # 示例1：不传任何参数（使用默认值）
        {},
        # 示例2：指定时区
        {"timezone": "Asia/Shanghai"},
        # 示例3：指定输出格式
        {"format": "YYYY年MM月DD日 HH:mm:ss"},
        # 示例4：同时指定时区和格式
        {"timezone": "America/New_York", "format": "YYYY-MM-DD"}
    ]
)
```

### 3.4.1 Examples 编写检查清单

编写 examples 后，请检查以下项目：

| 检查项 | 说明 |
|--------|------|
| **必填参数** | 每个示例都包含所有必填参数 |
| **参数类型** | 参数值类型与 schema 定义一致（string/integer/boolean/array） |
| **路径格式** | 路径参数使用绝对路径格式（如 D:/项目/文件） |
| **场景覆盖** | 覆盖至少 2 种不同使用场景 |
| **简洁性** | 每个示例只展示一种用法，不过于复杂 |
| **真实性** | 使用真实的路径和参数值 |

---

## 四、工具注册流程图

```
┌─────────────────────────────────────────────────────────────┐
│                      定义 Pydantic 模型                      │
│   class ListDirectoryInput(BaseModel):                     │
│       dir_path: str = Field(...)  # 必填                   │
│       recursive: bool = Field(default=False)  # 可选        │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                    @register_tool 注册                      │
│   @register_tool(                                           │
│       name="list_directory",                                │
│       description="...",                                    │
│       input_model=ListDirectoryInput,                       │
│       examples=[...]                                        │
│   )                                                         │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│              自动生成 OpenAI 格式 Schema                     │
│   get_tools_schema_for_function_calling()                  │
│   → 输出给 LLM 的 tools 参数                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、定义一个 Function 的核心事项

### 5.1 什么是"定义"

**"定义"** = 在系统中**创建并注册**一个工具，让 LLM 能够发现并调用它。

核心动作：
1. 告诉系统工具的名称
2. 告诉系统工具的用途
3. 告诉系统工具需要什么参数
4. （可选）给出使用示例

### 5.2 定义核心事项总结

| 核心事项 | 说明 | 必须？ |
|---------|------|--------|
| **name** | 工具叫什么（LLM 通过此名称调用） | ✅ 必填 |
| **description** | 工具干什么用（告诉 LLM 何时使用） | ✅ 必填 |
| **parameters** | 工具需要什么参数（类型、必填/可选、说明） | ✅ 必填 |
| **examples** | 使用示例（帮助 LLM 理解参数格式） | ❌ 可选 |

### 5.3 定义流程

```
1. 定义 Pydantic 模型
   ↓
2. 使用 @register_tool 注册
   ↓
3. 自动生成 OpenAI 格式 Schema
   ↓
4. LLM 调用时使用
```

### 5.4 核心定义工作（name 确定后）

在 **name** 确定后，**定义一个 Function 的核心工作就是 2 件事**：

| 核心工作 | 说明 | 关键点 |
|----------|------|--------|
| **1. 写准确的 description** | 告诉 LLM 何时使用这个工具 | 使用场景、用途说明 |
| **2. 写完整和准确的 parameters** | 告诉 LLM 需要传什么参数 | 类型、必填/可选、参数说明 |

### 5.5 核心工作如何实现到 2.1-2.3 步骤中

| 核心工作 | 对应步骤 | 实现方式 |
|----------|----------|----------|
| **description** | 2.1 @register_tool | 在装饰器中通过 `description` 参数手动编写 |
| **parameters** | 2.2 Pydantic模型 | 通过 Pydantic 模型类定义参数（类型、必填/可选） |
| | 2.3 自动生成 | 通过 `model_json_schema()` 自动转换为 JSON Schema |

**实现流程**：
```
编写 description → @register_tool(description="...")
                    ↓
定义 parameters → Pydantic 模型类
                    ↓
自动生成 → model_json_schema() → 最终 parameters Schema
```

---

**更新后的总结**：

> **定义一个 Function = name + 准确的 description + 完整准确的 parameters**

（其中 name 确定后，核心就是 description 和 parameters）

---

**更新时间**: 2026-04-04 06:30:00

---

## 六、一级实现10类分类Tool完整定义（10类35个Tool基本定义）

**说明**：本章按照第21章的分类，将全部35个一级实现Tool进行完整的Function定义。每个Tool的定义包含：
- **name**：工具名称
- **description**：完整准确的工具用途说明
- **parameters**：完整准确的参数定义（类型、必填/可选、参数描述）

**注**：第21章实际定义了46个Tool（1-46），本章节按序号一一对应编写完整的Function定义。

### 6.1 1类：文件操作（工具1-19）

#### Tool 1: read_text_file

**Function 定义**：
```python
{
    "name": "read_text_file",
    "description": "读取文本文件的完整内容，始终以 UTF-8 编码处理文件，支持中文等多字节字符。\n\n使用场景：\n- 当用户需要查看文本文件的内容时使用\n- 当用户想要读取配置文件、日志文件、代码文件等文本内容时使用\n- 当用户需要获取文件的前几行或后几行时使用\n\n参数说明：\n- file_path：文件的完整路径，必须是绝对路径，支持中文路径（如 D:/文档/测试.txt）\n- head：读取文件的前 N 行，不能与 tail 参数同时使用\n- tail：读取文件的后 N 行，不能与 head 参数同时使用\n\n【重要】返回文件的所有内容或指定行内容",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "文件的完整路径，必须是绝对路径，支持中文路径（如 D:/文档/测试.txt）"
            },
            "head": {
                "type": "integer",
                "description": "读取文件的前 N 行，不能与 tail 参数同时使用。用于只查看文件开头部分内容"
            },
            "tail": {
                "type": "integer",
                "description": "读取文件的后 N 行，不能与 head 参数同时使用。用于查看文件末尾部分内容，如日志文件的最新记录"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 2: read_media_file

**Function 定义**：
```python
{
    "name": "read_media_file",
    "description": "读取图片或音频文件，返回 Base64 编码的数据和对应的 MIME 类型。\n\n使用场景：\n- 当用户需要获取图片或音频文件的内容时使用\n- 当用户想要将媒体文件转换为 Base64 字符串以便传输或嵌入时使用\n- 当用户需要查看媒体文件的 MIME 类型时使用\n\n参数说明：\n- file_path：媒体文件的完整路径，必须是绝对路径\n\n【重要】返回 Base64 编码的媒体数据和 MIME 类型，适用于图片（JPG、PNG、GIF等）和音频（MP3、WAV等）文件",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "媒体文件的完整路径，必须是绝对路径，支持图片（JPG、PNG、GIF、BMP、WebP）和音频（MP3、WAV、OGG、M4A）格式"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 3: read_batch_file

**Function 定义**：
```python
{
    "name": "read_batch_file",
    "description": "同时读取多个文件的内容，单个文件读取失败不会中断整个操作。\n\n使用场景：\n- 当用户需要同时读取多个文件进行分析或对比时使用\n- 当用户想要批量获取多个配置文件内容时使用\n- 当用户需要快速了解多个相关文件的内容时使用\n\n参数说明：\n- file_paths：文件路径数组，每个元素必须是文件的完整绝对路径\n\n【重要】返回每个文件的路径及其对应的内容，读取失败的文件会返回错误信息但不影响其他文件",
    "parameters": {
        "type": "object",
        "properties": {
            "file_paths": {
                "type": "array",
                "items": {"type": "string"},
                "description": "文件路径数组，每个元素必须是文件的完整绝对路径，支持中文路径。数组长度建议不超过100个文件"
            }
        },
        "required": ["file_paths"]
    }
}
```

---

#### Tool 4: read_file

**Function 定义**：
```python
{
    "name": "read_file",
    "description": "从文件系统读取文件，支持文本、图片、PDF、Jupyter notebook，自动处理 UTF-8/GBK 编码。\n\n使用场景：\n- 当用户需要读取任意类型文件的内容时使用\n- 当用户想要查看代码文件、配置文件、文档文件时使用\n- 当用户需要按行读取大文件的部分内容时使用\n- 当用户想要读取图片、PDF 等非文本文件时使用\n\n参数说明：\n- file_path：文件的绝对路径，支持中文路径（如 D:/项目/代码/main.py）\n- offset：起始行号，从1开始，用于大文件的部分读取\n- limit：读取的行数，默认2000行，用于限制单次读取的数据量\n\n【重要】自动检测文件编码（UTF-8或GBK），支持文本文件和二进制文件的读取",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "文件的绝对路径，支持中文路径（如 D:/项目/代码/main.py）"
            },
            "offset": {
                "type": "integer",
                "description": "起始行号，从1开始。用于大文件的部分读取，从指定行开始读取"
            },
            "limit": {
                "type": "integer",
                "description": "读取的行数，默认2000行。用于限制单次读取的数据量，避免一次性读取过大文件"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 5: write_append_file

**Function 定义**：
```python
{
    "name": "write_append_file",
    "description": "写入或追加到文件，使用 UTF-8 编码，支持中文内容写入。\n\n使用场景：\n- 当用户需要创建新文件并写入内容时使用\n- 当用户需要在已有文件末尾追加内容时使用\n- 当用户需要保存文本内容到文件时使用\n- 当用户需要写入配置文件、日志内容时使用\n\n参数说明：\n- file_path：文件路径，如果是相对路径则基于当前工作目录\n- text：写入的文本内容，支持中文\n- append：是否以追加模式写入，默认为 false。设置为 true 时在文件末尾追加，设置为 false 时覆盖原文件\n\n【重要】文件不存在时会自动创建，追加模式会保留原文件内容",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "文件路径，如果是相对路径则基于当前工作目录。支持中文路径（如 D:/文档/输出.txt）"
            },
            "text": {
                "type": "string",
                "description": "写入的文本内容，支持中文。支持任意文本内容，包括换行符（\\n）"
            },
            "append": {
                "type": "boolean",
                "default": false,
                "description": "是否以追加模式写入。设置为 true 时在文件末尾追加内容，设置为 false 时覆盖原文件内容。默认值为 false"
            }
        },
        "required": ["file_path", "text"]
    }
}
```

---

#### Tool 6: precise_replace_in_file

**Function 定义**：
```python
{
    "name": "precise_replace_in_file",
    "description": "执行精确的字符串替换，支持中文内容精确匹配和替换。\n\n使用场景：\n- 当用户需要精确替换文件中特定的字符串时使用\n- 当用户需要对代码进行精确修改时使用\n- 当用户需要在文件中替换特定的文本内容时使用\n\n参数说明：\n- file_path：文件的绝对路径，支持中文路径\n- old_string：要替换的精确文本，支持中文。必须是文件中确实存在的文本\n- new_string：替换后的文本，支持中文\n- replace_all：是否替换所有匹配项，默认为 false。设置为 true 时替换所有匹配项，设置为 false 时只替换第一个匹配项\n\n【重要】执行精确字符串匹配替换，不是正则表达式替换",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "文件的绝对路径，支持中文路径（如 D:/项目/代码/main.py）"
            },
            "old_string": {
                "type": "string",
                "description": "要替换的精确文本，支持中文。必须是文件中确实存在的文本，进行精确匹配（非正则表达式）"
            },
            "new_string": {
                "type": "string",
                "description": "替换后的文本，支持中文。用于替换 old_string 的内容"
            },
            "replace_all": {
                "type": "boolean",
                "default": false,
                "description": "是否替换所有匹配项。设置为 true 时替换文件中所有匹配的 old_string，设置为 false 时只替换第一个匹配项。默认值为 false"
            }
        },
        "required": ["file_path", "old_string", "new_string"]
    }
}
```

---

#### Tool 7: edit_file

**Function 定义**：
```python
{
    "name": "edit_file",
    "description": "使用高级模式匹配进行选择性编辑，支持多同时编辑、缩进保留、dryRun 预览。\n\n使用场景：\n- 当用户需要同时对文件进行多处编辑时使用\n- 当用户想要先预览修改效果再实际执行时使用\n- 当用户需要对代码文件进行批量修改时使用\n\n参数说明：\n- file_path：要编辑的文件路径\n- edits：编辑操作数组，每个元素包含 oldText（要替换的文本）和 newText（替换后的文本），支持同时执行多个编辑操作\n- dryRun：预览模式，设置为 true 时不实际修改文件，只返回修改后的内容预览。默认值为 false\n\n【重要】支持一次性执行多个编辑操作，自动保留原文件的缩进格式",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "要编辑的文件路径，支持中文路径"
            },
            "edits": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "oldText": {
                            "type": "string",
                            "description": "要搜索并替换的文本，支持中文"
                        },
                        "newText": {
                            "type": "string",
                            "description": "替换后的文本，支持中文"
                        }
                    },
                    "required": ["oldText", "newText"]
                },
                "description": "编辑操作数组，每个元素包含 oldText（要替换的文本）和 newText（替换后的文本），支持同时执行多个编辑操作"
            },
            "dryRun": {
                "type": "boolean",
                "default": false,
                "description": "预览模式。设置为 true 时只返回修改后的内容预览，不实际修改文件；设置为 false 时执行实际修改。默认值为 false"
            }
        },
        "required": ["file_path", "edits"]
    }
}
```

---

#### Tool 8: copy_file

**Function 定义**：
```python
{
    "name": "copy_file",
    "description": "复制文件到指定位置。\n\n使用场景：\n- 当用户需要复制文件到另一个位置时使用\n- 当用户想要备份文件时使用\n- 当用户需要创建文件的副本时使用\n\n参数说明：\n- source_path：源文件路径，即要复制的文件\n- destination_path：目标文件路径，即复制后的文件保存位置\n\n【重要】如果目标路径已存在文件，会覆盖原文件",
    "parameters": {
        "type": "object",
        "properties": {
            "source_path": {
                "type": "string",
                "description": "源文件路径，即要复制的文件。必须是已存在的文件路径"
            },
            "destination_path": {
                "type": "string",
                "description": "目标文件路径，即复制后的文件保存位置。如果目标目录不存在会自动创建"
            }
        },
        "required": ["source_path", "destination_path"]
    }
}
```

---

#### Tool 9: move_file

**Function 定义**：
```python
{
    "name": "move_file",
    "description": "移动或重命名文件。\n\n使用场景：\n- 当用户需要将文件移动到另一个目录时使用\n- 当用户想要重命名文件时使用\n- 当用户需要整理文件结构时使用\n\n参数说明：\n- source_path：源文件路径，即要移动的文件当前路径\n- destination_path：目标文件路径，即文件移动后的新路径\n\n【重要】如果目标位置已存在同名文件，操作会失败",
    "parameters": {
        "type": "object",
        "properties": {
            "source_path": {
                "type": "string",
                "description": "源文件路径，即要移动的文件当前路径。必须是已存在的文件"
            },
            "destination_path": {
                "type": "string",
                "description": "目标文件路径，即文件移动后的新路径。如果目标目录不存在会自动创建"
            }
        },
        "required": ["source_path", "destination_path"]
    }
}
```

---

#### Tool 10: rename_file

**Function 定义**：
```python
{
    "name": "rename_file",
    "description": "重命名文件或目录，不改变所在目录。\n\n使用场景：\n- 当用户需要重命名文件时使用\n- 当用户想要重命名目录时使用\n- 当用户需要修改文件名但保持文件位置不变时使用\n\n参数说明：\n- file_path：当前文件或目录的路径\n- new_name：新的文件名或目录名\n\n【重要】只改变名称，不改变所在目录。如果新名称与同目录已有文件重名，操作会失败",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "当前文件或目录的路径。必须是已存在的文件或目录"
            },
            "new_name": {
                "type": "string",
                "description": "新的文件名或目录名。不能包含路径分隔符，只输入文件名或目录名"
            }
        },
        "required": ["file_path", "new_name"]
    }
}
```

---

#### Tool 11: delete_file

**Function 定义**：
```python
{
    "name": "delete_file",
    "description": "删除文件（永久删除，不进入回收站）。\n\n使用场景：\n- 当用户需要永久删除文件时使用\n- 当用户需要清理临时文件时使用\n- 当用户确认文件不需要后进行删除时使用\n\n参数说明：\n- file_path：要删除的文件路径\n\n【重要】此操作是永久删除，文件不会进入回收站，无法恢复。请谨慎使用，建议先备份",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "要删除的文件路径。必须是已存在的文件。删除后文件永久丢失，无法恢复"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 12: search_files

**Function 定义**：
```python
{
    "name": "search_files",
    "description": "递归搜索匹配或排除模式的文件/目录，返回完整路径，支持中文文件名搜索。\n\n使用场景：\n- 当用户需要在目录中搜索特定名称的文件时使用\n- 当用户想要查找符合条件的文件时使用\n- 当用户需要对文件进行批量操作前需要找到目标文件时使用\n\n参数说明：\n- search_dir：搜索的起始目录，支持中文目录名\n- pattern：搜索模式，支持 glob 风格通配符。支持中文搜索（如 \"*.txt\" 或 \"测试*\"）\n- excludePatterns：排除模式数组，符合排除模式的文件或目录不会被包含在结果中\n\n【重要】递归搜索所有子目录，返回所有匹配的文件完整路径",
    "parameters": {
        "type": "object",
        "properties": {
            "search_dir": {
                "type": "string",
                "description": "搜索的起始目录，必须是绝对路径，支持中文目录名（如 D:/项目/源码）"
            },
            "pattern": {
                "type": "string",
                "description": "搜索模式，支持 glob 风格通配符。支持中文文件名搜索。常用模式：\"*.txt\"（所有txt文件）、\"测试*\"（以测试开头的文件）、\"**/*.py\"（所有Python文件）"
            },
            "excludePatterns": {
                "type": "array",
                "items": {"type": "string"},
                "description": "排除模式数组，符合排除模式的文件或目录不会被包含在结果中。格式同 pattern，如 [\"node_modules\", \"*.log\"]"
            }
        },
        "required": ["search_dir", "pattern"]
    }
}
```

---

#### Tool 13: glob_files

**Function 定义**：
```python
{
    "name": "glob_files",
    "description": "快速的文件名模式匹配，按修改时间排序返回结果，支持中文文件名。\n\n使用场景：\n- 当用户需要使用 glob 模式快速匹配文件时使用\n- 当用户想要获取特定类型的文件列表时使用\n- 当用户需要按修改时间排序获取最新文件时使用\n\n参数说明：\n- pattern：Glob 模式，用于匹配文件名。如 \"**/*.js\" 匹配所有 JS 文件，\"src/**/*.ts\" 匹配 src 目录下所有 TS 文件\n- search_dir：搜索目录，默认当前工作目录\n\n【重要】按文件修改时间排序返回结果，最新修改的文件排在前面",
    "parameters": {
        "type": "object",
        "properties": {
            "pattern": {
                "type": "string",
                "description": "Glob 模式，用于匹配文件名。如 \"**/*.js\" 匹配所有 JS 文件，\"src/**/*.ts\" 匹配 src 目录下所有 TS 文件，\"*.py\" 匹配当前目录所有 Python 文件"
            },
            "search_dir": {
                "type": "string",
                "description": "搜索目录，默认为当前工作目录。必须是绝对路径，支持中文目录名"
            }
        },
        "required": ["pattern"]
    }
}
```

---

#### Tool 14: grep_file_content

**Function 定义**：
```python
{
    "name": "grep_file_content",
    "description": "基于 ripgrep 的强大内容搜索，支持正则表达式和多选项，支持 Unicode 中文字符搜索。\n\n使用场景：\n- 当用户需要在文件中搜索特定内容时使用\n- 当用户想要查找包含特定关键词的代码行时使用\n- 当用户需要对多个文件进行内容搜索时使用\n\n参数说明：\n- pattern：正则表达式搜索模式，支持中文（如搜索\"函数定义\"或\"class.*方法\"）\n- search_dir：搜索路径，默认为当前目录\n- output_mode：输出模式。content-显示匹配行的内容；files_with_matches-只显示包含匹配的文件名；count-显示每个文件的匹配数量\n- glob：文件类型过滤，使用 glob 通配符（如 \"*.ts\" 或 \"*.{js,py}\"）\n- type：语言类型，简化 glob 匹配（如 js, py, rust, html, json）\n- after_lines：匹配行之后额外显示的行数，用于查看后续上下文\n- before_lines：匹配行之前额外显示的行数，用于查看前面上下文\n- context_lines：匹配行前后各显示的行数，同时设置 before 和 after\n- ignore_case：搜索时忽略大小写，例如 \"test\" 会匹配 \"Test\" 和 \"TEST\"\n- show_line_no：是否在输出中显示行号，便于定位\n- multiline：启用多行匹配模式，允许正则表达式中的 . 匹配换行符\n- head_limit：限制返回的匹配结果数量，用于大文件搜索避免输出过多\n\n【重要】支持强大的正则表达式搜索，可以精确定位代码中的内容",
    "parameters": {
        "type": "object",
        "properties": {
            "pattern": {
                "type": "string",
                "description": "正则表达式搜索模式，支持中文内容搜索。常用示例：搜索\"函数定义\"或\"class.*方法\""
            },
            "search_dir": {
                "type": "string",
                "description": "搜索路径，默认当前目录。必须是绝对路径，支持中文目录名"
            },
            "output_mode": {
                "type": "string",
                "description": "输出模式。可选值：content（显示匹配行的内容）、files_with_matches（只显示包含匹配的文件名）、count（显示每个文件的匹配数量）"
            },
            "glob": {
                "type": "string",
                "description": "文件类型过滤，使用 glob 通配符。例如：\"*.ts\" 只搜索 TS 文件，\"*.{js,py}\" 搜索 JS 和 Python 文件"
            },
            "type": {
                "type": "string",
                "description": "语言类型，简化 glob 匹配。常用值：js（JavaScript）、py（Python）、rust、html、json 等"
            },
            "after_lines": {
                "type": "integer",
                "description": "匹配行之后额外显示的行数，用于查看后续上下文"
            },
            "before_lines": {
                "type": "integer",
                "description": "匹配行之前额外显示的行数，用于查看前面上下文"
            },
            "context_lines": {
                "type": "integer",
                "description": "匹配行前后各显示的行数，同时设置 before 和 after，用于查看完整上下文"
            },
            "ignore_case": {
                "type": "boolean",
                "description": "搜索时是否忽略大小写。设置为 true 时，\"test\" 会匹配 \"Test\" 和 \"TEST\"。默认 false"
            },
            "show_line_no": {
                "type": "boolean",
                "description": "是否在输出中显示行号，便于定位。默认 false"
            },
            "multiline": {
                "type": "boolean",
                "description": "启用多行匹配模式，允许正则表达式中的 . 匹配换行符。默认 false"
            },
            "head_limit": {
                "type": "integer",
                "description": "限制返回的匹配结果数量，用于大文件搜索避免输出过多"
            }
        },
        "required": ["pattern"]
    }
}
```

---

#### Tool 15: create_directory

**Function 定义**：
```python
{
    "name": "create_directory",
    "description": "创建新目录，如需要会创建父目录，目录已存在则静默成功。\n\n使用场景：\n- 当用户需要创建新目录时使用\n- 当用户需要创建多级目录时使用\n- 当用户需要在指定位置创建文件夹时使用\n\n参数说明：\n- dir_path：要创建的目录路径\n\n【重要】如果父目录不存在会自动创建，目录已存在时不会报错（静默成功）",
    "parameters": {
        "type": "object",
        "properties": {
            "dir_path": {
                "type": "string",
                "description": "要创建的目录路径，支持多级目录创建。如果父目录不存在会自动创建"
            }
        },
        "required": ["dir_path"]
    }
}
```

---

#### Tool 16: list_directory_with_sizes

**Function 定义**：
```python
{
    "name": "list_directory_with_sizes",
    "description": "列出目录内容，包含文件大小，按 name 或 size 排序，返回统计信息。\n\n使用场景：\n- 当用户需要查看目录内容及文件大小时使用\n- 当用户想要了解文件夹中哪些文件占用空间较大时使用\n- 当用户需要对文件进行排序查看时使用\n\n参数说明：\n- dir_path：目录路径\n- sortBy：排序方式。name-按名称排序；size-按文件大小排序。默认 name\n\n【重要】返回目录中每个文件/目录的名称、大小、类型，以及统计信息（文件数量、总大小等）",
    "parameters": {
        "type": "object",
        "properties": {
            "dir_path": {
                "type": "string",
                "description": "目录路径，必须是绝对路径，支持中文目录名"
            },
            "sortBy": {
                "type": "string",
                "description": "排序方式。可选值：name（按名称字母排序，默认值）、size（按文件大小排序，从大到小）"
            }
        },
        "required": ["dir_path"]
    }
}
```

---

#### Tool 17: get_directory_tree

**Function 定义**：
```python
{
    "name": "get_directory_tree",
    "description": "获取目录的递归 JSON 树结构，每个条目包含 name、type（file/directory）、children。\n\n使用场景：\n- 当用户需要查看完整目录树结构时使用\n- 当用户想要了解项目的整体文件结构时使用\n- 当用户需要生成目录树视图时使用\n\n参数说明：\n- dir_path：起始目录\n- excludePatterns：排除模式数组，符合排除模式的目录不会包含在结果中（glob 格式）\n\n【重要】返回 JSON 格式的目录树结构，包含每个文件/目录的名称、类型、子目录（仅目录有 children 字段）",
    "parameters": {
        "type": "object",
        "properties": {
            "dir_path": {
                "type": "string",
                "description": "起始目录，必须是绝对路径，支持中文目录名"
            },
            "excludePatterns": {
                "type": "array",
                "items": {"type": "string"},
                "description": "排除模式数组，符合排除模式的目录不会包含在结果中。格式为 glob 通配符，如 [\"node_modules\", \"__pycache__\", \"*.pyc\"]"
            }
        },
        "required": ["dir_path"]
    }
}
```

---

#### Tool 18: get_file_info

**Function 定义**：
```python
{
    "name": "get_file_info",
    "description": "获取文件/目录的详细元数据，包括大小、创建/修改/访问时间、类型、权限。\n\n使用场景：\n- 当用户需要获取文件的详细信息时使用\n- 当用户想要了解文件的大小、修改时间等元数据时使用\n- 当用户需要检查文件类型和权限时使用\n\n参数说明：\n- file_path：文件或目录路径\n\n【重要】返回文件的完整元数据信息，包括：文件大小（字节）、创建时间、修改时间、访问时间、文件类型、权限标志",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "文件或目录路径，必须是绝对路径，支持中文路径"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 19: list_allowed_directories

**Function 定义**：
```python
{
    "name": "list_allowed_directories",
    "description": "列出服务器允许访问的所有目录。\n\n使用场景：\n- 当用户需要确认可以访问哪些目录时使用\n- 当用户想要了解系统允许的文件操作范围时使用\n- 当用户需要检查是否有权限访问特定目录时使用\n\n参数说明：\n- 无参数\n\n【重要】返回服务器配置中允许访问的目录列表，用于确定文件操作的边界",
    "parameters": {
        "type": "object",
        "properties": {},
        "required": []
    }
}
```

---

### 6.2 2类：Shell 命令执行（工具20-22）

#### Tool 20: execute_shell_command

**Function 定义**：
```python
{
    "name": "execute_shell_command",
    "description": "在指定 shell 环境中执行命令。Windows 原生默认 PowerShell，可选 CMD；bash 需额外安装（未来扩展）。\n\n使用场景：\n- 当用户需要执行系统命令时使用\n- 当用户想要运行命令行工具时使用\n- 当用户需要执行脚本或程序时使用\n\n参数说明：\n- command：要执行的命令\n- shell_type：执行环境。powershell（默认）-Windows PowerShell；cmd-Windows 命令提示符；bash-Linux Bash（未来扩展）\n- timeout：超时毫秒数，默认120000（2分钟），最大600000（10分钟）\n- run_in_background：是否在后台运行命令\n\n【重要】返回命令的 stdout、stderr 和退出码",
    "parameters": {
        "type": "object",
        "properties": {
            "command": {
                "type": "string",
                "description": "要执行的命令。如 \"dir\"、\"ls -la\"、\"python script.py\" 等"
            },
            "shell_type": {
                "type": "string",
                "description": "执行环境。可选值：powershell（Windows PowerShell，默认值）、cmd（Windows 命令提示符）、bash（Linux Bash，未来扩展）"
            },
            "timeout": {
                "type": "integer",
                "description": "超时毫秒数。默认120000（2分钟），最大600000（10分钟）。命令执行超过此时间会被强制终止"
            },
            "run_in_background": {
                "type": "boolean",
                "description": "是否在后台运行命令。设置为 true 时命令在后台异步执行，返回 shell_id 用于后续获取输出"
            }
        },
        "required": ["command"]
    }
}
```

---

#### Tool 21: get_shell_output

**Function 定义**：
```python
{
    "name": "get_shell_output",
    "description": "获取后台运行的 shell 命令输出。\n\n使用场景：\n- 当用户需要获取后台命令的执行结果时使用\n- 当用户想要检查后台命令是否完成时使用\n- 当用户需要分批获取长命令输出时使用\n\n参数说明：\n- shell_id：后台 shell 的 ID，由 execute_shell_command 的 run_in_background=true 时返回\n- filter：过滤输出的正则表达式（可选）\n\n【重要】返回 shell 命令的 stdout 和 stderr 输出",
    "parameters": {
        "type": "object",
        "properties": {
            "shell_id": {
                "type": "string",
                "description": "后台 shell 的 ID，由 execute_shell_command 的 run_in_background=true 时返回"
            },
            "filter": {
                "type": "string",
                "description": "过滤输出的正则表达式（可选）。只返回匹配正则表达式的输出行"
            }
        },
        "required": ["shell_id"]
    }
}
```

---

#### Tool 22: terminate_shell

**Function 定义**：
```python
{
    "name": "terminate_shell",
    "description": "终止运行中的后台 shell 会话。\n\n使用场景：\n- 当用户需要终止正在运行的后台命令时使用\n- 当用户想要停止长时间运行的命令时使用\n- 当用户需要清理后台进程时使用\n\n参数说明：\n- shell_id：要终止的 shell ID\n\n【重要】强制终止后台进程，会丢失未读取的输出",
    "parameters": {
        "type": "object",
        "properties": {
            "shell_id": {
                "type": "string",
                "description": "要终止的 shell ID。通过 execute_shell_command 的 run_in_background=true 执行命令后获得"
            }
        },
        "required": ["shell_id"]
    }
}
```

---

### 6.3 3类：API/HTTP 调用（工具25）

#### Tool 25: http_request

**Function 定义**：
```python
{
    "name": "http_request",
    "description": "发送 HTTP 请求到指定的 URL，支持 GET、POST、PUT、DELETE、PATCH 等方法。\n\n使用场景：\n- 当用户需要调用 REST API 时使用\n- 当用户想要发送 HTTP 请求获取数据或提交数据时使用\n- 当用户需要进行网络请求时使用\n\n参数说明：\n- url：请求的目标 URL，必须是完全有效的 URL（如 https://api.example.com/data）\n- method：HTTP 方法。可选值：GET、POST、PUT、DELETE、PATCH\n- headers：请求头对象（可选），如 {\"Content-Type\": \"application/json\", \"Authorization\": \"Bearer token\"}\n- body：请求体（可选），用于 POST、PUT、PATCH 方法，格式为字符串\n- timeout：超时毫秒数，默认30000（30秒）\n\n【重要】返回响应的状态码、响应头和响应体",
    "parameters": {
        "type": "object",
        "properties": {
            "url": {
                "type": "string",
                "description": "请求的目标 URL，必须是完全有效的 URL（如 https://api.example.com/data）"
            },
            "method": {
                "type": "string",
                "description": "HTTP 方法。可选值：GET（获取数据）、POST（提交数据）、PUT（更新数据）、DELETE（删除数据）、PATCH（部分更新）"
            },
            "headers": {
                "type": "object",
                "description": "请求头对象（可选）。常用键：Content-Type（如 application/json）、Authorization（如 Bearer token）、Accept 等。示例：{\"Content-Type\": \"application/json\"}"
            },
            "body": {
                "type": "string",
                "description": "请求体（可选），用于 POST、PUT、PATCH 方法。格式为字符串，如 JSON 字符串或表单数据"
            },
            "timeout": {
                "type": "integer",
                "description": "超时毫秒数，默认30000（30秒）。请求超过此时间未响应则失败"
            }
        },
        "required": ["url", "method"]
    }
}
```

---

### 6.4 4类：网络通信（工具23-24）

#### Tool 23: fetch_webpage

**Function 定义**：
```python
{
    "name": "fetch_webpage",
    "description": "获取和处理网页内容，带 AI 分析功能。\n\n使用场景：\n- 当用户需要获取网页内容时使用\n- 当用户想要从网页中提取特定信息时使用\n- 当用户需要分析网页内容时使用\n\n参数说明：\n- url：完全有效的 URL（如 https://example.com/page）\n- prompt：要从中提取的信息描述，即告诉 AI 需要从页面提取什么信息\n\n【重要】返回网页的文本内容和 AI 分析结果",
    "parameters": {
        "type": "object",
        "properties": {
            "url": {
                "type": "string",
                "description": "完全有效的 URL（如 https://example.com/page）。必须是可访问的网页地址"
            },
            "prompt": {
                "type": "string",
                "description": "要从中提取的信息描述。告诉 AI 需要从页面提取什么信息，如 \"提取文章标题和正文\"、\"获取所有图片链接\" 等"
            }
        },
        "required": ["url", "prompt"]
    }
}
```

---

#### Tool 24: search_web

**Function 定义**：
```python
{
    "name": "search_web",
    "description": "搜索网络获取最新信息（注：仅美国地区可用）。\n\n使用场景：\n- 当用户需要搜索网络获取最新信息时使用\n- 当用户想要查询实时数据或新闻时使用\n- 当用户需要获取网上最新的技术文档时使用\n\n参数说明：\n- query：搜索查询字符串，至少2个字符\n- allowed_domains：包含的域名数组（可选），限制搜索结果来自指定域名\n- blocked_domains：排除的域名数组（可选），排除指定域名的搜索结果\n\n【重要】返回搜索结果列表，包含标题、URL 和摘要",
    "parameters": {
        "type": "object",
        "properties": {
            "query": {
                "type": "string",
                "description": "搜索查询字符串，至少2个字符。支持中英文搜索关键词"
            },
            "allowed_domains": {
                "type": "array",
                "items": {"type": "string"},
                "description": "包含的域名数组（可选）。限制搜索结果只来自指定域名，如 [\"github.com\", \"stackoverflow.com\"]"
            },
            "blocked_domains": {
                "type": "array",
                "items": {"type": "string"},
                "description": "排除的域名数组（可选）。排除指定域名的搜索结果，如 [\"ads.example.com\"]"
            }
        },
        "required": ["query"]
    }
}
```

---

### 6.5 5类：时间/日期（工具26-27）

#### Tool 26: get_current_time

**Function 定义**：
```python
{
    "name": "get_current_time",
    "description": "获取当前系统时间，支持多种格式输出。\n\n使用场景：\n- 当用户需要获取当前时间时使用\n- 当用户想要以特定格式显示时间时使用\n- 当用户需要进行时间相关的计算时使用\n\n参数说明：\n- timezone：时区（可选），如 \"Asia/Shanghai\"、\"UTC\"、\"America/New_York\"。默认本地时区\n- format：输出格式（可选），如 \"YYYY-MM-DD HH:mm:ss\"。默认 \"YYYY-MM-DD HH:mm:ss\"\n\n【重要】返回格式化后的当前时间字符串",
    "parameters": {
        "type": "object",
        "properties": {
            "timezone": {
                "type": "string",
                "description": "时区（可选）。如 \"Asia/Shanghai\"（中国时区）、\"UTC\"（世界标准时间）、\"America/New_York\"（美国东部时间）。默认使用本地时区"
            },
            "format": {
                "type": "string",
                "description": "输出格式（可选）。常用格式：\"YYYY-MM-DD HH:mm:ss\"（默认，格式如 2024-01-01 12:30:45）、\"YYYY-MM-DD\"（仅日期）、\"HH:mm:ss\"（仅时间）"
            }
        },
        "required": []
    }
}
```

---

#### Tool 27: calculate_date

**Function 定义**：
```python
{
    "name": "calculate_date",
    "description": "计算指定日期偏移后的日期。\n\n使用场景：\n- 当用户需要计算几天后的日期时使用\n- 当用户想要获取几天前的日期时使用\n- 当用户需要进行日期计算时使用\n\n参数说明：\n- date：起始日期，格式如 \"2024-01-01\"\n- days：偏移天数。正数为未来（几天后），负数为过去（几天前）\n- format：输出格式（可选），默认 \"YYYY-MM-DD\"\n\n【重要】返回计算后的日期",
    "parameters": {
        "type": "object",
        "properties": {
            "date": {
                "type": "string",
                "description": "起始日期，格式如 \"2024-01-01\"。支持 YYYY-MM-DD 格式"
            },
            "days": {
                "type": "integer",
                "description": "偏移天数。正数为未来（几天后），负数为过去（几天前）。如输入 7 返回7天后的日期，输入 -7 返回7天前的日期"
            },
            "format": {
                "type": "string",
                "description": "输出格式（可选），默认 \"YYYY-MM-DD\"。支持自定义格式如 \"YYYY年MM月DD日\""
            }
        },
        "required": ["date"]
    }
}
```

---

### 6.6 6类：环境变量（工具28-29）

#### Tool 28: get_env

**Function 定义**：
```python
{
    "name": "get_env",
    "description": "获取指定的环境变量值。\n\n使用场景：\n- 当用户需要获取环境变量时使用\n- 当用户想要读取系统配置时使用\n- 当用户需要获取 PATH 等系统环境变量时使用\n\n参数说明：\n- name：环境变量名称\n- default：默认值（可选），如果环境变量不存在则返回此默认值\n\n【重要】返回环境变量的值，如果不存在且提供了 default 则返回 default，否则返回 None",
    "parameters": {
        "type": "object",
        "properties": {
            "name": {
                "type": "string",
                "description": "环境变量名称。如 \"PATH\"、\"HOME\"、\"USER\"、\"JAVA_HOME\" 等"
            },
            "default": {
                "type": "string",
                "description": "默认值（可选）。如果指定的环境变量不存在，则返回此默认值"
            }
        },
        "required": ["name"]
    }
}
```

---

#### Tool 29: set_env

**Function 定义**：
```python
{
    "name": "set_env",
    "description": "设置指定的环境变量值。\n\n使用场景：\n- 当用户需要设置环境变量时使用\n- 当用户想要配置系统环境时使用\n- 当用户需要临时设置运行参数时使用\n\n参数说明：\n- name：环境变量名称\n- value：环境变量值\n\n【重要】设置环境变量。注意：此设置只对当前进程及其子进程有效，不会影响系统级环境变量",
    "parameters": {
        "type": "object",
        "properties": {
            "name": {
                "type": "string",
                "description": "环境变量名称。如 \"MY_VARIABLE\"、\"CONFIG_PATH\" 等"
            },
            "value": {
                "type": "string",
                "description": "环境变量值。任意字符串值"
            }
        },
        "required": ["name", "value"]
    }
}
```

---

### 6.7 7类：系统信息（工具30）

#### Tool 30: get_system_info

**Function 定义**：
```python
{
    "name": "get_system_info",
    "description": "获取系统基本信息，包括操作系统、版本、架构等。\n\n使用场景：\n- 当用户需要获取系统信息时使用\n- 当用户想要了解当前运行环境时使用\n- 当用户需要进行系统诊断时使用\n\n参数说明：\n- info_type：信息类型（可选）。all-获取所有信息；cpu-仅 CPU 信息；memory-仅内存信息；disk-仅磁盘信息。默认 all\n\n【重要】返回系统的完整信息",
    "parameters": {
        "type": "object",
        "properties": {
            "info_type": {
                "type": "string",
                "description": "信息类型（可选）。可选值：all（获取所有信息，默认值）、cpu（仅 CPU 信息）、memory（仅内存信息）、disk（仅磁盘信息）"
            }
        },
        "required": []
    }
}
```

---

### 6.8 8类：网络连接（工具31-32）

#### Tool 31: net_connections

**Function 定义**：
```python
{
    "name": "net_connections",
    "description": "获取当前网络连接列表。\n\n使用场景：\n- 当用户需要查看网络连接时使用\n- 当用户想要了解系统网络状态时使用\n- 当用户需要进行网络诊断时使用\n\n参数说明：\n- kind：连接类型（可选）。inet-网络连接；tcp-TCP 连接；udp-UDP 连接。默认 inet\n\n【重要】返回当前的网络连接列表",
    "parameters": {
        "type": "object",
        "properties": {
            "kind": {
                "type": "string",
                "description": "连接类型（可选）。可选值：inet（网络连接，默认值）、tcp（TCP 连接）、udp（UDP 连接）"
            }
        },
        "required": []
    }
}
```

---

#### Tool 32: event_log

**Function 定义**：
```python
{
    "name": "event_log",
    "description": "获取系统事件日志。\n\n使用场景：\n- 当用户需要查看系统日志时使用\n- 当用户想要进行系统诊断时使用\n- 当用户需要排查系统问题时使用\n\n参数说明：\n- log_name：日志名称（可选），如 System（系统日志）、Application（应用程序日志）。默认 System\n- max_events：最大返回事件数（可选），默认100\n\n【重要】返回系统事件日志条目",
    "parameters": {
        "type": "object",
        "properties": {
            "log_name": {
                "type": "string",
                "description": "日志名称（可选）。常用值：System（系统日志）、Application（应用程序日志）、Security（安全日志）。默认 System"
            },
            "max_events": {
                "type": "integer",
                "description": "最大返回事件数（可选），默认100。用于限制返回的日志条目数量，避免输出过多"
            }
        },
        "required": []
    }
}
```

---

### 6.9 9类：压缩/解压（工具33-34）

#### Tool 33: compress_archive

**Function 定义**：
```python
{
    "name": "compress_archive",
    "description": "将文件或目录压缩为 zip 或 tar 格式。\n\n使用场景：\n- 当用户需要压缩文件或目录时使用\n- 当用户想要创建备份归档时使用\n- 当用户需要打包文件进行传输时使用\n\n参数说明：\n- source_path：要压缩的文件或目录路径\n- output_path：输出压缩文件路径\n- format：压缩格式（可选）。zip-ZIP 格式（默认值）；tar-TAR 格式；gz-GZIP 压缩\n\n【重要】返回压缩结果信息",
    "parameters": {
        "type": "object",
        "properties": {
            "source_path": {
                "type": "string",
                "description": "要压缩的文件或目录路径。必须是已存在的文件或目录"
            },
            "output_path": {
                "type": "string",
                "description": "输出压缩文件路径。指定压缩文件的保存位置"
            },
            "format": {
                "type": "string",
                "description": "压缩格式（可选）。可选值：zip（ZIP 格式，默认值）、tar（TAR 格式）、gz（GZIP 压缩）"
            }
        },
        "required": ["source_path", "output_path"]
    }
}
```

---

#### Tool 34: extract_archive

**Function 定义**：
```python
{
    "name": "extract_archive",
    "description": "解压 zip、tar、gz 等格式的压缩文件。\n\n使用场景：\n- 当用户需要解压压缩文件时使用\n- 当用户想要解包归档文件时使用\n- 当用户需要访问压缩文件内的内容时使用\n\n参数说明：\n- archive_path：压缩文件路径\n- output_dir：解压目标目录（可选），默认当前目录\n\n【重要】自动识别压缩格式并解压",
    "parameters": {
        "type": "object",
        "properties": {
            "archive_path": {
                "type": "string",
                "description": "压缩文件路径。必须是已存在的压缩文件，支持 zip、tar、gz 格式"
            },
            "output_dir": {
                "type": "string",
                "description": "解压目标目录（可选），默认当前目录。指定解压后文件的存放位置"
            }
        },
        "required": ["archive_path"]
    }
}
```

---

### 6.10 10类：文件哈希（工具35）

#### Tool 35: get_file_hash

**Function 定义**：
```python
{
    "name": "get_file_hash",
    "description": "计算文件的哈希值（MD5、SHA1、SHA256 等）。\n\n使用场景：\n- 当用户需要计算文件哈希值时使用\n- 当用户想要验证文件完整性时使用\n- 当用户需要生成文件指纹时使用\n\n参数说明：\n- file_path：文件路径\n- algorithm：哈希算法（可选）。md5-MD5 算法；sha1-SHA1 算法；sha256-SHA256 算法（默认值）\n\n【重要】返回文件的哈希值字符串",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "文件路径，必须是绝对路径"
            },
            "algorithm": {
                "type": "string",
                "description": "哈希算法（可选）。可选值：md5（MD5 算法）、sha1（SHA1 算法）、sha256（SHA256 算法，默认值）"
            }
        },
        "required": ["file_path"]
    }
}
```

---

### 6.11 附录：一级工具配套保障（工具36-46）

#### Tool 36: check_path_exists

**Function 定义**：
```python
{
    "name": "check_path_exists",
    "description": "确认指定路径是否存在。\n\n使用场景：\n- 当用户需要检查路径是否存在时使用\n- 当用户想要在进行文件操作前确认路径时使用\n\n参数说明：\n- path：要检查的路径（文件或目录）\n\n【重要】返回路径是否存在（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "path": {
                "type": "string",
                "description": "要检查的路径，可以是文件路径或目录路径"
            }
        },
        "required": ["path"]
    }
}
```

---

#### Tool 37: ensure_directory_exists

**Function 定义**：
```python
{
    "name": "ensure_directory_exists",
    "description": "确保目标目录存在，不存在则创建。\n\n使用场景：\n- 当用户需要确保目录存在时使用\n- 当用户在进行文件操作前需要确保目标目录存在时使用\n\n参数说明：\n- dir_path：目录路径\n\n【重要】如果目录不存在会自动创建，已存在则静默成功",
    "parameters": {
        "type": "object",
        "properties": {
            "dir_path": {
                "type": "string",
                "description": "目录路径。如果目录不存在会自动创建"
            }
        },
        "required": ["dir_path"]
    }
}
```

---

#### Tool 38: check_write_permission

**Function 定义**：
```python
{
    "name": "check_write_permission",
    "description": "确认文件/目录有写入权限。\n\n使用场景：\n- 当用户需要检查写入权限时使用\n- 当用户在进行写入操作前需要确认权限时使用\n\n参数说明：\n- path：文件或目录路径\n\n【重要】返回是否有写入权限（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "path": {
                "type": "string",
                "description": "文件或目录路径"
            }
        },
        "required": ["path"]
    }
}
```

---

#### Tool 39: check_read_permission

**Function 定义**：
```python
{
    "name": "check_read_permission",
    "description": "确认目录有读取权限。\n\n使用场景：\n- 当用户需要检查读取权限时使用\n- 当用户在进行读取操作前需要确认权限时使用\n\n参数说明：\n- path：目录路径\n\n【重要】返回是否有读取权限（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "path": {
                "type": "string",
                "description": "目录路径"
            }
        },
        "required": ["path"]
    }
}
```

---

#### Tool 40: get_file_encoding

**Function 定义**：
```python
{
    "name": "get_file_encoding",
    "description": "确认文件编码（UTF-8/GBK），避免乱码。\n\n使用场景：\n- 当用户需要确认文件编码时使用\n- 当用户在进行文件读取前需要确定编码时使用\n\n参数说明：\n- file_path：文件路径\n\n【重要】返回文件的编码类型",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "文件路径"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 41: get_mime_type

**Function 定义**：
```python
{
    "name": "get_mime_type",
    "description": "确认文件 MIME 类型。\n\n使用场景：\n- 当用户需要确认文件 MIME 类型时使用\n- 当用户需要根据文件类型进行不同处理时使用\n\n参数说明：\n- file_path：文件路径\n\n【重要】返回文件的 MIME 类型（如 text/plain、image/png、application/json）",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "文件路径"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 42: backup_file

**Function 定义**：
```python
{
    "name": "backup_file",
    "description": "编辑/覆盖前自动备份原文件。\n\n使用场景：\n- 当用户需要在修改文件前进行备份时使用\n- 当用户想要创建文件的历史副本时使用\n\n参数说明：\n- file_path：要备份的文件路径\n- backup_dir：备份目录（可选），默认同目录\n\n【重要】创建文件的备份副本，返回备份文件路径",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "要备份的文件路径"
            },
            "backup_dir": {
                "type": "string",
                "description": "备份目录（可选），默认同目录。指定备份文件的存放位置"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 43: move_to_trash

**Function 定义**：
```python
{
    "name": "move_to_trash",
    "description": "删除到回收站而非永久删除，可恢复。\n\n使用场景：\n- 当用户需要安全删除文件时使用\n- 当用户想要将文件移到回收站而非永久删除时使用\n\n参数说明：\n- file_path：要删除的文件路径\n\n【重要】将文件移到系统回收站，可以从回收站恢复",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "要删除的文件路径。文件会被移到系统回收站，而非永久删除"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 44: get_current_working_dir

**Function 定义**：
```python
{
    "name": "get_current_working_dir",
    "description": "获取当前 Shell 工作目录。\n\n使用场景：\n- 当用户需要获取当前工作目录时使用\n- 当用户想要确定当前工作路径时使用\n\n参数说明：\n- 无参数\n\n【重要】返回当前工作目录的绝对路径",
    "parameters": {
        "type": "object",
        "properties": {},
        "required": []
    }
}
```

---

#### Tool 45: validate_command

**Function 定义**：
```python
{
    "name": "validate_command",
    "description": "检查命令安全性，防止危险操作。\n\n使用场景：\n- 当用户需要验证命令是否安全时使用\n- 当用户想要在执行命令前进行安全检查时使用\n\n参数说明：\n- command：要验证的命令\n\n【重要】返回命令是否安全（true/false）和安全评估信息",
    "parameters": {
        "type": "object",
        "properties": {
            "command": {
                "type": "string",
                "description": "要验证的命令。检查是否存在危险操作，如格式化磁盘、删除系统文件等"
            }
        },
        "required": ["command"]
    }
}
```

---

#### Tool 46: check_shell_running

**Function 定义**：
```python
{
    "name": "check_shell_running",
    "description": "检查后台 Shell 是否还在运行。\n\n使用场景：\n- 当用户需要检查后台命令状态时使用\n- 当用户想要确认后台进程是否完成时使用\n\n参数说明：\n- shell_id：Shell ID\n\n【重要】返回 shell 是否还在运行（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "shell_id": {
                "type": "string",
                "description": "Shell ID。通过 execute_shell_command 的 run_in_background=true 执行命令后获得"
            }
        },
        "required": ["shell_id"]
    }
}
```

---

**更新时间**: 2026-04-04 20:25:42

---

## 七、二级实现8类分类Tool完整定义（8类28个Tool定义）

**说明**：本章按照第22章的分类，将全部28个二级实现Tool进行完整的Function定义。每个Tool的定义包含：
- **name**：工具名称
- **description**：完整准确的工具用途说明
- **parameters**：完整准确的参数定义（类型、必填/可选、参数描述）

**注**：第22章实际定义了28个Tool（36-63），本章节按序号一一对应编写完整的Function定义。

### 7.1 11类：数据库访问（工具36-38）

#### Tool 36: query_sql

**Function 定义**：
```python
{
    "name": "query_sql",
    "description": "执行 SQL SELECT 查询并返回结果集，支持 SQLite、MySQL、PostgreSQL 等主流关系型数据库。\n\n使用场景：\n- 当用户需要查询数据库中的数据时使用\n- 当用户想要执行 SELECT 语句获取数据时使用\n- 当用户需要对数据库进行数据分析时使用\n\n参数说明：\n- db_path：SQLite 数据库文件路径（可选，若不填则使用默认数据库）\n- db_type：数据库类型，可选 sqlite（默认）/ mysql / postgresql\n- connection_string：数据库连接字符串（用于 MySQL/PostgreSQL 连接）\n- query：SQL SELECT 查询语句\n- params：查询参数（用于参数化查询，防止 SQL 注入）\n- limit：返回行数限制，默认 100\n- offset：返回起始偏移，默认 0\n\n【重要】仅支持 SELECT 查询，不支持修改数据的操作",
    "parameters": {
        "type": "object",
        "properties": {
            "db_path": {
                "type": "string",
                "description": "SQLite 数据库文件路径（可选）。若不填则使用系统默认数据库"
            },
            "db_type": {
                "type": "string",
                "description": "数据库类型（可选）。可选值：sqlite（默认值）、mysql、postgresql"
            },
            "connection_string": {
                "type": "string",
                "description": "数据库连接字符串（可选）。用于 MySQL/PostgreSQL 连接，如 mysql://user:pass@host:3306/dbname"
            },
            "query": {
                "type": "string",
                "description": "SQL SELECT 查询语句。如 SELECT * FROM users WHERE age > 18"
            },
            "params": {
                "type": "object",
                "description": "查询参数（可选）。用于参数化查询，防止 SQL 注入。如 {\"age\": 18}"
            },
            "limit": {
                "type": "integer",
                "description": "返回行数限制（可选），默认 100。用于限制查询结果数量"
            },
            "offset": {
                "type": "integer",
                "description": "返回起始偏移（可选），默认 0。用于分页查询"
            }
        },
        "required": ["query"]
    }
}
```

---

#### Tool 37: execute_sql

**Function 定义**：
```python
{
    "name": "execute_sql",
    "description": "执行 INSERT、UPDATE、DELETE 等 SQL 语句，返回影响的行数。\n\n使用场景：\n- 当用户需要向数据库插入新数据时使用\n- 当用户需要更新数据库中的记录时使用\n- 当用户需要删除数据库中的记录时使用\n\n参数说明：\n- db_path：SQLite 数据库文件路径\n- db_type：数据库类型：sqlite / mysql / postgresql\n- connection_string：数据库连接字符串\n- statement：SQL INSERT/UPDATE/DELETE 语句\n- params：语句参数（用于参数化查询）\n- commit：是否立即提交，默认 true\n\n【重要】执行会修改数据库的操作，请谨慎使用",
    "parameters": {
        "type": "object",
        "properties": {
            "db_path": {
                "type": "string",
                "description": "SQLite 数据库文件路径（可选）"
            },
            "db_type": {
                "type": "string",
                "description": "数据库类型（可选）。可选值：sqlite、mysql、postgresql"
            },
            "connection_string": {
                "type": "string",
                "description": "数据库连接字符串（可选）。用于 MySQL/PostgreSQL 连接"
            },
            "statement": {
                "type": "string",
                "description": "SQL INSERT/UPDATE/DELETE 语句。如 INSERT INTO users (name, age) VALUES (?, ?)"
            },
            "params": {
                "type": "object",
                "description": "语句参数（可选）。用于参数化查询，防止 SQL 注入。如 {\"name\": \"张三\", \"age\": 25}"
            },
            "commit": {
                "type": "boolean",
                "default": true,
                "description": "是否立即提交（可选），默认 true。设置为 false 时不自动提交，需要手动调用 commit_transaction"
            }
        },
        "required": ["statement"]
    }
}
```

---

#### Tool 38: query_nosql

**Function 定义**：
```python
{
    "name": "query_nosql",
    "description": "查询 MongoDB 等 NoSQL 数据库（需要安装 pymongo）。\n\n使用场景：\n- 当用户需要查询 MongoDB 数据库时使用\n- 当用户想要从 NoSQL 数据库中获取文档时使用\n- 当用户需要对 NoSQL 数据进行过滤查询时使用\n\n参数说明：\n- connection_string：MongoDB 连接字符串（如 mongodb://localhost:27017）\n- database：数据库名称\n- collection：集合名称\n- filter：查询过滤条件（MongoDB 查询语法）\n- projection：返回字段过滤\n- limit：返回文档数量限制，默认 100\n\n【重要】需要安装 pymongo 库",
    "parameters": {
        "type": "object",
        "properties": {
            "connection_string": {
                "type": "string",
                "description": "MongoDB 连接字符串。如 mongodb://localhost:27017 或 mongodb+srv://user:pass@cluster.mongodb.net"
            },
            "database": {
                "type": "string",
                "description": "数据库名称。如 mydb"
            },
            "collection": {
                "type": "string",
                "description": "集合名称。如 users"
            },
            "filter": {
                "type": "object",
                "description": "查询过滤条件（可选）。使用 MongoDB 查询语法，如 {\"age\": {\"$gt\": 18}}"
            },
            "projection": {
                "type": "object",
                "description": "返回字段过滤（可选）。指定返回哪些字段，如 {\"name\": 1, \"age\": 1, \"_id\": 0}"
            },
            "limit": {
                "type": "integer",
                "description": "返回文档数量限制（可选），默认 100"
            }
        },
        "required": ["connection_string", "database", "collection"]
    }
}
```

---

### 7.2 12类：注册表操作（工具39-41）

#### Tool 39: reg_read

**Function 定义**：
```python
{
    "name": "reg_read",
    "description": "读取 Windows 注册表指定键的值。\n\n使用场景：\n- 当用户需要读取 Windows 注册表中的配置信息时使用\n- 当用户想要获取系统或应用程序的注册表设置时使用\n- 当用户需要诊断注册表相关问题时使用\n\n参数说明：\n- key_path：注册表键路径（如 HKEY_CURRENT_USER\\Software\\Microsoft）\n- value_name：值名称（不填则返回键的默认值）\n- hive：注册表根键，可选 HKCU（默认）/ HKLM / HKCR / HKU / HKCC\n\n【重要】仅限 Windows 平台，使用 Python 内置库 winreg",
    "parameters": {
        "type": "object",
        "properties": {
            "key_path": {
                "type": "string",
                "description": "注册表键路径。如 Software\\Microsoft\\Windows\\CurrentVersion"
            },
            "value_name": {
                "type": "string",
                "description": "值名称（可选）。不填则返回键的默认值"
            },
            "hive": {
                "type": "string",
                "description": "注册表根键（可选）。可选值：HKCU（HKEY_CURRENT_USER，默认值）、HKLM（HKEY_LOCAL_MACHINE）、HKCR（HKEY_CLASSES_ROOT）、HKU（HKEY_USERS）、HKCC（HKEY_CURRENT_CONFIG）"
            }
        },
        "required": ["key_path"]
    }
}
```

---

#### Tool 40: reg_write

**Function 定义**：
```python
{
    "name": "reg_write",
    "description": "写入或创建 Windows 注册表键值。\n\n使用场景：\n- 当用户需要修改 Windows 注册表设置时使用\n- 当用户想要创建新的注册表项时使用\n- 当用户需要配置应用程序的注册表参数时使用\n\n参数说明：\n- key_path：注册表键路径\n- value_name：值名称\n- value：值数据\n- value_type：值类型，可选 REG_SZ（默认）/ REG_DWORD / REG_BINARY / REG_MULTI_SZ\n- hive：注册表根键，可选 HKCU（默认）/ HKLM / HKCR / HKU / HKCC\n\n【重要】仅限 Windows 平台，修改注册表可能影响系统稳定性",
    "parameters": {
        "type": "object",
        "properties": {
            "key_path": {
                "type": "string",
                "description": "注册表键路径。如 Software\\MyApp"
            },
            "value_name": {
                "type": "string",
                "description": "值名称。如 InstallPath"
            },
            "value": {
                "type": "string",
                "description": "值数据。要写入的注册表值"
            },
            "value_type": {
                "type": "string",
                "description": "值类型（可选）。可选值：REG_SZ（字符串，默认值）、REG_DWORD（32位整数）、REG_BINARY（二进制）、REG_MULTI_SZ（多字符串）"
            },
            "hive": {
                "type": "string",
                "description": "注册表根键（可选）。可选值：HKCU（默认值）、HKLM、HKCR、HKU、HKCC"
            }
        },
        "required": ["key_path", "value_name", "value"]
    }
}
```

---

#### Tool 41: reg_delete

**Function 定义**：
```python
{
    "name": "reg_delete",
    "description": "删除 Windows 注册表键或值。\n\n使用场景：\n- 当用户需要删除不再需要的注册表项时使用\n- 当用户想要清理应用程序的注册表残留时使用\n- 当用户需要修复注册表错误时使用\n\n参数说明：\n- key_path：注册表键路径\n- value_name：值名称（不填则删除整个键）\n- hive：注册表根键，可选 HKCU（默认）/ HKLM / HKCR / HKU / HKCC\n\n【重要】仅限 Windows 平台，删除注册表操作不可恢复，请谨慎使用",
    "parameters": {
        "type": "object",
        "properties": {
            "key_path": {
                "type": "string",
                "description": "注册表键路径。如 Software\\MyApp"
            },
            "value_name": {
                "type": "string",
                "description": "值名称（可选）。不填则删除整个键及其所有子键和值"
            },
            "hive": {
                "type": "string",
                "description": "注册表根键（可选）。可选值：HKCU（默认值）、HKLM、HKCR、HKU、HKCC"
            }
        },
        "required": ["key_path"]
    }
}
```

---

### 7.3 13类：日志记录（工具42-43）

#### Tool 42: log_message

**Function 定义**：
```python
{
    "name": "log_message",
    "description": "记录日志消息到指定日志文件或日志系统。\n\n使用场景：\n- 当用户需要记录操作日志时使用\n- 当用户想要进行审计追踪时使用\n- 当用户需要调试程序问题时使用\n\n参数说明：\n- message：日志消息内容\n- level：日志级别，可选 DEBUG / INFO / WARNING / ERROR / CRITICAL（默认 INFO）\n- logger_name：日志记录器名称（默认 root）\n- log_file：日志文件路径（若不填则只输出到控制台）\n\n【重要】使用 Python 内置库 logging，零依赖",
    "parameters": {
        "type": "object",
        "properties": {
            "message": {
                "type": "string",
                "description": "日志消息内容。如 用户登录成功"
            },
            "level": {
                "type": "string",
                "description": "日志级别（可选）。可选值：DEBUG、INFO（默认值）、WARNING、ERROR、CRITICAL"
            },
            "logger_name": {
                "type": "string",
                "description": "日志记录器名称（可选）。默认 root"
            },
            "log_file": {
                "type": "string",
                "description": "日志文件路径（可选）。若不填则只输出到控制台"
            }
        },
        "required": ["message"]
    }
}
```

---

#### Tool 43: get_logs

**Function 定义**：
```python
{
    "name": "get_logs",
    "description": "读取指定日志文件的内容。\n\n使用场景：\n- 当用户需要查看日志文件内容时使用\n- 当用户想要分析历史日志时使用\n- 当用户需要排查问题时使用\n\n参数说明：\n- log_file：日志文件路径\n- level：日志级别过滤，可选 DEBUG / INFO / WARNING / ERROR\n- start_time：起始时间过滤（格式：YYYY-MM-DD HH:mm:ss）\n- end_time：结束时间过滤\n- max_lines：返回最大行数，默认 500\n\n【重要】返回日志文件的文本内容",
    "parameters": {
        "type": "object",
        "properties": {
            "log_file": {
                "type": "string",
                "description": "日志文件路径。如 D:/logs/app.log"
            },
            "level": {
                "type": "string",
                "description": "日志级别过滤（可选）。可选值：DEBUG、INFO、WARNING、ERROR"
            },
            "start_time": {
                "type": "string",
                "description": "起始时间过滤（可选）。格式：YYYY-MM-DD HH:mm:ss"
            },
            "end_time": {
                "type": "string",
                "description": "结束时间过滤（可选）。格式：YYYY-MM-DD HH:mm:ss"
            },
            "max_lines": {
                "type": "integer",
                "description": "返回最大行数（可选），默认 500。用于限制输出量"
            }
        },
        "required": ["log_file"]
    }
}
```

---

### 7.4 14类：数据序列化（工具44-46）

#### Tool 44: read_json

**Function 定义**：
```python
{
    "name": "read_json",
    "description": "读取并解析 JSON 文件，返回 Python 对象。\n\n使用场景：\n- 当用户需要读取 JSON 配置文件时使用\n- 当用户想要解析 JSON 格式的数据文件时使用\n- 当用户需要加载 JSON 格式的应用程序数据时使用\n\n参数说明：\n- file_path：JSON 文件路径\n- encoding：文件编码，默认 utf-8\n\n【重要】使用 Python 内置库 json，零依赖",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "JSON 文件路径。如 D:/config/settings.json"
            },
            "encoding": {
                "type": "string",
                "description": "文件编码（可选），默认 utf-8"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 45: write_json

**Function 定义**：
```python
{
    "name": "write_json",
    "description": "将 Python 对象写入 JSON 文件。\n\n使用场景：\n- 当用户需要保存配置到 JSON 文件时使用\n- 当用户想要导出数据为 JSON 格式时使用\n- 当用户需要生成 JSON 格式的数据文件时使用\n\n参数说明：\n- file_path：JSON 文件路径\n- data：要写入的数据（Python 对象）\n- encoding：文件编码，默认 utf-8\n- indent：缩进空格数，默认 2\n\n【重要】使用 Python 内置库 json，零依赖",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "JSON 文件路径。如 D:/output/data.json"
            },
            "data": {
                "type": "object",
                "description": "要写入的数据（Python 对象）。可以是字典、列表等可序列化的对象"
            },
            "encoding": {
                "type": "string",
                "description": "文件编码（可选），默认 utf-8"
            },
            "indent": {
                "type": "integer",
                "description": "缩进空格数（可选），默认 2。设置为 0 或 None 时不缩进"
            }
        },
        "required": ["file_path", "data"]
    }
}
```

---

#### Tool 46: read_csv_basic

**Function 定义**：
```python
{
    "name": "read_csv_basic",
    "description": "使用 Python 标准库 csv 读取 CSV 文件，零依赖，轻量级读取。\n\n使用场景：\n- 当用户需要读取 CSV 格式的数据文件时使用\n- 当用户想要分析表格数据时使用\n- 当用户需要导入外部数据时使用\n\n参数说明：\n- file_path：CSV 文件路径\n- encoding：文件编码，默认 utf-8\n- delimiter：分隔符，默认逗号 ,\n- has_header：是否有表头，默认 true\n- max_rows：最大读取行数，默认 1000\n\n【重要】使用 Python 内置库 csv，零依赖，不支持 pandas 高级功能",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "CSV 文件路径。如 D:/data/users.csv"
            },
            "encoding": {
                "type": "string",
                "description": "文件编码（可选），默认 utf-8"
            },
            "delimiter": {
                "type": "string",
                "description": "分隔符（可选），默认逗号 ,。支持其他分隔符如 ; 或 \\t"
            },
            "has_header": {
                "type": "boolean",
                "default": true,
                "description": "是否有表头（可选），默认 true。设置为 false 时第一行作为数据处理"
            },
            "max_rows": {
                "type": "integer",
                "description": "最大读取行数（可选），默认 1000。用于限制大文件的读取量"
            }
        },
        "required": ["file_path"]
    }
}
```

---

### 7.5 15类：进程管理（工具47-48）

#### Tool 47: list_processes

**Function 定义**：
```python
{
    "name": "list_processes",
    "description": "获取当前运行的进程列表，支持过滤和排序。\n\n使用场景：\n- 当用户需要查看当前运行的进程时使用\n- 当用户想要查找特定进程时使用\n- 当用户需要监控系统资源占用时使用\n\n参数说明：\n- name：进程名称过滤（如 \"python.exe\"）\n- user：用户名过滤\n- status：状态过滤，可选 running / sleeping\n- limit：返回进程数量限制，默认 100\n- sort_by：排序方式，可选 pid / name / cpu / memory（默认 pid）\n\n【重要】需要安装 psutil 库（pip install psutil）",
    "parameters": {
        "type": "object",
        "properties": {
            "name": {
                "type": "string",
                "description": "进程名称过滤（可选）。如 python.exe、chrome.exe"
            },
            "user": {
                "type": "string",
                "description": "用户名过滤（可选）。如 Administrator"
            },
            "status": {
                "type": "string",
                "description": "状态过滤（可选）。可选值：running（运行中）、sleeping（睡眠）"
            },
            "limit": {
                "type": "integer",
                "description": "返回进程数量限制（可选），默认 100"
            },
            "sort_by": {
                "type": "string",
                "description": "排序方式（可选）。可选值：pid（默认值）、name（名称）、cpu（CPU 占用）、memory（内存占用）"
            }
        },
        "required": []
    }
}
```

---

#### Tool 48: kill_process

**Function 定义**：
```python
{
    "name": "kill_process",
    "description": "终止指定进程。\n\n使用场景：\n- 当用户需要终止卡死的进程时使用\n- 当用户想要关闭不需要的后台进程时使用\n- 当用户需要清理资源时使用\n\n参数说明：\n- pid：进程 ID\n- name：进程名称（可批量终止）\n- force：是否强制终止，默认 false\n\n【重要】需要安装 psutil 库，终止进程可能导致数据丢失",
    "parameters": {
        "type": "object",
        "properties": {
            "pid": {
                "type": "integer",
                "description": "进程 ID。如 1234"
            },
            "name": {
                "type": "string",
                "description": "进程名称（可选）。可批量终止同名进程，如 python.exe"
            },
            "force": {
                "type": "boolean",
                "default": false,
                "description": "是否强制终止（可选），默认 false。设置为 true 时强制终止，可能导致数据丢失"
            }
        },
        "required": []
    }
}
```

---

### 7.6 16类：服务管理（工具49-51）

#### Tool 49: service_list

**Function 定义**：
```python
{
    "name": "service_list",
    "description": "获取 Windows 服务列表。\n\n使用场景：\n- 当用户需要查看系统中安装的服务时使用\n- 当用户想要了解服务的运行状态时使用\n- 当用户需要查找特定服务时使用\n\n参数说明：\n- state：服务状态过滤，可选 running / stopped / all（默认 all）\n- name：服务名称过滤\n\n【重要】使用 subprocess 执行 sc 命令，零依赖",
    "parameters": {
        "type": "object",
        "properties": {
            "state": {
                "type": "string",
                "description": "服务状态过滤（可选）。可选值：running（运行中）、stopped（已停止）、all（全部，默认值）"
            },
            "name": {
                "type": "string",
                "description": "服务名称过滤（可选）。如 MySQL"
            }
        },
        "required": []
    }
}
```

---

#### Tool 50: service_start

**Function 定义**：
```python
{
    "name": "service_start",
    "description": "启动指定的 Windows 服务。\n\n使用场景：\n- 当用户需要启动已停止的服务时使用\n- 当用户想要重启服务时使用\n- 当用户需要启用某个功能服务时使用\n\n参数说明：\n- service_name：服务名称\n\n【重要】使用 subprocess 执行 sc 命令，需要管理员权限",
    "parameters": {
        "type": "object",
        "properties": {
            "service_name": {
                "type": "string",
                "description": "服务名称。如 MySQL80、wuauserv"
            }
        },
        "required": ["service_name"]
    }
}
```

---

#### Tool 51: service_stop

**Function 定义**：
```python
{
    "name": "service_stop",
    "description": "停止指定的 Windows 服务。\n\n使用场景：\n- 当用户需要停止正在运行的服务时使用\n- 当用户想要重启服务前先停止时使用\n- 当用户需要禁用某个功能服务时使用\n\n参数说明：\n- service_name：服务名称\n- force：是否强制停止，默认 false\n\n【重要】使用 subprocess 执行 sc 命令，停止服务可能影响相关功能",
    "parameters": {
        "type": "object",
        "properties": {
            "service_name": {
                "type": "string",
                "description": "服务名称。如 MySQL80、wuauserv"
            },
            "force": {
                "type": "boolean",
                "default": false,
                "description": "是否强制停止（可选），默认 false。设置为 true 时强制停止服务"
            }
        },
        "required": ["service_name"]
    }
}
```

---

### 7.7 17类：计划任务（工具52-54）

#### Tool 52: task_list

**Function 定义**：
```python
{
    "name": "task_list",
    "description": "获取 Windows 计划任务列表。\n\n使用场景：\n- 当用户需要查看系统中创建的计划任务时使用\n- 当用户想要了解任务的执行计划时使用\n- 当用户需要查找特定任务时使用\n\n参数说明：\n- folder：任务文件夹（如 \\Microsoft）\n- state：状态过滤，可选 ready / running / disabled\n\n【重要】使用 subprocess 执行 schtasks 命令，零依赖",
    "parameters": {
        "type": "object",
        "properties": {
            "folder": {
                "type": "string",
                "description": "任务文件夹（可选）。如 \\Microsoft、\\"
            },
            "state": {
                "type": "string",
                "description": "状态过滤（可选）。可选值：ready（就绪）、running（运行中）、disabled（已禁用）"
            }
        },
        "required": []
    }
}
```

---

#### Tool 53: task_create

**Function 定义**：
```python
{
    "name": "task_create",
    "description": "创建新的 Windows 计划任务。\n\n使用场景：\n- 当用户需要创建定时执行的任务时使用\n- 当用户想要设置开机启动任务时使用\n- 当用户需要创建定期执行的脚本时使用\n\n参数说明：\n- task_name：任务名称\n- command：要执行的命令\n- schedule：计划类型，可选 DAILY / WEEKLY / MONTHLY / ONCE / ONSTART / ONLOGON\n- start_time：开始时间（格式：HH:MM）\n- start_date：开始日期（格式：YYYY-MM-DD）\n- interval：重复间隔（分钟）\n\n【重要】使用 subprocess 执行 schtasks 命令",
    "parameters": {
        "type": "object",
        "properties": {
            "task_name": {
                "type": "string",
                "description": "任务名称。如 MyBackupTask"
            },
            "command": {
                "type": "string",
                "description": "要执行的命令。如 C:/scripts/backup.bat"
            },
            "schedule": {
                "type": "string",
                "description": "计划类型。可选值：DAILY（每天）、WEEKLY（每周）、MONTHLY（每月）、ONCE（一次性）、ONSTART（开机时）、ONLOGON（登录时）"
            },
            "start_time": {
                "type": "string",
                "description": "开始时间（可选）。格式：HH:MM，如 09:00"
            },
            "start_date": {
                "type": "string",
                "description": "开始日期（可选）。格式：YYYY-MM-DD，如 2026-04-01"
            },
            "interval": {
                "type": "integer",
                "description": "重复间隔（可选），单位为分钟"
            }
        },
        "required": ["task_name", "command", "schedule"]
    }
}
```

---

#### Tool 54: task_delete

**Function 定义**：
```python
{
    "name": "task_delete",
    "description": "删除指定的计划任务。\n\n使用场景：\n- 当用户需要删除不再需要的计划任务时使用\n- 当用户想要清理无用的任务时使用\n\n参数说明：\n- task_name：任务名称\n- folder：任务文件夹\n\n【重要】使用 subprocess 执行 schtasks 命令，删除后无法恢复",
    "parameters": {
        "type": "object",
        "properties": {
            "task_name": {
                "type": "string",
                "description": "任务名称。如 MyBackupTask"
            },
            "folder": {
                "type": "string",
                "description": "任务文件夹（可选）。如 \\Microsoft"
            }
        },
        "required": ["task_name"]
    }
}
```

---

### 7.8 18类：网络诊断（工具55-56）

#### Tool 55: ping

**Function 定义**：
```python
{
    "name": "ping",
    "description": "Ping 指定主机或 IP 地址，检测网络连通性。\n\n使用场景：\n- 当用户需要检测网络是否连通时使用\n- 当用户想要测试主机是否可达时使用\n- 当用户需要诊断网络问题时使用\n\n参数说明：\n- host：目标主机或 IP 地址\n- count：Ping 次数，默认 4\n- timeout：超时时间（秒），默认 5\n\n【重要】使用 subprocess 执行 ping 命令或 socket 库，零依赖",
    "parameters": {
        "type": "object",
        "properties": {
            "host": {
                "type": "string",
                "description": "目标主机或 IP 地址。如 8.8.8.8 或 www.google.com"
            },
            "count": {
                "type": "integer",
                "description": "Ping 次数（可选），默认 4"
            },
            "timeout": {
                "type": "integer",
                "description": "超时时间（可选），单位为秒，默认 5"
            }
        },
        "required": ["host"]
    }
}
```

---

#### Tool 56: port_check

**Function 定义**：
```python
{
    "name": "port_check",
    "description": "检查指定主机端口是否开放。\n\n使用场景：\n- 当用户需要检测服务端口是否开放时使用\n- 当用户想要确认防火墙规则是否生效时使用\n- 当用户需要诊断服务是否启动时使用\n\n参数说明：\n- host：目标主机或 IP 地址\n- port：端口号（1-65535）\n- timeout：超时时间（秒），默认 3\n\n【重要】使用 socket 库检测端口，零依赖",
    "parameters": {
        "type": "object",
        "properties": {
            "host": {
                "type": "string",
                "description": "目标主机或 IP 地址。如 192.168.1.1 或 localhost"
            },
            "port": {
                "type": "integer",
                "description": "端口号（1-65535）。如 80（HTTP）、443（HTTPS）、3306（MySQL）"
            },
            "timeout": {
                "type": "integer",
                "description": "超时时间（可选），单位为秒，默认 3"
            }
        },
        "required": ["host", "port"]
    }
}
```

---

### 7.9 附录：二级工具配套保障（工具57-63）

#### Tool 57: check_db_exists

**Function 定义**：
```python
{
    "name": "check_db_exists",
    "description": "确认数据库文件或连接是否存在。\n\n使用场景：\n- 当用户需要确认数据库是否可用时使用\n- 当用户在进行数据库操作前需要验证连接时使用\n\n参数说明：\n- db_path：SQLite数据库文件路径\n- connection_string：数据库连接字符串\n\n【重要】返回数据库是否存在（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "db_path": {
                "type": "string",
                "description": "SQLite数据库文件路径（可选）"
            },
            "connection_string": {
                "type": "string",
                "description": "数据库连接字符串（可选）。用于 MySQL/PostgreSQL"
            }
        },
        "required": []
    }
}
```

---

#### Tool 58: get_table_schema

**Function 定义**：
```python
{
    "name": "get_table_schema",
    "description": "获取数据库表结构，Agent知道有哪些字段。\n\n使用场景：\n- 当用户需要了解数据库表结构时使用\n- 当用户想要知道表中有哪些字段和类型时使用\n\n参数说明：\n- db_path：SQLite数据库文件路径\n- connection_string：数据库连接字符串\n- table_name：表名称\n\n【重要】返回表的字段名、类型、主键等信息",
    "parameters": {
        "type": "object",
        "properties": {
            "db_path": {
                "type": "string",
                "description": "SQLite数据库文件路径（可选）"
            },
            "connection_string": {
                "type": "string",
                "description": "数据库连接字符串（可选）"
            },
            "table_name": {
                "type": "string",
                "description": "表名称。如 users"
            }
        },
        "required": ["table_name"]
    }
}
```

---

#### Tool 59: begin_transaction

**Function 定义**：
```python
{
    "name": "begin_transaction",
    "description": "开启数据库事务，保证数据一致性。\n\n使用场景：\n- 当用户需要执行多个关联的数据库操作时使用\n- 当用户想要保证操作的原子性时使用\n\n参数说明：\n- connection_string：数据库连接字符串\n\n【重要】开启事务后需要调用 commit_transaction 或 rollback_transaction",
    "parameters": {
        "type": "object",
        "properties": {
            "connection_string": {
                "type": "string",
                "description": "数据库连接字符串"
            }
        },
        "required": ["connection_string"]
    }
}
```

---

#### Tool 60: commit_transaction

**Function 定义**：
```python
{
    "name": "commit_transaction",
    "description": "提交数据库事务。\n\n使用场景：\n- 当用户完成事务内的所有操作并确认无误时使用\n- 当用户想要永久保存事务中的更改时使用\n\n参数说明：\n- connection_string：数据库连接字符串\n\n【重要】提交后事务内的操作将永久生效",
    "parameters": {
        "type": "object",
        "properties": {
            "connection_string": {
                "type": "string",
                "description": "数据库连接字符串"
            }
        },
        "required": ["connection_string"]
    }
}
```

---

#### Tool 61: rollback_transaction

**Function 定义**：
```python
{
    "name": "rollback_transaction",
    "description": "回滚数据库事务。\n\n使用场景：\n- 当事务内操作出现错误时使用\n- 当用户想要撤销事务中的更改时使用\n\n参数说明：\n- connection_string：数据库连接字符串\n\n【重要】回滚后事务内的所有操作将被撤销",
    "parameters": {
        "type": "object",
        "properties": {
            "connection_string": {
                "type": "string",
                "description": "数据库连接字符串"
            }
        },
        "required": ["connection_string"]
    }
}
```

---

#### Tool 62: check_network_connectivity

**Function 定义**：
```python
{
    "name": "check_network_connectivity",
    "description": "检查网络是否连通。\n\n使用场景：\n- 当用户需要检测网络是否可用时使用\n- 当用户在进行网络操作前需要验证连通性时使用\n\n参数说明：\n- host：目标主机\n- timeout：超时秒数，默认5\n\n【重要】返回网络是否连通（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "host": {
                "type": "string",
                "description": "目标主机。如 8.8.8.8 或 www.baidu.com"
            },
            "timeout": {
                "type": "integer",
                "description": "超时秒数（可选），默认 5"
            }
        },
        "required": ["host"]
    }
}
```

---

#### Tool 63: validate_url

**Function 定义**：
```python
{
    "name": "validate_url",
    "description": "验证URL格式是否合法。\n\n使用场景：\n- 当用户需要确认URL格式是否正确时使用\n- 当用户在进行网络请求前需要验证URL时使用\n\n参数说明：\n- url：要验证的URL\n\n【重要】返回URL是否合法（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "url": {
                "type": "string",
                "description": "要验证的URL。如 https://example.com/api"
            }
        },
        "required": ["url"]
    }
}
```

---

## 八、三级实现3类分类Tool完整定义（3类17个Tool定义）

**说明**：本章按照第23章的分类，将全部17个三级实现Tool进行完整的Function定义。每个Tool的定义包含：
- **name**：工具名称
- **description**：完整准确的工具用途说明
- **parameters**：完整准确的参数定义（类型、必填/可选、参数描述）

**注**：第23章实际定义了17个Tool（64-71 + 附录9个），本章节按序号一一对应编写完整的Function定义。

### 8.1 19类：代码执行（工具64-65）

#### Tool 64: execute_python

**Function 定义**：
```python
{
    "name": "execute_python",
    "description": "在沙箱环境中执行 Python 代码并返回结果。\n\n使用场景：\n- 当用户需要执行 Python 代码片段时使用\n- 当用户想要测试 Python 代码逻辑时使用\n- 当用户需要进行数据处理或计算时使用\n\n参数说明：\n- code：要执行的 Python 代码\n- timeout：超时时间（秒），默认 30\n- imports：预先导入的模块（如 [\"json\", \"os\"]）\n\n【重要】在沙箱环境中执行，确保安全性",
    "parameters": {
        "type": "object",
        "properties": {
            "code": {
                "type": "string",
                "description": "要执行的 Python 代码。如 print(\"Hello World\") 或 import json; json.dumps({\"a\": 1})"
            },
            "timeout": {
                "type": "integer",
                "description": "超时时间（可选），单位为秒，默认 30。代码执行超过此时间会被强制终止"
            },
            "imports": {
                "type": "array",
                "items": {"type": "string"},
                "description": "预先导入的模块（可选）。如 [\"json\", \"os\", \"math\"]，代码执行前会自动导入这些模块"
            }
        },
        "required": ["code"]
    }
}
```

---

#### Tool 65: execute_javascript

**Function 定义**：
```python
{
    "name": "execute_javascript",
    "description": "使用 Node.js 执行 JavaScript 代码并返回结果。\n\n使用场景：\n- 当用户需要执行 JavaScript 代码片段时使用\n- 当用户想要测试 JavaScript 逻辑时使用\n- 当用户需要进行前端代码验证时使用\n\n参数说明：\n- code：要执行的 JavaScript 代码\n- timeout：超时时间（秒），默认 30\n- node_modules：预先安装的 npm 包\n\n【重要】需要系统安装 Node.js 环境",
    "parameters": {
        "type": "object",
        "properties": {
            "code": {
                "type": "string",
                "description": "要执行的 JavaScript 代码。如 console.log(\"Hello\") 或 JSON.stringify({a: 1})"
            },
            "timeout": {
                "type": "integer",
                "description": "超时时间（可选），单位为秒，默认 30。代码执行超过此时间会被强制终止"
            },
            "node_modules": {
                "type": "array",
                "items": {"type": "string"},
                "description": "预先安装的 npm 包（可选）。如 [\"lodash\", \"axios\"]，代码执行前会自动安装这些包"
            }
        },
        "required": ["code"]
    }
}
```

---

### 8.2 20类：数据分析（工具66-68）

#### Tool 66: read_csv_dataframe

**Function 定义**：
```python
{
    "name": "read_csv_dataframe",
    "description": "使用 pandas 读取 CSV 文件并进行数据分析，返回 DataFrame 格式支持后续统计分析。\n\n使用场景：\n- 当用户需要读取 CSV 文件并进行数据分析时使用\n- 当用户想要对表格数据进行统计、筛选、排序时使用\n- 当用户需要进行数据清洗和预处理时使用\n\n参数说明：\n- file_path：CSV 文件路径\n- encoding：文件编码，默认 utf-8\n- delimiter：分隔符，默认 ,\n- has_header：是否有表头，默认 true\n- max_rows：最大读取行数，默认 1000\n\n【重要】需要安装 pandas 库（pip install pandas）",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "CSV 文件路径。如 D:/data/users.csv"
            },
            "encoding": {
                "type": "string",
                "description": "文件编码（可选），默认 utf-8。支持 gbk、gb2312 等中文编码"
            },
            "delimiter": {
                "type": "string",
                "description": "分隔符（可选），默认 ,。支持其他分隔符如 ;、\\t（制表符）"
            },
            "has_header": {
                "type": "boolean",
                "default": true,
                "description": "是否有表头（可选），默认 true。设置为 false 时第一行作为数据处理"
            },
            "max_rows": {
                "type": "integer",
                "description": "最大读取行数（可选），默认 1000。用于限制大文件的读取量"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 67: generate_chart

**Function 定义**：
```python
{
    "name": "generate_chart",
    "description": "使用 matplotlib 生成数据可视化图表。\n\n使用场景：\n- 当用户需要将数据可视化展示时使用\n- 当用户想要生成柱状图、折线图、饼图等图表时使用\n- 当用户需要生成报告中的图表时使用\n\n参数说明：\n- data：图表数据（JSON 格式）\n- chart_type：图表类型，可选 bar/line/pie/scatter（默认 bar）\n- title：图表标题\n- x_label：X轴标签\n- y_label：Y轴标签\n- output_path：输出图片路径（默认 temp.png）\n\n【重要】需要安装 matplotlib 库（pip install matplotlib）",
    "parameters": {
        "type": "object",
        "properties": {
            "data": {
                "type": "object",
                "description": "图表数据（JSON 格式）。如 {\"labels\": [\"A\", \"B\", \"C\"], \"values\": [10, 20, 30]}"
            },
            "chart_type": {
                "type": "string",
                "description": "图表类型（可选）。可选值：bar（柱状图，默认值）、line（折线图）、pie（饼图）、scatter（散点图）"
            },
            "title": {
                "type": "string",
                "description": "图表标题（可选）。如 \"销售数据统计\""
            },
            "x_label": {
                "type": "string",
                "description": "X轴标签（可选）。如 \"月份\""
            },
            "y_label": {
                "type": "string",
                "description": "Y轴标签（可选）。如 \"销售额（万元）\""
            },
            "output_path": {
                "type": "string",
                "description": "输出图片路径（可选），默认 temp.png。支持 PNG、JPG、SVG 等格式"
            }
        },
        "required": ["data"]
    }
}
```

---

#### Tool 68: analyze_data

**Function 定义**：
```python
{
    "name": "analyze_data",
    "description": "对数据集进行统计分析，返回描述性统计信息。\n\n使用场景：\n- 当用户需要对数据进行统计分析时使用\n- 当用户想要获取数据的均值、总和、最大值、最小值等统计信息时使用\n- 当用户需要进行数据分组分析时使用\n\n参数说明：\n- data：要分析的数据（数组或 CSV 文件路径）\n- operations：分析操作，可选 mean/sum/count/min/max/std（默认全部）\n- group_by：分组字段\n\n【重要】需要安装 pandas 库",
    "parameters": {
        "type": "object",
        "properties": {
            "data": {
                "type": "object",
                "description": "要分析的数据。可以是数组（如 [{\"name\": \"A\", \"value\": 10}]）或 CSV 文件路径（如 \"D:/data/users.csv\"）"
            },
            "operations": {
                "type": "array",
                "items": {"type": "string"},
                "description": "分析操作（可选）。可选值：mean（均值）、sum（总和）、count（计数）、min（最小值）、max（最大值）、std（标准差）。不填时默认执行全部操作"
            },
            "group_by": {
                "type": "string",
                "description": "分组字段（可选）。如 \"department\"，按部门分组统计"
            }
        },
        "required": ["data"]
    }
}
```

---

### 8.3 21类：文档处理（工具69-71）

#### Tool 69: read_pdf

**Function 定义**：
```python
{
    "name": "read_pdf",
    "description": "读取 PDF 文件并提取文本内容。\n\n使用场景：\n- 当用户需要读取 PDF 文档内容时使用\n- 当用户想要从 PDF 中提取文字信息时使用\n- 当用户需要分析 PDF 文档内容时使用\n\n参数说明：\n- file_path：PDF 文件路径\n- pages：要读取的页面（如 \"1-5\" 或 \"1,3,5\"）\n- extract_images：是否提取图片，默认 false\n\n【重要】需要安装 pdfplumber 库（pip install pdfplumber）",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "PDF 文件路径。如 D:/documents/report.pdf"
            },
            "pages": {
                "type": "string",
                "description": "要读取的页面（可选）。如 \"1-5\" 表示读取第1到5页，\"1,3,5\" 表示读取第1、3、5页。不填时读取全部页面"
            },
            "extract_images": {
                "type": "boolean",
                "default": false,
                "description": "是否提取图片（可选），默认 false。设置为 true 时会提取 PDF 中的图片"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 70: read_docx

**Function 定义**：
```python
{
    "name": "read_docx",
    "description": "读取 Word 文档并提取文本内容。\n\n使用场景：\n- 当用户需要读取 Word 文档内容时使用\n- 当用户想要从 Word 文档中提取文字信息时使用\n- 当用户需要分析 Word 文档内容时使用\n\n参数说明：\n- file_path：Word 文件路径\n\n【重要】需要安装 python-docx 库（pip install python-docx）",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "Word 文件路径。如 D:/documents/report.docx"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 71: read_xlsx

**Function 定义**：
```python
{
    "name": "read_xlsx",
    "description": "读取 Excel 文件并提取表格数据。\n\n使用场景：\n- 当用户需要读取 Excel 表格数据时使用\n- 当用户想要从 Excel 中提取数据进行分析时使用\n- 当用户需要查看 Excel 文件内容时使用\n\n参数说明：\n- file_path：Excel 文件路径\n- sheet_name：工作表名称（默认第一个）\n- max_rows：最大读取行数，默认 1000\n\n【重要】需要安装 openpyxl 库（pip install openpyxl）",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "Excel 文件路径。如 D:/data/report.xlsx"
            },
            "sheet_name": {
                "type": "string",
                "description": "工作表名称（可选）。不填时默认读取第一个工作表"
            },
            "max_rows": {
                "type": "integer",
                "description": "最大读取行数（可选），默认 1000。用于限制大文件的读取量"
            }
        },
        "required": ["file_path"]
    }
}
```

---

### 8.4 附录：三级工具配套保障（工具72-80）

#### Tool 72: check_python_available

**Function 定义**：
```python
{
    "name": "check_python_available",
    "description": "检查Python环境是否可用。\n\n使用场景：\n- 当用户需要确认Python环境是否安装时使用\n- 当用户在执行Python代码前需要验证环境时使用\n\n参数说明：\n- 无参数\n\n【重要】返回Python环境是否可用（true/false）及版本信息",
    "parameters": {
        "type": "object",
        "properties": {},
        "required": []
    }
}
```

---

#### Tool 73: validate_code_safety

**Function 定义**：
```python
{
    "name": "validate_code_safety",
    "description": "验证代码安全性，防止危险操作。\n\n使用场景：\n- 当用户需要检查代码是否安全时使用\n- 当用户想要在执行代码前进行安全验证时使用\n\n参数说明：\n- code：要验证的代码\n\n【重要】返回代码是否安全（true/false）及安全评估信息",
    "parameters": {
        "type": "object",
        "properties": {
            "code": {
                "type": "string",
                "description": "要验证的代码。检查是否存在危险操作，如系统调用、文件删除、网络请求等"
            }
        },
        "required": ["code"]
    }
}
```

---

#### Tool 74: check_node_available

**Function 定义**：
```python
{
    "name": "check_node_available",
    "description": "检查Node.js环境是否可用。\n\n使用场景：\n- 当用户需要确认Node.js环境是否安装时使用\n- 当用户在执行JavaScript代码前需要验证环境时使用\n\n参数说明：\n- 无参数\n\n【重要】返回Node.js环境是否可用（true/false）及版本信息",
    "parameters": {
        "type": "object",
        "properties": {},
        "required": []
    }
}
```

---

#### Tool 75: check_module_available

**Function 定义**：
```python
{
    "name": "check_module_available",
    "description": "检查Python模块是否已安装。\n\n使用场景：\n- 当用户需要确认某个Python模块是否已安装时使用\n- 当用户在导入模块前需要验证是否可用时使用\n\n参数说明：\n- module_name：模块名称\n\n【重要】返回模块是否已安装（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "module_name": {
                "type": "string",
                "description": "模块名称。如 pandas、numpy、matplotlib"
            }
        },
        "required": ["module_name"]
    }
}
```

---

#### Tool 76: validate_csv_format

**Function 定义**：
```python
{
    "name": "validate_csv_format",
    "description": "验证CSV文件格式是否正确。\n\n使用场景：\n- 当用户需要确认CSV文件格式是否正确时使用\n- 当用户在读取CSV前需要验证文件完整性时使用\n\n参数说明：\n- file_path：CSV文件路径\n\n【重要】返回CSV文件格式是否正确（true/false）及错误信息",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "CSV文件路径。如 D:/data/users.csv"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 77: validate_chart_data

**Function 定义**：
```python
{
    "name": "validate_chart_data",
    "description": "验证图表数据格式是否正确。\n\n使用场景：\n- 当用户需要确认图表数据格式是否正确时使用\n- 当用户在生成图表前需要验证数据时使用\n\n参数说明：\n- data：图表数据（JSON格式）\n\n【重要】返回数据格式是否正确（true/false）及错误信息",
    "parameters": {
        "type": "object",
        "properties": {
            "data": {
                "type": "object",
                "description": "图表数据（JSON格式）。检查是否包含必要的 labels 和 values 字段"
            }
        },
        "required": ["data"]
    }
}
```

---

#### Tool 78: check_pdf_readable

**Function 定义**：
```python
{
    "name": "check_pdf_readable",
    "description": "检查PDF文件是否可读。\n\n使用场景：\n- 当用户需要确认PDF文件是否可读取时使用\n- 当用户在读取PDF前需要验证文件是否损坏时使用\n\n参数说明：\n- file_path：PDF文件路径\n\n【重要】返回PDF文件是否可读（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "PDF文件路径。如 D:/documents/report.pdf"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 79: check_docx_readable

**Function 定义**：
```python
{
    "name": "check_docx_readable",
    "description": "检查Word文件是否可读。\n\n使用场景：\n- 当用户需要确认Word文件是否可读取时使用\n- 当用户在读取Word前需要验证文件是否损坏时使用\n\n参数说明：\n- file_path：Word文件路径\n\n【重要】返回Word文件是否可读（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "Word文件路径。如 D:/documents/report.docx"
            }
        },
        "required": ["file_path"]
    }
}
```

---

#### Tool 80: check_xlsx_readable

**Function 定义**：
```python
{
    "name": "check_xlsx_readable",
    "description": "检查Excel文件是否可读。\n\n使用场景：\n- 当用户需要确认Excel文件是否可读取时使用\n- 当用户在读取Excel前需要验证文件是否损坏时使用\n\n参数说明：\n- file_path：Excel文件路径\n\n【重要】返回Excel文件是否可读（true/false）",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "Excel文件路径。如 D:/data/report.xlsx"
            }
        },
        "required": ["file_path"]
    }
}
```

---

**更新时间**: 2026-04-04 21:45:00
