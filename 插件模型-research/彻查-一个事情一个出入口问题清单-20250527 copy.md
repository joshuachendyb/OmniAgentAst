# 彻查报告："一个事情一个出入口"原则违反

---
彻查编号	添加引用	问题
[问题1.15]	=[清查:2.30]	chat_stream/init.py重导出
[问题1.16][1.17][1.18]	=[清查:2.20]	db连接逻辑重复
[问题1.22][1.25][1.26]	=[清查:2.19]	get_task_tracker同名冲突
[问题1.32]	=[清查:2.28]	get_system_info语义冲突
[问题1.35]	=[清查:2.21]	工具结果构建
[问题1.37]	=[清查:2.23]	截断函数4入口
[问题1.44]	=[清查:2.27]	文件哈希双入口
[问题1.54]	=[清查:2.22][2.29]	重试机制

清查(2.x)	彻查([问题1.x])	内容
2.19	1.22/1.25/1.26	get_task_tracker同名不同返回类型
2.20	1.16/1.17/1.18	db/chat_db与operations_db逻辑重复
2.21	1.35	工具结果构建build_xxx vs create_xxx
2.22	1.54	重试机制（清查4套，彻查RetryEngine/RetryController）
2.23	1.37	截断函数4入口
2.27	1.44	文件哈希双入口
2.28	1.32	get_system_info语义冲突
2.29	1.54	重试机制重叠
2.30	1.15	chat_stream/init.py重导出零使用

不重叠的部分
清查独有（21个）：
2.1 Fallback配置、2.2 LLM解析重复、2.32.10 未处理问题、2.11~2.18 已完成重构、2.24 Prompt构建、2.25 Fallback三重、2.26 超时配置 — 这些是重构执行状态总结，来自CRSS意图归类、llm_core拆分、safety横切等非"一个出入口"类工作

彻查独有（45个）：
1.1, 1.14 死函数标记（get_api_key、extract_display_name等）、
1.19 1.21 路由函数private化、
1.24 死引擎、
1.29 map_intent_to_agent、
1.33, 1.34 死函数、
1.38,1.42 file_helpers死函数集群、
1.451.53 各模块死代码 — 彻查粒度更细，逐个函数标记死代码和private化

`## app/ 根目录

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

[问题1.1]- **get_api_key**: `backend\app\config.py:206` — 便捷函数，零外部引用，应删除或改private
[问题1.2]- **get_log_config**: `backend\app\config.py:214` — 便捷函数，零外部引用，应删除或改private

### [SLAP] 同职责多入口

[问题1.3]-- **配置获取**: config.py 同时暴露 `get_config()` (Config实例) + `get()` (快捷键值获取) + `get_api_key()` + `get_max_steps()` + `get_log_config()` — 5个入口做同一件事（读配置），其中2个无外部引用

---

## app/api/v1/

### [DRY] 同名函数重复

无（FastAPI路由装饰器注册，框架调用不算死代码）

### [YAGNI] 零引用死函数

[问题1.4]- **generate_execution_stream**: `execution.py:43` — 无装饰器、无外部引用，仅被同文件get_execution_stream调用。应改private `_generate_execution_stream`
[问题1.5]- **extract_display_name_from_steps**: `sessions.py:31` — 零外部引用，仅同文件调用1次。应改private或移至utils/display_name_cache.py
[问题1.6]- **get_utc_timestamp**: `sessions.py:98` — 零外部引用，仅同文件调用5次。应改private
[问题1.7]- **get_timestamp_ms**: `sessions.py:102` — 零外部引用，仅同文件调用3次。应改private
[问题1.8]- **check_db_fields_exist**: `sessions.py:107` — 零外部引用，仅同文件调用5次。应改private
[问题1.9]- **extract_metadata**: `sessions.py:597` — 零外部引用，仅同文件调用2次。应改private
[问题1.10]- **ordered_dict**: `routes.py:104` — 零外部引用，仅同文件调用。应改private
[问题1.11]- **write_yaml_with_order**: `routes.py:148` — 零外部引用，仅同文件调用。应改private

### [SLAP] 同职责多入口

[问题1.12]- **DB连接管理**: sessions.py 的 `get_db_connection` (contextmanager) 与 chat_db.py 的 `get_connection` (raw) — 两个入口做同一件事，sessions.py版仅本文件使用，应改private

---

## app/chat_stream/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

[问题1.13]- **create_error_from_exception**: `error_handler.py:232` — 零外部引用，零内部调用。真死代码
[问题1.14]- **mark_session_valid**: `message_saver.py:29` — 零外部引用，零内部调用。真死代码
[问题1.15]- **整个 chat_stream/__init__.py 的重导出** =[清查:2.30]: 所有人都从子模块直接import（如 `from app.chat_stream.sse_formatter import ...`），无人从包级 `from app.chat_stream import ...` 导入。所有重导出均为YAGNI违反

### [SLAP] 同职责多入口

无

---

## app/db/

### [DRY] 同名函数重复

[问题1.16]- **get_connection** =[清查:2.20]: 出现在 `chat_db.py:11` 和 `operations_db.py:12` — 同名但连接不同DB（chat_history.db vs operations.db），逻辑几乎相同（WAL+busy_timeout），违反DRY。应抽取为 `db.base.get_connection(db_path)` 统一入口
[问题1.17]- **init_database** =[清查:2.20]: 出现在 `chat_db.py:27` 和 `operations_db.py:27` — 同名但建不同表，模式高度重复

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

[问题1.18]- **数据库连接获取** =[清查:2.20]: `chat_db.get_connection` 与 `operations_db.get_connection` — 同职责双入口，应统一为参数化单入口

---

## app/services/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

[问题1.19]- **route_with_fallback**: `chat_router.py:68` — 零外部引用，仅同文件调用2次。应改private
[问题1.20]- **dispatch_sse_event**: `react_sse_wrapper.py:142` — 零外部引用，仅同文件调用1次。应改private

### [SLAP] 同职责多入口

[问题1.21]- **意图路由**: `chat_router.route_with_fallback` (CRSS+fallback) 与 `chat_router.chat_stream_v2` (主入口) — 两个public入口做意图路由+流式生成，`route_with_fallback` 应private

---

## app/services/agent/

### [DRY] 同名函数重复

[问题1.22]- **get_task_tracker** =[清查:2.19]: 出现在 `session.py:186` 和 `mixins/task_tracker.py:119` — 同名不同返回类型！前者返回 `FileOperationSessionService`，后者返回 `TaskExecutionTracker`。严重违反"一个事情一个出入口"
[问题1.23]- **get_all_intent_types**: 出现在 `agent_config.py:107` 和 `intents/intent_mapper.py:140` — 同功能两处定义（见intents目录报告）

### [YAGNI] 零引用死函数

[问题1.24]- **get_tool_retry_engine**: `tool_retry_engine.py:245` — 零外部引用，仅同文件内使用。应改private

### [SLAP] 同职责多入口

[问题1.25]- **任务追踪器获取** =[清查:2.19]: `session.get_task_tracker` (→FileOperationSessionService) 与 `mixins/task_tracker.get_task_tracker` (→TaskExecutionTracker) — 同名不同类型双入口，调用者必须知道从哪个路径导入才能得到正确类型
[问题1.26]- **会话服务获取** =[清查:2.19]: `session.get_task_tracker` + 别名 `get_session_service` — 同一函数两个public名称，违反DRY

---

## app/services/agent/mixins/

### [DRY] 同名函数重复

（已在agent/目录中报告）

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无新增

---

## app/services/intents/

### [DRY] 同名函数重复

[问题1.27]- **get_all_intent_types**: `intent_mapper.py:140` — 与 `agent/agent_config.py:107` 同名同功能但实现不同。agent_config版委托intent_mapper版+追加别名，存在循环依赖风险
[问题1.28]- **resolve_category**: `intent_mapper.py:155` — 与 `tools/registry.py:186` 同名。registry版已标记"委托给intent_mapper统一入口"但保留向后兼容——双入口违反原则

### [YAGNI] 零引用死函数

[问题1.29]- **map_intent_to_agent**: `intent_mapper.py:110` — 零外部引用，仅被同文件的resolve_category调用。应改private

### [SLAP] 同职责多入口

[问题1.30]- **意图类型获取**: `agent_config.get_all_intent_types` 与 `intent_mapper.get_all_intent_types` — 两个入口做同一件事
[问题1.31]- **意图→类别解析**: `intent_mapper.resolve_category` 与 `registry.resolve_category` — 两个入口，registry版是兼容委托

---

## app/services/llm/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/services/llm/model_adapters/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/services/prompts/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

[问题1.32]- **系统Prompt获取 vs 系统信息获取** =[清查:2.28]: prompts/middle 导出 `get_system_prompt`，但在6个prompts文件中被 `import get_system_prompt as get_system_info` 重命名。而 `tools/system/system_tools.py` 有真正的 `get_system_info` 函数（获取CPU/内存等系统信息）。两个完全不同的功能被重命名为同一名字，造成语义混淆

---

## app/services/safety/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/services/safety/file/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/services/tools/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

[问题1.33]- **is_success**: `_response.py:202` — 零外部引用（仅同文件内定义）。真死代码
[问题1.34]- **is_alias**: `tool_aliases.py:76` — 零外部引用。真死代码

### [SLAP] 同职责多入口

[问题1.35]- **工具结果构建** =[清查:2.21]: `_response.py` 提供 `build_success/build_error/build_warning`（工具层），`agent/tool_result_utils.py` 提供 `create_tool_result/create_error_tool_result/create_warning_tool_result`（Agent层）。虽然文档声称分层委托，但对调用者而言同职责两套不同命名入口，违反"一个事情一个出入口"。应统一命名（如工具层叫 `build_xxx`，Agent层应叫 `build_xxx_with_metadata` 明确区分）
[问题1.36]- **同名文件**: `services/agent/tool_result_utils.py` 与 `services/tools/tool_result_utils.py` 同名但职责不同，造成导航困惑
[问题1.37]- **截断操作** =[清查:2.23]: 4个截断函数散布不同文件：`truncate_text`(tool_result_utils)、`truncate_value`(common_helper)、`safe_truncate`(tool_result_utils+data_utils)、`truncate_data_for_frontend`(tool_result_utils) — 同职责4入口

---

## app/services/tools/toolhelper/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

[问题1.38]- **check_windows_platform**: `common_helper.py:110` — 零外部引用（仅__init__.py导出，无人使用）。被 `check_win32_platform` 替代
[问题1.39]- **get_file_metadata**: `file_helpers.py:552` — 零外部引用，仅同文件调用。应改private
[问题1.40]- **calculate_distribution**: `file_helpers.py:624` — 零外部引用，真死代码
[问题1.41]- **is_binary_file**: `file_helpers.py:699` — 零外部引用，真死代码
[问题1.42]- **is_content_identical**: `file_helpers.py:732` — 零外部引用，真死代码

### [SLAP] 同职责多入口

[问题1.43]- **平台检查**: `check_windows_platform` (common_helper.py, 仅检查OS) 与 `check_win32_platform` (window_helper.py, 检查OS+pywin32) — 同职责双入口，前者已被后者替代但未删除
[问题1.44]- **文件哈希计算** =[清查:2.27]: `get_file_hash` (file_helpers.py, 返回Dict包装) 与 `compute_file_hash` (hash_helper.py, 返回hexdigest) — 同职责不同命名不同接口双入口

---

## app/services/tools/file/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

[问题1.45]- **get_all_file_tool_names**: `file_register.py:350` — 零外部引用。真死代码
[问题1.46]- **get_tool_input_models**: `file_register.py:355` — 零外部引用。真死代码

### [SLAP] 同职责多入口

无

---

## app/services/tools/shell/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/services/tools/network/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/services/tools/system/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/services/tools/desktop/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/services/tools/document/

### [DRY] 同名函数重复

[问题1.47]- **get_db_schema**: 同一文件内同名函数重复 — `database_tools.py:291` (有类型注解+docstring但无实现体) 被 `database_tools.py:365` (有实现但无类型注解) 静默覆盖。L291是遗弃的签名，Python运行时使用L365版本。这导致注册到tool_registry的get_db_schema丢失了完整类型注解

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/services/tools/meta/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

无

### [SLAP] 同职责多入口

无

---

## app/utils/

### [DRY] 同名函数重复

无

### [YAGNI] 零引用死函数

[问题1.48]- **classify_error**: `error_classifier.py:330` — 零外部引用
[问题1.49]- **get_error_info**: `error_classifier.py:335` — 零外部引用
[问题1.50]- **整个 error_classifier.py 文件**: 零import、零外部引用，是完整死文件
[问题1.51]- **clear_all_cache**: `display_name_cache.py:72` — 零外部引用
[问题1.52]- **wrap_with_idle_timeout**: `idle_timeout.py:164` — 零外部引用。IdleTimeout类也零引用
[问题1.53]- **record_error**: `monitoring.py:243` — 零外部引用

### [SLAP] 同职责多入口

[问题1.54]- **重试控制** =[清查:2.22][2.29]: `RetryEngine` (retry_engine.py, 有策略枚举+attempt记录) 与 `RetryController` (retry_controller.py, 简单计数器) — 同职责两个类两个入口，两者都有外部使用但职责重叠

---

## 跨目录汇总: import路径重复

| 问题序号 | 名称 | 路径1 | 路径2 | 问题 |
|---------|------|-------|-------|------|
| [问题1.16][1.18] =[清查:2.20] | `get_connection` | `app.db.chat_db` | `app.db.operations_db` | 同名不同DB，逻辑重复 |
| [问题1.22][1.25][1.26] =[清查:2.19] | `get_task_tracker` | `app.services.agent.session` | `app.services.agent.mixins.task_tracker` | 同名不同返回类型 |
| [问题1.23][1.27][1.30] | `get_all_intent_types` | `app.services.agent.agent_config` | `app.services.intents.intent_mapper` | 同功能两定义 |
| [问题1.28][1.31] | `resolve_category` | `app.services.tools.registry` | `app.services.intents.intent_mapper` | 双入口（registry版已标记兼容委托） |
| [问题1.32] =[清查:2.28] | `get_system_info` | `app.services.tools.system.system_tools` | `app.services.prompts.middle (as alias)` | 语义混淆: 系统信息工具 vs 系统Prompt |
| [问题1.44] =[清查:2.27] | `get_file_hash` | `app.services.tools.toolhelper.file_helpers` | (委托hash_helper.compute_file_hash) | 同职责不同命名 |
| [问题1.43] | `check_windows_platform` | `app.services.tools.toolhelper.common_helper` | `check_win32_platform in window_helper` | 同职责双入口 |

---

## 严重度排序 TOP 10

| 问题序号 | 排名 | 类型 | 问题 | 位置 |
|---------|------|------|------|------|
| [问题1.47] | 1 | DRY | `get_db_schema` 同文件内同名函数，L291无实现被L365覆盖，丢失类型注解 | `database_tools.py:291,365` |
| [问题1.22] =[清查:2.19] | 2 | DRY | `get_task_tracker` 同名但返回不同类型，调用者按import路径得不同对象 | `session.py:186` vs `task_tracker.py:119` |
| [问题1.32] =[清查:2.28] | 3 | SLAP | `get_system_prompt as get_system_info` 与 `system_tools.get_system_info` 语义冲突 | 6个prompts文件 vs `system_tools.py` |
| [问题1.50] | 4 | YAGNI | 整个 `error_classifier.py` 零引用死文件（13KB） | `utils/error_classifier.py` |
| [问题1.16][1.17][1.18] =[清查:2.20] | 5 | DRY | `get_connection` + `init_database` 在chat_db/operations_db中逻辑重复 | `db/chat_db.py` vs `db/operations_db.py` |
| [问题1.15] =[清查:2.30] | 6 | YAGNI | `chat_stream/__init__.py` 全部重导出零使用 | `chat_stream/__init__.py` |
| [问题1.37] =[清查:2.23] | 7 | SLAP | 4个截断函数同职责4入口 | `tool_result_utils` + `common_helper` + `data_utils` |
| [问题1.35] =[清查:2.21] | 8 | SLAP | `build_xxx` vs `create_xxx` 工具结果构建同职责两套命名 | `_response.py` vs `agent/tool_result_utils.py` |
| [问题1.38][1.39][1.40][1.41][1.42] | 9 | YAGNI | 5个 `file_helpers.py` 死函数: calculate_distribution, is_binary_file, is_content_identical, get_file_metadata(internal), check_windows_platform | `toolhelper/` |
| [问题1.26] =[清查:2.19] | 10 | DRY | `get_session_service` 是 `get_task_tracker` 的别名，同一函数两个public名 | `session.py:194` |
