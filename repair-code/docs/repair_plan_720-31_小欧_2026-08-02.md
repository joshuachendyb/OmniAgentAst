# 修复规划 — 7 月 20~31 误删代码重建 - 小欧 2026-08-02 22:25:00

创建人: 小欧 2026-08-02 22:25:00

## 1. 现场确认 (2026-08-02 22:25)

| 位置 | 状态 | 备注 |
|------|------|------|
| F:\OmniAgentAs-repair (当前) | v0.18.27 基线, feature/repair, 458 backend/app 文件, 工作区干净 | 唯一完整可用的本地 git + 代码 |
| 远端 origin | master=846de161c (7 月 5 日); tag v0.18.27(7 月 20); 无 v0.18.28+ | 远端无后续代码 |
| G:\OmniAgentAs-desk | 7 月 31 误删后残留; backend/app 已空, tests 2343 文件, reports/danger_cases 16 文件 | tests 里风险删除 case 已移出; F 盘 tests 已去风险 case |
| E:\git_rebuild | git repo, 无 7 月 20-31 提交 |
| E:\recovered_git / carved_git / omni_remote.git | 对象库 tree 不完整 (0dcd58213 的 tree cf7cb10308 inflate 失败) | 无法直接 checkout |

**结论**: 7 月 20~31 的系统代码 (v0.18.28~v0.18.39, 263 commits) 从 git 对象层面全部丢失 (tree 均 missing)。唯一完整的 Git 基线是远端 v0.18.27。

## 2. 丢失代码元信息来源 (用于重建)

1. **commit 清单** (263 条): `notes/repair_logs_720-31_commit清单_小欧_2026-08-02.md` —— commit 消息按时间正序, 含改动文件/模块提示, 以及 BUG-# 编号。
2. **changelog**: G:\OmniAgentAs-desk/version.txt 停留 v0.18.39 (7 月 30 日) —— 功能分区/BUG 范围证据。

## 3. 丢失代码覆盖模块 (按提交数)

```
27 execute_shell_command   12 action_handler    11 tool_constants   9 shell_engine
 8 http_request  8 desktop  7 read_pdf  7 logger  7 fetch_webpage
6 query_sql  5 syntax_validator  5 edit_text_file  5 message_builder
5 tool_retry_engine  5 win_registry  ...(见 notes 逐模块统计)
```
类型分布: fix 123 / refactor 54 / docs 44 / feat 22 / perf 4 / test 3

重点: `execute_shell_command` (CMD/管道超时/BUG#1-#7)、`shell_engine`(Singleton→ShellPoolManager)、
`tool_constants`(门限治理)、`http_request`(URL/ASCII转码)、`win_registry`(registry_write)、
`read_pdf`(BUG-001 表格结构化)、desktop 13-19 bug、`syntax_validator`(语法护栏)、`compress_files`(超时)、
`fetch_webpage`(Jina 403)。

## 4. 重建路线 (方案 A, 详见聊天)

- **阶段 0**: 修 `git fsck` 坏 archive refs/stash (孤立到 refs/archive + 打 tag 快照), 使 feature/repair 后续 `git gc/log --all` 干净。
- **阶段 1**: 沿 263 commit 顺序, 以 v0.18.27 基线为底, 逐模块回放 fix/feat/refactor。
- **阶段 2**: 回归 — 用 G 盘 tests (2363 文件) + F 盘干净 tests 回归回放; 风险 case 保留在 G 盘 reports/danger_cases, 不并入 F 盘。

## 5. 约定

- 重建提交标题沿原格式: `<type>:<file> desc - 署名-日期`, types: fix/refactor/feat/docs/perf。
- 仅回放系统代码 (backend/app), 不并入删除风险的测试。

## 6. 阶段 0 选项 (等待用户确认) — 小欧 2026-08-02 22:40:00

用于清理 F 盘 repo 的坏 refs/archive/* + refs/stash，使 `git fsck/log --all` 干净，为后续回放铺路。**不动 feature/repair 代码与远端。**

- **方案 A. 允**：给坏 refs/archive/* + refs/stash 裹 tag 快照 (sha1 不丢, 元消息留作参考) → 删除坏指针 → `git gc --prune=now`。
- **方案 B. 先看关键 commit**：先输出 v0.18.28~v0.18.39 之间的版本提交 + BUG-# 序列，决定回放优先级；暂不动 refs。
- **方案 C. 调整路线**：另有想法，告诉我方位。

## 8. 用户指令 + 重建路线 (2026-08-02 21:52:44)
- 指示: **不**用 E:\tmp_rec 磁盘恢复文件 (判定为不完整); 而是**从 commit 重建**。
  - 基线: F 盘当前 v0.18.27 (feature/repair, HEAD c5d5bdfa1)。
  - 263 commit tree 均 missing -> 仅凭 commit 消息+version.txt changelog+G 盘 tests 作为规范。
- 263 commits 归类 fix-family:
  | 数量 | 范畴 | 代表文件/commit |
  | 33 | execute_shell_command(Shell池/BUG#1-7/Exit码) | shell_engine->ShellPoolManager, execute_shell_command |
  | 17 | dataanalysis(SQL安全/CTE/PRAGMA/limit) | query_sql/execute_sql/get_db_schema/_strip |
  | 16 | react_cycle/action_handler(安全确认/观察) | auto_confirm, tool_safety_checker, observation |
  | 14 | http_request/URL ASCII转码/Jina 403 | transcode_url |
  | 12 | document read_pdf/docx/pptx 自然单位翻页/BUG-001 | read_pdf _process_page |
  | 11 | LLM stream/reasoning finish_reason/trim_history | base_service/llm_stream/message_builder |
  |  7 | tool_constants 门限治理 | OBS_/INER_/compress hint |
  |  7 | database/db卡死/sqlite3.Row | database/task_db/stream |
  |  6 | win_registry registry_write 0x-hex/负数转无符号 | registry_write |
  |  4 | syntax_validator 语法护栏/BOM | syntax_validator |
  | 10 | tool_search/write 文档工具 | write_xlsx,pptx,docx,compress_files |
- 建议重建顺序 (按基础服务依赖): LLM流/安全确认 -> SQL dataanalysis -> execute_shell_command(Shell池) -> http_request/URL转码 -> read_pdf文档翻页 -> tool_constants门限 -> win_registry -> syntax_validator -> tool_search/文档工具 -> database卡死.
