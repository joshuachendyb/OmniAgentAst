# 中断修复清单

## 立即修复（高优先级）

### 后端修复
1. **文件**: `backend/app/services/llm_core.py`
   - **修改**: 第197行，将 `timeout=5.0` 改为 `timeout=1.0`
   - **影响**: 中断响应时间从5秒减少到1秒

2. **文件**: `backend/app/services/react_sse_wrapper.py`
   - **修改**: 第625行，注释掉 `del running_tasks[task_id]`
   - **修改**: 添加任务标记为已取消，保留在字典中
   - **影响**: 防止中断检查失败

### 前端修复
3. **文件**: `frontend/src/components/Chat/NewChatContainer.tsx`
   - **修改**: 第1867-1869行，将UI状态更新移到API调用成功之后
   - **影响**: 修复竞态条件，确保状态一致性

4. **文件**: `frontend/src/components/Chat/NewChatContainer.tsx`
   - **修改**: 第1902行，将 `hasReceivedInterruptEventRef.current = false` 移到 `disconnect` 回调中
   - **影响**: 防止中断事件丢失

## 本周内修复（中优先级）

### 后端修复
5. **文件**: `backend/app/services/agent/base_react.py`
   - **修改**: 在工具执行前后添加中断检查
   - **影响**: 提高中断响应及时性

6. **文件**: `backend/app/services/react_sse_wrapper.py`
   - **修改**: 在 `cleanup_expired_tasks` 函数中添加对已取消任务的清理
   - **影响**: 防止内存泄漏

### 前端修复
7. **文件**: `frontend/src/components/Chat/NewChatContainer.tsx`
   - **修改**: 添加 `waitForInterruptEvent` 函数，替换硬编码的重试逻辑
   - **影响**: 更可靠的中断事件检测

8. **文件**: `frontend/src/utils/sse.ts`
   - **修改**: 第1358-1364行，为 `interrupted` 事件添加专门处理逻辑
   - **影响**: 更好的中断事件处理

## 下个版本修复（低优先级）

### 前端增强
9. **文件**: 新建 `frontend/src/utils/interruptMonitor.ts`
   - **内容**: 创建中断监控类
   - **影响**: 便于调试和监控中断过程

10. **文件**: `frontend/src/components/Chat/NewChatContainer.tsx`
    - **修改**: 添加中断进度反馈和重试机制
    - **影响**: 改善用户体验

## 测试验证

### 后端测试
11. **文件**: 新建 `backend/tests/test_interrupt.py`
    - **内容**: 添加中断功能单元测试
    - **影响**: 确保中断功能稳定

### 前端测试
12. **文件**: 新建 `frontend/src/tests/interrupt.test.tsx`
    - **内容**: 添加中断功能前端测试
    - **影响**: 确保前端中断逻辑正确

## 部署顺序

### 第一阶段（立即）
1. 后端：修改 `llm_core.py`（1分钟）
2. 后端：修改 `react_sse_wrapper.py`（2分钟）
3. 前端：修改 `NewChatContainer.tsx` 竞态条件（3分钟）
4. 前端：修改 `NewChatContainer.tsx` 标记重置（2分钟）

### 第二阶段（本周）
5. 后端：修改 `base_react.py`（10分钟）
6. 后端：修改 `cleanup_expired_tasks`（5分钟）
7. 前端：添加 `waitForInterruptEvent` 函数（10分钟）
8. 前端：修改 `sse.ts`（5分钟）

### 第三阶段（下版本）
9. 前端：创建 `interruptMonitor.ts`（15分钟）
10. 前端：添加进度反馈（20分钟）
11. 后端：添加中断测试（30分钟）
12. 前端：添加中断测试（30分钟）

## 验证步骤

### 部署后验证
1. 启动前后端服务
2. 发起一个长时间运行的任务（如文件搜索）
3. 点击中断按钮
4. 验证中断响应时间（应<1秒）
5. 验证UI状态更新正确
6. 验证任务确实停止

### 压力测试
1. 并发发起多个任务
2. 同时中断多个任务
3. 验证系统稳定性
4. 检查内存使用情况

## 回滚步骤

### 如果出现问题
1. 恢复 `llm_core.py` 的第197行
2. 恢复 `react_sse_wrapper.py` 的第625行
3. 恢复 `NewChatContainer.tsx` 的修改
4. 重启服务

## 监控指标

### 部署后监控
1. **中断成功率**: 目标 >95%
2. **平均响应时间**: 目标 <1秒
3. **错误率**: 目标 <1%
4. **内存使用**: 无明显增长

### 日志检查
1. 检查 `[中断]` 相关日志
2. 检查 `[TaskControl]` 相关日志
3. 检查错误日志中的中断相关错误

## 负责人
- 后端修复: 小沈
- 前端修复: 小新
- 测试验证: 小强
- 部署监控: 老杨