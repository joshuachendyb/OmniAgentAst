# 测试记录-E2E-SN-03-2026-06-19

**创建时间**: 2026-06-19 11:37:14
**测试编号**: E2E-SN-03
**测试结果**: PASSED

---

## 1 测试基本信息

| 项目 | 内容 |
|------|------|
| 测试编号 | E2E-SN-03 |
| 任务描述 | 技术调研流程 |
| 用户命令 | `我要调研Python异步编程：①搜索Python asyncio最佳实践 ②打开第一个结果的网页详细内容 ③执行一段asyncio示例代码验证 ④把调研结果和代码运行结果保存报告 ⑤列出报告文件确认，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中` |
| 执行时间 | 2026-06-19 11:37:14 |
| 执行耗时 | 261.0秒 |
| SSE总事件数 | 576 |
| LLM调用次数 | 17 |
| 逻辑步数 | 50 |
| 不重复步骤号数 | 17 |
| 测试结果 | **PASSED** |

## 2 LLM回复内容

```
报告文件已确认保存成功！文件 `python_asyncio_research_report.md`（9169字节）已保存在 `G:\OmniAgentAs-desk\backend\reports\` 目录下。

---

## 📋 任务完成汇总

所有5个步骤已全部完成：

| 步骤 | 任务 | 状态 | 详情 |
|------|------|------|------|
| ① | 搜索Python asyncio最佳实践 | ✅ 完成 | 搜索到5条结果，来自PythonAcademy、Real Python、张欣耕博客、CSDN等 |
| ② | 打开第一个结果的网页详细内容 | ✅ 完成 | 获取了PythonAcademy和shanechang.com的详细中文资料 |
| ③ | 执行asyncio示例代码验证 | ✅ 完成 | 创建并运行了5个示例（基础协程、gather并发、上下文管理器、取消任务、TaskGroup），全部通过 |
| ④ | 保存调研报告 | ✅ 完成 | 报告已保存至 `G:\OmniAgentAs-desk\backend\reports\python_asyncio_research_report.md`（9169字节） |
| ⑤ | 列出报告文件确认 | ✅ 完成 | 确认reports目录下共59个文件，包含本次生成的报告 |

**关键发现**：
- asyncio并发执行相比串行执行性能提升约 **42%**（2.01秒 vs 3.5秒）
- 5个最佳实践均已代码验证通过
- Python 3.13.11 完整支持 asyncio 所有特性包括 TaskGroup
```

## 3 工具调用链

tool_search -> search_web -> fetch_webpage -> fetch_webpage -> fetch_webpage -> execute_code -> execute_shell_command -> execute_code -> execute_shell_command -> write_text_file -> execute_shell_command -> edit_text_file -> execute_shell_command -> execute_shell_command -> write_text_file -> list_directory

| 序号 | 工具名 | 参数 |
|------|--------|------|
| 1 | tool_search | `{"query": "搜索网页 抓取URL内容"}` |
| 2 | search_web | `{"num_results": 5, "query": "Python asyncio最佳实践 best practices"}` |
| 3 | fetch_webpage | `{"url": "https://pythonacademy.io/articles/python-asyncio-best-practices", "prompt": "提取Python asyncio最佳实践的所有要点、代码示例和建议"}` |
| 4 | fetch_webpage | `{"prompt": "提取Python asyncio完整教程的所有要点、核心概念、代码示例、最佳实践、常见陷阱", "url": "https://realpython.com/async-io-python/"}` |
| 5 | fetch_webpage | `{"prompt": "提取Python asyncio完整教程的所有要点、核心概念、代码示例、最佳实践、常见陷阱", "url": "https://shanechang.com/zh-cn/p/python-asyncio-best-practices-pitfalls/"}` |
| 6 | execute_code | `{"code": "import asyncio\nimport time\n\n# ========== 示例1: 基础asyncio用法 ==========\nprint(\"=\" * 60)\nprint(\"示例1: 基础asyncio协程\")\nprint(\"=\" * 60)\n\nasync def fetch_data(url: str, delay: float):\n    \"\"\"模拟异步获取数据\"\"\"\n    print(f\"[{time.strftime('%H:%M:%S')}] 开始获取 {url}\")\n    await asyncio.sleep(delay)  # 模拟网络延迟\n    print(f\"[{time.strftime('%H:%M:%S')}] 完成获取 {url}\")\n    return f\"{url} 的数据\"\n\nasync def basic_example():\n    result = await fetch_data(\"https://example.com/api\", 1)\n    print(f\"结果: {result}\")\n\nasyncio.run(basic_example())\n\n# ========== 示例2: 并发执行多个协程 ==========\nprint(\"\\n\" + \"=\" * 60)\nprint(\"示例2: asyncio.gather 并发执行\")\nprint(\"=\" * 60)\n\nasync def concurrent_example():\n    start = time.time()\n    tasks = [\n        fetch_data(\"https://api.github.com\", 1),\n        fetch_data(\"https://api.python.org\", 2),\n        fetch_data(\"https://api.docs.python.org\", 0.5),\n    ]\n    results = await asyncio.gather(*tasks)\n    elapsed = time.time() - start\n    print(f\"\\n并发执行总耗时: {elapsed:.2f}秒\")\n    print(f\"结果: {results}\")\n\nasyncio.run(concurrent_example())\n\n# ========== 示例3: 异步上下文管理器 ==========\nprint(\"\\n\" + \"=\" * 60)\nprint(\"示例3: 异步上下文管理器\")\nprint(\"=\" * 60)\n\nclass AsyncConnection:\n    \"\"\"异步资源管理器\"\"\"\n    def __init__(self, name: str):\n        self.name = name\n    \n    async def __aenter__(self):\n        print(f\"[{time.strftime('%H:%M:%S')}] 连接 {self.name}\")\n        await asyncio.sleep(0.3)\n        return self\n    \n    async def __aexit__(self, exc_type, exc_val, exc_tb):\n        print(f\"[{time.strftime('%H:%M:%S')}] 关闭 {self.name}\")\n        await asyncio.sleep(0.1)\n        return False\n\nasync def context_manager_example():\n    async with AsyncConnection(\"DB-Pool-1\") as conn:\n        print(f\"正在使用连接: {conn.name}\")\n        await asyncio.sleep(0.2)\n        print(f\"查询完成\")\n\nasyncio.run(context_manager_example())\n\n# ========== 示例4: 优雅取消任务 ==========\nprint(\"\\n\" + \"=\" * 60)\nprint(\"示例4: 优雅取消任务 (CancelledError)\")\nprint(\"=\" * 60)\n\nasync def cancellable_task(task_name: str):\n    try:\n        for i in range(5):\n            print(f\"[{task_name}] 进行中... ({i+1}/5)\")\n            await asyncio.sleep(0.3)\n    except asyncio.CancelledError:\n        print(f\"[{task_name}] 收到取消信号，正在清理资源...\")\n        raise\n\nasync def cancellation_example():\n    task = asyncio.create_task(cancellable_task(\"Worker-A\"))\n    await asyncio.sleep(1)  # 运行1秒后取消\n    print(f\"\\n[{time.strftime('%H:%M:%S')}] 正在取消 Worker-A...\")\n    task.cancel()\n    try:\n        await task\n    except asyncio.CancelledError:\n        print(f\"Worker-A 已取消\")\n\nasyncio.run(cancellation_example())\n\n# ========== 示例5: asyncio.TaskGroup (Python 3.11+) ==========\nprint(\"\\n\" + \"=\" * 60)\nprint(\"示例5: asyncio.TaskGroup 管理任务组\")\nprint(\"=\" * 60)\n\nasync def task_group_example():\n    async def worker(name: str, delay: float):\n        print(f\"[Worker-{name}] 开始工作，耗时{delay}秒\")\n        await asyncio.sleep(delay)\n        print(f\"[Worker-{name}] 完成\")\n        return f\"Worker-{name} 完成\"\n    \n    try:\n        async with asyncio.TaskGroup() as tg:\n            t1 = tg.create_task(worker(\"A\", 0.5))\n            t2 = tg.create_task(worker(\"B\", 0.8))\n            t3 = tg.create_task(worker(\"C\", 0.3))\n        \n        print(f\"\\n所有任务完成: {[t.result() for t in tg.tasks]}\")\n    except* Exception as eg:\n        print(f\"任务组中发生错误: {eg}\")\n\ntry:\n    asyncio.run(task_group_example())\nexcept AttributeError:\n    print(\"(跳过: TaskGroup 需要 Python 3.11+)\")\n\nprint(\"\\n\" + \"=\" * 60)\nprint(\"✅ 所有示例执行完毕!\")\nprint(\"=\" * 60)\n", "timeout": 30}` |
| 7 | execute_shell_command | `{"command": "python --version"}` |
| 8 | execute_code | `{"code": "import asyncio\nimport time\n\nprint(\"=\" * 60)\nprint(\"示例1: 基础asyncio协程\")\nprint(\"=\" * 60)\n\nasync def fetch_data(url, delay):\n    print(f\"[{time.strftime('%H:%M:%S')}] 开始获取 {url}\")\n    await asyncio.sleep(delay)\n    print(f\"[{time.strftime('%H:%M:%S')}] 完成获取 {url}\")\n    return f\"{url} 的数据\"\n\nasync def basic_example():\n    result = await fetch_data(\"https://example.com/api\", 1)\n    print(f\"结果: {result}\")\n\nasyncio.run(basic_example())\n\nprint(\"\\n\" + \"=\" * 60)\nprint(\"示例2: asyncio.gather 并发执行\")\nprint(\"=\" * 60)\n\nasync def concurrent_example():\n    start = time.time()\n    tasks = [\n        fetch_data(\"https://api.github.com\", 1),\n        fetch_data(\"https://api.python.org\", 2),\n        fetch_data(\"https://api.docs.python.org\", 0.5),\n    ]\n    results = await asyncio.gather(*tasks)\n    elapsed = time.time() - start\n    print(f\"\\n并发执行总耗时: {elapsed:.2f}秒\")\n    print(f\"结果: {results}\")\n\nasyncio.run(concurrent_example())\n\nprint(\"\\n\" + \"=\" * 60)\nprint(\"示例3: 异步上下文管理器\")\nprint(\"=\" * 60)\n\nclass AsyncConnection:\n    def __init__(self, name):\n        self.name = name\n    \n    async def __aenter__(self):\n        print(f\"[{time.strftime('%H:%M:%S')}] 连接 {self.name}\")\n        await asyncio.sleep(0.3)\n        return self\n    \n    async def __aexit__(self, exc_type, exc_val, exc_tb):\n        print(f\"[{time.strftime('%H:%M:%S')}] 关闭 {self.name}\")\n        await asyncio.sleep(0.1)\n        return False\n\nasync def context_manager_example():\n    async with AsyncConnection(\"DB-Pool-1\") as conn:\n        print(f\"正在使用连接: {conn.name}\")\n        await asyncio.sleep(0.2)\n        print(f\"查询完成\")\n\nasyncio.run(context_manager_example())\n\nprint(\"\\n\" + \"=\" * 60)\nprint(\"示例4: 优雅取消任务\")\nprint(\"=\" * 60)\n\nasync def cancellable_task(task_name):\n    try:\n        for i in range(5):\n            print(f\"[{task_name}] 进行中... ({i+1}/5)\")\n            await asyncio.sleep(0.3)\n    except asyncio.CancelledError:\n        print(f\"[{task_name}] 收到取消信号，正在清理资源...\")\n        raise\n\nasync def cancellation_example():\n    task = asyncio.create_task(cancellable_task(\"Worker-A\"))\n    await asyncio.sleep(1)\n    print(f\"\\n[{time.strftime('%H:%M:%S')}] 正在取消 Worker-A...\")\n    task.cancel()\n    try:\n        await task\n    except asyncio.CancelledError:\n        print(f\"Worker-A 已取消\")\n\nasyncio.run(cancellation_example())\n\nprint(\"\\n\" + \"=\" * 60)\nprint(\"示例5: asyncio.TaskGroup\")\nprint(\"=\" * 60)\n\nasync def task_group_example():\n    async def worker(name, delay):\n        print(f\"[Worker-{name}] 开始工作，耗时{delay}秒\")\n        await asyncio.sleep(delay)\n        print(f\"[Worker-{name}] 完成\")\n        return f\"Worker-{name} 完成\"\n    \n    async with asyncio.TaskGroup() as tg:\n        t1 = tg.create_task(worker(\"A\", 0.5))\n        t2 = tg.create_task(worker(\"B\", 0.8))\n        t3 = tg.create_task(worker(\"C\", 0.3))\n    \n    print(f\"\\n所有任务完成: {[t.result() for t in tg.tasks]}\")\n\nasyncio.run(task_group_example())\n\nprint(\"\\n\" + \"=\" * 60)\nprint(\"✅ 所有示例执行完毕!\")\nprint(\"=\" * 60)", "timeout": 30}` |
| 9 | execute_shell_command | `{"command": "python -c \"\nimport asyncio\nimport time\n\nprint('=' * 60)\nprint('示例1: 基础asyncio协程')\nprint('=' * 60)\n\nasync def fetch_data(url, delay):\n    print(f'[{time.strftime(\\\"%H:%M:%S\\\")}] 开始获取 {url}')\n    await asyncio.sleep(delay)\n    print(f'[{time.strftime(\\\"%H:%M:%S\\\")}] 完成获取 {url}')\n    return f'{url} 的数据'\n\nasync def basic_example():\n    result = await fetch_data('https://example.com/api', 1)\n    print(f'结果: {result}')\n\nasyncio.run(basic_example())\n\nprint()\nprint('=' * 60)\nprint('示例2: asyncio.gather 并发执行')\nprint('=' * 60)\n\nasync def concurrent_example():\n    start = time.time()\n    tasks = [\n        fetch_data('https://api.github.com', 1),\n        fetch_data('https://api.python.org', 2),\n        fetch_data('https://api.docs.python.org', 0.5),\n    ]\n    results = await asyncio.gather(*tasks)\n    elapsed = time.time() - start\n    print(f'并发执行总耗时: {elapsed:.2f}秒')\n    print(f'结果: {results}')\n\nasyncio.run(concurrent_example())\n\nprint()\nprint('=' * 60)\nprint('示例3: 异步上下文管理器')\nprint('=' * 60)\n\nclass AsyncConnection:\n    def __init__(self, name):\n        self.name = name\n    async def __aenter__(self):\n        print(f'[{time.strftime(\\\"%H:%M:%S\\\")}] 连接 {self.name}')\n        await asyncio.sleep(0.3)\n        return self\n    async def __aexit__(self, exc_type, exc_val, exc_tb):\n        print(f'[{time.strftime(\\\"%H:%M:%S\\\")}] 关闭 {self.name}')\n        await asyncio.sleep(0.1)\n        return False\n\nasync def context_manager_example():\n    async with AsyncConnection('DB-Pool-1') as conn:\n        print(f'正在使用连接: {conn.name}')\n        await asyncio.sleep(0.2)\n        print('查询完成')\n\nasyncio.run(context_manager_example())\n\nprint()\nprint('=' * 60)\nprint('示例4: 优雅取消任务')\nprint('=' * 60)\n\nasync def cancellable_task(task_name):\n    try:\n        for i in range(5):\n            print(f'[{task_name}] 进行中... ({i+1}/5)')\n            await asyncio.sleep(0.3)\n    except asyncio.CancelledError:\n        print(f'[{task_name}] 收到取消信号，正在清理资源...')\n        raise\n\nasync def cancellation_example():\n    task = asyncio.create_task(cancellable_task('Worker-A'))\n    await asyncio.sleep(1)\n    print(f'[{time.strftime(\\\"%H:%M:%S\\\")}] 正在取消 Worker-A...')\n    task.cancel()\n    try:\n        await task\n    except asyncio.CancelledError:\n        print('Worker-A 已取消')\n\nasyncio.run(cancellation_example())\n\nprint()\nprint('=' * 60)\nprint('示例5: asyncio.TaskGroup')\nprint('=' * 60)\n\nasync def task_group_example():\n    async def worker(name, delay):\n        print(f'[Worker-{name}] 开始工作，耗时{delay}秒')\n        await asyncio.sleep(delay)\n        print(f'[Worker-{name}] 完成')\n        return f'Worker-{name} 完成'\n    async with asyncio.TaskGroup() as tg:\n        t1 = tg.create_task(worker('A', 0.5))\n        t2 = tg.create_task(worker('B', 0.8))\n        t3 = tg.create_task(worker('C', 0.3))\n    print(f'所有任务完成: {[t.result() for t in tg.tasks]}')\n\nasyncio.run(task_group_example())\n\nprint()\nprint('=' * 60)\nprint('所有示例执行完毕!')\nprint('=' * 60)\n\"\n", "timeout": 30000}` |
| 10 | write_text_file | `{"file_path": "G:\\OmniAgentAs-desk\\backend\\asyncio_demo.py", "content": "import asyncio\nimport time\n\nprint(\"=\" * 60)\nprint(\"示例1: 基础asyncio协程\")\nprint(\"=\" * 60)\n\nasync def fetch_data(url, delay):\n    print(f\"[{time.strftime('%H:%M:%S')}] 开始获取 {url}\")\n    await asyncio.sleep(delay)\n    print(f\"[{time.strftime('%H:%M:%S')}] 完成获取 {url}\")\n    return f\"{url} 的数据\"\n\nasync def basic_example():\n    result = await fetch_data(\"https://example.com/api\", 1)\n    print(f\"结果: {result}\")\n\nasyncio.run(basic_example())\n\nprint()\nprint(\"=\" * 60)\nprint(\"示例2: asyncio.gather 并发执行\")\nprint(\"=\" * 60)\n\nasync def concurrent_example():\n    start = time.time()\n    tasks = [\n        fetch_data(\"https://api.github.com\", 1),\n        fetch_data(\"https://api.python.org\", 2),\n        fetch_data(\"https://api.docs.python.org\", 0.5),\n    ]\n    results = await asyncio.gather(*tasks)\n    elapsed = time.time() - start\n    print(f\"并发执行总耗时: {elapsed:.2f}秒\")\n    print(f\"结果: {results}\")\n\nasyncio.run(concurrent_example())\n\nprint()\nprint(\"=\" * 60)\nprint(\"示例3: 异步上下文管理器\")\nprint(\"=\" * 60)\n\nclass AsyncConnection:\n    def __init__(self, name):\n        self.name = name\n    async def __aenter__(self):\n        print(f\"[{time.strftime('%H:%M:%S')}] 连接 {self.name}\")\n        await asyncio.sleep(0.3)\n        return self\n    async def __aexit__(self, exc_type, exc_val, exc_tb):\n        print(f\"[{time.strftime('%H:%M:%S')}] 关闭 {self.name}\")\n        await asyncio.sleep(0.1)\n        return False\n\nasync def context_manager_example():\n    async with AsyncConnection(\"DB-Pool-1\") as conn:\n        print(f\"正在使用连接: {conn.name}\")\n        await asyncio.sleep(0.2)\n        print(\"查询完成\")\n\nasyncio.run(context_manager_example())\n\nprint()\nprint(\"=\" * 60)\nprint(\"示例4: 优雅取消任务\")\nprint(\"=\" * 60)\n\nasync def cancellable_task(task_name):\n    try:\n        for i in range(5):\n            print(f\"[{task_name}] 进行中... ({i+1}/5)\")\n            await asyncio.sleep(0.3)\n    except asyncio.CancelledError:\n        print(f\"[{task_name}] 收到取消信号，正在清理资源...\")\n        raise\n\nasync def cancellation_example():\n    task = asyncio.create_task(cancellable_task(\"Worker-A\"))\n    await asyncio.sleep(1)\n    print(f\"[{time.strftime('%H:%M:%S')}] 正在取消 Worker-A...\")\n    task.cancel()\n    try:\n        await task\n    except asyncio.CancelledError:\n        print(\"Worker-A 已取消\")\n\nasyncio.run(cancellation_example())\n\nprint()\nprint(\"=\" * 60)\nprint(\"示例5: asyncio.TaskGroup\")\nprint(\"=\" * 60)\n\nasync def task_group_example():\n    async def worker(name, delay):\n        print(f\"[Worker-{name}] 开始工作，耗时{delay}秒\")\n        await asyncio.sleep(delay)\n        print(f\"[Worker-{name}] 完成\")\n        return f\"Worker-{name} 完成\"\n    async with asyncio.TaskGroup() as tg:\n        t1 = tg.create_task(worker(\"A\", 0.5))\n        t2 = tg.create_task(worker(\"B\", 0.8))\n        t3 = tg.create_task(worker(\"C\", 0.3))\n    print(f\"所有任务完成: {[t.result() for t in tg.tasks]}\")\n\nasyncio.run(task_group_example())\n\nprint()\nprint(\"=\" * 60)\nprint(\"所有示例执行完毕!\")\nprint(\"=\" * 60)"}` |
| 11 | execute_shell_command | `{"command": "python asyncio_demo.py", "cwd": "G:\\OmniAgentAs-desk\\backend", "timeout": 30000}` |
| 12 | edit_text_file | `{"file_path": "G:\\OmniAgentAs-desk\\backend\\asyncio_demo.py", "new_string": "    print(\"所有任务完成!\")", "old_string": "    print(f\"所有任务完成: {[t.result() for t in tg.tasks]}\")"}` |
| 13 | execute_shell_command | `{"command": "python asyncio_demo.py", "cwd": "G:\\OmniAgentAs-desk\\backend", "timeout": 30000}` |
| 14 | execute_shell_command | `{"command": "New-Item -ItemType Directory -Force -Path \"G:\\OmniAgentAs-desk\\backend\\reports\""}` |
| 15 | write_text_file | `{"file_path": "G:\\OmniAgentAs-desk\\backend\\reports\\python_asyncio_research_report.md", "content": "# Python asyncio 异步编程调研报告\n\n**调研日期**: 2026-06-19  \n**Python版本**: 3.13.11  \n**调研人**: OmniAgent  \n\n---\n\n## 一、调研概述\n\n本次调研围绕 Python asyncio 异步编程的最佳实践展开，通过搜索网络资料、阅读技术博客、以及实际代码验证，系统性地总结了 asyncio 的核心概念、最佳实践和常见陷阱。\n\n---\n\n## 二、搜索到的参考资料\n\n### 2.1 搜索结果概览\n\n搜索关键词：`Python asyncio最佳实践 best practices`\n\n| 序号 | 标题 | 来源 | URL |\n|------|------|------|-----|\n| 1 | Python Asyncio Best Practices | PythonAcademy | https://pythonacademy.io/articles/python-asyncio-best-practices |\n| 2 | Python's asyncio: A Hands-On Walkthrough | Real Python | https://realpython.com/async-io-python/ |\n| 3 | Asyncio最佳实践与常见陷阱 | 张欣耕博客 | https://shanechang.com/zh-cn/p/python-asyncio-best-practices-pitfalls/ |\n| 4 | Python 异步编程最佳实践：从 asyncio 到生产环境 | CSDN博客 | https://blog.csdn.net/weixin_48504920/article/details/159621190 |\n| 5 | Python asyncio 异步编程完全指南 | CSDN博客 | https://blog.csdn.net/2301_76889955/article/details/159087498 |\n\n### 2.2 重点参考资料\n\n**资料1**: [Python Asyncio Best Practices - PythonAcademy](https://pythonacademy.io/articles/python-asyncio-best-practices)\n- 内容概要：介绍了 asyncio 的基本用法、高级用法、实际应用场景和最佳实践\n- 代码示例：提供了基础协程、异步处理器类、异常处理等示例\n\n**资料2**: [Asyncio最佳实践与常见陷阱 - 张欣耕博客](https://shanechang.com/zh-cn/p/python-asyncio-best-practices-pitfalls/)\n- 内容概要：系统梳理了 asyncio 的关键最佳实践和常见错误\n- **核心要点**：\n  1. 使用 `asyncio.run()` 作为主入口\n  2. 优先使用异步上下文管理器\n  3. 始终等待协程（勿忘记 await）\n  4. 优雅地处理取消（捕获 CancelledError）\n\n---\n\n## 三、asyncio 核心概念\n\n### 3.1 什么是 asyncio？\n\nasyncio 是 Python 的标准库，用于编写**并发**代码，使用的是 **async/await** 语法。它通过**事件循环**（Event Loop）来调度协程的执行。\n\n### 3.2 关键组件\n\n| 组件 | 说明 |\n|------|------|\n| `async def` | 定义协程函数 |\n| `await` | 等待异步操作完成 |\n| `asyncio.run()` | 运行协程的主入口 |\n| `asyncio.gather()` | 并发执行多个协程 |\n| `asyncio.create_task()` | 创建后台任务 |\n| `asyncio.TaskGroup` | 管理一组任务（Python 3.11+） |\n| `async with` | 异步上下文管理器 |\n\n### 3.3 事件循环\n\n事件循环是 asyncio 的核心，负责调度和执行协程。每次调用 `asyncio.run()` 都会创建一个新的事件循环。\n\n---\n\n## 四、Best Practices 最佳实践总结\n\n### ✅ 4.1 使用 asyncio.run() 作为主入口\n\n```python\nasync def main():\n    # 你的异步代码\n    pass\n\nif __name__ == \"__main__\":\n    asyncio.run(main())\n```\n\n**理由**: `asyncio.run()` 会自动创建事件循环、运行协程、并在完成后清理资源。\n\n### ✅ 4.2 优先使用异步上下文管理器\n\n```python\nasync with aiohttp.ClientSession() as session:\n    async with session.get(url) as response:\n        data = await response.text()\n```\n\n**理由**: 确保资源（如网络连接）被正确释放。\n\n### ✅ 4.3 始终等待协程\n\n```python\n# ❌ 错误 - 协程被创建但未执行\nfetch_data(url)\n\n# ✅ 正确 - 协程被 await 并执行\nawait fetch_data(url)\n```\n\n### ✅ 4.4 优雅地处理取消\n\n```python\nasync def cancellable_task():\n    try:\n        while True:\n            await asyncio.sleep(1)\n            # 执行工作...\n    except asyncio.CancelledError:\n        # 在这里清理资源\n        print(\"任务被取消，正在清理...\")\n        raise  # 重新抛出，让调用者知道任务已被取消\n```\n\n### ✅ 4.5 使用 asyncio.gather() 并发执行\n\n```python\nresults = await asyncio.gather(\n    fetch_data(\"https://api.github.com\"),\n    fetch_data(\"https://api.python.org\"),\n    fetch_data(\"https://api.docs.python.org\"),\n)\n```\n\n### ✅ 4.6 使用 asyncio.create_task() 创建后台任务\n\n```python\ntask = asyncio.create_task(fetch_data(url))\n# 可以执行其他操作...\nresult = await task\n```\n\n### ✅ 4.7 使用 TaskGroup 管理任务组（Python 3.11+）\n\n```python\nasync with asyncio.TaskGroup() as tg:\n    t1 = tg.create_task(worker(\"A\", 0.5))\n    t2 = tg.create_task(worker(\"B\", 0.8))\n```\n\n---\n\n## 五、常见陷阱与注意事项\n\n### ⚠️ 5.1 忘记 await 协程\n\n**问题**: 协程对象被创建但没有被执行。  \n**解决**: 始终使用 `await` 调用协程。\n\n### ⚠️ 5.2 在同步代码中使用 await\n\n**问题**: 在非 async 函数中使用 await 会报错。  \n**解决**: 确保使用 `await` 的代码位于 `async def` 定义的协程函数中。\n\n### ⚠️ 5.3 阻塞调用\n\n**问题**: 在异步代码中使用了同步的阻塞调用（如 `time.sleep()`）。  \n**解决**: 使用 `await asyncio.sleep()` 代替 `time.sleep()`。\n\n### ⚠️ 5.4 忽略 CancelledError\n\n**问题**: 任务被取消时没有正确清理资源。  \n**解决**: 在可能被打断的地方捕获 `CancelledError` 并清理资源。\n\n---\n\n## 六、代码验证结果\n\n### 6.1 验证环境\n\n- **Python版本**: 3.13.11\n- **操作系统**: Windows\n- **工作目录**: G:\\OmniAgentAs-desk\\backend\n\n### 6.2 示例代码\n\n创建了完整的 asyncio 示例脚本 `asyncio_demo.py`，包含以下5个示例：\n\n| 示例 | 内容 | 验证结果 |\n|------|------|----------|\n| 示例1 | 基础协程 | ✅ 通过 |\n| 示例2 | asyncio.gather 并发执行 | ✅ 通过 |\n| 示例3 | 异步上下文管理器 | ✅ 通过 |\n| 示例4 | 优雅取消任务 | ✅ 通过 |\n| 示例5 | asyncio.TaskGroup | ✅ 通过 |\n\n### 6.3 运行结果\n\n**示例1: 基础asyncio协程**\n```\n[11:35:49] 开始获取 https://example.com/api\n[11:35:50] 完成获取 https://example.com/api\n结果: https://example.com/api 的数据\n```\n验证：单个协程正常执行，await 机制工作正常。\n\n**示例2: asyncio.gather 并发执行**\n```\n[11:35:50] 开始获取 https://api.github.com\n[11:35:50] 开始获取 https://api.python.org\n[11:35:50] 开始获取 https://api.docs.python.org\n[11:35:51] 完成获取 https://api.docs.python.org\n[11:35:51] 完成获取 https://api.github.com\n[11:35:52] 完成获取 https://api.python.org\n并发执行总耗时: 2.01秒\n```\n验证：三个协程并发执行（耗时2秒而非3秒），证明 asyncio.gather 确实实现了并发。\n\n**示例3: 异步上下文管理器**\n```\n[11:35:52] 连接 DB-Pool-1\n正在使用连接: DB-Pool-1\n查询完成\n[11:35:53] 关闭 DB-Pool-1\n```\n验证：异步上下文管理器正确执行了 `__aenter__` 和 `__aexit__` 方法。\n\n**示例4: 优雅取消任务**\n```\n[Worker-A] 进行中... (1/5)\n[Worker-A] 进行中... (2/5)\n[Worker-A] 进行中... (3/5)\n[Worker-A] 进行中... (4/5)\n[11:35:54] 正在取消 Worker-A...\n[Worker-A] 收到取消信号，正在清理资源...\nWorker-A 已取消\n```\n验证：任务可以被优雅取消，CancelledError 被正确捕获和处理。\n\n**示例5: asyncio.TaskGroup**\n```\n[Worker-A] 开始工作，耗时0.5秒\n[Worker-B] 开始工作，耗时0.8秒\n[Worker-C] 开始工作，耗时0.3秒\n[Worker-C] 完成\n[Worker-A] 完成\n[Worker-B] 完成\n所有任务完成!\n```\n验证：TaskGroup 正确管理了一组任务的并发执行和生命周期。\n\n---\n\n## 七、总结与建议\n\n### 7.1 核心要点\n\n1. **使用 asyncio.run()** 作为异步程序的入口点\n2. **始终使用 await** 来调用协程\n3. **优先使用异步上下文管理器** 管理资源\n4. **优雅处理取消**，捕获 CancelledError\n5. **使用 gather/taskgroup** 管理并发任务\n6. **避免阻塞调用**，使用 asyncio.sleep() 代替 time.sleep()\n\n### 7.2 适用场景\n\n- ✅ I/O 密集型应用（网络请求、数据库查询、文件读写）\n- ✅ 高并发服务器\n- ✅ 实时数据处理\n- ❌ CPU 密集型应用（应使用 multiprocessing）\n\n### 7.3 性能提升\n\n从示例2的验证结果可以看到：\n- 串行执行3个任务（1s + 2s + 0.5s）= **3.5秒**\n- 并发执行3个任务 = **2.01秒**\n- **性能提升约 42%**\n\n对于大量并发 I/O 操作，asyncio 的性能优势会更加显著。\n\n---\n\n## 八、附录\n\n### 8.1 参考链接\n\n- [PythonAcademy - Python Asyncio Best Practices](https://pythonacademy.io/articles/python-asyncio-best-practices)\n- [Real Python - Async IO in Python](https://realpython.com/async-io-python/)\n- [张欣耕博客 - Asyncio最佳实践与常见陷阱](https://shanechang.com/zh-cn/p/python-asyncio-best-practices-pitfalls/)\n\n### 8.2 相关文件\n\n- `asyncio_demo.py` - 示例代码文件\n- `reports/python_asyncio_research_report.md` - 本报告\n\n---\n\n**报告生成时间**: 2026-06-19 11:35  \n**调研完成状态**: ✅ 全部完成\n"}` |
| 16 | list_directory | `{"dir_path": "G:\\OmniAgentAs-desk\\backend\\reports"}` |

## 4 SSE事件详情

- start 步骤=1
- thought 步骤=1 工具=tool_search
- action_tool 步骤=1 工具=tool_search
- observation 步骤=1
- thought 步骤=2 工具=search_web
- action_tool 步骤=2 工具=search_web
- observation 步骤=2
- thought 步骤=3 工具=fetch_webpage
- action_tool 步骤=3 工具=fetch_webpage
- observation 步骤=3
- thought 步骤=4 工具=fetch_webpage
- action_tool 步骤=4 工具=fetch_webpage
- observation 步骤=4
- thought 步骤=5 工具=fetch_webpage
- action_tool 步骤=5 工具=fetch_webpage
- observation 步骤=5
- thought 步骤=6 工具=execute_code
- action_tool 步骤=6 工具=execute_code
- observation 步骤=6
- thought 步骤=7 工具=execute_shell_command
- action_tool 步骤=7 工具=execute_shell_command
- observation 步骤=7
- thought 步骤=8 工具=execute_code
- action_tool 步骤=8 工具=execute_code
- observation 步骤=8
- thought 步骤=9 工具=execute_shell_command
- action_tool 步骤=9 工具=execute_shell_command
- observation 步骤=9
- thought 步骤=10 工具=write_text_file
- action_tool 步骤=10 工具=write_text_file
- observation 步骤=10
- thought 步骤=11 工具=execute_shell_command
- action_tool 步骤=11 工具=execute_shell_command
- observation 步骤=11
- thought 步骤=12 工具=edit_text_file
- action_tool 步骤=12 工具=edit_text_file
- observation 步骤=12
- thought 步骤=13 工具=execute_shell_command
- action_tool 步骤=13 工具=execute_shell_command
- observation 步骤=13
- thought 步骤=14 工具=execute_shell_command
- action_tool 步骤=14 工具=execute_shell_command
- observation 步骤=14
- thought 步骤=15 工具=write_text_file
- action_tool 步骤=15 工具=write_text_file
- observation 步骤=15
- thought 步骤=16 工具=list_directory
- action_tool 步骤=16 工具=list_directory
- observation 步骤=16
- final 步骤=17
  ... (chunk x526)

## 5 数据库验证详情

| 检查项 | 结果 |
|--------|------|
| 会话是否存在 | True |
| 是否有效 | True |
| 创建时间 | None |
| 更新时间 | None |
| 消息顺序正确 | True |
| 消息数量 | 2 |
| 执行步骤数 | 576 |
| 步骤字段问题数 | 0 |

### 5.2 执行步骤（前15条）

| 序号 | 步骤号 | 类型 | 工具 | 状态 |
|------|--------|------|------|--------|
| 1 | 1 | start |  |  |
| 2 | 1 | chunk |  |  |
| 3 | 1 | chunk |  |  |
| 4 | 1 | chunk |  |  |
| 5 | 1 | chunk |  |  |
| 6 | 1 | chunk |  |  |
| 7 | 1 | chunk |  |  |
| 8 | 1 | chunk |  |  |
| 9 | 1 | chunk |  |  |
| 10 | 1 | chunk |  |  |
| 11 | 1 | chunk |  |  |
| 12 | 1 | chunk |  |  |
| 13 | 1 | chunk |  |  |
| 14 | 1 | chunk |  |  |
| 15 | 1 | chunk |  |  |
| ... | (剩余561条) | | | |

### 5.3 步骤数据内容(action_tool)

**步骤1: tool_search**
- 参数: `{"query": "搜索网页 抓取URL内容"}`
- 观察结果: `Tool execution succeeded`

**步骤2: search_web**
- 参数: `{"num_results": 5, "query": "Python asyncio最佳实践 best practices"}`
- 观察结果: `Tool execution succeeded`

**步骤3: fetch_webpage**
- 参数: `{"url": "https://pythonacademy.io/articles/python-asyncio-best-practices", "prompt": "提取Python asyncio最佳实践的所有要点、代码示例和建议"}`
- 观察结果: `Tool execution succeeded`

**步骤4: fetch_webpage**
- 参数: `{"prompt": "提取Python asyncio完整教程的所有要点、核心概念、代码示例、最佳实践、常见陷阱", "url": "https://realpython.com/async-io-python/"}`
- 观察结果: `获取网页失败 (HTTP 403):https://realpython.com/async-io-python/`

**步骤5: fetch_webpage**
- 参数: `{"prompt": "提取Python asyncio完整教程的所有要点、核心概念、代码示例、最佳实践、常见陷阱", "url": "https://shanechang.com/zh-cn/p/python-asyncio-best-practices-pitfalls/"}`
- 观察结果: `Tool execution succeeded`

**步骤6: execute_code**
- 参数: `{"code": "import asyncio\nimport time\n\n# ========== 示例1: 基础asyncio用法 ==========\nprint(\"=\" * 60)\nprint(\"示例1: 基础asyncio协程\")\nprint(\"=\" * 60)\n`
- 观察结果: `{"code": "import asyncio\nimport time\n\n# ========== 示例1: 基础asyncio用法 ==========\nprint(\"=\" * 60)\nprint(\"示例1: 基础asyncio协程\")\nprint(\"=\" * 60)\n\nasync def fetch_data(url: str, delay: float):\n `

**步骤7: execute_shell_command**
- 参数: `{"command": "python --version"}`
- 观察结果: `Tool execution succeeded`

**步骤8: execute_code**
- 参数: `{"code": "import asyncio\nimport time\n\nprint(\"=\" * 60)\nprint(\"示例1: 基础asyncio协程\")\nprint(\"=\" * 60)\n\nasync def fetch_data(url, delay):\n    p`
- 观察结果: `{"code": "import asyncio\nimport time\n\nprint(\"=\" * 60)\nprint(\"示例1: 基础asyncio协程\")\nprint(\"=\" * 60)\n\nasync def fetch_data(url, delay):\n    print(f\"[{time.strftime('%H:%M:%S')}] 开始获取 {url}\"`

**步骤9: execute_shell_command**
- 参数: `{"command": "python -c \"\nimport asyncio\nimport time\n\nprint('=' * 60)\nprint('示例1: 基础asyncio协程')\nprint('=' * 60)\n\nasync def fetch_data(url, del`
- 观察结果: `命令执行失败(退出码1),当前shell_type=powershell`

**步骤10: write_text_file**
- 参数: `{"file_path": "G:\\OmniAgentAs-desk\\backend\\asyncio_demo.py", "content": "import asyncio\nimport time\n\nprint(\"=\" * 60)\nprint(\"示例1: 基础asyncio协程`
- 观察结果: `Tool execution succeeded`

**步骤11: execute_shell_command**
- 参数: `{"command": "python asyncio_demo.py", "cwd": "G:\\OmniAgentAs-desk\\backend", "timeout": 30000}`
- 观察结果: `命令执行失败(退出码1),当前shell_type=powershell`

**步骤12: edit_text_file**
- 参数: `{"file_path": "G:\\OmniAgentAs-desk\\backend\\asyncio_demo.py", "new_string": "    print(\"所有任务完成!\")", "old_string": "    print(f\"所有任务完成: {[t.result`
- 观察结果: `Tool execution succeeded`

**步骤13: execute_shell_command**
- 参数: `{"command": "python asyncio_demo.py", "cwd": "G:\\OmniAgentAs-desk\\backend", "timeout": 30000}`
- 观察结果: `Tool execution succeeded`

**步骤14: execute_shell_command**
- 参数: `{"command": "New-Item -ItemType Directory -Force -Path \"G:\\OmniAgentAs-desk\\backend\\reports\""}`
- 观察结果: `Tool execution succeeded`

**步骤15: write_text_file**
- 参数: `{"file_path": "G:\\OmniAgentAs-desk\\backend\\reports\\python_asyncio_research_report.md", "content": "# Python asyncio 异步编程调研报告\n\n**调研日期**: 2026-06-`
- 观察结果: `Tool execution succeeded`

**步骤16: list_directory**
- 参数: `{"dir_path": "G:\\OmniAgentAs-desk\\backend\\reports"}`
- 观察结果: `Tool execution succeeded`

## 6 验证结果

| 验证项 | 结果 | 说明 |
|--------|------|------|
| 流结束 | final | - |
| 是否有error事件 | PASS | - |
| 回复内容 | PASS | 723字 |
| 数据库验证 | PASS | - |
| SSE-DB一致性 | PASS | 0个问题 |
| DB-Prompt日志一致性 | PASS | PASS |
| 步骤字段完整性 | PASS | 0个问题 |
| 步骤合理性 | PASS | 0个问题 |
| 日志中ERROR | PASS | 0条 |
| 日志中异常堆栈 | PASS | 0条 |

## 7 三方一致性（DB/应用日志/Prompt日志）

| 对比项 | DB | SSE | 日志 | 是否匹配 |
|--------|-----|-----|------|----------|
| 工具数量 | 16 | 16 | 1次LLM调用 | PASS |
| 工具名称 | ['tool_search', 'search_web', 'fetch_webpage', 'fetch_webpage', 'fetch_webpage'] | ['tool_search', 'search_web', 'fetch_webpage', 'fetch_webpage', 'fetch_webpage'] | - | PASS |
| 观察结果数 | 16 | 16 | - | PASS |
| Prompt日志文件 | - | - | ['prompt_343+20260619_113248.json'] | PASS |

---
**更新时间**: 2026-06-19 11:37:14
