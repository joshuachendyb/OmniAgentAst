# 前端错误统一处理中心设计方案

**创建时间**: 2026-04-11 18:30:00
**版本**: v1.2
**作者**: 小强
**更新说明**: 吸收小资审查意见，补充8种遗漏错误类型，优化章节结构

---

## 一、现状分析

### 1.1 现有错误提示分布

| 文件 | 错误类型 | 问题 |
|------|---------|------|
| `chatMessages.ts` | 成功/错误/警告提示 | 函数分散，未统一 |
| `sse.ts` | SSE连接错误 | 独立处理，风格各异 |
| `api.ts` | API响应错误 | 拦截器处理，分散 |
| `NewChatContainer.tsx` | 31处catch块 | 直接调用message，风格不统一 |

### 1.2 现有问题

1. **风格不统一** - 有的用duration:3，有的用5，有的没有
2. **位置分散** - catch块里直接调用message.error()
3. **重试逻辑分散** - 每个地方自己实现重试
4. **提示文案不统一** - 类似的错误，提示文字不同
5. **遗漏特殊场景** - 危险操作、安全降级等未覆盖

---

## 二、需要统一处理的前端错误类型

### 2.1 核心错误类型（已确认）

| 序号 | 错误类型 | 代码标识 | 处理方式 |
|------|---------|---------|---------|
| 1 | 连接被拒绝 | CONNECTION_REFUSED | 提示+重连3次+指数退避 |
| 2 | 请求超时 | REQUEST_TIMEOUT | 提示+重连3次 |
| 3 | 服务器500 | SERVER_500 | 提示+停止+建议刷新 |
| 4 | 网络断开 | NETWORK_ERROR | 提示+重连3次 |
| 5 | 存储满了 | QUOTA_EXCEEDED | 提示+清理建议 |
| 6 | 会话冲突 | SESSION_CONFLICT | 提示+刷新页面 |
| 7 | 认证失败 | AUTH_401/403 | 提示+跳转配置 |
| 8 | 请求取消 | REQUEST_ABORT | **静默处理** |
| 9 | 组件卸载 | COMPONENT_UNMOUNTED | **静默处理** |

### 2.2 业务场景错误（v1.2新增）

| 序号 | 错误类型 | 代码标识 | 处理方式 |
|------|---------|---------|---------|
| 10 | 存储满了 | STORAGE_FULL | 提示+清理建议 |
| 11 | 危险操作拦截 | DANGEROUS_OPERATION | 提示+删除消息+记录日志 |
| 12 | 内容过长 | CONTENT_TOO_LONG | 提示精简内容 |
| 13 | 会话不存在 | SESSION_NOT_FOUND | 提示+返回 |
| 14 | 危险确认失败 | DANGER_CONFIRM_FAIL | 提示确认失败 |
| 15 | 安全服务异常 | SECURITY_SERVICE_DOWN | warning+降级普通模式 |
| 16 | 用户消息保存失败 | USER_MESSAGE_SAVE_FAILED | error提示+继续执行 |
| 17 | 重复点击 | DUPLICATE_CLICK | 提示"处理中"+忽略 |

### 2.3 不需要处理的错误

| 错误类型 | 原因 |
|---------|------|
| 后端发送的 `type=error` | 原样显示即可 |
| 用户主动取消(AbortError) | 静默处理 |
| 组件卸载后的异步错误 | 静默处理 |

---

## 三、UI一致性规范

### 3.1 提示样式标准

```typescript
const UI_CONFIG = {
  error: { duration: 5, maxCount: 1 },      // 错误提示5秒
  warning: { duration: 3, maxCount: 3 },    // 警告提示3秒
  success: { duration: 2, maxCount: 1 },    // 成功提示2秒
  info: { duration: 3, maxCount: 3 },        // 信息提示3秒
  loading: { duration: 0, maxCount: 1 },    // 不消失loading
};
```

### 3.2 提示文案规范

| 场景 | 格式 |
|------|------|
| 错误提示 | "操作失败：具体原因，请检查/重试/刷新" |
| 警告提示 | "正在重试 (X/3)..." 或 "请注意：具体事项" |
| 成功提示 | "操作成功" 或 "已完成" |
| 信息提示 | "提示：具体信息" |
| loading提示 | duration:0，手动关闭 |

### 3.3 错误去重规范

```typescript
const ERROR_DEDUP_WINDOW = 30000; // 30秒内不重复提示
const recentErrors = new Map<string, number>();
```

### 3.4 API与SSE处理差异

| 维度 | API错误处理 | SSE错误处理 |
|------|------------|------------|
| 模式 | 请求-响应 | 长连接 |
| 重试策略 | 失败立即提示，可手动重试 | 自动重连+指数退避 |
| 提示时机 | 立即显示 | 连接断开后延迟显示 |
| 恢复检测 | 手动刷新 | 自动检测恢复 |

---

## 四、错误类型枚举与配置

### 4.1 错误类型枚举

```typescript
enum ErrorType {
  // 网络错误
  CONNECTION_REFUSED = "connection_refused",
  CONNECTION_RESET = "connection_reset",
  NETWORK_ERROR = "network_error",
  WEAK_NETWORK = "weak_network",
  
  // 超时错误
  REQUEST_TIMEOUT = "request_timeout",
  IDLE_TIMEOUT = "idle_timeout",
  
  // 服务器错误
  SERVER_500 = "server_500",
  SERVER_502 = "server_502",
  SERVER_503 = "server_503",
  
  // 认证错误
  AUTH_401 = "auth_401",
  AUTH_403 = "auth_403",
  
  // 业务错误
  SESSION_CONFLICT = "session_conflict",
  SESSION_NOT_FOUND = "session_not_found",
  VERSION_CONFLICT = "version_conflict",
  CONTENT_TOO_LONG = "content_too_long",
  
  // 存储错误
  QUOTA_EXCEEDED = "quota_exceeded",
  STORAGE_ERROR = "storage_error",
  
  // 操作错误
  SAVE_FAILED = "save_failed",
  LOAD_FAILED = "load_failed",
  SEND_FAILED = "send_failed",
  USER_MESSAGE_SAVE_FAILED = "user_message_save_failed",
  
  // 安全相关
  DANGEROUS_OPERATION = "dangerous_operation",
  DANGER_CONFIRM_FAIL = "danger_confirm_fail",
  SECURITY_SERVICE_DOWN = "security_service_down",
  DANGER_CANCELLED = "danger_cancelled",
  
  // 防抖
  DUPLICATE_CLICK = "duplicate_click",
  
  // 任务控制[v1.4新增]
  TASK_CONTROL_FAILED = "task_control_failed",
  TASK_INTERRUPT = "task_interrupt",
  IDLE_STATE = "idle_state",
  
  // 会话相关[v1.4新增]
  CREATE_SESSION_FAILED = "create_session_failed",
  
  // 成功类型[v1.4新增]
  SAVE_SUCCESS = "save_success",
  TASK_SUCCESS = "task_success",
  
  // 静默处理
  REQUEST_ABORT = "request_abort",
  COMPONENT_UNMOUNTED = "component_unmounted",
  
  // 其他
  BACKEND_ERROR = "backend_error",
  UNKNOWN = "unknown",
}
```

### 4.2 错误配置

```typescript
interface ErrorConfig {
  retryable: boolean;
  maxRetries: number;
  retryDelay: number;
  retryBackoff?: number;
  message: string;
  description?: string;
  severity: "critical" | "warning" | "info";
  silent?: boolean;
  dedupWindow?: number;
  // 特殊处理标志
  continueOnError?: boolean;  // 是否继续执行（如用户消息保存失败但继续发送）
  deleteMessage?: boolean;  // 是否删除消息（如危险操作）
  fallbackMode?: string;  // 降级模式（如安全服务异常）
}
```

---

## 五、重试次数配置

### 5.1 重试次数分类

| 错误类型 | 重试次数 | 重试延迟 | 说明 |
|---------|----------|---------|------|
| **网络类** | | | |
| CONNECTION_REFUSED | 3次 | 1000ms | 后端未运行 |
| NETWORK_ERROR | 2次 | 1500ms | 网络不稳定 |
| WEAK_NETWORK | 1次 | 2000ms | 弱网只1次 |
| **超时类** | | | |
| REQUEST_TIMEOUT | 3次 | 1000ms | 请求等待超时 |
| IDLE_TIMEOUT | 2次 | 500ms | 空闲超时 |
| **服务器类** | | | |
| SERVER_500 | 0次 | - | 不重试 |
| SERVER_502/503 | 2次 | 2000ms | 可重试 |
| **认证类** | | | |
| AUTH_401/403 | 0次 | - | 不重试 |
| **业务类** | | | |
| SESSION_CONFLICT | 0次 | - | 不重试 |
| SESSION_NOT_FOUND | 0次 | - | 404不存在 |
| CONTENT_TOO_LONG | 0次 | - | 不重试 |
| **存储类** | | | |
| QUOTA_EXCEEDED | 0次 | - | 不重试 |
| STORAGE_ERROR | 1次 | 500ms | 可尝试一次 |
| **操作类** | | | |
| SAVE_FAILED | 3次 | 1000ms | 可重试 |
| LOAD_FAILED | 2次 | 1000ms | 可重试 |
| SEND_FAILED | 3次 | 1000ms | 可重试 |
| **静默类** | | | |
| REQUEST_ABORT | 0次 | - | 静默 |
| COMPONENT_UNMOUNTED | 0次 | - | 静默 |
| **特殊类** | | | |
| DANGEROUS_OPERATION | 0次 | - | 拦截+删消息 |
| DANGER_CANCELLED | 0次 | - | info提示 |
| DUPLICATE_CLICK | 0次 | - | 静默忽略 |
| **任务控制类[v1.4新增]** | | | |
| TASK_CONTROL_FAILED | 2次 | 1000ms | 可重试 |
| TASK_INTERRUPT | 0次 | - | info提示，静默 |
| IDLE_STATE | 0次 | - | warning提示，静默 |
| **会话类[v1.4新增]** | | | |
| CREATE_SESSION_FAILED | 3次 | 1000ms | 可重试 |
| **成功类[v1.4新增]** | | | |
| SAVE_SUCCESS | 0次 | - | success提示 |
| TASK_SUCCESS | 0次 | - | success提示 |

### 5.2 API vs SSE 重试差异

| 场景 | API | SSE |
|------|-----|-----|
| 网���错�� | 3次手动重试 | 3次自动重连 |
| 超时错误 | 3次手动重试 | 2次自动重连 |
| 服务器500 | 0次 | 0次 |

---

## 六、统一错误处理中心设计

### 6.1 文件结构

```
src/utils/errorHandler.ts  ← 统一错误处理中心
```

### 6.2 核心模块

```
errorHandler.ts
    │
    ├── ErrorType          → 错误类型枚举（53种）
    ├── ErrorConfig        → 错误配置接口
    ├── ERROR_CONFIG_MAP  → 错误配置映射表
    │
    ├── classifyError()    → 分类错误
    ├── handleError()        → 统一处理入口
    ├── handleApiError()   → API错误处理
    ├── handleSSEError()   → SSE错误处理
    ├── handleReconnect() → 重连处理
    ├── showMessage()      → 统一显示
    │
    └── 去重与静默处理
          ├── shouldShowError()  → 判断是否显示
          ├── isSilentError()      → 静默错误判断
          └── dedupErrors()       → 错误去重
```

### 6.3 统一入口函数

```typescript
/**
 * 统一错误处理入口
 * @param error - 原始错误对象
 * @param context - 错误上下文(source/onRetry/continue)
 */
handleError(error: any, context: ErrorContext): ActionResult

interface ActionResult {
  handled: boolean;
  shouldContinue?: boolean;  // 保存失败但继续执行
  deleteMessage?: boolean;  // 危险操作需删除
  fallbackMode?: string;   // 降级模式
}
```

### 6.4 特殊处理示例

```typescript
// 危险操作拦截
if (errorType === 'DANGEROUS_OPERATION') {
  return { handled: true, deleteMessage: true };
}

// 安全服务降级
if (errorType === 'SECURITY_SERVICE_DOWN') {
  return { handled: true, fallbackMode: 'normal' };
}

// 用户消息保存失败但继续
if (errorType === 'USER_MESSAGE_SAVE_FAILED') {
  return { handled: true, shouldContinue: true };
}

// 重复点击
if (errorType === 'DUPLICATE_CLICK') {
  return { handled: true, shouldContinue: false };  // 忽略
}
```

---

## 七、实施计划

### 7.1 第一阶段：创建错误处理中心

**目标**：创建errorHandler.ts统一入口文件

| 步骤 | 任务 | 具体操作 | 产出文件 |
|------|------|---------|---------|
| 1.1.1 | 创建errorHandler.ts | 在frontend/src/utils/下创建新文件 | errorHandler.ts |
| 1.1.2 | 定义ErrorType枚举 | 实现53种错误类型枚举 | ErrorType枚举 |
| 1.1.3 | 定义ErrorConfig接口 | 实现配置接口（含特殊处理标志） | ErrorConfig接口 |
| 1.1.4 | 实现ERROR_CONFIG_MAP | 按第5章配置53种错误的映射表 | 配置映射表 |
| 1.2.1 | 实现classifyError() | 根据error.message/name/response分类错误 | 错误分类函数 |
| 1.2.2 | 实现shouldShowError() | 30秒去重逻辑检查 | 去重判断函数 |
| 1.2.3 | 实现isSilentError() | 判断是否静默处理 | 静默判断函数 |
| 1.3.1 | 实现showMessage() | 统一显示message+duration | 统一显示函数 |
| 1.3.2 | 实现handleReconnect() | 指数退避+Full Jitter重连 | 重连处理函数 |
| 1.4.1 | 实现handleApiError() | API错误处理入口 | API入口函数 |
| 1.4.2 | 实现handleSSEError() | SSE错误处理入口 | SSE入口函数 |
| 1.4.3 | 实现handleError() | 统一入口函数 | 统一入口函数 |
| 1.5.1 | 导出所有函数 | export所有需要暴露的函数 | 模块导出 |

**代码量预估**：约300-400行

### 7.2 第二阶段：迁移现有代码

**目标**：将现有分散的错误处理迁移到统一入口

#### 2.1 迁移sse.ts（步骤1）

| 步骤 | 任务 | 操作 |
|------|------|------|
| 2.1.1 | 导入errorHandler | 在sse.ts顶部添加import |
| 2.1.2 | 替换ERROR_CONFIG_MAP | 删除sse.ts内 ERR_CONFIG_MAP，使用errorHandler的 |
| 2.1.3 | 替换classifyError | 使用errorHandler的classifyError |
| 2.1.4 | 替换handleSSEError调用 | 将现有handleSSEError改为调用errorHandler.handleSSEError |
| 2.1.5 | 测试SSE功能 | 启动后端，测试各种SSE错误场景 |

#### 2.2 迁移api.ts（步骤2）

| 步骤 | 任务 | 操作 |
|------|------|------|
| 2.2.1 | 导入errorHandler | 在api.ts顶部添加import |
| 2.2.2 | 修改401处理 | message.error改为handleApiError |
| 2.2.3 | 修改429处理 | message.error改为handleApiError |
| 2.2.4 | 修改ECONNABORTED处理 | message.error改为handleApiError |
| 2.2.5 | 修改default处理 | message.error改为handleApiError |
| 2.2.6 | 测试API功能 | 调用几个API接口测试错误处理 |

#### 2.3 迁移NewChatContainer.tsx（步骤3）

| 步骤 | 任务 | 操作 |
|------|------|------|
| 2.3.1 | 导入errorHandler | 在NewChatContainer.tsx顶部添加import |
| 2.3.2 | 迁移catch块错误（高优先级P0-P2） | 逐个迁移catch块 |
| 2.3.2.1 | 会话保存409错误 | 改为handleError |
| 2.3.2.2 | 会话保存404/400错误 | 改为handleError |
| 2.3.2.3 | 网络错误 | 改为handleError |
| 2.3.3 | 迁移P3-P5特殊场景 | 特殊处理逻辑 |
| 2.3.3.1 | 危险操作拦截 | 改为handleError，测试拦截+删消息 |
| 2.3.3.2 | 安全服务降级 | 改为handleError，测试降级继续 |
| 2.3.3.3 | 用户消息保存失败但继续 | 改为handleError，测试继续执行 |
| 2.3.3.4 | 重复点击防护 | 改为handleError，测试忽略 |
| 2.3.3.5 | 存储满处理 | 改为handleError，测试提示 |
| 2.3.4 | 迁移[v1.4新增]错误类型 | 补充迁移7种新错误类型 |
| 2.3.4.1 | 任务控制失败(TASK_CONTROL_FAILED) | 改为handleError测试重试 |
| 2.3.4.2 | 任务中断中(TASK_INTERRUPT) | info提示+静默 |
| 2.3.4.3 | 无进行中任务(IDLE_STATE) | warning提示+静默 |
| 2.3.4.4 | 创建会话失败(CREATE_SESSION_FAILED) | 改为handleError测试重试 |
| 2.3.4.5 | 危险命令已取消(DANGER_CANCELLED) | info提示 |
| 2.3.4.6 | 保存成功(SAVE_SUCCESS) | success提示 |
| 2.3.4.7 | 任务操作成功(TASK_SUCCESS) | success提示 |
| 2.3.5 | 测试完整功能 | 测试发送消息各种场景 |

**注意**：NewChatContainer.tsx有31处catch块，需要逐个迁移，确保功能不退化

#### 2.4 chatMessages.ts作为包装层（步骤4）

| 步骤 | 任务 | 操作 |
|------|------|------|
| 2.4.1 | 修改showSaveError | 内部调用handleError |
| 2.4.2 | 修改showLoadError | 内部调用handleError |
| 2.4.3 | 修改showNetworkError | 内部调用handleError |
| 2.4.4 | 修改showConflictError | 内部调用handleError |
| 2.4.5 | 测试各类型提示 | 触发各类型提示验证 |

### 7.3 第三阶段：规范化

| 步骤 | 任务 | 操作 |
|------|------|------|
| 3.1 | 检查所有catch块 | grep统计确认31处全部迁移 |
| 3.2 | 配置ESLint规则 | 禁止直接调用message.error/warning（可选） |
| 3.3 | 统一duration | 确认全部使用UI_CONFIG |
| 3.4 | 添加注释 | 所有catch块必须使用handleError |

### 7.4 实施优先级与检查点

| 优先级 | 任务 | 检查点 | 涉及文件 |
|--------|------|--------|---------|
| **P0** | 静默处理(AbortError) | AbortError不弹窗 | api.ts,sse.ts |
| **P0** | 错误去重(30秒) | 30秒内相同错误不重复提示 | errorHandler.ts |
| **P1** | handleError入口 | 编译通过，类型正确 | errorHandler.ts |
| **P1** | handleApiError | API错误正确处理+重试 | api.ts |
| **P1** | handleSSEError | SSE错误正确处理+重连 | sse.ts |
| **P2** | 危险操作拦截 | 提示+删除消息 | NewChatContainer.tsx |
| **P2** | 安全降级处理 | warning+普通模式 | NewChatContainer.tsx |
| **P3** | 重复点击防护 | 提示处理中+忽略 | NewChatContainer.tsx |
| **P4** | 存储满处理 | 提示清理建议 | NewChatContainer.tsx |
| **P5** | 会话加载loading | duration:0+手动关闭 | NewChatContainer.tsx |
| **P6** | [v1.4新增]任务控制错误 | TASK_CONTROL_FAILED重试 | NewChatContainer.tsx |
| **P7** | [v1.4新增]创建会话失败 | CREATE_SESSION_FAILED重试 | NewChatContainer.tsx |
| **P8** | [v1.4新增]成功提示 | SAVE_SUCCESS/TASK_SUCCESS | NewChatContainer.tsx |

### 7.5 测试用例说明

#### 7.5.1 单元测试（errorHandler.ts）

| 测试用例 | 测试内容 | 预期结果 |
|----------|----------|----------|
| classifyError_AbortError | classifyError({name:'AbortError'}) | 返回REQUEST_ABORT |
| classifyError_401 | classifyError({response:{status:401}}) | 返回AUTH_401 |
| classifyError_403 | classifyError({response:{status:403}}) | 返回AUTH_403 |
| classifyError_404 | classifyError({response:{status:404}}) | 返回SESSION_NOT_FOUND |
| classifyError_409 | classifyError({response:{status:409}}) | 返回SESSION_CONFLICT |
| classifyError_429 | classifyError({response:{status:429}}) | 返回RATE_LIMIT_429 |
| classifyError_500 | classifyError({response:{status:500}}) | 返回SERVER_500 |
| classifyError_QuotaExceeded | classifyError(DOMException) | 返回QUOTA_EXCEEDED |
| shouldShowError_first | 首次调用shouldShowError(NETWORK_ERROR) | 返回true |
| shouldShowError_dedup | 30秒内再次调用shouldShowError | 返回false |
| shouldShowError_expired | 31秒后再次调用shouldShowError | 返回true |
| isSilentError_abort | isSilentError({name:'AbortError'}) | 返回true |
| isSilentError_normal | isSilentError({message:'normal'}) | 返回false |
| UI_CONFIG_error | UI_CONFIG.error.duration | 等于5 |
| UI_CONFIG_warning | UI_CONFIG.warning.duration | 等于3 |

#### 7.5.2 API错误测试

| 测试用例 | 测试内容 | 预期结果 |
|----------|----------|----------|
| handleApiError_401 | 触发401错误 | message显示"API Key无效"，不重试 |
| handleApiError_429 | 触发429错误 | message显示"请求太频繁"，不重试 |
| handleApiError_timeout | 触发ECONNABORTED | message显示"请求超时"，可重试 |
| handleApiError_retry | 点击重试按钮 | 触发onRetry回调 |

#### 7.5.3 SSE错误测试

| 测试用例 | 测试内容 | 预期结果 |
|----------|----------|----------|
| handleSSEError_connection_refused | 触发连接拒绝 | 显示warning+自动重连3次 |
| handleSSEError_timeout | 触发超时 | 显示warning+自动重连3次 |
| handleSSEError_maxRetry | 超过最大重试 | 显示error+进入failed状态 |
| handleSSEError_reconnectProgress | 重连过程中 | 显示"正在重试(1/3)..." |

#### 7.5.4 特殊场景测试

| 测试用例 | 测试内容 | 预期结果 |
|----------|----------|----------|
| dangerous_operation | 危险操作触发 | 提示拦截+删除消息+日志 |
| security_service_down | 安全服务异常 | warning提示+降级普通模式 |
| user_message_save_failed | 用户消息保存失败 | error显示但继续发送AI |
| duplicate_click | 连续快速点击 | 提示"处理中"+忽略重复 |
| quota_exceeded | 存储满 | 提示"存储满了"+清理建议 |
| task_control_failed | 暂停/继续操作失败 | error提示+可重试 |
| task_interrupt | 任务中断中 | info提示，不弹窗 |
| idle_state | 无进行中任务 | warning提示，不弹窗 |
| create_session_failed | 创建会话失败 | error提示+可重试 |
| danger_cancelled | 危险命令已取消 | info提示 |
| save_success | 保存成功 | success提示 |
| task_success | 任务操作成功 | success提示 |

#### 7.5.5 集成测试

| 测试用例 | 测试内容 | 预期结果 |
|----------|----------|----------|
| end2end_sendMessage | 发送正常消息 | 成功接收AI回复 |
| end2end_networkError | 发送中网络断开 | 自动重连+继续接收 |
| end2end_saveError | 保存时网络错误 | 3次重试后提示失败 |
| end2end_conflict | 保存时409冲突 | 提示冲突+同步数据 |

### 7.6 常见问题与解决

| 问题 | 原因 | 解决方案 |
|------|------|-----------|
| 编译错误 | 类型定义缺失 | 确保ErrorType枚举完整 |
| 循环依赖 | errorHandler导入chatMessages | 单向依赖，errorHandler不导入chatMessages |
| 消息不消失 | duration:0未手动关闭 | 记录message.key并调用message.destroy |
| 重连不触发 | pendingMessage为空 | 确保重连时保留pendingMessage |

---

## 八、验收标准

### 8.1 代码验收

| 验收项 | 检查方法 | 通过标准 |
|--------|----------|----------|
| errorHandler.ts存在 | ls frontend/src/utils/errorHandler.ts | 文件存在 |
| ErrorType枚举完整 | grep "enum ErrorType" -c | 53种错误类型 |
| ERROR_CONFIG_MAP | grep "CONNECTION_REFUSED" | 完整的配置映射 |
| classifyError实现 | grep "classifyError()" | 返回ErrorType |
| handleError实现 | grep "handleError(" | 有统一入口 |
| handleApiError实现 | grep "handleApiError(" | 有API入口 |
| handleSSEError实现 | grep "handleSSEError(" | 有SSE入口 |
| shouldShowError实现 | grep "shouldShowError(" | 有去重逻辑 |
| isSilentError实现 | grep "isSilentError(" | 有静默判断 |
| UI_CONFIG导出 | grep "UI_CONFIG" | 有统一配置 |

### 8.2 功能验收

| 验收项 | 测试方法 | 通过标准 |
|--------|----------|----------|
| 静默处理AbortError | 触发取消操作 | 不弹窗，不影响用户 |
| 错误去重30秒 | 连续触发同错误 | 30秒内只弹一次 |
| API错误处理 | 调用API触发错误 | 正确提示+可重试 |
| SSE错误处理 | SSE连接断开 | 自动重连+显示进度 |
| 危险操作拦截 | 发送危险命令 | 提示拦截+消息被删 |
| 安全降级处理 | 安全服务异常时发送 | 普通模式发送成功 |
| 用户消息保存失败继续 | 保存失败时发送 | 提示失败但继续发送 |
| 重复点击防护 | 快速连续点击 | 提示处理中+忽略 |
| 存储满处理 | sessionStorage满时 | 提示清理建议 |

### 8.3 测试验收

| 验收项 | 检查方法 | 通过标准 |
|--------|----------|----------|
| 单元测试通过 | npm test | 全部通过 |
| 覆盖率 | npm test --coverage | 覆盖核心函数 |
| E2E测试通过 | playwright test | 关键流程通过 |

### 8.4 代码质量验收

| 验收项 | 检查方法 | 通过标准 |
|--------|----------|----------|
| ESLint检查 | npm run lint | 无error |
| TypeScript检查 | npx tsc --noEmit | 无错误 |
| 无直接message调用 | grep "message\.(error\|warning)(" chatMessages.ts | 仅包装层 |

### 8.5 验收检查表

```
□ errorHandler.ts文件存在
□ 53种ErrorType枚举完整
□ ERROR_CONFIG_MAP配置完整
□ classifyError正确分类28种错误
□ shouldShowError 30秒去重有效
□ isSilentError静默判断有效
□ handleApiError处理正确
□ handleSSEError处理正确
□ P0静默处理AbortError测试通过
□ P1错误去重30秒测试通过
□ P2危险操作拦截测试通过
□ P3安全降级处理测试通过
□ P4重复点击防护测试通过
□ P5存储满处理测试通过
□ 单元测试全部通过
□ E2E测试通过
□ ESLint检查无error
```

---

## 九、版本历史

| 版本 | 时间 | 签名 | 更新内容 |
|------|------|------|---------|
| v1.0 | 2026-04-11 18:30:00 | 小强 | 初始版本 |
| v1.1 | 2026-04-11 20:10:00 | 小强 | 补充重试次数配置 |
| v1.2 | 2026-04-11 21:10:00 | 小强 | 补充8种遗漏错误类型 |
| v1.3 | 2026-04-11 21:20:00 | 小强 | 详细化实施计划+测试用例+验收标准 |
| v1.4 | 2026-04-11 21:40:00 | 小强 | 补充小健审查结果，增加25种ErrorType |

## 十、文件位置

| 文件 | 位置 | 说明 |
|------|------|------|
| errorHandler.ts | frontend/src/utils/errorHandler.ts | 统一错误处理中心（待创建） |
| sse.ts | frontend/src/utils/sse.ts | SSE处理（需修改） |
| api.ts | frontend/src/services/api.ts | API服务（需修改） |
| chatMessages.ts | frontend/src/utils/chatMessages.ts | 消息提示（需修改） |
| NewChatContainer.tsx | frontend/src/components/Chat/NewChatContainer.tsx | 主聊天组件（需修改） |

---

## 十、小健审查补充（2026-04-11 21:34:56）

### 10.1 审查方法

使用grep统计所有message.error/warning/success/info调用，定位到具体文件和行号。

### 10.2 统计结果

| 分类 | 设计有 | 设计无 | 覆盖率 |
|------|--------|--------|---------|
| NewChatContainer.tsx (33处) | 20 | 13 | 61% |
| sse.ts (7处) | 7 | 0 | 100% |
| api.ts (4处) | 4 | 0 | 100% |
| chatMessages.ts (20处) | 20 | 0 | 100% |
| Settings/index.tsx (22处) | 0 | 22 | 0% |
| History/index.tsx (12处) | 0 | 12 | 0% |
| **总计** | **51** | **47** | **52%** |

### 10.3 需要补充的ErrorType（采用统一处理方法）

#### Settings页面相关（13种）

```typescript
// 配置管理错误
PROVIDER_CONFIG_ERROR = "provider_config_error",     // Provider配置错误
MODEL_CONFIG_ERROR = "model_config_error",           // Model配置错误
ADD_PROVIDER_FAILED = "add_provider_failed",        // 添加Provider失败
DELETE_PROVIDER_FAILED = "delete_provider_failed",    // 删除Provider失败
UPDATE_PROVIDER_FAILED = "update_provider_failed",   // 更新Provider失败
ADD_MODEL_FAILED = "add_model_failed",               // 添加Model失败
DELETE_MODEL_FAILED = "delete_model_failed",        // 删除Model失败
SWITCH_MODEL_FAILED = "switch_model_failed",         // 切换模型失败
LOAD_CONFIG_FAILED = "load_config_failed",           // 加载配置失败
SAVE_CONFIG_FAILED = "save_config_failed",          // 保存配置失败
OPEN_CONFIG_DIR_FAILED = "open_config_dir_failed", // 打开配置目录失败
READ_CONFIG_FAILED = "read_config_failed",         // 读取配置文件失败
VALIDATE_CONFIG_FAILED = "validate_config_failed",  // 检测配置失败
```

#### History页面相关（5种）

```typescript
// 会话管理错误
SESSION_LIST_FAILED = "session_list_failed",         // 加载会话列表失败
REFRESH_FAILED = "refresh_failed",                  // 刷新失败
DELETE_SESSION_FAILED = "delete_session_failed",    // 删除会话失败
BATCH_DELETE_FAILED = "batch_delete_failed",          // 批量删除失败
NAVIGATE_FAILED = "navigate_failed",                // 跳转失败
```

#### NewChatContainer补充（7种）

```typescript
// 任务控制错误
TASK_CONTROL_FAILED = "task_control_failed",          // 暂停/继续/中断失败
TASK_INTERRUPT = "task_interrupt",                    // 任务中断中
IDLE_STATE = "idle_state",                          // 无进行中任务
CREATE_SESSION_FAILED = "create_session_failed",   // 创建会话失败
DANGER_CANCELLED = "danger_cancelled",              // 危险命令已取消

// 成功类型（统一处理）
SAVE_SUCCESS = "save_success",                      // 保存成功
TASK_SUCCESS = "task_success",                   // 任务操作成功
```

### 10.4 错误配置补充

```typescript
// Settings页面错误配置
const ERROR_CONFIG_MAP: Record<ErrorType, ErrorConfig> = {
  // ... 现有配置保持不变 ...

  // Settings补充
  PROVIDER_CONFIG_ERROR: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "Provider配置错误，请检查配置",
    severity: "error",
    silent: false,
  },
  MODEL_CONFIG_ERROR: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "Model配置错误，请检查配置",
    severity: "error",
    silent: false,
  },
  ADD_PROVIDER_FAILED: {
    retryable: true,
    maxRetries: 2,
    retryDelay: 1000,
    message: "添加Provider失败，请重试",
    severity: "error",
  },
  DELETE_PROVIDER_FAILED: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "删除Provider失败",
    severity: "error",
  },
  UPDATE_PROVIDER_FAILED: {
    retryable: true,
    maxRetries: 2,
    retryDelay: 1000,
    message: "更新Provider失败，请重试",
    severity: "error",
  },
  ADD_MODEL_FAILED: {
    retryable: true,
    maxRetries: 2,
    retryDelay: 1000,
    message: "添加Model失败，请重试",
    severity: "error",
  },
  DELETE_MODEL_FAILED: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "删除Model失败",
    severity: "error",
  },
  SWITCH_MODEL_FAILED: {
    retryable: true,
    maxRetries: 2,
    retryDelay: 1000,
    message: "切换模型失败，请重试",
    severity: "error",
  },
  LOAD_CONFIG_FAILED: {
    retryable: true,
    maxRetries: 3,
    retryDelay: 1000,
    message: "加载配置失败，请重试",
    severity: "error",
  },
  SAVE_CONFIG_FAILED: {
    retryable: true,
    maxRetries: 3,
    retryDelay: 1000,
    message: "保存配置失败，请重试",
    severity: "error",
  },
  OPEN_CONFIG_DIR_FAILED: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "打开配置目录失败",
    severity: "error",
  },
  READ_CONFIG_FAILED: {
    retryable: true,
    maxRetries: 2,
    retryDelay: 1000,
    message: "读取配置文件失败，请重试",
    severity: "error",
  },
  VALIDATE_CONFIG_FAILED: {
    retryable: true,
    maxRetries: 2,
    retryDelay: 1000,
    message: "检测配置失败，请重试",
    severity: "error",
  },

  // History补充
  SESSION_LIST_FAILED: {
    retryable: true,
    maxRetries: 3,
    retryDelay: 1000,
    message: "加载会话列表失败，请重试",
    severity: "error",
  },
  REFRESH_FAILED: {
    retryable: true,
    maxRetries: 2,
    retryDelay: 1000,
    message: "刷新失败，请重试",
    severity: "error",
  },
  DELETE_SESSION_FAILED: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "删除会话失败",
    severity: "error",
  },
  BATCH_DELETE_FAILED: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "批量删除会话失败",
    severity: "error",
  },
  NAVIGATE_FAILED: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "跳转失败",
    severity: "error",
  },

  // NewChatContainer补充
  TASK_CONTROL_FAILED: {
    retryable: true,
    maxRetries: 2,
    retryDelay: 1000,
    message: "暂停/继续请求失败，请重试",
    severity: "error",
  },
  TASK_INTERRUPT: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "正在中断任务...",
    severity: "info",
    silent: true,
  },
  IDLE_STATE: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "当前没有进行中的任务",
    severity: "warning",
    silent: true,
  },
  CREATE_SESSION_FAILED: {
    retryable: true,
    maxRetries: 3,
    retryDelay: 1000,
    message: "创建会话失败，请重试",
    severity: "error",
  },
  DANGER_CANCELLED: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "已取消危险命令的执行",
    severity: "info",
    silent: true,
  },
  SAVE_SUCCESS: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "操作成���",
    severity: "info",
    silent: false,
  },
  TASK_SUCCESS: {
    retryable: false,
    maxRetries: 0,
    retryDelay: 0,
    message: "任务操作成功",
    severity: "info",
    silent: false,
  },
};
```

### 10.5 修正方法（采用统一处理方法）

#### 步骤1：更新ErrorType枚举

在errorHandler.ts中补充25种ErrorType。

#### 步骤2：更新ERROR_CONFIG_MAP

在errorHandler.ts中补充25种错误配置。

#### 步骤3：迁移Settings页面错误

| 步骤 | 任务 | 操作 |
|------|------|------|
| 10.3.1 | 导入errorHandler | 在Settings/index.tsx添加import |
| 10.3.2 | 修改catch块 | 逐个迁移到handleError |
| 10.3.3 | 替换message.error | 改为handleApiError |

#### 步骤4：迁移History页面错误

| 步骤 | 任务 | 操作 |
|------|------|------|
| 10.4.1 | 导入errorHandler | 在History/index.tsx添加import |
| 10.4.2 | 修改catch块 | 逐个迁移到handleError |
| 10.4.3 | 替换message.error | 改为handleApiError |

### 10.6 核心文件 vs 非核心文件

**说明**：本次补充后覆盖率：

| 分类 | 文件 | 设计覆盖 |
|------|------|----------|
| **核心文件** | NewChatContainer+sse.ts+api.ts+chatMessages | 100% ✅ |
| **扩展文件** | Settings+History | 100% ✅ |

**设计范围说明**：
- v1.3核心范围：聊天流程错误处理
- v1.4扩展范围：全站点统一错误处理（本补充为可选扩展）

### 10.7 审查结论

| 分类 | 设计有 | 设计无 | 覆盖率 |
|------|--------|--------|---------|
| 核心文件 | 51 | 0 | 100% ✅ |
| 扩展文件 | 0 | 47 | 0% |
| 总计 | 51 | 47 | 52% |

小健审查建议已补充，采用与前面一致的统一处理方法。核心文件100%覆盖，扩展文件可选。

---

**文档结束**

---

## 九、版本历史

| 版本 | 时间 | 签名 | 更新内容 |
|------|------|------|---------|
| v1.0 | 2026-04-11 18:30:00 | 小强 | 初始版本 |
| v1.1 | 2026-04-11 20:10:00 | 小强 | 补充重试次数配置 |
| v1.2 | 2026-04-11 21:10:00 | 小强 | 补充8种遗漏错误类型，优化章节结构 |

---

**更新时间**: 2026-04-11 21:46:31
**版本**: v1.4
**更新内容**: 补充25种ErrorType到前文章节+实施计划+测试用例