# [3] 数据存储任务级表重构修改方案

**创建时间**: 2026-08-19 09:15:54
**编写人**: 小欧
**性质**: 计划规范（编辑型）

## 版本历史

| 版本 | 时间 | 更新人 | 更新内容 |
|------|------|--------|----------|
| v1.0 | 2026-08-19 09:15:54 | 小欧 | 首版：四大改动（表改名 / 清 execution_steps 列 / 落地 C1C2 / 清 title_history 死表）完整修改方案，供北京老陈审核 |
| v1.1 | 2026-08-19 09:30:40 | 小欧 | 应北京老陈要求新增**改动5：消息↔任务关联补全**（user 消息挂 task_id + assistant 回填 reply_to_message_id）；原第五、六章顺延为六、七章；D5 决策项范围更新为含改动5 |
| v1.2 | 2026-08-19 09:34:45 | 小欧 | 应北京老陈要求**新增加 0 章：四张主表字段梳理与合理性分析**（chat_sessions / chat_messages / chat_message_steps / chat_tasks 全字段 + 索引 + 实测使用率 + 缺失/多余/查询便利判定）；全篇章号重排为 1~10 |
| v1.3 | 2026-08-19 09:41:55 | 小欧 | ① 4 张字段表末列新增**「建议删除」打 X** 标注（metadata×3、device/network、execution_steps、display_name，其余留空）；② 应北京老陈要求新增 **2.11 字段命名名实核对**：结论四表无必须改名字段，`execution_steps` 删而不改、`chat_task_steps.message_id` 保留加注（task 主/message 降级） |
| v1.4 | 2026-08-19 09:54:19 | 小欧 | ① 新增 **2.12 字段名 ↔ 代码变量名对应核对**：全栈（DB↔后端 DTO/函数↔前端类型）蛇形命名贯通，仅 `step_json`↔入参 `step_dict` 一处语义对应差异；② 应北京老陈定调：**命名以后端系统为准，前端必须与后端对齐**（AGENTS 1.5），写入 2.12 原则 |
| v2.0 | 2026-08-19 10:37:31 | 小欧 | **核心数据模型重构定稿**（北京老陈 2026-08-19 拍板）：① 新建 **`chat_user_message`** 表承载"user 消息 + AI 最终回答汇总"（去 role，含 response/reasoning/outcome/model/provider/accumulated_usage，与 final 字段对齐）；② 老 **`chat_messages` 冻结废弃**（新链路不写、存量历史降级可读）；③ `chat_message_steps`→`chat_task_steps` 改名；④ `chat_tasks` 保留为汇总视图；⑤ C1/C2 走新链路。**以 v2.0 定稿章为最终准绳，v1.x 各章为讨论依据（保留不删）** |
| v2.1 | 2026-08-19 10:44:55 | 小欧 | ① 2.3 核对 `chat_task_steps` 字段（后端取证）：现有列定位/排序/关联/step_json 内容承载**完全够**，**唯一缺"每一轮的 token 信息"**；② 新增 `usage` 列承载每步所属轮 token（`{prompt/completion/total_tokens}`，对齐 token_usage/_usage_events 口径）；③ 3.1/3.6 同步（步骤表每轮 token 列 + 改动清单第 8 项） |

## 1、方案背景与目标

当前聊天存储存在 5 处"名实不符 / 冗余双写 / 关联缺失 / 未落地接口"问题，本方案一次性收敛：

1. **表名 `chat_message_steps` 名实不符**：该表实际存的是**任务级整套执行步骤**（一行一步，已挂 `task_id`），但名字暗示"消息级步骤"。按原始设计意图（一个 task 对应一整套 step 消息）改名 `chat_task_steps`。
2. **`chat_messages.execution_steps` 旧列仍在双写 + 回退读取**：系统同时把步骤快照写进 `chat_messages.execution_steps` 列（整块 JSON），而权威步骤早已逐步落库 `chat_message_steps`。存在双重写入、违反 DRY 与「禁止backward」。
3. **任务级回放/统计接口（C1/C2）未实现**：现有 `chat_tasks` 已建行且有步骤流可查，但无按 `task_id` 的详情统计（C1）与步骤回放（C2）。只有按 message 的降级回放（C3，`execution_stream`）。
4. **`chat_session_title_history` 死表未清理**：建表 + 写入函数 + 调用链均已存在，但系统已不再消费该历史，属待清理死表。
5. **消息↔任务关联缺失**：`chat_messages` 中 **user 消息从不挂 `task_id`**（994 条全 None），`reply_to_message_id` 关联字段全表 0 条被填（废字段）。task↔user 唯一关联在 `chat_tasks.user_message_id`，聊天消息表内部无法 self 达任务/互指 user↔assistant。

> 本文档所有【已证实】引用均经本地代码逐一核对。
---
## 2、四张主表字段梳理与合理性分析

> 数据来源：现网 `~/.omniagent/chat_history.db` 实测（PRAGMA table_info / sqlite_master 索引 / 字段空值率）+ `db_initializer.py` DDL 逐项核对。【CH=chat_history 库】【DDL=建表脚本】【实测=现网查询】
> "使用率" = 该字段非空且非空串的记录数 / 表总行数，用于判死字段。

### 2.1 chat_sessions（会话表）字段清单

| 字段 | 类型 | 约束 | 实测使用率 | 判定 | 建议删除 |
|------|------|------|-----------|------|------|
| id | TEXT | PK | 100% | 会话唯一标识，正常 | |
| title | TEXT | NOT NULL | 100% | 会话标题，正常 | |
| created_at | TIMESTAMP | | 100% | 创建时间，正常 | |
| updated_at | TIMESTAMP | | 100% | 更新时间（排序用），正常 | |
| message_count | INTEGER | | 1075/1075 | 消息计数，正常（S2 每任务+1） | |
| is_deleted | BOOLEAN | | 1075/1075 | 软删除，正常 | |
| is_valid | BOOLEAN | | 1075/1075 | 会话有效性（save_message 自愈），正常 | |
| title_locked | BOOLEAN | | 1075/1075 | 标题锁定开关，正常 | |
| title_updated_at | TIMESTAMP | | 1074/1075 | 标题更新时间，正常 | |
| version | INTEGER | | 1075/1075 | 会话版本，正常 | |
| metadata | TEXT | | **0/1075** | 【多余·死字段】零使用 | **X** |
| model_override | TEXT | | **0/1075** | 【待用】功能已落地（get_session_model_override 读 / 会话配置写），当前无配置未触发，保留 | |

### 2.2 chat_messages（消息表）字段清单

| 字段 | 类型 | 约束 | 实测使用率 | 判定 | 建议删除 |
|------|------|------|-----------|------|------|
| id | INTEGER | PK | 100% | 消息ID，正常 | |
| session_id | TEXT | NOT NULL | 100% | 所属会话，正常 | |
| role | TEXT | NOT NULL | 100% | user/assistant/unknown，正常 | |
| content | TEXT | NOT NULL | 100% | 消息内容，正常 | |
| timestamp | TEXT | | 100% | 时间（排序用），正常 | |
| execution_steps | TEXT | | 144/2132 | 【多余·改动2清】双写快照列 | **X** |
| display_name | TEXT | | **0/2132** | 【多余·半死】insert_assistant_message 有写路径但主路径 allocate 未写，零使用；展示可改从 steps 提取 | **X** |
| client_os | TEXT | | 72/2132 | 【半用】仅 unknown 消息带，保留 | |
| browser | TEXT | | 72/2132 | 【半用】同上，保留 | |
| device | TEXT | | **0/2132** | 【多余·死字段】零使用 | **X** |
| network | TEXT | | **0/2132** | 【多余·死字段】零使用 | **X** |
| reply_to_message_id | INTEGER | | **0/2132** | 【待用】字段已建，未填；**改动5将启用**（assistant→user 互指） | |
| status | TEXT | | 840/2132 | 消息终态（completed 等），正常 | |
| thought | TEXT | | 831/2132 | 思考内容（finalize_message 写），正常 | |
| task_id | TEXT | | 26/2132 | 【在用·改动5扩大】现仅 26 条 assistant 挂任务号；改动5回填 user 消息 | |
| metadata | TEXT | | **0/2132** | 【多余·死字段】零使用 | **X** |

### 2.3 chat_message_steps（→ `chat_task_steps` 步骤表）字段清单

| 字段 | 类型 | 约束 | 实测使用率 | 判定 | 建议删除 |
|------|------|------|-----------|------|------|
| id | INTEGER | PK | 100% | 步骤行ID，正常 | |
| message_id | INTEGER | NOT NULL FK→chat_messages | 100% | 所属消息，正常 | |
| session_id | TEXT | NOT NULL | 100% | 所属会话（冗余，供会话级回放免 join，可接受） | |
| step_index | INTEGER | NOT NULL | 100% | 步骤序号，正常 | |
| step_json | TEXT | NOT NULL | 100% | 步骤 JSON 体，正常 | |
| created_at | TIMESTAMP | | 100% | 落库时间，正常 | |
| task_id | TEXT | | 267/1504092 | 【在用·极低】历史 150 万行无 task_id（仅 267 行=26 任务）；改名 chat_task_steps 后新数据 100% 挂载 | |
| usage | TEXT(JSON) | | 待新增 | **【需新增·每轮 token】** 该步骤所属 LLM 轮的 token 明细 `{prompt_tokens, completion_tokens, total_tokens}`，口径与 `token_usage`/`agent._usage_events` 对齐；每行一步、可带所属轮 token（北京老陈 2026-08-19 定） | |
| — | — | — | — | 无多余字段，结构精简（加 usage 后 8 列）最优 | |

### 2.4 chat_tasks（任务表）字段清单

| 字段 | 类型 | 约束 | 实测使用率 | 判定 | 建议删除 |
|------|------|------|-----------|------|------|
| id | INTEGER | PK | 100% | 自增主键，正常 | |
| task_id | TEXT | NOT NULL UNIQUE | 100% | 任务ID（全局唯一，自动索引），正常 | |
| session_id | TEXT | NOT NULL | 100% | 所属会话，正常 | |
| user_message_id | INTEGER | | 100% | 指向 chat_messages 的 user 消息，**task↔user 关联枢纽**，正常 | |
| user_input | TEXT | | 26/26 | 用户输入快照（任务不可变，免 join），**设计性冗余·合理** | |
| response | TEXT | | 26/26 | 助手回答快照（同上），**设计性冗余·合理** | |
| artifacts | TEXT | | 26/26 | 产物列表（当前全 `[]`，收集逻辑未落地【C3 范畴】），保留 | |
| status | TEXT | | 26/26 | 任务终态（当前全 completed），正常 | |
| start_time | TEXT | | 26/26 | 开始时间，正常 | |
| end_time | TEXT | | 26/26 | 结束时间，正常 | |
| duration | REAL | | 26/26 | 耗时秒，正常 | |
| context_link_mode | TEXT | | 26/26 | 上下文链模式（当前全 independent，续聊功能落地未触发），保留 | |
| context_root_task_id | TEXT | | 26/26 | 链根任务ID（同上未触发），保留 | |
| provider | TEXT | | 26/26 | 供应商，正常 | |
| model | TEXT | | 26/26 | 模型名，正常 | |
| display_name | TEXT | | 26/26 | 展示名（provider (model)），正常 | |
| accumulated_usage | TEXT | | 26/26 | token 汇总 JSON（与 token_usage 明细并存，汇总副本），**设计性冗余·合理** | |
| llm_call_count | INTEGER | | 26/26 | LLM 调用数，正常 | |
| total_steps | INTEGER | | 26/26 | 步骤总数（C1 复用），正常 | |
| retry_count | INTEGER | | 26/26 | 重试次数，正常 | |
| max_steps | INTEGER | | 26/26 | 最大步数上限，正常 | |
| error_type | TEXT | | **0/26** | 错误类型（错误终态字段，未触发），保留 | |
| error_message | TEXT | | **0/26** | 错误信息（同上），保留 | |
| metadata | TEXT | | **0/26** | 【多余·死字段】零使用 | **X** |

### 2.5 索引现状

| 表 | 索引 | 列 | 判定 |
|----|------|----|------|
| chat_sessions | idx_sessions_updated | updated_at DESC | 会话列表排序，正常 |
| chat_sessions | idx_sessions_deleted | is_deleted | 软删除过滤，正常 |
| chat_messages | idx_messages_session | session_id | 会话消息查询，正常 |
| chat_messages | idx_messages_timestamp | timestamp | **【冗余·重复】** 与下重复 |
| chat_messages | idx_msg_timestamp | timestamp | **【冗余·重复】** 同列双索引，清其一 |
| chat_messages | idx_msg_task | task_id | 任务→消息（C 回放），正常（改动5后覆盖面扩大） |
| chat_message_steps | idx_steps_message | message_id | 消息步骤回放（C3），正常 |
| chat_message_steps | idx_steps_session | session_id, step_index | 会话步骤回放，正常 |
| chat_message_steps | idx_steps_task | task_id, step_index | 任务步骤回放（C2），正常 |
| chat_tasks | idx_tasks_session | session_id | 会话任务列表，正常 |
| chat_tasks | task_id UNIQUE | 自动 | 任务唯一，正常 |
| token_usage / chat_session_trust | 各索引 | — | 明细表索引，正常 |

### 2.6 合理性总评

> 四表命名规范、主键/外键/类型合理，索引基本覆盖存量查询路径。问题集中为"**冗余与死字段**"而非"命名缺失"。

- **命名/结构**：合理，无命名歧义。
- **关键冗余判断**：
  1. `chat_messages.execution_steps` 列 = 双写快照（改动2 清）。
  2. `chat_messages` 的 `timestamp` **双索引**（idx_messages_timestamp + idx_msg_timestamp）。
  3. `metadata` 在 chat_sessions / chat_messages / chat_tasks 三表均为 **0 使用（死字段）**。
  4. `chat_messages.device / network` 0 使用（死字段）。
- **设计性冗余（保留）**：chat_tasks 的 `user_input/response/accumulated_usage` 为任务"快照/汇总"语义，返不违反 DRY 的合理落地；`message_id+session_id` 双列在步骤表为免 join 冗余，可接受。




---

### 2.7 缺失字段（需补）

| 缺失 | 补法 | 归属 |
|------|------|------|
| user 消息 `task_id` | 任务启动后回填（改动5） | 改动5 |
| `reply_to_message_id` 实际写入 | assistant 落库时回填（改动5，字段已有） | 改动5 |
| `chat_messages` 未来 C1 回放按 task 查消息 | 索引已建（idx_msg_task），够用 | 改动3 |
| `chat_tasks` (session_id, created_at) 排序索引 | 可选，现 26 行规模小，暂不加 | 非必须 |

### 2.8 多余字段（可清）

| 字段/索引 | 表 | 依据 | 处置 |
|-----------|----|------|------|
| metadata | chat_sessions / chat_messages / chat_tasks | 3 表均 0 使用 | 随重构清理（列入改动清单） |
| device / network | chat_messages | 0 使用 | 随重构清理 |
| execution_steps 列 | chat_messages | 双写快照 | 改动 2 已计划 |
| display_name | chat_messages | 0 使用（半死） | 改动5 顺手统一（清或补写，待决策） |
| idx_msg_timestamp / idx_messages_timestamp 之一 | chat_messages | 同列双索引 | 随重构清理其一 |
| display_name（steps 提取 vs 列） | chat_tasks | 26/26 在使用 | 保留 |

> 说明：以上清理项中 `metadata`、`device/network`、双索引**尚未计入前 5 改动**，建议作为第 6 项"**删死字段/冗余索引**"纳入本次。

### 2.9 查询便利性分析

- **会话维度**：会话列表/详情（session_id、updated_at、is_deleted 索引）✅
- **消息维度**：按会话查消息（idx_messages_session）、按任务查消息（idx_msg_task）✅
- **步骤维度**：按消息（idx_steps_message / C3）、按任务（idx_steps_task / C2）、按会话（idx_steps_session）✅
- **任务维度**：按任务（task_id UNIQUE）、按会话（idx_tasks_session）✅
- **结论**：C1/C2/C3 查询均已有直达索引，**查询路径便利**；唯一优化点是清理冗余双索引与死列，无新索引刚需。

### 2.10 对照各改动处置汇总

| 处置 | 涉及字段/表 | 归属改动 |
|------|-------------|---------|
| 新增 | user 消息 task_id 回填、reply_to_message_id 写入 | 改动 5 |
| 新增 | C1/C2 按 task_id 查询 | 改动 3 |
| 清理 | chat_messages.execution_steps 列 | 改动 2 |
| 清理 | chat_session_title_history 表 | 改动 4 |
| 清理 | chat_message_steps → 改名 chat_task_steps | 改动 1 |
| 清理（新） | metadata（3 表）/ device / network / timestamp 双索引 | 建议并入改动6 |
| 保留 | model_override / client_os / browser / chat_tasks 快照与 error / context_link 字段 | — |

### 2.11 字段命名名实核对（是否需要改名）

> 原则：名随实、实合名；改名以有真实收益为前提（YAGNI，禁为改而改）。逐表核对名实是否相符：

| 表 | 字段 | 名实核对 | 是否需要改名 |
|----|------|---------|-------------|
| chat_sessions | message_count | 名"消息计数"，存会话内消息总条数（S2 后 user+assistant 独立行均计入），名实一致 | 否 |
| chat_sessions | title_locked / title_updated_at / version | 标题锁定/更新时间/版本，名实一致 | 否 |
| chat_messages | role / content / timestamp / status / thought | 角色/内容/时间/终态/思考，名实一致 | 否 |
| chat_messages | execution_steps | 名为"步骤快照"，但该列语义与消息表定位矛盾，且步骤归属已迁 `chat_task_steps`（任务级） | **不改名·直接删**（改动 2）——改名为 churn 无收益，列本应退役 |
| chat_messages | reply_to_message_id | 名"回复的消息 id"，改动5后实为"该 assistant 回答对应的 user 消息 id"，名实一致 | 否（字段保留即用） |
| chat_messages | display_name | 名"展示名"，存于消息表但 0 使用（半死）；展示名应从步骤/任务侧提取 | 不改名·建议删除（已打 X） |
| chat_message_steps | message_id | 表改名为 `chat_task_steps`（任务语义）后，**主关联列仍名为"message"**，名实偏"消息" | **不改名·保留**：C3 按 message 回放仍需要，保留并界定"task_id 为主维度、message_id 为降级关联" |
| chat_message_steps | step_index / step_json / created_at / task_id / session_id | 步骤序号/步骤体/时间/任务/会话，名实一致 | 否 |
| chat_tasks | user_input / response | 任务输入/最终回答快照，名实一致 | 否 |
| chat_tasks | display_name | 实为"模型展示名（provider (model)）"，名可接受 | 否（若求更精确可名 `model_display_name`，收益低，不改） |
| chat_tasks | duration / accumulated_usage / llm_call_count / total_steps / retry_count / max_steps / error_type / error_message / context_* | 耗时/token汇总/各计数/错误/上下文链，名实一致 | 否 |

**结论**：四表**无必须改名字段**。两处名实微妙点处置：
1. `chat_messages.execution_steps` → **删除（改动 2）**，不靠改名解决（列本应退役）。
2. `chat_task_steps.message_id` → **保留**，在表注释明确"task_id 为权威主维度、message_id 为按消息降级回放关联"，避免名实误导。

### 2.12 字段名 ↔ 代码变量名对应核对

> 原则（北京老陈 2026-08-19 定调）：**DB 列名与代码变量名（DTO 字段 / 函数参数 / 前端类型）应同名贯通**。变量名和字段名差异过大会导致"读代码看不懂字段、看字段对不上代码"，维护灾难。
> **命名权威：以后端系统为准，前端必须与后端对齐**（AGENTS.md 1.5：后端为主、前端迎合后端）。后端 DTO 字段名 / 函数参数名为权威，前端类型定义必须同名，不另起别名。逐列核对【已证实】：

| 表 | DB 列 | 代码引用（DTO 字段 / 函数参数 / 前端） | 一致 |
|----|-------|--------------------------------------|------|
| chat_sessions | id / title / created_at / updated_at / message_count / is_valid | `Session`/`SessionResponse`（chat_models.py:15/:40）同名 | ✅ |
| chat_sessions | is_deleted / title_locked / title_updated_at / version | session_service 读写同名 | ✅ |
| chat_sessions | metadata / model_override | 读写函数同名 | ✅ |
| chat_messages | id / session_id / role / content / timestamp / thought / display_name / execution_steps | `MessageResponse`（chat_models.py:63-72）同名 | ✅ |
| chat_messages | status / reply_to_message_id / content | `ExecutionStepsUpdate`（storage.py:89-94）同名 | ✅ |
| chat_messages | task_id / session_id | `allocate_and_insert_message`（storage.py:283）参数同名 | ✅ |
| chat_messages | client_os / browser / device / network | message_service INSERT 同名 | ✅ |
| chat_messages | metadata | 读写同名 | ✅ |
| chat_message_steps | message_id / session_id / step_index / task_id | `append_execution_step`（storage.py:375）参数同名 | ✅ |
| chat_message_steps | step_json | 入参名 `step_dict`（入参是 dict、列存 JSON 字符串） | ⚠️ 语义对应、名略异 |
| chat_tasks | task_id / session_id / user_message_id / user_input / context_link_mode / context_root_task_id / provider / model / display_name | `insert_task`（storage.py:427）参数全同名 | ✅ |
| chat_tasks | status / total_steps / llm_call_count / … 终态字段 | `update_task`（storage.py:446）`**kw` 同名 | ✅ |
| 前端 | execution_steps / reply_to_message_id / content | `frontend/src/services/api.ts:676-694` saveExecutionSteps 同名透传 | ✅ |

**结论**：全栈（DB ↔ 后端 DTO/函数 ↔ 前端类型）**蛇形命名贯通**，无"文档一套、代码一套"。唯一差异点处置：
- `chat_message_steps.step_json`（列名）↔ `append_execution_step(step_dict)`（入参名）：语义对应明确（dict 入参→JSON 落列），可接受不改（YAGNI）；如需彻底同名，可将入参更名 `step_json`，收益低，本次不动。
- **附注**：`MessageResponse` 目前未暴露 `task_id / status / reply_to_message_id`（DTO 未含，属"未透出"而非"命名不一致"）；改动 3 落地 C1/C2 时若前端需展示，补充 DTO 字段并**沿用同名**贯通。

---

## 3、v2.0 定稿：核心数据模型重构（以本章为最终准绳）

> **决策（北京老陈 2026-08-19 拍板）**：v1.x 各章为讨论依据；本 v2.0 定稿为最终可执行依据。凡与 v2.0 冲突处，以 v2.0 为准。
> 背后主线：`chat_messages` 名实不符（混存 user/assistant 双角色、assistant 信息散乱）→ **新建专属 user 消息表，assistant 信息与 final 字段对齐**，老表冻结废弃。

### 3.1 定稿后的四表数据模型

| 表 | 定位 | 说明 |
|----|------|------|
| **`chat_user_message`**（新建） | user 消息 + AI 最终回答汇总 | `content`(用户输入) + `response/reasoning/outcome/model/provider/accumulated_usage`（对齐 final）+ `session_id`/`task_id` + client 采集字段 + `created_at` |
| **`chat_task_steps`**（原名 `chat_message_steps`，改名） | AI 响应的全部执行 step | final step 完整信息（response/reasoning/outcome/model/provider/accumulated_usage）均在其 `step_json`；**每行新增 `usage` 列承载该轮 token** |
| **`chat_tasks`**（保留） | 任务汇总视图 | `response` 快照 + `model/provider/accumulated_usage` + 统计（status/duration/llm_call_count/total_steps…） |
| **`chat_messages`**（**冻结废弃**） | 存量历史（不写新链路，降级可读） | 保留 2132 条历史供前端降级展示；不再有代码写入；后续择机清理 |

关系：`chat_user_message.task_id` ⇄ `chat_tasks.task_id`；`chat_task_steps.task_id/message_id` ⇄ 任务/消息；`chat_tasks.user_message_id` → `chat_user_message.id`。

### 3.2 新建 `chat_user_message` 表（最终字段清单）

| 字段 | 类型 | 来源/对齐 | 处置 |
|------|------|-----------|------|
| id | INTEGER PK | 自增 | 保留 |
| session_id | TEXT NOT NULL | 所属会话 | 保留 |
| content | TEXT NOT NULL | 用户输入（去 role 后恒为 user） | 保留 |
| task_id | TEXT | 关联任务（= chat_tasks.task_id） | 在（改动7 挂接） |
| response | TEXT | = **FinalStep.response**（AI 最终回答） | 新增（对齐 final） |
| reasoning | TEXT | = **FinalStep.reasoning**（推理过程，原 `chat_messages.thought` 语义） | 新增（对齐 final） |
| outcome | TEXT | = **FinalStep.outcome**（终态 completed/failed/cancelled，原 `chat_messages.status` 语义） | 新增（对齐 final） |
| model | TEXT | = **FinalStep.model** | 新增（对齐 final） |
| provider | TEXT | = **FinalStep.provider** | 新增（对齐 final） |
| accumulated_usage | TEXT | = **FinalStep.accumulated_usage**（token 累计） | 新增（对齐 final） |
| client_os / browser / device / network | TEXT | 客户端采集（功能未落地，老陈定保留） | 保留（待用） |
| created_at | TEXT | 落库时间 | 新增 |

> **不进入新表的旧气泡字段**（随老 `chat_messages` 一并废弃，无需新表承担）：`role`、`execution_steps`、`display_name`、`status`、`thought`、`metadata`、`reply_to_message_id`（新表以 `task_id` 关联替代）。
> **成果**：一行 `chat_user_message` 即可取出"用户问什么 + AI 怎么答 + 用什么模型 + token 花多少 + 终态 outcome"，前端直接用，无需 join。

### 3.3 与 FinalStep 字段对齐（最终命名）

| FinalStep 字段 | chat_user_message 列 | 原 chat_messages 对应 |
|---------------|----------------------|----------------------|
| response | response | content（assistant 行） |
| reasoning | reasoning | thought |
| outcome | outcome | status |
| model | model | （无，仅 display_name 展示串） |
| provider | provider | （无） |
| accumulated_usage | accumulated_usage | （无，chat_tasks 有） |

### 3.4 老 `chat_messages` 冻结废弃处置

- **不再有任何新链路代码写入** `chat_messages`（用户发消息、assistant 落库全部改走 `chat_user_message` + `chat_task_steps`）。
- 存量数据（user 994 + assistant 1066，共 2132 条）**保留不动**，供前端历史消息降级展示；数据回读处设"新表有则读新表、无则读老表"的降级。
- 冻结后，原"清 execution_steps 列 / 删死字段（status/thought/display_name/metadata/device/network）"的清理**不再逐一执行**——整表逐步废弃即统一覆盖，避免在老表上做零碎 DDL。
- 保留 `chat_messages` 元信息（索引）即可，不新建索引。

### 3.5 对 v1.x 各改动的调整

| v1.x 改动 | v2.0 调整 |
|-----------|-----------|
| 改动1 `chat_message_steps`→`chat_task_steps` 改名 | **保留执行**（步骤表始终需要，改名不受影响） |
| 改动2 清 `chat_messages.execution_steps` 列 | **不再单独清列** → 并入"chat_messages 冻结废弃"（不写旧列即可，历史列随表废弃） |
| 改动3 C1/C2 | **数据源改 `chat_user_message` + `chat_task_steps`**（任务详情读新表/任务表，步骤回放读步骤表） |
| 改动4 清 `chat_session_title_history` 死表 | **保留执行**（独立死表，与 chat_messages 无关，仍清理） |
| 改动5 消息↔任务关联 | **落到新表**：`chat_user_message.task_id` 挂接 + `chat_task_steps.task_id` 贯通（原 reply_to 方案被新表 task_id 关联取代） |
| 清理死字段（原 2.8 / 计划改动6） | **在 chat_messages 走"整体废弃"路径统一处理**，不再在新表引入这些字段 |

### 3.6 v2.0 改动清单（最终）

| # | 改动 | 要点 |
|---|------|------|
| 1 | 新建 `chat_user_message` 表 + DDL + 字段 | 见 3.2 |
| 2 | 新链路写入：user 消息 + final 汇总落 `chat_user_message` | stream_orchestrator / 路由改造，最终回答 final 字段回填（response/reasoning/outcome/model/provider/accumulated_usage） |
| 3 | `chat_message_steps` → `chat_task_steps` 改名 | 原改动1 |
| 4 | 老 `chat_messages` 冻结废弃 + 降级读取 | 见 3.4 |
| 5 | C1/C2 走新链路 | 见 3.5 |
| 6 | 清 `chat_session_title_history` 死表 | 原改动4 |
| 7 | 消息↔任务关联落新表 | `chat_user_message.task_id` / `chat_task_steps.task_id` |
| 8 | `chat_task_steps` 加"每轮 token"`usage` 列 | 每行一步、可带所属 LLM 轮 usage `{prompt/completion/total_tokens}`；数据源 `agent._usage_events`/LLM 轮，与 `token_usage` 同口径（北京老陈 2026-08-19 定） |

### 3.7 迁移要点

- 现网 `chat_messages` 2132 行冻结，**不迁移**到新表（历史不补，降级可读）。
- `chat_message_steps` 150 万行 → RENAME `chat_task_steps`（幂等）。
- 新表 `chat_user_message` 随新链路从零积累；旧会话历史在历史上看旧表。
- 需停服窗口执行 DDL（新建表 / 改名表），无并发写入。

### 3.8 验收

- 新发一条消息后：`chat_user_message` 一行（含 final 汇总字段）、`chat_task_steps` 含全部 step、`chat_tasks` 汇总，`chat_messages` **无新增写入**。
- 前端历史消息：老会话走 `chat_messages` 降级展示；新会话走新链路，字段正确。
- C1/C2 返回按 `chat_task_steps` + `chat_user_message` 拼出的完整回放。
- 全量 pytest 无新增失败。


## 4、改动 1：`chat_message_steps` → `chat_task_steps`

### 4.1 目标
将步骤表改为任务语义命名，名实相符。改动仅"表标识"，不改结构与数据。

### 4.2 波及代码（【已证实】）
| # | 文件:行 | 现状 | 改为 |
|---|---------|------|------|
| 1 | `backend/app/db/db_initializer.py:68` | `CREATE TABLE IF NOT EXISTS chat_message_steps (...)` | `CREATE TABLE IF NOT EXISTS chat_task_steps (...)` |
| 2 | `backend/app/services/chat/storage.py:382` | `INSERT INTO chat_message_steps(...)` | `INSERT INTO chat_task_steps(...)` |
| 3 | `backend/app/services/chat/storage.py:395` | `SELECT step_json FROM chat_message_steps WHERE message_id=? AND task_id=?` | `chat_task_steps` |
| 4 | `backend/app/services/chat/storage.py:400` | `SELECT step_json FROM chat_message_steps WHERE message_id=?` | `chat_task_steps` |

### 4.3 关联安全说明（【已证实】）
- 该表仅自身带外键（`message_id → chat_messages.id`, `session_id → chat_sessions.id`），**无任何表外键指向它**，SQLite `ALTER TABLE ... RENAME TO` 无阻。
- 现网该表 1,504,092 行，其中 **`task_id` 为 NULL 的 1,503,825 行**（历史消息级数据），仅 267 行挂 `task_id`（26 个任务）。RENAME 不影响数据；改名后这些历史行仍在，`task_id` 为空行的任务级回放不命中（见 2.3 数据现状）。

### 4.4 现网迁移（【已证实】数据结构）
幂等迁移脚本（注册到 `schema_migrations`，一次性）：
```
IF 存在表 chat_message_steps 且 不存在 chat_task_steps:
    ALTER TABLE chat_message_steps RENAME TO chat_task_steps
```
新库直接按新 DDL 建 `chat_task_steps`。迁移登记名建议 `rename_chat_message_steps_to_chat_task_steps`。

### 4.5 文档同步
版本库文档中 `chat_message_steps` 引用需替换为 `chat_task_steps`（直接改文档，不打勾/不加注释）：
- 文档[1]：`chat_message_steps` 共 **63** 处
- 文档[2]：`chat_message_steps` 共 **48** 处

### 4.6 验收
- 建新库后表名为 `chat_task_steps`；现网库迁移后表名 `chat_task_steps`，行数 1,504,092 不变。
- 全量 pytest 无新增失败。

---

## 5、改动 2：移除 `chat_messages.execution_steps` 列（步骤唯一归 `chat_task_steps`）

> 核心认知：`save_execution_steps_to_db` / `save_execution_steps` **不能整体删除**——它除写步骤快照外，还负责 assistant 消息终态（`content/status/thought`）与消息分配/计数（`insert_assistant_message` / `update_session_message_count`）。因此本改动是"**剥离列快照职责、保留消息终态职责**"，把步骤权威唯一收敛到 `chat_task_steps`。

### 5.1 写入点剥离（【已证实】）
| # | 文件:行 | 现状 | 改为 |
|---|---------|------|------|
| 1 | `storage.py:209-211` | `update_message_fields` 中 `fields.append("execution_steps = ?")`（步骤整块快照写入列） | **删除**该字段构建；`update_message_fields` 仍写 `content/status/thought` 等终态字段。`save_execution_steps` 职责收敛为消息终态落库 |
| 2 | `message_service.py:120-124` | `INSERT INTO chat_messages(..., execution_steps, ...)` | 从 INSERT 列与取值中移除 `execution_steps` |
| 3 | `db_initializer.py:53` | chat_messages DDL 含 `execution_steps TEXT` | **删除**该列定义 |

### 5.2 读取点剥离（【已证实】）
| # | 文件:行 | 现状 | 改为 |
|---|---------|------|------|
| 1 | `storage.py:405-409` | `load_execution_steps` 中"无 `chat_message_steps` 数据时回退读 `chat_messages.execution_steps` 列" | **删除**回退分支；仅查 `chat_task_steps`，无数据直接返回 `[]` |

### 5.3 历史迁移脚本处理（【已证实】）
- `migrate_steps.py:169` `SELECT id, execution_steps FROM chat_messages`：该一次性迁移已 `applied`（注册守卫），正常启动不再扫描。但 **DDL 删除该列后，新库一旦触发此迁移将因列不存在报错**。
- 处置（择一，待决策）：
  - A（建议）：在 `db_initializer` 末尾移除对该迁移的调用（该迁移守护旧列时代的数据表达改写，旧列退役后失去意义）。
  - B：在 `migrate_execution_steps_status` 内加"列存在"守卫（`PRAGMA table_info(chat_messages)` 判断 `execution_steps` 列存在才执行）。

### 5.4 现网 144 行历史数据保底迁移（【已证实】数据）
现网 `chat_messages` 中 `execution_steps IS NOT NULL` 共 **144** 行。删除列前需一次性保底，**确保回放不丢**：
```
对每条 chat_messages WHERE execution_steps IS NOT NULL:
    IF chat_task_steps 中无该 message_id 的行:
        将该列 JSON 解析后逐条 INSERT 进 chat_task_steps（task_id=NULL，step_index 还原）
    （若 chat_task_steps 已有该 message_id 的行 → 列属冗余，直接忽略）
完成后再实施 DROP COLUMN 语义（见 5.5）
```
迁移登记名建议 `backfill_steps_from_execution_steps_column`。

### 5.5 列删除落地方式（SQLite 约束）
SQLite 3.35+ 支持 `ALTER TABLE chat_messages DROP COLUMN execution_steps`。
- 若环境 SQLite 版本 < 3.35，则退化为"重建表"（CREATE new → COPY → DROP → RENAME），需在迁移脚本内探测 `PRAGMA user_version`/`sqlite_version()` 判走。
- 必须先完成 5.4 保底，再删列（避免恢复无门）。

### 5.6 文档同步
文档[1] `execution_steps` 共 **55** 处、文档[2] 共 **21** 处，按"决策域为 `chat_task_steps`、旧列已退役"统一修订描写。

### 5.7 验收
- 后端保存 assistant 消息后：`chat_messages.execution_steps` 不再被写（`execution_steps IS NULL`）；步骤全部在 `chat_task_steps`。
- 回放（含历史 144 行消息）步骤不丢，顺序正确。
- 主链路（SSE 流式）content/status 仍正常落库。
- 全量 pytest 无新增失败。

---

## 6、改动 3：落地 C1 / C2 任务级回放与统计

> 背景定义（文档2 4.8.2，【已证实】）：C1=任务详情统计（`chat_tasks` + 工具统计重建）；C2=按 `task_id` 步骤回放；C3=按 message 降级回放（`execution_stream` 已实现）。现 C1/C2 缺失。

### 6.1 C1 任务详情统计
- **数据基础【已证实】**：`chat_tasks` 建行终态已由 `stream_orchestrator`（`insert_task` / `update_task`，storage:427/446）落地，含 `status/total_steps/llm_call_count/error_*` 等；`token_usage` 已按 task 记 token。
- **新增存储函数**：`storage.get_task_detail(conn, task_id)` → 读 `chat_tasks` 单行（`WHERE task_id=?`）。
- **新增工具统计重建**：`storage.get_task_tool_stats(conn, task_id)` → 从 `chat_task_steps` 该 task 的步骤流中，`COUNT`（`type='action'` 的 `tool_name`）聚合。
- **新增开局校验**：需确认 `update_task` 的 `total_steps` 是否已落（当前 S2 已实现终态）；若总步数与步骤数不一致，以 `COUNT(chat_task_steps)` 为准在地重建口径（口径以 COUNT 重建为准）。

### 6.2 C2 按 task_id 步骤回放
- **新增存储函数**：`storage.load_steps_by_task(conn, task_id)` → `SELECT step_json FROM chat_task_steps WHERE task_id=? ORDER BY step_index ASC`，复用现有 `parse_json` 组装（与 `load_execution_steps` 返回结构一致）。
- 【已证实】现无 `load_execution_steps_by_task` 同类函数，需新建（先查后建，登记 `FUNCTIONS.md`，禁止局部重造）。
- **新路由**：
  - C2 步骤回放：`GET /api/v1/chat/execution/task/{task_id}/steps`
  - C1 任务详情：`GET /api/v1/chat/execution/task/{task_id}`（含详情 + tool_stats）
- 建议落在 **chat 域**（与 `execution_stream` C3 同域），避免与 `agent/task_tracker` 域（`tasks/{id}`）冲突。

### 6.3 C3 保留
`execution_stream` 按 message DB 回放（降级路径）保留不动，作为并行能力。

### 6.4 文档同步
文档[2] 4.8.2 相关接口定义在落地后核对修订，标注 C1/C2 已实现。

### 6.5 验收
- 对一个已完成任务，C1 返回 `chat_tasks` 详情与 tool_stats 正确；C2 返回按 `step_index` 升序的完整步骤序列。
- 历史 `task_id=NULL` 的 150 万行：C2 不命中（无 task_id），属预期（只覆盖新任务）。是否回填历史 `task_id`（按 message→task 反查）为后续可选优化，本次不做。

---

## 7、改动 4：清理 `chat_session_title_history` 死表

### 7.1 目标
删除从未被消费的标题历史存储（建表 + 写入函数 + 调用），回归单一职责，杜绝死代码。

### 7.2 波及代码（【已证实】）
| # | 文件:行 | 现状 | 改为 |
|---|---------|------|------|
| 1 | `db_initializer.py:57-65` | `CREATE TABLE IF NOT EXISTS chat_session_title_history (...)` | **删除**该 DDL |
| 2 | `session_service.py:90-106` | `def record_title_history(...)`：`INSERT INTO chat_session_title_history` | **删除**该函数 |
| 3 | `session_service.py:205` | 调用 `record_title_history(cursor, session_id, old_title, ...)` | **删除**该调用 |
| 4 | `session_service.py:5/:24` | 顶部关于 `record_title_history` 的关系注释 | 同步校正（纯注释，随清理更新） |

### 7.3 现网清理（【已证实】表存在）
现网库中 `chat_session_title_history` 表存在。清理：
- 确认无业务流程读取该表后，`DROP TABLE chat_session_title_history`（独立表，无外键被引用，直接 DROP）。
- 迁移登记名建议 `drop_chat_session_title_history`。

### 7.4 边界确认
`record_title_history` 仅做"标题历史记录"写入，**不影响会话 title 主流程**（`chat_sessions.title` 更新仍在 `session_service` 正常执行）。清理后标题更新功能不受影响。

### 7.5 文档同步
- 文档[1]：`chat_session_title_history` 共 **10** 处；`title_history` 共 **12** 处。
- 文档[2]：`chat_session_title_history` 共 **12** 处；`title_history` 共 **21** 处。
- 统一标记为已清理/移除（直接改文档，不加注释）；其中涉及"标题历史"设计描写的按现状收敛为"不落历史"。

### 7.6 验收
- 现网库无 `chat_session_title_history` 表；新库 DDL 不再建。
- 会话标题仍可正常更新/展示。
- 全量 pytest 无新增失败。

---

## 8、改动 5：消息↔任务关联补全（user 挂 task_id + user/assistant 互指）

> 来源：北京老陈 2026-08-19 现场核对实测数据后拍板纳入。实测：`chat_messages` 中 user 消息 994 条全部 `task_id=None`；`reply_to_message_id` 字段全表 0 条被填（废字段）；task↔user 唯一关联在 `chat_tasks.user_message_id`。

### 8.1 目标
补齐消息↔任务双向关联，使三者互达（KISS-DIRECT，不引入反向冗余存储）：
- user 消息挂 `task_id`（任务启动后回填）。
- assistant 消息落库时写 `reply_to_message_id = 其 user 消息 id` + `task_id`（已写）。

### 8.2 现状根因（【已证实】）
| 事实 | 证据 |
|------|------|
| 现网主用 assistant 插入走 S2 `allocate_and_insert_message` | `storage.py:283` `INSERT INTO chat_messages(id, task_id, session_id, role, content, timestamp)`——**无 `reply_to_message_id`** |
| 旧链路 `insert_assistant_message` 支持 `reply_to` | `storage.py:191-196` INSERT 含 `reply_to_message_id`，但非现网主路径 |
| user 消息插入早于 task_id 生成（时序） | 实测同任务 user id=1003854 / assistant id=1003855，task_id 在任务启动时才生成，故 user 消息落库时无号可挂 |

### 8.3 改动点（【已证实】落点）
| # | 文件:行 | 现状 | 改为 |
|---|---------|------|------|
| 1 | `stream_orchestrator.py`（db_ops.insert_task 闭包 :254-259） | 建 task 时仅 INSERT chat_tasks | 在 insert_task 后紧接 `UPDATE chat_messages SET task_id=? WHERE id=?`（`id=_user_msg_id`），为 user 消息回填任务号。`db_ops` 增 `link_user_task`（或并入 insert_task 回调） |
| 2 | `storage.py:283`（allocate_and_insert_message） | INSERT 无 `reply_to_message_id` | INSERT 增 `reply_to_message_id` 列，值 = 本任务的 `user_message_id`。`db_ops.allocate_and_insert` lambda（stream_orchestrator:246）需把 `_user_msg_id` 一并传入函数签名 |
| 3 | `chat_messages` DDL / `_ensure_column` | `reply_to_message_id` 列已存在（`db_initializer.py:129` 附近 ensure 列表内） | 无需新增列；user 侧不反存 assistant_message_id（KISS，靠 reply_to 单向可达） |

### 8.4 关联可达矩阵（改动后）
```
task ──chat_tasks.user_message_id──▶ user 消息
user 消息 ──task_id──▶ task
assistant 消息 ──task_id──▶ task ──chat_tasks.user_message_id──▶ user
assistant 消息 ──reply_to_message_id──▶ user 消息（直接互指）
```

### 8.5 文档同步
- 文档[2] 4.8.x 关联定义、消息落库时序章节核对修订，标注关联已补齐。

### 8.6 验收
- 新完成一个任务后：user 消息 `task_id` 非空、assistant 消息 `task_id` 非空且 `reply_to_message_id = 对应 user 消息 id`。
- C1/C2 能通过该链拼出"用户输入 → 步骤流 → 助手回答"完整回放。
- 全量 pytest 无新增失败。

---

## 9、实施方案（按 v2.0 改动清单，真实 diff）

> 以 v2.0 定稿（第 3 章）为最终准绳。以下按依赖顺序排列，每个改动列出所有涉及文件的真实 diff（`-` 删除 / `+` 新增）。

---

### 9.1 改动 1：新建 `chat_user_message` 表 + DDL

**文件：`backend/app/db/db_initializer.py`**

在 `chat_session_trust` 表 DDL 之后、`'''` 闭合之前，新增：

```diff
+            -- v2.0 新建 chat_user_message 表（用户消息+AI最终回答汇总）— 小欧 2026-08-19
+            CREATE TABLE IF NOT EXISTS chat_user_message (
+                id INTEGER PRIMARY KEY AUTOINCREMENT,
+                session_id TEXT NOT NULL,
+                content TEXT NOT NULL,
+                task_id TEXT,
+                response TEXT,
+                reasoning TEXT,
+                outcome TEXT,
+                model TEXT,
+                provider TEXT,
+                accumulated_usage TEXT,
+                client_os TEXT,
+                browser TEXT,
+                device TEXT,
+                network TEXT,
+                created_at TEXT,
+                FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE
+            );
+
         ''')
```

**文件：`backend/app/services/chat/storage.py`**

在文件末尾（`get_previous_task_chain` 函数之后），新增：

```diff
+ # ====================================================================
+ # v2.0 chat_user_message 读写（2026-08-19）
+ # ====================================================================
+
+ def insert_user_message(
+     conn: Connection, *,
+     session_id: str, content: str,
+     client_os: str = None, browser: str = None,
+     device: str = None, network: str = None,
+ ) -> int:
+     """新建 chat_user_message 行（用户发消息时落库）"""
+     now = get_local_iso_timestamp()
+     cursor = conn.execute(
+         """INSERT INTO chat_user_message
+            (session_id, content, client_os, browser, device, network, created_at)
+            VALUES (?,?,?,?,?,?,?)""",
+         (session_id, content, client_os, browser, device, network, now),
+     )
+     return cursor.lastrowid
+
+
+ def update_user_message_final(
+     conn: Connection, *,
+     user_message_id: int, task_id: str,
+     response: str, reasoning: str = None,
+     outcome: str = None, model: str = None,
+     provider: str = None, accumulated_usage: str = None,
+ ) -> None:
+     """任务完成后回填 final 字段到 chat_user_message"""
+     conn.execute(
+         """UPDATE chat_user_message
+            SET task_id=?, response=?, reasoning=?, outcome=?,
+                model=?, provider=?, accumulated_usage=?
+            WHERE id=?""",
+         (task_id, response, reasoning, outcome, model, provider,
+          accumulated_usage, user_message_id),
+     )
+
+
+ def load_user_message_by_task(conn: Connection, task_id: str) -> Optional[dict]:
+     """按 task_id 读 chat_user_message（C1 详情用）"""
+     row = conn.execute(
+         "SELECT * FROM chat_user_message WHERE task_id=?", (task_id,),
+     ).fetchone()
+     return dict(row) if row else None
```

---

### 9.2 改动 3：`chat_message_steps` → `chat_task_steps` 改名 + 加 `usage` 列（改动 8 合并）

**文件：`backend/app/db/db_initializer.py`**

```diff
-            -- 独立步骤表 — 小欧 2026-07-14
-            CREATE TABLE IF NOT EXISTS chat_message_steps (
+            -- 独立步骤表 — 小欧 2026-07-14; v2.0 改名 chat_task_steps — 小欧 2026-08-19
+            CREATE TABLE IF NOT EXISTS chat_task_steps (
                 id INTEGER PRIMARY KEY AUTOINCREMENT,
                 message_id INTEGER NOT NULL,
                 session_id TEXT NOT NULL,
                 step_index INTEGER NOT NULL,
                 step_json TEXT NOT NULL,
                 created_at TEXT,  -- 本地ISO无Z
+                task_id TEXT,
+                usage TEXT,
                 FOREIGN KEY (message_id) REFERENCES chat_messages(id) ON DELETE CASCADE
             );
```

DDL 之后的 `_ensure_column` 和索引全部改名：

```diff
-        _ensure_column(conn, "chat_message_steps", "task_id", "TEXT")
+        _ensure_column(conn, "chat_task_steps", "task_id", "TEXT")
```

```diff
-        conn.execute("CREATE INDEX IF NOT EXISTS idx_steps_message ON chat_message_steps(message_id)")
-        conn.execute("CREATE INDEX IF NOT EXISTS idx_steps_session ON chat_message_steps(session_id, step_index)")
+        conn.execute("CREATE INDEX IF NOT EXISTS idx_steps_message ON chat_task_steps(message_id)")
+        conn.execute("CREATE INDEX IF NOT EXISTS idx_steps_session ON chat_task_steps(session_id, step_index)")
```

```diff
-        # chat_message_steps 复合索引：按 message_id 与 (task_id, step_index)
-        conn.execute("CREATE INDEX IF NOT EXISTS idx_steps_task ON chat_message_steps(task_id, step_index)")
+        # chat_task_steps 复合索引：按 message_id 与 (task_id, step_index)
+        conn.execute("CREATE INDEX IF NOT EXISTS idx_steps_task ON chat_task_steps(task_id, step_index)")
```

**文件：`backend/app/services/chat/storage.py`**

`append_execution_step` 函数：

```diff
- def append_execution_step(conn: Connection, message_id: int, session_id: str,
-                           step_index: int, step_dict: dict, task_id: Optional[str] = None) -> None:
-     """运行期逐步落库 — 小欧 2026-07-14
-     小欧 2026-07-21: 落库前截断超大 tool_result(列表+字符串)防 SQLite 撑爆; 不碰 observation
-     2026-08-16 - 小欧 - S2②-2: chat_message_steps 补 task_id 列（任务级贯通，10.1.7②-2）"""
-     step_dict = _truncate_step_dict(step_dict)
-     conn.execute(
-         "INSERT INTO chat_message_steps(message_id, task_id, session_id, step_index, step_json, created_at) "
-         "VALUES (?, ?, ?, ?, ?, ?)",
-         (message_id, task_id, session_id, step_index, safe_json_dumps(step_dict), get_local_iso_timestamp()),
-     )
+ def append_execution_step(conn: Connection, message_id: int, session_id: str,
+                           step_index: int, step_dict: dict, task_id: Optional[str] = None,
+                           usage: Optional[str] = None) -> None:
+     """运行期逐步落库 — 小欧 2026-07-14
+     小欧 2026-07-21: 落库前截断超大 tool_result(列表+字符串)防 SQLite 撑爆; 不碰 observation
+     2026-08-16 - 小欧 - S2②-2: chat_task_steps 补 task_id 列（任务级贯通，10.1.7②-2）
+     2026-08-19 - 小欧 - v2.0: 表改名 chat_task_steps + 新增 usage 列（每轮 token）"""
+     step_dict = _truncate_step_dict(step_dict)
+     conn.execute(
+         "INSERT INTO chat_task_steps(message_id, task_id, session_id, step_index, step_json, created_at, usage) "
+         "VALUES (?, ?, ?, ?, ?, ?, ?)",
+         (message_id, task_id, session_id, step_index, safe_json_dumps(step_dict),
+          get_local_iso_timestamp(), usage),
+     )
```

`load_execution_steps` 函数：

```diff
- def load_execution_steps(conn: Connection, message_id: int, task_id: Optional[str] = None) -> Optional[list]:
-     """从 chat_message_steps 表组装步骤列表..."""
-     if task_id is not None:
-         rows = conn.execute(
-             "SELECT step_json FROM chat_message_steps WHERE message_id=? AND task_id=? ORDER BY step_index ASC",
-             (message_id, task_id),
-         ).fetchall()
-     else:
-         rows = conn.execute(
-             "SELECT step_json FROM chat_message_steps WHERE message_id=? ORDER BY step_index ASC",
-             (message_id,),
-         ).fetchall()
-     if rows:
-         return [parse_json(r["step_json"], label="step_json") for r in rows]
-     row = conn.execute(
-         "SELECT execution_steps FROM chat_messages WHERE id=?", (message_id,),
-     ).fetchone()
-     if row and row["execution_steps"]:
-         return parse_json(row["execution_steps"], label="execution_steps")
-     return []
+ def load_execution_steps(conn: Connection, message_id: int, task_id: Optional[str] = None) -> Optional[list]:
+     """从 chat_task_steps 表组装步骤列表（v2.0: 不再回退读 chat_messages.execution_steps）"""
+     if task_id is not None:
+         rows = conn.execute(
+             "SELECT step_json FROM chat_task_steps WHERE message_id=? AND task_id=? ORDER BY step_index ASC",
+             (message_id, task_id),
+         ).fetchall()
+     else:
+         rows = conn.execute(
+             "SELECT step_json FROM chat_task_steps WHERE message_id=? ORDER BY step_index ASC",
+             (message_id,),
+         ).fetchall()
+     if rows:
+         return [parse_json(r["step_json"], label="step_json") for r in rows]
+     return []
```

**文件：`backend/app/services/chat/stream_orchestrator.py`**

`_db_ops` 闭包中 `append_step` 改名引用：

```diff
-            append_step=lambda c, mid, sid, idx, d: append_execution_step(c, mid, sid, idx, d, task_id),
+            append_step=lambda c, mid, sid, idx, d, usage=None: append_execution_step(c, mid, sid, idx, d, task_id, usage=usage),
```

---

### 9.3 改动 4：老 `chat_messages` 冻结废弃

**文件：`backend/app/db/db_initializer.py`**

DDL 中删除 `execution_steps` 列定义：

```diff
             CREATE TABLE IF NOT EXISTS chat_messages (
                 id INTEGER PRIMARY KEY AUTOINCREMENT,
                 session_id TEXT NOT NULL,
                 role TEXT NOT NULL,
                 content TEXT NOT NULL,
                 timestamp TEXT,  -- 本地ISO无Z
-                execution_steps TEXT,
                 display_name TEXT
             );
```

删除 `migrate_steps` 调用：

```diff
-        # 小欧 2026-07-13: 一次性迁移旧 execution_steps(幂等)
-        from app.services.chat.migrate_steps import migrate_execution_steps_status
-        migrate_execution_steps_status(get_conn)
```

**文件：`backend/app/services/chat/storage.py`**

`update_message_fields` 函数——删除 execution_steps 写入：

```diff
 def update_message_fields(
     conn: Connection, ai_message_id: int,
     update_data, display_name: str,
 ) -> None:
     """拷贝自 conversation.py 第134-156行"""
     cursor = conn.cursor()
     fields: list = []
     values: list = []
-    if update_data.execution_steps:
-        fields.append("execution_steps = ?")
-        values.append(safe_json_dumps(update_data.execution_steps))
     if update_data.content is not None:
         fields.append("content = ?")
         values.append(update_data.content)
     if getattr(update_data, "status", None) is not None:
         fields.append("status = ?")
         values.append(update_data.status)
```

**文件：`backend/app/services/chat/message_service.py`**

`save_message` 函数——删除 execution_steps 写入：

```diff
-        execution_steps_json = safe_json_dumps(message.execution_steps) if message.execution_steps is not None else None
         cursor.execute(
-            "INSERT INTO chat_messages(session_id, role, content, timestamp, display_name, execution_steps, client_os, browser, device, network) VALUES(?,?,?,?,?,?,?,?,?,?)",
-            (session_id, message.role, message.content, local_time, display_name_to_save,
-             execution_steps_json, message.client_os, message.browser, message.device, message.network))
+            "INSERT INTO chat_messages(session_id, role, content, timestamp, display_name, client_os, browser, device, network) VALUES(?,?,?,?,?,?,?,?,?)",
+            (session_id, message.role, message.content, local_time, display_name_to_save,
+             message.client_os, message.browser, message.device, message.network))
```

---

### 9.4 改动 7：消息↔任务关联（user 挂 task_id + assistant 写 reply_to）

**文件：`backend/app/services/chat/stream_orchestrator.py`**

编排⑨ `insert_task` 之后，新增 user 消息回填 task_id：

```diff
         try:
             with db.get_conn_with_retry("chat") as _conn3:
                 _db_ops.insert_task(_conn3)
+                # v2.0 改动7: user 消息回填 task_id — 小欧 2026-08-19
+                if _user_msg_id:
+                    _conn3.execute(
+                        "UPDATE chat_messages SET task_id=? WHERE id=?",
+                        (task_id, _user_msg_id),
+                    )
         except Exception as _task_e:
             logger.warning(f"[chat] chat_tasks INSERT 失败(task={task_id}): {_task_e}")
```

**文件：`backend/app/services/chat/storage.py`**

`allocate_and_insert_message` 函数——增加 `reply_to_message_id` 参数：

```diff
 def allocate_and_insert_message(conn: Connection, session_id: str,
-                                task_id: Optional[str] = None) -> int:
+                                task_id: Optional[str] = None,
+                                reply_to_message_id: Optional[int] = None) -> int:
     """预分配 assistant 消息ID + 插入空白行"""
     ensure_session_exists(session_id, conn)
     ai_message_id, is_new = _allocator.allocate(session_id, conn, always_new=True)
     local_time = get_local_iso_timestamp()
     if is_new:
         conn.execute(
-            "INSERT INTO chat_messages(id, task_id, session_id, role, content, timestamp) "
-            "VALUES (?, ?, ?, 'assistant', ?, ?)",
-            (ai_message_id, task_id, session_id, "", local_time),
+            "INSERT INTO chat_messages(id, task_id, session_id, role, content, timestamp, reply_to_message_id) "
+            "VALUES (?, ?, ?, 'assistant', ?, ?, ?)",
+            (ai_message_id, task_id, session_id, "", local_time, reply_to_message_id),
         )
```

**文件：`backend/app/services/chat/stream_orchestrator.py`**

`_db_ops` 闭包中 `allocate_and_insert` 传入 `reply_to_message_id`：

```diff
-            allocate_and_insert=lambda c, sid: allocate_and_insert_message(c, sid, task_id),
+            allocate_and_insert=lambda c, sid: allocate_and_insert_message(c, sid, task_id, reply_to_message_id=_user_msg_id),
```

---

### 9.5 改动 6：清 `chat_session_title_history` 死表

**文件：`backend/app/db/db_initializer.py`**

删除 DDL：

```diff
-            CREATE TABLE IF NOT EXISTS chat_session_title_history (
-                id INTEGER PRIMARY KEY AUTOINCREMENT,
-                session_id TEXT NOT NULL,
-                title TEXT NOT NULL,
-                created_at TEXT,  -- 本地ISO无Z
-                updated_by TEXT,
-                change_reason TEXT,
-                FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE
-            );
```

**文件：`backend/app/services/chat/session_service.py`**

删除 `record_title_history` 函数定义（行 90-106）：

```diff
-def record_title_history(
-    cursor, session_id: str, old_title: Optional[str],
-    local_time: str, updated_by: str = "user",
-):
-    """拷贝自 sessions.py 第230-255行"""
-    if not old_title:
-        return
-    try:
-        cursor.execute(
-            """INSERT INTO chat_session_title_history
-               (session_id, title, created_at, updated_by, change_reason)
-               VALUES (?, ?, ?, ?, ?)""",
-            (session_id, old_title, local_time, updated_by, "user_edit"),
-        )
-        logger.info(f"记录标题历史: session_id={session_id}, old_title={old_title}")
-    except Exception:
-        logger.debug("chat_session_title_history表不存在,跳过标题历史记录")
```

删除 `update_session` 中的调用（行 205）：

```diff
-            record_title_history(cursor, session_id, old_title, local_time, update_data.updated_by or "user")
```

**文件：`backend/app/services/chat/migrate_steps.py`**

该文件为一次性旧数据迁移（将旧格式 execution_steps 改写为新统一表示），已由 `schema_migrations` 表守卫（跑过一次后不再执行）。9.3 中已从 `db_initializer.py` 移除对其调用（该迁移守护旧列时代的数据改写，旧列退役后失去意义）。

**处置：保留文件，不删除**。理由：该文件无副作用（守卫确保不再执行），删除需改 git 历史且无实际收益（YAGNI）；若未来需参考旧迁移逻辑，文件仍在。

---

### 9.6 改动 2 + 改动 5：新链路写入 + C1/C2 接口

**文件：`backend/app/services/chat/message_service.py`**

user 消息落库时同步写 `chat_user_message`（改动2 核心写入点）——在 `save_message` 函数中，`chat_messages` INSERT 之后、`_track_user_message` 之前新增：

```diff
  import json
  from typing import Optional
  from app.logger import logger
  from app.utils.json_utils import safe_json_dumps, parse_json
  from app.utils.cache import LRUCache
  from app.constants import MAX_CACHE_SIZE
  from app.utils.display_utils import extract_display_name_from_steps
  from app.utils.time_utils import ensure_timestamp_milliseconds, get_local_iso_timestamp, to_local_iso, format_timestamp
  from app.db import db
  from app.db.models.chat_models import MessageResponse
  from app.services.chat.storage import track_user_message, get_user_message_id, load_execution_steps
+ from app.services.chat.storage import insert_user_message  # v2.0 改动2: user消息同步写chat_user_message — 小欧 2026-08-19
```

`save_message` 函数体改动——user 消息 INSERT `chat_messages` 之后新增同步写：

```diff
          cursor.execute(
              "INSERT INTO chat_messages(session_id, role, content, timestamp, display_name, execution_steps, client_os, browser, device, network) VALUES(?,?,?,?,?,?,?,?,?,?)",
              (session_id, message.role, message.content, local_time, display_name_to_save,
               execution_steps_json, message.client_os, message.browser, message.device, message.network))
          message_id = cursor.lastrowid

+         # v2.0 改动2: user消息同步写chat_user_message — 小欧 2026-08-19
+         if message.role == "user":
+             try:
+                 insert_user_message(
+                     conn,
+                     session_id=session_id,
+                     content=message.content,
+                     client_os=message.client_os,
+                     browser=message.browser,
+                     device=message.device,
+                     network=message.network,
+                 )
+             except Exception as _um_e:
+                 logger.warning(f"[save_message] 写chat_user_message失败(session={session_id}): {_um_e}")
+
          if message.role == "user":
              _track_user_message(session_id, message_id)
```

**文件：`backend/app/services/chat/storage.py`**

新增 C1/C2 存储函数（在 `load_user_message_by_task` 之后）：

```diff
+ def get_task_detail(conn: Connection, task_id: str) -> Optional[dict]:
+     """C1: 按 task_id 读 chat_tasks 单行详情"""
+     row = conn.execute(
+         "SELECT * FROM chat_tasks WHERE task_id=?", (task_id,),
+     ).fetchone()
+     return dict(row) if row else None
+
+
+ def get_task_tool_stats(conn: Connection, task_id: str) -> list:
+     """C1: 从 chat_task_steps 统计该任务的工具调用次数"""
+     rows = conn.execute(
+         """SELECT
+              json_extract(step_json, '$.tools[0].name') as tool_name,
+              COUNT(*) as call_count
+            FROM chat_task_steps
+            WHERE task_id=? AND json_extract(step_json, '$.type')='action'
+            GROUP BY tool_name""",
+         (task_id,),
+     ).fetchall()
+     return [dict(r) for r in rows]
+
+
+ def load_steps_by_task(conn: Connection, task_id: str) -> list:
+     """C2: 按 task_id 读全部步骤（升序）"""
+     rows = conn.execute(
+         "SELECT step_json FROM chat_task_steps WHERE task_id=? ORDER BY step_index ASC",
+         (task_id,),
+     ).fetchall()
+     return [parse_json(r["step_json"], label="step_json") for r in rows]
```

**新文件：`backend/app/api/v1/chat/task_execution.py`**

```python
# -*- coding: utf-8 -*-
# v2.0 C1/C2 任务级回放与统计接口 — 小欧 2026-08-19
"""
task_execution — C1 任务详情统计 + C2 按 task_id 步骤回放
落在 chat 域（与 execution_stream C3 同域）
"""
from fastapi import APIRouter, HTTPException
from app.db import db
from app.services.chat.storage import (
    get_task_detail, get_task_tool_stats, load_steps_by_task, load_user_message_by_task,
)

router = APIRouter()


@router.get("/chat/execution/task/{task_id}")
async def get_task_detail_endpoint(task_id: str):
    """C1: 任务详情 + 工具统计"""
    with db.get_conn("chat") as conn:
        task = get_task_detail(conn, task_id)
        if not task:
            raise HTTPException(status_code=404, detail=f"任务不存在: {task_id}")
        tool_stats = get_task_tool_stats(conn, task_id)
        user_msg = load_user_message_by_task(conn, task_id)
    return {
        "task": task,
        "tool_stats": tool_stats,
        "user_message": user_msg,
    }


@router.get("/chat/execution/task/{task_id}/steps")
async def get_task_steps_endpoint(task_id: str):
    """C2: 按 task_id 步骤回放"""
    with db.get_conn("chat") as conn:
        steps = load_steps_by_task(conn, task_id)
    return {"task_id": task_id, "steps": steps, "count": len(steps)}
```

**文件：`backend/app/api/v1/chat/chat_routes.py`**

注册新路由：

```diff
+ from app.api.v1.chat.task_execution import router as task_execution_router
+ router.include_router(task_execution_router, tags=["task-execution"])
```

**文件：`backend/app/services/chat/stream_orchestrator.py`**

db_ops 命名空间增加 `user_msg_id` 属性（供 agent_runner 回填 final 字段时使用）：

```diff
          _db_ops = _types.SimpleNamespace(
              allocate_and_insert=lambda c, sid: allocate_and_insert_message(c, sid, task_id),
              append_step=lambda c, mid, sid, idx, d, usage=None: append_execution_step(c, mid, sid, idx, d, task_id, usage=usage),
              finalize=finalize_message,
              save_steps=save_execution_steps_to_db,
              load_previous=lambda sid: _load_previous_messages(
                  sid, context_link_mode=context_link_mode, context_root_task_id=_context_root_task_id,
                  upper_message_id=_user_msg_id),
              log_task_end=_log_task_end,
              insert_task=lambda c: insert_task(
                  c, task_id=task_id, session_id=session_id, user_message_id=_user_msg_id,
                  user_input=user_input, context_link_mode=context_link_mode,
                  context_root_task_id=_context_root_task_id,
                  provider=agent.llm_client.provider, model=agent.llm_client.model,
                  display_name=f"{agent.llm_client.provider} ({agent.llm_client.model})"),
              update_task=lambda c, **kw: update_task(c, task_id=task_id, **kw),
              insert_token=lambda c, **kw: token_usage_insert(
                  c, task_id=task_id, session_id=session_id,
                  model=agent.llm_client.model, provider=agent.llm_client.provider, **kw),
+             user_msg_id=_user_msg_id,  # v2.0 改动2: 供agent_runner回填chat_user_message — 小欧 2026-08-19
          )
```

**文件：`backend/app/services/agent/agent_runner.py`**

新增 import（`update_user_message_final` + `derive_status_from_steps` + `safe_json_dumps` 用于 final 字段回填）：

```diff
  from app.services.chat.storage import save_execution_steps, ExecutionStepsUpdate
+ from app.services.chat.storage import update_user_message_final, derive_status_from_steps  # v2.0 改动2 — 小欧 2026-08-19
+ from app.utils.json_utils import safe_json_dumps  # v2.0 改动2: accumulated_usage序列化 — 小欧 2026-08-19
```

任务完成后回填 `chat_user_message` final 字段——在 `update_task` 调用之后（line 412 附近）新增：

```diff
              with db.get_conn_with_retry("chat") as _conn:
                  db_ops.update_task(
                      _conn, status=_terminal_status, response=_terminal,
                      end_time=get_local_iso_timestamp(),
                      duration=round(time.time() - start_time, 1) if start_time else None,
                      accumulated_usage=getattr(agent, "accumulated_usage", None),
                      llm_call_count=getattr(agent, "llm_call_count", 0),
                      total_steps=_total, retry_count=getattr(agent, "_retry_count", 0),
                      error_type=_err_type, error_message=_err_msg)
+
+                 # v2.0 改动2: 任务完成后回填 chat_user_message final 字段 — 小欧 2026-08-19
+                 try:
+                     _last_final = None
+                     for _s in (current_execution_steps or [])[::-1]:
+                         if isinstance(_s, dict) and _s.get("type") == "final":
+                             _last_final = _s
+                             break
+                     update_user_message_final(
+                         _conn,
+                         user_message_id=db_ops.user_msg_id,
+                         task_id=task_id,
+                         response=saved_content or "",
+                         reasoning=saved_thought or "",
+                         outcome=_terminal_status,
+                         model=_last_final.get("model") if _last_final else None,
+                         provider=_last_final.get("provider") if _last_final else None,
+                         accumulated_usage=safe_json_dumps(getattr(agent, "accumulated_usage", None)),
+                     )
+                 except Exception as _um_e:
+                     logger.warning(f"[Runner] 回填chat_user_message final失败(task={task_id}): {_um_e}")
          except Exception as _task_e:
              logger.warning(f"[Runner] chat_tasks 终态 UPDATE 失败(task={task_id}): {_task_e}")
```

---

### 9.7 现网迁移脚本（停服窗口执行）

新建 `backend/app/db/migrations/v2_chat_restructure.sql`：

```sql
-- v2.0 核心数据模型重构迁移脚本 — 小欧 2026-08-19
-- 执行条件：停服窗口，无并发写入
-- 幂等：每步 IF EXISTS 守卫

-- 1. 改名 chat_message_steps → chat_task_steps
ALTER TABLE chat_message_steps RENAME TO chat_task_steps;

-- 2. 为 chat_task_steps 新增 usage 列
ALTER TABLE chat_task_steps ADD COLUMN usage TEXT;

-- 3. 新建 chat_user_message 表
CREATE TABLE IF NOT EXISTS chat_user_message (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id TEXT NOT NULL,
    content TEXT NOT NULL,
    task_id TEXT,
    response TEXT,
    reasoning TEXT,
    outcome TEXT,
    model TEXT,
    provider TEXT,
    accumulated_usage TEXT,
    client_os TEXT,
    browser TEXT,
    device TEXT,
    network TEXT,
    created_at TEXT,
    FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE
);

-- 4. 清 chat_session_title_history 死表
DROP TABLE IF EXISTS chat_session_title_history;

-- 5. 冻结 chat_messages：不再写入（代码层面冻结，不删表不删列，存量保留降级可读）
-- 不做 DDL 变更，仅由代码保证新链路不写 chat_messages

-- 6. 清死字段与冗余索引
-- chat_messages.metadata（已在chat_messages冻结范畴，代码不再写入，列保留不删）
-- chat_sessions.metadata 死字段
ALTER TABLE chat_sessions DROP COLUMN metadata;
-- chat_tasks.metadata 死字段
ALTER TABLE chat_tasks DROP COLUMN metadata;
-- 删除冗余 timestamp 双索引（保留 idx_msg_timestamp）
DROP INDEX IF EXISTS idx_messages_timestamp;
```

### 9.8 清理死字段与冗余索引（2.8 落地）

> 来源：2.8 多余字段处置汇总。v2.0 走"整体废弃"路径覆盖 chat_messages 内死字段；chat_sessions / chat_tasks 的 metadata 与 timestamp 双索引需单独清理。

**文件：`backend/app/db/db_initializer.py`**

删除 `chat_messages.metadata` 的 ensure_column（line 140）：

```diff
-        _ensure_column(conn, "chat_messages", "metadata", "TEXT")   # 发展性兼容列(5.3.1, 文档2 3.1.3)
```

删除 `chat_sessions.metadata` 的 ensure_column（line 143）：

```diff
-        _ensure_column(conn, "chat_sessions", "metadata", "TEXT")          # 扩展列(0.2.4-1)
```

删除 `chat_tasks` DDL 中的 `metadata` 列（line 93）：

```diff
                  error_type TEXT, error_message TEXT,
-                 metadata TEXT,
                  created_at TEXT, updated_at TEXT,
```

删除 `idx_messages_timestamp` 冗余索引（line 149，保留 line 161 `idx_msg_timestamp` 为权威）：

```diff
-        conn.execute("CREATE INDEX IF NOT EXISTS idx_messages_timestamp ON chat_messages(timestamp)")
```