# 数据存储及 yield 前端消息及数据设计

版本: v2.17
创建时间: 2026-08-13 17:39:38
更新时间: 2026-08-14 09:28:51
创建人: 小欧
更新人: 小欧
状态: 方案定案（北京老陈定案：仅设计文档，代码落地另立任务）

> 定位：本设计的核心收益落在「历史回放保真 + 排查」与「前端展示重写为流水线式」两条主线。存储侧：完整 observation 文本主要供多轮回放与排查使用，前端 observation 步骤仅展示摘要即够（后端产出为准）；展示侧（2.8/3.6，完整设计见 3.6.1~3.6.8）：推翻"分步卡片"式展示，按后端事件流顺序流水线式自然流出、去掉全部框框，实时与回放同一套渲染。第一章节标题"yield 前端消息数据"沿用最初命名，实际以回放、取数修正与展示重写为主。

## 版本历史

| 版本 | 时间 | 更新人 | 更新内容 |
|------|------|--------|---------|
| v2.17 | 2026-08-14 09:28:51 | 小欧 | 代码名称同步（名实相符改名）：chat/openai.py→chat_routes.py、chat/sse.py→execution_stream.py、services/chat/stream.py→stream_reader.py、services/chat/handlers.py→sse_events.py、api/model_routes→config_routes、正文行号引用同步 |
| v2.16 | 2026-08-14 07:40:16 | 小欧 | 第一部分开头新增后端业务架构总图（六层全貌：入口/API/编排/业务服务/支撑/基础设施，附六层与 1.2 四层对应表；修正编排层范围、llm 归属、sse_formatter 物理与功能位置、task_rollback 命名、handlers 不重复） |
| v2.15 | 2026-08-14 06:53:43 | 小欧 | 新增 1.2 后端代码分层（原 1.2~1.7 顺延为 1.3~1.8），交叉引用同步 |
| v2.14 | 2026-08-13 23:59:08 | 小欧 | 3.6 流水线式展示扩写为完整设计（3.6.1~3.6.8），4.1 步骤 5 同步细化 |

---

## 后端业务架构总图

> 定位：一张图看清后端整体全貌与前后端交互链路。图采用六层体系（① 应用入口 → ② API 接口适配 → ③ 编排层 → ④ 业务服务层 → ⑤ 支撑层 → ⑥ 公共基础设施层），前端浏览器与 LLM 服务商为外部依赖。与 backend架构分层达标分析 2.1 图的差异：① 编排层仅含 `services/agent/` + `services/chat/{stream_orchestrator, stream_reader, sse_events}.py`，llm/model/tool/prompts 等均属④业务服务层，不在编排层；② `stream_parser.py` 实为不存在的文件，SSE 流解析在 `base_service.py` 的 `_parse_sse_data()`（base_service.py:445-479）；③ `utils/sse_formatter.py` 物理在⑥、功能属③编排层（SSE 格式化）。六层与本文档 1.2 四层的对应关系见文末。

```
                     ┌───────────────────────────────────────────────────────────┐
                     │            前端浏览器（frontend/src/）                     │
                     │  utils/sse.ts processSSEData 解析 SSE 流                  │
                     └──────────────────────────┬────────────────────────────────┘
                                                │ HTTP /api/v1（SSE: data: {json}\n\n）
                                                ▼
                     ┌───────────────────────────────────────────────────────────┐
                     │  ① 应用入口层：main.py（注册路由 + 启动/关闭事件）          │
                     └──────────────────────────┬────────────────────────────────┘
                                                │
                                                ▼
                     ┌───────────────────────────────────────────────────────────┐
                     │  ② API / 接口适配层（app/api/v1/ 薄壳）                   │
                     │  chat/chat_routes.py · chat/execution_stream.py · chat/models.py   │
                     │  sessions · messages · config_routes · tool_routes                │
                     │  task_queries · metrics · health                          │
                     └──────────────────────────┬────────────────────────────────┘
                                                │
                                                ▼
  ┌───────────────────────────────────────────────────────────────────────────────┐
  │  ③ 编排层（services/agent/ 核心 + services/chat/ 编排文件）                    │
  │  agent/                              chat/                                    │
  │  ├─ react_cycle（ReAct薄调度）        ├─ stream_orchestrator.py                │
  │  ├─ tool_executor（工具执行入口）     │    （任务生命周期 + SSE编排 + 启动后台）  │
  │  ├─ agent_runner（后台/生产者）       ├─ stream_reader.py（stream_reader 消费者）  │
  │  ├─ handlers/（action/answer 产出）    └─ sse_events.py（send_start_step 等）      │
  │  ├─ message_builder（会话状态管理）                                           │
  │  ├─ steps/（7种 Step 类型）                                                   │
  │  └─ llm_stream.py（LLM流式调用入口 → 下调④业务服务层 llm/）                   │
  └──────────────┬────────────────────────────────────────────────────────────────┘
                 │
                 ▼
  ┌───────────────────────────────────────────────────────────────────────────────┐
  │  ④ 业务服务层（services/ 其余业务子域，chat/stream_orchestrator 等已入③）   │
  │  chat 业务：session_service · message_service · storage                       │
  │  task 管理：task_runtime · task_registry · task_state · hitl_confirmation     │
  │            · task_db · task_rollback_service                                  │
  │  llm/（与 LLM 收发基础层）：                                                  │
  │  ├─ base_service（重试/超时/取消）                                            │
  │  ├─ client_sdk（httpx 唯一收发）                                              │
  │  ├─ core（ChatResponse/StreamChunk）                                          │
  │  └─ error_classifier · reasoning · xml_adapter                               │
  │  model/ · tool/ · prompts/ · lifecycle/ · monitoring/ · visualization/（略）   │
  └──────────────┬───────────────────────────────────────────────────┬────────────┘
                 │                                                    │ httpx /chat/completions
                 ▼                                                    ▼
  ┌──────────────────────────────────────────────────────────┐  ┌─────────────────┐
  │  ⑤ 支撑层                                                │  │ LLM 服务商（外部）│
  │  安全层(safety) · 工具层(tools) · 数据访问层(db)           │  │ OpenAI/DeepSeek… │
  └──────────────────────────────┬───────────────────────────┘  └─────────────────┘
                                 │
                                 ▼
  ┌──────────────────────────────────────────────────────────┐
  │  ⑥ 公共基础设施层（utils/ + logger/ + config.py + constants）│
  │  —— 最底层，无依赖；utils/sse_formatter 物理在此、功能属     │
  │     ③ 编排层（SSE 格式化），见 1.2.2                       │
  └──────────────────────────────────────────────────────────┘
```

关键结论：LLM 通信（`services/llm/`）属④业务服务层，唯一真正发 HTTP 的是 `client_sdk.py` 的 `LLMClient`；③编排层的 `llm_stream.py` 只是调用入口，经 `agent.llm_client`（base_agent.py:48，由 get_service() 注入）向下调业务服务层。`chat/sse_events.py`（send_start_step、create_error_response）归③编排层，不在④重复出现；`task_rollback_service` 是准确命名。

六层与本文档 1.2 四层的对应关系：

| 六层 | 对应 1.2 四层 |
|------|--------------|
| ① 应用入口层（main.py） | 无（纯启动） |
| ② API / 接口适配层 | 1.2.1 API 路由层 |
| ③ 编排层 | 1.2.2 服务编排层（chat 侧）+ 1.2.3 事件产出层（agent 侧） |
| ④ 业务服务层 | 1.2.4 任务控制层（task 侧）+ 其余业务子域 |
| ⑤ 支撑层（安全/工具/数据访问） | 无（支撑） |
| ⑥ 公共基础设施层 | 无（最底层，含 sse_formatter 物理位置） |

四层聚焦"前后端交互链路"这条主线，六层展示后端全貌，两者无冲突：读链路看 1.2，读全貌看本图。

---

## 第一部分　yield 前端消息数据

### 1 现状

#### 1.1 全链路数据流（一次任务事件如何到达前端）

```
UniversalAgent.run_react_cycle() 产出 Step 对象
        │  event.to_dict() → step_dict（agent_runner.py:164-171）
        ▼
step_dict（同一份对象，双路分发）
        ├─ ① 落库路：append_execution_step → chat_message_steps.step_json（见第二部分）
        └─ ② yield 路：_append(event_dict)（agent_runner.py:102-113）
                │  d["seq"] = len(buffer.event_log)
                │  buffer.event_log.append(d) + cond.notify_all()
                ▼
          stream_reader(buffer, task_id, after_seq)（stream_reader.py:119-144）
                │  按 seq 偏移读取 → format_agent_sse(step_dict)
                ▼
          SSE 字符串："data: {json}\n\n"（utils/sse_formatter.py:35-59）
                │
                ▼
          前端 fetch/SSE 解析（frontend/src/utils/sse.ts processSSEData）
```

关键结论：SSE 消息 == step_dict 本身（format_agent_sse 只加 type/step/timestamp 头，其余字段原样透传），yield 与落库是同一份 step_dict，两路同构。因此「前端能看到的字段」==「落库能还原的字段」==「to_dict() 序列化的字段」。前端看不到的字段，历史回放也还原不了。

> start 事件属旁路（不予 1.1 双路分发混淆）：上图"run_react_cycle 双路分发"只覆盖运行期步骤（thought/action/observation/final 等）。start 事件由 `step_start`（stream_orchestrator.py:208-220）在 agent 启动前直接 `yield format_agent_sse(start_dict)`，不进 1.3 的缓冲、不走双路分发。后果：start 不落 event_log → 断线重连(after_seq=N)不会重放；它只 append 进 execution_steps 列表随消息落库 DB。

#### 1.2 处理前后端交互事件的后端代码分层

处理前后端交互业务事件的代码，在后端分布在四层：

##### 1.2.1 API 路由层（前端 HTTP/SSE 入口）

backend/app/api/v1/chat/chat_routes.py — 路由薄壳，解包请求调编排器
backend/app/api/v1/chat/execution_stream.py — SSE 端点
backend/app/api/v1/chat/models.py — DTO

##### 1.2.2 服务编排层（SSE 消费与下发）

backend/app/services/chat/stream_orchestrator.py — start 下发、_stream_with_control、断线重连
backend/app/services/chat/stream_reader.py — stream_reader（消费者）
backend/app/services/chat/sse_events.py — send_start_step、create_error_response
backend/app/utils/sse_formatter.py — format_agent_sse 事件格式化

##### 1.2.3 事件产出层（业务事件生成）

backend/app/services/agent/agent_runner.py — 生产者 _append 写 event_log
backend/app/services/agent/react_cycle.py — chunk/usage/retrying 等 emit
backend/app/services/agent/handlers/action_handler.py、answer_handler.py — 产出 ThoughtStep/ActionStep/ObservationStep/ErrorStep/paused/resumed
backend/app/services/agent/steps/ — 各 Step 定义（含 MetaStep）
backend/app/services/agent/llm_stream.py — LLM 流式 chunk

##### 1.2.4 任务控制层（暂停/取消/授权）

backend/app/services/task/task_runtime.py — paused/resumed/cancelled SSE 下发
backend/app/services/task/task_registry.py — build_step_dict、任务生命周期
backend/app/services/task/hitl_confirmation.py — HITL 人工确认

链路一句话：路由 → 编排器 → agent 产出事件 → event_log 缓冲 → stream_reader 消费 → SSE → 前端。

四层与 1.3 的解耦模型对应：生产者（三层）写缓冲，消费者（二层）读缓冲；事件类型见 1.5，字段见 1.6。

#### 1.3 生产者/消费者解耦模型（task_state.py:24-55）
生产者 = run_agent_in_background（agent_runner.py:69），内部 _append（agent_runner.py:102-113）把 run_react_cycle() 产出的每个事件写入 event_log 并 notify_all 唤醒消费者。

消费者 = stream_reader（stream_reader.py:119-144），纯消费者，按 seq 从 event_log 读取并转发为 SSE，重连时传 after_seq 续传；它由 _stream_with_control（stream_orchestrator.py:223-239）包装后经编排器推给前端

- StreamBuffer（task_state.py:24-34）：`event_log`(append-only 含 seq) / `cond` / `done` 三件套。
- create_stream_buffer（task_state.py:41-45）：注册 task_id→buffer。
- agent_streams（task_state.py:38）：全量流态缓冲表（task_id→StreamBuffer）。
- _append（agent_runner.py:102-113）：生产者写 `event_log` + `notify_all`。
- stream_reader（stream_reader.py:119-144）：纯消费者，按 seq 转发，重连传 `after_seq` 续传。
- reclaim_stream_buffer（task_state.py:53-55）：任务结束后延迟 300ms 回收（agent_runner.py:350-352）。

- 断线重连：`chat_stream_reconnect_orchestrator(task_id, session_id, after_seq)`（stream_orchestrator.py:242-254）复用同一 buffer 按 seq 续传，不重复事件。
- 防挂死：`cond.wait()` 加 60s 超时（asyncio.wait_for），超时后重检 `buffer.done`（stream_reader.py:136-144，2026-07-30 后端卡死根因修复）。
- 防漏事件：排空后复查 `len(buffer.event_log)`（stream_reader.py:131-133，#30 fix，防 done 前追加丢事件）。

> event_log 定性：`event_log` 是 `StreamBuffer` 的成员（task_state.py:32），一个 append-only 的进程内存 `List[Dict]`，存放在 `agent_streams`（task_state.py:38，进程内全局 dict，task_id→StreamBuffer）。它只服务于当次运行的 SSE 实时转发 + 断线按 seq 续传，不是持久化——任务结束由 `reclaim_stream_buffer` pop 回收即消失。真正的持久化走另一条独立路：`append_execution_step` 落 `chat_message_steps.step_json`（DB）。即 event_log（内存回放）与 step_json（DB 落盘）是两条互不相同但同构独立的通道；event_log 不写磁盘、不写 DB。

#### 1.4 SSE 事件格式（utils/sse_formatter.py:35-59）

> 本节定位：本节省格式化层（纯字符串工具），不是"事件清单"。所有发给前端的事件都会经过 `format_agent_sse` 拼成此格式，但"有哪些事件"由 1.5（前后端事件总览）定义，各事件字段由 1.6（Step 字段契约）、各场景产出由 1.7（各场景产出序列）定义；start 事件见 1.5.1、在 1.7 各场景开头均有，只是不在此节。文件在 `backend/app/utils/sse_formatter.py`（不在 services/chat/）。

```
data: {"type":"<type>","step":<N>,"timestamp":"<本地ISO>", ...step_dict其余字段}\n\n
```

- `format_agent_sse(step_dict)`（sse_formatter.py:53-59）→ `format_sse_event`（:35-50）：纯字符串拼接，`json.dumps(ensure_ascii=False)`；循环引用由 `_break_circular` 兜底（sse_formatter.py:20-32）。
- timestamp 全程本地时区 ISO 无 Z（2026-08-08 全程统一本地时区）。
- 调用方（均经此处出口）：consumer `stream_reader` 转发（stream_reader.py:129）、start 旁路（stream_orchestrator.py:218）、config_error 终态（stream_orchestrator.py:156-166）、错误/终态（sse_events.py:44/122）、任务状态（task_runtime.py:76/81）。

#### 1.5 前后端事件总览（一次对话的完整事件清单）

> 本节定位：给读者一张前后端沟通交互的事件全图——每个事件 type 是什么、何时由后端产出、携带哪些关键字段、前端如何消费。字段细节见 1.6（各 Step 字段契约）、各事件在场景中的出现顺序见 1.7（各场景产出序列）、SSE 传输格式见 1.4。start 事件是唯一旁路（见 1.1），不进缓冲、断线重连不重放。

##### 1.5.1 start（任务开始）

① 含义：一次任务开始。
② 产出时机：agent 启动前由 `step_start` 旁路直接产出（stream_orchestrator.py:208-220，send_start_step sse_events.py:129-153）。
③ 关键字段：display_name、provider、model、task_id、user_message、security_check{is_safe,risk_level,risk,blocked}、warning。
④ 前端消费：case 'start'（sse.ts:1150）映射 startStep 到执行步骤列表，创建"AI 正在思考..."占位。

##### 1.5.2 thought（思考过程）

① 含义：模型推理中间过程。
② 产出时机：react_cycle 每轮推理产出 ThoughtStep。
③ 关键字段：thought、reasoning、tool_name、tool_params。
④ 前端消费：case 'thought'（sse.ts:1209）追加思考步骤展示。

##### 1.5.3 action_tool（工具调用，含结果）

① 含义：一次工具执行及其结果。
② 产出时机：工具执行完产出 ActionStep（action_handler.py:656-663）。
③ 关键字段：tool_name、tool_params、execution_status、execution_result{data,llm_data,other_data}、action_retry_count、execution_time_ms。
④ 前端消费：case 'action_tool'（sse.ts:1481）交给 ToolResultRenderer（30+ Renderer 全读 execution_result.llm_data）。

##### 1.5.4 observation（工具结果观察）

① 含义：工具结果的汇总观察。
② 产出时机：工具结果汇总产出 ObservationStep（action_handler.py:817-823）。
③ 关键字段：content(summary)、llm_data、tool_result、other_data、parallel_results。现状缺陷：前端读 obsStep.observation 是死分支，见 2.6。
④ 前端消费：case 'observation'（sse.ts:1567）走 StepContent.tsx:140-474 渲染，现状实际渲染为空。

##### 1.5.5 chunk（流式回复片段）

① 含义：模型流式生成中的回复片段。
② 产出时机：模型流式生成中产出 ChunkStep。
③ 关键字段：content（片段）。
④ 前端消费：case 'chunk'（sse.ts:1250）追加到回复内容。

##### 1.5.6 final（最终回复/终态）

① 含义：任务最终回复，多态终态（completed/failed/cancelled）。
② 产出时机：任务结束产出 FinalStep（final_step.py:24-105）。
③ 关键字段：response、thought、outcome、error_type、error_message、model、provider、display_name、accumulated_usage。
④ 前端消费：case 'final'（sse.ts:1312）终结消息展示，按 outcome 分流。

##### 1.5.7 error（错误）

① 含义：任务出错。
② 产出时机：错误分支产出 ErrorStep（sse_events.py:44 create_error_response）。
③ 关键字段：error_type、error_message。
④ 前端消费：case 'error'（sse.ts:1394）错误提示。

##### 1.5.8 cancelled（取消）

① 含义：任务被取消。
② 产出时机：取消分支 + finally 守卫补发 MetaStep(cancelled)（agent_runner.py:205-216）。
③ 关键字段：content（统一文本）。
④ 前端消费：case 'cancelled'（sse.ts:1675）标记取消状态。

##### 1.5.9 paused（暂停）

① 含义：任务暂停。
② 产出时机：暂停控制产出 MetaStep(paused)。
③ 关键字段：content（统一文本）。
④ 前端消费：case 'paused'（sse.ts:1676）标记暂停状态。

##### 1.5.10 resumed（恢复）

① 含义：任务恢复。
② 产出时机：恢复控制产出 MetaStep(resumed)。
③ 关键字段：content（统一文本）。
④ 前端消费：case 'resumed'（sse.ts:1677）标记恢复状态。

##### 1.5.11 retrying（重试中）

① 含义：空响应/未调工具时进入重试。
② 产出时机：空响应/未调工具循环产出 MetaStep(retrying)（react_cycle.py:609-613）。
③ 关键字段：content（统一文本）。
④ 前端消费：case 'retrying'（sse.ts:1678）标记重试状态。

##### 1.5.12 authorization_required（需要安全确认）

① 含义：高安全级工具需要用户确认。
② 产出时机：高安全级工具触发 MetaStep(authorization_required)。
③ 关键字段：confirm_id、tool_name、params、safety_level。
④ 前端消费：无 sse.ts case，由 useChatCallbacks.ts:769 触发 CustomEvent，NewChatContainer.tsx:107 监听弹确认框。

##### 1.5.13 truncated（上下文超限截断）

① 含义：上下文超限被截断。
② 产出时机：上下文超限产出 MetaStep(truncated)。
③ 关键字段：content。
④ 前端消费：无 sse.ts case，前端走默认兜底（不显式处理）。

> 全图速览：一次正常对话 = `start`（开）→ `thought` → `action_tool` → `observation`（可循环）→ `chunk`（流式回复）→ `final`（收）；异常/控制路径穿插 `error`/`retrying`/`cancelled`/`paused`/`resumed`/`authorization_required`/`truncated`。

#### 1.6 各 Step 字段契约（steps/ 目录，to_dict 输出 == SSE == step_json）

`ReasoningStep.to_dict()`（base.py:70-78）统一输出 `{type, step, timestamp, content}` 四键，再叠加各 Step 的 `_extra_fields()`。`content` 由 `get_content()` 产出。

- ThoughtStep（type=`thought`）：content 来源 = content/thought；顶层字段 = thought、reasoning、tool_name、tool_params。
- ActionStep（type=`action_tool`）：content 来源 = 空串（get_content 返回 ""，action_step.py:40-41）；顶层字段 = tool_name、tool_params、execution_status、execution_result（完整 {data, llm_data, other_data} 3 字段）、action_retry_count、execution_time_ms。
- ObservationStep（type=`observation`）：content 来源 = llm_data 各条 summary 拼接（observation_step.py:44-48）；顶层字段 = content(summary)、llm_data(列表)、tool_result、other_data、parallel_results。
- FinalStep（type=`final`）：content 来源 = response；顶层字段 = response、thought、outcome(completed/failed/cancelled)、error_type、error_message、model、provider、display_name、accumulated_usage。
- ErrorStep（type=`error`）：content 来源 = error_message；顶层字段 = error_type、error_message。
- ChunkStep（type=`chunk`）：content 来源 = 流式回复片段；顶层字段 = content。
- MetaStep（type=`start`/`cancelled`/`paused`/`resumed`/`retrying`/`authorization_required`）：content 来源 = 统一 content 文本（base.py:126 字段契约）；顶层字段 = 透传 kwargs（confirm_id/tool_name/params/safety_level/wait_time/data 等）。

- 终态统一由 `final` 承载：FinalStep 多态（outcome=completed/failed/cancelled），前端按 outcome 分流（final_step.py:24-105，2026-07-18 重构）。
- 步骤计数：`create_step_counter()`（base.py:84-93）闭包 `next_step()` 全局唯一递增。
- 各 Step 在同一抽象层：Step 对象在 react_cycle 内 `emit` → `_step_emitter.emit(step)`，最终回到 agent_runner 的 `async for event` 循环。

#### 1.7 各场景产出的 Step 类型（前端看到的剧本）

> 注：`MetaStep(start)` 由 `step_start`（stream_orchestrator.py:208-220）在 agent 启动前前置产出，不走 1.3 缓冲（见 1.1 旁路说明）；其余步骤均由 react_cycle 产出。

- 正常调工具（单/并行）：MetaStep(start) → ThoughtStep → ActionStep(每工具一条) → ObservationStep → … → FinalStep(completed)。
- 并行工具：1 条 assistant 带 N 个 tool_calls + N 条 ActionStep + 1 条 ObservationStep（action_handler.py:656-663 配对规则）。
- 空响应/未调工具（B3）：MetaStep(start) → MetaStep(retrying) → 循环 → FinalStep。
- 同工具/同参数死循环：ThoughtStep + 纠偏 warning（_temp_same_tool_warn 临时标记，不入库）→ 循环 → FinalStep。
- 截断：MetaStep(truncated)（上下文超限）。
- 失败：MetaStep(start) → … → FinalStep(outcome="failed", error_type, error_message)。
- 取消：MetaStep(cancelled) + FinalStep(outcome="cancelled")（agent_runner.py:205-216 取消分支 + finally 守卫补发）。
- 暂停：MetaStep(paused) / MetaStep(resumed)。

#### 1.8 前端消费实况

- 工具结果渲染：ToolResultRenderer 的 30+ 个 Renderer（frontend/src/components/Chat/ToolResultRenderer/types/*.tsx）全部从 `step.execution_result` 取 `llm_data`：
  ```ts
  const execResult = (step.execution_result || {}) as Record<string, unknown>;
  const llmData = (execResult.llm_data || {}) as Record<string, unknown>;
  ```
  成功判定统一以 `llm_data.status.exec_code === "success"` 为准（各 Renderer 文件头注释一致）。
- observation 步骤展示（现状有缺陷）：StepContent.tsx:140-474 observation 分支以 `obsStep.observation` 是否为 object 判定展示（`isObjFormat = obsStep.observation !== null && obsStep.observation !== undefined && typeof obsStep.observation === 'object'`，与 StepContent.tsx:144-146 一致），但后端 `ObservationStep._extra_fields()` 从不产出 `observation` 字段（仅输出 `llm_data`/`tool_result`/`other_data`/`parallel_results`，observation_step.py:50-59）。故 `obsStep.observation` 恒为 `undefined` → `rawObs = null` → `llmData/summary` 全走 `??` 兜底 → 最终 `summary = ''`。即当前 observation 步骤在前端实际渲染为空（ExecutionPanel.tsx:387-391 case 'observation' 同理）。这是"前端看不到真实 observation"的更直接根因，比"只存 summary"更根因。
- 依赖方向：前端随后端修改，契约以后端 step_dict 为准，前端不质疑后端字段（AGENTS.md 1.5 前后端规范）。

### 2 问题

#### 2.1 真身不入库/不 yield

`build_observation_text`（observation_formatter.py:1466）的产物 `obs_text`（action_handler.py:729 异常分支 / 733 正常分支）与最终 `merged_obs`（action_handler.py:779，`"\n\n".join(obs_parts)`）是喂给 LLM 的确切 observation 文本（经 message_builder.add_tool_result 进 conversation_history，message_builder.py:124-144、action_handler.py:768/772）。但 ObservationStep.to_dict() 不序列化该文本——它不进 SSE、不入库。

#### 2.2 落库的只是 summary 拼接

ObservationStep.get_content() 仅从 llm_data 各条取 `summary` 字段拼接（observation_step.py:44-48）。summary 是各 tool 自己写的短摘要，不是喂 LLM 的完整 observation 文本。

#### 2.3 历史回放失真

多轮上下文加载 `_load_previous_messages`（stream_reader.py:87-116）→ `_parse_observations`（stream_reader.py:67-84）只读 `step.get("content")`（= summary），用它重建 `role:tool` 消息。结果：历史回放中 LLM 看到的 observation 是截断后的 summary，而非原始完整 observation——功能退化（多轮对话上下文质量下降、复杂任务需回读全文时信息丢失）。

#### 2.4 排查困难

日志/DB 中无 observation 原文，排查"LLM 为什么这么回答"需重新跑 observation_formatter 格式化逻辑才能复原（action_handler.py:738-745 处仅日志记录 raw_data，无格式化后文本）。

#### 2.5 字段冗余（次要问题）

ObservationStep 同时携带 content(summary) 与 llm_data/tool_result，两处信息冗余且口径不一致（summary 截断 vs 完整 data），字段职责不清。

#### 2.6 前端事实性硬伤

原版本（v2.0）1.6 节称"前端 observation 分支读 `obsStep.observation`（object 时读其 llm_data）"，但后端 `ObservationStep` 从不产出 `observation` 字段，该分支是永不触发的死路——前端 observation 步骤实际渲染为空。这才是"前端看不到真实 observation"的直接根因；改进方案必须同步修正前端取数字段名（读 `observation_text`/`content`），而非沿用 `obsStep.observation`。

#### 2.7 chunk 思考流与 thought 完整思考重复展示（内容同源）

同一份思考内容，前端会收到两遍：
- chunk（is_reasoning=true）：LLM 流式返回时每吐一段思考即 yield `ChunkStep`（llm_stream.py:166），前端 sse.ts:1250 逐段实时显示（MessageContent ChunkRenderer 灰色斜体"思考区"）。
- thought：响应解析完，后端再 emit `ThoughtStep(content=thought, reasoning=reasoning)`（action_handler.py:910-915、answer_handler.py:179-193），前端 sse.ts:1209 追加一条完整思考步骤（StepContent.tsx:475 渲染"💭 思考"+"🧠 reasoning 卡片"）。

两者文本同源：`ThoughtStep.reasoning` 来自 `parsed.reasoning`，而它正是流式阶段累积的 `full_reasoning`（llm_stream.py:163 同一份累积串）。因此正常流式场景下，同一份思考文字在前端显示两遍（思考区 + 思考卡片）。

但 thought 不是纯冗余，不可删：
① 行动声明：ThoughtStep 带 `tool_name`/`tool_params`（下一步要调什么工具），chunk 只带文本——"思考→调用工具"的步骤链靠 thought 衔接；
② 非流式 reasoning 兜底：部分 LLM 思考是一次吐完（reasoning 不流式），此时 chunk 里根本没有 is_reasoning=true，思考只能靠 thought 显示；
③ 历史回放载体：断线重连（after_seq 续传）与 DB 回放时 chunk 不重放（start 同理见 1.1 旁路），thought 是完整思考的唯一可靠来源。

#### 2.8 现有前端展示模式与流水线式交互不匹配（北京老陈裁定推翻重写）

现状前端是"分步卡片"式：StepRow/StepContent/ExecutionPanel 把 thought/action_tool/observation/final 各渲染成独立边框卡片，带"💭 思考""🧠 推理""下一步""工具调用"等明显分块标记，对话页呈现为卡片堆叠。

北京老陈裁定：前端显示模式全部推翻重写，改为流水线式——前端发送任务消息后，AI 的处理按后端事件流顺序（start → thought → action_tool → observation → chunk → final）在对话页面连贯自然流出，去掉全部框框，不再有分块视觉标记。

与既有问题/方案的关系：本裁定与 2.6/2.7/3.5 方向一致但更彻底——2.7/3.5 是"保留卡片只去重"，本裁定是展示层整体重写，thought/chunk 重复、observation 死分支、分块割裂感一并在新模式下消解（思考统一走灰色斜体自然流出，工具过程内联弱化显示，不再有独立边框卡片）。

### 3 更好的方案（先派生、后保真，单向依赖）

#### 3.1 首选方案（DRY 单一来源，零新增字段）

回放时由 `ActionStep.execution_result` + 同一 `build_observation_text`（action_handler.py:733）重新派生 observation 文本，而非落库一份派生字符串。`execution_result`（`{data, llm_data, other_data}`）已是完整权威副本，喂 LLM 的 `merged_obs` 由其确定性派生（= `build_observation_text(result, tool_name, tool_params)` 拼接），无需新增字段，最契合 DRY/YAGNI。

> 诚实边界："重跑同一函数"可近似复原，不能逐字节复原——`merged_obs` 含运行时上下文，回放无法重建：①异常分支包装 `f"Observation: 工具...执行异常: {result}"`（action_handler.py:729）；②`repair_warning` 前缀（action_handler.py:759-761，来自运行时 `_temp_same_tool_warn`，不入库）。这些前缀只在发生相应运行时分支时存在，多数正常路径可精确复原；但在异常/纠偏路径上与当时喂 LLM 的文本有差异。

#### 3.2 首选/保真决策标准

是否新增 `observation_text`，取决于是否接受两处近似——(a) `repair_warning`/异常包装的近似复原；(b) `build_observation_text` 日后变更导致的漂移。默认用首选（零字段、零膨胀）；仅在需要"回看当时逐字喂 LLM 的 observation"的排查/审查场景启用保真。启用保真须满足下述约束，否则引入新冗余：

- 依赖方向（单向依赖）：要求——"加 observation_text" 依赖 "删四字段"（纯加会冗余增到 5 个）；"删四字段" 本身独立安全，首选方案下可单独落地。理由——删四字段只需"前端不从 ObsStep 加字段取数 + 回放改从 execution_result 派生"，与是否加保真字段无耦合；只有"只加不删"才会退化，故仅约束"加须建立在删之上"，不强制双向同批回滚。
- 落库口径：要求——`observation_text` 直接对已截断后的 execution_result 派生、原样落库，不再二次截断。理由——各 tool 已自行截断输出（第二部分 5.6 阈值依据），故 `merged_obs` 体量受 tool 层截断约束、无爆炸风险；对已截断数据派生使两源天然同口径，无需为"是否截断"设双轨，消解"保真 vs 截断"矛盾。
- 前端取数修正：要求——前端 observation 分支改读 `observation_text`/`content`，删除 `obsStep.observation` 死分支（问题 2.6）。理由——否则加字段前端也读不到，定案名不副实。
- 多轮回放：要求——`_parse_observations`（stream_reader.py:67-84）：首选方案下由 execution_result 重新派生；保真方案下优先读 `observation_text`、回退 `content`。理由——多轮对话上下文质量与首次一致（功能增强，禁止 backward）。

#### 3.3 两方案对比

- 字段：首选 = 不新增；保真兜底 = ObservationStep 增 `observation_text`（observation_step.py 增构造参数 + `_extra_fields()` 输出 + `get_content()` 优先返回）。
- 数据源：首选 = execution_result（action_handler.py:656-662 已产出）；保真兜底 = action_handler.py:779 `merged_obs`（含异常分支 obs_text:729 与 repair_warning 前缀:759-761）。
- 落库：首选 = 无新增、回放派生；保真兜底 = 随 step_json 持久化。
- yield：首选 = 无新增；保真兜底 = SSE 自动透传，前端无需改传输层。
- 体积：首选 = 零增长；保真兜底 = 需评估（可能与 execution_result 截断口径冲突）。

#### 3.4 方案理由

原 v2.0"一增全通"用 DRY 作为删四字段的理由，转头却新增一份派生文本 `observation_text` 落库，标准不自洽。本方案把"删冗余原始副本"与"是否存派生文本"分开决策——先选零字段派生（最优），确有保真需求再存，且存时须建立在"删四字段"之上（单向依赖）、口径一致、前端改读。不新增 is_success/summary 等冗余字段（llm_data.status.exec_code 已是权威源）。

#### 3.5 chunk 思考流 vs thought 完整思考：前端展示去重（问题 2.7 收敛）

两事件都保留，只在前端去重展示——后端零改动，纯前端一处收敛。

- 不删 chunk 思考流：流式实时打字机体验不可替代；删除后流式阶段无思考展示。
- 不删 thought：它承载行动声明（tool_name/tool_params）、非流式 reasoning 兜底、历史回放载体（详见 2.7 三点），删除即功能退化（禁止 backward）。
- 收敛动作：StepContent.tsx:475 thought 分支——当同 step 已收到 is_reasoning=true 的 chunk（前端 executionSteps 中该 step 已存在思考流 chunk）时，thought 卡片只渲染行动声明（"💭 思考 → 调用工具 X"），不重复贴完整 reasoning 文本；当无流式思考（非流式 reasoning 的 LLM、断线重连续传、历史回放）时，thought 仍按现逻辑完整渲染。判定依据是前端既有数据（executionSteps 内同 step 的 chunk.is_reasoning），无需新增字段、无需后端配合。
- 效果：正常流式 = 思考区实时显示（chunk）+ 步骤卡片只留行动声明；异常/回放 = 思考完整卡片兜底。功能增强、零退化。

#### 3.6 前端展示重写：流水线式（北京老陈设计定案，完整设计）

推翻现有"分步卡片"式展示，改为流水线式——前端发送任务消息后，AI 的处理按后端事件流顺序在对话页连贯自然流出，去掉全部框框。本方案是 2.8 裁定的完整落地设计，覆盖界面结构、事件呈现、流式过程、交互、回放一致、边界情况与改造范围（3.6.1~3.6.8）。

##### 3.6.1 设计总则（四条铁律）

- 一条线：一次 AI 回复 = 一条流水线，从任务开始到最终回复，页面自上而下完全按后端事件产出顺序（start → thought → action_tool → observation → chunk → final，异常/控制事件穿插其中，顺序定义见 1.5/1.7）连贯自然流出，不打断、不跳跃。
- 去全部框框：所有步骤不再有边框卡片，不再有"💭 思考""🧠 推理""下一步""工具调用"等明显分块标签；视觉区分完全靠字体样式（灰色斜体=思考、黑色正文=回复）、缩进、颜色，靠对话节奏自然流转。
- 后端事件流驱动：实时 = SSE 逐事件生长流水线；回放 = DB step_json 按 seq 顺序重放同一流水线。前端只按事件流排布，不自己造结构。
- 显示什么样、回显就什么样：实时与回放共用同一套流水线渲染函数，视觉元素与布局零差异。

##### 3.6.2 对话界面整体结构

- 对话区纵向排布：用户消息在上、AI 流水线在下，多轮交替。
- 一条 AI 流水线自上而下由三段构成：
  - 头部·模型标识：极弱化灰色小字（模型名，取 start.display_name，start 缺失用 final.display_name 兜底），提示本次回答来源；无边框、无头像卡片。
  - 正文流：思考流（灰色斜体）↔ 工具行（内联弱化）↔ 思考流 ↔ … ↔ 最终回复（黑色正文），完全按事件流生长。
  - 尾部·消耗统计（可选）：最终回复下方一行极弱化灰色小字（token 消耗，取 final.accumulated_usage，无值不显示）。
- 去框框范围：AI 侧全部无边框；用户消息沿用现有对话气泡样式（对话惯例，不在本设计改动范围）。

##### 3.6.3 各事件类型的流水线呈现（严格按后端字段契约）

按事件逐类设计（字段以 1.6 契约为准，行号以核实源码为准）：

###### 3.6.3.1 start（模型标识 + 安全拦截）

- 呈现两件事：① 模型标识小字放流水线最上方（display_name）；② security_check.blocked=true 时在流水线顶部显示一行弱化拦截提示（risk_level/risk），流水线不开始。
- 不再显示"🤔 AI 正在思考..."占位卡片（sse.ts:1163 现文案废弃）；start.content（用户消息回显）不重复显示——用户消息在对话界面本就可见。
- warning（模型不在列表）场景：后端随后直接下发 final(outcome=failed, config_error)（stream_orchestrator.py:156-166），前端按 3.6.3.6 failed 渲染。

###### 3.6.3.2 thought（思考段）

- 字段：content/thought/reasoning/tool_name/tool_params。
- 呈现：灰色斜体文字自然流出，无框、无标签，成为流水线中的"思考段"。
- 行动声明（tool_name/tool_params）不单独展示，由随后的 action_tool 工具行呈现（3.6.3.4），thought 只贡献思考文本。
- 与 chunk(is_reasoning=true) 的合并：同一轮思考，chunk 先逐段流出灰色斜体思考流，thought 到达时其 reasoning 与已流出文本同源（问题 2.7），不重复追加（合并规则见 3.6.6）。

###### 3.6.3.3 chunk（流式）

- is_reasoning=true：思考流片段，灰色斜体逐字流出（延续 MessageContent ChunkRenderer 既有灰度斜体约定），与 thought 合并为同一思考段。
- is_reasoning=false：最终回复片段，黑色正文逐字流出（打字机效果）。
- 流式光标：流出中的文本末位显示闪烁光标，流结束后消失。

###### 3.6.3.4 action_tool（工具行）

- 字段：content=""、tool_name、tool_params、execution_status、execution_result、action_retry_count、execution_time_ms。
- 呈现：内联弱化一行小字 "→ 调用工具 {tool_name}"，紧跟在其思考段之后，缩进与字号弱于正文：
  - 执行中（实时）：行尾显示"…"，流水线在此短暂停留；
  - 完成：按 execution_status 显示状态符号（成功不标、失败"⚠"、warning"!"）；
  - 重试次数 action_retry_count>0：行尾极弱化小注"（重试 N 次）"。
- 整行可点开展开执行结果：再次点击收起。展开区无边框、缩进弱化文字，复用既有 ToolResultRenderer 对 execution_result.llm_data 的渲染逻辑（去掉边框卡片包装）。默认收起，保持流水线轻量。

###### 3.6.3.5 observation（并入工具行展开区）

- 字段：content(首条 summary)、llm_data[]、tool_result、other_data、parallel_results。
- 呈现：不单独占流水线一行——并入紧邻 action_tool 的展开区（与 execution_result 同源，见 3.1/3.2 首选方案）。并行多工具（parallel_results）时，每个 action_tool 工具行对应一个展开区，llm_data[] 按索引 1:1 匹配。
- 回放兜底：老数据缺 execution_result 时，展开区回退显示 observation 的 summary/content 弱化文本。
- 现状 obsStep.observation 死分支（问题 2.6）在新模式下彻底不渲染，随 3.6.8 删除。

###### 3.6.3.6 final（终态正文 + 状态）

- 字段：content=response、response、thought、outcome、error_type、error_message、display_name、accumulated_usage。
- outcome=completed：response 作为最终回复黑色正文；若前面已有 is_reasoning=false 的 chunk 流式正文（同源），不重复渲染，chunk 无正文时由 final 补上；尾部显示消耗统计小字（accumulated_usage 有值才显）。
- outcome=failed：显示可读正文（response/thought 内容）+ 下方一行弱化警示文字（error_message；若已由 error 事件展示则不重复）。
- outcome=cancelled：保留已流出内容 + 一行弱化"已取消"。
- model/provider/display_name：与 start 同源，只取一次放流水线头部。

###### 3.6.3.7 error（警示行）

- 字段：error_type、error_message。
- 呈现：流水线中一行弱化警示文字（如"⚠ 工具确认超时，已跳过：xxx"），无框。error 后若任务继续（工具被拒/超时跳过），流水线继续生长；若任务终止，后随 final(outcome=failed) 收尾。

###### 3.6.3.8 usage（token 报告，不显示）

- 字段：content=""、prompt_tokens/completion_tokens/total_tokens（react_cycle.py:398-407）。
- 呈现：不进流水线视觉（前端 sse.ts 无 case，静默忽略，本设计维持）。token 消耗仅由 final.accumulated_usage 在回复尾部极弱化一行呈现，避免打断对话流。

###### 3.6.3.9 paused（暂停，两种来源）

- HITL 工具确认（paused 带 confirm_id/tool_name/params/safety_level，action_handler.py:240-248）：内联提示行 + 文字式操作按钮（无框）——"需要确认执行工具 {tool_name}：〔允许〕〔拒绝〕〔信任本次会话〕"，与工具行同样弱化内联。确认后流水线继续、该工具行随后呈现；拒绝/超时后跟 error 警示行（action_handler.py:271-282）。
- 用户手动暂停（content="任务已暂停"，task_runtime.py:120）：弱化小字"任务已暂停"插在当前流水线位置，已流出内容冻结。

###### 3.6.3.10 resumed（恢复）

- HITL 授权后（content="已临时授权白名单外路径…"，action_handler.py:292-296）：弱化小字记录后流水线继续。
- 用户恢复（content="任务已恢复"，task_runtime.py:138）：弱化小字"任务已恢复"，流水线继续生长。

###### 3.6.3.11 retrying（重试）

- 字段：content、wait_time(可选)。
- 呈现：弱化小字"LLM 请求异常，准备重试…"（react_cycle.py:609-613 / answer_handler.py:140-145），之后继续思考流；重试达上限后 error/final 结束。

###### 3.6.3.12 cancelled（取消）

- 字段：content="任务已被取消"（task_runtime.py:73 / agent_runner.py finally 守卫）。
- 呈现：弱化小字"已取消"收尾，流水线终止；已流出内容完整保留。

##### 3.6.4 流式过程与终态呈现

- 思考流打字机：灰色斜体文本逐字流出，末位闪烁光标；工具行执行中行尾"…"（或轻旋转小图标，无框）。
- 事件衔接无缝：事件与事件之间无卡片空隙、无分块空隔，仅靠字体/颜色/缩进区分段落。
- 终态定格：final/error/cancelled 到达后光标消失、流水线定格为最终形态。

##### 3.6.5 交互设计

- 工具行展开/收起：点击整行切换；展开区无边框、缩进弱化文字；默认收起。
- HITL 授权确认：内联文字式按钮（允许/拒绝/信任本次会话），确认后从流水线移除提示行（回放时该确认步骤在事件流中已不存在，流水线由事件流重建，故"移除"与回放天然一致）；拒绝后跟 error 行。
- 滚动跟随：流式过程中用户未手动滚动时自动滚到底部，手动滚动后暂停跟随。
- 取消/暂停控制：沿用现有对话页控制按钮（不在本设计改动范围），流水线按 3.6.3.9/3.6.3.12 呈现状态。

##### 3.6.6 回放一致性（同一套渲染）

- 同一个渲染函数：实时（SSE 逐事件增量构建流水线数组）与回放（DB step_json 全量按 seq 组装）喂给同一个 PipelineRenderer，事件元素与布局完全一致，实现"显示什么样、回显就什么样"。
- 思考合并规则一致：实时与回放都按"chunk 思考流 + thought 完整思考合并为同一思考段（同源文本去重）"规则渲染；回放时 DB 无 chunk 思考流（非流式 LLM、老数据）则直接用 thought 文本作为思考段（即 2.7 的 thought 兜底价值，在新模式下统一进思考流，无卡片）。
- 工具展开兜底一致：回放缺 execution_result 时展开区显示 observation 的 summary/content 弱化文本（3.6.3.5）。
- 断线续传（after_seq）：已渲染部分不重绘，从断点 seq 继续生长，同一渲染函数天然支持（列表末尾 append）。

##### 3.6.7 边界情况

- 安全拦截：start.security_check.blocked=true → 流水线顶部拦截提示行，不进入思考流。
- 空思考/非流式推理：无 chunk(is_reasoning=true) 思考流时，thought 直接作为完整思考段（灰色斜体）呈现。
- 无回复直接失败：无正文、final(outcome=failed) 时，流水线显示 error_message 弱化警示行收尾。
- 纯工具任务：思考→工具→思考→工具反复，最终回复短或空；流水线以工具行序列为主体，final 收尾。
- 多轮对话：每轮"用户消息 + AI 流水线"为一组，轮次间以自然间距分隔（非卡片、非分隔框）。
- 模型不在列表（config_error）：start 后直接 final(outcome=failed)，流水线头部模型标识 + 失败警示行。
- HITL 超时：paused 提示后等待超时，跟 error 警示行（"确认超时未响应"），流水线继续。

##### 3.6.8 前端改造范围

- 废弃/删除：
  - StepRow 体系卡片渲染（StepRow/StepHeader/StepFooter/StepContent.tsx 各 type 分支边框卡片）；
  - ExecutionPanel 分块卡片式展示（:387-391 observation 死分支一并删除）；
  - sse.ts:1163 start 的"🤔 AI 正在思考..."占位文案。
- 新建：
  - PipelineRenderer（流水线渲染器：输入=按 seq 排序的事件数组，输出=3.6.2 三段式流水线，含 3.6.3 全部事件元素）；
  - 子元素：ThinkingStream（灰色斜体思考流，含光标）、ToolCallLine（工具行+展开区）、ResponseStream（正文打字机）、StatusLine（paused/resumed/retrying/cancelled/error 弱化行）、AuthorizationInline（HITL 内联确认按钮）。
- 保留复用：
  - MessageContent ChunkRenderer 的 is_reasoning 灰度斜体样式（演进为 ThinkingStream/ResponseStream 的基础）；
  - ToolResultRenderer 内容渲染逻辑（去边框包装后复用为展开区）；
  - sse.ts 事件解析/落库/断线续传骨架（字段映射不变，去掉卡片语义、start 改产出模型标识+安全字段、final 补 accumulated_usage 映射、usage 显式忽略防误入 default）。
- 验证：发送任务后 AI 处理按事件流顺序流水线式自然流出、无边框卡片；工具行可展开；HITL 内联确认可用；刷新/断线续传后回显与实时显示一致。

### 4 实施计划

#### 4.1 实施计划

##### 4.1.1 首选方案落地

回放由 `execution_result` 重新派生 observation，`_parse_observations` 不再依赖 `content` 重建 role:tool 消息。
- 涉及文件：`backend/app/services/chat/stream_reader.py`（`_parse_observations:67-84`）
- 依赖：第二部分「删四字段」收敛 execution_result 为单一权威（先落）
- 验证：多轮对话历史回放，对比每次喂 LLM 的 observation 是否为完整原文

##### 4.1.2 前端取数修正

前端删 `obsStep.observation` 死分支，改读 `observation_text`/`content`。
- 涉及文件：`frontend/src/components/Chat/StepRow/StepContent.tsx:140-474`、`ExecutionPanel.tsx:387-391`
- 依赖：4.1.1
- 验证：observation 步骤前端正常展示总结，无空渲染

##### 4.1.3 保真兜底（仅排查/审查场景启用）

ObservationStep 增 `observation_text`，数据源 `merged_obs`，随 step_json 持久化。
- 涉及文件：`backend/app/services/agent/steps/observation_step.py`（构造参数/_extra_fields/get_content）、`action_handler.py:779`
- 依赖：4.1.1 + 第二部分「删四字段」单向依赖
- 验证：排查场景可回看当时逐字喂 LLM 的 observation 原文

##### 4.1.4 文档同步

后端索引/文档同步：本文档结论、`AGENTS.md` 字段契约、`FUNCTIONS.md` 若有公用函数变化。
- 涉及文件：文档
- 依赖：4.1.1~4.1.3
- 验证：契约一致

##### 4.1.5 前端展示流水线重写（问题 2.8，方案 3.6，完整设计见 3.6.1~3.6.8）

推翻"分步卡片"式展示，改为流水线式——思考走灰色斜体自然流出、工具过程内联弱化显示、去掉全部边框卡片；实时与回放用同一套渲染（显示什么样回显就什么样）。本步骤取代原 2.7/3.5 的"卡片去重"方案（去框后不再有卡片，重复与死分支在新模式下自然消解）。
- 涉及文件：废弃——`frontend/src/components/Chat/StepRow/StepContent.tsx`、`StepRow/StepHeader.tsx`、`StepRow/StepFooter.tsx`、`Chat/ExecutionPanel.tsx` 卡片渲染；新建——`PipelineRenderer`（3.6.2 三段式流水线 + 3.6.3 全部事件元素，含 ThinkingStream/ToolCallLine/ResponseStream/StatusLine/AuthorizationInline 子组件）；改——`MessageContent/index.tsx`（灰度斜体样式演进为思考流/正文流）、`MessageItem/AIMessageBubble.tsx`（接入流水线渲染）、`frontend/src/utils/sse.ts`（start 改产出模型标识+安全字段、final 补 accumulated_usage、usage 显式忽略）
- 依赖：无（纯前端，独立落地）
- 验证：发送任务后 AI 处理按事件流顺序流水线式自然流出、无边框卡片；工具行可展开；HITL 内联确认可用；刷新/断线续传后回显与实时显示一致

#### 4.2 实施顺序与风险

- 顺序：第二部分「一删、一治本」先落地（收敛单一权威）→ 本部分 4.1.1（回放派生）→ 4.1.2（前端取数）→ 4.1.3 按需 → 4.1.5（前端流水线重写）建议在 4.1.2 之后、4.1.3 之前落地（流水线重写会一并处理 observation 展示与 thought/chunk 展示，避免 4.1.2 的卡片式修改做两遍）。
- 风险：`_parse_observations` 重写派生逻辑时，须用与 `build_observation_text` 完全一致的入参（tool_name/tool_params/result），否则回放与首次口径漂移；建议抽公用函数（复用优先，先查 FUNCTIONS.md）避免两份格式化逻辑。
- 回滚：4.1.1/4.1.2/4.1.5 属行为等价重构，回退仅限本部分；4.1.3 增字段须随删四字段单向回滚。

---

## 第二部分　存储数据

### 5 现状

#### 5.1 三库总览

- chat_history.db（~/.omniagent/）：会话/消息/步骤（本设计聚焦）。
- operations.db（~/.omniagent/）：文件操作（备份/回滚/查询）。
- task_tracker.db（~/.omniagent/）：Task 追踪。

#### 5.2 chat_history.db 表结构（db_initializer.py）

- chat_sessions：id/title/created_at/updated_at/message_count/is_deleted —— 会话主表。
- chat_messages：id/session_id/role/content/timestamp/status/thought（execution_steps 列仅旧数据读取兼容）—— 消息表：一行=一条消息；steps 已拆到独立表。
- chat_message_steps：id/message_id/session_id/step_index/step_json/created_at —— 步骤主存储：一行=一步。
- chat_session_title_history：session_id/title/created_at —— 标题历史。

#### 5.3 数据库访问机制（database.py）

- get_conn 统一入口：全库走统一连接入口，杜绝 SQLite 越界。
- _ParamSafeConnection：参数化查询闸门，防 SQL 注入。
- WAL：全库 WAL 模式，读写并发。
- busy_timeout + 指数退避重试：get_conn_with_retry（0.5/1/2s，database.py），5 处热循环连接改用它（agent_runner.py 2026-07-23 #14 修复）。

#### 5.4 落库链路（storage.py，四函数）

- allocate_and_insert_message（storage.py:232-249）：预分配 assistant 消息 ID + 插入空白行 + message_count+1；local_time 已提至 if 外赋值（2026-08-13 #1 修复 NameError）。
- append_execution_step（storage.py:325-334）：每步独立事务写 chat_message_steps（截断 → safe_json_dumps → INSERT）。
- finalize_message（storage.py:353-358）：finally 轻量终态：UPDATE content/status/thought。
- load_execution_steps（storage.py:337-350）：读侧从新表组装，无数据回退 chat_messages.execution_steps 旧列（兼容）。

调用关系（agent_runner.py）：
- 每产出一步：`append_execution_step`（agent_runner.py:179-188，ai_message_id 首次经 allocate_and_insert_message 分配）
- 异常分支：final 步也立即落库（agent_runner.py:231-234）
- finally 守卫补发 FinalStep（agent_runner.py:246-276）→ `finalize_message`（agent_runner.py:305-306）
- ai_message_id 未分配兜底：`save_steps` 整批写（agent_runner.py:307-310）
- IntegrityError 不重试（UNIQUE，YAGNI）（agent_runner.py:312-314）

依赖方向（2026-08-13 P4 重构）：agent_runner 不再直接 import chat 模块，6 个持久化回调由 stream_orchestrator 以 `db_ops` SimpleNamespace 注入（stream_orchestrator.py:171-183），依赖方向 chat→agent 单向。

#### 5.5 渐进耐久设计

- 每步独立事务 → 崩溃时已产出步骤不丢；finally 只做轻量终态更新。
- 启动迁移：migrate_steps.py 一次性把旧 execution_steps 列数据迁移到 chat_message_steps（schema_migrations 守卫），启动从 25s→<1s。

#### 5.6 存储层截断机制（storage.py:252-322）

- MAX_TOOL_RESULT_ITEMS = 1000：列表超 1000 条截断（storage.py:255）。
- MAX_TOOL_RESULT_STR_LEN = 100000：单字符串超 10 万字符截断并附加「原长 N 字符」标记（storage.py:256, 307-322）。
- _truncate_tool_result（storage.py:258-273）：递归截断过大的列表（2026-08-13 #9 修复 list 内嵌超长 list）。
- _truncate_tool_result_strings（storage.py:307-322）：递归截断所有超长字符串（base64/长文本）。
- _truncate_step_dict（storage.py:276-298）：作用对象：`tool_result` / `execution_result` / `parallel_results[].tool_result / execution_result`（不碰 observation）。
- storage.py:254 标注：「实验性的功能: TODO 做正式的持久化设计后进行更新」——本设计文档即该 TODO 的正式落点。

> 阈值依据（2026-07-23 北京老陈驱动）：各 tool 已自行截断输出，storage 仅做安全兜底防 SQLite 行溢出（约 1MB TEXT 上限），不做激进取舍。

#### 5.7 现状存储形态：同一工具结果两处存放（问题根源）

ActionStep（action_handler.py:656-663）：
```python
ActionStep(step, tool_name, tool_params, execution_result=result, execution_status=_ec)
```
存完整 `execution_result` = {data, llm_data, other_data} 3 字段。

ObservationStep（action_handler.py:817-823）：
```python
ObservationStep(step, llm_data=llm_data_list, tool_result=…, other_data=merged_other, parallel_results=_parallel_results or None)
```
又存一遍 `llm_data`(列表) + `tool_result` + `other_data` + `parallel_results`。

提取过程（action_handler.py:781-800）：从每个 result 重新拆 `_all_llm_data/_all_tool_results/_all_other_data/_parallel_results`，同一份数据第二份副本。

### 6 问题

#### 6.1 双重存储（核心问题）

同一工具结果存两份——ActionStep.execution_result（完整 3 字段，action_handler.py:656-663）与 ObservationStep.tool_result/llm_data/other_data/parallel_results（action_handler.py:786-800、817-823）。后果：
- 库膨胀：大工具结果（文件列表、搜索全文）双份持久化，chat_message_steps 体积翻倍；
- 写 I/O 翻倍：每步截断 + dumps + INSERT 处理两份数据；
- 字段漂移风险：两处口径一旦不同步（如某 tool 改返回结构），前端读 execution_result、回放读 observation，出现不一致；
- 截断双重处理：_truncate_step_dict 需同时兜底 execution_result 与 observation 的 tool_result/parallel_results（storage.py:282-297），复杂度与漏截风险高。

#### 6.2 截断机制未定案

storage.py:254 临时 TODO「实验性的功能」未正式化；截断范围（哪些字段）与阈值（1000/100000）无正式设计依据背书。

### 7 更好的方案（一删、一治本、一收敛）

#### 7.1 动作清单

##### 7.1.1 删四字段

删 ObservationStep 四字段：`tool_result`/`llm_data`/`other_data`/`parallel_results`。理由——单一权威副本=ActionStep.execution_result；前端渲染本就全从 execution_result 取（第一部分 1.8 节），删除无退化。独立安全落地；若启用保真 observation_text（第一部分），其"加"须建立在本次"删"之上（单向依赖，纯加会冗余）。

##### 7.1.2 治本删二次拆包

`build_observation` 内 `action_handler.py:781-800` 的二次拆包（重循环 `ctx.results` 拆 `_all_llm_data/_all_tool_results/_all_other_data/_parallel_results`）删除，ObservationStep 直接复用 ActionStep 已持有的 `execution_result`（或第一轮循环结果）。理由——双存储的根因是"同一份 result 被拆两遍"，仅删字段只堵出口、二次拆包代码仍残留；根治须让 ObservationStep 引用已产出副本，消除冗余拆包与算力浪费。

##### 7.1.3 截断对象收敛

截断对象收敛为仅 ActionStep.execution_result。理由——observation 已删 tool_result，截断只需兜底 execution_result 一处。

##### 7.1.4 保真口径

`observation_text`（保真兜底时）对已截断 execution_result 派生、原样落库，不二次截断。理由——两源天然同口径，无需"是否截断"双轨；tool 层已自行截断故无膨胀，DB 行溢出风险已由 execution_result 截断兜底覆盖（原 TODO 本为防 SQLite 行溢出）。

##### 7.1.5 删临时 TODO

删 storage.py:254 临时 TODO，引用本设计文档。理由——消除「实验性」残留，正式定案。

##### 7.1.6 保留 content（summary）

ObservationStep 保留 content（summary）用于前端展示。理由——前端 observation 步骤展示总结文本（第一部分 1.8 节修正后改读 observation_text/content），不重复取数。

#### 7.2 收敛后存储形态

- ActionStep.execution_result：完整权威副本（截断兜底对象）；首选方案下回放由其重新派生 observation，零新增字段。
- ObservationStep.observation_text（仅保真兜底时）：喂 LLM 的完整 observation 文本，对已截断 execution_result 派生、原样落库，不二次截断（两源天然同口径）。
- ObservationStep.content：summary 展示文本；
- 其余（tool_result/llm_data/other_data/parallel_results）不再入 ObservationStep，随 execution_result 单一存放；`build_observation` 内二次拆包代码同步删除（治本）。

#### 7.3 不做

- 不新增 is_success/summary 冗余字段（llm_data.status.exec_code 已是权威源）；
- 不新建独立 observation 表（step_json 已结构化，无需第三张表）；
- 不拆多条步骤（保持一行=一步的既有契约）；
- 不"只加不删"（纯加 observation_text 会让冗余从 4 个字段增至 5 个）；"删四字段"本身独立安全、可单独落地（7.1.1 删四字段），与是否加保真字段无耦合。

#### 7.4 影响面核对

- 前端 30+ Renderer 全从 execution_result.llm_data 取数 → 删 observation 内冗余字段不影响；
- StepContent.tsx observation 分支：删除 `obsStep.observation` 死分支，改为读 `observation_text`/`content`（第一部分 2.6）；
- _parse_observations：首选方案下由 execution_result 重新派生；保真方案下优先读 observation_text 回退 content，回放质量增强；
- _truncate_step_dict：删 observation 相关分支（tool_result/parallel_results），仅保留 execution_result；
- react_cycle 终态语义、derive_status_from_steps（storage.py:71-83）、load_execution_steps 均不受影响。

### 8 实施计划

#### 8.1 实施计划

##### 8.1.1 删四字段

删 ObservationStep 四字段（tool_result/llm_data/other_data/parallel_results），前端渲染本全从 execution_result 取，删除无退化。
- 涉及文件：`backend/app/services/agent/steps/observation_step.py`（`_extra_fields:50-59` 移除）、`action_handler.py:786-800`
- 依赖：无（独立安全落地，本步先行）
- 验证：单步工具调用后前端 ToolResultRenderer 展示不变，observation 步骤仅剩 content

##### 8.1.2 治本删二次拆包

删 `build_observation` 内二次拆包（`action_handler.py:781-800` 重循环 `ctx.results` 拆四字段），ObservationStep 直接复用 ActionStep 已持有 `execution_result`。
- 涉及文件：`backend/app/services/agent/handlers/action_handler.py:781-800`
- 依赖：8.1.1
- 验证：同一工具结果不再双份持久化；chat_message_steps 体积减半

##### 8.1.3 截断收敛

`_truncate_step_dict` 删 observation 分支（tool_result/parallel_results），仅兜底 execution_result。
- 涉及文件：`backend/app/services/chat/storage.py:282-297`
- 依赖：8.1.1
- 验证：截断逻辑只处理 execution_result 一处

##### 8.1.4 删临时 TODO

删 storage.py:254 临时 TODO，引用本设计文档正式定案。
- 涉及文件：`backend/app/services/chat/storage.py:254`
- 依赖：8.1.1~8.1.3
- 验证：无「实验性」残留

##### 8.1.5 保真兜底（按需）

ObservationStep 增 `observation_text`，对已截断 execution_result 派生、不改写逻辑。
- 涉及文件：`observation_step.py`、`action_handler.py:779`、`storage.py`
- 依赖：第一部分 4.1.3 单向依赖
- 验证：排查场景可回看原文 observation

#### 8.2 实施顺序与风险

- 顺序：加保真字段作最后一步、可推迟；`一删、一治本、一收敛、一清` 属行为等价的收敛重构，先行落地，独立安全可单独提交。
- 风险：8.1.1 删四字段后，任何仍从 ObservationStep.read 取 llm_data/平行结果的代码会取到空——须全仓搜引用（`grep` obsStep.` / `llm_data` / ObservationStep）确保前端与回放已迁移到 execution_result 后再删。
- 回滚：8.1.1~8.1.3 独立可回退；8.1.5 增字段随删四字段单向回滚。

---

本次仅设计定案，代码改动待北京老陈批准后另立实施任务。
