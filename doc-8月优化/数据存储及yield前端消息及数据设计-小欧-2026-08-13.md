# 数据存储及 yield 前端消息及数据设计

**版本**: v2.0
**创建时间**: 2026-08-13 17:39:38
**更新时间**: 2026-08-13 18:07:57
**创建人**: 小欧
**更新人**: 小欧
**状态**: 方案定案（北京老陈定案：仅设计文档，代码落地另立任务）

## 版本历史

| 版本 | 时间 | 更新人 | 更新内容 |
|------|------|--------|---------|
| v1.4 | 2026-08-13 17:5x | 小欧 | 严格两部分 |
| v1.5 | 2026-08-13 17:51:58 | 小欧 | 每部分「现状→问题→更好的方案」三段式 |
| **v2.0** | **2026-08-13 18:07:57** | **小欧** | **全面重写为详尽版：逐行核对源码（storage.py/stream.py/agent_runner.py/action_handler.py/message_builder.py/steps/observation_formatter.py/database.py/migrate_steps.py/stream_orchestrator.py/task_state.py/sse_formatter.py/前端 sse.ts/StepContent.tsx/ExecutionPanel.tsx），补齐数据流全链路、生产消费模型、各 Step 字段契约、落库四函数、截断机制、迁移、前端渲染取数实况；每部分按「现状→问题→更好的方案」三段式展开，方案保持 v1.5 定案不变** |

---

## 第一部分　yield 前端消息数据

### 一、现状

#### 1.1 全链路数据流（一次任务事件如何到达前端）

```
UniversalAgent.run_react_cycle() 产出 Step 对象
        │  event.to_dict() → step_dict（agent_runner.py:164-171）
        ▼
step_dict（同一份对象，双路分发）
        ├─ ① 落库路：append_execution_step → chat_message_steps.step_json（见第二部分）
        └─ ② yield 路：_append(event_dict)（agent_runner.py:102-113）
                │  d["seq"] = len(buffer.event_log)
                │  buffer.event_log.append(d) + cond.notify_all()
                ▼
          stream_reader(buffer, task_id, after_seq)（stream.py:119-144）
                │  按 seq 偏移读取 → format_agent_sse(step_dict)
                ▼
          SSE 字符串："data: {json}\n\n"（sse_formatter.py:35-59）
                │
                ▼
          前端 fetch/SSE 解析（frontend/src/services/sse.ts processSSEData）
```

关键结论：**SSE 消息 == step_dict 本身**（format_agent_sse 只加 type/step/timestamp 头，其余字段原样透传），yield 与落库是同一份 step_dict，两路同构。因此「前端能看到的字段」==「落库能还原的字段」==「to_dict() 序列化的字段」。前端看不到的字段，历史回放也还原不了。

#### 1.2 生产者/消费者解耦模型（task_state.py:24-55）

| 组件 | 位置 | 职责 |
|------|------|------|
| StreamBuffer | task_state.py:24-34 | event_log(append-only 含 seq) / cond / done 三件套 |
| create_stream_buffer | task_state.py:41-45 | 注册 task_id→buffer |
| agent_streams | task_state.py:38 | 全量流态缓冲表（task_id→StreamBuffer） |
| _append | agent_runner.py:102-113 | 生产者写 event_log + notify_all |
| stream_reader | stream.py:119-144 | 纯消费者，按 seq 转发，重连传 after_seq 续传 |
| reclaim_stream_buffer | task_state.py:53-55 | 任务结束后延迟 300ms 回收（agent_runner.py:350-352） |

- 断线重连：`chat_stream_reconnect_orchestrator(task_id, session_id, after_seq)`（stream_orchestrator.py:242-254）复用同一 buffer 按 seq 续传，不重复事件。
- 防挂死：`cond.wait()` 加 60s 超时（asyncio.wait_for），超时后重检 `buffer.done`（stream.py:136-144，2026-07-30 后端卡死根因修复）。
- 防漏事件：排空后复查 `len(buffer.event_log)`（stream.py:131-133，#30 fix，防 done 前追加丢事件）。

#### 1.3 SSE 事件格式（sse_formatter.py:35-59）

```
data: {"type":"<type>","step":<N>,"timestamp":"<本地ISO>", ...step_dict其余字段}\n\n
```

- 纯字符串拼接，`json.dumps(ensure_ascii=False)`；循环引用由 `_break_circular` 兜底（sse_formatter.py:20-32）。
- timestamp 全程本地时区 ISO 无 Z（2026-08-08 全程统一本地时区）。

#### 1.4 各 Step 字段契约（steps/ 目录，to_dict 输出 == SSE == step_json）

`ReasoningStep.to_dict()`（base.py:70-78）统一输出 `{type, step, timestamp, content}` 四键，再叠加各 Step 的 `_extra_fields()`。`content` 由 `get_content()` 产出。

| Step | type | content 来源 | 顶层字段（_extra_fields 叠加） |
|------|------|--------------|------------------------------|
| ThoughtStep | thought | content/thought | thought、reasoning、tool_name、tool_params |
| ActionStep | action_tool | 空串（get_content 返回 ""，action_step.py:40-41） | tool_name、tool_params、execution_status、**execution_result**(完整 {data, llm_data, other_data} 3 字段)、action_retry_count、execution_time_ms |
| ObservationStep | observation | llm_data 各条 summary 拼接（observation_step.py:44-48） | content(summary)、**llm_data**(列表)、**tool_result**、**other_data**、**parallel_results** |
| FinalStep | final | response | response、thought、outcome(completed/failed/cancelled)、error_type、error_message、model、provider、display_name、accumulated_usage |
| ErrorStep | error | error_message | error_type、error_message |
| ChunkStep | chunk | 流式回复片段 | content |
| MetaStep | start/cancelled/paused/resumed/retrying/authorization_required | 统一 content 文本（base.py:126 字段契约） | 透传 kwargs（confirm_id/tool_name/params/safety_level/wait_time/data 等） |

- 终态统一由 `final` 承载：FinalStep 多态（outcome=completed/failed/cancelled），前端按 outcome 分流（final_step.py:24-105，2026-07-18 重构）。
- 步骤计数：`create_step_counter()`（base.py:84-93）闭包 `next_step()` 全局唯一递增。
- 各 Step 在同一抽象层：Step 对象在 react_cycle 内 `emit` → `_step_emitter.emit(step)`，最终回到 agent_runner 的 `async for event` 循环。

#### 1.5 react_cycle 各场景产出的 Step 类型（前端看到的剧本）

| 场景 | 产出序列 |
|------|---------|
| 正常调工具（单/并行） | MetaStep(start) → ThoughtStep → ActionStep(每工具一条) → ObservationStep → … → FinalStep(completed) |
| 并行工具 | 1 条 assistant 带 N 个 tool_calls + N 条 ActionStep + 1 条 ObservationStep（action_handler.py:657-662 配对规则） |
| 空响应/未调工具（B3） | MetaStep(start) → MetaStep(retrying) → 循环 → FinalStep |
| 同工具/同参数死循环 | ThoughtStep + 纠偏 warning（_temp_same_tool_warn 临时标记，不入库）→ 循环 → FinalStep |
| 截断 | MetaStep(truncated)（上下文超限） |
| 失败 | MetaStep(start) → … → FinalStep(outcome="failed", error_type, error_message) |
| 取消 | MetaStep(cancelled) + FinalStep(outcome="cancelled")（agent_runner.py:205-216 取消分支 + finally 守卫补发） |
| 暂停 | MetaStep(paused) / MetaStep(resumed) |

#### 1.6 前端消费实况

- **工具结果渲染**：ToolResultRenderer 的 30+ 个 Renderer（frontend/src/components/Chat/ToolResultRenderer/types/*.tsx）**全部从 `step.execution_result` 取 `llm_data`**：
  ```ts
  const execResult = (step.execution_result || {}) as Record<string, unknown>;
  const llmData = (execResult.llm_data || {}) as Record<string, unknown>;
  ```
  成功判定统一以 `llm_data.status.exec_code === "success"` 为准（各 Renderer 文件头注释一致）。
- **observation 步骤展示**：StepContent.tsx:140-291 observation 分支读 `obsStep.observation`（object 时读其 llm_data / 字符串时直接展示）；ExecutionPanel.tsx:387-391 case 'observation'。
- **依赖方向**：前端随后端修改，契约以后端 step_dict 为准，前端不质疑后端字段（AGENTS.md 1.5 前后端规范）。

### 二、问题

**核心问题：喂给 LLM 的真实 observation 文本不进 SSE、不入库。**

1. **真身不入库/不 yield**：`build_observation_text`（observation_formatter.py:1466）的产物 `obs_text`（action_handler.py:719/723/751）与最终 `merged_obs`（action_handler.py:769，`"\n\n".join(obs_parts)`）是**喂给 LLM 的确切 observation 文本**（经 message_builder.add_tool_result 进 conversation_history，message_builder.py:124-144、action_handler.py:756-758）。但 ObservationStep.to_dict() **不序列化该文本**——它不进 SSE、不入库。

2. **落库的只是 summary 拼接**：ObservationStep.get_content() 仅从 llm_data 各条取 `summary` 字段拼接（observation_step.py:44-48）。summary 是各 tool 自己写的短摘要，**不是**喂 LLM 的完整 observation 文本。

3. **历史回放失真**：多轮上下文加载 `_load_previous_messages`（stream.py:87-116）→ `_parse_observations`（stream.py:67-84）只读 `step.get("content")`（= summary），用它重建 `role:tool` 消息。结果：**历史回放中 LLM 看到的 observation 是截断后的 summary，而非原始完整 observation**——功能退化（多轮对话上下文质量下降、复杂任务需回读全文时信息丢失）。

4. **排查困难**：日志/DB 中无 observation 原文，排查"LLM 为什么这么回答"需重新跑 observation_formatter 格式化逻辑才能复原（action_handler.py:723 处仅日志记录 raw_data，无格式化后文本）。

**次要问题（设计层面）**：
5. ObservationStep 同时携带 content(summary) 与 llm_data/tool_result，两处信息冗余且口径不一致（summary 截断 vs 完整 data），字段职责不清。

### 三、更好的方案（增字段，一增全通）

ObservationStep 增 `observation_text` 字段，数据源为 `merged_obs`（action_handler.py:769）。

| 维度 | 方案 | 说明 |
|------|------|------|
| 字段 | ObservationStep 增 `observation_text`（喂给 LLM 的最终文本） | observation_step.py 增加构造参数 + `_extra_fields()` 输出 + `get_content()` 优先返回它 |
| 数据源 | action_handler.py:769 `merged_obs` | 单/并行统一；异常分支 obs_text（action_handler.py:719）与 repair_warning 前缀（action_handler.py:749-753）天然已含在 merged_obs 中，一次赋值全覆盖 |
| 落库 | 随 step_json 持久化（append_execution_step 原链路） | 历史回放可完整还原真实 observation |
| yield | SSE 同步携带（to_dict 自动透传，format_agent_sse 无感） | 前端无需改传输层 |
| 前端 | 随后端改：observation 步骤只展示 observation_text/summary 总结文本 | 完整结构化结果仍从 action_tool.execution_result.llm_data 读，**不重复取数** |
| 多轮回放 | `_parse_observations`（stream.py:67-84）改为**优先读 observation_text，回退 content** | 多轮对话上下文质量与首次对话一致（功能增强，禁止 backward） |
| 截断 | observation_text 不参与存储层截断（第二部分第 3.3 节） | 它是喂 LLM 的完整文本，需原样入库 |

**理由**：一增全通——只加一个字段，SSE、落库、历史回放、前端展示四处自然受益，零破坏性。不新增冗余字段（is_success/summary 已有权威源 llm_data.status.exec_code）。

---

## 第二部分　存储数据

### 一、现状

#### 1.1 三库总览

| 库 | 路径 | 职责 |
|----|------|------|
| chat_history.db | ~/.omniagent/ | 会话/消息/步骤（本设计聚焦） |
| operations.db | ~/.omniagent/ | 文件操作（备份/回滚/查询） |
| task_tracker.db | ~/.omniagent/ | Task 追踪 |

#### 1.2 chat_history.db 表结构（db_initializer.py）

| 表 | 关键字段 | 说明 |
|----|---------|------|
| chat_sessions | id/title/created_at/updated_at/message_count/is_deleted | 会话主表 |
| chat_messages | id/session_id/role/content/timestamp/status/thought（execution_steps 列仅旧数据读取兼容） | 消息表：一行=一条消息；steps 已拆到独立表 |
| chat_message_steps | id/message_id/session_id/step_index/step_json/created_at | **步骤主存储**：一行=一步 |
| chat_session_title_history | session_id/title/created_at | 标题历史 |

#### 1.3 数据库访问机制（database.py）

| 机制 | 说明 |
|------|------|
| get_conn 统一入口 | 全库走统一连接入口，杜绝 SQLite 越界 |
| _ParamSafeConnection | 参数化查询闸门，防 SQL 注入 |
| WAL | 全库 WAL 模式，读写并发 |
| busy_timeout + 指数退避重试 | get_conn_with_retry（0.5/1/2s，database.py），5 处热循环连接改用它（agent_runner.py 2026-07-23 #14 修复） |

#### 1.4 落库链路（storage.py，四函数）

| 函数 | 位置 | 职责 |
|------|------|------|
| allocate_and_insert_message | storage.py:232-249 | 预分配 assistant 消息 ID + 插入空白行 + message_count+1；local_time 已提至 if 外赋值（2026-08-13 #1 修复 NameError） |
| append_execution_step | storage.py:325-334 | **每步独立事务**写 chat_message_steps（截断 → safe_json_dumps → INSERT） |
| finalize_message | storage.py:353-358 | finally 轻量终态：UPDATE content/status/thought |
| load_execution_steps | storage.py:337-350 | 读侧从新表组装，无数据回退 chat_messages.execution_steps 旧列（兼容） |

调用关系（agent_runner.py）：
- 每产出一步：`append_execution_step`（agent_runner.py:179-188，ai_message_id 首次经 allocate_and_insert_message 分配）
- 异常分支：final 步也立即落库（agent_runner.py:231-234）
- finally 守卫补发 FinalStep（agent_runner.py:246-276）→ `finalize_message`（agent_runner.py:305-306）
- ai_message_id 未分配兜底：`save_steps` 整批写（agent_runner.py:307-310）
- IntegrityError 不重试（UNIQUE，YAGNI）（agent_runner.py:312-314）

依赖方向（2026-08-13 P4 重构）：agent_runner 不再直接 import chat 模块，6 个持久化回调由 stream_orchestrator 以 `db_ops` SimpleNamespace 注入（stream_orchestrator.py:171-183），依赖方向 chat→agent 单向。

#### 1.5 渐进耐久设计

- 每步独立事务 → 崩溃时已产出步骤不丢；finally 只做轻量终态更新。
- 启动迁移：migrate_steps.py 一次性把旧 execution_steps 列数据迁移到 chat_message_steps（schema_migrations 守卫），启动从 25s→<1s。

#### 1.6 存储层截断机制（storage.py:252-322）

| 项 | 值 | 说明 |
|----|-----|------|
| MAX_TOOL_RESULT_ITEMS | 1000 | 列表超 1000 条截断（storage.py:255） |
| MAX_TOOL_RESULT_STR_LEN | 100000 | 单字符串超 10 万字符截断并附加「原长 N 字符」标记（storage.py:256, 307-322） |
| _truncate_tool_result | storage.py:258-273 | 递归截断过大的列表（2026-08-13 #9 修复 list 内嵌超长 list） |
| _truncate_tool_result_strings | storage.py:307-322 | 递归截断所有超长字符串（base64/长文本） |
| _truncate_step_dict | storage.py:276-298 | 作用对象：`tool_result` / `execution_result` / `parallel_results[].tool_result / execution_result`（**不碰 observation**） |
| storage.py:254 标注 | 「实验性的功能: TODO 做正式的持久化设计后进行更新」 | **本设计文档即该 TODO 的正式落点** |

> 阈值依据（2026-07-23 北京老陈驱动）：各 tool 已自行截断输出，storage 仅做安全兜底防 SQLite 行溢出（约 1MB TEXT 上限），不做激进取舍。

#### 1.7 现状存储形态：同一工具结果两处存放（问题根源）

**ActionStep**（action_handler.py:646-652）：
```python
ActionStep(step, tool_name, tool_params, execution_result=result, execution_status=_ec)
```
存**完整** `execution_result` = {data, llm_data, other_data} 3 字段。

**ObservationStep**（action_handler.py:807-813）：
```python
ObservationStep(step, llm_data=llm_data_list, tool_result=…, other_data=merged_other, parallel_results=_parallel_results or None)
```
又存一遍 `llm_data`(列表) + `tool_result` + `other_data` + `parallel_results`。

提取过程（action_handler.py:771-790）：从每个 result 重新拆 `_all_llm_data/_all_tool_results/_all_other_data/_parallel_results`，同一份数据第二份副本。

### 二、问题

1. **双重存储（核心问题）**：同一工具结果存两份——ActionStep.execution_result（完整 3 字段，action_handler.py:650）与 ObservationStep.tool_result/llm_data/other_data/parallel_results（action_handler.py:776-790、807-813）。后果：
   - **库膨胀**：大工具结果（文件列表、搜索全文）双份持久化，chat_message_steps 体积翻倍；
   - **写 I/O 翻倍**：每步截断 + dumps + INSERT 处理两份数据；
   - **字段漂移风险**：两处口径一旦不同步（如某 tool 改返回结构），前端读 execution_result、回放读 observation，出现不一致；
   - **截断双重处理**：_truncate_step_dict 需同时兜底 execution_result 与 observation 的 tool_result/parallel_results（storage.py:282-297），复杂度与漏截风险高。

2. **截断机制未定案**：storage.py:254 临时 TODO「实验性的功能」未正式化；截断范围（哪些字段）与阈值（1000/100000）无正式设计依据背书。

### 三、更好的方案（一删一收敛）

| 动作 | 内容 | 理由 |
|------|------|------|
| 删 | ObservationStep 删 `tool_result`/`llm_data`/`other_data`/`parallel_results` | 单一权威副本=ActionStep.execution_result；前端渲染本就全从 execution_result 取（1.6 节），删除无退化 |
| 定 | 截断对象收敛为**仅 ActionStep.execution_result** | observation 已删 tool_result，截断只需兜底 execution_result 一处 |
| 定 | `observation_text`（第一部分新增）不参与存储层截断 | 已行×列收口，且是喂 LLM 的完整文本，需原样入库 |
| 清 | 删 storage.py:254 临时 TODO，引用本设计文档 | 消除「实验性」残留，正式定案 |
| 保 | ObservationStep 保留 content（summary）用于前端展示 | 前端 observation 步骤展示总结文本（1.6 节），不重复取数 |

**收敛后存储形态**：
- ActionStep.execution_result：完整权威副本（截断兜底对象）；
- ObservationStep.observation_text：喂 LLM 的完整 observation 文本（不截断）；
- ObservationStep.content：summary 展示文本；
- 其余（tool_result/llm_data/other_data/parallel_results）不再入 ObservationStep，随 execution_result 单一存放。

**不做**：
- 不新增 is_success/summary 冗余字段（llm_data.status.exec_code 已是权威源）；
- 不新建独立 observation 表（step_json 已结构化，无需第三张表）；
- 不拆多条步骤（保持一行=一步的既有契约）。

**影响面核对（无退化证据）**：
- 前端 30+ Renderer 全从 execution_result.llm_data 取数 → 删 observation 内冗余字段不影响；
- StepContent.tsx observation 分支：随前端适配改为读 observation_text/content，不再读 obsStep.llm_data；
- _parse_observations：改优先读 observation_text 回退 content，回放质量增强；
- _truncate_step_dict：删 observation 相关分支（tool_result/parallel_results），仅保留 execution_result；
- react_cycle 终态语义、derive_status_from_steps（storage.py:71-83）、load_execution_steps 均不受影响。

---

## 结论

| 主题 | 现状 | 问题 | 方案 |
|------|------|------|------|
| 第一部分　yield 前端消息 | SSE==step_dict 双路同构，生产/消费解耦，断线重连已通 | 喂 LLM 的真实 observation 文本（merged_obs）不进 SSE、不入库；历史回放只还原 summary | ObservationStep 增 observation_text（数据源 action_handler.py:769 merged_obs）；_parse_observations 优先读它回退 content |
| 第二部分　存储数据 | 三库三表已齐，渐进耐久落库 + 截断兜底 | 工具结果双重存储（ActionStep.execution_result vs ObservationStep 四字段）；截断临时 TODO 未定案 | ObservationStep 删四字段收敛单一权威；截断收敛为仅 execution_result；observation_text 不截断；删 TODO 正式定案 |

**本次仅设计定案**，代码改动待北京老陈批准后另立实施任务。
