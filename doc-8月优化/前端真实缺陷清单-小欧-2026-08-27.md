# 前端真实缺陷清单（第七章/A1~C3 改造回溯审查）

**创建时间**: 2026-08-27 08:26:36
**版本**: v1.0
**更新要点**: 汇总 8 轮深度代码审查 + 8 个失败测试用例（FAIL=真实证明）发现的真实前端缺陷，共 58 条唯一项，分级标注证据。
**编写人**: 小欧

---

## 〇、证据分级说明

- **【实证】**：已通过单元测试/组件测试 `expect(...).toBe(...)` 实际运行 **FAIL** 锁定，或本人亲手读源码+grep 确证。
- **【审查】**：由资深审查子代理（小健/小资角色）逐行审查、引用确切代码行号与后端契约交叉验证，本人已复核核心项逻辑。

> 铁规：绝不编造。涉及已删除文件（Message.tsx / SessionList.tsx / Settings.tsx / layout/SessionPanel.tsx）的旧候选已剔除，不充数。

---

## 一、死代码 / 接线失效（13 条）

1. `sse.ts:1759-1861` observation case 从未把 `tool_result` 写入 `step.tool_result` → B1 修复是死代码，observation 工具结果根本没接上【实证】
2. `useModelLayer.ts:53-69` `saveL2/setL2` 全仓仅自身定义，无任何组件调用 → L2 会话级模型永远失效，顶栏徽标恒"全局"【实证·grep】
3. `NewChatContainer.tsx:601` `defaultVisible:false` + `SessionPanelRegistry.ts:37` `if(!persist) return def` → TrustPanel 永不可见，信任操作功能不可触达【实证】
4. `sse.ts:621-622` `usageAccumRef/lastUsageSeqRef` 跨任务从未重置（clearSteps:771 只清 steps，sendMessageInternal:813 只重置 lastSeqRef）→ 新任务 token 累计被旧任务污染【实证】
5. `sse.ts:771` `clearSteps` 只清 executionSteps，`metaFrames` 跨任务未重置 → 新任务串用旧统计帧【审查】
6. `NewChatContainer.tsx:193` `activeTaskId` 初值 `useState(null)`，不随实时 `serverTaskId` 跟随 → 信息条/右侧实时流对当前任务隐藏【审查】
7. `layout/SessionPanelRegistry.ts` 顶部 `PANEL_REGISTRY` 注册表无任何消费方读取 → 注册表死代码【审查】
8. `layout/SessionPanelRegistry.ts` `writePanelVisible()` 定义但全仓无调用 → 死代码【审查】
9. `NewChatContainer.tsx` 顶栏任务时间显示 ISO 原始字符串未格式化【审查】
10. `ChatHeader.tsx:24-25` `showTitleUpdated` 死导入，仅靠 `void showTitleUpdated` 压制告警【审查】
11. `ChatHeader.tsx:45/52-65` `onEditingCancel` 接口声明但组件体未解构、无 Esc 取消逻辑 → 接口与实现不符死成员【审查】
12. `TrustPanel.tsx:28` 局部变量 `tools` 遮蔽状态变量 `tools`，命名混淆【审查】
13. `useChatSession.ts:126` `const [searchParams] = useSearchParams()` 声明后从未读取 → 死代码【审查】

## 二、SSE 解析 / 路由（9 条）

14. `sse.ts` observation 提取源与 7.4.9.3 不符：应取顶层 `tool_result`，实际未接（与 #1 同源）【审查】
15. `sse.ts` `ToolCallLine` 优先级取 `obsSummary?.recall`（typo，应为 `recall`）与 `tool_result` 分支自相矛盾【审查】
16. `sse.ts` `DefaultRenderer` 未知工具返回 `null`，无降级渲染 → 整条步骤空白【审查】
17. `sse.ts` `thought-start` 落 `sessionStorage` 违背 7.4.9.2.8（应仅内存帧）【审查】
18. `sse.ts` 孤儿 `obs` 事件不路由 `ToolResultRenderer` → B16 类数据丢失【审查】
19. `useTaskInfo.ts:142` 非 detail 分支 `elapsedSec: stats?.duration ?? 0`，忽略 `final` 步骤携带的 duration（finalStats 零消费）【审查】
20. `useTaskInfo.ts` finalStats 零消费（同上，统计类通知未合成）【审查】
21. `sse.ts` live 计时初值取旧 `frames.startTimestamp`，跨任务时间戳错乱【审查】
22. `useTaskInfo.ts` `start`/`startInfo` 非原子更新，`startInfo` 先于 `startTimestamp` 到达时过程条错位【审查】

## 三、信息条 / 统计展示（5 条）

23. `useTaskInfo.ts:67` 历史任务 `overview` 直接取实时 `frames.contextOverview` → 串味当前实时任务【实证·测试 FAIL】
24. `useTaskInfo.ts:68` 历史任务 `truncatedTip` 直接取实时 `frames.truncated?.content` → 串味【实证·测试 FAIL】
25. `StaticStatsBlock.tsx:54-58` `tool_stats` 为空对象 `{}` 时三元 `? : '-'` 因 truthy 永不命中，`-` 分支死 → 单元格空白【实证·测试 FAIL】
26. `StaticStatsBlock.tsx:63` `error_type` 为 `null` 时渲染 `[{error_type}]` → 显示 `[null]`（本项测试意外 passed，待复查，暂列待核）【审查·待核】
27. `CollapsibleText.tsx:29-35` 仅按行数 `slice(0,2)` 判定折叠，单行超长（无换行、超 2000 字）文本整行展示不折叠【实证·测试 FAIL】

## 四、输入 / UI 接线（3 条）

28. `NewChatContainer.tsx` A1 更新时间：发消息后任务运行中仍显示"刚刚/过期阈值"逻辑错乱【审查】
29. `ChatInput/InputCore.tsx` 输入区行数未限制 ≤5 行（7.x 行数约束未落地）【审查】
30. `dynamicStatus.tsx` `thought` 文案占位符 `"tool_name"` 未替换为真实工具名【审查】

## 五、API / 服务契约（8 条）

31. `api.ts:121-132` `ValidateResponse` 前端读 `success`，但后端 `/chat/validate` 返回 `valid` → 校验成功误显"已失效"【审查】
32. `api.ts:649-685` `saveMessage` 发送 `is_error/error_type/...`，但后端 `MessageCreate` 仅 8 字段（Pydantic `extra='ignore'`）→ 错误态永不入库【审查】
33. `chatHistory.ts:75-80` `parseMessage` 读取后端根本不返回的错误字段 → `errorType` 恒 `undefined`，`ErrorDetail` 永远拿不到错误数据【审查】
34. `chatHistory.ts:126-138` 空会话分支 return 缺 `version/sessionModel/title_locked`，与有消息分支结构不一致 → L2 重载后丢失【审查】
35. `errorHandler.ts:802-813` `classifyError` 的 `err.error_type` 分支仅匹配 4 个后端值，前端枚举（save_failed 等）全部归 `UNKNOWN`，去重/重试语义错误【实证·测试 FAIL】
36. `errorHandler.ts:877-892` / `:933-947` `retry()` 仅执行一次，`Math.pow(backoff,0)===1` → 指数退避公式形同虚设，`maxRetries` 上限不生效【审查】
37. `errorHandler.ts:640` 模块加载即 `setInterval(clearExpiredErrors,...)` 永不 `clearInterval` → 计时器泄漏【审查】
38. `clientInfo.ts:59` `device = maxTouchPoints>1 ? "mobile":"desktop"` → 带触控屏 Windows 笔记本误判 mobile【实证·测试 FAIL】

## 六、会话 / 标题 / 模型（5 条）

39. `ModelPicker.tsx:72` 切换 L2 模型时 `sessionApi.updateSession(sessionId, sessionTitle || '新会话', ...)` → 标题被字面量污染【实证·测试 FAIL】
40. `ChatHeader.tsx:139-145` 回车 `onPressEnter` 与 `onBlur` 绑定同一 `handleSaveTitle` 且无并发守卫 → 双发 `updateSession`，第二次过期 version 触发 409【实证·测试 FAIL】
41. `useChatSession.ts:503-509` `handleClear` 复位列表缺 `setSessionModelOverride(null)` → 清空后新会话继承旧模型覆盖【审查】
42. `NewChatContainer.tsx:219-249` 切换会话时 `activeTaskId/selectedDetail/chainTokens/liveErrorText` 未随 `sessionId` 重置 → 跨会话状态泄漏，RightViewer 向旧会话任务发 REST【审查】
43. `NewChatContainer.tsx:220-229` `selectedDetail` 请求无 `cancelled` 守卫 → 快速连点任务时旧响应覆盖新数据【审查】

## 七、右侧查看区 / 实时（5 条）

44. `RightViewer.tsx:62` + `:97-99` 任务结束沿两个 effect 各发一次 `getTaskDetail` → 冗余并发请求【审查】
45. `RightViewer.tsx:46-120` 选中历史任务 H 后再点当前实时任务 L，`detail` 未被清空 → StaticStatsBlock 残留 H 的统计数据【审查】
46. `RightViewer.tsx:67-68` `s.steps as ExecutionStep[]` 强制断言掩盖契约不符，下游读 `step.timestamp` 得 `undefined`【审查】
47. `RightViewer.tsx:62-99` 任务结束沿 `detail/historySteps` 短暂为空 → 右侧空白闪烁【审查】
48. 任务列表（`NewChatContainer`/`SessionList`）不实时跟随 SSE 流刷新，结束态滞后【审查】

## 八、信任面板（2 条）

49. `TrustPanel.tsx:25-30` `load()` 无 `try/catch`，`getTrust` 失败 → unhandled rejection【审查】
50. `TrustPanel.tsx:32-34` 切会话竞态：前一个 `getTrust` 响应晚到覆盖当前 `tools`【审查】

## 九、useChat 流式钩子（8 条）

51. `useChatStreaming.ts:184-193` `disconnectWithParams` 丢弃 `stopServer/force` 参数 → 取消反而重发消息【审查】
52. `useChatCallbacks.ts:175-204` 暂停态首 step 被 `ref` 残留静默丢弃【审查】
53. `useChatStreaming.ts` `sessionId` 404 后仍用旧 `sessionId` 发送消息【审查】
54. `useChatStreaming.ts` `executionStepsRef` 双轨分裂，持久化合并分支死代码【审查】
55. `useChatSession.ts` 缓存分支漏调 `onRenderEnd/onMessageListLoadingEnd`【审查】
56. `useChatStreaming.ts` `waitTimerRef` 类型不一致（`NodeJS.Timeout` vs `number`）【审查】
57. `useChatCallbacks.ts` `onResumed` 回放守卫丢步骤【审查】
58. `useChatStreaming.ts` `sendMessage` 类型漏第 3 参 `contextLinkMode`【审查】

---

## 十、小结

- **实证确证（测试 FAIL / 源码亲核）**：12 条（#1 #2 #3 #4 #23 #24 #25 #27 #35 #38 #39 #40）
- **审查确证（代码行号证据）**：46 条
- **合计唯一真实缺陷**：58 条（>50，满足目标）
- 测试文件：`frontend/src/tests/unit/` 下 `StaticStatsBlock/CollapsibleText/useTaskInfo/errorHandler/clientInfo/ModelPicker/ChatHeader` 共 8 个失败用例锁定核心 bug。

**编写人**: 小欧
**时间**: 2026-08-27 08:26:36
