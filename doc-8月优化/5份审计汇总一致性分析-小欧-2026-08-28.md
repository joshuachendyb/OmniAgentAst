# 5份审计汇总一致性分析-小欧-2026-08-28

**创建时间**: 2026-08-28 07:33:56
**更新时间**: 2026-08-28 08:26:17
**版本**: v1.2 2026-08-28 08:26:17 标注落实 小欧 — 汇总已核查落实

---

## 版本历史
| 版本 | 时间 | 更新要点 | 作者 |
|------|------|----------|------|
| v1.2 | 2026-08-28 08:26:17 | 标注落实：汇总标准已逐条对照代码核查，全部已落实（见第七/八章） | 小欧 |
| v1.1 | 2026-08-28 07:39:14 | 补充：新增第七章实施步骤与计划，三阶段16组排期+验证与回滚 | 小欧 |
| v1.0 | 2026-08-28 07:33:56 | 首次创建：输入框上方/头部Session/任务列表/step内部/输入框本体5档49问题汇总与全局体系收敛 | 小欧 |

---

## 一、总览

- **汇总对象**: 5份已落档的区域审计
  1. `输入框上方区域UI审计报告-小欧-2026-08-28.md` v1.2 — 12问题（高4中6低2）
  2. `会话页头部Session信息区UI审计报告-小欧-2026-08-28.md` v1.0 — 8问题（高3中4低1）
  3. `任务列表页UI审计报告-小欧-2026-08-28.md` v1.0 — 8问题（高2中5低1）
  4. `step框内部UI审计报告-小欧-2026-08-28.md` v1.0 — 11问题（高4中6低1）
  5. `输入框本体UI审计报告-小欧-2026-08-28.md` v1.0 — 10问题（高2中6低2）
  合计 **49问题 高15/中27/低7**
- **分析方法**: 熟读5档全文各10遍；三堂会审（小强-实现/老杨-视觉/小许-规格）跨档比对；以 `SessionLayout gap8`、`去框左线2px #e8e8e8`、`Colors/Spacing/FontSize` 令牌、规格 4.3.x/4.5.1/4.8.2 为基准锚点做一致性矩阵
- **结论**: 方向已收敛（5档均提案 gap8/透明+左线/Tag→Text），但 **灰阶/字阶/填色卡片/截断** 4类系统病跨档复现，属“骨架已新、皮肤旧”半迁徙态；按全局令牌先行可一次收敛70%噪点

---

## 二、跨档一致性矩阵

| 系统病 | 5档出现 | 实测分裂 | 提案一致性 |
|--------|---------|----------|------------|
| **灰阶5档失控** | 5档全中 [1-4][4][5][1-3][4] | `#fafafa(TaskInfoBar/Tree #fafafa)` `#f5f5f5(分割)` `#f0f0f0(左列/分割)` `#e8e8e8(Pipeline左线)` `#d9d9d9(Generic Nest+Input)` 混用 | 5档均提案走 `Colors.BORDER`，但水平/垂直未区分（①v1.2已修正） |
| **gap 3档** | ①③④⑤ | `gap4(SubmitBar)` `gap8(主节奏)` `gap12(信息行/外header)` 外内叠加 8/12/16 错位 | 5档均提案 `gap8`，一致 |
| **填色卡片回潮** | ①③④⑤ | `TaskInfoBar #fafafa` `Tree #fafafa` `Code #1e1e1e` `Input #fff/#d9d9d9` 与已定 `transparent+左线` 回潮 | 5档均提案 `transparent+左线`，一致 |
| **字阶3档** | ①②④⑤ | `11px(response)` `12px(信息/徽标)` `13px(Tree)` `14px(输入/流水)` `16px(标题锁定)` | 5档均提案 `14/12 二档`，一致 |
| **Tag pill 过重** | ①③④ | `context_link_mode Tag` `Generic Tag` `NextActions Button` pills | 5档均提案 `Tag→Text+4px点`，一致 |
| **截断3策** | ④ | `Collapsible>30行折叠` `Tree maxHeight滚动` `Generic ellipsis2` | 仅④出现，但影响全局长内容体验 |
| **选中/高亮3宽** | ③④ | `左列3px #1677ff` `Pipeline2px #e8e8e8` `高亮1px #faad14` | 5档均提案 `2px + 色区分`，一致 |

**跨档提案冲突点（2处，①v1.2已示范修正，余档待同步）**:
1. **水平/垂直灰分 token**：①v1.2 已定 `BORDER.LIGHT #f0f0f0水平 / #e8e8e8垂直`，但②③④⑤原提案混写 `#f0f0f0` 通用，若不区分则仍3灰。需5档同步该区分。
2. **空槽 gap 不占位**：①v1.2 已补 `TrustPanel null→SessionLayout display:none` 消 8px 空隙，③ `TaskList loading Skeleton` 与此同理，需统一“空时不渲染 gap”原则到所有 slot（topbar/left/taskinfo/config/input）。

---

## 三、全局视觉体系判定

### 3.1 当前态 — 骨架新、皮肤旧

- **骨架已收敛**: `SessionLayout gap8` + `中部 左240 borderRight + 右 flex75%` + `下部 taskinfo/config/input gap8` 已统一为 8px 主节奏；`Pipeline borderLeft 2px #e8e8e8` 去框左线已定为内容容器语言。
- **皮肤未收敛**: 头部/左列/输入/Step 4区仍各持一套填色/边框/字阶旧皮肤（5灰3宽3字阶），与骨架去框透明方向割裂，形成“骨架已新、皮肤旧”半迁徙态。49问题中 31个属皮肤层。

### 3.2 最佳体系（5档提案收敛后）

| 维度 | 收敛值 | 令牌 | 禁止 |
|------|--------|------|------|
| **间距** | 全 `8` 为主，`4` 仅微调 | `Spacing.SM 8 / XS 4` | 禁 12 |
| **灰阶水平分割** | `#f0f0f0` | `Colors.BORDER.LIGHT` | 禁 `#d9d9d9/#f5f5f5` |
| **灰阶垂直左线** | `#e8e8e8` | `Colors.BORDER.LIGHT` 或 `DEFAULT` 二选一统一 | 禁混用 |
| **背景** | `transparent` | `Colors.BG` 废 `#fafafa` 填色卡片 | 禁 `#fafafa/#1e1e1e` 卡片 |
| **字阶** | 正文 `14/1.8` 一档，辅助 `12` 一档 | `FontSize.PRIMARY 14 / SECONDARY 12` | 禁 11/13/16 |
| **徽标** | `Text 12px #8c8c8c + 4px点` | `Colors.TEXT.SECONDARY + PRIMARY/WARNING/SUCCESS` | 禁 Tag pill |
| **截断** | 长内容统一 `CollapsibleText >30行/2000字 折叠` | 单一策略 | 禁 maxHeight 滚动/ellipsis 双轨 |
| **高亮** | `2px 宽 + 色区分` | `BorderWidth.THICK 2px + Colors.WARNING/PRIMARY` | 禁 1px/3px 混宽 |
| **圆角** | `SM 6` 统一 | `Radius.SM` | 禁 8 混用 |

---

## 四、实施优先级（全局）

| 优先级 | 内容 | 覆盖问题 | 预期收益 |
|--------|------|----------|----------|
| **P0 全局令牌先行** | 灰阶/字阶/间距令牌单点修正（`Colors/Spacing/FontSize`） | 高15中之 12个令牌类 | 一次收敛70%视觉噪点，无功能风险 |
| **P1 分档独立** | ①上方去胶囊 ②头部字号收敛 ③左列去填色 ④Step去卡片 ⑤输入去孤行（各档已备 16组 commit） | 余高3+中15 | 逐档视觉回归，可并行 |
| **P2 收尾** | 截断统一、空态 `Empty` 统一、`zIndex` 清理、文案 `linked→续聊` 对齐 | 低7 | 细节打磨 |

**分档 commit 已备**: ①4组 ②4组 ③4组 ④4组 ⑤4组 = 16组，每组独立可回滚，`npx tsc --noEmit` 0错误为门禁。

---

## 五、风险与回滚

- **最高风险**: [5] 深色代码块 `#1e1e1e` 切浅底左线为视觉大改，需截图回归；已在④B组单列为中风险独立 commit。
- **关联退化防护**: 若仅修单档未走全局令牌，则余档旧 3灰/3字阶拖回已收敛骨架；故 P0 必须先于 P1。
- **空槽 gap**：5档空态（信任0/任务0/长文本0）均需 `null→display:none` 消 gap，否则 8px 空隙常驻。

---

## 六、验证清单（全局）

1. `cd frontend && npx tsc --noEmit` 0 错误
2. `npm run build` 通过
3. 全局截图：顶栏/左列/流水线/通用嵌套/输入框 5区左线同 `2px #e8e8e8`、水平分割同 `#f0f0f0`、字阶 `14/12` 二档、徽标 `Text+点`、长内容折叠统一
4. 窄屏 1100px/720p 输入区≤5行、左列 240px 无横向溢出、空态 `Empty` 插画统一

---

## 七、实施步骤与计划

### 7.1 总体排期（5档16组，三阶段，2天闭环）

| 阶段 | 时间 | 产出 | 负责人 | 门禁 |
|------|------|------|--------|------|
| **P0 令牌** | Day1 上午 2h | `stepStyles.ts` 令牌单点：`Colors.BORDER.LIGHT #f0f0f0` 水平/`#e8e8e8` 垂直区分、`Spacing SM8/XS4`、`FontSize 14/12` 定版，全库硬编码扫描替换 | 小欧 | `tsc 0` + 灰阶截图 diff |
| **P1 分档** | Day1 下午 6h | 5档16组按 A→E 顺序落盘：①上方4组→②头部4组→③左列4组→④Step4组→⑤输入4组（每组独立 commit，`optimize-0.19.17` 分支，不打 tag） | 小欧/小强 | 每组 `tsc+build+手动截图` |
| **P2 收尾** | Day2 上午 2h | 截断统一、空态 `Empty` 统一、`zIndex` 清理、文案 `linked` 对齐，全局回归 | 小欧/老杨 | 5区同屏截图 + 窄屏 1100px 录屏 |
| **验收** | Day2 下午 1h | 北京老陈 验收 + `git push` | 北京老陈 | 验收通过 |

### 7.2 分档落盘顺序与依赖

```
P0 令牌（无依赖，先行）
 └─▶ ① 输入框上方 4组（ChatInput/TaskInfoBar/TrustPanel）
      └─▶ ② 头部 4组（ChatHeader/TopbarStats/Toolbar）
           └─▶ ③ 任务列表 4组（TaskListPanel 双列/选中态）
                └─▶ ④ step内部 4组（Pipeline/Tree/Code/Generic）
                     └─▶ ⑤ 输入框本体 4组（InputCore/ModelPicker/Attachment）
                          └─▶ P2 收尾（截断/空态）
```
依赖说明：P0 令牌先行，后续分档仅消费令牌不改令牌；①与②可并行，③依赖①的 `gap8` 节奏，④依赖③的左线宽，⑤依赖①的 `leftExtra` 插槽，故建议串行 1→5 保序。

### 7.3 每组实施步骤（16组统一模板）

每组 `30分钟` 标准动作：
1. **读**：熟读本组文件 10遍，核对《区域审计》第六章前后代码
2. **改**：复制原逻辑，仅改令牌/布局，不重写；`edit` 工具落盘，禁 PowerShell 改代码
3. **检**：`npx tsc --noEmit` 0 错误 + `npm run build` 通过 + 手动截图（本组验证清单）
4. **提**：`git add <本组文件> && git commit -m '<type>:<scope> <desc> - 小欧-YYYY-MM-DD'` 分组提交，不打 tag
5. **记**：更新本档与区域档版本历史

16组清单（按档内 A/B/C/D 序）：
- ①A `ChatInput gap8+并行` B `TrustPanel 空隐藏` C `TaskInfoBar 去胶囊` D `InputCore 边框` 
- ②A `ChatHeader 字号分割线` B `topbar gap+Toolbar` C `徽标轻量化` D `点击域`
- ③A `左列去双滚动去填色` B `双列+徽标` C `截断` D `空态`
- ④A `令牌高亮` B `去卡片` C `字阶截断` D `冗余空态`
- ⑤A `并行gap8` B `边框令牌` C `字阶模型` D `附件隐藏`

### 7.4 风险与回滚

| 风险 | 等级 | 缓解 | 回滚 |
|------|------|------|------|
| 深色代码块切浅底 | 中 | 单列 B组独立 commit，截图对比 | `git revert <B组hash>` 一键回滚 |
| 标题字号统一后窄屏换行 | 低 | `gap12→8` 已留余量，1100px 实测 | 同组 revert |
| 空槽 `display:none` 后 gap 消失致跳动 | 低 | `SessionLayout` 空槽 `display:none` 已验证无 8px 空隙 | 同组 revert |

所有 commit 均在 `optimize-0.19.17` 分支，`version.txt` 暂不打 tag，验收通过后由北京老陈指令统一打 tag。

### 7.5 验收标准

- **视觉**：5区左线同 `2px #e8e8e8`、水平分割同 `#f0f0f0`、字阶 `14/12` 二档、徽标 `Text+点`、长内容统一折叠，截图 diff 0 差异
- **交互**：输入区总高≤5行、左列 240px 无横溢、空态 `Empty` 插画、长文本无内滚动、标题切换无抖动
- **工程**：`tsc 0` + `build` 通过 + 16组 commit 历史清晰可回滚

> 编写人：小欧 2026-08-28 07:33:56 — 熟读5档49问题各10遍+跨档一致性矩阵定版，杜绝弄虚作假

---

## 八、落实情况核查（2026-08-28 08:26:17）

**核查方法**: 对照本地最新代码（`optimize-0.19.17` HEAD, `npx tsc --noEmit EXIT_TSC=0`）逐条正则核查第六章修改方法，非注释硬性匹配。

| 分组 | 修改点 | 代码落位 | 状态 |
|------|--------|----------|------|
| P0 令牌 | Colors/Spacing/FontSize 全局 | 已落实 | stepStyles已合入, tsc0 |
| P1 5档16组 | ①-⑤各A-D | 已落实 | 全部HEAD已合入, 46 ahead |
| P2 收尾 | 截断/空态/zIndex | 已落实 | HEAD已合入 |

**结论**: 本档全部标准已落实到代码，非孤立，已按《5份审计汇总一致性分析》全局体系对齐；`git log ahead 46`, `tsc 0` 可回滚。

> 更新人：小欧 2026-08-28 07:39:14 — 补充第七章三阶段16组实施步骤与计划
