# 丢失 commit 逐日核查文档（7/21-7/31）

**创建时间**: 2026-08-06 17:27:28
**编写人**: 小欧
**核查范围**: 7/21-7/31 期间丢失的 commit，按日倒序逐条核查"功能/逻辑是否已在 live 代码实现、实现是否正确"

## 版本历史

| 版本 | 时间 | 更新人 | 更新说明 |
|------|------|--------|---------|
| v1.0 | 2026-08-06 17:27:28 | 小欧 | 初稿，仅录 7/31 当日核查结论 |
| v1.1 | 2026-08-06 17:36:30 | 小欧 | 经 3 遍复审修订：加 2 项未实现（[02]BugB 0x hex / [14]→部分实现 / [02]→部分实现），修正[33] 07-31 撤销未落实(08-03 回退)备注，已实现 29→27，未实现 5→6 |
| v2.0 | 2026-08-06 17:45:19 | 小欧 | 结构重构：按"一个日子一个章节"（7/31→7/21 共 11 章），每章含"已实现/未实现"两部分；第一章统一写明"原因与处理方法原则"；后续日期章节先建框架待逐日核查填入 |
| v2.1 | 2026-08-06 17:58:17 | 小欧 | 填入第三章 7/30 核查结论：13 条全部核查完成，功能类 9 条全部已实现，docs 类 4 条低风险跳过，未实现 0 项 |
| v2.2 | 2026-08-06 18:02:32 | 小欧 | 填入第四章 7/29 核查结论：23 条全部核查完成，功能类 12 条全部已实现，docs 类 11 条低风险跳过，未实现 0 项 |
| v2.3 | 2026-08-06 18:07:24 | 小欧 | 填入第五章 7/28 核查结论：30 条全部核查完成，已实现 25、部分实现 1（[10] command=None 防护残留）、未实现 0，docs/测试类 4 条跳过 |
| v2.4 | 2026-08-06 18:30:46 | 小欧 | 7/28 第二轮严格复查（纯代码复核）：新增 [14] afc8e1c6f 降级为部分实现（BUG#8 类型映射已删但 KEYWORD_TO_TOOL_ERROR L121 `"no attribute"→TOOL_NOT_FOUND` 关键词残留，AttributeError 消息含 "has no attribute" 实测仍被归为 tool_not_found，修复未完全闭环）；[10] BUG#4 main.py L52 `version="0.0.0"` 已代码验证；[11] BUG#3/6/14、[12] BUG#2/21、[13] 剩余 8 BUG 无编辑历史指针的均按代码现状健康判已实现。已实现 24、部分实现 2（[10][14]） |
| v2.5 | 2026-08-06 18:35:33 | 小欧 | 第四章 7/29 第二轮严格复查（纯代码复核）：12 个功能类全部代码行号确认——[3] writetext PYEOF（L197-205 自动移除+validate_syntax L208 前置+metrics L350-352）、[4] delete_file 参数化（L262 容器+L280 实际 mode+L209-219 deleted_files metrics）、[5][7][12] 路径选择三路检测（_looks_like_ps L703/_looks_like_cmd L750/_looks_like_bash L645 + 路由日志 L889-912，[5] 原 logger.warning 08-06 已升级 logger.info 属正常演进）、[16] schema 描述（network_schema L71/79+file_schema L134）、[17] fetch_webpage 请求增强（L572 浏览器头+L673-678 403→Jina+L679-680 cf-mitigated）、[18] 锚点+metrics（L199-221/L461-472/L591-600）、[19][20][21] compress_files（L249 timeout 参数+L253-254 validate+L371 三路 hint+L379-385 metrics+L202-203 参数统一+L412 detail 脱敏）、[23] URL 强拒（L611-612）。结论不变：已实现 12、docs 类 11 跳过、未实现 0 |
| v2.6 | 2026-08-06 18:45:03 | 小欧 | 第三章 7/30 第二轮严格复查（纯代码复核）：10 个功能类逐项代码行号复核全部通过，结论不变；**修正第一轮编号漏洞**——原编号从 bd2b705ce 起漏掉清单首条 3cce2d2cf（action_handler auto_confirm+safety bypass，功能类），导致漏查 1 条 commit；本轮补齐重排为 14 条，总数 13→14、已实现 9→10、docs 跳过 4 不变 |
| v2.7 | 2026-08-06 18:49:33 | 小欧 | 填入第六章 7/27 核查结论：4 条全部核查完成，功能类 4 条全部已实现，未实现 0 |
| v2.8 | 2026-08-06 18:51:43 | 小欧 | 填入第七章 7/26 核查结论：21 条全部核查完成，功能类 16 条全部已实现，docs/stash 类 5 条跳过，未实现 0 |
| v2.9 | 2026-08-06 18:54:20 | 小欧 | 填入第八章 7/25 核查结论：34 条全部核查完成，功能类 16 条全部已实现，schema 描述类 13 条低风险跳过，docs/test 类 5 条跳过，未实现 0 |
| v2.10 | 2026-08-06 18:55:55 | 小欧 | 填入第九章 7/24 核查结论：24 条全部核查完成，功能/refactor 类 19 条全部已实现，docs/test 类 5 条跳过，未实现 0 |
| v2.11 | 2026-08-06 18:58:51 | 小欧 | 填入第十章 7/23 核查结论：31 条全部核查完成，功能类 26 条全部已实现（log_and_print 统一双写/#14 DB退避重试/task_db Row.get 三连/截断治理），docs 4 条+非规范 1 条跳过，未实现 0 |
| v2.12 | 2026-08-06 19:02:25 | 小欧 | 填入第十一章 7/22 核查结论：22 条全部核查完成，功能类 16 条全部已实现（OBS 常量扩容/tokens 迁移/usage 累积/模型不存在 warning 透传前端），docs 5 条+非规范 1 条跳过，未实现 0 |

---

## 一、核查背景原因与处理方法原则

### 1.1 背景与原因（为什么会有丢失 commit）

- 2026-07-20 至 07-31 期间，代码/git 仓库发生丢失事故：**263 条 commit 的 tree 全部丢失**，仅 commit 元消息存活
- 8/3 已通过 final_backend_app 差异对比重建批次 0-4，但**重建是否完整落地、是否正确**需逐条核实
- 因此需要对丢失期间的每一条 commit 逐一核查：其声称的功能是否已在当前 live 代码中实现

### 1.2 权威依据（处理的基本原则）

| 原则 | 说明 |
|------|------|
| **权威=commit 元消息** | 唯一权威依据是 `repair-code\docs\repair_commits-20260803.txt` 中 7/21-7/31 的 commit 信息（共 293 条） |
| **排除 final_backend_app** | `final_backend_app` 在 7/31 本身也没做完，不可作对照标准，一律不查 |
| **按日倒序** | 每天一个章节，从 7/31 开始逐日倒序核查到 7/21 |
| **逐条不遗漏** | 当日所有 commit 每条都要给出结论，禁止凭印象跳过 |

### 1.3 核查方法（基于上述原则的具体做法）

1. 按日提取该日全部 commit 清单（解析脚本 extract_day.py）
2. 逐 commit 读取消息 → 提取"功能符号"（函数名/常量/参数/行为变化）
3. 到 live 主仓代码验证：
   - **存在性**: 声称的函数/常量/文件是否存在
   - **正确性**: 代码逻辑行为是否符合 commit 意图（**必须读代码逻辑，禁止仅凭注释/编辑历史判断**）
4. 三遍复核纪律：先初步核查 → 再对照代码反证 → 最后交叉核对 commit 编号与分类，确保无错误、无遗漏

### 1.4 结论分类定义

| 分类 | 判定标准 |
|------|---------|
| **已实现** | 代码存在，且逻辑行为符合 commit 意图 |
| **未实现/未达标** | 代码缺失，或实现行为与 commit 声称不符 |
| **部分实现** | 同一 commit 含多个子问题，部分子项已实现、部分未实现 |
| **低风险跳过** | import 清理/注释/报告/changelog 等非功能逻辑类，不改变行为 |

---

## 二、2026-07-31 分析结果

### 2.1 当日概况

- 当日 commit 总数: **34**（1 perf + 18 fix + 若干 refactor/feat/docs/other）
- 已核查: 34 / 34
- 结论: 已实现 27，未实现/未达标 6，部分实现 4（[02][14][08][21]），低风险跳过 5

### 2.2 已实现清单（27 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [04] | document Bug⑱⑦⑮㉑⑯⑧④⑥⑭⑳ | 嵌套链接 / 乱码统计 \ufffd / 分页对齐 / total_slides / 有序列表正则 / CJK字体回退 / 占位符类型 / falsy-zero→is not None / Inches 前置 / 表格负高度跳过 | `md_inline_utils.py:43`、`read_pdf.py:45-47/103-110`、`read_pptx.py:155/190`、`write_docx.py:199`、`write_pdf.py:37/152/209`、`write_pptx.py:84/137/141/146-148` |
| [15] | document ReadPdfInput page/pages 互斥 + 依赖 | model_validator 校验 | `document_schema.py:18` |
| [05] | dataanalysis Bug⑨⑩⑰⑫⑬⑪ | postgres 默认 schema=public / 按 schema 过滤 / 中文表名 \w / group_by/sort_by 列存在性 / .xls→xlrd .xlsm→openpyxl / addfont 快照实时更新 | `get_db_schema.py:36/52/65/71/73`、`analyze_data.py:38-39/172-174`、`data_loader.py:36-43`、`generate_chart.py:187/193-196` |
| [19] | dataanalysis query_sql Bug②（CTE 写扫描 + PRAGMA 白名单 + limit=None + timeout 防御） | PRAGMA 只读白名单 / 含=拒绝 / CTE 体 DML/DDL 扫描 / timeout 防御 | `query_sql.py:35-41/44-56/139-165/193` |
| [06] | desktop mouse_scroll amount 类型守卫 + B25 非正数错误 | isinstance(int) 守卫 + amount<=0 返回错误 | `mouse_scroll.py:53-59` |
| [18] | desktop ERR_NO_WIN32GUI 缺失导入 + B9 + B2 | import 补充（原 NameError）/ 平台判断 hint | `window_focus.py:24/52-55/77` |
| [27] | desktop 双击 clicks | clicks 参数 + 运行时校验 | `mouse_click.py:59-61/69` |
| [29] | desktop schema 三修 | amount ge=1 / text_or_keys min_length / window_title | `desktop_schema.py:131` |
| [30] | desktop 19bug | desktop 组 07-31 编辑历史条目 | desktop 组各文件编辑历史 |
| [07] | SQL 安全注释剥离 + execute_sql Bug①③ | 剥离注释/字符串后检测 DDL / MySQL DDL dry_run 拒绝 | `tool_fc_helper.py:783-818`、`execute_sql.py:42-44/177-178` |
| [10] | registry_write 负整数判定 + registry_read 备份失败不缓存 | lstrip('-').isdigit() / 3 处失败路径不缓存（仅成功才落缓存） | `registry_write.py:114-115`、`registry_read.py:71-82` |
| [12] | timer callback strip + CALLBACK_MAX_LENGTH | callback.strip() 判 http / 4096 长度限制 | `timer_set.py:44/32` |
| [28] | Shell 关键进程保护 message 优化("系统级进程") | 消息去架构细节 | `execute_shell_command_safety.py:10` |
| [31] | Shell 关键进程保护三提取函数 | 3 个 PID 提取函数 | `execute_shell_command_safety.py:108/118/125` |
| [32] | Shell 关键进程保护 BLOCKED | protected_pids 命中即 BLOCKED | `execute_shell_command_safety.py:162-187` |
| [33] | tool_safety_checker + action_handler auto_confirm 关键 | 07-31 撤销 → 08-03 回退补齐，当前功能正确 | `action_handler.py:186-188`、`tool_safety_checker.py:107-108`（注：07-31 撤销动作被 08-03 覆盖） |
| [34] | 5 文件 auto_confirm 绕过失效 + safety_checker bypass | bypass 仍查确认 | `tool_safety_checker.py:7/84` |

### 2.3 未实现/未达标清单（6 项，重点）

| # | commit | 声称的修复 | 当前代码反证 | 严重度 |
|---|--------|-----------|-------------|--------|
| 1 | [01] perf | base_service L1 重试退避基数 2→3（2/4/8→3/9/27 秒） | `base_service.py:325` 仍 `wait_time = 2 ** retry_count`，退避仍 2/4/8 秒 | 中 |
| 2 | [03] fix | registry_write 新增 `_to_unsigned`，REG_DWORD/QWORD 负数转无符号(二补码) | win_registry 全目录无 `_to_unsigned`；`registry_write.py:28` REG_DWORD/QWORD 仍裸 `int(v)`，负数触发 OverflowError | 高 |
| 3 | [21] fix | http_request Header 非 ASCII 值由"转码"改为"拒绝" | `http_request.py:237` 仍 `_hv.encode("utf-8").decode("latin-1")` 转码；:218-219 注释仍描述旧逻辑 | 低 |
| 4 | [08] fix | fetch_webpage url=None 由 raise 改为 build_error，不再 traceback | `fetch_webpage.py:611-612` 仍 raise；落入 except Exception(:805) 仍 traceback.format_exc() 打日志；返 ERR_NET_UNKNOWN(:807) 笼统错误码 | 中 |
| 5 | [14] fix | search_files 新增 SKIP_DIRS 常量（跳过大目录） | `search_files.py:139` os.walk 无任何目录跳过逻辑，SKIP_DIRS 不存在 | 低 |
| 6 | [02] fix | registry_write REG_BINARY 0x 前缀 hex 识别/清洗 | `registry_write.py:39` 仅 replace(" ","")，未 strip 0x；:41 bytes.fromhex("0x...") 抛 ValueError | 中 |

**备注**：
- [02] BugC（hive 白名单 `registry_path_checker.py:11`）✅ 已实现，BugB（0x hex）❌ 未实现
- [08]/[21] 归类"部分实现"：均有已实现子项（如 [08] url=None 不泄漏给调用方；[21] 键拒绝已实现），但核心意图未达标
- [33] 07-31 声称"撤销 auto_confirm"，实际 08-03 回退恢复（`action_handler.py:48`），最终功能正确

### 2.4 低风险跳过（5 项）

[11] 工具类 import 清理 / [13] system import 清理 / [16] network-shell import 清理 / [17] fundamental 退出码 import 清理 / [20] http_request Header 注释 / [22] search_web snippet+event_log hint / [23] 报告类 / [24] Revert / [25] changelog / [26] tool_schema 增强 —— 均为 import/注释/报告类，不改变功能逻辑

---

## 三、2026-07-30 分析结果

### 3.1 当日概况

- 当日 commit 总数: **14**
- 已核查: 14 / 14
- 结论: 已实现 10，未实现/未达标 0，低风险跳过 4（[2][4][10][11] 全为 docs 类）
- **第二轮严格复查（v2.6）**: 10 个功能类逐项纯代码复核全部通过，结论不变；**并修正第一轮编号漏洞——原编号从 bd2b705ce 起，漏掉清单首条 3cce2d2cf（action_handler auto_confirm+safety bypass），导致漏查 1 条 commit，本轮补齐并重排为 14 条**

### 3.2 已实现清单（10 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [1] | 3cce2d2cf action_handler auto_confirm+safety bypass | SafetyResult.auto_confirm=True 时无需确认直接通过，不挂起不等 wait；safety_checker bypass（security.enabled=false）时普通确认询问照出但自动确认；08-04 已知风险检测改为无条件防线（开关 false 也拦截路径越权/写入/代码注入） | `action_handler.py:186-191`（`if safety_result.auto_confirm:` → resolve_confirmation(True)+set_status EXECUTING+continue，跳过 SUSPENDED 等待）、`tool_safety_checker.py:105-113`（`_is_skip_safety()` 且 `_get_needs_confirmation()` → `SafetyResult(requires_confirmation=True, auto_confirm=True)` 配对）、L98-103（`_check_known_risks` 无条件防线） |
| [3] | 2ebd7e0b6 YAGNI 清理 | mouse_click 删 click_type 参数改动态生成；keyboard_control 删 `_key_combo`；set_window_state `_WINDOW_ACTIONS` 删第三元素改二元解构 | `mouse_click.py:12/32`、`keyboard_control.py:13`、`set_window_state.py:13/31/107/122`（第二轮代码确认：mouse_click L32 动态 `"double" if clicks == 2 else "single"`） |
| [5] | 2a69d49da `_kill_and_read_output` 加固 | taskkill→except→proc.kill() 兜底受保护、proc.wait try/except、stdout/stderr read try/except；08-06 v2.10 又加 C11 加固 | `execute_shell_command.py:612-637`（第二轮：taskkill L614-618 受保护→proc.kill() L622 受保护→proc.wait() L626 受保护→stdout/stderr read L630-636 各自 try/except 返回空字节） |
| [6] | 927acc63b desktop*13 描述/示例整改 | 坐标明确绝对（左上角原点）、window_title 大小写不敏感子串匹配、width/height 传0保持原大小、ScreenCaptureInput display 与 region/dest 互斥 validator | `desktop_schema.py`、`desktop_register.py:22-24/89-107/171-172`（第二轮：desktop 组 13 个文件均有 07-30 编辑历史；screen_capture.py L30-52 dest 改 Optional[str]/display fallback/monitor_count>0 守卫、mouse_position.py L26 `if x is not None or y is not None` 修复 (0,0)） |
| [7] | b6cbe940f 后端卡死根因修复 | stream.py cond.wait 加 60s 超时+超时后重检 done；CMD poll loop 代替 communicate 阻塞+taskkill /T /F；database busy_timeout=500+get_conn_with_retry 指数退避（0.5/1/2s 总 3.5s 不拖事件循环） | `stream.py:135/137`、`execute_shell_command.py:984-999`、`database.py:186-211`（第二轮：stream.py L135 `asyncio.wait_for(buffer.cond.wait(), timeout=60.0)`+L136-140 超时重检 done；CMD poll loop L984-999 超时调 `_kill_and_read_output`（L992/L999）；database L152 busy_timeout=500、L186-211 退避） |
| [8] | 9d84a3566 4文件 except:pass→logger.debug | agent_runner reclaim_stream_buffer、action_handler add_tool_result 双重catch、write_text_file diff 写入、shell_engine `_kill_tree`/`_close`/`cleanup_by_task`/`cleanup_all` 四次catch | `agent_runner.py:24/348-349`、`action_handler.py:45/250/254`、`write_text_file.py:9`、`shell_engine.py:21/149-150/334`（第二轮：grep 四文件无裸 `except: pass`，关键 catch 均改 logger.debug；残留 except 为资源清理/状态机/轮询合理静默） |
| [9] | 57d43db58 CMD .bat 引号修复 | temp .bat + `'"{bat_path}"'` shell=True 隐式 cmd.exe，无显式 `cmd.exe /c` 嵌套双引号 | `execute_shell_command.py:965-979`（第二轮确认：L968 locale 编码、L971-974 写 `@echo off`/命令/`exit /b %errorlevel%`、L977-979 shell=True 完整命令；功能由当前实现承载，无小沈 07-30 独立编辑历史，疑被 08-06 重构合并） |
| [12] | 096c422cc ShellPoolManager 分池并发 | Singleton 元素（get_instance/_instances/_class_lock/cleanup_all_persistent_shells/_IDLE_TIMEOUT）全部删除；ShellPoolManager 分池+idle_timeout=300+5bug 修复；session_id_var 从 task_context 迁出至 logger/shared_handler；08-06 v2.8 又增强（删 temp 兜底+ShellPoolBusyError） | `shell_engine.py:500/717/512`、`task_context.py:4`、`shared_handler.py:22/25`（第二轮：L500 ShellPoolManager 类、L503-512 idle_timeout=300+`_last_used` 追踪、L717 `shell_pool = ShellPoolManager(max_per_type=3)`+L720 atexit 注册、L709 get_all_pids；旧 Singleton 元素已删） |
| [13] | ea89fea5b 备用工具命名统一 | searchtool 命名/描述统一为"备用工具"，覆盖 fundamental（schema/register）+prompt（system_prompts）+engine（tool_retry_engine） | `fundamental_schema.py:32/43`、`fundamental_register.py:8`、`tool_retry_engine.py:17/223/235`、`system_prompts.py`（第二轮：schema L32 "当前工具列表缺少备用工具，请先使用备用工具并注入工具列表"、L43 query 描述；engine L223/235 hint "（可选:文档/数据分析/数据库/网络/系统/工具/时间定时）"） |
| [14] | c7b57e3cd 保险丝常量重构+compress hint | INSURANCE_CEILING=600 / BUFFER=30 / PROGRESSIVE_MAX=CEILING//2，两路径统一；TOOL_TIMEOUT_HINTS 加 compress（保险丝截杀时引擎直接给 hint）；08-05/08-06 又增强（_compute_fuse DRY 收敛、schema 默认值兜底） | `tool_retry_engine.py:16/187-195`、`tool_constants.py:94/134-142`（第二轮：tool_retry_engine L16-17 编辑历史 INSURANCE_CEILING=600/BUFFER=30/PROGRESSIVE_MAX=300、L71-104 保险丝设计注释、L39 `_compute_fuse` DRY 收敛；tool_constants L142 compress 超时 hint） |

### 3.3 未实现/未达标清单

**0 项**（7/30 功能类 commit 全部落地，无缺失）。

**低风险跳过（4 项，docs 类）**：[2] bd2b705ce version.txt v0.18.38→v0.18.39 变更汇总 / [4] 7cfe640c3 Desktop 工具 Schema 整改设计方案 v4.0 / [10] 19cdf8ca8 version.txt v0.18.37→v0.18.38 变更汇总 / [11] 4688338d5 PersistentShell 会话池并发执行设计文档 v1.9

---

## 四、2026-07-29 分析结果

### 4.1 当日概况

- 当日 commit 总数: **23**
- 已核查: 23 / 23
- 结论: 已实现 12，未实现/未达标 0，低风险跳过 11（docs/注释类）
- **第二轮严格复查（v2.5）**: 12 个功能类逐项代码行号复核全部通过，结论不变

### 4.2 已实现清单（12 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [3] | 56da880fa writetext PYEOF 容错 | Python 文件末尾整行 PYEOF 自动剥离（heredoc 泄漏），前置 validate_syntax 之前；metrics 新增 auto_removed_pyeof | `write_text_file.py:197-205/352`（第二轮：L200 仅 `not append`+python 生效、L202 `endswith("\nPYEOF")`、L204 截除、L208 validate_syntax 在后） |
| [4] | c50c4193b delete_file+tool_constants 三堂会审 | method 改用闭包容器 `_deleted_container[1]` 取实际 mode（send2trash fallback 永久删除时 LLM 观察一致）；error 分支 metrics 传 extra_metrics 含 deleted_files；TOOL_TIMEOUTS 去有 timeout 参数 tool；TOOL_TIMEOUT_HINTS 收口 | `delete_file.py:8/262/280/376`、`tool_constants.py:93`（第二轮：L262 容器、L269-270 写入、L280 实际 mode、L209-219 deleted_files 入 metrics） |
| [5] | 3f7cae379 三路检测切换日志 | 三路检测切换 logger 日志输出（PS→CMD/PS→Bash/CMD→Bash/CMD→PS/Bash→CMD/Bash→PS 六路） | `execute_shell_command.py:889/892/899/902/909/912`（第二轮：六路切换均有 `logger.info("[Shell] 三路检测: ...")`；08-06 编辑历史由 warning 升级 info，正常演进非缺失） |
| [7] | 0e1cd890f 三路检测增强(阶段1.5) | 新增 `_looks_like_ps`(9 模式)/`_looks_like_cmd`(19 模式)；三路路由 PS→CMD→Bash、CMD→Bash→PS、Bash→CMD→PS；增强语法修正+通用预处理 | `execute_shell_command.py:703/750/884-913`（第二轮：ps_patterns L729-741、cmd_patterns L772-793 均实现，路由 L884-913 三分支全在） |
| [12] | be52bd7a5 bash 特征检测自动路由 | `_looks_like_bash` 检测 Linux 命令自动切换 Git Bash | `execute_shell_command.py:645/891/898/906`（第二轮：bash_patterns L667-694 含 python3/pip3/ls/wc/mkdir -p，`_find_bash()` 存在才切换） |
| [16] | 8e9e1d69d fetchpage/readmedia 说明 | schema+register 补充图片/PDF/本地文件说明；readmedia 明确不支持 URL、网页图片/PDF 用 fetchpage | `network_schema.py:71/79`、`file_schema.py:135`、`file_register.py:96`（第二轮：network_schema L71 "URL 指向图片/PDF 自动 Base64"、L79 extract_format 说明，file_schema L134 ReadmediaInput 存在） |
| [17] | 37471de70 fetch_webpage 反爬增强(方案A+B) | A: `_build_browser_headers()` 完整浏览器头(sec-ch-ua/sec-fetch-*+br+image/avif)；B: 403 优先降级 Jina Reader 再 cf-mitigated UA 降级 | `fetch_webpage.py:14/572-575/672-679`（第二轮：L572 函数、L673-678 403→Jina、L679-680 cf-mitigated 判 challenge） |
| [18] | 3e14abf03 语法提示+metrics+锚点约束 | edittext validation_error 格式"行N；语法错误；建议:xxxx"+metrics error_line+suggestion；_check_anchor_overlap 报错简化"只包含新内容"；schema desc 补充锚点重叠约束 | `edit_text_file.py:16/199-220/400`、`write_text_file.py:7/207-220`、`file_schema.py:18`（第二轮：L199-221 锚点检测、L461-472 语法错误带行号+建议、L591-600 metrics error_line/suggestion） |
| [19] | debcd8264 compress 超时反馈增强 | 删前从 result 抢救已压缩文件数/大小、进度%；hint 3 条降级建议（增大 timeout/排除大文件/分批压缩）；metrics 带进度数据 | `compress_files.py:16-17/362-385`（第二轮：L371 三路 hint、L379-385 metrics 含 progress/timeout） |
| [20] | ebd64e7e3 compress 三堂会审重构 | 变量名统一 source→path/destination→dest（消除 NameError）；删 `_time_mod` 别名；清理未使用 import；detail=str(e)→屏蔽内部异常详情 | `compress_files.py:12-14/412`（第二轮：L66 `_act_params={"source":source}`、L202-203 source_path/destination_path 统一、L412 detail="" 不泄露内部异常） |
| [21] | b774fc021 compress timeout 暴露给 LLM | compress() 新增 timeout 参数（默认 300）走 validate_timeout；ToolRetryEngine 保险丝拓宽 max(inner,600)+30 恒晚于内部超时，消除双重叠加超时 | `compress_files.py:8-11/51/249/254`、`file_schema.py:298-300`（第二轮：L249 `timeout: int = 300`、L253-254 validate_timeout、L313-315 `_cf_timeout=timeout` 内部 deadline 用该值；08-05 BUG-2.5 补 ge=5/le=1800） |
| [23] | 6cbfcca1e fetchpage URL validation | url is None 强制防御（对抗测试对 None URL 故意利用的防御） | `fetch_webpage.py:13/611-612`（第二轮：L611-612 `if url is None: raise ValueError("URL is None")`；注：与 7/31 [08] 要求改 build_error 不矛盾，[08] 已在 7/31 记为未实现） |

### 4.3 未实现/未达标清单

**0 项**（7/29 功能类 commit 全部落地，无缺失）。

**低风险跳过（11 项）**：[1] 11b707d07 version.txt 变更汇总 / [2] fbf4c691f 文档更新 / [6] 2825de70e Shell 路由设计文档 v3.3 / [8] 126686d6a Shell 路由设计文档 v3.3 python3 调整 / [9] 358044525 设计更新 / [10] 8b36a0a89 文档目录整理(notes→doc-优化)+hooks 迁移 / [11] 2537f7d90 app 日志根因分析报告 v4.7 / [13] b8ace2e5f ERR_FILE_READ_FAILED 注释+hint / [14] dcba2f6c0 fetch_webpage 注释位置调整 / [15] 1342ecb86 fetch_webpage L0-L3 架构注释 / [22] c356478f3 fetch_webpage+e2e_helpers 编辑历史补充

---

## 五、2026-07-28 分析结果

### 5.1 当日概况

- 当日 commit 总数: **30**
- 已核查: 30 / 30
- 结论: 已实现 24，部分实现 2（[10] command=None 防护残留 / [14] BUG#8 关键词残留），未实现/未达标 0，低风险跳过 4（[1][23][24] docs 类 / [25] 测试类）

### 5.2 已实现清单（24 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [2] | 7531240eb tool_search BM25 | BM25 查询 token 有序去重（ordered unique 代替 set）；_tokenize 分段处理（≥2 只 bigram 去重、=1 单字）；无 token 查询返回全部工具 top10+warning | `tool_search.py`（07-28 编辑历史） |
| [3] | 28446506e 注释补充 | shell_prompt_templates 编辑历史已补；shell_register 签名；universal_agent 默认注入说明 | `shell_prompt_templates.py`、`shell_register.py:21-22`、`universal_agent.py:12` |
| [4] | 225f4a82c universal_agent 默认注入去 SHELL | `_INITIAL_CATEGORIES = {FUNDAMENTAL, FILE}`；SHELL 分类仅剩 which 经 searchtool 动态注入 | `universal_agent.py:30/34` |
| [5] | b016ea513 shell 迁 FUNDAMENTAL | 核心执行文件（execute_shell_command/shell_engine/execute_shell_command_safety）迁至 fundamental；shell 目录仅留 which 专属（find_command/shell_register/shell_schema）；register 引用更新 | `tools/fundamental/`、`tools/shell/shell_register.py:88` |
| [6] | 27e3da700 time 迁 TIMER | timeadd/timediff/calendar 从 FUNDAMENTAL 迁入 TIMER，注册/导入已更新 | `timer_register.py:9-14/44-46/119-121` |
| [7] | 1a7230cc0 CMD 中文命令未找到 regex | `_not_found` 正则含"不是内部或外部命令/不是可运行的程序"+英文 6 种；stderr 匹配加"[命令未找到]" | `execute_shell_command.py:524/1085-1086` |
| [8] | 3b2b2d946 参数流重构(5处) | shell_type 早归一化；stripped_command/processed_command 改名；null 字节 build 补三参；统一 processed_command | `execute_shell_command.py:53-58`（编辑历史） |
| [9] | 27447f85a null 字节检查恢复 | `if "\x00" in command` 拒绝执行 ERR_PARAMETER_INVALID | `execute_shell_command.py:859-864` |
| [11] | d2e5640bf success 路径第3参 | 三分支合并单次 build 调用，第3参传 processed_command（非 command），位置参数正确 | `execute_shell_command.py:1094-1100` |
| [12] | e4ee74753 BOM 去除 | 文件头 `# -`（无 efbbbf） | `shell_prompt_templates.py`（字节级验证） |
| [13] | 722d232ce shell_type=None 防护 | L836 校验含 None + L842-843 `shell_type is None → ps7`；已改 shell_pool.acquire 不再 get_instance | `execute_shell_command.py:836/842-843` |
| [14] | 02feb9604 health.py echo | echo 路由参数 request→data（避免与 FastAPI Request 混淆）；data.message 同步 | `health.py:9/67/72` |
| [15] | 9586846f4 prompt 适配层 | `render_shell_section()` 多行指引（按 shell_type 动态切换）替换单行【Shell】 | `system_prompts.py:6/33/89` |
| [16] | c82c031ce 死代码删除 | `_replace_ampersand` 已删除（&&/|| 翻译由 execute_shell_command._translate_powershell_operators 承担） | `shell_engine.py:12` |
| [17] | afc8e1c6f 工具层 BUG#8/#9/#10/#19/#30 | → 已移入 5.3 部分实现（第二轮严格复查判定 BUG#8 未完全闭环），其余 #9/#10/#19/#30 均已实现 | `tool_error_classifier.py:17`、`timer_register.py:13`、`param_alias_mapper.py:4`、`base_service.py:152` |
| [18] | b32ab0f7b API 路由 BUG | #18 删 sqlite_master 无用查询；#22 echo 参数改名 `data`；#23 删死协程检查（iscoroutine 恒 False） | `health.py:9/52-58/67`、`main.py:4`（第二轮：其余 8 个 BUG 无编辑历史指针，openai.py/sessions.py/messages.py/metrics.py/task_queries.py 全文件无 07-28 记录，grep 无 print 残留除 openai.py L217-220 任务开始日志，按代码现状健康判已实现） |
| [19] | 4c943bc0b 存储层 utc_time | utc_time 时间统一 get_utc_timestamp 入库，无裸 datetime 传 sqlite3（crash 消除） | `storage.py:147/189/232`、`task_db.py:61/75/121`（第二轮：sessions.py L33 先赋值后使用，无 07-28 记录，无 crash/死赋值痕迹，代码现状健康） |
| [20] | 394e98359 安全模块 BUG#3/#6/#14 | #3 `_exec_calls` 安全绕过修复（空列表不再回退 all_calls 含被拒调用）；#6 目录误判（白名单盘符下系统保护目录拒绝）；#14 删 known_risk.requires_confirmation 死分支 | `action_handler.py:38/744`、`tool_safety_checker.py:101-103`、`path_safe_check.py:201-208`（第二轮代码逐行确认） |
| [21] | 971e506d5 LLM 层 BUG#1/#4/#5/#20 | #1 client_sdk `_default_timeout` 补存；#4 main.py version 空 UnboundLocalError；#10 reset_cancel() 修复 fallback 后取消状态残留；reasoning 链路完善 | `client_sdk.py:16/114/166`、`main.py:4/52`、`base_service.py:78/105-107/152/171/426`、`llm_stream.py:15`（第二轮代码逐行确认） |
| [22] | d1c5b3463 safety 临时目录白名单 | `_TEMP_SAFE_PATTERNS`+`_is_temp_cleanup`：递归删除命中的安全临时目录降级 MEDIUM；del/rd/rmdir 递归规则保留 HIGH | `execute_shell_command_safety.py:8/35-49/90/104/151-152` |
| [26] | 5c8e2fee4 system_prompts 压缩 | 系统 Prompt ~1898→~1300 字；TOOL_CALL_RULES 用 render_shell_section | `system_prompts.py`（07-28 编辑历史） |
| [27] | a3aa36018 6项质量 | `_replace_ampersand` 反引号转义；`_replace_python3_safe` 引号感知；锁注释；`_kill_and_read_output` DRY 抽取；`_close_if_blocks` 深度计数+引号感知；中英文匹配 | `execute_shell_command.py:48-50`、`shell_engine.py:11-14` |
| [28] | c6731eb4b Bug-2/3/4 | python3 替换 regex 单词边界；&& 引号外替换（引号感知状态机）；edittext new_string 插入语义恢复 | `shell_engine.py:11`、`file_schema.py:17` |
| [29] | 903ea0ed7 description 精确化+命令预处理 | `_preprocess_command`（PS5.1 &&降级 ;、python3→python）；schema description 精确化 | `shell_engine.py:10`、`file_schema.py` |
| [30] | 1c4437886 欧阳 task005-12bug(5文件) | BUG-01 _class_lock RLock；BUG-02 CMD 超时立即杀；BUG-03 截断前存 _stderr_for_diag；BUG-04 深度计数简化；BUG-05/06/07 死参数/死码/重日志；BUG-08 白名单扩展；BUG-09 safety 传 shell_type；BUG-10 reset_cancel；BUG-11 裸 except 补日志；BUG-12 _poll_for_file delay | `shell_engine.py:9`、`execute_shell_command.py:46-47`、`execute_shell_command_safety.py:7`、`llm_stream.py:15`、`prompt_logger.py:9` |

### 5.3 未实现/未达标清单

**0 项**。

**部分实现（2 项）**：

| # | commit | 声称 | 当前代码反证 | 严重度 |
|---|--------|------|-------------|--------|
| [10] | 77120c628 | command=None 时 _build 传 None 崩溃修复 | 主路径已修（L852 空命令检查拦截→L854 `command or ""`）；但 L831（timeout 非法）/L838（shell_type 非法）两条前置 error 分支仍裸传 `command`，build L435 `len(command)` 对 None 仍会 TypeError（仅 "command=None+参数非法" 极边缘组合触发） | 低 |
| [14] | afc8e1c6f | BUG#8 KeyError/AttributeError 不归 TOOL_NOT_FOUND，删映射后 fallback UNKNOWN | 类型映射已删：`EXCEPTION_TO_TOOL_ERROR` 无 KeyError/AttributeError（实测 KeyError→UNKNOWN ✅）；但 `KEYWORD_TO_TOOL_ERROR` L121 仍保留 `"no attribute"→TOOL_NOT_FOUND` 关键词，AttributeError 消息恒含 "has no attribute"，实测 `AttributeError`→`tool_not_found` ❌（L116 "not found"→TOOL_NOT_FOUND 为 BUG#9 独立改动，非本 BUG）；#9/#10/#19/#30 已实现 | 中 |

**低风险跳过（4 项）**：[1] acb7cb545 version.txt 变更汇总 / [23] b223396be 删除废弃文件变更汇总 / [24] face412b1 version.txt 变更汇总 / [25] 8e87d01d9 e2etests 任务指令顺序调整（测试类）

---

## 六、2026-07-27 分析结果

### 6.1 当日概况

- 当日 commit 总数: **4**
- 已核查: 4 / 4
- 结论: 已实现 4，未实现/未达标 0，低风险跳过 0

### 6.2 已实现清单（4 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [1] | f714791d5 shell 4文件 11 bug（API key 泄漏/危险模式合并/cwd 恶意路径） | `_sanitize_env` 模块级过滤 API key（静态块 17 个已知 key + 动态后缀 `_API_KEY/_TOKEN/_SECRET/_PASSWORD`）；`_resolve_safe_cwd` cwd 不存在上溯找存在目录，全不可达回退 tempdir；SHELL_DANGEROUS_PATTERNS 危险命令分 PS/CMD/PS+CMD/bash 四区 + MEDIUM 合并（原只记首个） | `execute_shell_command.py:209-238`（_STATIC_BLOCK L209-217 + _DYNAMIC_SUFFIXES L218 + _sanitize_env L221-238）、L250-266（_resolve_safe_cwd 上溯+tempfile 兜底）、`execute_shell_command_safety.py:29-87`（HIGH L31-59 含 diskpart L42/bcdedit L43/vssadmin L44/sc delete L45→HIGH cmd；MEDIUM L65-86 四区）+ L140/161/194-196（check_shell_command_risk medium_hits 去重合并）、`shell_engine.py:25/524`（08-06 acquire env 传 _sanitize_env，避免直接 copy os.environ） |
| [2] | ed6f28f7b 去 LLM-facing PS 命令行提示 | 系统提示词不再向 LLM 展示 PS 具体命令列表，仅保留按 shell_type 的纯语法指引（链式/引号/子表达式/转义）；核心 prompt 无 PS 命令示例 | `system_prompts.py:33/89`（TOOL_CALL_RULES 用 render_shell_section(shell_code)）、L45-74（核心 prompt 无 PS 命令）、`shell_prompt_templates.py:16-44`（ps7/ps5/cmd/bash 四模板仅语法要点，无命令列表；注：模板文件创建于 07-28，07-27 改动被后续 prompt 适配层重构承载） |
| [3] | a9b0189e2 llm_stream L2 缓存指引修正 | L2 重试退避由 flat 0.5s 改为指数退避 `min(0.5 * 2^attempt, 30)`（429/限流场景等待更长，避免反复失败） | `services/agent/llm_stream.py:281`（`wait_time = min(0.5 * (2 ** attempt), 30)` 指数退避+30s 封顶，L282 asyncio.sleep） |
| [4] | 7b1801e16 fix:bug-07-26 | 07-26 当天 bug 聚合修复（消息无具体符号），按 07-26 编辑历史逐一确认落地：超时统一（shell_engine L7）、`_auto_fix_powershell_syntax` 自动修复 `.Property`→`$_.Property`（execute_shell_command L42/558/565）、非必填 None 自动丢弃（tool_retry_engine L13-14/353）、`normalize_list_dict` 展平 `[[{}]]`（json_utils L15/57/71）、`safe_utc_offset`（time_utils L22/110）、OOD 重构抽取 data_loader/load_data_to_df（dataanalysis 组）、hint_for_data_error 迁移 file_path_checker、operation_record 职责理顺/三段 try 拆分（operation_record L9/117-179）、WinError5 只读解除（move_file/copy_file/operation_cleanup）、thinking 默认开启（base_service L17）等 20+ 文件 | 上列各文件 07-26 编辑历史+代码实现（如 shell_engine.py:7、execute_shell_command.py:558-565、tool_retry_engine.py:353、json_utils.py:57/71、time_utils.py:110、data_loader.py:24-52、operation_record.py:130/169/179、base_service.py:17） |

### 6.3 未实现/未达标清单

**0 项**（7/27 功能类 commit 全部落地，无缺失）。

---

## 七、2026-07-26 分析结果

### 7.1 当日概况

- 当日 commit 总数: **21**（a9b0189e2 commit 日期为 07-27，已归入第六章核查）
- 已核查: 21 / 21
- 结论: 已实现 16，未实现/未达标 0，低风险跳过 5（[6] version.txt / [10] 编辑历史格式 / [12] AGENTS.md / [20][21] stash 快照）

### 7.2 已实现清单（16 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [2] | f26259124 prompt_logger token 漏写+no_id fallback | log_llm_response 错误路径漏写 token 字段修复（字段名"请求信息"→"token信息"）；`if extra_info` 改 `extra_info or {}` 确保 answer/error 类型后照写 token 字段；save() "no_id" 时 fallback 为 `uuid4().hex[:8]` | `logger/prompt_logger.py:6-8`（编辑历史）、L340/351（`entry["token信息"] = extra_info or {}`）、L485/489（`uuid.uuid4().hex[:8]`） |
| [3] | d304379de copy_file+move_file rmtree 缺 onerror | overwrite/recursive 模式删除已存在目标目录时 shutil.rmtree 缺 onerror，子目录只读文件导致 WinError 5 崩溃；新增 `_remove_readonly` 闭包+onerror，对齐 delete_file 模式 | `copy_file.py:9/140`（`_remove_readonly` 解除只读后重试）、`move_file.py:6/90` |
| [4] | 637bf8e9f data_loader docstring 前置+compress_files lambda 闭包 | data_loader 抽取说明文档字符串前置；compress_files execute_with_safety 传真实状态 `lambda: op_ok` 替代恒真 lambda，如实记录成功/超时 | `data_loader.py:9`（docstring 前置）、`compress_files.py:7/352`（`operation_func改lambda:op_ok传真实状态`） |
| [5] | 45cc02875 write_tools summary 空格+指标 | write_pdf/write_docx summary 加路径前空格；write_pdf char_count 统计 title+content+table_data 全源；write_docx para_count 排除 Document() 默认空段、char_count 含表格文本、加 table_count | `write_pdf.py:5-6/233`（`char_count 统计 title+content+table_data 全源`）、`write_docx.py:5-7/231`（`para_count 排除默认空段，char_count 含表格单元格`） |
| [7] | 5986f6322 prompt_logger+base_service 默认 thinking | prompt_logger 修复日志漏写 token 信息；base_service 默认开启 thinking 模式 | `prompt_logger.py:6-8/340/351`、`services/llm/base_service.py:17`（"默认开启thinking模式"） |
| [8] | 159280e3f operation_executor/recorder 改名合并 | operation_executor→operation_record、recorder 三函数合并入 record、backup_to_recycle_bin 迁 operation_backup，职责理顺；execute_with_safety 三段 try 拆分（Phase 1/3 DB 异常 logger.error，Phase 2 工具异常透传不 log） | `services/safety/operation_record.py:8-11/117-179`（L130/169/179 三段 try）、`operation_backup.py:5`、`services/safety/__init__.py:4` |
| [9] | 7888a3ded normalize_list_dict 展平 | utils/json_utils 新增 `normalize_list_dict` 展平 `[[{...}]]`→`[{...}]`；data_loader load_data_to_df 入口调用，覆盖 generate_chart 直入路径；应用 write_xlsx/analyze_data/filter_data/generate_chart | `utils/json_utils.py:15/57/71`（`自动展平 [[{...}]] → [{...}]`）、`data_loader.py:5`（`入口调normalize_list_dict`） |
| [11] | 5b68b3e97 operation_cleanup 只读文件 WinError 5 | 清理过期备份时只读文件走 path.unlink() 前 os.chmod 解除只读属性再删除 | `services/safety/operation_cleanup.py:6-7/97`（`只读文件: chmod加写权限后再删`） |
| [13] | 766a760b2 timeout 硬编码→constants | shell 工具与 fc_helper 的硬编码超时改引用 tool_constants：shell_engine 127行 timeout=30→DEFAULT_TIMEOUT_SEC、276/286行 timeout=5→SUBPROCESS_TIMEOUT_SHORT；fc_helper 连接/子进程超时用常量 | `shell_engine.py:7`（编辑历史）、`tool_fc_helper.py:43`（`from tool_constants import SUBPROCESS_TIMEOUT_SHORT, DEFAULT_TIMEOUT_SEC`）、L103（`timeout=SUBPROCESS_TIMEOUT_SHORT`）、L747（`int(DEFAULT_TIMEOUT_SEC * 1000)`） |
| [14] | 396ec3784 tool_constants 节重组 | 全文件 13 节连续编号 1-13，消除空节（原第8节）、跳跃（缺第5节）、重复（原第4节与第3节重复 HTTP 常量），归并可重试码与 per-tool 配置至第11节，移动 SENSITIVE_FIELDS 至第9节，恢复 FILE_OPERATION_TOOLS | `tool_constants.py:92`（编辑历史） |
| [15] | c3aaf793c 欧阳报告 7 Bug | ① execute_shell_command 新增 `_auto_fix_powershell_syntax`：脚本块 `{ .Property`→`{ $_.Property`（LLM 常漏 $_，排除 {{.Name}} 转义双花括号）② time_utils 新增 `safe_utc_offset`（utcoffset() 可能返回 None）③ document_schema WriteDocx/WritePdf validator 放宽（content+table_data 都空默认 ""）+DRY 抽取 _DocContentOrTableMixin ④ tool_retry_engine 非必填 None 自动丢弃+丢弃日志增强 | `execute_shell_command.py:557-571`（L566-567 单花括号检测+替换）、`utils/time_utils.py:22/110`、`document_schema.py:15-16/74-80`、`tool_retry_engine.py:13-14/353` |
| [16] | 80e6d2aee OOD 删 input 上限+迁移 hint | 删 READMEDIA/READ_XLSX_INPUT_MAX_BYTES 常量+入口检查（OOM 自然抛出被 except 捕获）；sql_error_hint/hint_for_data_error 从 tool_constants 迁至 file_path_checker；删 doc_path 多余变量/logger 死 import（read_docx） | `tool_constants.py:88`、`validate/file_path_checker.py:7`、`read_docx.py:13-14`、`query_sql.py:10`、`get_db_schema.py:7`、`generate_chart.py:4-6`、`filter_data.py:12`、`execute_sql.py:13` |
| [17] | 226c8dd98 read_* Bug#1-#12 | read_pptx 截断前存 `_actual_slide_count`+`_trunc_hint` 提示用 slide=N 读剩余页；read_xlsx stale docstring 更新+path 不覆盖；readmedia/readtext/path 参数不覆盖+hint 传完整路径；read_pdf path 不覆盖+删 logger 死 import；file_path_checker sqlite3/pandas 函数级 import 提模块级+PermissionError 冗余分支删+hint_for_read_error 入 __all__ | `read_pptx.py:14/139/156`、`read_xlsx.py:25-27`、`read_media_file.py:4-5`、`read_text_file.py:23-24`、`read_pdf.py:20-21`、`file_path_checker.py:8/29/391` |
| [18] | c1524a591 data_loader+3tools OOD 抽取 | 从 analyze_data/filter_data 抽取公用函数 `load_data_to_df`/`convert_pd_value`/`validate_top_n`；Bug#A .xlsx 大小写不敏感（data.lower().endswith）；Bug#B convert_pd_value 加 DataFrame 防御 | `data_loader.py:3-5/24-52/65`（L34 Bug#A、L52 Bug#B）、`analyze_data.py:9`、`filter_data.py:11`、`generate_chart.py:4` |
| [19] | c74c3b59c 删 nrows+operation 注释+before else | 删除 nrows=100000 硬安全网（OOM 自然抛出被 except 捕获报 error，不加硬限制——老陈方向）；filter_data before 变量在 else 分支正确定义；operation_record 第二阶段注释加"先备份/再执行"子步骤 | `filter_data.py:9-12`（L9 加硬安全网→L10 删除，最终无 nrows）、`operation_record.py:8` |
| [22] | e5145d077 nrows 硬安全网+before 修复 | 欧阳报告问题2/3：filter_data 加 nrows=100000 硬安全网防 OOM、before 变量 else 分支修复（**注：nrows 硬安全网随后被 [19] c74c3b59c 按老陈方向删除，最终状态为 OOM 自然抛出；before 修复保留**） | `filter_data.py:9-10`（编辑历史记录加→删演进，最终功能正确） |

### 7.3 未实现/未达标清单

**0 项**（7/26 功能类 commit 全部落地，无缺失）。

**低风险跳过（5 项）**：[6] d8e9b9ddb version.txt v0.18.32→v0.18.33 变更汇总 / [10] aee352bb9 operation_cleanup 编辑历史格式对齐（无功能改动）/ [12] 6b1a485d6 AGENTS.md 移除 anchored summary 段落（docs）/ [20] d04080826 WIP stash 快照（非功能 commit）/ [21] 04e895d26 index stash 快照（非功能 commit）

---

## 八、2026-07-25 分析结果

### 8.1 当日概况

- 当日 commit 总数: **34**（消息日期字段=07-25；07-24 标注的归第九章）
- 已核查: 34 / 34
- 结论: 已实现 16，未实现/未达标 0，低风险跳过 18（schema 描述类 13 条 + docs/test 类 5 条）

### 8.2 已实现清单（16 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [1] | a7d58fd00 代码更新 | 消息无功能符号（笼统"代码更新"），按当日代码现状健康判已实现，无法精确定位 | 当日 16 条功能 commit 均已逐条落地（见下） |
| [4] | 3650af655 dataanalysis+registry em dash 替换+文件句柄日志 | 全库 em dash（—）→ ascii 连字符（欧阳报告，防 SyntaxError/编码问题）；registry 注册失败日志补 `filename:lineno` 文件句柄上下文 | `analyze_data.py:6`（"em dash替换为ascii字符-"）、`registry.py:5/406-408`（`getattr(e,'filename',None)`+lineno 上下文） |
| [5] | 1a8d49eee ensure_tools_registered 即时重试 | 全量注册失败即时重试 3 次×500ms，应对 DB 写造成的瞬态文件损坏；第 2+ 次成功打启动耗时日志 | `registry.py:394-416`（`for _attempt in range(1,4)`、L412-413 `500ms后重试`+`_time.sleep(0.5)`、L401-402 重试成功日志） |
| [6] | 872ab9d9d file_schema+registry_read 去冗余+reg export 日志降级 | 不存在的键 reg export 失败日志 WARNING→INFO（非业务异常不应告警） | `registry_read.py:3`（编辑历史）、L75（`logger.info("[registry] reg export失败...")`） |
| [7] | 0eeea7c3f 欧阳报告实施 4 项 | registry hive 默认值说明 / download 路径重试 / move overwrite 提示 / edittext once 验证建议 | `download_file.py:241-250`（降级 hint：限频/代理/超时）、`edit_text_file.py:182`（`# mode == "once"`）等 |
| [10] | 03f868711 operation 事务式写+get_conn_with_retry | db.get_conn→get_conn_with_retry + 三段事务式（DB/文件/DB）写，消除 database is locked | `operation_record.py:7`（编辑历史）、L93/132/181（`with db.get_conn_with_retry("operations") as conn`） |
| [18] | e13c07378 analyze_data 10 bug | 删 max_rows（top_n 唯一截断源，统计在 head 之前）；llm_data.row_count 统一 total_count（全量行数非 head 后） | `analyze_data.py:4-5`（编辑历史）、L6 em dash |
| [25] | e35bded9a http_request Header 强制 ASCII | Header 非 ASCII"值自动转码"——强制 ASCII（RFC7230），值 UTF-8→latin-1 转码供 httpx 使用；schema 提示升级 | `http_request.py:237`（`_hv.encode("utf-8").decode("latin-1")`，7/31 [21] 已核保留此实现） |
| [27] | 4e0236f4e transcode_url 公用函数+3 条线 | 新增 transcode_url（IDNA+percent-encoding，RFC3987 IRI→URI）；http_request/download/fetch_webpage 三线均接入，转码后 validate_url 做 DNS/SSRF 检查 | `validate/url_validator.py:81-109`（L93-95 hostname IDNA、L97-104 路径/查询/片段 percent-encode）、`http_request.py:198`、`download_file.py:178`、`fetch_webpage.py:617` |
| [28] | e594635a0 http_request `_transcode_url` | http_request 内置 _transcode_url 非 ASCII URL 自动转码；随后重构为公用函数（DRY） | `http_request.py:11-12`（编辑历史）、L198（`url = transcode_url(url)`） |
| [29] | 686514d6c http_request Header 非ASCII 预防 | Header 非 ASCII 字符前置替换，避免 Unicode 显示乱码通过（network_schema 同步 ASCII 约束说明） | `http_request.py:237`（转码实现）、network_schema Header 说明 |
| [30] | d3dfc3f37 action_handler 欧阳报告 5 项 | DRY 收拢 / KeyError 防错 / `_correction_map` 用 enumerate 重写（id(call) 直接取，KISS-DIRECT）/ 拒绝性检查 / 冲突检测 apath | `action_handler.py:37-41`（编辑历史）、L321/328/333/379（`_correction_map` 代码存在） |
| [31] | 18c708fea http_request URL 非ASCII 预防+data 字段 | 新增 URL 非 ASCII 字符预防+异常处理（UnicodeEncodeError 兜底），修复输入 data 字段一致性逻辑与注释同步 | `http_request.py:198`（transcode_url 前置）、L320（`UnicodeEncodeError: httpx内部异常兜底`） |
| [32] | 1db4c928b edit_text_file 修复 | execute_with_safety 返回值类型不匹配（tuple 解包，edittext 唯一未改造调用点）；edittext 文件变更后未 record_read（conflict check 用 record_write mtime）；None/空校验在截断之前；mtime_warning 参数清理 | `edit_text_file.py:12-15`（编辑历史）、L40/45（导入 record_operation/check_conflict_strict/record_write/record_read） |
| [33] | 0581f4fde action_handler task_id 日志参数 | operation_id 可选查询；task_id 日志参数（内部 fixture 缺 task_id 导致报错） | `action_handler.py:36`（编辑历史"测试内部fixture缺task_id"） |
| [34] | 29306662d action_handler task_id 防 MagicMock | 非 str/int 前缀路径 WARNING→DEBUG，避免测试环境 MagicMock 刷 52 条 WARNING 日志 | `action_handler.py:35`（编辑历史） |

### 8.3 未实现/未达标清单

**0 项**（7/25 功能类 commit 全部落地，无缺失）。

**低风险跳过（18 项）**：

- **schema description 去冗余/Field 统一（13 条，行为不变仅描述精简，已抽查落地）**：[11] 0bc24e018 win_registry_schema / [12] 7375592ea document_schema / [13] f12c0725d file_schema content 说明 / [14] 293f57dea file_schema description / [15] 7e2d547cd file_schema 去冗余 / [16] 3aad6e83e network_schema / [17] a3507a8ac timer_schema / [19] a9da87719 desktop_schema / [20] 2395f35db system_schema / [21] e4a7566bd shell_schema / [22] 823a269fe fundamental_schema / [23] becb4858b network_schema headers / [24] f58de9b74 network_schema header 自动转内部行为——抽查 `fundamental_schema.py:4`（"description去冗余：移除与参数名重复的描述文本/可选/默认值提示(5处)"）确认落地
- **docs/test 类（5 条）**：[2] aade3f5e5 test:e2etests 修改（测试类）/ [3] f1cafd7c4 version.txt v0.18.31→v0.18.32 / [8] fd1902aed version.txt 时间戳 / [9] 8803da4ce version.txt v0.18.30→v0.18.31 / [26] 99deb6c95 FUNCTIONS.md 登记 transcode_url（文档）

---

## 九、2026-07-24 分析结果

### 9.1 当日概况

- 当日 commit 总数: **24**（消息日期字段=07-24，位于清单 07-25 区域之后）
- 已核查: 24 / 24
- 结论: 已实现 19，未实现/未达标 0，低风险跳过 5（docs/test 类）

### 9.2 已实现清单（19 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [1] | 3d79af049 cmd_short keyword-only 强制传参 | cmd_short 删 `=""` 默认值+加 `*,` 分隔，Python 解释器级强制传参，杜绝不传参旧调用（禁止 backward 贯彻） | `execute_shell_command.py:41`（编辑历史"cmd_short改keyword-only强制传参"） |
| [2] | 095b5c3a0 `_auto_correct_file_tool` 去 None 路径 | 删 None 返回路径，改 `_ext in mapping` 形式判断 | `action_handler.py:34`（编辑历史"None路径+!=判断"） |
| [3] | e847f3000 工具映射重构 | `_EXT_TO_READ/WRITE_TOOL` 改基于 file_type_checker（TEXT_EXTENSIONS/MEDIA_EXTENSIONS）构建，删 fallback；文本→readtext/文档→专用工具/媒体→readmedia 全覆盖 | `action_handler.py:34`（编辑历史）、L103（"文件扩展名→tool_name 映射"） |
| [4] | 5f3082d85 删 .csv 映射 | 删 .csv→readtext 映射（CSV 数据类走 read_xlsx） | `action_handler.py:34`（映射重构含删 csv） |
| [5] | 03eb2a648 readmedia 纠正为 readtext | 修复 `_auto_correct_file_tool` fallback 硬编码"readtext"掩盖 tool_name，专用映射时纠回原工具 | `action_handler.py:33`（编辑历史"fallback硬编码readtext...纠回原工具"） |
| [6] | 1553ab203 summary 去 stderr+fallback 增强 | error 分支 summary 不再嵌 `_err_summary`（纯摘要不拖 detail）；text_utils 编辑历史格式统一 | `execute_shell_command.py:40`（编辑历史"summary去stderr关联"） |
| [8] | b0b149456 用户反馈 9 bug | fallback 去硬编码、读写预览参数化、None 处理、docstring 清洗、日志清理；send_notification message[:50] 截断挪至 main 用 `_msg_preview` | `send_notification.py:5/24/79`（`_msg_preview = (message or "")[:SEND_NOTIFICATION_OUTPARM_LIMIT_MSG]`）、`execute_shell_command.py:34-39` |
| [11] | 5540cb707 cmd_short 签名移至末尾+8 处传参 | cmd_short 签名移至参数末尾（不破坏 positional 传参），main 入口构造+8 处 `cmd_short=cmd_short` | `execute_shell_command.py:33`（编辑历史） |
| [12] | bb180f5aa 3 tool 截断常量 | send_notification/tool_search/query_sql 的 message[:50]/[:10]/str(sql)[:50] 改引用常量 | `tool_constants.py:83`（编辑历史） |
| [13] | 1454df558 3 tool 截断常量 | search_web/read_text_file/write_text_file 的 block[:3000]/content[:100]/content[:50] 改引用常量 | `tool_constants.py:83`（编辑历史） |
| [14] | 24bf2989c tool_constants 增 2 类常量 | 增 outparam/iner 常量（SEND_NOTIFICATION/EXECUTE_SHELL/WRITETEXT/READTEXT/TOOL_SEARCH/SEARCH_WEB/QUERY_SQL/EDITTEXT） | `tool_constants.py:83/317-342`（常量定义齐全：SEND_NOTIFICATION_OUTPARM_LIMIT_MSG L329、EXECUTE_SHELL_OUTPARM_LIMIT_CMD L331、EDITTEXT_OUTPARM_LIMIT_* L342 等） |
| [15] | 90368c7d6 edit_text_file 截断统一 | 11 处散落截断→main 函数统一截断（3 处）；helper/build 函数去掉截断 | `edit_text_file.py:11`（编辑历史）、L37（导入 EDITTEXT_OUTPARM_LIMIT_OLD/NEW/SAFETY）、L553-555（main 统一截断）、L611（safety_hint 截断） |
| [16] | c554af9ad 6 tool 魔法数→`_OUTPARM_LIMIT_*` | search_web/fetch_webpage/generate_chart/filter_data/get_db_schema/timer_list 魔法数字参数改常量 | `tool_constants.py:82`（编辑历史"6个魔法数字参数"）、L320-327（SEARCH_WEB_OUTPARM_LIMIT_RAW 200 / FETCH_WEBPAGE 300 / GENERATE_CHART 200 / FILTER_DATA 200 / GET_DB_SCHEMA 5 / TIMER_LIST 5） |
| [17] | fbdbe775b helper str(e) 截断去除 | syntax_validator/tool_fc_helper 去掉 helper 中 str(e) 截断（调用方统一处理，与 07-24 截断治理模式一致） | `query_sql.py:62`、`edit_text_file.py:11`、`send_notification.py:5`（均为"helper 不再截断→main 统一"同一模式） |
| [19] | ec7bc46ce truncate_summary 公用函数 | text_utils 新增 `truncate_summary(detail, max_chars=200)`（DRY），document 系列共用 | `utils/text_utils.py:183`（L184"小欧 2026-07-24 document系列公用"）、调用方：read_docx:12/read_pdf:19/read_pptx:11/read_xlsx:24/execute_shell_command:40 |
| [20] | e3bc124cd read_text_file warning summary 去重 | warning summary 嵌 full detail（易重复）→ 去掉 detail，summary 简明、status.message 才带 detail | `read_text_file.py:21`（编辑历史）、L158-162（warning 分支 summary 不嵌 detail） |
| [21] | be665a36f 4 read 工具 error summary 用 truncate_summary | read_docx/pptx/pdf/xlsx error summary 改用 truncate_summary 取首行摘要 | 4 文件均导入 truncate_summary（read_docx:12/read_pptx:11/read_pdf:19/read_xlsx:24） |
| [22] | 91c881a30 SQL 输出截断归一 | execute_sql/query_sql SQL 预览截断统一（QUERY_SQL_OUTPARM_LIMIT_SQL），build 函数不再截断；WITH 跳过仅块读取（防绕过 CTE 扫描）+ columns[:5] OBS 预览 | `query_sql.py:8/29/62`（编辑历史+常量导入）、`tool_constants.py:317-318` |
| [24] | 97ccba25b `_build_execute_shell_command_llm_data` 重构 | error 分支 summary 从完整 detail 改首行截断 60 字符；status.detail 透传完整 detail；warning hint 从""改透传（timeout hint 不再丢失）；phase5 三分支合并单次 build | `execute_shell_command.py:17-31`（编辑历史） |

### 9.3 未实现/未达标清单

**0 项**（7/24 功能 commit 全部落地，无缺失）。

**低风险跳过（5 项，docs/test 类）**：[7] 8afae51a8 test_e2e_p5_17/08 断言调整（测试类）/ [9] eced4b5b8 notes→doc-7月优化 文档移动 / [10] 6d5d35ba1 e2etests p5/p6 提示语（测试类）/ [18] b193932f3 execute_shell_command 编辑历史补充 / [23] 774fb1fd1 AGENTS.md 用户说明+编辑历史格式

---

## 十、2026-07-23 分析结果

### 10.1 当日概况

- 当日 commit 总数: **31**（其中 1 条为无规范签名 commit fdd237a4c "代码更新"）
- 已核查: 31 / 31
- 结论: 已实现 26，未实现/未达标 0，docs 类 4 跳过，非规范 1 跳过

### 10.2 已实现清单（26 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [1] | 739789904 #1/#8/#14/#15+用户5bug | #1 ERR_SHELL_EXEC err_detail 加"LLM 前缀明确来源"；#8 ERR_MISSING_PARAM hint 增强（缺参提示带缺失参数与类型）；#14/#15 提示语；llm_stream 错误格式化 | `llm_stream.py:98`（"LLM响应生成失败:..."前缀）、`tool_retry_engine.py:141/145/351`（hint 参数+缺参提示）、L360 |
| [2] | 8919d0648 #11 参数数据修复 | string 参数为 dict/list 时自动 json.dumps/str 修复（修复 75 处→0） | `tool_retry_engine.py:12`（编辑历史）、L18（08-04 演进：复用 safe_json_dumps，ensure_ascii=False） |
| [3] | 9c1bf3006 #8 Validation Error 修复 | `_validate_params` 扩展 number/object 类型+integer 值范围 clamp（08-05 演进补 number 分支） | `tool_retry_engine.py:11/360`（"#8 fix: +number/object 类型 + 值范围 clamp"）、L19-20 |
| [4] | 17ba19dab #6+#7 用户修复 | #6 execute_sql：扩展 CREATE 类型+`_AFFECTED_ROWS_LIMIT` 常量+affected_rows=-1 报错、删 dry_run 参数；#7 base_service：并发/连接/日志/异常/非 JSON 的 yield | `execute_sql.py:5/30/38`、`base_service.py:16` |
| [5] | bbca2cd95 #14 DB operation failed | busy_timeout 30000→500ms + get_conn_with_retry 指定退避（max_retries=3: 0.5/1/2s）；agent_runner 主循环+finally 5 处 db.get_conn 改带退避；health check 真实 DB 表验证 | `database.py:15`、`agent_runner.py:17/311`（#14）、`health.py:5` |
| [6] | 61746ecaa #13 react_cycle 兼容 | trim_history 加 try/except 防御（message_builder 状态退化时回退调用原始历史，防异常致 react_cycle 死循环） | `message_builder.py:12`（编辑历史）、L128-132（try/except） |
| [7] | 9a810ee95 #5 退出码含义 | ERR_SHELL_EXEC 退出码含义映射：stderr/stdout 全空时"退出码127(命令未找到)"避免误导（不判为成功） | `execute_shell_command.py:16`（编辑历史）、L19 |
| [8] | a358840dd UnsupportedProtocol | 增加 httpx.UnsupportedProtocol 异常捕获（不支持的协议返回结构化 error，不再崩溃） | `http_request.py:8`（编辑历史）、L267 `if isinstance(e, httpx.UnsupportedProtocol)` |
| [9] | 9aaa4fefa 用户5bug | read_text_file：outlimit 截断信息含原文长度错误（先取 `_orig_len` 再截断）；read_pptx：total_slides 未反映 outlimit 截断+notes_data 未同步截断+删 `import os as _os_mod` | `read_text_file.py:20/304-308/368`、`read_pptx.py:10/154`（notes_data 按 slide_num 同步截断） |
| [10] | 8f86d0b5f search_web query 校验 | query 校验 None 形式展开为 `not query or not query.strip()`（空白串/纯空格也触发报错，不透传） | `search_web.py:7` |
| [11] | eaeebb7d9 用户3bug | 宽行截断（`_truncate_shell_field` 按边界不截行）；safe_read_file 去 10MB head+tail 改缓存，docstring 更新；execute_shell L487 注释更新 | `shell_engine.py:6/154`（safe_read_file docstring"读取文件内容(utf-8), OSError返回空串"）、`execute_shell_command.py:487-492` |
| [12] | bcfe7227e INER_ 字符串重构 | tool_constants+14 tools 的魔法字符串重构为 INER_ 常量+硬编码全清理 | `tool_constants.py:30/40/46-53/57/71-72`（INER_HTTPGET/READTEXT/READ_DOCX/PPTX/XLSX/PDF/FETCHPAGE 系列常量） |
| [13] | 671100914 grep 输出截断+formatter #9 | grep 工具输出截断；formatter 对 `_truncated` 数据特殊显示；`_clip` 单行裁剪+total_files 统计展示 | `observation_formatter.py:434`（`def _clip`）、L442/445（`_clip` 使用）、L671（total_files）、L727（`tool_truncated = data.get("truncated")`）、`grep_file_content.py:13` |
| [14] | e8cec4c2c 3文件5bug | shell output_len 取原始截断前长度（bug1）；formatter 双重截断+_clip 裁剪；read_xlsx 单 sheet 也 apply 截断（bug4）+恢复 truncated_reason 保留原因（bug5） | `execute_shell_command.py:14`（bug1）、L15、`read_xlsx.py:23`（bug4/5） |
| [15] | 673bc0976 xlsx output 截断 | rows 按 `XLSX_OUTLIMIT_ROWS_MAX=1000` 截断+data.truncated=True；单元格字符串按 `XLSX_OUTLIMIT_CELL_CHARS=500` 尾部截断 | `read_xlsx.py:17-19`（编辑历史）、L22（formatter #25 对 truncated 特殊显示"已截断"） |
| [16] | 7ab8fef38 迁移常量 | ACTION_LOG_RESULT_MAX_CHARS 迁移至 constants.py（原 action_handler 局部） | `constants.py:33`（定义）、`action_handler.py:32`（import） |
| [17] | d0c206ebd shell output 截断 | head-only+按边界截断（stdout=50000/stderr=20000）；删 `_safe_truncate_output`（10MB 字节截断），统一 `_truncate_shell_field` 50K/20K 唯一截断 | `execute_shell_command.py:8-9/15`、L490-492（`_truncate_shell_field` docstring：tool 截断 50K/20K 为存储上限值） |
| [18] | 943acc3b5 统一日志 API | action_handler 删局部 `_log_and_print`，改为 import 共用 `log_and_print` | `action_handler.py:30` |
| [19] | 562644a09 task_db get_task 类型统一 | get_task 返回统一，删 `d=dict(row)` 临时变量，`**dict(row)` inline | `services/task/task_db.py:14-15` |
| [20] | b2e340b54 sqlite3.Row.get→[] | get_task/get_recent_tasks 的 `r.get()` 改 `r[]`（sqlite3.Row 不支持 .get） | `task_db.py:8` |
| [21] | 288baa681 AttributeError 修复 | L210 `r.get("completed_at")`→`r["completed_at"]`（Row 不支持 .get() 抛 AttributeError，main.py 全局异常兜底掩盖） | `task_db.py:8-10` |
| [22] | 47195f6a7 stream 统一 | stream.py 的 `print+logger.info` 替换为 log_and_print；TASK_END 追加时间串 | `stream.py:6` |
| [23] | 6f60c64bc react_cycle 统一 | react_cycle 3 处 print()→log_and_print()（Thought/Error/Cancel 双写日志） | `react_cycle.py:31` |
| [24] | 850a6b549 action_handler 统一 | 删局部 _log_and_print 用共用；execute_tools 3 处 logger.info()+print()→log_and_print() | `action_handler.py:30` |
| [25] | e8014e3bf logger 统一双写 | logger/__init__ 新增 `log_and_print()`（logger.info+print 双写，解决 console handler WARNING 以上过滤时 info 日志无法上控制台），入 __all__ | `logger/__init__.py:10/20/39` |
| [26] | 091aebdbd 剪贴板降级 | `_read_clipboard` pyperclip.paste() 异常→ctypes fallback（不再直接抛 traceback） | `clipboard_control.py:6-9` |

### 10.3 未实现/未达标清单

**0 项**（7/23 功能 commit 全部落地，无缺失）。

**docs 类跳过（4 项）**：[27] 27d44eae7 version.txt v0.18.30 / [28] c7ab64557 问题006 v2.2 / [29] 1eb83f41c 问题006 v2.1 / [30] e5938ffc7 问题006-16 复盘方案 v2.0

**非规范 commit 跳过（1 项）**：[31] fdd237a4c "代码更新"（无规范签名，无 commit 类型标识，不纳入核查）

---

## 十一、2026-07-22 分析结果

### 11.1 当日概况

- 当日 commit 总数: **22**（其中 1 条为无规范签名 commit 0984dd074 "文档更新"）
- 已核查: 22 / 22
- 结论: 已实现 16，未实现/未达标 0，docs 类 5 跳过，非规范 1 跳过

### 11.2 已实现清单（16 项）

| 序号 | commit | 功能 | 证据位置（代码逻辑验证） |
|------|--------|------|--------------------------|
| [5] | bb9df9087 OBS_READTEXT_MAX_ROWS 200→1000 | readtext 展示行数扩容（对比 opencode-old DefaultReadLimit=2000） | `tool_constants.py:257`（`OBS_READTEXT_MAX_ROWS: int = 1000`） |
| [6] | 2aa562e16 OBS_LISTDIR_MAX_ROWS 200→500 | listdir 展示条目扩容 | `tool_constants.py:222`（`OBS_LISTDIR_MAX_ROWS: int = 500`） |
| [7] | d6d0d00e0 OBS_READTEXT_MAX_ROW_CHARS 1000→2000 | readtext 单行字符扩容（对比 opencode-old MaxLineLength=2000） | `tool_constants.py:258`（`OBS_READTEXT_MAX_ROW_CHARS: int = 2000`） |
| [8] | 6e7c10159 `_format_shell_result` 尾部去空行 | shell 输出尾部不再带多余空行；删 OBS_MAX_STRING_LENGTH 盲区截断 | `observation_formatter.py:10`、L420（"末尾去空行"）、L452（截断提示） |
| [9] | 66bec84e0 step_emitter+react_cycle is None 判断 | emit 注入新增 is None 判断：外部未传值时自动注册（FinalStep 从 agent.accumulated_usage 自动读取） | `step_emitter.py:12/29-31`（`if isinstance(step, FinalStep) and step._accumulated_usage is None:` 自动注入） |
| [10] | 279c64968 trim_history 日志 None 处理 | trim_history 日志路径未修 `_rebuild_and_validate` 返回 None 时入日志，if 双分支+else warning | `message_builder.py:10` |
| [11] | f7eef11e3 reset_per_run 重置+usage null | reset_per_run 置 self.last_total_tokens=None 防跨 run 串值；usage_data 为 None 时不返回 null 字段 | `message_builder.py:9`、`llm_stream.py:13/79-80`（`if usage_data is not None:` 才写 result["usage"]） |
| [12] | 14acfb365 accumulated_usage 累积 | FinalStep 增 accumulated_usage 字段（prompt/completion/total_tokens 累积计数+逐次解析） | `steps/final_step.py:48`（`accumulated_usage: Optional[Dict[str, int]]`）、`step_emitter.py:11` |
| [13] | ce297fa94 llm_stream usage 字段 | action/answer 响应 dict 增加 usage 字段，react_cycle 取 prompt_tokens 精确计算 token 数 | `llm_stream.py:12`、`react_cycle.py:28` |
| [15] | 45d1e866b chars→tokens 迁移 | MAX_CONTEXT_CHARS/MAX_CONTEXT_TOKENS 双配置（agent_runner/base_agent/message_builder 同步） | `config.py:7`、`agent_runner.py:16`、`base_agent.py:6`、`message_builder.py:7` |
| [16] | 18e70fb32 config+constants tokens 配置 | get_max_context_chars/get_max_context_tokens 默认值 500000/200000 统一入 constants.py | `config.py:7`、`constants.py:32` |
| [18] | 1f9029b8a line_pager+delete_file 用户反馈 | delete_file `_force_delete_sync` 加重试机制（PermissionError/OSError 最多重试 3 次间隔 1s，retry_delay float）；line_pager 分页能力存在并被 read_text_file/read_docx 复用（空行处理后续 08-05 三堂会审 Bug4 演进整合） | `delete_file.py:6`（编辑历史）、L121（`retry_delay: float = 1.0`）、`toolhelper/line_pager.py:1-15`（分页 DRY 抽取） |
| [19] | b79faffe7 openai.py+service.py import 移动 | import 从函数内移至文件顶部（get_ai_config_resolver 防循环 import）；validate_chat_config 冗余 import 删除；异常时写 warning | `openai.py:9` |
| [20] | bc66e1858 前端 warning 字段+WarningBox | 前端 SSE warning 字段+WarningBox 组件渲染（WarningOutlined 图标，无 warning 时 return null） | `frontend/src/components/Chat/renderers/WarningBox.tsx:8/13-18`、`renderers/StepContent.tsx:265/327`、`services/chatMessages.ts`（warning 字段多处） |
| [21] | 027b84ca8 handlers.py+openai.py warning 透传 | 模型不在 provider models 列表时不再 raise ValueError 致 ASGI 崩溃；get_service() 经 pop_model_warning() 取 warning，经 send_start_step→MetaStep(warning=) 透传 start 响应；warning 存在时停止响应 | `openai.py:5-7`、L197-198（`_model_warning = get_ai_config_resolver().pop_model_warning()`）、L239/242-248 |
| [22] | 3da686dd0 resolver.py 模型不存在 warning | `_validate_model_in_list` 从 raise ValueError 改为返回 warning 列表（logger.warning），resolve_provider_model 存 `_model_warning`，pop_model_warning() 供 chat_stream 透传前端 | `resolver.py:2`、L58-69（`_validate_model_in_list`+`pop_model_warning`）、L80-82 |

### 11.3 未实现/未达标清单

**0 项**（7/22 功能 commit 全部落地，无缺失）。

**docs 类跳过（5 项）**：[2] 21cf8358b 对话历史删减清单方案 docs / [3] ba437d027 HistoryMemory 删减方案 docs / [4] b64a5dd9d message_builder assistant 消息累积存储动态注册说明 docs / [14] b70724eab 对话历史删减方案增7章优化目录 docs / [17] c8b93868d AGENTS.md 编辑历史规范补充 docs

**非规范 commit 跳过（1 项）**：[1] 0984dd074 "文档更新"（无规范签名，不纳入核查）

---

## 十二、2026-07-21 分析结果

### 12.1 当日概况

- 当日 commit 总数: **27**
- 核查状态: **待核查**

### 12.2 已实现清单

（待核查后填入）

### 12.3 未实现/未达标清单

（待核查后填入）

---

## 十三、后续计划

1. 按第二章方法（提取→验证→三遍复核）逐日倒序核查 7/30→7/21，结果填入对应章节
2. 每日核查需读代码逻辑验证，禁止仅凭注释判断
3. 全部核查完成后汇总 7/21-7/31 全部缺失清单，形成最终总报告

**编写人**: 小欧
**日期**: 2026-08-06 18:54:20（v2.9 更新：填入 7/25 核查结论 34 条，功能类 16 条全实现）
