# 系统 model 表示结构化分析报告

**版本**: v1.27
**创建时间**: 2026-08-22 20:58:05
**更新时间**: 2026-08-23 07:44:06
**编写人**: 小欧

---

## 版本历史

| 版本 | 时间 | 更新要点 | 作者 |
|------|------|---------|------|
| v1.20 | 2026-08-22 21:54:30 | 全文一致性核查（10遍）：纠 6.3/6.7 残留"保留列"与旧列名，统一为单列归一 | 小欧 |
| v1.21 | 2026-08-22 22:03:46 | 三堂会审 3 轮复核后补遗漏：前端 `SessionModelOverride.api_base`、监控 `json_extract` 查询改写（各 3 遍合规/合理/关联核查） | 小欧 |
| v1.22 | 2026-08-22 22:10:50 | 严格 10 遍读文档+10 遍读本地码，3 轮三堂会审：发现 8 处实质遗漏/错误（F1-F8），已直接修正进第六章各对应 diff（调用点同步/监控双写路径/DDL归属/前端契约稳定/读者同步/删未定义调用/清除backward兼容），不再单列第七章 | 小欧 |
| v1.24 | 2026-08-22 22:32:05 | 纠前版 6.6 B3 之严重错误：原「方案 A 保持对外 DTO 稳定、前端零改动」违反 AGENTS-1.5「前端随后端修改」铁律，已改为「方案 B」——后端 `ConfigUpdate/ConfigResponse/FullConfigResponse` 归一为 `ModelRef` 封装字段，前端 `api.ts` 契约必须同步改（补 6.6 后端+前端双向 diff 及调用点同步） | 小欧 |
| v1.25 | 2026-08-22 22:57:07 | 全文重读 10 遍+本地码熟读 10 遍+严格 3 轮三堂会审，发现并修正 15 处（G1-G15）：①v1.24 自身错误 3 处——伪导入路径 `app.services.model.refs`→`app.db.models.chat_models`、FIELD_HANDLERS 值误写 ModelRef 构造（实为 handler 映射）、FinalStep 违规保留 model/provider 委托 property；②重大关联遗漏 5 处——lifecycle 全链闭环（`_instance.model` 缓存判定/create_service_instance 构造点/`_current_provider` 结构态）、stream_orchestrator L2 sessionModel 覆盖+AE 还原双变量块归单 ModelRef 原子切换、resolver.validate_config 四元组与 /chat/validate 响应归一、validation.py 下游全链、base_service 内部 16 处 self.model/self.provider 迁移清单；③其余遗漏 7 处——storage 三读者(list_session_tasks/query_token_usage/load_user_messages_by_session)、config_helpers fail_result 与 FIELD_HANDLERS 合一、config_service update_config 返回、startinfo display_name 键消亡同步、create_*_chunk 签名、base_agent 假数据缺陷顺修、prompt_logger 签名归一、FullConfigValidationResponse 死DTO按YAGNI删除 | 小欧 |
| v1.26 | 2026-08-23 06:02:01 | 按本设计完成全量代码落地（后端 28 文件+前端 4 文件，未提交待北京老陈审核）；代码三轮三堂会审补设计同步 2 处：①storage.get_task_detail / load_user_message_by_task（SELECT * 读者）按 list_session_tasks 同模式派生 model/provider 键；②idx_token_model 索引改 json_extract(task_model) 表达式索引。验证：python compileall 全过、app.main 导入通过、ModelRef 序列化回环通过、前端 tsc 零新增错误(sse.ts 4 错误为既有) | 小欧 |
| v1.27 | 2026-08-23 07:44:06 | 对全部改动代码再执行熟读+严格三轮三堂会审，实锤 11 个问题并当场修复（BUG1-P0 agent_runner 回填 ModelRef(provider=None) 必抛 ValidationError 致 chat_user_message 终态回填 100% 失败→改条件构造；BUG2-P0 迁移吞异常→新增 _verify_model_ref_columns fail-loud；BUG3-P1 L2 换模 api_base 与 SDK 缓存不一致→base_service.reset_sdk()+换模/还原两处随调；BUG4-P1 create_final_response 零调用死函数被改造违反 YAGNI→删除；BUG5-P1 insert_task 死防御 else None 删除+sessionModel 补 NOT NULL；BUG6-P2 _ALLOWED_KWARGS 裸字段暗道删除；BUG7-P2 services/model/__init__ docstring 名实同步；BUG8-P2 monitoring/storage 顶层 app import 违背零依赖声明→函数内惰性导入；BUG9-P2 updated_fields 嵌套 null 噪声剔除；BUG10-P1 单例还原竞态（既有）根治——base_agent 新增 _task_llm_model 任务快照，react_cycle 三处/telemetry.finalize 改读快照；BUG11-P2 token_usage ?model= 语义收窄为当前 provider 成对过滤（设计接受, 记录备查）。11 处修复均补编辑历史条目。复验：compileall=0、import OK、tsc 零新增 | 小欧 |

---

## 一、设计要求（强制）— 小欧-2026-08-22

**1. 归一化范围**：除部分设置类（`ProviderInfo.models` 等纯配置元数据）不涉及外，**系统所有流程逻辑中对 `provider+model` 的处理一律归一为同一 `ModelRef` 结构**，覆盖读写、传输、流转各环节，确保无遗漏、便于统一处理与演进（北京老陈 2026-08-22 补充）。
**2. display_name**：后端流转零依赖 display_name，仅作前端展示派生；`chat_tasks.display_name` 列废弃，`SessionModelOverride.display_name` 仅作可选用户自定义别名，系统不拼接，缺省前端派生。
**3. ModelRef 形态**：`provider: str` + `model: str` + `api_base: Optional[str]`（端点定位，私有/本地必需）+ `display_name: Optional[str]`（仅别名）；**不纳入**调用参数与目录元数据。
**4. 命名铁律**：凡承载 `provider+model` 组合的变量，**严禁裸 `model`/`provider`**，必须 `前导+model` 且名副其实（四章改造表"变量名"列已逐项按此落地：`task_model`/`chunk_model`/`step_model`/`final_model`/`resolved_model`/`tele_model`/`llm_model` 等）；允许裸 `model`/`provider` 仅限真正单值场景（如拼 `{"model": model}`）且须注释。

---

## 二、分析背景与目标 — 小欧-2026-08-22

北京老陈 2026-08-22 在 `chat_sessions.sessionModel` 落地了系统内**首个结构化模型定义** `SessionModelOverride(provider+model+display_name?)`，作为规范模板。

本报告目标：以专业视角，全面、周到、合乎逻辑地梳理**全系统中所有"模型身份"的表示点**——
- 数据表（持久化层）中 yield 给前端的数据；
- 中间变量（LLM 层、Agent 编排层、监控遥测层）；

判断其是否可复用同一结构化模板（provider+model 结构），并给出统一改造的优先级与风险。归一化目的与三项设计基准详见第一章设计要求。

---

## 三、现状总览：5 种表示形态、21 个落点 — 小欧-2026-08-22

当前全系统"模型身份"的表示**不统一**，散落 5 种形态：

| 编号 | 形态 | 说明 | 代表落点 |
|------|------|------|---------|
| ① | JSON 结构 | `SessionModelOverride(provider+model+display_name?)` | **仅** `chat_sessions.sessionModel` ✅ |
| ② | 两列分离 | `provider` + `model`（TEXT/字段分离） | chat_tasks、chat_user_message、token_usage、llm_calls、task_metrics、Config DTOs(ai_provider/ai_model, current_provider/current_model)、BaseAIService、LLMClient、ChatResponse、FinalStep、StartStep、ErrorStep、ReasoningStep 基类、agent_runner、react_cycle |
| ③ | 二元组 | `(provider, model)` 返回值拆包 | `resolver.resolve_provider_model()` |
| ④ | 单字段 model | **只存 model，缺 provider** | **仅** `StreamChunk`（core.py:77） |
| ⑤ | 派生字符串 | `display_name` 由 provider+model 拼接另存 | chat_tasks.display_name、start_step 注入 |

> 除 ① 外，其余均为"裸 model 或 provider+model 分离"形态，未复用结构化模板；`ModelInfo`（config_schemas.py:58 `provider+model+display_name+current_model`）已为结构化 DTO，**正例无需归一**。

> 注：三项设计基准与归一化范围详见第一章设计要求；本章仅作现状形态归类。

---

## 四、逐类落点清单（file:line 佐证）— 小欧-2026-08-22

### A. 持久化层（DB 表列）

| # | 落点 | 位置 | 形态 |
|---|------|------|------|
| A1 | `chat_sessions.sessionModel` | storage.py:620（`SELECT sessionModel`）；db_initializer.py:182（列定义） | ① JSON ✅已结构化 |
| A2 | `chat_tasks.provider / model / display_name` | storage.py:470（INSERT 三列分离 + display_name 派生） | ② + ⑤【改造：display_name 列废弃，三列并入 sessionModel JSON】 |
| A3 | `chat_user_message.model / provider` | storage.py:750（final 回填两列） | ② |
| A4 | `monitoring.llm_calls.model / provider` | monitoring/storage.py:59 | ② |
| A5 | `monitoring.task_metrics.model / provider` | monitoring/storage.py:32 | ② |
| A6 | `token_usage.model / provider` | db_initializer.py:117；storage.py:509 `token_usage_insert` | ② |

### B. 配置层（全局"当前模型"事实源）

| # | 落点 | 位置 | 形态 |
|---|------|------|------|
| B1 | `config/config.yaml`: `ai.provider` / `ai.model` | `load_config()` 加载（config.py 铁证），裸字符串 + `ai[provider].models[]`；仓库中该文件由前端创建，路径可由 `OMNIAGENT_CONFIG_PATH` 覆盖 | ⑤源 |
| B2 | `resolver.resolve_provider_model()` → `(provider, model)` | resolver.py:38, 84 | ③【改造：返回 ModelRef，消除二元组拆包】 |
| B3 | `ConfigUpdate.ai_provider/ai_model`、`ConfigResponse.ai_provider/ai_model`、`FullConfigResponse.current_provider/current_model` | config_schemas.py:23-24,35-36,87-88 | ② DTO分离【改造：后端 DTO 随归一改为 `ModelRef` 封装字段，前端 `api.ts` 契约同步改，见 6.6 方案 B（前端随后端修改）】 |

### C. LLM 客户端层（内存态 / 给前端的响应）

| # | 落点 | 位置 | 形态 |
|---|------|------|------|
| C1 | `BaseAIService.model / .provider` | base_service.py:64, 77 | ② |
| C2 | `LLMClient.model / .provider` | client_sdk.py:40, 109 | ② |
| C3 | `ChatResponse.model / .provider` | core.py:64, 67（非流式响应给前端） | ② |
| C4 | `StreamChunk.model`（**无 provider**） | core.py:77, 86（流式片段给前端） | ④【改造：补 provider，或承载 ModelRef】 |

### D. Agent 编排层（中间变量）

| # | 落点 | 位置 | 形态 |
|---|------|------|------|
| D1 | `start_step`：`_ai.provider`+`_ai.model` 拼 `display_name` 注入 chat_tasks | start_step.py:201-202 | ⑤【改造：不再拼 display_name 落库】 |
| D2 | `FinalStep.model / .provider` | final_step.py:39-40, 53-54（多态终态，**yield 前端**） | ②【改造：承载 ModelRef】 |
| D3 | `StartStep.provider/model/display_name` | start_content_step.py:15-16 | ②【改造：承载 ModelRef】 |
| D4 | `ErrorStep.provider/model` | error_step.py:20-21 | ②【改造：承载 ModelRef】 |
| D5 | `agent_runner`：`event_dict["provider"/"model"]` | agent_runner.py:256-257 | ② |
| D6 | `react_cycle`：`getattr(agent.llm_client,'provider'/'model')` | react_cycle.py:405,511 | ② |
| D7 | `Step.base.provider/model` property | steps/base.py:46,50 | ② |

### E. 监控遥测层（中间变量 → 落库）

| # | 落点 | 位置 | 形态 |
|---|------|------|------|
| E1 | `agent_telemetry` 的 `model`/`provider` 形参 → 落 llm_calls/task_metrics | agent_telemetry.py:72, 80 | ② |

> **SSE 给前端的结构**：FinalStep.model/provider 经 step_emitter 入 SSE（D2）；StreamChunk.model 经流式通道入 SSE（C4）。前端目前仅 `ModelPicker` 读 sessionModel（A1），其余 model 信息未在前端模型选择器消费。

---

## 五、结构化改造建议（复用 `ModelRef` 模板，三项基准已融入）— 小欧-2026-08-22

将 `SessionModelOverride` 提升为全系统通用 `ModelRef` 规范类，统一所有"模型身份"语义（归一化范围、形态、命名等设计要求详见第一章）。

| 优先级 | 落点（编号） | 改造方向（含三项基准约束） |
|--------|------|---------|
| **高** | A2（chat_tasks ②+⑤） | provider/model/display_name 三列 → 合并为 `sessionModel` JSON 列；**display_name 列废弃**（不写不存，展示前端派生）；变量名 `task_model`（禁裸 model/provider） |
| **高** | C4（StreamChunk ④） | 承载 `ModelRef`（变量名 `chunk_model`）；真正拼请求体处可用裸 `model` 单值并注"仅取 model 单值调 API" |
| **高** | D2/D3/D4（Step ②） | `provider/model` 两列 → `ModelRef(provider+model+api_base?)`；变量名 `step_model` / `final_model` 等 |
| **中** | B2（resolver ③） | 返回 `ModelRef` 对象替代二元组；调用点统一 `resolved_model = resolver.resolve_model_ref()`，以 `.provider`/`.model` 属性访问，杜绝拆包；变量名 `resolved_model` |
| **中** | B3（Config DTOs ②） | 后端 DTO 随归一改为 `ModelRef` 封装字段（`ai_model_ref`/`current_model_ref`），前端 `api.ts` 契约**同步改**（6.6 方案 B，前端随后端修改）；调用点一并迁移（config_helpers/config_service/config_routes） |
| **中** | C1/C2/C3（LLM 层 ②） | `.model`/`.provider` → 单 `ModelRef` 属性（`BaseAIService`/`LLMClient` 统一 `llm_model`、`ChatResponse`→`chat_model`、`StreamChunk`→`chunk_model`）；真正拼请求体处可用裸 `model` 单值并注说明 |
| **低** | B1（config/config.yaml ⑤源） | `ai.provider`/`ai.model` 读取时封装为 `ModelRef`（变量名 `config_model`） |
| **低** | A4/A5/A6/E1（持久化+监控 ②） | `model/provider` 2列 → `task_model`/`chat_model`/`llm_model` JSON 单列；变量名 `task_model`/`chat_model`/`llm_model` 等 |

命名铁律详见第一章设计要求，下表变量名已逐项按前导+model 落地。

---

## 六、基于本地代码的详细代码修改 diff（可落地，无伪代码）— 小欧-2026-08-22

本章以前五章 21 落点为范围，逐文件给出基于本地代码实况的可落地 diff（统一 `ModelRef`，变量名一律 `前导+model`）。**已逐 file:line 复核本地代码，diff 与实况一致。**

**全覆盖对照（第3章 21 落点 → 第六章 diff 小节，无遗漏）：**

| 章三编号 | 落点 | 第六章 diff 小节 | 核查 |
|---|---|---|---|
| A1 | `chat_sessions.sessionModel` | 6.1 规范定义 | 已结构化 |
| A2 | `chat_tasks.provider/model/display_name` | 6.2 DB + 6.3 存储 | ✓ |
| A3 | `chat_user_message.model/provider` | 6.3 存储 | ✓ |
| A4 | `monitoring.llm_calls` | 6.7 监控 | ✓ |
| A5 | `monitoring.task_metrics` | 6.7 监控 | ✓ |
| A6 | `token_usage.model/provider` | 6.2 DB + 6.3 存储 + 6.7 | ✓ |
| B1 | `config/config.yaml ai.provider/model` | 6.6 配置解析 | ✓ |
| B2 | `resolver` 二元组 | 6.6 解析 | ✓ |
| B3 | Config DTOs `ai_/current_` | 6.6 DTOs | ✓ |
| C1 | `BaseAIService` | 6.4 LLM | ✓ |
| C2 | `LLMClient` | 6.4 LLM | ✓ |
| C3 | `ChatResponse` | 6.4 LLM | ✓ |
| C4 | `StreamChunk` | 6.4 LLM | ✓ |
| D1 | `start_step` | 6.5 Agent | ✓ |
| D2 | `FinalStep` | 6.5 Agent | ✓ |
| D3 | `StartStep` | 6.5 Agent | ✓ |
| D4 | `ErrorStep` | 6.5 Agent | ✓ |
| D5 | `agent_runner` | 6.5 Agent | ✓ |
| D6 | `react_cycle` | 6.5 Agent | ✓ |
| D7 | `Step.base` | 6.5 Agent | ✓ |
| E1 | `agent_telemetry` | 6.7 监控 | ✓ |

### 6.1 规范定义 `ModelRef`（`backend/app/db/models/chat_models.py:16`）

```diff
- class SessionModelOverride(BaseModel):
-     """会话级模型覆盖结构(L2) — 北京老陈 2026-08-22"""
-     provider: str = Field(..., description="模型供应商, 如 openai / anthropic")
-     model: str = Field(..., description="模型名, 如 gpt-4o")
-     display_name: Optional[str] = Field(None, description="展示名(可选, 缺省由 provider+model 构建)")

+ class SessionModelOverride(BaseModel):
+     """会话级模型覆盖结构(L2) — 北京老陈 2026-08-22；即全系统通用 ModelRef 规范"""
+     provider: str = Field(..., description="模型供应商, 如 openai / anthropic")
+     model: str = Field(..., description="模型名, 如 gpt-4o")
+     api_base: Optional[str] = Field(None, description="端点定位(私有/本地模型必需)")
+     display_name: Optional[str] = Field(None, description="仅用户自定义别名，系统不拼接，缺省前端派生)")

- # 旧代码无别名
+ ModelRef = SessionModelOverride  # 全系统统一别名，便于前导+model 变量名 `xxx_model: ModelRef`
```

```diff
# frontend/src/types/chat.ts:347 前端对齐后端 ModelRef（3 遍核查：合规复用/合理可选/关联透传）
- export interface SessionModelOverride {
-   provider: string;
-   model: string;
-   display_name?: string;
- }
+ export interface SessionModelOverride {
+   provider: string;
+   model: string;
+   api_base?: string; // 端点定位，与后端 ModelRef 对齐（可选，不强制）
+   display_name?: string;
+ }
```

### 6.2 DB 表结构归一（`backend/app/db/db_initializer.py:102,117,141`（chat_tasks/token_usage/chat_user_message）；`backend/app/monitoring/storage.py:27,57`（task_metrics/llm_calls））

```diff
 # chat_tasks 原三列分离+display_name 派生
-                 provider TEXT, model TEXT, display_name TEXT,
+                 sessionModel TEXT,  -- 归一 JSON(ModelRef)，display_name 列废弃

 # token_usage 原两列分离
-                 model TEXT NOT NULL, provider TEXT,
+                 task_model TEXT NOT NULL,  -- 归一 JSON(ModelRef)

 # chat_user_message 原两列分离
-                 model TEXT, provider TEXT,
+                 chat_model TEXT,  -- 归一 JSON(ModelRef)

 # monitoring.llm_calls 原两列分离
-                 model TEXT, provider TEXT,
+                 llm_model TEXT,  -- 归一 JSON(ModelRef)

 # monitoring.task_metrics 原两列分离
-                 model TEXT, provider TEXT,
+                 task_model TEXT,  -- 归一 JSON(ModelRef)
```

**迁移分析（5 表涉字段变更，均需幂等迁移）：**

| 表 | 字段变更 | 是否需迁移 | 策略 |
|---|---|---|---|
| `chat_tasks` | `provider/model/display_name` 3列 → `sessionModel` JSON 1列 | **是** | 幂等迁移：老行 provider/model/display_name → sessionModel JSON，旧列废弃保留不删（SQLite 不支持 DROP 则保留无害） |
| `token_usage` | `model/provider` 2列 → `task_model` JSON 1列 | **是** | 幂等迁移：老行 model/provider → task_model JSON，旧列废弃 |
| `chat_user_message` | `model/provider` 2列 → `chat_model` JSON 1列 | **是** | 幂等迁移：老行 model/provider → chat_model JSON，旧列废弃 |
| `monitoring.llm_calls` | `model/provider` 2列 → `llm_model` JSON 1列 | **是** | 幂等迁移：老行 model/provider → llm_model JSON，旧列废弃 |
| `monitoring.task_metrics` | `model/provider` 2列 → `task_model` JSON 1列 | **是** | 幂等迁移：老行 model/provider → task_model JSON，旧列废弃 |

*迁移实现（幂等，`_ensure_column` 后执行，5 表全量，读写全链路）：*
```python
# 1. chat_tasks: provider/model/display_name → sessionModel JSON
cur = conn.execute("PRAGMA table_info(chat_tasks)")
cols = [r[1] for r in cur.fetchall()]
if "provider" in cols or "model" in cols:
    _ensure_column(conn, "chat_tasks", "sessionModel", "TEXT")
    for r in conn.execute("SELECT task_id, provider, model, display_name FROM chat_tasks").fetchall():
        if r["provider"] or r["model"]:
            j = json.dumps({"provider": r["provider"], "model": r["model"], "display_name": r["display_name"]})
            conn.execute("UPDATE chat_tasks SET sessionModel=? WHERE task_id=?", (j, r["task_id"]))

# 2. token_usage: model/provider → task_model JSON
cur = conn.execute("PRAGMA table_info(token_usage)")
cols = [r[1] for r in cur.fetchall()]
if "model" in cols and "task_model" not in cols:
    _ensure_column(conn, "token_usage", "task_model", "TEXT")
    for r in conn.execute("SELECT id, model, provider FROM token_usage").fetchall():
        if r["model"] or r["provider"]:
            j = json.dumps({"provider": r["provider"], "model": r["model"]})
            conn.execute("UPDATE token_usage SET task_model=? WHERE id=?", (j, r["id"]))

# 3. chat_user_message: model/provider → chat_model JSON
cur = conn.execute("PRAGMA table_info(chat_user_message)")
cols = [r[1] for r in cur.fetchall()]
if "model" in cols and "chat_model" not in cols:
    _ensure_column(conn, "chat_user_message", "chat_model", "TEXT")
    for r in conn.execute("SELECT id, model, provider FROM chat_user_message").fetchall():
        if r["model"] or r["provider"]:
            j = json.dumps({"provider": r["provider"], "model": r["model"]})
            conn.execute("UPDATE chat_user_message SET chat_model=? WHERE id=?", (j, r["id"]))

# 4. monitoring.llm_calls: model/provider → llm_model JSON
with get_conn("monitoring") as mconn:
    mcols = [r[1] for r in mconn.execute("PRAGMA table_info(llm_calls)").fetchall()]
    if "model" in mcols and "llm_model" not in mcols:
        mconn.execute("ALTER TABLE llm_calls ADD COLUMN llm_model TEXT")
        for r in mconn.execute("SELECT id, model, provider FROM llm_calls").fetchall():
            if r["model"] or r["provider"]:
                j = json.dumps({"provider": r["provider"], "model": r["model"]})
                mconn.execute("UPDATE llm_calls SET llm_model=? WHERE id=?", (j, r["id"]))

# 5. monitoring.task_metrics: model/provider → task_model JSON
with get_conn("monitoring") as mconn:
    mcols = [r[1] for r in mconn.execute("PRAGMA table_info(task_metrics)").fetchall()]
    if "model" in mcols and "task_model" not in mcols:
        mconn.execute("ALTER TABLE task_metrics ADD COLUMN task_model TEXT")
        for r in mconn.execute("SELECT id, model, provider FROM task_metrics").fetchall():
            if r["model"] or r["provider"]:
                j = json.dumps({"provider": r["provider"], "model": r["model"]})
                mconn.execute("UPDATE task_metrics SET task_model=? WHERE id=?", (j, r["id"]))
# 旧列废弃保留不删，读写一律走新 JSON 列
```

### 6.3 存储层归一（`backend/app/services/chat/storage.py:463,509,743`）

```diff
- def insert_task(conn: Connection, *, task_id: str, session_id: str, provider: str, model: str, display_name: str, ...):
+ def insert_task(conn: Connection, *, task_id: str, session_id: str, task_model: ModelRef, ...):
-         """INSERT INTO chat_tasks (..., provider, model, display_name, ...) VALUES (?,?,?,?, ...)"""
-         (..., provider, model, display_name, ...)
+         """INSERT INTO chat_tasks (..., sessionModel, ...) VALUES (?, ...)"""
+         _sm_json = task_model.model_dump_json() if task_model else None
+         (..., _sm_json, ...)

- def token_usage_insert(conn: Connection, *, session_id: str, task_id: str, llm_call_count: int, model: str, provider: Optional[str], ...):
+ def token_usage_insert(conn: Connection, *, session_id: str, task_id: str, llm_call_count: int, task_model: ModelRef, ...):
-            (session_id, task_id, llm_call_count, model, provider, ...)
+            (session_id, task_id, llm_call_count, task_model.model_dump_json(), ...)

- def update_user_message_final(conn: Connection, *, user_message_id: int, task_id: str, model: str = None, provider: str = None, ...):
+ def update_user_message_final(conn: Connection, *, user_message_id: int, task_id: str, task_model: Optional[ModelRef] = None, ...):
-                model=?, provider=?, ...
-         (task_id, response, reasoning, outcome, model, provider, ...)
+                chat_model=?, ...
+         (task_id, response, reasoning, outcome, task_model.model_dump_json() if task_model else None, ...)
```

**读侧归一（storage.py / monitoring 读 JSON→ModelRef）：**
```diff
- row = conn.execute("SELECT model, provider FROM token_usage WHERE task_id=?", (task_id,)).fetchone()
+ row = conn.execute("SELECT task_model FROM token_usage WHERE task_id=?", (task_id,)).fetchone()
+ task_model = json.loads(row["task_model"]) if row and row["task_model"] else None  # → ModelRef

- rows = conn.execute("SELECT model, provider FROM chat_user_message WHERE session_id=?", (sid,)).fetchall()
+ rows = conn.execute("SELECT chat_model FROM chat_user_message WHERE session_id=?", (sid,)).fetchall()
+ # 每行 chat_model JSON → ModelRef，旧 model/provider 列忽略

- rows = conn.execute("SELECT model, provider FROM llm_calls WHERE task_id=?", (tid,)).fetchall()
+ rows = conn.execute("SELECT llm_model FROM llm_calls WHERE task_id=?", (tid,)).fetchall()
+ # llm_model JSON → ModelRef

- rows = conn.execute("SELECT model, provider FROM task_metrics WHERE task_id=?", (tid,)).fetchall()
+ rows = conn.execute("SELECT task_model FROM task_metrics WHERE task_id=?", (tid,)).fetchall()
```

**调用点同步（F4，签改须随调用点一起落地，否则 NameError/TypeError）**：
```diff
# backend/app/services/chat/stream_orchestrator.py:272-278 insert_task 调用
- insert_task(c, task_id=task_id, session_id=session_id, user_message_id=_user_msg_id,
-     ai_message_id=None, user_input=user_input, context_link_mode=context_link_mode,
-     context_root_task_id=_context_root_task_id,
-     provider=agent.llm_client.provider, model=agent.llm_client.model,
-     display_name=f"{agent.llm_client.provider} ({agent.llm_client.model})")
+ _task_model = agent.llm_client.llm_model   # LLMClient 持 ModelRef（见 6.4）
+ insert_task(c, task_id=task_id, session_id=session_id, user_message_id=_user_msg_id,
+     ai_message_id=None, user_input=user_input, context_link_mode=context_link_mode,
+     context_root_task_id=_context_root_task_id, task_model=_task_model)

# stream_orchestrator.py:280-282 token_usage_insert 调用
- token_usage_insert(c, task_id=task_id, session_id=session_id,
-     model=agent.llm_client.model, provider=agent.llm_client.provider, **kw)
+ token_usage_insert(c, task_id=task_id, session_id=session_id, task_model=agent.llm_client.llm_model, **kw)

# agent_runner.py:446-454 update_user_message_final 调用
- update_user_message_final(_conn, user_message_id=db_ops.user_msg_id, task_id=task_id,
-     response=saved_content or "", reasoning=saved_thought or "", outcome=_terminal_status,
-     model=_last_final.get("model") if _last_final else None,
-     provider=_last_final.get("provider") if _last_final else None, ...)
+ update_user_message_final(_conn, user_message_id=db_ops.user_msg_id, task_id=task_id,
+     response=saved_content or "", reasoning=saved_thought or "", outcome=_terminal_status,
+     task_model=ModelRef(provider=_last_final.get("provider"), model=_last_final.get("model"))
+                if _last_final else None, ...)
```

**读者同步（F6：`fetch_session_user_message_pairs` storage.py:782-798）**：
```diff
- SELECT cum.id AS user_id, ..., cum.model AS model, cum.provider AS provider, cum.task_id AS task_id, ...
+ SELECT cum.id AS user_id, ..., cum.chat_model AS chat_model, cum.task_id AS task_id, ...
# 返回 dict 中以 chat_model JSON→ModelRef 派生 model/provider 供前端（旧 model/provider 列忽略）
```

**其余读者同步（三堂会审第3轮补·原漏，同在 storage.py；v1.26 落码时补齐 get_task_detail/load_user_message_by_task 两个 SELECT * 读者，同模式派生）**：
```diff
# :834-839 list_session_tasks(消费方 sessions.py:81-84 GET /sessions/{id}/tasks → 前端任务列表)
-     rows = conn.execute(
-         """SELECT task_id, user_input, status, duration, model, provider,
-                   total_steps, llm_call_count, created_at, updated_at
-            FROM chat_tasks WHERE session_id=? ORDER BY id DESC""",
-         (session_id,),
-     ).fetchall()
+     rows = conn.execute(
+         """SELECT task_id, user_input, status, duration, sessionModel,
+                   total_steps, llm_call_count, created_at, updated_at
+            FROM chat_tasks WHERE session_id=? ORDER BY id DESC""",
+         (session_id,),
+     ).fetchall()
+ # 返回行以 sessionModel JSON→ModelRef 派生 model/provider 键(键名不变供旧前端消费点渐进迁移)

# :678-696 query_token_usage(model 过滤形参 — token_usage.task_model 归一 JSON 后裸列查询失效;
#   消费方 api/v1/token_usage.py:52 GET /token-usage?model= 查询参数)
  def query_token_usage(
      conn: Connection, *, session_id: Optional[str] = None,
-     task_id: Optional[str] = None, model: Optional[str] = None,
+     task_id: Optional[str] = None, model_ref: Optional[ModelRef] = None,
  ) -> Dict:
      _w, _v = [], []
-     for _k in ("session_id", "task_id", "model"):
-         _x = {"session_id": session_id, "task_id": task_id, "model": model}[_k]
-         if _x:
-             _w.append(f"{_k} = ?"); _v.append(_x)
+     for _k in ("session_id", "task_id"):
+         _x = {"session_id": session_id, "task_id": task_id}[_k]
+         if _x:
+             _w.append(f"{_k} = ?"); _v.append(_x)
+     if model_ref:
+         _w.append("json_extract(task_model,'$.provider')=? AND json_extract(task_model,'$.model')=?")
+         _v.extend([model_ref.provider, model_ref.model])
      ...

# :765-771 load_user_messages_by_session(SELECT * 含 model/provider 旧列 — 消费方 messages.py:60;
#   返回 dict 改由 chat_model 列派生 model/provider 键, 旧列值不再读取)
  def load_user_messages_by_session(conn: Connection, session_id: str) -> list:
      rows = conn.execute(
          "SELECT * FROM chat_user_message WHERE session_id=? ORDER BY created_at ASC",
          (session_id,),
      ).fetchall()
-     return [dict(r) for r in rows]
+     out = []
+     for r in rows:
+         d = dict(r)
+         _cm = parse_session_model(d.pop("chat_model", None))   # 复用唯一解析点(DRY)
+         d["model"] = _cm.model if _cm else None                # 键名保留, 值源自新 JSON 列
+         d["provider"] = _cm.provider if _cm else None
+         out.append(d)
+     return out
```

### 6.4 LLM 客户端层归一（`backend/app/llm/core.py:62,75`、`base_service.py:64`、`client_sdk.py:40`）

```diff
 # core.py ChatResponse / StreamChunk
 - class ChatResponse:
 -     def __init__(self, content: str, model: str, provider: str = "", ...):
 -         self.model = model; self.provider = provider
 + class ChatResponse:
 +     def __init__(self, content: str, chat_model: ModelRef, ...):
 +         self.chat_model = chat_model  # 前导+model，禁裸 model/provider（F8：不保留 self.model/self.provider 兼容别名）

 - class StreamChunk:
 -     def __init__(self, content: str, model: str, ...):
 -         self.model = model
 + class StreamChunk:
 +     def __init__(self, content: str, chunk_model: ModelRef, ...):
 +         self.chunk_model = chunk_model  # 补 provider（F8：不保留 self.model 兼容别名）

  # base_service.py / client_sdk.py
 - class BaseAIService:
 -     def __init__(self, model: str, provider: str, ...):
 -         self.model = model; self.provider = provider
 + class BaseAIService:
 +     def __init__(self, llm_model: ModelRef, ...):
 +         self.llm_model = llm_model   # F8：不保留 self.model/self.provider 兼容别名

 - class LLMClient:
 -     def __init__(self, model: str, provider: str, ...):
 + class LLMClient:
 +     def __init__(self, llm_model: ModelRef, ...):
 +         self.llm_model = llm_model   # F8：不保留 self.model/self.provider 兼容别名；与 BaseAIService.llm_model 同名统一
 +
 + # core.py:99-110 工厂函数(签名 model:str 属裸单值形态④同类, 随归一)
 + - def create_cancelled_chunk(model: str) -> StreamChunk:
 + - def create_error_chunk(model: str, error: str, ...) -> StreamChunk:
 + + def create_cancelled_chunk(chunk_model: ModelRef) -> StreamChunk:
 + + def create_error_chunk(chunk_model: ModelRef, error: str, ...) -> StreamChunk:
```

> **F8 合规（禁止backward）— 迁移点全量清单（三堂会审第3轮补全）**：
> **① 类内部自用点（原漏，量最大）**：`base_service.py` 共 **16 处** `self.model`/`self.provider`/`self.api_base` 随属性消亡必须改读 `self.llm_model.*`——`:108-111`（`_ensure_client` 构造 SDK：`provider=self.llm_model.provider or "openai", model=self.llm_model.model, base_url=self.llm_model.api_base`）、`:116`（cancel 日志）、`:147/:169/:179-180/:185`（ChatResponse/error 构造传参）、`:228`（`create_cancelled_chunk(self.llm_model)`）、`:337/:340/:373/:457/:469/:471`（StreamChunk 构造传参）；`client_sdk.py` 内部 `:111`（`_default_base_url(self.llm_model.provider)`）、`:161/:194`（请求体 `model=self.llm_model.model`，属拼请求体裸单值场景，符合设计要求4）。
> **② 外部读取点**：`stream_orchestrator.py:227-232,272-282`（见 6.5 L2 块与 6.3 调用点）、`react_cycle.py:398,404-405,508-513`（见 6.5）、`start_step.py:201-202`（见 6.5）、`lifecycle/service.py:47`（见 6.6 缓存判定）、`agent_telemetry.py:217-218`（见 6.7）、`base_agent.py:124`（见 6.5）。
> **③ prompt_logger 归一（原漏）**：`log_llm_call(round_number, messages, model=..., provider=..., call_type, tools)` 签名本身是形态②分离形参 → 改 `llm_model: Optional[ModelRef] = None`，内部落盘行取 `.model/.provider` 拼日志文本（展示派生，允许）。调用点 `react_cycle.py:401-408` 改传 `llm_model=getattr(agent.llm_client, 'llm_model', None)`。
> **④ 构造处**统一传 `SessionModelOverride(provider=..., model=..., api_base=...)`（唯一构造点 lifecycle/service.py:73-90，见 6.6）。

### 6.5 Agent 层归一（`steps/*`、`agent_runner.py:256`、`react_cycle.py:405`、`start_step.py:201`）

```diff
 # final_step.py(不设 model/provider 委托 property — 与基类同裁定: step 只承载 ModelRef,
 #   裸键仅允许在 _extra_fields 序列化输出层从 ModelRef 派生, 禁止类属性级兼容别名)
 - class FinalStep(ReasoningStep):
 -     def __init__(self, ..., model: Optional[str] = None, provider: Optional[str] = None, ...):
 -         self._model = model; self._provider = provider
 + class FinalStep(ReasoningStep):
 +     def __init__(self, ..., final_model: Optional[ModelRef] = None, ...):
 +         self._final_model = final_model
 +     @property
 +     def final_model(self) -> Optional[ModelRef]: return self._final_model
 +     def _extra_fields(self):
 +         _m = self._final_model
 +         return {"model": _m.model if _m else None, "provider": _m.provider if _m else None,
 +                 "api_base": _m.api_base if _m else None, ...}

 # start_content_step.py StartStep
- class StartStep(ReasoningStep):
-     def __init__(self, ..., display_name: str = "", provider: Optional[str] = None, model: Optional[str] = None, ...):
-         self._display_name = display_name; self._provider = provider; self._model = model
+ class StartStep(ReasoningStep):
+     def __init__(self, ..., start_model: Optional[ModelRef] = None, ...):
+         self._start_model = start_model  # display_name 由 ModelRef.display_name 或前端派生，不再单独落库
+     def _extra_fields(self):
+         return {"provider": self._start_model.provider if self._start_model else None, "model": self._start_model.model if self._start_model else None, ...}

 # error_step.py 同理
- class ErrorStep(ReasoningStep):
-     def __init__(self, ..., model: Optional[str]=None, provider: Optional[str]=None, ...):
+ class ErrorStep(ReasoningStep):
+     def __init__(self, ..., error_model: Optional[ModelRef]=None, ...):
+         self._error_model = error_model

  # base.py ReasoningStep 基类（承载结构 ModelRef，禁止裸 model/provider 属性；step 统一走 ModelRef，SSE 输出由各子类 _extra_fields 从 ModelRef 派生，前端随之后端）
 -         self._model: Optional[str] = None; self._provider: Optional[str] = None
 +         self._step_model: Optional[ModelRef] = None
 +     @property
 +     def step_model(self) -> Optional[ModelRef]: return self._step_model

 # agent_runner.py:250-259 startinfo(设计要求2: display_name 后端零依赖仅前端派生 —
 #   StartStep 不再输出 display_name 键, startinfo 同步删该行, 前端随后端从 provider/model 派生)
      _startinfo = {
          "type": "startinfo", "step": event_dict.get("step", 0),
          "timestamp": event_dict.get("timestamp"),
          "content": "任务开始", "severity": "info",
          "task_id": event_dict.get("task_id"),
-         "display_name": event_dict.get("display_name"),
          "provider": event_dict.get("provider"),
          "model": event_dict.get("model"),
          "ai_message_id": ai_message_id,
      }

 # react_cycle.py:398 日志行(F8 属性迁移)
 -     logger.info(f"[LLM] 调用#{agent.llm_call_count}, ..., model={getattr(agent.llm_client, 'model', '?')}")
 +     logger.info(f"[LLM] 调用#{agent.llm_call_count}, ..., model={getattr(getattr(agent.llm_client, 'llm_model', None), 'model', '?')}")

 # react_cycle.py:401-408 log_llm_call 调用(随 6.4③ prompt_logger 签名归一改传结构)
 -     prompt_logger.log_llm_call(
 -         round_number=agent.llm_call_count,
 -         messages=messages,
 -         model=getattr(agent.llm_client, 'model', 'unknown'),
 -         provider=getattr(agent.llm_client, 'provider', 'unknown'),
 -         call_type="tools",
 -         tools=openai_tools,
 -     )
 +     prompt_logger.log_llm_call(
 +         round_number=agent.llm_call_count,
 +         messages=messages,
 +         llm_model=getattr(agent.llm_client, 'llm_model', None),
 +         call_type="tools",
 +         tools=openai_tools,
 +     )

 # base_agent.py:119-124 _create_cancelled_chunk(顺带修复既有假数据缺陷:
 #   BaseAgent 本无 model 属性, getattr 恒落 'unknown'; 归一后读真实 llm_model)
      def _create_cancelled_chunk(self):
          from app.llm.core import create_cancelled_chunk
 -         return create_cancelled_chunk(getattr(self, 'model', 'unknown'))
 +         return create_cancelled_chunk(self.llm_client.llm_model)

 # start_step.py 注入 chat_tasks
-         display_name=f"{_ai.provider} ({_ai.model})",
-         provider=_ai.provider, model=_ai.model,
+         task_model=_ai.llm_model,  # 直接传 ModelRef，不再拼 display_name 落库
```

**L2 sessionModel 覆盖/还原块归一（三堂会审第3轮补·原漏，`backend/app/services/chat/stream_orchestrator.py`）**：
> BaseAIService 删 `self.model/self.provider` 后，原「拆 provider/model 两属性分别覆盖+分别还原」的双变量方案整体断裂；归一后改为**整个 ModelRef 单变量原子切换**（KISS-DIRECT：双变量→单结构，覆盖/还原各一行，消除半覆盖中间态）。
```diff
# :208-209 预初始化(两变量合一)
-     _orig_model = None  # 三堂会审-AE修复: 预初始化, 供 finally 还原单例 model
-     _orig_provider = None  # sessionModel 结构化: 还原单例 provider 同源
+     _orig_llm_model: Optional[ModelRef] = None  # 覆盖前保存单例原 ModelRef, finally 整体还原

# :227-232 [TASK_START] 日志(F8 属性迁移)
-     log_and_print(
-         f"[TASK_START] provider={ai_service.provider} model={ai_service.model} |\n "
+     log_and_print(
+         f"[TASK_START] provider={ai_service.llm_model.provider} model={ai_service.llm_model.model} |\n "

# :247-260 编排⑥ S2 sessionModel 生效(结构化覆盖: 缺省键回退原值合并, 不再逐属性赋值)
          if session_id:
              try:
                  with db.get_conn("chat") as _conn2:
                      _ov = get_session_model(_conn2, session_id)
                  if _ov and (_ov.model or _ov.provider):
-                     if _ov.model:
-                         _orig_model = ai_service.model
-                         ai_service.model = _ov.model
-                     if _ov.provider:
-                         _orig_provider = ai_service.provider
-                         ai_service.provider = _ov.provider
-                     logger.info(f"[chat] L2 sessionModel 已生效: session={session_id}, provider={_ov.provider}, model={_ov.model}")
+                     _orig_llm_model = ai_service.llm_model   # 整体保存, finally 整体还原
+                     ai_service.llm_model = ModelRef(
+                         provider=_ov.provider or _orig_llm_model.provider,
+                         model=_ov.model or _orig_llm_model.model,
+                         api_base=_ov.api_base or _orig_llm_model.api_base,
+                         display_name=_ov.display_name)
+                     logger.info(f"[chat] L2 sessionModel 已生效: session={session_id}, "
+                                 f"provider={ai_service.llm_model.provider}, model={ai_service.llm_model.model}")
              except Exception as _ov_e:
                  logger.warning(f"[chat] 读会话sessionModel失败(session={session_id}): {_ov_e}")

# finally :352-359 AE 还原(两段还原合一, 单变量判定)
-         if _orig_model is not None and getattr(ai_service, "model", None) != _orig_model:
-             ai_service.model = _orig_model
-             logger.info(f"[chat_stream_orchestrator] AE还原共享单例model: task={task_id}, restore={_orig_model}")
-         if _orig_provider is not None and getattr(ai_service, "provider", None) != _orig_provider:
-             ai_service.provider = _orig_provider
-             logger.info(f"[chat_stream_orchestrator] AE还原共享单例provider: task={task_id}, restore={_orig_provider}")
+         if _orig_llm_model is not None and ai_service.llm_model != _orig_llm_model:
+             ai_service.llm_model = _orig_llm_model
+             logger.info(f"[chat_stream_orchestrator] AE还原共享单例llm_model: task={task_id}, restore={_orig_llm_model.model}")
```

### 6.6 配置与解析层归一（`resolver.py:38`、`config_schemas.py:23,35,87`）

```diff
 # resolver.py
 -     def resolve_provider_model(self) -> tuple[str, str]:
 -         provider, model = self._extract_provider_model(ai_config)
 -         return provider, model
 +     def resolve_model_ref(self) -> ModelRef:
 +         provider, model = self._extract_provider_model(ai_config)
 +         return ModelRef(provider=provider, model=model)  # F7：api_base 当前无调用方，不调未定义 _get_api_base
 # F8：删除 resolve_provider_model（禁止 backward 兼容），所有调用点改 resolve_model_ref（见调用点同步）
```

**F5 决策（方案 B，前端随后端修改，必须同步改前端契约）**：后端 `ConfigUpdate/ConfigResponse/FullConfigResponse` 的 `ai_provider+ai_model` / `current_provider+current_model` 分离字段**归一为 `ModelRef` 封装字段**（字段名随之变更，不再保留裸 `ai_provider`/`ai_model`/`current_provider`/`current_model`）。依据 AGENTS-1.5「前端随后端修改」铁律，**前端 `frontend/src/services/api.ts` 必须同步改契约**，不得以「保持前端契约稳定」为由保留旧分离字段（旧版方案 A 即此错误，已作废）。后端构造/解析处由 `ModelRef` 直接读写，前端 `api.ts` 复用 `SessionModelOverride` 结构化类型（与后端 `ModelRef` 形态一致：`provider+model+api_base?+display_name?`）。

**后端 DTO 归一（`backend/app/api/v1/config_schemas.py:6,21-36,84-88`）**：
```diff
  from typing import Optional, Dict, Any, List
+ from app.db.models.chat_models import ModelRef   # ModelRef 别名唯一归属 chat_models(6.1), 禁新建 refs 模块(DRY)
  from pydantic import BaseModel, Field
  from app.constants import DEFAULT_MAX_STEPS

  class ConfigUpdate(BaseModel):
      """配置更新请求"""
-     ai_provider: Optional[str] = Field(None, description="AI提供商")
-     ai_model: Optional[str] = Field(None, description="AI模型名称")
+     ai_model_ref: Optional[ModelRef] = Field(None, description="AI模型(provider+model 结构)")
      provider_api_keys: Optional[Dict[str, str]] = Field(None, description="Provider API Key字典: {provider_name: api_key}")
      ...

  class ConfigResponse(BaseModel):
      """配置响应"""
-     ai_provider: str = Field(..., description="当前AI提供商")
-     ai_model: str = Field(..., description="当前AI模型")
+     ai_model_ref: ModelRef = Field(..., description="当前AI模型(provider+model 结构)")
      api_key_configured: bool = Field(..., description="API Key是否已配置")
      ...

  class FullConfigResponse(BaseModel):
      """完整配置响应"""
      providers: dict[str, ProviderInfo] = Field(..., description="所有Provider配置")
-     current_provider: str = Field(..., description="当前使用的Provider")
-     current_model: str = Field(..., description="当前使用的模型")
+     current_model_ref: ModelRef = Field(..., description="当前使用的模型(provider+model 结构)")
```

> **死 DTO 清除（三堂会审第1轮合规·YAGNI）**：`config_schemas.py:124-131 FullConfigValidationResponse`（provider/model 分离字段）经全仓 grep 确认**无任何路由/服务引用**（config_routes.py 未 import），属死代码——**直接删除，不做归一迁移**；若未来需要完整校验响应端点，届时按 ModelRef 结构新建。

**前端契约同步（`frontend/src/services/api.ts:160-162,181-188,222-225`）—— 随之后端，禁用旧分离字段**：
```diff
  export interface Config {
-   ai_provider: "zhipuai" | "opencode" | "longcat";
-   ai_model: string;
+   ai_model_ref: SessionModelOverride;
    api_key_configured: boolean;
    ...
  }

  export interface ConfigUpdate {
-   // ⭐⭐⭐ 重要：provider 和 model 必须成对使用！...
-   ai_provider?: string;
-   ai_model?: string;
+   // provider+model 已归一为结构，前端直接传 SessionModelOverride（含 provider+model+api_base?+display_name?）
+   ai_model_ref?: SessionModelOverride;
    provider_api_keys?: Record<string, string>;
    ...
  }

  export interface FullConfigResponse {
    providers: Record<string, ProviderInfo>;
-   current_provider: string;
-   current_model: string;
+   current_model_ref: SessionModelOverride;
  }
```
> 前端消费点须随之迁移（不得保留旧 `current_provider`/`ai_provider` 读取）：`ProviderSettings.tsx:121,125,133`（`data.current_provider` → `data.current_model_ref.provider`）、`:422-423`（`ai_provider/ai_model` → `ai_model_ref`）、`Layout/index.tsx:286-287`（`ai_provider/ai_model` → `ai_model_ref`）、`GlobalConfigArea.tsx:79,173`（`m.current_model` 属 `ModelInfo` 列表项，不受影响）。`SessionModelOverride`（types/chat.ts:347）即后端 `ModelRef` 镜像类型，须含 `provider+model+api_base?+display_name?`（v1.21 已加 `api_base?`，若缺失须补）。

**调用点同步（F4，resolve_model_ref 替代 resolve_provider_model，删元组拆包；含 lifecycle 全链闭环）**：
```diff
# backend/app/services/lifecycle/service.py:28-34
  def get_resolver_and_config():
      from app.services.model.resolver import get_ai_config_resolver
      resolver = get_ai_config_resolver()
-     final_provider, final_model = resolver.resolve_provider_model()
+     resolved_model = resolver.resolve_model_ref()
      ai_config = resolver.get_ai_config()
-     return resolver, final_provider, final_model, ai_config
+     return resolver, resolved_model, ai_config

# service.py:24/45-47 缓存判定(原读 _instance.model 在 BaseAIService 删 .model 后断裂; 归一后整体 ModelRef 比较,
#   _current_provider 单值态升级为 _current_model_ref 结构态)
- _current_provider: str = ""
+ _current_model_ref: Optional[ModelRef] = None
  def check_cache_valid(final_provider: str, final_model: str) -> bool:
-     return _instance is not None and _current_provider == final_provider and _instance.model == final_model
+ def check_cache_valid(config_model: ModelRef) -> bool:
+     return _instance is not None and _current_model_ref == config_model   # Pydantic 值相等

# service.py:50-56 cleanup_old_instance / :140-148 set_instance 同步结构态
- def cleanup_old_instance(new_provider: str = "") -> None:
-     global _instance, _current_provider
+ def cleanup_old_instance(new_model_ref: Optional[ModelRef] = None) -> None:
+     global _instance, _current_model_ref
      old_instance = _instance
      _instance = None
-     _current_provider = new_provider
+     _current_model_ref = new_model_ref
      close_instance_sync(old_instance)
  # set_instance(instance, provider="") → set_instance(instance, model_ref: Optional[ModelRef] = None) 同理

# service.py:73-90 create_service_instance(BaseAIService 签改 llm_model 后构造点必须随改;
#   api_base 此处有真实来源 provider_config, 按设计要求3纳入 ModelRef — F7"resolver 不取 api_base"不受影响)
  def create_service_instance(provider_config: dict, final_provider: str, final_model: str) -> BaseAIService:
      ...
      return BaseAIService(
          api_key=(provider_config.get("api_key") or "").strip(),
-         model=final_model,
-         api_base=(provider_config.get("api_base") or "https://api.openai.com/v1").strip(),
-         provider=final_provider,
+         llm_model=ModelRef(
+             provider=final_provider, model=final_model,
+             api_base=(provider_config.get("api_base") or "https://api.openai.com/v1").strip()),
          timeout=..., ...

# service.py:93-124 get_service 主体(拆包变量随 get_resolver_and_config 新返回值同步)
-     resolver, final_provider, final_model, ai_config = get_resolver_and_config()
+     resolver, config_model, ai_config = get_resolver_and_config()
      try:
-         validate_provider_model(final_provider, final_model)
-         if check_cache_valid(final_provider, final_model): return _instance
+         validate_provider_model(config_model.provider, config_model.model)
+         if check_cache_valid(config_model): return _instance
          with _instance_lock:
-             if check_cache_valid(final_provider, final_model): return _instance
-             cleanup_old_instance(final_provider)
+             if check_cache_valid(config_model): return _instance
+             cleanup_old_instance(config_model)
              log_service_creation(final_provider, final_model)
-             provider_config = get_provider_config(ai_config, final_provider)
-             _instance = create_service_instance(provider_config, final_provider, final_model)
+             provider_config = get_provider_config(ai_config, config_model.provider)
+             _instance = create_service_instance(provider_config, config_model.provider, config_model.model)

# service.py:151-172 get_service_for_model(provider, model) → 入参归一 model_ref: ModelRef
- def get_service_for_model(provider: str, model: str):
+ def get_service_for_model(model_ref: ModelRef):
      ...
-     provider_config = resolver.get_service_config(provider, model)
+     provider_config = resolver.get_service_config(model_ref.provider, model_ref.model)
      ...
-     instance = create_service_instance(provider_config, provider, model)
-     set_instance(instance, provider)
+     instance = create_service_instance(provider_config, model_ref.provider, model_ref.model)
+     set_instance(instance, model_ref)

# backend/app/services/lifecycle/validation.py 全链闭环(仅改 :68 一行则下游全断, 必须整链随改)
- def _resolve_provider_model(resolver=None) -> Tuple[Optional[str], Optional[str], list]:
+ def _resolve_provider_model(resolver=None) -> Tuple[Optional[ModelRef], list]:
      ...
      try:
-         provider, model = resolver.resolve_provider_model()
-         return provider, model, []
+         return resolver.resolve_model_ref(), []
      except ValueError as e:
-         return None, None, [str(e)]
+         return None, [str(e)]
  # ConfigValidationResult dataclass(:18-25) 字段归一:
-     provider: str
-     model: str
+     model_ref: Optional[ModelRef]
  # :74-80/:91-96/:99-106 各 make_*_result 工厂与 :109-128 validate_config 主流程的 provider/model 形参
  #   一并改为透传 model_ref(message 日志内取 .provider/.model 拼文本属展示派生, 允许)

# backend/app/services/model/config_service.py:124,163,180,213
- final_provider, final_model = resolver.resolve_provider_model()
+ resolved_model = resolver.resolve_model_ref()
```

**resolver.validate_config 四元组归一 + /chat/validate 响应归一（三堂会审第3轮补，原漏）**：
```diff
# backend/app/services/model/resolver.py:92-109(内部用 _extract_provider_model 不受 F8 删 resolve_provider_model 影响,
#   但返回的 (is_valid, provider, model, errors) 分离四元组属形态②, 必须随归一)
  def validate_config(self) -> tuple:
-     """验证AI配置有效性
-     Returns:
-         (is_valid, provider, model, error_messages)
-     """
+     """验证AI配置有效性
+     Returns:
+         (is_valid, config_model: ModelRef, error_messages)
+     """
      ai_config = self.get_ai_config()
      provider, model = self._extract_provider_model(ai_config)
      errors = []
      try:
          ...  # 校验逻辑不变
      except ValueError as e:
          errors.append(str(e))
-     return (len(errors) == 0, provider or "unknown", model or "", errors)
+     return (len(errors) == 0, ModelRef(provider=provider or "unknown", model=model or ""), errors)

# backend/app/services/chat/stream_orchestrator.py:111-137 validate_chat_config
#   (/chat/validate 端点响应给前端 — 方案B前端随后端, 响应键 provider/model 归一为 model_ref 结构)
  async def validate_chat_config():
      from app.logger import logger
      try:
          resolver = get_ai_config_resolver()
-         is_valid, final_provider, final_model, error_messages = resolver.validate_config()
+         is_valid, config_model, error_messages = resolver.validate_config()
          if not is_valid:
              return {
                  "valid": False,
                  "message": f"配置验证失败: {', '.join(error_messages)}",
-                 "provider": final_provider or "unknown",
-                 "model": final_model or ""
+                 "model_ref": {"provider": config_model.provider or "unknown",
+                               "model": config_model.model or ""},
              }
          return {
              "valid": True,
-             "message": f"配置验证通过: {final_provider} ({final_model})",
-             "provider": final_provider,
-             "model": final_model
+             "message": f"配置验证通过: {config_model.provider} ({config_model.model})",  # 文本派生, 允许
+             "model_ref": {"provider": config_model.provider, "model": config_model.model},
          }
      except Exception as e:
          logger.error(f"验证AI服务配置失败: {e}")
          return {
              "valid": False,
              "message": f"验证失败: {str(e)}",
-             "provider": "unknown",
-             "model": ""
+             "model_ref": None,
          }
```

**后端 DTO 消费点同步（方案 B 字段变更，须一并改，否则 AttributeError/KeyError）**：
```diff
# backend/app/services/model/config_helpers.py:302-321 两 handler 合一(provider/model 成对原子写入, DRY;
#   原 _update_provider/_update_model 拆两键分两次处理反而依赖 FIELD_HANDLERS 迭代顺序, 合并后消除该耦合)
- def _update_provider(config_data: dict, update) -> None:
-     ai_config = config_data.get('ai', {})
-     if update.ai_provider not in ai_config:
-         raise HTTPException(status_code=400, detail=f"不支持的提供商: {update.ai_provider}")
-     config_data['ai']['provider'] = update.ai_provider
-     reset()
-     logger.info(f"更新AI Provider: {update.ai_provider}")
-
- def _update_model(config_data: dict, update) -> None:
-     _ai_cfg = config_data.get('ai', {})
-     provider = update.ai_provider or _ai_cfg.get('provider')
-     ...
-     if provider in config_data.get('ai', {}):
-         config_data['ai']['model'] = update.ai_model
-         logger.info(f"更新AI Model: {update.ai_model} (provider={provider})")
-         reset()
+ def _update_model_ref(config_data: dict, update) -> None:
+     """provider+model(+api_base) 成对原子写入 — 单一权威入口"""
+     ai_config = config_data.get('ai', {})
+     if update.ai_model_ref.provider not in ai_config:
+         raise HTTPException(status_code=400, detail=f"不支持的提供商: {update.ai_model_ref.provider}")
+     config_data['ai']['provider'] = update.ai_model_ref.provider
+     config_data['ai']['model'] = update.ai_model_ref.model
+     if update.ai_model_ref.api_base:
+         config_data['ai'][update.ai_model_ref.provider]['api_base'] = update.ai_model_ref.api_base
+     reset()
+     logger.info(f"更新AI模型: provider={update.ai_model_ref.provider}, model={update.ai_model_ref.model}")

  # config_helpers.py:355-364 FIELD_HANDLERS(字段名→handler 映射, 值是函数非 ModelRef!)
  FIELD_HANDLERS: Dict[str, Any] = {
-     "ai_provider": _update_provider,
-     "ai_model": _update_model,
+     "ai_model_ref": _update_model_ref,
      "provider_api_keys": _update_api_keys,
      ...

# backend/app/services/model/config_helpers.py:234-240 校验失败 fail_result(漏项补齐)
      original_ai = original_config_data.get('ai', {})
      fail_result = {
          "success": False, "message": "配置验证失败", "errors": errors, "warnings": warnings,
          "backup_path": str(backup_path) if backup_path else None,
-         "current_provider": original_ai.get('provider', 'unknown'),
-         "current_model": original_ai.get('model', 'unknown'),
+         "current_model_ref": ModelRef(
+             provider=str(original_ai.get('provider', 'unknown')),
+             model=str(original_ai.get('model', ''))),
      }
```

**config_service.py / config_routes.py 消费点同步（含 v1.24 漏项 :91-96）**：
```diff
# backend/app/services/model/config_service.py:91-97 update_config 返回 dict(PUT /config 直接返回前端)
-     current_provider = config_data.get('ai', {}).get('provider', '')
-     current_model = config_data.get('ai', {}).get('model', '')
      return {
          "success": True, ...,
          "backup_path": str(backup_path) if backup_path else None,
-         "current_provider": current_provider, "current_model": current_model,
+         "current_model_ref": {"provider": config_data.get('ai', {}).get('provider', ''),
+                               "model": config_data.get('ai', {}).get('model', '')},
      }
  # 注: "updated_fields" 经 config_update.model_dump(exclude_none=True) 自动随 DTO 变为 ai_model_ref 结构, 无需改

# backend/app/services/model/config_service.py:144-145 / 233-234 响应构造
- "ai_provider": final_provider,
- "ai_model": final_model,
+ "ai_model_ref": resolved_model,          # resolve_model_ref() 返回值直接透传(见调用点同步)
- "current_provider": final_provider,
- "current_model": final_model,
+ "current_model_ref": resolved_model,

# backend/app/api/v1/config_routes.py:73-74 / 118-119 落 DTO
- ai_provider=data["ai_provider"], ai_model=data["ai_model"],
+ ai_model_ref=data["ai_model_ref"],
- current_provider=result["current_provider"], current_model=result["current_model"],
+ current_model_ref=result["current_model_ref"],
```

### 6.7 监控层归一（`monitoring/storage.py:32,59`、`agent_telemetry.py:72`）

```diff
 # monitoring/storage.py persist_task_metrics（F1 补 task_metrics 写入源 _cols）
 - _cols = [..., "model", "provider", ...]
 + _cols = [..., "task_model", ...]   # 删 model/provider，统一 JSON 单列

 # monitoring/storage.py persist_llm_calls
 - def persist_llm_calls(rows: List[Dict[str, Any]]) -> None:
 -     (r["task_id"], r["session_id"], r["model"], r["provider"], ...)
 + def persist_llm_calls(rows: List[Dict[str, Any]]) -> None:
 +     (r["task_id"], r["session_id"], r["tele_model"].model_dump_json() if isinstance(r["tele_model"], ModelRef) else None, ...)

 # agent_telemetry.py finalize()（F1 补 task_metrics 输出）
 - "model": getattr(_llm_client, "model", None),
 - "provider": getattr(_llm_client, "provider", None),
 + "task_model": (_llm_client.llm_model.model_dump_json()
 +                if getattr(_llm_client, "llm_model", None) else None),

 # agent_telemetry.py on_llm_call
 -     def on_llm_call(self, ..., model: Optional[str]=None, provider: Optional[str]=None, ...):
 -             "model": model, "provider": provider,
 +     def on_llm_call(self, ..., tele_model: Optional[ModelRef]=None, ...):
 +             "tele_model": tele_model,
 +             "model": tele_model.model if tele_model else None,
 +             "provider": tele_model.provider if tele_model else None,
```

**调用点同步（F2：react_cycle.py:508-513 改传 tele_model）**：
```diff
- agent.telemetry.on_llm_call(_usage, duration=_call_dur,
-     model=getattr(agent.llm_client, "model", None),
-     provider=getattr(agent.llm_client, "provider", None),
-     error_type=_llm_err, finish_reason=_fin)
+ agent.telemetry.on_llm_call(_usage, duration=_call_dur,
+     tele_model=getattr(agent.llm_client, "llm_model", None),
+     error_type=_llm_err, finish_reason=_fin)
```

**监控查询改写（JSON 列，3 遍核查）：**
```diff
- SELECT * FROM llm_calls WHERE provider=? AND model=?
+ SELECT * FROM llm_calls WHERE json_extract(llm_model,'$.provider')=? AND json_extract(llm_model,'$.model')=?
- SELECT * FROM task_metrics WHERE provider=? 
+ SELECT * FROM task_metrics WHERE json_extract(task_model,'$.provider')=?
# 封装 helper parse_llm_model / parse_task_model 统一解析，避免各处 json.loads 重复
```

### 6.8 SSE 传输层归一（`backend/app/services/chat/sse_events.py:42,118`）

```diff
- def create_error_response(error_type: str, error_message: str, model: Optional[str]=None, provider: Optional[str]=None, ...):
-     error_step = ErrorStep(..., model=model, provider=provider)
+ def create_error_response(error_type: str, error_message: str, error_model: Optional[ModelRef]=None, ...):
+     error_step = ErrorStep(..., error_model=error_model)

- def create_final_response(content: str, ..., provider: Optional[str]=None, model: Optional[str]=None, ...):
-     final_step = FinalStep(..., model=model, provider=provider)
+ def create_final_response(content: str, ..., final_model: Optional[ModelRef]=None, ...):
+     final_step = FinalStep(..., final_model=final_model)
```

**调用点同步（F4：stream_orchestrator.py:171,175,349,387 传 model/provider 处改传 final_model/error_model）**：
```diff
# stream_orchestrator.py 终态拼装/错误处
- yield create_error_response(error_type=..., error_message=..., model=..., provider=...)
+ yield create_error_response(error_type=..., error_message=..., error_model=...)
- yield create_final_response(content=..., provider=..., model=..., reasoning=...)
+ yield create_final_response(content=..., final_model=..., reasoning=...)
# 无 model 的简单错误响应（如 invalid_request）保持不传，签名兼容
```

> 本章 6.1-6.8 覆盖前五章全部 21 归一落点（A1-A6、B1-B3、C1-C4、D1-D7、E1）+ ModelInfo 正例，均已逐 file:line 对照本地代码实况修正（含 F1-F8 与 G1-G15 全部补丁：lifecycle 全链闭环、L2 sessionModel 原子切换、validate_config/校验响应归一、base_service 内部迁移清单、storage 三读者、FIELD_HANDLERS 合一、prompt_logger 签名、死 DTO 清除等），均为可落地 diff，无伪代码；变量名一律 `前导+model`（`task_model`/`chunk_model`/`final_model`/`resolved_model`/`tele_model`/`llm_model` 等），符合第一章设计要求。


