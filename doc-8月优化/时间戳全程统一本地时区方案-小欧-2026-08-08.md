# 时间戳全程统一本地时区方案

**版本**: v1.6
**创建时间**: 2026-08-08 17:42:42
**更新时间**: 2026-08-08 19:24:51
**创建人**: 小欧
**更新人**: 小欧

> **v1.6 更新说明（2026-08-08 19:24:51，小欧）**：
> 老陈明确判定标准并确认"设计整体必须与此一致，否则出错"：
> 1. **统一/不统一判定标准（核心）**：时间值只要"流出边界"（落盘DB / 输出API / SSE / 日志）→ 必须统一"本地ISO无Z"；只要"困在 tool 内部"做计算/展示 → 保持 aware 自有规则，不改。
> 2. 新增 4.1.1 统一判定标准表（含 tool 分类：time_now/time_diff/time_add/tool_fc_helper 不统一的原因）。
> 3. 设计整体（22文件清单 + 实施计划 + 迁移脚本 + 迁移表 timers 纳入）与判定标准一致——落盘/显示边界的全改，tool 内部计算的均不动。

> **v1.5 更新说明（2026-08-08 19:17:06，小欧）**：
> 全方位核查"需要修改的文件务必都找全（不能多也不能少）"时，发现 **2 处遗漏文件/改动点**：
> 1. 【严重】`app/services/monitoring/collector.py` **整文件遗漏**（第22个文件）：`Metric.timestamp` 用 `datetime.now(timezone.utc)`（aware UTC），经 `_calculate_basic_stats` L130 `.isoformat()` 输出 `+00:00` 偏移到 metrics API 的 **summary 内每个指标的 timestamp 字段**。metrics.py 响应 timestamp 改本地后，summary 指标 timestamp 仍带 `+00:00`，前后不一致，违反"全程本地无Z"。修复：L30 改 `datetime.now()`（naive），L94 `cutoff_time` 必须同步改 `datetime.now()`（否则 L100 `metric.timestamp > cutoff_time` naive vs aware 比较 TypeError！），L130 `.isoformat()` 自动输出本地无Z。
> 2. 【中】`app/tools/timer/timer_set.py` **L39 漏改**：`_invoke_timer_callback` 内 `"triggered_at": datetime.now().astimezone().isoformat()` 带 `+08:00` 偏移，进入内存 `_timer_events` 列表。4.3.11b 只覆盖 L113/117/127/143，漏 L39。统一改为 `get_local_iso_timestamp()`。
> 3. 【范围界定】本次改动**仅涉及系统代码**（DB/API/前端/日志存储线），**不涉及 tool 函数内部逻辑**。`time_now`/`time_diff`/`time_add`/`tool_fc_helper` 等 tool 工具内部的时间解析逻辑可自有规则（老陈明确），**只要求落盘/显示时必须遵守"本地ISO无Z"统一规则**。

> **v1.4 更新说明（2026-08-08 19:03:56，小欧）**：
> 对 v1.3 做**全文熟读3遍 + 上上下下关联逻辑核查（含测试文件）**，发现并修正 **4个严重问题 + 2个中等问题**：
> 1. 【严重】4.3.9 duration 修复逻辑错误：`replace(tzinfo=None)` 只剥tzinfo不转本地，迁移前UTC旧数据（如`08:30Z`）duration 仍差8h（实测验证）。修正：`created_at_dt.astimezone().replace(tzinfo=None)` 先转本地再去tzinfo。
> 2. 【严重】测试适配遗漏：`test_8_05_concurrent_create.py:76` patch `sessions.get_utc_timestamp`、`test_observation_step.py:89` 断言 `endswith("Z")` —— 改本地无Z后两测试必失败。需同步改测试（阶段5新增测试适配步骤）。
> 3. 【严重】`operation_record.py:19` `from datetime import ... timezone` 在 L148/190/204 全改后 timezone 仅 L148 用 → 需在 import 中移除 timezone 避免未用导入。
> 4. 【中】`test_8_05` patch 返回值 `"2026-07-03T00:00:00Z"` 需同步改本地无Z。
> 5. 【中】实施计划阶段2标题写"10个文件"实为11个文件（2.1~2.11）→ 修正为11个。
> 6. 【中】4.4 迁移脚本 `has_tz` 判断对负数偏移（`-05:00`）需复核：`'-' in s[10:]` 在日期部分（`2026-08-08` 的 `-`）不误判（s[10:] 从索引10开始即 T 之后）——核查通过无问题，已在8.4注明。

> **v1.3 更新说明（2026-08-08 18:55:13，小欧）**：
> 对 v1.2 实施代码做**逐文件真实代码核查**，修正 4 处不准确/不完整：
> 1. 4.3.4 messages.py：实际 import 是两行（L33 与 L34 分开），v1.2 合并成一行与真实代码不符 → 修正为两行修改方案
> 2. 4.3.11b timer_set.py：v1.2 漏改 L113 `trigger_at = datetime.now().astimezone() + timedelta(...)`（aware），导致 `trigger_at.isoformat()` 输出带 `+08:00` 偏移，违反"全程本地无Z"目标 → 补全为 L113 也改 naive 本地（最简方案：L113 改后 L118/L143/L148 引用 trigger_at 处自然无偏移）
> 3. 六、验证计划 → 重写为**完整的分步实施计划**（按依赖排序，每步含文件/改动点/验证方式/退出标准）
> 4. 补充 db_initializer.py/chat_models.py/operation_queries.py 注释同步的具体位置

> **v1.2 更新说明（2026-08-08 18:41:31，小欧）**：
> 对 v1.1 进行三堂会审，修正 6 个问题：
> 1. `to_local_iso` 偏移解析改用 `fromisoformat` 统一处理（不依赖 `'Z'/'+00:00'` 判断分支，兼容 `+08:00` 等任意偏移）
> 2. 迁移脚本 `utc_z_to_local` 改用 `astimezone()` 自动转本地（替代 `safe_utc_offset()` 手动加偏移，Python 自动处理时区，更简单直接）
> 3. `operation_record.py` duration 解析兼容迁移前后（`replace('Z','+00:00')` 保留）
> 4. `timer_set.py` 同步改 `get_local_iso_timestamp()`，timers 表纳入统一（不再排除）
> 5. `convert_to_utc` 保留原因补充（测试 test_bug_discovery 依赖）
> 6. 文件清单增加 `timer_set.py`（共 21 个文件）

---

## 一、需求与要求

1. **核心需求**：全程统一到本地时区，杜绝UTC/本地混用
2. **覆盖范围**：DB存储、API输出、前端展示、日志文件（prompt log）
3. **约束**：
   - 不能固定偏移（如+8），必须用实际时区偏移（`safe_utc_offset()`）
   - 排序字符串必须一致（新旧混存会导致乱序）
   - 前端零改动（`Date.parse` 对无Z ISO按本地解析）
   - 关联逻辑正确，增强不能退化

## 二、解决的问题

### 2.1 task004报告问题1（属实）
prompt log 中 `步骤产出[].数据.timestamp` 是 UTC Z（唯一），其余7个时间字段是本地，差8小时。

### 2.2 老陈发现（DB存储线全UTC）
整个存储线（chat/task/operations 三库）全部写入 `get_utc_timestamp()`（UTC Z 字符串），不止日志。

### 2.3 输出层破坏本地化
- `format_timestamp()` 对字符串**强制追加 Z** → DB 本地时间也会被标成 UTC
- `convert_to_utc()` 用于输出 title_updated_at → 转回 UTC
- `database.py _validate` datetime/date 参数自动转 UTC

### 2.4 duration计算bug
`operation_record.py:146-148` 把 naive datetime 强制补 UTC，与 `executed_at_dt = datetime.now(timezone.utc)` 相减 → TypeError（naive vs aware）

## 三、现状代码位置（全部恢复原状，确认读取）

### 3.1 存储线（DB写入全为UTC）

| 文件 | 行号 | 说明 |
|------|------|------|
| `app/utils/time_utils.py` | 33-35 | `get_utc_timestamp()` 返回 UTC Z |
| `app/services/agent/steps/base.py` | 17,28 | import + 默认 `get_utc_timestamp()` |
| `app/services/chat/storage.py` | 147,189,232,329 | chat_messages.timestamp、chat_sessions.updated_at、chat_message_steps.created_at |
| `app/api/v1/messages.py` | 138 | save_message 写入 chat_messages.timestamp |
| `app/api/v1/sessions.py` | 33,190,271 | create/update/delete 会话的时间戳 |
| `app/services/task/task_db.py` | 61,75,121 | tasks.created_at/completed_at、task_operations.created_at |
| `app/services/safety/operation_record.py` | 105,152,189,206 | file_operations.created_at/executed_at |
| `app/services/safety/operation_rollback.py` | 103 | file_operations.rolled_back_at |
| `app/services/safety/operation_cleanup.py` | 86 | file_operations.backup_expires_at 比较 |
| `app/utils/sse_formatter.py` | 43 | SSE默认 timestamp |
| `app/api/v1/chat/sse.py` | 95 | 默认 timestamp |
| `app/main.py` | 101,115,131 | 异常响应 timestamp |
| `app/api/v1/metrics.py` | 70,89,110,125 | 监控响应 timestamp |
| `app/api/v1/health.py` | 61,73 | 健康检查 timestamp |

### 3.2 输出层（强制转回UTC）

| 文件 | 行号 | 问题 |
|------|------|------|
| `app/utils/time_utils.py` | 104-106 | `format_timestamp()` 字符串分支强制加Z |
| `app/db/database.py` | 79,86 | `_validate` datetime/date 用 `convert_to_utc()` 转UTC |
| `app/api/v1/messages.py` | 90 | `convert_to_utc(session['title_updated_at'])` 转UTC输出 |
| `app/api/v1/sessions.py` | 251 | `convert_to_utc(row['title_updated_at'])` 转UTC输出 |

### 3.3 内部已有本地工具

| 函数 | 位置 | 说明 |
|------|------|------|
| `safe_utc_offset()` | `time_utils.py:109-120` | 安全获取本地UTC偏移小时数（utcoffset()可能返回None时默认8） |
| `now_str()` | `time_utils.py:79-84` | 本地时间格式化，prompt_logger 用 |

### 3.4 前端消费链（已兼容本地无Z格式）

| 位置 | 解析方式 |
|------|---------|
| `sse.ts:1103-1109` | `Date.parse(rawData.timestamp)` |
| `timestamp.ts:68` | `new Date(timestamp)` |
| `timeFormatters.ts:12,30` | `new Date()` |
| `chatHistory.ts:67` | `new Date(msg.timestamp)` |

前端 `Date.parse` 对无时区后缀的 ISO 格式按**本地时区**解析，零改动兼容。

## 四、方案设计

### 4.1 核心原则

1. **存储统一本地**：三库所有时间列写 `get_local_iso_timestamp()`（本地 ISO，T分隔，**无Z/无偏移**）
2. **转换动态偏移**：UTC→本地 用 `safe_utc_offset()` 获取实际偏移，**不能固定+8**
3. **输出统一本地**：`format_timestamp()` 改语义——字符串原样返回，不再强制加Z
4. **前端零改动**：本地无Z ISO 格式 Date.parse 自动按本地解析
5. **tool 自有规则、落盘显示必须统一**（v1.5 老陈明确）：tool 函数内部的时间解析逻辑可自有规则，**但凡是落盘（DB写入）或显示（API输出/SSE/日志）的时间值，必须遵守"本地ISO无Z"统一规则**。本次改动范围仅系统代码，不涉及 tool 函数内部逻辑。

### 4.1.1 统一/不统一判定标准（v1.6 老陈明确，设计整体必须与此一致否则出错）

**核心判据（一条）**：时间值是否"流出边界"——

- **流出边界**（落盘DB / 输出API / SSE / 日志）→ **必须统一为"本地ISO无Z"**
- **困在 tool 内部**（仅做计算 / 展示）→ 保持 aware 自有规则，**不统一、不改**

| 场景 | 是否统一 | 原因 |
|------|---------|------|
| DB 落盘（chat/task/operations 三库 + timers 表） | ✅ 统一 | 跨模块共享格式，不统一则排序/迁移/混存乱 |
| API 响应 timestamp（health/metrics/messages/sessions） | ✅ 统一 | 前端 Date.parse 按无Z本地解析 |
| SSE 默认 timestamp / prompt log | ✅ 统一 | 显示边界 |
| collector.py metrics timestamp | ✅ 统一 | API 输出（v1.5 已补，第22文件） |
| timer_set.py（tool 但落盘 timers 表） | ✅ 统一 | 写 DB，必须遵守（已纳入 4.3.11b） |
| `time_now` | ❌ 不统一 | 输出本就是本地无偏移字符串（formatted），`isoformat()` 死参数未使用——天然合规 |
| `time_diff` | ❌ 不统一 | 纯计算型，输出 humanized 文本（"X天前"），不落盘不显示绝对时间戳；内部必须 aware 才能正确算差值，改 naive 反而出错 |
| `time_add` | ❌ 不统一 | 同 time_diff，输出本地无偏移字符串（strftime %Y-%m-%d %H:%M:%S），计算用 aware 正确 |
| `tool_fc_helper.parse_datetime_any` | ❌ 不统一 | 工具参数解析（用户输入→aware datetime），喂给内部计算，不出边界 |

**关键点**：计算正确性要求 aware —— time_diff/time_add 内部若用 naive 本地，跨时区/输入带偏移会算错。`datetime.now().astimezone()` + `replace(tzinfo=timezone.utc).astimezone()` 正是"用户输入→统一到本地 aware"的正确做法，是 tool 自有规则的**正当理由**，不是该统一的"不一致"。

**timer 族分化**：同一工具族里 `timer_set`（落盘）改、`timer_list`（只读展示排序）不改、`time_diff/time_add`（纯计算）不改 —— 判断依据正是"是否流出边界"。

**设计整体一致性**：22文件清单 / 实施计划阶段2 / 迁移脚本 / 迁移表（timers 纳入）全部与判定标准一致——落盘/显示边界的全改，tool 内部计算的均不动。违反此判据的设计即为错误设计。

### 4.2 time_utils.py 改造（核心）

#### 4.2.1 新增 `get_local_iso_timestamp()`

```python
# -*- coding: utf-8 -*-
# 编辑历史:
# 2026-08-08 - 小欧 - 全程统一本地时区: 新增 get_local_iso_timestamp 返回本地ISO无Z

from datetime import datetime, timezone, timedelta
from typing import Any, Optional

def get_local_iso_timestamp() -> str:
    """获取本地时间戳,ISO格式(无Z/无时区偏移) — 小欧 2026-08-08
    
    全程统一本地时间(task004报告问题1): 与get_utc_timestamp()(UTC)对应, 
    返回本地时区ISO格式(如 2026-08-08T16:52:34.123456)。
    前端 Date.parse 对 T 分隔ISO解析按本地时区, 展示正确;
    若含Z则按UTC解析会差8小时, 故本地时间禁止带Z/偏移后缀。
    """
    return datetime.now().isoformat()
```

#### 4.2.2 新增 `to_local_iso(time_value)`

```python
def to_local_iso(time_value: Any) -> Optional[str]:
    """任意时间值 → 本地ISO(无Z/无偏移) — 小欧 2026-08-08
    
    支持: None→None; datetime(aware/naive)→本地; int/float(毫秒)→本地;
          str(含Z/+00:00/+08:00等任意偏移/naive)→本地。解析失败原样返回。
    用于: database.py 参数归一化、迁移脚本、输出层。
    会审修正: 不依赖'Z'/'+00:00'判断分支, 统一 fromisoformat 解析(Python3.11+
             fromisoformat 原生支持 Z/+08:00 偏移), 转 astimezone 本地后去偏移。
    """
    if time_value is None:
        return None
    # datetime 类型
    if isinstance(time_value, datetime):
        if time_value.tzinfo is not None:
            time_value = time_value.astimezone()  # aware → 本地
        return time_value.replace(tzinfo=None).isoformat()
    # int/float 毫秒
    if isinstance(time_value, (int, float)):
        try:
            return datetime.fromtimestamp(time_value / 1000).isoformat()
        except (OSError, OverflowError, ValueError):
            return str(time_value)
    # 字符串: 统一 fromisoformat 解析(原生支持 Z/偏移), 成功则转本地去偏移, 失败原样返回
    s = str(time_value).strip()
    if not s:
        return None
    try:
        dt = datetime.fromisoformat(s.replace('Z', '+00:00'))  # 兼容裸Z(Python<3.13)
        if dt.tzinfo is not None:
            dt = dt.astimezone()  # 任意偏移 → 本地
        return dt.replace(tzinfo=None).isoformat()  # 去偏移
    except (ValueError, TypeError, OverflowError):
        return s  # 解析失败原样返回
```

#### 4.2.3 修改 `format_timestamp()` 语义

```python
def format_timestamp(val: Any) -> Optional[str]:
    """通用时间戳格式化(本地时区) — 小沈 2026-02-17
    小欧 2026-07-04 修复: 增加OSError捕获，处理Windows不支持负时间戳的问题
    小欧 2026-07-10 M-19: 为None时返回None
    小欧 2026-08-08 全程统一本地: 字符串不再强制追加Z, 走 to_local_iso 统一转换
    """
    if val is None:
        return None
    return to_local_iso(val)
```

#### 4.2.4 `safe_utc_offset()` 保留不动

```python
def safe_utc_offset() -> int:
    """安全获取本地UTC偏移小时数 — 小沈 2026-07-26
    datetime.now().astimezone().utcoffset() 在某些环境返回None, 此时默认UTC+8。
    注意: 此函数获取的是实际偏移, 不固定+8。"""
    try:
        offset = datetime.now().astimezone().utcoffset()
        if offset is None:
            return 8
        return int(offset.total_seconds() / 3600)
    except (OSError, AttributeError, TypeError):
        return 8
```

#### 4.2.5 不需要改的函数

| 函数 | 原因 |
|------|------|
| `create_timestamp()` | 返回毫秒int（epoch），与时区无关，前端 `new Date(ms)` 正确 |
| `ensure_timestamp_milliseconds()` | 返回毫秒int，同上 |
| `convert_to_utc()` | 保留作为工具函数（可能有历史数据/外部格式读取场景），但业务层不再调用 |

#### 4.2.6 `__all__` 更新

```python
__all__ = [
    "create_timestamp",
    "get_utc_timestamp",
    "get_local_iso_timestamp",  # 新增
    "to_local_iso",             # 新增
    "convert_to_utc",
    "ensure_timestamp_milliseconds",
    "timestamp_for_filename",
    "now_str",
    "format_timestamp",
    "safe_utc_offset",
]
```

### 4.3 各文件改动（代码diff）

#### 4.3.1 `app/db/database.py` — datetime参数归一化改本地

```diff
- from app.utils.time_utils import convert_to_utc
+ from app.utils.time_utils import to_local_iso

  class _ParamSafeConnection:
      _SAFE_PARAM_TYPES = (str, int, float, bytes, bool, type(None))

      @staticmethod
      def _validate(params):
-         """验证并转换参数: datetime/date 自动 UTC ISO 8601 Z 字符串"""
+         """验证并转换参数: datetime/date 自动本地ISO无Z字符串"""
          ...
          if isinstance(params, dict):
-             return {k: convert_to_utc(v) if isinstance(v, (datetime, date)) else v
+             return {k: to_local_iso(v) if isinstance(v, (datetime, date)) else v
                      for k, v in params.items()}
          ...
          for _p in params:
              if isinstance(_p, (datetime, date)):
-                 _p = convert_to_utc(_p)  # 边界自动归一化: datetime→UTC ISO 8601 Z
+                 _p = to_local_iso(_p)  # 边界自动归一化: datetime→本地ISO无Z
```

#### 4.3.2 `app/services/chat/storage.py` — 4处改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  # L147 insert_assistant_message
- utc_time = get_utc_timestamp()
+ local_time = get_local_iso_timestamp()
  cursor.execute(...)

  # L189 update_session_message_count
- utc_time = get_utc_timestamp()
+ local_time = get_local_iso_timestamp()

  # L232 allocate_and_insert_message
- utc_time = get_utc_timestamp()
+ local_time = get_local_iso_timestamp()

  # L329 append_execution_step
  (message_id, session_id, step_index, safe_json_dumps(step_dict), get_local_iso_timestamp()),
```

#### 4.3.3 `app/services/agent/steps/base.py` — timestamp默认值改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  class ReasoningStep(ABC):
      def __init__(self, step: int, timestamp: Optional[str] = None):
          self._step = step
-         self._timestamp = timestamp or get_utc_timestamp()
+         self._timestamp = timestamp or get_local_iso_timestamp()
```

#### 4.3.4 `app/api/v1/messages.py` — save_message + 输出改本地

```diff
  # 实际代码是两行 import（L33 + L34），必须分两行改
- from app.utils.time_utils import convert_to_utc, ensure_timestamp_milliseconds, get_utc_timestamp
- from app.utils.time_utils import format_timestamp
+ from app.utils.time_utils import ensure_timestamp_milliseconds, get_local_iso_timestamp, to_local_iso
+ from app.utils.time_utils import format_timestamp

  # L138 save_message
- utc_time = get_utc_timestamp()
+ local_time = get_local_iso_timestamp()

  # L90 title_updated_at 输出
- "title_updated_at": convert_to_utc(session['title_updated_at']),
+ "title_updated_at": to_local_iso(session['title_updated_at']),
```

#### 4.3.5 `app/api/v1/sessions.py` — 3处写入 + 输出改本地

```diff
- from app.utils.time_utils import get_utc_timestamp, now_str, format_timestamp, convert_to_utc
+ from app.utils.time_utils import get_local_iso_timestamp, now_str, format_timestamp, to_local_iso

  # L33 create_session
- utc_time = get_utc_timestamp()
+ local_time = get_local_iso_timestamp()

  # L190 update_session
- utc_time = get_utc_timestamp()
+ local_time = get_local_iso_timestamp()

  # L271 delete_session
- utc_time = get_utc_timestamp()
+ local_time = get_local_iso_timestamp()

  # L251 title_updated_at 输出
- "title_updated_at": convert_to_utc(row['title_updated_at'])
+ "title_updated_at": to_local_iso(row['title_updated_at'])
```

#### 4.3.6 `app/api/v1/chat/sse.py` — 默认timestamp改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  # L95 默认值
- timestamp=step.get('timestamp', get_utc_timestamp())
+ timestamp=step.get('timestamp', get_local_iso_timestamp())
```

#### 4.3.7 `app/utils/sse_formatter.py` — 默认timestamp改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  # L43 默认值
- base['timestamp'] = get_utc_timestamp()
+ base['timestamp'] = get_local_iso_timestamp()
```

#### 4.3.8 `app/services/task/task_db.py` — 写入改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  # L61 create_task
- (task_id, "", agent_id, description, TaskStatus.EXECUTING.value, get_utc_timestamp()),
+ (task_id, "", agent_id, description, TaskStatus.EXECUTING.value, get_local_iso_timestamp()),

  # L75 complete_task
- (status, get_utc_timestamp(), success_count, task_id),
+ (status, get_local_iso_timestamp(), success_count, task_id),

  # L121 add_operation
- json.dumps(details) if details else None, error, get_utc_timestamp(),
+ json.dumps(details) if details else None, error, get_local_iso_timestamp(),
```

#### 4.3.9 `app/services/safety/operation_record.py` — 写入改本地 + duration修复

```diff
- from app.utils.time_utils import get_utc_timestamp, convert_to_utc
+ from app.utils.time_utils import get_local_iso_timestamp, to_local_iso

  # L19 from datetime import ... 需移除 timezone（v1.4修正: L148/190/204 全改后 timezone 不再使用）
- from datetime import datetime, timedelta, timezone
+ from datetime import datetime, timedelta
```

#### 4.3.10 `app/services/safety/operation_rollback.py` — rolled_back_at改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  # L103
- (OperationStatus.ROLLBACK.value, get_utc_timestamp(), operation_id),
+ (OperationStatus.ROLLBACK.value, get_local_iso_timestamp(), operation_id),
```

#### 4.3.11 `app/services/safety/operation_cleanup.py` — 比较基准改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  # L86
- (get_utc_timestamp(),),
+ (get_local_iso_timestamp(),),
```

#### 4.3.11b `app/tools/timer/timer_set.py` — 写入改本地（会审新增，问题4）

**v1.3 修正**：实际代码 L113 `trigger_at` 用 `datetime.now().astimezone()`（aware），若只改 INSERT 处，`trigger_at.isoformat()` 仍带 `+08:00` 偏移，违反"全程本地无Z"。**最简方案：L113 直接改 naive 本地**，则 L118/L143/L148 引用 trigger_at 处自然无偏移（KISS-DIRECT）。

```diff
+ from app.utils.time_utils import get_local_iso_timestamp  # 新增导入

  # L113 trigger_at 计算（v1.3 修正：aware → naive 本地，杜绝 +08:00 偏移）
- trigger_at = datetime.now().astimezone() + timedelta(seconds=delay)
+ trigger_at = datetime.now() + timedelta(seconds=delay)

  # L39 _invoke_timer_callback 事件 triggered_at（v1.5 补：4.3.11b 原漏改，进入内存 _timer_events 列表）
-         "triggered_at": datetime.now().astimezone().isoformat(),
+         "triggered_at": get_local_iso_timestamp(),

  # L117-118 内存 _timer_callbacks（created_at 改本地无Z；trigger_at 因 L113 改 naive 已无偏移）
-         "created_at": datetime.now().astimezone().isoformat(),
+         "created_at": get_local_iso_timestamp(),
          "trigger_at": trigger_at.isoformat(),

  # L127 _timer_cb 触发时间
- conn.execute("UPDATE timers SET status='triggered', triggered_at=? WHERE timer_id=?", (datetime.now().astimezone().isoformat(), timer_id))
+ conn.execute("UPDATE timers SET status='triggered', triggered_at=? WHERE timer_id=?", (get_local_iso_timestamp(), timer_id))

  # L143 INSERT created_at/trigger_at
- conn.execute("INSERT OR REPLACE INTO timers (timer_id, delay, callback, created_at, trigger_at, status) VALUES (?, ?, ?, ?, ?, 'active')",
-     (timer_id, delay, callback, datetime.now().astimezone().isoformat(), trigger_at.isoformat()))
+ conn.execute("INSERT OR REPLACE INTO timers (timer_id, delay, callback, created_at, trigger_at, status) VALUES (?, ?, ?, ?, ?, 'active')",
+     (timer_id, delay, callback, get_local_iso_timestamp(), trigger_at.isoformat()))
```

注：`trigger_at.isoformat()` 在 L113 改 naive 后无偏移（本地无Z），与统一格式一致；`timer_list.py` 读取 timers 表仅排序展示，无时区转换，统一后排序正确。
**v1.4 补充**：L148 `trigger_at_str = trigger_at.strftime("%Y-%m-%d %H:%M:%S")` 仅用于展示（空格分隔），L113 改 naive 后自然无偏移，无需改动；`timer_list.py:58 ORDER BY created_at DESC` + `:73 timers.sort(key=trigger_at)` 字符串排序，统一无Z后排序正确。

#### 4.3.12 `app/main.py` — 异常响应timestamp改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  # L101 http_exception_handler
- "timestamp": get_utc_timestamp()
+ "timestamp": get_local_iso_timestamp()

  # L115 validation_exception_handler
- "timestamp": get_utc_timestamp()
+ "timestamp": get_local_iso_timestamp()

  # L131 general_exception_handler
- "timestamp": get_utc_timestamp()
+ "timestamp": get_local_iso_timestamp()
```

#### 4.3.13 `app/api/v1/metrics.py` — 响应timestamp改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  # L70,89,110,125
- timestamp=get_utc_timestamp(),
+ timestamp=get_local_iso_timestamp(),
```

#### 4.3.13b `app/services/monitoring/collector.py` — 指标timestamp改本地（v1.5新增，第22文件）

**v1.5 新增（严重）**：metrics.py 响应 timestamp 改本地后，**summary 内每个指标的 timestamp 仍带 `+00:00`** —— 因为该字段来自 `collector.py`：

```diff
- from datetime import datetime, timedelta, timezone
+ from datetime import datetime, timedelta

  # L30 Metric 时间戳（v1.5: aware UTC → naive 本地）
-     timestamp: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
+     timestamp: datetime = field(default_factory=lambda: datetime.now())

  # L94 清理旧指标比较基准（v1.5: 必须与 L30 同步改 naive，否则 L100 naive vs aware 比较 TypeError）
-         cutoff_time = datetime.now(timezone.utc) - timedelta(seconds=self.retention_period)
+         cutoff_time = datetime.now() - timedelta(seconds=self.retention_period)

  # L130 _calculate_basic_stats 输出（改后自动为本地无Z，无需改动行）
          "timestamp": metrics_list[-1].timestamp.isoformat(),
```

**关键关联**：L30 与 L94 必须**同步**改 —— L30 改 naive 后，若 L94 仍 aware，L100 `metric.timestamp > cutoff_time` 会抛 `TypeError: can't compare offset-naive and offset-aware datetimes`。同时 `timezone` import 在 L30/L94 改后不再使用，需从 import 移除。


#### 4.3.14 `app/api/v1/health.py` — 响应timestamp改本地

```diff
- from app.utils.time_utils import get_utc_timestamp
+ from app.utils.time_utils import get_local_iso_timestamp

  # L61,73
- timestamp=get_utc_timestamp(),
+ timestamp=get_local_iso_timestamp(),
```

### 4.4 迁移脚本设计

新建 `backend/scripts/migrate_utc_to_local.py`：

**关键点**：UTC→本地 转换用 `astimezone()` 自动转本地（Python 自动处理时区/夏令时），**不能固定+8，也不用手动 safe_utc_offset**。

```python
def utc_z_to_local(utc_str: str) -> str:
    """UTC Z / 带偏移 字符串 → 本地ISO无Z — 小欧 2026-08-08
    会审修正: 用 astimezone() 自动转本地(Python 按当前系统时区/夏令时处理),
    替代 safe_utc_offset() 手动加偏移(后者对历史数据/夏令时切换不准确, 且是绕路)。
    判断逻辑扩展: 以 Z 结尾 或 含时区偏移(如+08:00) 均需转换; 本地naive无偏移原样跳过。"""
    if not utc_str or not isinstance(utc_str, str):
        return utc_str  # 空/非字符串，跳过
    s = utc_str.strip()
    # 判断是否含时区信息(Z 或 +08:00 偏移) — 会审修正: 兼容 timers 表的 +08:00 格式
    has_tz = s.endswith('Z') or ('+' in s[10:]) or ('-' in s[10:])
    if not has_tz:
        return utc_str  # 已是本地naive无偏移，跳过(幂等)
    try:
        dt = datetime.fromisoformat(s.replace('Z', '+00:00'))  # Z → +00:00
        local_dt = dt.astimezone()  # 任意时区 → 本地(自动处理偏移/夏令时)
        return local_dt.replace(tzinfo=None).strftime('%Y-%m-%dT%H:%M:%S.%f')
    except (ValueError, TypeError, OverflowError):
        return utc_str  # 解析失败原样返回(幂等)
```

**迁移步骤**：
1. 自动备份三个DB文件（.bak）
2. 遍历三库所有时间列
3. 逐行判断：以 `Z` 结尾 → 用 `utc_z_to_local()` 转换
4. 已是非Z格式跳过（幂等）
5. SQL抽查验证

**涉及表/列**：

| DB | 表 | 列 |
|----|----|----|
| chat_history.db | chat_messages | timestamp |
| chat_history.db | chat_sessions | created_at, updated_at, title_updated_at |
| chat_history.db | chat_message_steps | created_at |
| chat_history.db | chat_session_title_history | created_at |
| operations.db | file_operations | created_at, executed_at, rolled_back_at, backup_expires_at |
| operations.db | timers | created_at, trigger_at, triggered_at |
| task_tracker.db | tasks | created_at, completed_at |
| task_tracker.db | task_operations | created_at |

**会审修正（timers 表纳入统一）**：
- 原 v1.1 不迁移 timers，但其 `created_at/trigger_at/triggered_at` 用 `datetime.now().astimezone().isoformat()`（带 `+08:00` 偏移）——与其他表本地无Z格式不一致，违反"全程统一"目标
- **修正**：`timer_set.py` 同步改 `get_local_iso_timestamp()`（L127 triggered_at、L143 INSERT created_at/trigger_at），timers 表纳入迁移脚本
- 迁移时 timers 的 `+08:00` 偏移值走 `utc_z_to_local` 的分支逻辑需兼容：带 `+08:00` 但不以 Z 结尾。**迁移脚本需扩展判断**：`Z` 结尾 或 含 `+` 偏移 都转本地无Z

**不迁移**：
- epoch毫秒字段：与时区无关，不需转换

### 4.5 排序关联验证

改完后所有时间列格式统一为 `YYYY-MM-DDTHH:MM:SS.microseconds`（无Z），字符串排序即时间排序：

- `chat_messages` ORDER BY timestamp ASC — OK
- `chat_sessions` ORDER BY updated_at DESC, created_at DESC — OK
- `tasks` ORDER BY created_at DESC — OK
- `file_operations` ORDER BY created_at — OK

**必须在迁移完成后才一致**，否则新旧混存会错位。

## 五、改动文件清单

| # | 文件 | 改动类型 |
|---|------|---------|
| 1 | `app/utils/time_utils.py` | 新增 get_local_iso_timestamp + to_local_iso + format_timestamp改语义 + __all__补2项 |
| 2 | `app/db/database.py` | _validate datetime 归一化改 to_local_iso（import + dict分支 + tuple分支 3处） |
| 3 | `app/services/chat/storage.py` | 4处 get_utc_timestamp → get_local_iso_timestamp |
| 4 | `app/services/agent/steps/base.py` | timestamp默认值改 get_local_iso_timestamp |
| 5 | `app/api/v1/messages.py` | save_message改本地 + title_updated_at输出改 to_local_iso（import 两行分改） |
| 6 | `app/api/v1/sessions.py` | 3处写入改本地 + title_updated_at输出改 to_local_iso |
| 7 | `app/api/v1/chat/sse.py` | 默认timestamp改 get_local_iso_timestamp |
| 8 | `app/utils/sse_formatter.py` | 默认timestamp改 get_local_iso_timestamp |
| 9 | `app/services/task/task_db.py` | 3处写入改 get_local_iso_timestamp |
| 10 | `app/services/safety/operation_record.py` | 5处写入改本地 + duration修复(naive/aware) + backup_expires_at改本地 |
| 11 | `app/services/safety/operation_rollback.py` | rolled_back_at改 get_local_iso_timestamp |
| 12 | `app/services/safety/operation_cleanup.py` | 比较基准改 get_local_iso_timestamp |
| 13 | `app/tools/timer/timer_set.py` | L39 triggered_at + L113 trigger_at 改naive + L117 created_at 改本地 + L127 triggered_at + L143 INSERT（v1.3补全 + v1.5补L39） |
| 14 | `app/main.py` | 3处异常响应timestamp改 get_local_iso_timestamp |
| 15 | `app/api/v1/metrics.py` | 4处响应timestamp改 get_local_iso_timestamp |
| 16 | `app/api/v1/health.py` | 2处响应timestamp改 get_local_iso_timestamp |
| 17 | `app/db/db_initializer.py` | 注释同步（`-- UTC ISO 8601` → `-- 本地ISO无Z`，共10处） |
| 18 | `app/db/models/chat_models.py` | L68 注释同步（ISO 8601 UTC格式 → 本地ISO无Z） |
| 19 | `app/db/operation_queries.py` | format_timestamp跟随（不单独改） |
| 20 | `app/api/v1/task_queries.py` | format_timestamp跟随 |
| 21 | `backend/scripts/migrate_utc_to_local.py` | **新增**迁移脚本 |
| 22 | `app/services/monitoring/collector.py` | **v1.5新增**：L30 timestamp + L94 cutoff_time 改 naive 本地（timezone import 移除） |

## 六、完整实施计划（分阶段，按依赖排序）

> **v1.3 重写**：每阶段含【文件】【改动点】【验证】【退出标准】，按依赖顺序执行，后一阶段依赖前一阶段成功。

### 阶段 0：预检（不动代码）

| 步骤 | 动作 | 验证 |
|------|------|------|
| 0.1 | 确认 DB 现状：三库路径 `~/.omniagent/chat_history.db`、`operations.db`、`task_tracker.db` | 存在且可连接 |
| 0.2 | 抽查现有数据时间格式：`SELECT created_at FROM chat_messages LIMIT 5` | 确认 Z 结尾（待迁移） |
| 0.3 | `git status` 确认工作区基线（当前改动仅 time_utils.py + database.py） | 与文档一致 |

**退出标准**：三库时间列确认为 UTC Z 格式，工作区基线明确。

### 阶段 1：核心工具层（time_utils.py + database.py）

| 步骤 | 文件 | 改动点 | 验证 |
|------|------|--------|------|
| 1.1 | `app/utils/time_utils.py` | ① 新增 `get_local_iso_timestamp()`（返回 `datetime.now().isoformat()`）；② 新增 `to_local_iso(time_value)`（统一解析转本地无Z）；③ `format_timestamp()` 改 `return to_local_iso(val)`；④ `__all__` 补 `get_local_iso_timestamp`、`to_local_iso` | `python -c "from app.utils import time_utils as t; print(t.get_local_iso_timestamp()); print(t.to_local_iso('2026-08-08T08:30:00Z')); print(t.format_timestamp('2026-08-08T08:30:00.123+00:00'))"` 输出本地无Z |
| 1.2 | `app/db/database.py` | import 改 `to_local_iso`；`_validate` dict分支 + tuple分支的 `convert_to_utc` → `to_local_iso`（共3处） | `python -m py_compile app/db/database.py` |
| 1.3 | — | 单元验证：`to_local_iso` 输入 None/naive/aware/int/str(Z)/str(+08:00) 全用例 | 无异常，全转本地无Z |

**退出标准**：py_compile 通过；`to_local_iso` 6类输入验证正确。

### 阶段 2：存储线改本地（12个文件，互不依赖可并行改）

| 步骤 | 文件 | 改动点（行号） |
|------|------|------|
| 2.1 | `app/services/chat/storage.py` | L147/189/232/329 四处 `get_utc_timestamp()` → `get_local_iso_timestamp()`，import 同步 |
| 2.2 | `app/services/agent/steps/base.py` | L17 import、L28 默认值改 `get_local_iso_timestamp()` |
| 2.3 | `app/api/v1/messages.py` | L33 import 改（去 convert_to_utc/get_utc_timestamp，加 get_local_iso_timestamp/to_local_iso）、L34 保留 format_timestamp；L138 写入改本地；L90 title_updated_at 输出改 `to_local_iso` |
| 2.4 | `app/api/v1/sessions.py` | L17 import 改；L33/190/271 写入改本地；L251 title_updated_at 输出改 `to_local_iso` |
| 2.5 | `app/api/v1/chat/sse.py` | L18 import、L95 默认值改 `get_local_iso_timestamp()` |
| 2.6 | `app/utils/sse_formatter.py` | L16 import、L43 默认值改 `get_local_iso_timestamp()` |
| 2.7 | `app/services/task/task_db.py` | L28 import；L61/75/121 写入改 `get_local_iso_timestamp()` |
| 2.8 | `app/services/safety/operation_record.py` | L19 import 移除 timezone；L28 import 改（去 get_utc_timestamp/convert_to_utc，加 get_local_iso_timestamp/to_local_iso）；L105/152/189/206 写入改本地；L146-148 duration 修复（**astimezone().replace(tzinfo=None) 先转本地再去tzinfo**，v1.4修正）；L189 executed_at_dt 改 `datetime.now()`；L204 backup_expires_at 改 `(datetime.now() + timedelta(...)).isoformat()` |
| 2.9 | `app/services/safety/operation_rollback.py` | L18 import、L103 rolled_back_at 改本地 |
| 2.10 | `app/services/safety/operation_cleanup.py` | L20 import、L86 比较基准改本地 |
| 2.11 | `app/tools/timer/timer_set.py` | 新增 import；**L39 triggered_at 改 `get_local_iso_timestamp()`（v1.5补）**；L113 trigger_at 改 `datetime.now()`（naive）；L117 created_at 改 `get_local_iso_timestamp()`；L127 triggered_at 改本地；L143 INSERT created_at 改本地 |
| 2.12 | `app/services/monitoring/collector.py` | **v1.5新增**：L30 `Metric.timestamp` 改 `datetime.now()`（naive）；**L94 `cutoff_time` 同步改 `datetime.now()`**（否则 L100 比较 TypeError）；import 移除 timezone |

**验证（每文件）**：`python -m py_compile <file>` 通过；grep 确认无残留 `get_utc_timestamp`/`convert_to_utc`（预期仅 time_utils.py 定义处 + 测试保留）。

**退出标准**：12个改动文件全部 py_compile 通过；`grep -r "get_utc_timestamp" app/` 仅剩 time_utils.py 定义 + 测试文件。

### 阶段 3：注释同步（db_initializer.py + chat_models.py）

| 步骤 | 文件 | 改动点 |
|------|------|--------|
| 3.1 | `app/db/db_initializer.py` | 10处 `-- UTC ISO 8601` 注释 → `-- 本地ISO无Z`（L31/32/37/46/55/68/118/127/128/129/190/191/209） |
| 3.2 | `app/db/models/chat_models.py` | L68 `时间戳(ISO 8601 UTC格式)` → `时间戳(本地ISO无Z)` |

**验证**：py_compile 通过；注释与代码语义一致。

### 阶段 4：迁移脚本 + 数据迁移

| 步骤 | 文件 | 改动点 | 验证 |
|------|------|--------|------|
| 4.1 | `backend/scripts/migrate_utc_to_local.py`（**新增**） | `utc_z_to_local()` 用 `astimezone()` 自动转本地；has_tz 判断 `Z`结尾 或 `s[10:]`含 `+`/`-`；遍历三库8张表17列；逐表备份 `.bak`；幂等 | `python -c "from scripts.migrate_utc_to_local import utc_z_to_local; print(utc_z_to_local('2026-08-08T08:30:00Z')); print(utc_z_to_local('2026-08-08T16:30:00.123+08:00')); print(utc_z_to_local('2026-08-08T16:30:00.123'))"` 前两者→本地无Z，第三者原样 |
| 4.2 | 执行迁移 | `python scripts/migrate_utc_to_local.py` | 三库 `.bak` 生成；日志显示逐表行数 |
| 4.3 | SQL 抽查 | 三库各抽查：原 Z 值 → 本地无Z，且比 UTC 多 8h（本机） | 时间正确 |
| 4.4 | 幂等验证 | 重跑迁移脚本 | 二跑无行变更（naive 跳过） |

**退出标准**：迁移完成、`.bak` 存在、抽查正确、幂等验证通过。

### 阶段 5：测试适配 + 回归验证

**v1.4 新增（严重）**：改本地无Z后，两个测试文件必须同步适配，否则回归失败：

| 步骤 | 测试文件 | 改动点 |
|------|---------|--------|
| 5.0a | `tests/test_8_05_concurrent_create.py` | L76 `patch("app.api.v1.sessions.get_utc_timestamp", return_value="2026-07-03T00:00:00Z")` → 改 patch `app.api.v1.sessions.get_local_iso_timestamp`，返回值改 `"2026-07-03T00:00:00"`（本地无Z） |
| 5.0b | `tests/steps/test_observation_step.py` | L89 `assert s._timestamp.endswith("Z")` → 改 `assert not s._timestamp.endswith("Z")`（本地无Z）；L85/L6 注释同步 |

> ⚠️ 测试适配**只允许本地修改用于验证，不 commit 测试文件**（AGENTS.md 铁规：严令禁止 commit 测试相关代码文件）。

| 步骤 | 动作 | 验证 |
|------|------|------|
| 5.1 | 后端启动 `python -m uvicorn app.main:app --port 8000` | 启动无异常 |
| 5.2 | 单元测试 `pytest -x --tb=short tests/test_same_tool_loop_defense.py`（含 test_bug_discovery） | 全过 |
| 5.3 | `pytest -x --tb=short tests/test_8_05_concurrent_create.py tests/steps/test_observation_step.py`（测试适配后） | 全过 |
| 5.4 | API 抽查：`/api/v1/health`、`/api/v1/metrics`、会话列表 | 响应 timestamp 为本地无Z |
| 5.5 | 前端冒烟：聊天列表排序、消息时间显示、执行步骤时间显示 | 时间显示本地正确，排序正常 |

### 阶段 6：提交

| 步骤 | 动作 |
|------|------|
| 6.1 | 分文件提交：`fix:<文件名> 时间戳全程统一本地时区 - 小欧-2026-08-08` |
| 6.2 | 迁移脚本单独提交：`feat:migrate_utc_to_local.py ... - 小欧-2026-08-08` |
| 6.3 | 更新 version.txt（从上一tag以来的变更汇总） |

## 七、风险与回退

| 风险 | 应对 |
|------|------|
| 迁移前新代码已写本地、旧数据仍UTC → 混存乱序 | 迁移脚本幂等，可重跑 |
| 前端对历史UTC数据解析 | 迁移已全部转本地，前端无需兼容旧格式 |
| 迁移出错 | 自动备份.bak，可恢复 |
| 跨时区环境偏移不同 | `astimezone()` 自动转本地，不固定+8，不依赖 safe_utc_offset |
| 迁移脚本遇 timers 表 `+08:00` 偏移格式 | 判断逻辑扩展：`Z` 结尾 或 含 `+`/`-` 偏移 均转本地无Z（会审修正） |
| duration 解析遇迁移前 UTC Z 旧数据 | **astimezone().replace(tzinfo=None) 先转本地再去tzinfo**（v1.4修正，实测验证差8h问题已消除） |
| 测试适配 | test_8_05_concurrent_create.py:76 / test_observation_step.py:89 需同步改本地无Z（仅本地验证，不commit测试文件） |

## 八、三堂会审结论（2026-08-08 18:41:31，小欧）

### 8.1 合规检查（10大规范）

| 规范 | 结论 |
|------|------|
| SRP 单一职责 | ✅ get_local_iso_timestamp 只做"当前本地时间"；to_local_iso 只做"值转本地ISO" |
| DRY 不重复 | ✅ 转换逻辑集中在 to_local_iso，format_timestamp/迁移脚本复用 |
| KISS-DIRECT | ⚠️ 迁移脚本原用 safe_utc_offset 手动加偏移→改为 astimezone() 自动转（修正） |
| SLAP 同一抽象层 | ✅ 业务文件只换函数名，不混入转换细节 |
| YAGNI 不过度设计 | ✅ 未引入不必要抽象 |

### 8.2 合理检查（逻辑最优）

| 项 | 结论 |
|----|------|
| duration 计算 | ✅ created_at 本地naive - executed_at 本地naive，类型一致 |
| 排序一致性 | ✅ 全部本地无Z格式，字符串排序即时间排序 |
| 前端零改动 | ✅ Date.parse 无Z ISO按本地解析 |
| timers 表 | ⚠️ 原排除→纳入统一并改 timer_set.py（修正） |

### 8.3 关联逻辑检查（增强不退化）

| 项 | 结论 |
|----|------|
| to_local_iso 偏移解析 | ⚠️ 原仅判断 Z/+00:00 → 改 fromisoformat 统一解析（兼容+08:00） |
| 迁移判断逻辑 | ⚠️ 原仅 Z 结尾 → 扩展为 Z 或 含偏移（修正） |
| convert_to_utc 保留 | ✅ 测试 test_bug_discovery 依赖，保留不删 |
| format_timestamp 语义 | ✅ 走 to_local_iso，str 原样返回不再强制加 Z |

**会审结论**：6 个问题（4严重+2中）已全部修正，方案 v1.2 通过三堂会审，可实施。

### 8.4 v1.3 复核结论（2026-08-08 18:55:13，小欧）

对 v1.2 实施代码**逐文件对照真实代码**复核，确认实施代码准确、实施计划完整：

| 核查项 | 结论 |
|--------|------|
| messages.py import | ⚠️ v1.2 合并成一行与真实代码(L33+L34两行)不符 → 已修正为两行分改（4.3.4） |
| timer_set.py trigger_at | ⚠️ v1.2 漏改 L113 aware trigger_at → 已补全为 L113 naive 本地（4.3.11b），杜绝 +08:00 偏移残留 |
| 迁移脚本 has_tz 判断 | ✅ 核查通过：`s[10:]` 从 "T" 开始取，本地无Z格式不误判，Z/+08:00 均正确识别 |
| 文件清单 | ✅ 21文件全部核对，db_initializer 注释10处、chat_models L68 位置已补准确 |
| 实施计划 | ⚠️ v1.2 验证计划仅6条 → v1.3 重写为六阶段完整实施计划（阶段0预检~阶段6提交，含每步文件/改动点/验证/退出标准） |
| database.py 三处改动 | ✅ import + dict分支 + tuple分支 全部覆盖（v1.2 已含 tuple 分支，无遗漏） |

**v1.3 复核结论**：v1.2 的 4 处不准确/不完整已全部修正，实施代码与实际代码逐行一致，实施计划完整可执行，方案 v1.3 通过复核，可实施。

### 8.5 v1.4 三堂会审复核结论（2026-08-08 19:03:56，小欧）

**方法**：设计文档全文熟读3遍 + 逐文件对照真实代码（含测试文件）+ Python 实测验证关键逻辑。

| 核查项 | 严重度 | 结论 |
|--------|--------|------|
| 4.3.9 duration 修复 | 🔴严重 | `replace(tzinfo=None)` 只剥tzinfo不转本地，迁移前UTC旧数据 duration 差8h。**实测**：`08:30Z` 剥后 `08:30`(错) vs `astimezone()` 转 `16:30`(对)。→ 已修正为 `astimezone().replace(tzinfo=None)` |
| test_8_05_concurrent_create.py:76 | 🔴严重 | patch `sessions.get_utc_timestamp`，sessions.py 改 import 后属性消失 → 测试崩。→ 阶段5新增适配步骤（patch 改 `get_local_iso_timestamp`，返回值改本地无Z） |
| test_observation_step.py:89 | 🔴严重 | `assert s._timestamp.endswith("Z")`，base.py 默认值改本地无Z后断言失败。→ 阶段5新增适配步骤（断言改 `not endswith("Z")`） |
| operation_record.py:19 timezone import | 🟡中 | L148/190/204 全改后 timezone 不再使用 → import 移除 timezone（4.3.9/2.8 已补充） |
| test_8_05 返回值 `"2026-07-03T00:00:00Z"` | 🟡中 | patch 返回值需同步改本地无Z（阶段5） |
| 阶段2标题"10个文件" | 🟡中 | 实为11个（2.1~2.11）→ 已修正为11个 |
| 迁移脚本 has_tz 判断 | ✅ | `s[10:]` 从索引10("T")开始，日期部分 `-` 不误判；负数偏移 `-05:00` 正确识别 |
| 4.3.11b L148 strftime 展示 | ✅ | 仅展示用，L113 改 naive 后无偏移，无需改动 |
| timer_list.py 排序 | ✅ | L58 ORDER BY created_at + L73 sort(trigger_at)，统一无Z后字符串排序正确 |
| format_timestamp 语义 | ✅ | 走 to_local_iso，str 原样返回不再强制加 Z；test_bug_discovery test_format_timestamp_negative 依赖保留 |

**v1.4 复核结论**：4个严重 + 3个中等问题已全部修正，测试适配已纳入实施计划阶段5，方案 v1.4 通过三堂会审复核，可实施。

### 8.6 v1.5 全方位漏查复核结论（2026-08-08 19:17:06，小欧）

**方法**：针对"需要修改的文件务必都找全（不能多也不能少）"，对全部时区相关代码（`timezone.utc`/`datetime.now(timezone)`/`astimezone()`/`get_utc_timestamp`/`convert_to_utc` grep 全量 33+68 处）逐项定论。**范围界定：仅系统代码（DB/API/前端/日志存储线），不涉及 tool 函数内部逻辑（tool 可自有规则，仅落盘/显示时遵守统一规则）。**

| 代码位置 | 严重度 | 定论 |
|----------|--------|------|
| `collector.py` L30 `Metric.timestamp = datetime.now(timezone.utc)` | 🔴严重 | **整文件遗漏（第22文件）**。aware UTC 经 L130 `.isoformat()` 输出 `+00:00` 到 metrics summary 每指标 timestamp，与 metrics.py 响应 timestamp 改本地后不一致 → 补 4.3.13b + 阶段2.12 |
| `collector.py` L94 `cutoff_time = datetime.now(timezone.utc)` | 🔴严重 | 必须与 L30 **同步**改 naive（L100 比较否则 TypeError）→ 已纳入 4.3.13b/2.12 |
| `timer_set.py` L39 `triggered_at = datetime.now().astimezone().isoformat()` | 🟡中 | **漏改**。4.3.11b 原覆盖 L113/117/127/143 漏 L39，进入内存 `_timer_events` 列表带 `+08:00` → 补 4.3.11b/2.11 |
| `operation_record.py` L148 `replace(tzinfo=timezone.utc)` | ✅已覆盖 | v1.4 已改 `astimezone().replace(tzinfo=None)`（先转本地再去tzinfo） |
| `migrate_time_format.py`（旧脚本） | ✅不改 | 历史脚本（UTC Z 迁移），非本次目标，不执行不修改 |
| tool 函数 `time_now`/`time_diff`/`time_add`/`tool_fc_helper` | ✅不涉及 | **本次仅系统代码，不涉及 tool 函数内部逻辑**。tool 可自有规则，仅要求落盘（DB写入）/显示（API/前端/日志）时遵守统一规则 |

**v1.5 复核结论**：遗漏的 collector.py（第22文件）与 timer_set.py L39 已补入设计、文件清单、实施计划（阶段2.11/2.12）、三堂会审结论。文件清单由 21 → **22 个**，全部代码位置定论无更多遗漏，方案 v1.5 通过全方位漏查，可实施。

### 8.7 v1.6 设计整体一致性复核结论（2026-08-08 19:24:51，小欧）

**判定标准**（老陈明确）：时间值流出边界（落盘DB/API/SSE/日志）→ 必须统一本地无Z；困在 tool 内部（计算/展示）→ 保持 aware 自有规则不改。**设计整体必须与此一致，否则出错。**

**逐项核对设计整体与判定标准一致性**：

| 设计项 | 判定标准要求 | 设计现状 | 一致性 |
|--------|-------------|---------|--------|
| 22 文件清单 | 落盘/显示边界全改 | 22 文件全部属落盘/显示边界 | ✅ |
| 阶段2 实施（12文件存储线） | 落盘全改 | storage/base/messages/sessions/sse/task_db/operation_*/timer_set/collector 全为落盘/输出边界 | ✅ |
| 迁移脚本 + 迁移表（8表17列） | DB 存量数据统一 | timers 表纳入（+08:00 也转本地无Z） | ✅ |
| `time_now` 不纳入 | 困在内部、输出天然本地 | 不改，天然合规 | ✅ |
| `time_diff`/`time_add` 不纳入 | 纯计算型、内部 aware 必须 | 不改，输出 humanized/本地无偏移字符串 | ✅ |
| `tool_fc_helper` 不纳入 | 参数解析、不出边界 | 不改 | ✅ |
| `timer_list` 不纳入 | 只读展示排序、不落盘不输出时间值 | 不改，统一无Z后排序正确 | ✅ |
| collector.py L30/L94 | 落盘/输出边界同步改 naive | 已补 4.3.13b/2.12 | ✅ |

**v1.6 复核结论**：设计整体（文件清单/实施计划/迁移脚本/迁移表/tool 不纳入范围）与统一判定标准**完全一致**，无越界改动、无遗漏改动。方案 v1.6 通过设计整体一致性复核，可实施。

---

**编写人**: 小欧
**更新人**: 小欧（v1.2 三堂会审修正 / v1.3 实施代码复核 / v1.4 全文熟读3遍上上下下关联逻辑复核含测试 / v1.5 全方位漏查 collector.py+timers_set.py L39 / v1.5 范围界定 tool 自有规则、落盘显示统一 / v1.6 统一/不统一判定标准固化 4.1.1 + 设计整体一致性复核 8.7）
**日期**: 2026-08-08 19:24:51
