# 规范：Tool / Format 门限分工（三域：输入 / 输出-数据 / 输出-显示）

**创建时间**：2026-07-19 16:45:00
**版本**：v3.17
**编写人**：小欧（陈）— 三思三省后自定
**更新时间**：2026-07-20 04:00:41
**更新要点**：v3.17 据实纠错——修正截断说明行模板多余`)`(3.2.2/5.3.2 对齐 observation_formatter.py:990)、第七章与5.5结论加范围限定(grep已落地、其余工具待逐工具清理)、创建时间对齐初版v3.0；grep代码(#3)已于2026-07-20落地零改动；shell归下阶段不处理；原"参数表误删literal/output_mode"与"3.5字面张力"经复核为误读不修正
**版本历史**：
- v3.0（2026-07-19 16:45:00，小欧）：初版三域门限分工；3.5铁律输入/输出常量工具专用；Format行×列200×150；_SKIP_DIRS合并公用
- v3.1（2026-07-20 01:20:03，小欧 / 北京老陈）：grep 全部输入限制去除决策（对齐 opencode-old rg 无限制）
- v3.2（2026-07-20 01:23:11，小欧 / 北京老陈）：明确参数规定= schema（5.1.1）；schema 之外叠加的输入大小/长度限制属多余，依 3.6 去除
- v3.3（2026-07-20 01:26:25，小欧 / 北京老陈）：依 3.6 调整第5章——5.1.2 改"仅安全检测+已去除输入门限"；5.4.1/5.4.2 第3点"输入限制去除"取代原"输入常量专用化"（与原 3.6 冲突，作废）
- v3.4（2026-07-20 01:29:53，小欧 / 北京老陈）：修正全部"已落实/=0"为假落实——代码实测三项仍在（10MB/200/0–10）；落实状态统一改"待落地/代码未改"；3.1 改仅安全检测、3.5/3.6/5.1.2/5.4.1/5.4.2/七 同步修正一致
- v3.5（2026-07-20 01:32:43，小欧 / 北京老陈）：删除 5.1.3 验证节（临时提限对比表/hermes注属测试产物，不进规范）；规范仅留规则+决策+落地计划
- v3.6（2026-07-20 01:42:24，小欧 / 北京老陈）：新增 3.7 Tool 输出零限制铁律——tool 输出不设任何限制（条数/大小/长度），依参数输出多少即多少；唯一限量收口于 Format 行×列截断
- v3.7（2026-07-20 01:43:06，小欧 / 北京老陈）：新增截断说明行铁律（3.2.2/5.3.2）——Format 行×列截断后于详情末尾追加一行，清晰说明按何规则截、截多少、完整数据在 raw data；全部 tool 统一按此
- v3.8（2026-07-20 01:47:54，小欧 / 北京老陈）：核查纠错——修正与 3.7 铁律冲突的自相矛盾（5.4.2 第2点废止条数上限、4.1/4.2 改 Tool 保真收口 Format、3.5 输出门限注作废、二/七 末尾残句清理）；文档与 3.6+3.7+3.2.2 全自洽
- v3.9（2026-07-20 01:49:46，小欧 / 北京老陈）：审核第5章 grep 设计与实施，补5处遗漏（A 5.1.2 补超时保留项/B 5.3.1 补 3.4 truncated 例外/C 5.3.1 补 content 字段 Tool 不截断/D 5.4.2 第4点补截断说明行动作/E 5.4.2 第1点超时归属 3.1）；文档与全部铁律自洽
- v3.10（2026-07-20 01:52:52，小欧 / 北京老陈）：截断说明行格式定稿——首行"本详细有截断。"+ 随后详细说明（行数上限省略多少/单行上限超宽截断/完整结果在 raw data）；3.2.2 与 5.3.2 统一
- v3.11（2026-07-20 01:54:21，小欧 / 北京老陈）：截断说明行格式按北京老陈最终口径定稿（去"完整结果见 raw data"）——"本详细有截断。\n截断说明：行数上限{ROWS}（实际{M}行，省略{K}行）；单行上限{CHARS}字符（超宽行已尾部截断）"；3.2.2 与 5.3.2 统一
- v3.12（2026-07-20 02:00:25，小欧 / 北京老陈）：截断说明行格式再修订（按北京老陈口径）——"本详情{有截断/无截断-完整}:\n截断情况：保留{n}行,实际 {M} 行，截断 {K} 行）；单行上限 {CHARS} 字符（超宽的{n}行尾部截断）"；3.2.2 与 5.3.2 统一
- v3.13（2026-07-20 02:02:13，小欧 / 北京老陈）：审核第5章（v3.12口径）再核，补1处遗漏 F——5.4.2 第4点截断说明行动作项旧格式"⚠ 已截断：…"改为引用 3.2.2/5.3.2 v3.12 新格式；第5章与全部铁律自洽
- v3.14（2026-07-20 02:04:02，小欧 / 北京老陈）：截断说明行两态定稿——有截断保留"⚠ 已截断"（北京老陈定），无截断自定"✓ 无截断-完整"（小欧定）；3.2.2 / 5.3.2 / 5.4.2 第4点统一
- v3.15（2026-07-20 02:22:00，小欧 / 北京老陈）：第5章代码全部落地——grep_file_content.py 删 OBS_MAX_DISPLAY_ITEMS/head_limit/_MAX_PATTERN_LENGTH/输入大小限制(context仅校验<0)；observation_formatter.py _format_matches 改行×列(200×150)+截断说明行两态；list_directory.py/grep_file_content.py _SKIP_DIRS 合并为公用 SKIP_DIRS；tool_constants.py MAX_SEARCH_FILE_SIZE=0 + OBS_GREP_MAX_ROWS/CHARS；import + 实验验证通过
- v3.16（2026-07-20 03:36:13，小欧 / 北京老陈）：文档规则对齐代码——5.x 据实修订(5.1.1 参数7→5、5.1.2 待落地→已落地、5.2.1/5.4.1/5.4.2 改前行号改历史对照、5.3.1 补 skipped_binary_files、5.3.2 无截断改一行、5.4.2/5.5 去行号锚定与结论修正)；原则同步对齐(3.2.2 无截断改一行不附明细、3.6 MAX_SEARCH_FILE_SIZE 改已删除非置0、3.7 落实状态改 grep 已落地)
- v3.17（2026-07-20 04:00:41，小欧 / 北京老陈）：据实纠错——截断说明行模板多余`)`删除(3.2.2/5.3.2 与 observation_formatter.py:990 对齐)；第七章/5.5 结论加范围限定(grep已落地、其余工具待逐工具清理见 3.6/六)；创建时间对齐初版v3.0；grep代码#3已于2026-07-20落地零改动；shell归下阶段不处理；原"参数表误删literal/output_mode"与"3.5字面张力"经复核为误读不修正
**适用**：backend 全部工具（grep / shell / read_* / search_* / network / document / dataanalysis ...）

---

## 一、背景与问题（为何立此规矩）

grep 死循环（step48→99）排查中暴露门限处理混乱，根因是**同一类门限在被重复/错位放置**：

1. **输入输出不分**：输入校验（文件大小/超时/pattern长度）和输出截断混为一谈。
2. **输出两头都管**：grep 在 Format 层用 `OBS_MAX_DISPLAY_ITEMS`（500）管条数 + `OBS_MAX_STRING_LENGTH`（10000）管内容截断，粒度不统一。
3. **Format 无行×列**：只截单串（10000），没管"一共显示多少行×多宽"，36 条×7 行累加 919KB >> `MAX_CONTEXT_CHARS=200K`，被 `trim_history` 整条丢弃 → LLM 失明 → 退化重调。
4. **shell 同病**：Tool 截 `SHELL_OUTPUT_MAX_CHARS=20000`，Format 又截 `OBS_MAX_STRING_LENGTH=10000`。

结论：域不分、职责乱、缺行×列。必须立规矩。

## 二、三域架构定义

| 域 | 层 | 职责 | 产出 |
|----|------|------|------|
| **输入域** | 输入校验/安全层（App/validate、tool 输入校验） | 校验参数合法/安全，防 OOM/防超时/防 ReDoS | 合法参数或拒绝 |
| **输出-数据域** | Tool（`app/tools/*/_tools.py`） | 执行操作，产出**原始保真 data** | `data`（常规不设截断；仅 3.4 极端硬安全例外附 `truncated` 标志） |
| **输出-显示域** | Format（`app/services/agent/observation_formatter.py`） | 把 data 渲染成**给 LLM 的观察文本** | 观察字符串（行×列） |

`data` 的消费者不止 LLM：前端原始展示、日志、链式调用。故 `data` 必须**保真**。
上下文裁剪（`message_builder.py`）是第三阶段，与本规范的门限分工无关。

## 三、规矩（铁律）

### 3.1 输入限制
所有工具入参须做**安全类校验**（与输出无关），仅保留：
- ReDoS / 安全检测（嵌套量词等危险正则）
- 路径合法/安全校验（目录存在、权限等）
- 其它安全类检测（非大小/长度限制）

**注**：原"单文件读取大小上限 / pattern 长度上限 / context 范围限制"等输入大小/长度门限，依 3.6 铁律**全部去除**——参数已由 schema（5.1.1）规定，不必另立大小/长度限制。本 3.1 仅保留安全检测类校验，大小/长度类输入门限不再列入。

### 3.2 输出限制

#### 3.2.1 Tool（输出-数据域）——保真，不设任何限制

- **铁律（依 3.7）**：Tool 输出**不设任何限制**——结果条数、字段大小、字段长度统统不限制、不截断，依参数执行后是多少就是多少，全部保真返回。
- **禁止**：为"显示"截断任何字段字符串（只返 raw 保真）；不得用 `GREP_MAX_RESULTS` / `OBS_MAX_DISPLAY_ITEMS` / `SHELL_OUTPUT_MAX_CHARS` / `OBS_MAX_STRING_LENGTH` 等常量对输出设条数/大小/长度上限（已作废）。
- 原"结果条数上限 + `truncated=true`"方案作废：条数超限不再由 Tool 截断，统一收口于 Format 行×列（见 3.2.2 / 3.7）。

#### 3.2.2 Format（输出-显示域）——行×列
- **行数上限**：单条观察最多显示多少行（含 content/上下文/空行）。超过尾部截断，注"还有 N 条"。
- **单行上限（列宽）**：每一渲染行最长字符数，超过末尾截断+标记。
- 别的显示门限（snippet/头尾/预览）作为实现策略内嵌在行×列中，不另立门限。
- **禁止**：修改 raw `data`。
- **截断说明行（铁律）**：必须在观察详情末尾追加状态说明，首行标注截断状态。有截断时另起一行说明截断情况；无截断仅一行状态行，不附冗余明细。两态格式（末尾追加）：
  - 有截断（北京老陈定）：
    ```
    ⚠ 已截断
截断情况：保留{n}行,实际 {M} 行，截断 {K} 行；单行上限 {CHARS} 字符（超宽的{n}行尾部截断）
    ```
  - 无截断（小欧定）：
    ```
    ✓ 无截断-完整
    ```
  此说明只说明截断、不改 raw `data`。

### 3.3 内部规范（非门限）
既不属于输入校验也不属于输出门限的代码内部约定，归为内部规范：
- **数据组织**：工具返回的字段结构、命名约定
- **实现策略**：snippet 构建、上下文组织、遍历策略等内部逻辑
- **内部常量**：与输入校验/输出门限无关的代码常量（如跳过目录名单、错误码）
- **与三域的关系**：内部规范不属于输入/输出-数据/输出-显示任何一域，不设门限值，只描述代码实际做法

### 3.4 例外：硬安全上限
某字段可能撑爆序列化/OOM 时（如单字段>数十 MB），Tool 可设硬安全上限作安全网；须显式命名区分，不得用显示值充当。

### 3.5 门限常量命名规则
**铁律：输入/输出常量必须工具专用，即便数字一样、意义一样也必须专用。只有内部常量可以公用。**

按域约定前缀：
- **输入域常量**：仅安全检测类输入常量须工具专属（如 ReDoS 检测常量）；**大小/长度类输入门限依 3.6 已去除，不再设专用常量**。
- **输出-数据域门限（Tool）**：依 3.7 Tool 输出零限制，`GREP*` 前缀的输出-数据域条数/大小上限（如 `GREP_MAX_RESULTS`）**已作废，不再设**；仅 3.4 极端硬安全上限可作安全网（须显式命名区分，不属常规输出限制）。
- **输出-显示域门限（Format）**：`OBS_GREP_` 前缀（grep 专属渲染），如 `OBS_GREP_MAX_ROWS` / `OBS_GREP_MAX_ROW_CHARS`。
- **内部常量（非门限）**：grep 专属内部常量保持代码原名（如 `_REDOS_PATTERNS`），公用内部常量保持原名（如 `BINARY_EXTENSIONS` / `SKIP_DIRS` / `ERR_FILE_CONTENT_SEARCH_FAILED`），不进 `tool_constants.py` 门限区。

**分组规则**：`tool_constants.py` 中，各工具专属常量（输入域 + 输出域）必须按工具集中分组，不得与公用常量混放。

凡新增门限常量必须按此命名，旧常量（`OBS_MAX_DISPLAY_ITEMS` / `OBS_MAX_STRING_LENGTH`）按上文由工具专属常量或 `OBS_GREP_` 常量取代。公用内部常量保持原名，不加前缀。

### 3.6 输入限制去除铁律（北京老陈 2026-07-20 01:20:03）

**铁律：所有 tool 的输入项目（大小 / 长度）限制统统删掉，不保留任何输入大小/长度门限。**

- **范围**：文件读取大小上限、pattern/查询长度上限、context 等参数范围上限、其它入参长度上限——凡属"输入项大小/长度"的门限，一律去除。
- **理由**：opencode-old 的 rg 无任何输入大小/长度限制；我们的 grep（及全部 tool）须与之对齐，保证匹配数/匹配行号与 opencode-old 完全一致。
- **参数规定以 schema 为准**：参数本身由 schema（5.1.1）定义其类型/默认值/取值范围，schema 即为参数规定的唯一来源；schema 之外再叠加的输入大小/长度门限属多余设计，依本铁律去除——schema 存在即是为规定参数，不应另立限制。
- **例外**：ReDoS / 安全检测（嵌套量词等危险正则）仍保留——属安全检测，非大小/长度限制，不在去除范畴。
- **落实状态（grep 已落地，其余 tool 待逐工具清理）**：
  - `MAX_SEARCH_FILE_SIZE`：已删除（不再限制文件大小），代码已落地（常量删除，`safe_read_lines` 默认不限制），对齐 opencode-old rg 无文件大小限制
  - `GREP_MAX_PATTERN_LENGTH`（pattern 长度上限）、`GREP_CONTEXT_MAX`（context 范围上限）等输入长度/范围上限：已删除（pattern 长度不再限制；context 仅校验 `<0`），代码已落地
  - 其余 tool（read_*/search_*/shell/network/document/dataanalysis…）的输入大小/长度上限同理去除，逐工具清理，待落地

### 3.7 Tool 输出零限制铁律（北京老陈 2026-07-20 01:42:24）

**铁律：Tool 输出不设任何限制。依 tool 参数执行后，输出是多少就是多少——结果条数、字段大小、字段长度统统不限制、不截断。唯一允许对输出数据做限制的地方，只有 Format 格式化过程（行×列截断）。**

- **范围**：凡属"输出数据"的门限一律去除——结果条数上限（如 `GREP_MAX_RESULTS` / `OBS_MAX_DISPLAY_ITEMS`）、字段字符串大小/长度上限（如 `SHELL_OUTPUT_MAX_CHARS` / `OBS_MAX_STRING_LENGTH`）、输出截断——Tool 层一律不设。
- **唯一收口点**：输出的限量只发生在 Format 输出-显示域，由行×列（`OBS_*_MAX_ROWS` + `OBS_*_MAX_ROW_CHARS`）负责（见 3.2.2）。除此之外，任何环节不得对输出设大小/条数/长度限制。
- **理由**：Tool 返回的 `data` 须对操作忠实交代（多消费者：前端、日志、链式调用），条数/大小/长度截断会漏掉关键匹配、造成结果失真；唯一必要的"限量"是喂给 LLM 的展示量，本就该由 Format 负责，不该 Tool 越权。
- **与 3.6 互补**：3.6 管"输入大小/长度去除"（入参不限），本律管"输出大小/长度/条数去除"（出参不限）；二者共同确立"限制只收口于 Format 行×列"的单一出口原则。
- **与 3.4 关系**：仅 3.4 硬安全上限（单字段数十 MB 撑爆序列化/OOM）可作极端安全网保留，属安全例外，不属常规输出限制；常规输出一律不限制。
- **落实状态（grep 已落地，其余 tool 待逐工具清理）**：`GREP_MAX_RESULTS` / `OBS_MAX_DISPLAY_ITEMS` / `SHELL_OUTPUT_MAX_CHARS` / `OBS_MAX_STRING_LENGTH` 等输出限制常量引用，须从 Tool 代码去除，交 Format 行×列；grep 已落地，其余 tool 依 5.4 逐工具清理。

## 四、原则（对规矩的解释）

### 4.1 全域分三域
所有门限先确定属于哪个域，三域互不混淆：
- **输入域**：只管输入安全/合法，与输出无关。越过门槛直接拒绝，不产 data。
- **输出-数据域**：管"操作结果的真不真、全不全"——依 3.7 不设条数/大小截断，`data` 全量保真；仅 3.4 极端硬安全例外可附 `truncated`。
- **输出-显示域**：管"呈现给 LLM 的方块有多大"——行数 × 列宽。

### 4.2 输出-划分原则（数据归 Tool / 显示归 Format）
仅适用于输出域。**判据**：该门限改变的是"操作结果的真实性/完整性"（数据），还是仅改变"结果呈现给 LLM 的形态"（显示）？
- **数据（结果保真）→ Tool**：依 3.7，结果条数/大小一律不截断，`data` 全量保真返回；仅 3.4 极端硬安全例外才置 `truncated`。理由：`data` 多消费者，须保真；Tool 不知在喂 LLM，没资格替 LLM 定显示，也不得替 LLM 限制条数。
- **显示（行数 × 单行 / 头尾 / snippet）→ Format**，且不动 data 半分。理由：Format 唯一消费者是 LLM，只有它知道在为 LLM 渲染。
- **试金石**：套用后 `data` 还是对操作的忠实交代？否→ Tool（仅 3.4 极端硬安全例外 +truncated）；是→ Format（行×列截断，见 3.7）。

### 4.3 输出-大小原则（行×列，认知充分性驱动）
仅适用于输出显示域（Format）。任何输出展开都是**行×列（方块）**，Format 门限就两个：
- **列宽（单行上限）**：一行显示多宽。以 LLM 认知充分性定值（影响尺）。
- **行数（行数上限）**：一共显示多少行。以 LLM 认知充分性定值。
总观察量 = 行数 × 单行上限，自然导出，不需要独立"总预算"门限。

**影响尺操作法**：①问"截断到 N，LLM 理解/行动是否受损？"②受损大→加长，受损小→就取 N。③取影响变小的最小 N。④校验 ≪ 200K（护栏）。

**grep 行宽示例**：50 字符→关键 payload 被砍，影响大；100→覆盖级别+关键词，影响明显变小；150→覆盖大多数匹配行，影响很小。故单行上限定为 150。

### 4.4 两条原则正交
先按 4.2 定归属（数据→Tool / 显示→Format），再按 4.3 定大小（行×列认知充分性）。归属错或数值错皆违规。

## 五、grep 落地说明

### 5.1 输入规范

#### 5.1.1 输入参数（schema）

| # | 参数 | 类型 | 默认值 | 说明 |
|---|------|------|--------|------|
| 1 | `pattern` | str | 必填 | 搜索模式(Python re 语法；纯文本特殊字符需自行转义) |
| 2 | `path` | str | 必填 | 搜索文件或目录(传文件只搜该文件；传目录递归子目录) |
| 3 | `glob` | Optional[str] | None | 文件名过滤(仅目录递归生效) |
| 4 | `ignore_case` | bool | True | 忽略大小写 |
| 5 | `context` | int | 0 | 上下文行数(仅校验非负) |

**本表（5.1.1）为参数规定的唯一来源**：参数之类型、默认值、取值范围均以此为据。schema 之外不得再叠加任何输入大小/长度门限（见 3.6 铁律）。

#### 5.1.2 输入验证（依 3.6 铁律）

参数规定以 schema（5.1.1）为准，schema 之外不得叠加任何输入大小/长度门限（见 3.6 铁律）。故 grep 输入校验**仅保留安全类检测**，原叠加的大小/长度门限一律去除：

**保留（安全检测，非大小/长度限制，依 3.6 例外）**：

| # | 校验项 | 说明 |
|---|--------|------|
| 1 | ReDoS / 危险正则检测 | `_REDOS_PATTERNS`（嵌套量词等），属安全检测，非大小/长度限制 |
| 2 | 路径合法/安全校验 | `validate_path`（目录存在、权限等） |
| 3 | 执行超时安全 | `TOOL_TIMEOUTS["grep"]`（防卡死/OOM，属安全检测非大小/长度限制，依 3.1/3.6 例外保留） |

**已去除的输入大小/长度门限（原 schema 之外叠加的多余设计，依 3.6 去除）**：

| # | 原常量 | 原值 | 状态 |
|---|--------|------|------|
| 1 | `MAX_SEARCH_FILE_SIZE` | 10MB | **已落地**：常量已删除(不再限制文件大小)；`safe_read_lines` 默认不限制，对齐 opencode-old rg 无文件大小限制 |
| 2 | `_MAX_PATTERN_LENGTH` | 200 | **已落地**：已删除，pattern 长度不再限制 |
| 3 | context 范围 0–10 | 0–10 | **已落地**：改为仅校验 `<0`(上限去除) |

### 5.2 内部规范

#### 5.2.1 数据组织与 Snippet 约定

grep Tool 代码内部实际的数据组织约定。**不是门限**，只是代码当前的做法。

**1. 字段结构**

每条结果含：`file`（路径）、`line`（行号）、`content`（行原文）、`matched`（正则匹配列表）。`context>0` 时额外含 `before`（前 N 行，每行 `{line, text}`）、`after`（后 N 行，每行 `{line, text}`）。代码位置：`_grep_files_sync:206-223`。

**2. Snippet 构建**

`context>0` 时，匹配行前后各取 context 行存为 `before`/`after` 数组，每项 `{line, text}`。代码位置：`_grep_files_sync:212-223`。

**与 5.3 的关系**：<u>原</u> Format 层用 `OBS_MAX_DISPLAY_ITEMS`（500）管条数 + `OBS_MAX_STRING_LENGTH`（10000）管内容截断，粒度不统一；v3.15 已改为行×列（见 5.3.2 / 5.4，**当前代码已实现**）。

#### 5.2.2 内部常量

| # | 常量名 | 当前值 | 定义位置 | 说明 |
|---|--------|--------|---------|------|
| 1 | `_REDOS_PATTERNS` | frozenset，2 条嵌套量词正则 | `grep_file_content.py:42-45` | ReDoS 检测模式集 |
| 2 | `_SKIP_DIRS` | frozenset，20+ 目录 | `grep_file_content.py:32-39` | 遍历时跳过目录（grep + list_directory 各自定义，值相同 → **已合并至 `tool_constants.py:159` 为 `SKIP_DIRS` 公用常量**） |
| 3 | `TOOL_TIMEOUTS["grep"]` | 120 秒 | `tool_constants.py:48` | grep 执行超时，内部安全限 |
| 4 | `BINARY_EXTENSIONS` | set，扩展名集合 | `tool_constants.py:147` | 已知二进制扩展名（**公用**，grep + readtext/writetext/edittext） |
| 5 | `ERR_FILE_CONTENT_SEARCH_FAILED` | 错误码字符串 | `tool_constants.py:511` | 搜索失败错误码（**公用**，file 类工具共用） |

### 5.3 输出规范

#### 5.3.1 Tool 输出限制（条数上限）——已去除，对齐 opencode-old

**决策**：grep Tool **不设结果条数上限**，返回全部匹配（对齐 rg 无条数限制）。理由：条数上限属多余防御——防上下文撑爆由 Format 行×列层负责；上限会漏掉第 N 条之后的关键匹配、造成结果失真、且与 rg 行为不一致（见 5.3 开头三省分析）。

`data` 结构（代码 `grep_file_content.py:346-348`）：
```python
{
  "matches": [...]        # 全部匹配，每字段原样保真
  "total_matches": 整数    # 实际匹配数（如实）
  "total_files": 整数     # 实际文件数
  "skipped_binary_files": [str]   # 跳过的二进制/超宽文件(警告时返回)
  "skipped_binary_count": 整数    # 跳过文件数(等于上述列表长度)
}
```
- 常规不再置 `truncated`（因不再因条数/大小截断）；仅 3.4 极端硬安全上限触发时才附 `truncated` 标志。`total_matches` 如实回报真实匹配总数。
- `OBS_MAX_DISPLAY_ITEMS` / `GREP_MAX_RESULTS` 等条数上限常量作废，grep 不再引用。

**禁止**：Tool 层不对单条 `content` 等字段做大小/长度截断（依 3.7 字段大小/长度不限制，全量保真）；Format 层亦不得用 `OBS_MAX_STRING_LENGTH` 一刀切截断字段字符串，须改用行×列（`OBS_GREP_MAX_ROWS` + `OBS_GREP_MAX_ROW_CHARS`）。

#### 5.3.2 Format 输出限制——行×列

Format（`observation_formatter.py` 的 `_format_matches`，行号随代码变动）渲染时遵守两个门限：

| # | 门限 | 常量名 | 值 | 说明 |
|---|------|--------|-----|------|
| 1 | 行数上限 | `OBS_GREP_MAX_ROWS` | **200** | 覆盖绝大多数搜索结果，超尾部截断+标记 |
| 2 | 单行上限（列宽） | `OBS_GREP_MAX_ROW_CHARS` | **150** | 够放 `[file:line] [matched] content + …` 标记 |

注：原 `OBS_MAX_STRING_LENGTH=10000` 由 `OBS_GREP_MAX_ROW_CHARS=150` 取代（旧值偏大，改认知充分性）。

**行数规则**：每条结果占 1 行，context 行额外占。超过 200 行时尾部截断，并按 3.2.2 铁律于详情末尾追加截断说明行（不再用旧"…还有 N 条未显示"措辞）。

**单行规则**：每行 ≥150 字符即末尾截断，追加`…`标记。`file:line`、`[matched]`、`content`、`before`/`after` 上下文共同竞争这 150 字符。优先保 `file:line` + `[matched]` + 关键 content，context 长则断。

snippet/头尾/预览等**内容组织方式**通过行×列自然实现（用行数装 snippet，用列宽装预览），不另设独立门限。

**与 Tool 的关系**：Format 只读 `data["matches"]`，不做任何修改；仅控制渲染后的文本形态。

**截断说明行（依 3.2.2 铁律）**：`_format_matches` 渲染后于该条观察详情末尾追加状态说明（有截断 `⚠ 已截断` / 无截断 `✓ 无截断-完整`，格式见 3.2.2）：
```
⚠ 已截断
截断情况：保留{200}行,实际 {M} 行，截断 {K} 行；单行上限 {150} 字符（超宽的{n}行尾部截断）
```
无截断仅一行 `✓ 无截断-完整`（不附冗余明细）。（注：仅说明，不改动 raw `data`）。

**已落地**：`grep_file_content.py` 已去除 `OBS_MAX_DISPLAY_ITEMS` / `GREP_MAX_RESULTS` 等条数上限引用（Tool 输出零限制，见 3.7/5.3.1）；`observation_formatter.py` 的 `_format_matches` 已改用 `OBS_GREP_MAX_ROWS` + `OBS_GREP_MAX_ROW_CHARS` 行×列渲染，并按 3.2.2 追加截断说明行。

### 5.4 修改实施计划（概要）

#### 5.4.1 改前本地代码的限制（历史参考，v3.16 已全部落地）

> 下列行号为改前代码位置，落地后行号随代码变动，仅作历史对照。

**(1) Tool 代码（`grep_file_content.py`，改前）**

| # | 位置(改前) | 改前做法 | 问题 / 依 3.6 处置 |
|---|------|---------|------|
| 1 | `:24` 导入 | `from tool_constants import OBS_MAX_DISPLAY_ITEMS, MAX_SEARCH_FILE_SIZE` | `MAX_SEARCH_FILE_SIZE` 输入大小限制依 3.6 去除(已删除)；`OBS_MAX_DISPLAY_ITEMS` 属输出域，见第2点 |
| 2 | `:46` 常量 | `_MAX_PATTERN_LENGTH = 200`（局部硬编码） | 依 3.6 去除 pattern 长度限制，本常量已删除 |
| 3 | `:101/106` 提示 | `f"结果数量达到上限（{OBS_MAX_DISPLAY_ITEMS}条）"` | 输出域条数上限，依 3.7/5.3.1 已作废，已删除此提示并去除条数上限，交 Format 行×列 |
| 4 | `:145` 参数 | `head_limit: int = OBS_MAX_DISPLAY_ITEMS` | 输出域条数上限，依 3.7/5.3.1 已作废，已删除 `head_limit` 参数，Tool 返回全部匹配 |
| 5 | `:190` 校验 | `safe_read_lines(fpath, max_size=MAX_SEARCH_FILE_SIZE)` | 依 3.6 已去除输入大小限制(常量删除) |
| 6 | `:298` 校验 | `len(pattern) > _MAX_PATTERN_LENGTH` | 依 3.6 已去除 pattern 长度限制，本校验已删除 |
| 7 | `:326` 调用 | `_grep_files_sync(..., OBS_MAX_DISPLAY_ITEMS, ...)` | 输出域条数上限，依 3.7/5.3.1 已作废，已删除该实参 |

**(2) Format 代码（`observation_formatter.py`，改前）**

| # | 位置(改前) | 改前做法 | 问题 |
|---|------|---------|------|
| 1 | `:949` 条数 | `if i >= OBS_MAX_DISPLAY_ITEMS: ... "还有 N 个匹配项"` | 用公用 500 管条数，粒度粗 |
| 2 | `:960` 内容 | `if len(content) > OBS_MAX_STRING_LENGTH: content[:10000] + "…"` | 用公用 10000 一刀切截断，无行×列 |
| 3 | `:969` context | `(ctx.get('text', '') or '')[:OBS_MAX_STRING_LENGTH]` | 同上 |
| 4 | `:972` context | 同上 | 同上 |

#### 5.4.2 计划修改的全部要点

> 下列行号为改前代码位置，落地后随代码变动，仅作历史对照；第 3 点已附 2026-07-20 02:22 小欧代码现状核对。

**1. 超时确认（依 3.1 安全检测类，保留）**
- 当前：`TOOL_TIMEOUTS["grep"]=120`（`tool_constants.py:48`）
- 归属：超时属防卡死/OOM 安全检测（非大小/长度限制），依 3.1/3.6 例外保留
- 动作：确认120秒是否合理，无需改代码

**2. 条数上限已废（依 3.7 / 5.3.1）——不新增 `GREP_MAX_RESULTS`**
- Tool 输出零限制：`grep_file_content.py` **不得**新增 `GREP_MAX_RESULTS`，原 `OBS_MAX_DISPLAY_ITEMS` 引用全部去除（非替换为另一常量，而是彻底删除条数上限）。
- `grep_file_content.py:24` 导入 `OBS_MAX_DISPLAY_ITEMS` → 删除该导入（输出限制收口于 Format 行×列）。
- `grep_file_content.py:101/106/145/326` 引用 `OBS_MAX_DISPLAY_ITEMS` / `head_limit` → 全部删除，Tool 返回全部匹配，条数截断交 Format。

**3. 输入限制去除（依 3.6 铁律，非"专用化"，代码待落地）**
- `tool_constants.py` 的 `MAX_SEARCH_FILE_SIZE` 输入大小限制已删除（不再限制文件大小），与 opencode-old rg 无文件大小限制对齐
- `grep_file_content.py:46` 的 `_MAX_PATTERN_LENGTH=200` → 删除（pattern 长度限制去除，不再限制）
- `grep_file_content.py:298` 的 `len(pattern) > _MAX_PATTERN_LENGTH` 校验 → 删除
- `grep_file_content.py` 的 context 范围校验由 `0–10` 改为仅校验 `< 0`（上限去除）
- 注：schema（5.1.1）已定义参数，输入大小/长度门限属 schema 之外多余叠加，依 3.6 去除，**不改为"专用常量"**（原"专用化"方案与 3.6 冲突，作废）
- **代码现状（2026-07-20 02:22 小欧 核对，2026-07-20 03:36 小欧 修订）**：上述三项已全部落地——`MAX_SEARCH_FILE_SIZE` 已删除（非置 0）/`_MAX_PATTERN_LENGTH` 已删除 / context 仅校验 `<0`（`0–10` 上限已去除）；文档决策已实施

**4. Format 改行×列渲染**
- `tool_constants.py` 新增 `OBS_GREP_MAX_ROWS=200` / `OBS_GREP_MAX_ROW_CHARS=150`
- `observation_formatter.py:949` 条数截断 `OBS_MAX_DISPLAY_ITEMS` → 改用 **行数截断** `OBS_GREP_MAX_ROWS`
  - **"行数"指每个渲染行**，不是匹配数。1条匹配 + context=3 = 1(匹配) + 3(before) + 3(after) = 7行
  - 当前代码 `if i >= limit: break` 计的是匹配条数，需改为累计实际渲染行数≥200时截断
- `observation_formatter.py:960/969/972` 内容截断 `OBS_MAX_STRING_LENGTH` → 改用 `OBS_GREP_MAX_ROW_CHARS`
- 截断说明行：渲染后于观察详情末尾追加状态说明（有截断 `⚠ 已截断` / 无截断 `✓ 无截断-完整`，格式见 3.2.2 / 5.3.2）

**5. `_SKIP_DIRS` 合并为公用**
- `tool_constants.py` 新增 `SKIP_DIRS`（已完成）
- `grep_file_content.py:32-39` 的 `_SKIP_DIRS` → 删除，改导入 `SKIP_DIRS`
- `list_directory.py:25-39` 的 `_SKIP_DIRS` → 删除，改导入 `SKIP_DIRS`

**6. `_REDOS_PATTERNS` 保持原名**
- `grep_file_content.py:42` 的 `_REDOS_PATTERNS` 保持原名，不移动

**7. grep 常量分组**
- `tool_constants.py` 中 grep 专属常量（输入域 + 输出域）必须集中分组，不得与公用常量混放

### 5.5 代码落地验证结论（2026-07-20 02:22:00 小欧）

**验证项与结果**

| # | 验证点 | 方法 | 结果 |
|---|--------|------|------|
| 1 | import 三个改动模块无 SyntaxError/NameError | `python -c "import list_directory; grep_file_content; observation_formatter"` | ✅ IMPORT_OK |
| 2 | `_format_matches` 无截断态 | 构造 42 匹配（ctx=1）→ 126 渲染行 + 1 说明行 = 127 行 / 9583 字符 | ✅ 末一行 `✓ 无截断-完整` |
| 3 | `_format_matches` 截断态 | 构造 300 渲染行（>200）→ 保留 200 行 + 2 说明行 = 202 行 / 16654 字符 | ✅ 末两行 `⚠ 已截断` + 截断情况明细 |
| 4 | Tool 输出零限制（不再 head_limit 截断） | `grep_file_content.py` 删除 `head_limit`/`OBS_MAX_DISPLAY_ITEMS`/`reached_head_limit` | ✅ 全量返回 |
| 5 | 输入限制去除 | `MAX_SEARCH_FILE_SIZE` 已删除(非置0)；删 `_MAX_PATTERN_LENGTH`；context 仅 `<0` | ✅ 对齐 opencode-old rg 无限制 |
| 6 | `_SKIP_DIRS` 合并 | grep/list_directory 改导入公用 `SKIP_DIRS`，私有定义删除 | ✅ 无残留 |

**结论**：第5章（grep）设计与 3.6+3.7+3.2.2 全自洽，grep 代码已落地并验证通过；彻底消除旧代码"919KB > 200K 被丢弃→LLM 失明"死循环根因（新 Format 上限约 29KB 永不被丢弃）。（验证范围：grep / list_directory / observation_formatter；shell / read_* / document 等其余工具按同铁律待逐工具清理，见 3.6 / 六 待落地清单）

## 六、shell 落地

| 域 | 门限类型 | 门限值 | 当前代码是否修改 |
|------|------|------|------|
| 输入域 | 超时 | `TOOL_TIMEOUTS["shell"]` | 否（保留） |
| 输出-数据域(Tool) | — | stdout/stderr 原样返回，禁止截断 | **是（删除 SHELL_OUTPUT_MAX_CHARS=20000）** |
| 输出-显示域(Format) | 行数上限 | 待定（同原则定） | **是（Format 新增行×列）** |
| 输出-显示域(Format) | 单行上限（列宽） | 待定（同原则定） | **是（Format 新增行×列）** |

其余工具凡在 Tool 做字符串长度截断者，一律删除，交 Format 行×列统一处理。

## 七、总结

本规范确立三域（输入/输出-数据/输出-显示）、铁律（三 3.1 输入安全校验 / 3.2 输出限制 Tool+Format / 3.3 内部规范非门限 / 3.4 硬安全例外 / 3.5 门限命名规则 / **3.6 输入限制去除 / 3.7 Tool 输出零限制**）、原则（四 4.1–4.4）。**3.6 铁律优先于 3.1；3.7 铁律确立输出限制唯一收口于 Format 行×列；3.2.2 截断说明行铁律要求全部 tool 的 Format 在行×列截断后于末尾追加一行清晰截断说明**。输入域不参与 Tool/Format 划分，输出显示域仅行数上限和单行上限两个门限，内部规范不属于三域，门限常量按域命名。归属错或数值错皆违规。**全部工具（grep / shell / read_* / search_* / network / document / dataanalysis …）统一按 3.6 + 3.7 + 3.2.2 规矩执行**（其中 grep 已落地并验证通过；shell / read_* / document 等其余工具按同铁律待逐工具清理，见 3.6 / 六 待落地清单）：输入大小/长度门限一律去除（3.1 仅保留安全检测类校验）；Tool 输出零限制、条数/大小/长度不截断；唯一限量收口于 Format 行×列，截断后附说明行。输入域不参与 Tool/Format 划分，输出显示域仅行数上限和单行上限两个门限，内部规范不属于三域，门限常量按域命名。归属错或数值错皆违规。

---

**签名**：小欧（陈） 2026-07-19 16:45:00
