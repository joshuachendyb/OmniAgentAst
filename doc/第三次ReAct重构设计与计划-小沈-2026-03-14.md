# 第三次ReAct重构设计与计划

**编写人**: 小沈
**编写时间**: 2026-03-14 22:52:11
**存放位置**: D:\OmniAgentAs-desk\doc\

---

## 一、当前ReAct实现情况及问题分析

### 1.1 ReAct实现现状

当前后端 chat.py 中的流式响应实现了8种type，分别是：
- start、thought、action_tool、observation、final、chunk、error、incident

代码位置：`backend/app/api/v1/chat.py`

### 1.2 意图检测函数问题

**问题现象**：LongCat模型无法执行文件操作（查看桌面、查看E盘），只返回"不具备执行能力"等文本答案。

**问题根因**：`detect_file_operation_intent()` 函数有两个问题：

1. **完整词匹配**：必须精确匹配"查看文件"或"查看目录"，无法识别"查看桌面目录的文件有什么"等自然语言
2. **阈值0.6太高**：导致意图检测失败后，程序走普通LLM路径，LLM不知道有工具可用

**时间线**：
- 3月5日：阈值从0.2提高到0.6
- 3月11日：重写为完整词匹配
- 3月8日：用户最后一次正常使用
- 3月10日后：突然失效

**已修复**：2026-03-14将阈值从0.6降到0.2，并扩展关键词列表。

---

## 二、8种type字段现状

### 2.1 start

| 字段 | 当前实现 | 说明 |
|------|---------|------|
| type | ✅ start | 固定值 |
| display_name | ✅ 有 | AI显示名称 |
| model | ✅ 有 | AI模型 |
| provider | ✅ 有 | AI提供商 |
| task_id | ✅ 有 | 唯一标识 |
| security_check | ✅ 有 | 含is_safe/risk_level/risk/blocked |
| timestamp | ❌ 缺 | 需补充 |

### 2.2 thought

| 字段 | 当前实现 | 说明 |
|------|---------|------|
| type | ✅ thought | 固定值 |
| step | ✅ 有 | 轮次序号 |
| input_message | ❌ 缺 | 需补充 |
| content | ❌ 缺 | 需补充LLM返回 |
| action_tool | ❌ 缺 | 需补充LLM返回 |
| params | ❌ 缺 | 需补充LLM返回 |
| timestamp | ❌ 缺 | 需补充 |

**当前代码**：
```python
thought_data = {'type': 'thought', 'step': next_step(), 'thinking_prompt': '正在分析任务...'}
```
仅有基础结构，缺少LLM返回的实际内容。

### 2.3 action_tool

| 字段 | 当前实现 | 说明 |
|------|---------|------|
| type | ✅ action_tool | 固定值 |
| step | ✅ 有 | 步骤序号 |
| tool_name | ⚠️ 字段名不同 | 应为action_tool |
| tool_params | ✅ 有 | 工具参数 |
| execution_status | ⚠️ 字段名不同 | 应为action_status |
| summary | ⚠️ 字段名不同 | 应为action_summary |
| raw_data | ⚠️ 字段名不同 | 应为action_data |
| action_error_message | ❌ 缺 | 需补充 |
| action_retry_count | ✅ 有 | 重试次数 |
| timestamp | ❌ 缺 | 需补充 |

### 2.4 observation

| 字段 | 当前实现 | 说明 |
|------|---------|------|
| type | ✅ observation | 固定值 |
| step | ✅ 有 | 步骤序号 |
| obs_execution_status | ✅ 有 | 执行状态 |
| obs_summary | ✅ 有 | 结果摘要 |
| obs_raw_data | ✅ 有 | 原始数据 |
| content | ✅ 有 | LLM思考 |
| obs_action_tool | ✅ 有 | 下一步工具 |
| obs_params | ✅ 有 | 工具参数 |
| is_finished | ✅ 有 | 是否完成 |
| timestamp | ❌ 缺 | 需补充 |

### 2.5 final

| 字段 | 当前实现 | 说明 |
|------|---------|------|
| type | ✅ final | 固定值 |
| content | ✅ 有 | 完整回复 |
| display_name | ✅ 有 | AI显示名称 |
| timestamp | ❌ 缺 | 需补充 |

### 2.6 chunk

| 字段 | 当前实现 | 说明 |
|------|---------|------|
| type | ✅ chunk | 固定值 |
| content | ✅ 有 | 文本内容 |
| is_reasoning | ✅ 有 | 是否思考过程 |

**状态**：已100%实现，符合最新简化设计。

### 2.7 error

| 字段 | 当前实现 | 说明 |
|------|---------|------|
| type | ✅ error | 固定值 |
| code | ✅ 有 | 错误码 |
| message | ✅ 有 | 错误消息 |
| error_type | ✅ 有 | 错误类型 |
| details | ✅ 有 | 详细错误 |
| stack | ❌ 缺 | 需补充 |
| retryable | ✅ 有 | 是否可重试 |
| retry_after | ❌ 缺 | 需补充 |

### 2.8 incident

| 字段 | 当前实现 | 说明 |
|------|---------|------|
| type | ✅ incident | 固定值 |
| incident_value | ✅ 有 | interrupted/paused/resumed/retrying |
| message | ✅ 有 | 事件消息 |
| timestamp | ✅ 有 | 时间戳 |

**状态**：已100%实现，4种状态已整合。

---

## 三、实现程度汇总

| type | 完成度 | 优先级 |
|------|--------|-------|
| chunk | 100% | 无需处理 |
| incident | 100% | 无需处理 |
| start | 95% | 低 |
| observation | 85% | 低 |
| error | 75% | 中 |
| final | 75% | 中 |
| action_tool | 60% | 中 |
| thought | 15% | **高** |
