# 文件工具完整分析文档

**创建时间**: 2026-03-23 15:30:00
**版本**: v1.0
**编写人**: 小沈

---

## 一、文件工具总览

| 工具名称 | 功能 | 返回数据类型 | 关键字段 |
|---------|------|------------|---------|
| **list_directory** | 列出目录内容 | entries[] | name, path, type, size |
| **read_file** | 读取文件内容 | content, total_lines | content (带行号) |
| **write_file** | 写入文件内容 | success, operation_id | bytes_written |
| **delete_file** | 删除文件 | success, operation_id | deleted_path |
| **move_file** | 移动/重命名 | success, operation_id | source, destination |
| **search_files** | 搜索文件内容 | matches[], files_searched | file, matches, match_count |
| **generate_report** | 生成操作报告 | reports{} | report_paths |

---

## 二、后端数据格式详解

### 2.1 list_directory

**后端返回结构** (`_to_unified_format`):
```python
{
    "status": "success",
    "summary": "成功读取目录，共 100 个项目",
    "data": {
        "success": True,
        "entries": [
            {
                "name": "src",
                "path": "D:/项目代码/src",        # 相对路径或绝对路径
                "type": "directory",               # "directory" 或 "file"
                "size": None                        # 目录无 size
            },
            {
                "name": "README.md",
                "path": "D:/项目代码/README.md",
                "type": "file",
                "size": 1234
            }
        ],
        "total": 100,                    # 总数
        "has_more": False,               # 是否还有更多
        "next_page_token": None,
        "directory": "D:/项目代码"      # 查询的目录
    }
}
```

**特点**:
- entries 是数组，每个 entry 有 name, path, type, size
- total 是总数
- 递归模式下 path 可能是相对路径
- 非递归模式下 path 是绝对路径

---

### 2.2 read_file

**后端返回结构**:
```python
{
    "status": "success",
    "summary": "成功读取文件，内容长度：5000 字符，共 200 行",
    "data": {
        "success": True,
        "content": "1: 第一行内容\n2: 第二行内容\n...",  # 带行号
        "total_lines": 200,
        "start_line": 1,
        "end_line": 200,
        "has_more": False,
        "file_size": 5000,
        "encoding": "utf-8"
    }
}
```

**特点**:
- content 带行号格式: `1: 内容\n2: 内容\n...`
- 有 total_lines, file_size 等元信息
- 支持分页读取（offset, limit）

---

### 2.3 write_file

**后端返回结构**:
```python
{
    "status": "success",
    "summary": "成功写入文件 D:/项目代码/test.txt，共 1234 字节",
    "data": {
        "success": True,
        "operation_id": "op_xxx",        # 操作ID（用于回滚）
        "file_path": "D:/项目代码/test.txt",
        "bytes_written": 1234
    }
}
```

**特点**:
- 返回 operation_id（可用于回滚）
- bytes_written 是写入的字节数

---

### 2.4 delete_file

**后端返回结构**:
```python
{
    "status": "success",
    "summary": "成功删除 D:/项目代码/test.txt（已备份到回收站）",
    "data": {
        "success": True,
        "operation_id": "op_xxx",
        "deleted_path": "D:/项目代码/test.txt",
        "message": "File deleted (backup in recycle bin)"
    }
}
```

**特点**:
- 自动备份到回收站
- operation_id 可用于回滚

---

### 2.5 move_file

**后端返回结构**:
```python
{
    "status": "success",
    "summary": "成功移动文件：C:/old.txt -> D:/new.txt",
    "data": {
        "success": True,
        "operation_id": "op_xxx",
        "source": "C:/old.txt",
        "destination": "D:/new.txt",
        "message": "Moved: old.txt -> D:/new.txt"
    }
}
```

**特点**:
- source 和 destination 清晰
- operation_id 可用于回滚

---

### 2.6 search_files

**后端返回结构**:
```python
{
    "status": "success",
    "summary": "搜索完成，找到 10 个文件，共 50 处匹配",
    "data": {
        "success": True,
        "pattern": "TODO",
        "path": "D:/项目代码",
        "files_searched": 100,
        "files_matched": 10,
        "total_matches": 50,
        "matches": [
            {
                "file": "src/main.py",           # 相对路径
                "matches": [
                    {
                        "start": 100,
                        "end": 104,
                        "matched": "TODO",
                        "context": "... TODO: 修复bug ..."  # 前后各50字符
                    }
                ],
                "match_count": 3
            }
        ]
    }
}
```

**特点**:
- matches 是数组，每个匹配文件有 file, matches, match_count
- context 是匹配位置前后各 50 字符的上下文
- 按 match_count 降序排序

---

### 2.7 generate_report

**后端返回结构**:
```python
{
    "status": "success",
    "summary": "成功生成 3 个报告",
    "data": {
        "success": True,
        "session_id": "session_xxx",
        "reports": {
            "tree": "D:/报告/tree_20260323.html",
            "operation": "D:/报告/operation_20260323.html",
            "summary": "D:/报告/summary_20260323.html"
        }
    }
}
```

**特点**:
- reports 是字典，key 是报告类型，value 是文件路径
- 通常有 tree, operation, summary 三种报告

---

## 三、数据库存储格式（action_tool 步骤）

### 3.1 action_tool 步骤结构

```python
{
    "type": "action_tool",
    "step": 2,
    "timestamp": 1773742493718,  # 毫秒时间戳
    "tool_name": "list_directory",      # 工具名称
    "tool_params": {                    # 工具参数
        "dir_path": "D:/项目代码",
        "recursive": True
    },
    "tool_description": "列出目录内容",
    "action_description": "正在列出目录 D:/项目代码 的内容...",
    "raw_data": {                       # 工具返回的原始数据
        "status": "success",
        "summary": "成功读取目录，共 100 个项目",
        "data": {
            "success": True,
            "entries": [...],           # list_directory 的 entries
            "total": 100,
            "has_more": False,
            "directory": "D:/项目代码"
        }
    }
}
```

### 3.2 observation 步骤结构

```python
{
    "type": "observation",
    "step": 3,
    "timestamp": 1773742493818,
    "tool_name": "list_directory",
    "obs_reasoning": "我已成功获取目录内容，现在可以分析...",  # 可选
    "obs_raw_data": {                   # 原始返回数据（完整保存）
        "status": "success",
        "summary": "...",
        "data": {...}
    }
}
```

**注**: observation 步骤的数据存储在 `obs_raw_data` 字段中。

---

## 四、前端显示方案

### 4.1 核心原则：根据 tool_name 分支处理

**不同工具的 `raw_data.data` 结构完全不同，前端必须根据 `tool_name` 字段判断使用哪种解析方式。**

```
action_tool 步骤数据结构：
{
  "type": "action_tool",
  "tool_name": "xxx",           // ← 用这个判断工具类型
  "tool_params": {...},
  "raw_data": {
    "data": {...}               // ← 这个结构因工具而异
  }
}
```

### 4.2 分支处理结构（本次重点实现）

```tsx
// 根据 tool_name 选择显示方式
const renderToolResult = (step: any) => {
  // 获取工具返回的数据
  const data = step.raw_data?.data;
  
  if (!data) return null;
  
  // 🔴 根据 tool_name 分支处理
  switch (step.tool_name) {
    
    case 'list_directory':
      // data.entries[] - 文件列表
      // data.total - 总数
      return (
        <div>
          <span>📁 文件列表 ({data.total}个)</span>
          <div className="file-list" style={getFileListBackground()}>
            {data.entries.map((entry: any, idx: number) => (
              <div key={idx} style={{ padding: "4px 0", borderBottom: "1px solid #e8e8e8" }}>
                {entry.type === 'directory' ? '📁' : '📄'} {entry.name}
                {entry.size && <span style={{ color: "#888" }}> ({formatSize(entry.size)})</span>}
              </div>
            ))}
          </div>
        </div>
      );
    
    case 'read_file':
      // data.content - 带行号内容
      // data.total_lines - 总行数
      return (
        <div>
          <span>📄 共 {data.total_lines} 行</span>
          <pre style={{ whiteSpace: "pre-wrap", maxHeight: 400, overflow: "auto" }}>
            {data.content}
          </pre>
        </div>
      );
    
    case 'write_file':
      // data.file_path - 文件路径
      // data.bytes_written - 字节数
      return (
        <div>
          <span>✅ 已写入 {data.file_path}</span>
          <span style={{ color: "#888" }}>（{data.bytes_written} 字节）</span>
        </div>
      );
    
    case 'delete_file':
      // data.deleted_path - 删除路径
      // data.message - 提示信息
      return (
        <div>
          <span>🗑️ 已删除 {data.deleted_path}</span>
          <span style={{ color: "#888" }}>{data.message}</span>
        </div>
      );
    
    case 'move_file':
      // data.source - 源路径
      // data.destination - 目标路径
      return (
        <div>
          <span>📦 {data.source}</span>
          <span> → </span>
          <span>{data.destination}</span>
        </div>
      );
    
    case 'search_files':
      // data.matches[] - 匹配数组
      // data.files_matched - 匹配文件数
      // data.total_matches - 总匹配数
      return (
        <div>
          <span>🔍 找到 {data.files_matched} 个文件，{data.total_matches} 处匹配</span>
          {data.matches.map((match: any, idx: number) => (
            <div key={idx} style={{ marginTop: 8 }}>
              <div>📄 {match.file} ({match.match_count}处)</div>
              {match.matches.slice(0, 3).map((m: any, i: number) => (
                <pre key={i} style={{ fontSize: 11, color: "#666", margin: "4px 0" }}>
                  ...{m.context}...
                </pre>
              ))}
            </div>
          ))}
        </div>
      );
    
    case 'generate_report':
      // data.reports{} - 报告字典
      return (
        <div>
          <span>📊 已生成 {Object.keys(data.reports).length} 个报告</span>
          {Object.entries(data.reports).map(([type, path]: [string, string]) => (
            <div key={type}>
              {type}: {path}
            </div>
          ))}
        </div>
      );
    
    default:
      // 未知工具，显示原始数据
      return <pre>{JSON.stringify(data, null, 2)}</pre>;
  }
};
```

### 4.3 raw_data.data 字段对照表

| 工具 | data 关键字段 | 类型 | 说明 |
|------|-------------|------|------|
| **list_directory** | entries | array | 条目数组 |
| | total | number | 总数 |
| | has_more | boolean | 是否还有更多 |
| **read_file** | content | string | 带行号内容 |
| | total_lines | number | 总行数 |
| | has_more | boolean | 是否还有更多 |
| **write_file** | file_path | string | 文件路径 |
| | bytes_written | number | 写入字节数 |
| **delete_file** | deleted_path | string | 删除路径 |
| | message | string | 提示信息 |
| **move_file** | source | string | 源路径 |
| | destination | string | 目标路径 |
| **search_files** | files_matched | number | 匹配文件数 |
| | total_matches | number | 总匹配数 |
| | matches | array | 匹配详情数组 |
| **generate_report** | reports | object | 报告字典 |

---

### 4.4 统一显示原则

```
action_tool 步骤显示：
├── 工具名称（tool_name）
├── 工具参数（tool_params）
└── 执行结果（renderToolResult 根据 tool_name 分支显示）

observation 步骤显示：
├── 思考过程（obs_reasoning）- 可选
└── 执行结果（renderToolResult 根据 tool_name 分支显示）
```

### 4.2 各工具的前端显示方案

#### 4.2.1 list_directory

**前端字段映射**:
| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| raw_data.data.entries | step.raw_data?.data?.entries | 文件列表数组 |
| raw_data.data.total | step.raw_data?.data?.total | 总数 |

**前端显示代码**:
```tsx
{step.type === "action_tool" && step.raw_data?.data?.entries && (
    <div>
        <span>📁 文件列表 ({step.raw_data.data.total}个)</span>
        {step.raw_data.data.entries.map((entry: any, idx: number) => (
            <div key={idx}>
                {entry.type === "directory" ? "📁" : "📄"} {entry.name}
            </div>
        ))}
    </div>
)}
```

**字段说明**:
- `entry.name`: 文件/目录名
- `entry.type`: "directory" 或 "file"
- `entry.size`: 文件大小（目录为 null）
- `entry.path`: 完整路径

---

#### 4.2.2 read_file

**前端字段映射**:
| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| raw_data.data.content | step.raw_data?.data?.content | 文件内容（带行号） |
| raw_data.data.total_lines | step.raw_data?.data?.total_lines | 总行数 |

**前端显示代码**:
```tsx
{step.type === "action_tool" && step.raw_data?.data?.content && (
    <div>
        <span>📄 共 {step.raw_data.data.total_lines} 行</span>
        <pre style={{whiteSpace: "pre-wrap"}}>
            {step.raw_data.data.content}
        </pre>
    </div>
)}
```

**特点**:
- 内容已带行号，直接显示即可
- 考虑长文件折叠或虚拟滚动

---

#### 4.2.3 write_file

**前端字段映射**:
| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| raw_data.data.file_path | step.raw_data?.data?.file_path | 写入的文件路径 |
| raw_data.data.bytes_written | step.raw_data?.data?.bytes_written | 写入的字节数 |

**前端显示代码**:
```tsx
{step.type === "action_tool" && step.raw_data?.data?.file_path && (
    <div>
        <span>✅ 已写入 {step.raw_data.data.file_path}</span>
        <span>共 {step.raw_data.data.bytes_written} 字节</span>
    </div>
)}
```

---

#### 4.2.4 delete_file

**前端字段映射**:
| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| raw_data.data.deleted_path | step.raw_data?.data?.deleted_path | 删除的文件路径 |
| raw_data.data.message | step.raw_data?.data?.message | 提示信息 |

**前端显示代码**:
```tsx
{step.type === "action_tool" && step.raw_data?.data?.deleted_path && (
    <div>
        <span>🗑️ 已删除 {step.raw_data.data.deleted_path}</span>
        <span style={{color: "#888"}}>{step.raw_data.data.message}</span>
    </div>
)}
```

---

#### 4.2.5 move_file

**前端字段映射**:
| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| raw_data.data.source | step.raw_data?.data?.source | 源路径 |
| raw_data.data.destination | step.raw_data?.data?.destination | 目标路径 |

**前端显示代码**:
```tsx
{step.type === "action_tool" && step.raw_data?.data?.source && (
    <div>
        <span>📦 {step.raw_data.data.source}</span>
        <span> → </span>
        <span>{step.raw_data.data.destination}</span>
    </div>
)}
```

---

#### 4.2.6 search_files

**前端字段映射**:
| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| raw_data.data.files_matched | step.raw_data?.data?.files_matched | 匹配文件数 |
| raw_data.data.total_matches | step.raw_data?.data?.total_matches | 总匹配数 |
| raw_data.data.matches | step.raw_data?.data?.matches | 匹配详情数组 |

**前端显示代码**:
```tsx
{step.type === "action_tool" && step.raw_data?.data?.matches && (
    <div>
        <span>🔍 找到 {step.raw_data.data.files_matched} 个文件，{step.raw_data.data.total_matches} 处匹配</span>
        {step.raw_data.data.matches.map((match: any, idx: number) => (
            <div key={idx} style={{marginTop: 8}}>
                <div>📄 {match.file} ({match.match_count}处)</div>
                {match.matches.slice(0, 3).map((m: any, i: number) => (
                    <pre key={i} style={{fontSize: 11, color: "#666"}}>
                        ...{m.context}...
                    </pre>
                ))}
            </div>
        ))}
    </div>
)}
```

---

#### 4.2.7 generate_report

**前端字段映射**:
| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| raw_data.data.reports | step.raw_data?.data?.reports | 报告字典 |

**前端显示代码**:
```tsx
{step.type === "action_tool" && step.raw_data?.data?.reports && (
    <div>
        <span>📊 已生成 {Object.keys(step.raw_data.data.reports).length} 个报告</span>
        {Object.entries(step.raw_data.data.reports).map(([key, path]) => (
            <div key={key}>
                {key}: {path}
            </div>
        ))}
    </div>
)}
```

---

## 五、当前前端问题

### 5.1 前端代码中的问题

**文件**: `frontend/src/components/Chat/MessageItem.tsx`

| 行号 | 问题 | 应改为 |
|-----|------|-------|
| 320 | `step.thought` | `step.obs_reasoning` |
| 344 | `step.observation?.result` | `step.raw_data?.data` |
| 410 | `step.thinking_prompt` | `step.content` |

### 5.2 缺失的显示逻辑

1. **action_tool 步骤**缺少显示 `raw_data.data.entries`（文件列表）
2. **search_files** 没有专门的显示逻辑
3. **read_file** 没有显示内容

---

## 六、修复计划

### 6.1 修复步骤

1. **修复前端字段映射错误**（Line 320, 344, 410）
2. **为 list_directory 添加文件列表显示**（action_tool 步骤）
3. **为 search_files 添加搜索结果显示**
4. **为 read_file 添加内容显示**
5. **统一折叠/展开功能**

### 6.2 修复优先级

| 优先级 | 工具 | 原因 |
|-------|------|------|
| P0 | list_directory | 用户最常用，已发现数据丢失问题 |
| P1 | search_files | 搜索结果显示混乱 |
| P2 | read_file | 长文件需要折叠显示 |
| P3 | 其他工具 | 相对简单 |

---

## 七、数据库查询验证

```bash
# 查询 list_directory 的 action_tool 步骤
curl -s "http://127.0.0.1:8000/api/v1/sessions/{session_id}/messages" | python -c "
import sys, json
data = json.load(sys.stdin)
for msg in data.get('messages', []):
    for step in msg.get('execution_steps', []):
        if step.get('type') == 'action_tool' and step.get('tool_name') == 'list_directory':
            entries = step.get('raw_data', {}).get('data', {}).get('entries', [])
            print(f'step {step[\"step\"]}: {len(entries)} entries')
"
```

---

**更新时间**: 2026-03-23 15:30:00
**版本**: v1.0
