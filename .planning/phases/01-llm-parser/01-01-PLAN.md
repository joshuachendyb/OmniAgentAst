---
phase: 01-llm-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/agent/tool_parser.py
autonomous: true
requirements:
  - LLM-01
  - LLM-02

must_haves:
  truths:
    - "JSON前面的纯文本被分离到content字段"
    - "JSON里的thought被单独提取到thought字段"
    - "截断的JSON能被正确解析"
    - "字符串内的花括号不干扰解析"
    - "总结性文本的finish判断只匹配行首"
  artifacts:
    - path: "backend/app/services/agent/tool_parser.py"
      provides: "LLM响应解析器核心逻辑"
      exports: "ToolParser.parse_response, ToolParser._extract_from_text"
    - path: "backend/tests/test_tool_parser_new_features.py"
      provides: "新增测试用例"
      contains: "TestNewFeatures, TestSummarizePatternFix"
  key_links:
    - from: "backend/app/services/agent/tool_parser.py"
      to: "backend/tests/test_tool_parser_new_features.py"
      via: "pytest tests/test_tool_parser_new_features.py"
      pattern: "ToolParser\\.parse_response"
---

<objective>
根据设计文档实施LLM响应解析器的更新，修复已知bug并实现新功能。

**目标**：
1. 新增_extract_json_with_balanced_braces方法，实现JSON前文本分离
2. 修复summarize_patterns误判问题
3. 分开提取content和thought字段
4. 删除返回时的旧字段（保持向后兼容的读取，删除返回的旧字段）

**输出**：更新后的tool_parser.py文件 + 新增测试用例
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md

**设计文档参考**:
- `D:\OmniAgentAs-desk\doc-4月优化\LLM响应解析器最终设计方案-小沈-2026-04-13-v3.md`
- 章节: 5.3 各层的更新实施方法, 5.5 更新的检查清单, 5.6 代码更新实施方法
</execution_context>

<context>
@.planning/phases/01-llm-parser/01-01-TEST-PLAN.md

# 设计文档关键信息

## 5.2 各层的问题定位
| 层 | 文件 | 问题 | 影响 |
|----|------|------|------|
| 2 | tool_parser.py:145 | BUG: `r'(?:根据.*?结果)'` 误判finish | JSON前文本被误判为finish |
| 2 | tool_parser.py:54 | 只取content字段，JSON前文本丢失 | content和thought混在一起 |
| 3 | tool_parser.py:138-154 | summarize_patterns错误 | 正常内容被误判为finish |

## 实施步骤顺序（必须按顺序执行）
```
第1步：Step 3 - 新增_extract_json_with_balanced_braces()方法
       ↓ 新方法定义在前面，供后续调用
第2步：Step 5 - 分开提取content和thought 【调用Step 3的方法】
       ↓ 需要使用Step 3定义的新方法
第3步：Step 2 - 修复summarize_patterns
       ↓ 独立修改，无依赖
第4步：Step 1 - 删除返回时的旧字段
       ↓ 独立修改，无依赖
第5步：Step 4 - json.loads()验证（保持不变）
       ↓ 无需修改
```

## 关键代码位置
- tool_parser.py 第22行: 插入新方法位置
- tool_parser.py 第36-52行: 调用新方法位置
- tool_parser.py 第54-78行: 提取content和thought位置
- tool_parser.py 第140-154行: summarize_patterns位置
- tool_parser.py 第76-77行: 删除旧字段位置
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建备份</name>
  <files>None</files>
  <action>创建tool_parser.py的带时间戳备份目录</action>
  <verify>备份目录存在且包含tool_parser.py</verify>
  <done>备份完成</done>
</task>

<task type="auto">
  <name>Task 2: 新增_extract_json_with_balanced_braces方法</name>
  <files>backend/app/services/agent/tool_parser.py</files>
  <action>
在parse_response()方法之前（约第22行）新增静态方法：

```python
@staticmethod
def _extract_json_with_balanced_braces(text: str) -> tuple:
    """
    Stage 1: 使用平衡括号匹配找到JSON，提取JSON前面的纯文本
    
    返回：(json_text, content_before_json)
    - json_text: 找到的JSON文本（可能截断）
    - content_before_json: JSON前面的纯文本
    """
    start = -1
    brace_count = 0
    in_string = False
    escape_next = False
    
    for i, char in enumerate(text):
        if escape_next:
            escape_next = False
            continue
        
        if char == '\\':
            escape_next = True
            continue
        
        if char == '"' and not escape_next:
            in_string = not in_string
            continue
        
        if in_string:
            continue
        
        if char == '{':
            if start == -1:
                start = i
            brace_count += 1
        elif char == '}':
            brace_count -= 1
            if brace_count == 0 and start != -1:
                json_text = text[start:i+1]
                content_before = text[:start].strip()
                return json_text, content_before
    
    if start != -1 and brace_count > 0:
        return text[start:], text[:start].strip()
    
    return None, text.strip()
```
  </action>
  <verify>
grep "_extract_json_with_balanced_braces" backend/app/services/agent/tool_parser.py
  </verify>
  <done>方法已添加</done>
</task>

<task type="auto">
  <name>Task 3: 修改parse_response调用新方法</name>
  <files>backend/app/services/agent/tool_parser.py</files>
  <action>
将第36-45行修改为：

```python
# Step 1: 去除Markdown代码块
json_match = re.search(
    r'```(?:json)?\s*\n?(.*?)\n?```',
    response,
    re.DOTALL | re.IGNORECASE
)

if json_match:
    json_str = json_match.group(1).strip()
    json_without_backticks = json_str
else:
    json_without_backticks = response.strip()

# Step 2: 用平衡括号提取JSON和纯文本
json_text, content_before = ToolParser._extract_json_with_balanced_braces(json_without_backticks)
if json_text:
    json_str = json_text
else:
    json_str = json_without_backticks
    content_before = ""
```
  </action>
  <verify>
grep "content_before = " backend/app/services/agent/tool_parser.py
  </verify>
  <done>parse_response方法已调用新方法</done>
</task>

<task type="auto">
  <name>Task 4: 分开提取content和thought</name>
  <files>backend/app/services/agent/tool_parser.py</files>
  <action>
将第54行修改为：

```python
# JSON前面的纯文本作为content（用于显示）
content = content_before
# JSON里的thought单独提取
thought = parsed.get("thought", parsed.get("thinking", ""))
```

将第68-78行修改为：

```python
reasoning = parsed.get("reasoning")

return {
    "content": content,          # JSON前面的纯文本
    "thought": thought,          # JSON里的thought
    "tool_name": tool_name,
    "tool_params": tool_params,
    "reasoning": reasoning,
}
```
  </action>
  <verify>
grep -A 5 "return {" backend/app/services/agent/tool_parser.py | head -20
  </verify>
  <done>content和thought已分开提取</done>
</task>

<task type="auto">
  <name>Task 5: 修复summarize_patterns</name>
  <files>backend/app/services/agent/tool_parser.py</files>
  <action>
将第140-154行的summarize_patterns修改为：

```python
# 【修复 2026-04-13】改进的finish判断
# 只匹配行首/句首的总结词，不匹配中间的内容
summarize_patterns = [
    # 英文总结 - 必须行首/句首
    r'^(?:summarize|summary|I have found|I will)',
    # 中文总结 - 必须行首/句首
    r'^(?:总结|已完成|任务完成|结束了)',
]
```
  </action>
  <verify>
grep -A 3 "summarize_patterns" backend/app/services/agent/tool_parser.py | head -10
  </verify>
  <done>summarize_patterns已修复</done>
</task>

<task type="auto">
  <name>Task 6: 删除返回时的旧字段</name>
  <files>backend/app/services/agent/tool_parser.py</files>
  <action>
删除第76-77行：
```python
# 保持向后兼容
"action_tool": tool_name,
"params": tool_params
```
  </action>
  <verify>
grep "action_tool" backend/app/services/agent/tool_parser.py
  </verify>
  <done>返回时的旧字段已删除</done>
</task>

<task type="auto">
  <name>Task 7: 运行测试验证</name>
  <files>backend/tests/test_tool_parser_new_features.py</files>
  <action>
运行新增测试用例：
```bash
cd D:\OmniAgentAs-desk\backend
pytest tests/test_tool_parser_new_features.py -v
```

运行回归测试用例：
```bash
pytest tests/test_tool_parser.py -v
```
  </action>
  <verify>
pytest tests/test_tool_parser_new_features.py tests/test_tool_parser.py -v --tb=short
  </verify>
  <done>所有测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON输入 | LLM返回的响应文本，可能包含恶意构造的内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Injection | JSON解析 | mitigate | json.loads()有内置防护，_extract_json_with_balanced_braces正确处理字符串转义 |
| T-01-02 | Denial of Service | 超大JSON | mitigate | 平衡括号方法有brace_count上限保护 |
</threat_model>

<verification>
- [ ] 新增测试用例TC001-TC010全部通过
- [ ] 回归测试用例TC029-TC050全部通过
- [ ] content字段：JSON前面的纯文本正确分离
- [ ] thought字段：JSON里的thought单独提取
- [ ] summarize_patterns：不再误判"根据...结果"等中间内容
</verification>

<success_criteria>
1. 所有测试用例通过（新增+回归）
2. content和thought字段正确分离
3. 截断JSON能正确解析
4. summarize_patterns只匹配行首
5. 向后兼容：读取时仍支持旧字段名
</success_criteria>

<output>
After completion, create `.planning/phases/01-llm-parser/01-01-SUMMARY.md`
</output>
