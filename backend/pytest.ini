[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
; ==========================================================
; 超时策略 — 仅此一处设置，测试脚本严禁手动设任何超时
; 触发后 pytest 强杀线程，ensure_backend_ready/会话恢复等
; 前置操作不受此超时影响（它们各自有 3s/10s 连接超时）
; -- 小欧 2026-07-03
; ==========================================================
timeout = 3000
markers =
    e2e_full_link: full link E2E test
    asyncio: asyncio test