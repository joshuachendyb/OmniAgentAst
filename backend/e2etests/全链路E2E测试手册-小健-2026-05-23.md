# 全链路端到端集成测试方案（Full-Link E2E Integration Test）

**创建时间**: 2026-05-23 01:00:44  
**版本**: v2.6  
**编写人**: 小健

---

## 版本记录

| 版本 | 时间 | 签名 | 更新内容 |
|------|------|------|---------|
| v2.2 | 2026-06-18 20:10:00 | 小欧 | 3.4删除已实现的14项验证标准(只保留工具选择+参数正确性2项需人工)；3.5删除已实现的铁律+模板(只保留一行引用)；e2e_helpers.py文件头docstring补充3.4/3.5全部已实现项对照 |
| v2.3 | 2026-06-18 20:40:00 | 小欧 | 按北京老陈4步流程重构：3.1检查→3.2编写脚本→3.3执行脚本→3.4检查结果→3.5错误处理，合并原3.1ABC+3.2+3.3碎片为统一结构，删除所有旧编号(步骤1/5/6/9) |
| v2.4 | 2026-06-25 17:30:00 | 小欧 | 第2/3/5刀修改：1.1/1.3/1.5/3.1/6.1节诚实化(DB隔离未实现)；P2-04/P3-02~04/P5-06标⚠️；6.1节Linux→Windows命令；删除第9章Mock测试；节号全角→半角；章节排序P0→P1→P2→P3→P4→P5→P6→P7→P8→P9→P10；测试数统一65；P3用例简化去掉「写入md报告」|
| v2.5 | 2026-06-25 17:40:00 | 小欧 | 3.1节新增启动后端检查项(必须用--reload模式)；6.1节删除OMNIAGENT_CONFIG_PATH环境变量(E:\test_dir\是测试用例数据，与后端运行时无关)；uvicorn命令加--reload |
| v2.6 | 2026-06-26 22:51:00 | 小欧 | 3.1节启动后端改为用 start_e2e.bat（AI执行时弹独立cmd窗口），原裸uvicorn命令删除 |
| v2.7 | 2026-06-26 23:35:00 | 小欧 | 3.1节去除--reload描述(start_e2e.bat已简化去掉--reload嵌套) |

---

## 一、测试定义

### 1.1 测试名称

**全链路端到端集成测试**（Full-Link End-to-End Integration Test）

**唯一目标**：用真实LLM（后端从config.yaml读取配置）模拟真实用户操作
 抓出烂代码的问题, 找不到 bug的测试都是垃圾测试.垃圾case---发现任何问题必须深挖问题的烂代码是什么 务必揪出烂代码.

**具体来说**：
1. Agent在真实LLM下能否正确完成用户请求
2. Agent在异常情况下（LLM返回垃圾/空内容/错误工具）能否容错
3. 系统各组件的串联是否完整（API→Agent→LLM→工具→DB）
4. 日志和数据库记录是否完整准确


### 1.2 为什么叫这个名

| 关键词 | 含义 | 在这个测试中的体现 |
|--------|------|-------------------|
| 全链路 | 覆盖系统所有组件 | 前端API→后端→Agent→LLM→工具执行→数据库→SSE响应 |
| 端到端 | 从用户发消息到收到完整结果 | POST /chat → SSE事件流 → final/error`事件 |
| 集成 | 验证各组件对接是否正确 | API路由、SSE封装、Agent循环、LLM调用、工具执行、数据库写入 |

### 1.3 前置环境依赖分析（v1.7 - 小健 - 2026-06-15 核实）

**铁律1：测试用例使用独立配置文件，不修改系统配置**

**注意：数据库隔离尚未实现，当前测试使用系统数据库（~/.omniagent/chat_history.db）**

| 依赖项 | 当前状态 | 需要准备 |
|--------|---------|---------|
| 后端服务 | 已运行/需重启 | 每次测试前确认存活 |
| **测试配置** | E:\test_dir\test_config\config.yaml（security.enabled=false） | 已存在，从系统配置复制而来 |
| **数据库** | 使用系统DB：~/.omniagent/chat_history.db | 隔离尚未实现，注意不污染生产数据 |
| E:\test_dir | 测试根目录 | 已存在 | OK |
| E:\test_dir\test.txt | 内容丰富（含ERROR/WARNING示例） | 已更新 | OK（已核实 2026-06-22） |
| E:\test_dir\test_old.txt | 内容丰富（旧版本文档） | 已更新 | OK（已核实 2026-06-22） |
| E:\test_dir\backup | 空目录 | **空目录** | OK（已核实 2026-06-15） |
| E:\test_dir\data.csv | 含2026年数据，多字段 | 11行数据含6条2026年 | OK（已更新 2026-06-22） |
| E:\test_dir\test.docx | 简单Word文档 | 已存在 | OK |
| E:\test_dir\empty_dir | 空目录 | 已存在且为空 | OK |
| E:\test_dir\reports | 空目录（供汇总报告） | 已存在 | OK |
| E:\test_dir\check_system.py | Python测试脚本 | 已创建 | OK（已创建 2026-06-22） |
| 日志目录 | logs/可写 | 已存在 | OK |

### 1.4 与现有测试的区别

| 测试类型 | 测试什么 | 用什么驱动 | 能发现什么问题 |
|---------|---------|-----------|--------------|
| 单元测试（现有~200个） | 单个工具函数 | Mock/Patch | 函数级bug |
| MessageBuilder测试（43个） | Prompt构建逻辑 | Mock | Prompt构建bug |
| **全链路E2E（本方案）** | **Agent行为+系统集成** | **真实后端（读取config.yaml的LLM配置）+ 真实SQLite** | **死循环、工具选错、参数传错、会话污染、日志异常、数据库异常** |


### 1.5 核心测试原则（铁律）

**铁律1：测试的目的是发现问题，不是跑脚本。**

**铁律2：严禁批量跑测试用例。一次只能跑一个测试用例。**

| 允许 | 禁止 |
|------|------|
| ✅ 写一个脚本 → 跑一个 → 观察 → 检查 | ❌ 一次写完所有脚本 |
| ✅ 一个用例跑通后再写下一个 | ❌ 一次批量跑多个用例 |
| ✅ 每次跑完仔细观察系统运行过程 | ❌ 只看pass/fail就过 |
| ✅ 失败了停下来分析修复 | ❌ 失败了跳过继续下一个 |

**铁律3：所有测试统一基于真实后端运行，禁止Mock：**

| 环节 | 要求 | 禁止 |
|------|------|------|
| **后端** | 真实uvicorn启动，从config.yaml读取LLM配置 | ❌ 不启动后端、Mock后端 |
| **LLM** | 真实调用config.yaml中配置的provider/model | ❌ Mock LLM返回、假响应 |
| **工具** | 真实执行文件操作、命令、网络请求等 | ❌ Mock工具返回值 |
| **数据库** | 真实读写SQLite（系统DB：~/.omniagent/chat_history.db） | ❌ Mock数据库、内存数据库 |
| **SSE** | 真实解析后端返回的SSE事件流 | ❌ 假造SSE事件 |

**一句话：测试输入就是用户会说的话，测试过程就是真实系统的完整运行。**

---

## 二、测试架构
###  2.1  测试架构
```
┌─────────────────────────────────────────────────────────────────┐
│                      测试脚本（pytest）                          │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    测试执行引擎                            │   │
│  │  ① 初始化测试环境                                         │   │
│  │  ② 通过httpx发送POST /chat → 接收SSE事件流               │   │
│  │  ③ 逐事件解析、记录、断言                                  │   │
│  │  ④ 查询日志文件，检查ERROR/WARNING                        │   │
│  │  ⑤ 查询SQLite数据库，检查session/messages/steps          │   │
│  │  ⑥ 清理测试残留文件（DB记录保留供检查）                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│         ↓ HTTP POST /chat           ↓ SSE event stream          │
│    ┌──────────────────────────────────────────────────┐         │
│    │              后端服务（uvicorn）                   │         │
│    │  main.py → chat_router.py → Agent → LLM → 工具   │         │
│    │        ↑                                         │         │
│    │   从 config.yaml 读取 LLM provider/model        │         │
│    │                          ↓                        │         │
│    │                    SQLite DB / 日志文件             │         │
│    └──────────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```
###  2.2  测试架构E2E测试模板目录

模板目录位置: `backend/e2emodel/`

```
backend/e2emodel/
├── test_e2e_p0_01_introduce_self.py    ← 模板A: 无工具调用
├── test_e2e_p0_02_tool_call.py         ← 模板B: 单工具调用
├── test_e2e_p0_03_multi_step.py        ← 模板C: 多步推理
├── test_e2e_p0_04_data_persistence.py  ← 模板D: 数据持久化
└── e2e_helpers.py                      ← 公共/核心函数库(唯一来源)
```
**四类模板说明**:

| 模板 | 适用场景 | 典型用例 | 特点 |
|------|---------|---------|------|
| **模板A (P0-01)** | LLM直接回复，不调用工具 | 自我介绍、闲聊、知识问答 | 无工具断言，侧重回复语义验证 |
| **模板B (P0-02)** | LLM调用单个工具完成任务 | 创建文件、读取文件 | 单工具调用，侧重SSE含action事件+tool_calls验证 |
| **模板C (P0-03)** | LLM多步推理 | 先读文件再回复内容 | 验证多step记录+tool_name非空+observation存在 |
| **模板D (P0-04)** | LLM调用工具后验证DB三表 | 目录列表、数据持久化 | 侧重DB三表完整验证+steps字段完整性 |
---
**测试case的脚本** 必须在目录: `backend/tests/` 下创建

## 三、完整测试流程

每个测试用例严格按 **4步流程** 执行：检查 → 编写脚本 → 执行脚本 → 检查结果。

---

### 3.1 第1步：检查和重启后端服务

**每次执行测试前，逐项检查，全部通过才能开始。**

```
检查项 1: 杀死旧的后端进程（每次测试前必须执行）
  · 命令: Stop-Process -Id (Get-NetTCPConnection -LocalPort 8000).OwningProcess -Force
  · 或: netstat -ano | findstr :8000 → taskkill /F /PID <pid>
  · 确认进程已杀: Get-Process -Id <pid> -ErrorAction SilentlyContinue (无输出=已杀)
检查项 2: 日志目录
  · backend/logs/ 和 backend/logs/prompt-logs/ 是否可写

**重启后端服务**
    启动后端 : ps窗口  - reload-dir  监视 backend/app
    只能启动一个有后台服务的窗口
    - uvicorn app.main:app --reload --reload-dir app --port 8000
    - 运行 pytest 不准确设置超时,(核心脚本已经设置超时2000s)
  · 验证: 新窗口显示 "Application startup complete"，或 curl http://127.0.0.1:8000/api/v1/health 返回200
```

### 3.2 第2步：编写用例脚本

**铁律：一次只写一个，验证完成后再执行下一个,case脚本 不要设置超时,核心代码已经默认设置为10分钟**

**测试case的脚本** 必须在目录: `backend/tests/` 下创建
```
禁止一次性编写所有脚本！
  · 一次性写完 → 无法验证每个脚本是否正确
  · 一次性写完 → 错误会累积,难以定位
  · 批量测试 = 自欺欺人,发现不了真正的问题

正确做法: 一个用例一个脚本,写完跑通再写下一个

  第1个: 看测试用例 E2E-P0-01 "详细介绍自己"
         → 要测什么? (API→SSE通路)
         → 要发什么指令? ("详细介绍一下你自己，你能做什么")
         → 要验证什么? (收到final、无error、DB有记录)
         → 编写 test_e2e_p0_01_introduce_self()
         → 运行 → 观察 → 检查
         → 通过 → 进入第2个
         → 失败 → 分析错误、修复、重跑、通过后进入第2个

  第2个: 看测试用例 E2E-P0-02 "创建文件"
         → 编写 → 运行 → 观察 → 检查
         → 通过 → 进入第3个

  ...逐个推进...
---

### 3.3 第3步：执行脚本-严禁设置超时

**一次跑一个case用例严禁一次多脚本**
**严禁设置启动脚本超时**核心脚本已有超时2000s 
记录测试起始时间 T0（测试脚本手动记录）

### 3.4 第4步：[手动检查]分析测试结果

**手动检查** 用例执行完毕后，逐项验证，全部通过才算完成。
**手动检查结果** 必须追加到相应的测试记录的尾巴上,才可以进入下一个case
```
用例执行完毕
      │
      ▼
┌─────────────────────────────────────┐
│ 验证项1: 代码错误异常检查（铁律）    │
│  · 检查日志中是否有traceback/ERROR   │
│  · 检查SSE中是否有error事件          │
│  · 有 → 立即停止,走3.5修复流程       │
│  · 无 → 继续验证项1                  │
│  · 原则: 代码错误优先级最高,必须先修  │
└─────────────────────────────────────┘
      │
      │ (无代码错误)
      ▼
┌─────────────────────────────────┐
│ 验证项2: 调用链分析（手动）       │
│  · 对照手册"预期调用链"逐个核对   │
│  · 工具选择是否合理               │
│  · 调用顺序是否合理               │
│  · LLM调用次数是否合理            │
│  · 输出: [CALL CHAIN] tool→tool  │
│  · 正常标准: 与预期一致或语义等价  │
└─────────────────────────────────┘
      │
      │
      ▼
┌─────────────────────────────────┐
│ 验证项3: 参数正确性（人工）       │
│  · tool_params中的路径/关键词正确 │
└─────────────────────────────────┘
      │
      ▼
   全部通过？
    ├─ 是 → [务必遵守]-务必将手动的分析结果追加到测试记录的尾巴上,才可以进入下一个case
    └─ 否 → 执行错误处理（见3.5）
```

### 3.5 分析的错误处理报告和处理流程（第4步发现FAIL时的必须流程）

```
**脚本错误的修改规则** 脚本分为:用例case脚本和核心测试过程的脚本,
 有典型性问题修改都必须修改 e2e模板库的 核心e2e_helpers.py代码,

错误分两类:
  A类(运行时): LLM超时/工具崩溃/死循环/SSE断流 → 查日志traceback
  B类(数据不一致): session缺失/messages丢失/steps不匹配 → 对比SSE vs DB

处理流程:
  ① 分类 → A类还是B类
  ② 定位 → 查后端日志 + SSE事件 + prompt-logs,定位到文件:行号
  ③ 分析 → 是代码bug还是测试用例问题
  ④ 修复 → 遵守10大规范 (SRP/DRY/KISS/YAGNI,禁止backward compat)
  ⑤ 复测 → 重跑该用例 + 跑关联测试确认无回归
  ⑥ 记录 → 写入测试报告

严重等级:
  P0(致命): 系统崩溃/死循环 → 立即停止,修复后从P0重跑
  P1(严重): 功能异常但不崩溃 → 修复后重跑该用例
  P2(一般): 非核心异常 → 记录,批量修复
  P3(警告): 性能偏差 → 记录,不阻塞

禁止事项:
  ❌ 看到FAIL就跳过   ❌ 只改测试不改代码
  ❌ 修复时不跑单元测试  ❌ 不记录直接修复
```

## 四、测试场景用例清单（共57个工具覆盖，1个异步工具专项测试）

**设计原则：**
1. 每个测试必须至少覆盖1个工具，所有57个工具至少被覆盖1次（timer异步工具单独测试）
2. 单工具任务（P1-P4）验证每个工具在真实LLM下能否被正确识别和调用
3. 复合任务（P5）验证多工具串联、多步推理能力
4. 异常任务（P6）验证系统容错和恢复能力
5. 覆盖率追踪：每节末尾标注已覆盖工具数和累计覆盖率

**前置准备（测试数据统一创建）：**

在开始所有测试前，先统一创建测试所需的基础文件和目录：

```
【基础目录结构】
· E:\test_dir\                     — 测试根目录
· E:\test_dir\backup\              — 空目录（供复制/归档测试）
· E:\test_dir\empty_dir\           — 空目录（供空结果测试）
· E:\test_dir\reports\             — 空目录（供汇总报告存放）

【基础测试文件】
· E:\test_dir\test.txt             — 内容"hello world"（供读取/搜索/编辑测试）
· E:\test_dir\test_old.txt         — 内容"old content"（供压缩测试）
· E:\test_dir\data.csv             — 内容如下（供数据分析测试）:
  
· E:\test_dir\test.docx            — 简单Word文档，内容"这是测试文档内容"（供文档读取测试）

【系统环境要求】
· python命令在PATH中（开发环境检查用）
· git命令在PATH中（开发环境检查用）
· 网络连通（网络诊断/搜索/获取网页用）
· 记事本窗口打开（窗口管理流程用，可手动打开或脚本自动打开）
· chat_history.db存在且有数据（数据库分析用，后端启动后自动创建）

【缺失数据补充方案】
1. python+git环境: 测试前执行`where python`和`where git`确认
2. 网络连通: 测试前执行`ping baidu.com`确认
3. 记事本窗口: 测试前手动打开记事本，或在脚本中用`os.system("notepad")`自动打开
4. chat_history.db数据: 先启动后端并发几条消息产生数据，或手动INSERT测试数据

【每个用例的数据清理规则】

```

---

### 4.1 P0：核心链路验证（10个混合多类任务）

**设计原则**：每个case混合多种工具类型，覆盖除desktop和win注册表外的所有工具。

| 编号 | 任务 | 用户输入 | 前置数据 | 预期调用链(混合) | 通过标准 | 失败标准 |
|------|------|---------|---------|------------------|---------|---------|
| E2E-P0-01 | LLM对话通路 | `"详细介绍一下你自己，你能做什么，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 无 | LLM直接回复，不调用工具 | 收到final事件；回复语义完整；无error事件 | 超时未收到final；回复为空或胡言乱语；收到error |
| E2E-P0-02 | FILE基础通路 | `"在E盘根目录创建一个e2e_p0_02.txt，内容为hello，然后读出来确认写对了，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | E盘根目录可写 | write_text_file→read_text_file | 文件存在；内容正确；DB有记录 | 文件未创建；内容错误 |
| E2E-P0-03 | FILE+SHELL混合 | `"帮我创建一个E:\test_dir\run_python.py文件，内容是print('test')，然后执行它看看输出，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | E:\test_dir\可写；python在PATH中 | write_text_file→execute_shell_command(python) | 文件存在；执行输出包含"test" | 任一步骤失败 |
| E2E-P0-04 | FILE+NETWORK混合 | `"帮我从网上搜索Python教程，把第一个结果的网页内容保存到E:\test_dir\python_tutorial.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通；E:\test_dir\可写 | search_web→fetch_webpage→write_text_file | 文件存在且包含网页内容 | 任一步骤失败 |
| E2E-P0-05 | SHELL+SYSTEM混合 | `"帮我查看当前系统信息，然后执行dir命令列出E:\test_dir的文件，把结果保存到E:\test_dir\sys_info.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 系统正常运行；E:\test_dir\可写 | get_system_info→execute_shell_command(dir)→write_text_file | 文件包含系统信息和文件列表 | 任一步骤失败 |
| E2E-P0-06 | NETWORK+DATA混合 | `"帮我搜索今天的科技新闻，分析搜索结果的关键词，把分析报告保存到E:\test_dir\news_analysis.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通；E:\test_dir\可写 | search_web→analyze_data→write_text_file | 文件包含新闻和关键词分析 | 任一步骤失败 |
| E2E-P0-07 | FILE+SHELL+NETWORK混合 | `"帮我：①创建一个脚本文件E:\test_dir\check_network.py检查网络连通性 ②执行脚本 ③搜索网络诊断最佳实践 ④把结果汇总保存到E:\test_dir\network_report.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通；python在PATH中；E:\test_dir\可写 | write_text_file→execute_shell_command→search_web→write_text_file | 脚本存在；报告包含执行结果和搜索结果 | 任一步骤失败 |
| E2E-P0-08 | FILE+DATA+DOCUMENT混合 | `"帮我：①读取E:\test_dir\data.csv数据 ②分析统计特征 ③生成分析报告Word文档保存到E:\test_dir\analysis.docx ④列出目录确认文件生成，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | data.csv存在；E:\test_dir\可写 | read_text_file→analyze_data→write_docx→list_directory | Word文档存在且包含分析结果 | 任一步骤失败 |
| E2E-P0-09 | SHELL+SYSTEM+NETWORK混合 | `"帮我：①查看系统CPU和内存使用率 ②执行ping命令测试网络 ③搜索系统优化建议 ④把结果保存到E:\test_dir\optimize.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 系统正常运行；网络连通；E:\test_dir\可写 | get_system_info→execute_shell_command(ping)→search_web→write_text_file | 报告包含系统信息、网络状态、优化建议 | 任一步骤失败 |
| E2E-P0-10 | FILE+SHELL+DATA+SYSTEM混合 | `"帮我：①读取E:\test_dir\data.csv数据 ②分析数据统计特征 ③查看系统内存使用情况 ④执行python命令确认环境 ⑤把所有结果汇总保存到E:\test_dir\comprehensive.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | data.csv存在；python在PATH中；系统正常；E:\test_dir\可写 | read_text_file→analyze_data→get_system_info→execute_shell_command(python)→write_text_file | 报告包含数据分析、系统信息、python版本 | 任一步骤失败 |

**覆盖率**：10个case覆盖FILE/SHELL/NETWORK/SYSTEM/DATA/DOCUMENT工具类型（除desktop和win注册表）

---

### 4.2 P1：FILE工具多任务场景（15个工具 → 5个真实工作场景）

**设计原则**：每个case都是真实工作场景，至少调用3-5个工具，多轮交互完成。

| 编号 | 工作场景 | 用户输入 | 前置数据 | 预期调用链(多工具) | 通过标准 | 失败标准 |
|------|---------|---------|---------|-------------------|---------|---------|
| E2E-P1-01 | 项目初始化流程 | `"我要在E:\test_dir下创建一个新项目myproject-react，项目类实现是前端 react项目,帮我：①创建项目目录 ②创建README.md写入项目说明 ③创建src子目录 ④在src下创建main.py骨架 ⑤列出项目结构确认都创建好了，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | E:\test_dir\可写；用例后清理myproject目录 | write_text_file(README)→write_text_file(main.py)→list_directory→write_text_file(汇总报告) | myproject目录存在；README.md和main.py都存在；回复包含项目结构 | 任一文件未创建 |
| E2E-P1-02 | 日志分析流程 | `"E:\test_dir\下有个test.txt文件，帮我：①先读出来看看内容 ②搜索里面有没有error或warning关键词 ③把内容和搜索结果整理成分析报告 ④保存到E:\test_dir\analysis_report.txt ⑤确认报告文件已生成，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | test.txt存在，内容="hello world"；用例后删除analysis_report.txt | read_text_file→grep_file_content→write_text_file→read_text_file(确认) | analysis_report.txt存在且包含原文内容和搜索结果 | 分析报告未生成 |
| E2E-P1-03 | 代码重构流程 | `"我要重构E:\test_dir\test.txt，帮我：①先备份到test_backup.txt ②把hello改成你好 ③读取修改后的内容确认改对了 ④对比备份和修改后的差异 ⑤把重构过程记录到refactor_log.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | test.txt存在，内容="hello world"；用例后恢复test.txt为"hello world"，删除test_backup.txt和refactor_log.txt | copy_file→edit_text_file→read_text_file→write_text_file | test_backup.txt存在；test.txt内容含"你好"；refactor_log.txt存在 | 任一步骤失败 |
| E2E-P1-04 | 数据归档流程 | `"帮我把E:\test_dir下的文件整理归档：①列出所有文件 ②把test.txt和test_old.txt打包成archive.zip ③把zip移动到backup目录 ④解压验证内容完整 ⑤清理临时文件，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | test.txt和test_old.txt存在；backup/为空；用例后清理backup下zip和解压文件 | list_directory→compress_files→move_file→extract_archive→list_directory(验证) | backup下有archive.zip；解压后有原文件 | 归档失败 |
| E2E-P1-05 | 配置管理流程 | `"我要管理项目配置：①读取E:\test_dir\data.csv看看数据格式 ②创建config.json配置文件记录数据源路径 ③创建settings.yaml记录处理参数 ④列出目录确认配置文件都创建了 ⑤把配置信息汇总报告，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | data.csv存在；用例后删除config.json和settings.yaml | read_text_file→write_data_file(json)→write_data_file(yaml)→list_directory→write_text_file | config.json和settings.yaml都存在且可读 | 配置文件未创建 |

**覆盖率**：5个场景覆盖15个FILE工具(read/write/edit/copy/move/compress/extract/list/search/grep/read_data/write_data)

---

### 4.3 P2：SHELL+NETWORK多任务场景（9个工具 → 4个真实工作场景）

| 编号 | 工作场景 | 用户输入 | 前置数据 | 预期调用链(多工具) | 通过标准 | 失败标准 |
|------|---------|---------|---------|-------------------|---------|---------|
| E2E-P2-01 | 开发环境检查流程 | `"我要检查开发环境是否就绪：①查找python命令路径 ②查找git命令路径 ③执行python --version确认版本 ④执行git --version确认版本 ⑤把环境检查结果保存到E:\test_dir\env_check.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | python和git在PATH中；用例后删除env_check.txt | find_command(python)→find_command(git)→execute_shell_command(python)→execute_shell_command(git)→write_text_file | env_check.txt存在且包含python和git路径及版本 | 任一命令找不到 |
| E2E-P2-02 | 网络诊断修复流程 | `"网络好像有问题，帮我诊断修复：①ping测试baidu.com连通性 ②如果通了就获取百度首页内容 ③检查当前网络连接状态 ④把诊断结果和网页内容保存报告 ⑤给出网络状态建议，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通；用例后删除网络报告 | network_diagnose(ping)→fetch_webpage→net_connections→write_text_file | 报告包含ping结果、网页内容、连接状态 | 诊断失败 |
| E2E-P2-03 | 技术调研流程 | `"我要调研Python异步编程：①搜索Python asyncio最佳实践 ②打开第一个结果的网页详细内容 ③执行一段asyncio示例代码验证 ④把调研结果和代码运行结果保存报告 ⑤列出报告文件确认，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通；用例后删除调研报告 | search_web→fetch_webpage→execute_code→write_text_file→list_directory | 报告包含搜索结果、网页内容、代码执行结果 | 任一步骤失败 |
| E2E-P2-04 | 后台任务监控流程 ⚠️环境敏感 | `"我要启动后台任务并监控：①启动后台ping baidu.com持续5秒 ②等2秒后查看输出 ③再等2秒查看最新输出 ④终止后台任务 ⑤把监控日志保存报告，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通；用例后删除监控报告 | execute_shell_command(后台)→shell_session(读输出)→shell_session(读输出)→shell_session(终止)→write_text_file | 报告包含多次ping输出 | 后台任务管理失败 |

**覆盖率**：4个场景覆盖9个SHELL+NETWORK工具(execute/find/session/code/search/fetch/diagnose/http/connections)

---

### 4.4 P3：SYSTEM+DESKTOP多任务场景

| 编号 | 工作场景 | 用户输入 | 前置数据 | 预期调用链(多工具) | 通过标准 | 失败标准 |
|------|---------|---------|---------|-------------------|---------|---------|
| E2E-P3-01 | 系统健康检查 | `"电脑感觉不太对劲，帮我全面检查：①查CPU和内存使用率 ②列出占用资源最多的前10个进程 ③检查网络连接状态 ④查看系统日志最近的错误 ⑤生成健康检查报告保存到E:\test_dir\health_check.txt"` | 系统正常运行 | get_system_info→list_processes→net_connections→event_log→write_text_file | 报告包含CPU/内存/进程/网络/日志信息 | 任一检查失败 |
| E2E-P3-02 | 环境配置诊断 ⚠️需管理员权限 | `"帮我诊断Python环境：①查看PATH环境变量 ②查找python命令路径 ③查看PYTHONPATH环境变量 ④读取注册表中Python安装路径 ⑤保存诊断报告"` | 系统正常运行 | get_env→find_command(python)→get_env→registry_read→write_text_file | 报告包含PATH/python路径/注册表信息 | 任一步骤失败 |
| E2E-P3-03 | 桌面自动化 ⚠️环境敏感 | `"帮我操作桌面：①列出当前所有窗口 ②截取当前屏幕 ③读取剪贴板内容 ④发送一个测试通知 ⑤保存操作记录"` | 系统正常运行，有窗口打开 | window_info→screen_capture→clipboard_read→send_notification→write_text_file | 报告包含窗口列表/截图路径/剪贴板内容 | 任一步骤失败 |
| E2E-P3-04 | 窗口管理 ⚠️环境敏感 | `"帮我整理窗口：①列出所有窗口 ②找到记事本窗口并最大化 ③截屏看看效果 ④把记事本还原 ⑤记录操作过程"` | 有记事本窗口打开 | window_info→window_maximize→screen_capture→window_restore→write_text_file | 报告包含窗口操作记录 | 窗口操作失败 |
| E2E-P3-05 | 系统监控告警 | `"帮我监控系统：①获取当前时间 ②查看CPU使用率 ③查看内存使用率 ④如果CPU>80%或内存>90%就发通知告警 ⑤保存监控数据报告"` | 系统正常运行 | time_now→get_system_info→get_system_info→send_notification→write_text_file | 报告包含时间和系统资源数据 | 监控失败 |

**覆盖率**：5个场景覆盖19个SYSTEM+DESKTOP工具

---

### 4.5 P4：DOCUMENT+DATAANALYSIS多任务场景

| 编号 | 工作场景 | 用户输入 | 前置数据 | 预期调用链(多工具) | 通过标准 | 失败标准 |
|------|---------|---------|---------|-------------------|---------|---------|
| E2E-P4-01 | 文档处理流程 | `"我要处理一批文档：①读取E:\test_dir\test.docx内容 ②创建新的Word文档写入处理结果 ③把原文档转成PDF格式 ④列出目录确认文件都生成了 ⑤记录处理过程"` | test.docx存在；用例后删除生成的文档和PDF | read_docx→write_docx→convert_document→list_directory→write_text_file | 新Word文档和PDF都存在 | 任一步骤失败 |
| E2E-P4-02 | 数据分析报告 | `"我要分析销售数据并生成报告：①读取E:\test_dir\data.csv数据 ②分析数据的统计特征(均值/最大/最小) ③筛选2026年的数据 ④生成柱状图可视化 ⑤把分析结果保存到Word报告"` | data.csv存在，含数值列和2026年数据；用例后删除图表和报告 | read_text_file→analyze_data→filter_data→generate_chart→write_docx | 图表和Word报告都存在 | 分析失败 |
| E2E-P4-03 | 数据库分析 | `"我要分析应用数据库：①获取chat_history.db的表结构 ②查询sessions表最近5条记录 ③统计messages表的消息数量 ④把数据库分析结果保存报告 ⑤列出报告文件确认"` | chat_history.db存在且有数据；用例后删除分析报告 | get_db_schema→query_sql(sessions)→query_sql(messages)→write_text_file→list_directory | 报告包含表结构和查询结果 | 查询失败 |
| E2E-P4-04 | 时间规划 | `"我要规划下周的工作：①获取当前时间 ②计算从今天到下周一是几天 ③查询下周一是不是工作日 ④计算从今天到月底还有多少天 ⑤把时间规划保存报告"` | 无；用例后删除规划报告 | time_now→time_diff→query_calendar→time_diff→write_text_file | 报告包含时间计算结果 | 时间计算错误 |
| E2E-P4-05 | 综合数据处理 | `"我要综合处理多种数据：①读取E:\test_dir\data.csv数据 ②分析统计特征 ③生成折线图 ④把图表和统计数据写入Excel报告 ⑤创建Word文档总结分析结论"` | data.csv存在；用例后删除图表/Excel/Word | read_text_file→analyze_data→generate_chart→write_xlsx→write_docx | 图表/Excel/Word都存在 | 任一步骤失败 |

**覆盖率**：5个场景覆盖15个DOCUMENT+DATAANALYSIS工具

---

### 4.6 P5：复合多步任务（多tool、多类型、多步骤，18个任务）

这些任务验证Agent多步推理和跨分类调用能力。每个任务独立执行，不依赖其他任务。

#### 4.5.1 同分类复合任务（6个）

| 编号 | 任务 | 用户输入 | 前置数据 | 预期调用链 | 通过标准 | 失败标准 |
|------|------|---------|---------|-----------|---------|---------|
| E2E-P5-01 | 文件全流程 | `"帮我在E:\test_dir下创建一个e2e_mc01.txt，里面写上'测试数据——2026年6月'，然后读出来看看写对了没有，再搜一下里面有没有'测试'这个词，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | E:\test_dir\可写；用例后清理e2e_mc01*.txt | write→read→grep | 三步都执行；文件内容包含"测试" | 任一步骤失败 |
| E2E-P5-02 | 文件管理链 | `"刚才创建的e2e_mc01.txt我想改个名叫e2e_mc02.txt，改完再复制一份到backup目录备份一下，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | e2e_mc01.txt存在（需先执行MC-01创建）；backup/为空；用例后清理 | rename→copy | 新文件在backup目录存在 | 操作未完成 |
| E2E-P5-03 | Shell管理链 | `"帮我查一下notepad.exe装在哪了，查到了帮我把它打开，我懒得自己去找，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | notepad在系统中存在 | find→execute | 两步都返回结果 | 任一步骤失败 |
| E2E-P5-04 | 网络查询链 | `"帮我搜一下今天科技圈有什么大新闻，搜到了把第一条结果的详细内容打开给我看看，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通 | search_web→fetch_webpage | 返回网页内容 | 获取失败 |
| E2E-P5-05 | 系统诊断链 | `"电脑开机越来越慢了，帮我查一下CPU和内存的占用情况，再看看哪些进程在吃资源，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 系统正常运行 | get_system_info→list_processes | 两步都返回系统数据 | 任一步骤失败 |
| E2E-P5-06 | 桌面操作链 ⚠️环境敏感 | `"帮我截个当前屏幕的图，然后看看屏幕上有哪些文字，把识别出来的内容告诉我，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 系统正常运行，屏幕有文字 | screen_capture→ocr | 返回识别出的文字 | OCR失败 |

#### 4.5.2 跨分类复合任务（6个）

| 编号 | 任务 | 用户输入 | 前置数据 | 预期调用链（跨分类） | 通过标准 | 失败标准 |
|------|------|---------|---------|-------------------|---------|---------|
| E2E-P5-07 | 时间写文件 | `"帮我看看现在几点了，然后以当前时间做文件名（比如20260615_1430.txt）创建个文件放到E:\test_dir，里面写上当前的时间信息，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | E:\test_dir\可写；用例后清理创建的文件 | get_time→write_text_file | 文件名包含时间；内容有时间信息 | 文件未创建 |
| E2E-P5-08 | 网页存文件 | `"帮我查一下今天北京的天气怎么样，搜到的结果保存到E:\test_dir\weather.txt里，回头我好出门前看一眼，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通+E:\test_dir\可写；用例后删除weather.txt | search_web→write_text_file | weather.txt存在且包含天气信息 | 文件未创建 |
| E2E-P5-09 | 系统报告存文件 | `"帮我把这台电脑的系统信息（CPU、内存、操作系统版本）查出来，整理成一份报告保存到E:\test_dir\sys_report.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 系统正常运行+E:\test_dir\可写；用例后删除sys_report.txt | get_system_info→write_text_file | sys_report.txt存在且包含系统信息 | 文件未创建 |
| E2E-P5-10 | 搜索发通知 | `"帮我搜一下2026年AI领域的最新进展，挑重点用Windows通知弹给我看看，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通+系统正常运行(低风险副作用) | search_web→send_notification | 通知发送成功 | 通知失败 |
| E2E-P5-11 | SQL结果存文件 | `"帮我查一下sessions表里前3条会话记录是什么样的，查到的结果保存到E:\test_dir\query_result.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | chat_sessions表有数据+E:\test_dir\可写；用例后删除query_result.txt | query_sql→write_text_file | 文件包含查询结果 | 文件未创建 |
| E2E-P5-12 | 综合报告 | `"帮我收集三样信息：①当前时间 ②系统CPU和内存使用情况 ③E:\test_dir下的文件列表，汇总成一份综合报告保存到E:\test_dir\report.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 系统正常运行+E:\test_dir\有文件；用例后删除report.txt | get_time→get_system_info→list_directory→write_text_file | report.txt包含三类信息 | 任一步骤失败 |

#### 4.5.3 新增跨分类复合任务（6个，每个跨越4~5种tool类型）

| 编号 | 任务 | 用户输入 | 前置数据 | 预期调用链（跨分类） | 通过标准 | 失败标准 |
|------|------|---------|---------|-------------------|---------|---------|
| E2E-P5-13 | 网络搜索+系统诊断+文件存档 | `"帮我从网上搜一下Windows系统时间同步的方法，然后用systeminfo命令查一下我这台电脑的详细系统信息，把搜索结果和系统信息汇总保存到E:\test_dir\sys_research.txt，最后用dir确认文件已经在了，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通+系统正常运行；用例后清理sys_research.txt | search_web→execute_shell_command(systeminfo)→write_text_file→execute_shell_command(dir) | 文件存在且包含搜索内容和系统信息 | 任一步骤失败 |
| E2E-P5-14 | 计时+系统监控+截屏+报告归档 | `"帮我看看当前时间，查一下CPU和内存的使用情况，再截个当前屏幕的图，然后把时间和系统状态写成一个报告保存到E:\test_dir\monitor_report.txt，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 系统正常运行+E:\test_dir\可写；用例后清理monitor_report.txt和截图 | get_time→get_system_info→screen_capture→write_text_file | 报告文件存在且包含时间和系统信息 | 任一步骤失败 |
| E2E-P5-15 | 命令查询+系统分析+文档生成+文件验证 | `"用systeminfo命令查一下这台电脑的初始安装日期，然后把硬件信息整理成一份Word文档放到E:\test_dir\hardware_report.docx，再用dir命令确认文档已生成，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 系统正常运行+E:\test_dir\可写；用例后删除hardware_report.docx | execute_shell_command(systeminfo)→write_document→execute_shell_command(dir) | Word文档存在且可读；dir输出包含文件名 | 任一步骤失败 |
| E2E-P5-16 | 网络搜索+文件保存+时间记录+通知提醒 | `"帮我搜一下今天AI行业的热点新闻，把新闻摘要保存到E:\test_dir\ai_news.txt，然后告诉我现在几点几分，最后弹个Windows通知告诉我全部搞定了，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通+E:\test_dir\可写；用例后删除ai_news.txt | search_web→write_text_file→get_time→send_notification | 文件存在且包含新闻内容；通知发送成功 | 任一步骤失败 |
| E2E-P5-17 | 文档读取+文字提取+日期计算+通知完成 | `"帮我把E:\test_dir\test.docx的内容读出来，把里面的文字提取出来保存到E:\test_dir\doc_content.txt，再帮我算一下从2026年1月1日到今天过了多少天，最后发个通知告诉我提取完成，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | test.docx存在且可读+E:\test_dir\可写；用例后删除doc_content.txt | read_document→write_text_file→time_diff→send_notification | 文件存在且包含docx文字内容；天数计算正确；通知发送成功 | 任一步骤失败 |
| E2E-P5-18 | 计时+网络搜索+系统监控+文件汇总+通知完成 | `"帮我做一件事：①看看现在几点了 ②搜一下今天的科技新闻 ③查一下CPU使用率 ④把①②③的信息汇总成一个综合报告保存到E:\test_dir\daily_report.txt ⑤搞定后弹个Windows通知告诉我，把任务完成的过程分析完整并写入md的报告文件并存入report目录"` | 网络连通+系统正常运行+E:\test_dir\可写；用例后删除daily_report.txt | get_time→search_web→get_system_info→write_text_file→send_notification | 报告文件包含时间+新闻+系统信息；通知发送成功 | 任一步骤失败 |

---

### 4.7 P6：异常与容错场景（E2E测试，通过send_chat触发，8个场景）

**测试目标**：验证Agent在遇到错误输入、不存在的资源、不支持的操作时，能否正常处理而不崩溃、不死循环。

**测试方法**：每个场景通过`send_chat`发送一条用户指令，验证Agent返回合理响应。

| 编号 | 测试目标 | 用户输入 | 前置数据 | 预期行为 | 验证标准 | 能发现的问题 |
|------|---------|---------|---------|---------|---------|-------------|
| E2E-P6-01 | 文件不存在时Agent的容错 | `"读取E:\test_dir\no_such_file.txt"` | no_such_file.txt不存在 | Agent调用read_file，工具返回错误，Agent回复告知用户 | final事件存在；回复包含"不存在"/"找不到"/"无法"等提示；不死循环（steps<50） | Agent对文件不存在的异常处理逻辑是否正确 |
| E2E-P6-02 | 目录不存在时Agent的容错 | `"列出E:\test_dir\no_such_dir下的文件"` | no_such_dir不存在 | Agent调用list_directory，工具返回错误，Agent回复告知用户 | final事件存在；回复包含错误提示；不死循环 | Agent对目录不存在的异常处理逻辑 |
| E2E-P6-03 | 网络请求失败时Agent的容错 | `"访问http://this-does-not-exist-12345.com"` | 无 | Agent尝试访问，网络工具返回连接失败，Agent回复告知用户 | final事件存在；回复包含"无法访问"/"连接失败"/"不存在"等提示；不死循环 | Agent对网络异常的处理，是否会陷入重试循环 |
| E2E-P6-04 | SQL查询错误时Agent的容错 | `"帮我查一下chat_history.db里有没有user_settings这张表"` | chat_history.db存在 | Agent调用工具查询，返回表不存在，Agent回复告知用户 | final事件存在；回复包含"不存在"/"没有"等提示；不死循环 | Agent对数据库查询异常的处理 |
| E2E-P6-05 | 长文本输入时Agent的处理 | `"把下面这段文字保存到E:\test_dir\long_text.txt：这是一段测试文字，用于验证Agent对长输入的处理能力。" + "重复内容x100` | E:\test_dir\可写；用例后删除long_text.txt | Agent调用write_text_file，正确写入长文本，回复确认 | final事件存在；E:\test_dir\long_text.txt存在且内容长度>1000；不死循环 | Agent对大payload的处理能力，是否OOM或超时 |
| E2E-P6-06 | 不同session的消息隔离 | 用asyncio.gather同时发2个请求：A=`"现在几点了"` B=`"今天星期几"` | 无 | 两个请求各自独立处理，各自有自己的session | 两个都收到final；A的session中无B的消息，B的session中无A的消息；各自DB记录独立 | session隔离机制是否正确，是否有消息串扰 |
| E2E-P6-07 | 不支持的操作Agent的响应 | `"帮我把E:\test_dir\test.txt加密一下"` | test.txt存在 | Agent说明无法加密（没有加密工具），不崩溃 | final事件存在；回复说明无法执行/不支持；不死循环 | Agent对不支持操作的处理，是否会尝试不存在的工具 |
| E2E-P6-08 | 空结果场景Agent的响应 | `"在E:\test_dir\empty_dir中搜索所有.txt文件"` | empty_dir/为空目录 | Agent调用搜索工具，返回空结果，正确报告"未找到" | final事件存在；回复包含"未找到"/"没有"/"空"等提示；不死循环 | Agent对空搜索结果的处理，是否会反复重试 |

---

### 4.7 P7：ReAct循环异常处理（E2E测试，通过send_chat触发，4个场景）

**测试目标**：验证Agent在异常输入（空输入、乱码、模糊意图、中英混合）下的处理能力。

| 编号 | 测试目标 | 用户输入 | 前置数据 | 预期行为 | 验证标准 | 能发现的问题 |
|------|---------|---------|---------|---------|---------|-------------|
| E2E-P7-01 | 空输入Agent的处理 | `"   "` | 后端+LLM可用 | Agent收到空消息，回复或finish | final事件存在；不崩溃；不死循环 | Agent对空输入的边界处理 |
| E2E-P7-02 | 乱码输入Agent的处理 | `"asdfghjkl12345!@#$%^&*"` | 后端+LLM可用 | Agent尝试理解，回复说明无法理解或尝试回答 | final事件存在；有回复内容；不死循环 | Agent对无法理解输入的处理 |
| E2E-P7-03 | 模糊意图Agent的处理 | `"帮我处理一下那个文件的事情"` | 后端+LLM可用 | Agent追问具体文件或尝试合理推断 | final事件存在；回复内容合理（追问或尝试）；不死循环 | Agent对模糊指令的理解和澄清能力 |
| E2E-P7-04 | 中英混合输入Agent的处理 | `"create一个新文件叫e2e_mixed.txt在E:\test_dir"` | 后端+LLM可用+E:\test_dir\可写；用例后删除e2e_mixed.txt | Agent理解中英混合指令并执行 | final事件存在；E:\test_dir\e2e_mixed.txt存在；不死循环 | Agent对中英混合输入的理解能力 |

---

### 4.8 P8：工具执行容错（E2E测试，通过send_chat触发，2个场景）

**测试目标**：验证工具在执行失败时（权限不足、命令不存在）Agent的容错能力。

| 编号 | 测试目标 | 用户输入 | 前置数据 | 预期行为 | 验证标准 | 能发现的问题 |
|------|---------|---------|---------|---------|---------|-------------|
| E2E-P8-01 | 读取权限受限文件的容错 | `"读取C:\Windows\System32\config\SAM"` | Windows系统，SAM文件存在但无权限 | Agent调用read_file，工具报权限错误，Agent回复告知用户 | final事件存在；回复包含"权限"/"拒绝"/"无法访问"；不死循环 | read_file工具对权限不足的异常处理 |
| E2E-P8-02 | 执行无效命令的容错 | `"执行命令 invalid_command_xyz_12345"` | 无 | Agent调用execute_shell_command，工具报错，Agent回复告知用户 | final事件存在；回复包含"找不到"/"不是内部或外部命令"；不死循环 | execute_shell_command工具对无效命令的异常处理 |

---


---

### 4.10 测试场景总计

| 级别 | 场景数 | 测试方法 | 覆盖范围 | 设计特点 |
|------|--------|---------|---------|---------|
| P0 核心链路 | 14 | E2E(send_chat) | API→Agent→LLM→DB→SSE完整链路 | 基础通路验证 |
| P1 FILE工具 | 5 | E2E(send_chat) | 文件操作15个工具 | 多工具串联 |
| P2 SHELL+NETWORK | 4 | E2E(send_chat) | 命令执行+网络请求9个工具 | 多工具串联 |
| P3 SYSTEM+DESKTOP | 5 | E2E(send_chat) | 系统+桌面19个工具 | 多工具串联 |
| P4 DOCUMENT+DATA | 5 | E2E(send_chat) | 文档+数据分析15个工具 | 多工具串联 |
| P5 复合多步 | 18 | E2E(send_chat) | 多工具串联+跨分类调用 | 跨分类复杂场景 |
| P6 异常容错 | 8 | E2E(send_chat) | 错误输入+不存在资源+边界条件 | 异常处理验证 |
| P7 ReAct异常 | 4 | E2E(send_chat) | 空输入/乱码/模糊意图/中英混合 | 边界条件 |
| P8 工具容错 | 4 | E2E(send_chat) | 权限不足/无效命令 | 异常处理 |
| **合计** | **65** | | | |

---

## 五、测试脚本编写规则

### 5.1 铁律：1个用例 = 1个函数

```
每个 E2E-XX-XX 编号对应一个 async def test_e2e_xx_xx_xxx()
每个函数只发一条用户指令
每个函数独立验证、独立清理
函数名 = test_ + 编号小写（如 test_e2e_p0_01_introduce_self）

为什么不能合并：
  · 合并后无法定位哪个场景失败
  · 合并后无法单独重跑失败的用例
  · 合并后一个场景的残留数据会影响下一个场景
```

### 5.2 验证分层

```
MUST层   → assert 硬断言   → 无死循环、DB有记录、文件存在
SHOULD层 → if...assert    → DB steps数偏差在2以内
MAY层    → if...print     → 工具名差异仅记录日志
```

### 5.3 测试记录写入（铁律）

```
每个测试用例跑完后，无论通过还是不通过，都必须自动生成测试记录文件。

触发位置：测试脚本的 finally 块中调用 write_test_record()
即使失败也要写：assert失败/异常退出时，记录文件必须写入（含失败原因）
文件位置：G:\OmniAgentAs-desk\notes\测试记录-{用例编号}-{日期}.md

脚本结构：
  passed = False; r = None; sid = None; ...
  try:
      # ... 测试逻辑 ...
      passed = True
  except Exception as e:
      passed = False; raise
  finally:
      write_test_record("用例编号","任务描述",u,r or {},db,ci,si,lc,passed,...)

注意事项：
  · 所有变量必须在try前初始化默认值（防止assert失败后未定义）
  · write_test_record必须在finally中调用（确保无论通过/失败都写入）
```

---

## 六、执行计划

### 6.1 前提条件与操作步骤

```powershell
# === 前置条件 ===
# 注意: E:\test_dir\ 和 config.yaml 都是测试用例用的测试数据，与后端运行时无关
# 数据库: 使用系统DB ~/.omniagent/chat_history.db（隔离尚未实现）

# 1. 杀死旧的后端进程（每次测试前必须执行）
netstat -ano | findstr :8000
# 记下 PID，然后:
taskkill /F /PID <PID>

# 2. 降低日志级别（避免控制台刷屏）
$env:LOG_LEVEL = "WARNING"

# 3. 启动后端（必须 --reload 模式，热重载避免修复Bug后反复手动重启）
cd G:\OmniAgentAs-desk\backend
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload --reload-dir app

# 4. 等待启动（约5秒），验证后端正常
Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/v1/health"
# 返回 {"status":"healthy",...} 即正常

# 5. 另一个终端运行测试
cd G:\OmniAgentAs-desk\backend
pytest e2emodel/test_e2e_p0_01_introduce_self.py -v --tb=short

# 6. 每次跑完一个case，手动检查日志和DB
#    日志: backend/logs/app_YYYY-MM-DD.log
#    DB:   ~/.omniagent/chat_history.db
```

### 6.2 测试数量与时间预估

> **注意**：测试的目的=发现问题。时间长不是问题，跑100天也应该。每个测试都值得认真观察系统行为。

### 6.3 标记与分批执行

所有测试使用 `@pytest.mark.e2e_full_link` 标记。

```bash
# 跑全部全链路测试（约4-5小时）
pytest -m e2e_full_link -v --tb=short

# 只跑P0（核心链路验证，~5分钟）
pytest -m e2e_full_link -k "p0" -v --tb=short

# 只跑P1-P2（快速单工具，FILE+SHELL+NETWORK，~20分钟）
pytest -m e2e_full_link -k "p1 or p2" -v --tb=short

# 只跑P5-P6（复合+异常，~60分钟）
pytest -m e2e_full_link -k "p5 or p6" -v --tb=short

# 只跑P7-P10（生命周期+API+容错，~30分钟）
pytest -m e2e_full_link -k "p7 or p8 or p9 or p10" -v --tb=short

# 只跑P3-P4（低优先级，SYSTEM+DESKTOP+DOCUMENT+META，~70分钟）
pytest -m e2e_full_link -k "p3 or p4" -v --tb=short

# 跑指定单个测试
pytest -m e2e_full_link -k "e2e_p0_01" -v --tb=short
```

---

## 七、测试报告格式

每个测试运行后输出（控制台`print_report`生成）：

```
=== E2E-P0-01: 核心链路验证 ===
  Status: PASSED
  Elapsed: 15.2s (15200ms)
  SSE events: 3 (start->thought->final)
  LLM calls: 1
  DB: [OK] session, [OK] is_valid, [OK] order, [OK] msgs, [WARN] steps, [OK] fields
  Consistency: [OK] (0 issues)
  Step reason: [OK] (0 issues)
  Logs: [OK] no-ERROR, [OK] no-TB, [OK] sess-records, [WARN] SSE-records
  Tools: []
  Conclusion: PASSED
```

**v1.8变更：`流结束方式`不再作为PASS/FAIL判定依据，改为记录具体结束方式。**
测试记录文件中的验证结果表新增"流结束"行，显示 final / error / 中断。`has_error`从MUST降为SHOULD WARN，不再阻断。回复中含错误关键词由`write_test_record`自动标FAIL。

所有测试跑完后输出汇总：

```
=== 全链路E2E测试汇总 ===
    总测试数: 98
    通过: X
    失败: X
    跳过: X
    
    ❌ 失败详情:
      [实际失败列表]
    
    ⚠️ 跳过详情:
      [实际跳过列表]
```

---

## 八、与其他测试的关系

```
所有测试体系
│
├── L1: Mock单元测试（~200个, ~10秒）
│   ├── 单工具函数测试（file/shell/network/system/desktop/document/meta）
│   ├── MessageBuilder测试（43个）
│   └── 运行时集成测试（14个）
│
├── L2: 全链路E2E集成测试（65个）← 本方案主测
│   ├── P0: 核心链路验证（4个）
│   ├── P1: FILE工具全覆盖（5个）
│   ├── P2: SHELL+NETWORK全覆盖（4个）
│   ├── P3: SYSTEM+DESKTOP全覆盖（5个）
│   ├── P4: DOCUMENT+DATA全覆盖（5个）
│   ├── P5: 复合多步任务（18个）
│   ├── P6: 异常容错场景（8个）
│   ├── P7: 任务控制API（5个）
│   ├── P8: 会话管理API（5个）
│   ├── P9: ReAct异常处理（4个）
│   └── P10: 工具执行容错（2个）
```

**L2覆盖三层验证**：
- **工具层**（P1-P4）：57个工具在真实LLM下能否被正确调用
- **流程层**（P5-P7）：多步推理、任务控制、系统串联是否完整
- **容错层**（P6/P9-P10）：异常输入、工具失败、服务中断时能否优雅处理

---

---

**更新时间**: 2026-06-25  
**版本**: v2.4  
**更新人**: 小欧
