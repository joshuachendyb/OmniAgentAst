一、策略选择的完整调用链
┌─────────────────────────────────────────────────────────────────────────┐
│ ReactAgentMixin._call_llm()                    [mixin L204-222, 19行]  │
│   策略在首次LLM调用时确定，后续直接用缓存(self._strategy)             │
└─────────────────────────────────────────────────────────────────────────┘

文件与行数统计：

文件	行数	职责
llm_adapter.py	68	探测FC支持→返回策略(tools/text)，首次探测后缓存

_init_llm_strategies()  → 创建adapter+策略对象, _strategy=None
_call_llm()
  ├─ _strategy is None? → adapter.detect_strategy() → 缓存到 _strategy（只一次）
  ├─ _inject_tools_hint(messages, _strategy)   text时注入工具描述，tools时不注入
  └─ _dispatch_strategy(_strategy, messages)
       ├─ "tools" → ToolsStrategy.call()
       └─ "text"  → TextStrategy.call()

调用链：
react_sse_wrapper.py:418  generate_sse_stream()
  │
  ├─ _run_agent_sse_stream()
  │    │
  │    ├─ AgentFactory.create(intent_type, llm_client, ...)
  │    │    └─ Agent.__init__()
  │    │         └─ _init_llm_strategies()          ← react_agent_mixin.py:50
  │    │              ├─ text_strategy = TextStrategy()
  │    │              ├─ adapter = LLMAdapter(api_base, api_key, model)
  │    │              ├─ tools_strategy = ToolsStrategy(tools=openai_tools)
  │    │              └─ _strategy = None (待首次调用时确定)
  │    │                  无adapter时直接 _strategy = "text"
  │    │
  │    └─ agent.run_stream()                        ← base_react.py:300
  │         └─ while True (ReAct循环)
  │              └─ _call_llm()                     ← react_agent_mixin.py:204
  │                   │
  │                   ├─ _strategy is None?           ← 首次调用时
  │                   │    └─ adapter.detect_strategy() ← llm_adapter.py:25
  │                   │         └─ _probe_tools()   ← llm_adapter.py:38
  │                   │              (HTTP POST + tools探测, 结果缓存)
  │                   │              → self._strategy = "tools" 或 "text"
  │                   │              后续轮次直接用 _strategy，不再探测
  │                   │
  │                   ├─ _inject_tools_hint(messages, _strategy)
  │                   │    text策略时注入工具描述，tools策略时不注入
  │                   │
  │                   └─ _dispatch_strategy(_strategy, msgs) ← react_agent_mixin.py:271
  │                        │
  │                        ├─ "tools" → ToolsStrategy.call()  ← llm_strategies.py:403
  │                        │    └─ chat_with_tools("", msgs, tools) ← llm_core.py:544
  │                        │         └─ _post_with_retry(POST /chat/completions)
  │                        │            (非流式, 带 tools+tool_choice)
  │                        │
  │                        └─ "text" → TextStrategy.call()   ← llm_strategies.py:65
  │                             └─ llm_client("", history=msgs) ← llm_core.py:180
  │                                  └─ __call__ → chat → chat_stream
  │                                       └─ _stream_with_retry(POST /chat/completions)
  │                                          (流式, 无tools)


二、消息构建流程分析

核心链路（_call_llm，mixin L204-222）：

_call_llm()
  |
  1. mb.prepare_messages_for_llm()     [message_builder L126-135, 10行]
  |    合并原split+merge+assemble，不再拆出last_message再拼回
  |    conversation_history + temp_history → List[Dict] 贯穿整个流程
  |
  2. _strategy is None?                ← 首次确定策略，后续直接用
  |    adapter.detect_strategy() → "tools" 或 "text"
  |
  3. _inject_tools_hint(messages, _strategy) [mixin L224-241, 18行]
  |    text策略时注入工具描述文本，tools策略时不注入
  |
  4. mb.inject_executed_summary()       [message_builder L160-170, 11行]
  |    注入已执行工具汇总（防重复调用）
  |
  5. _inject_schema(messages)           [mixin L242-248, 7行]
  |    仅text策略时注入Schema文本
  |
  6. _dispatch_strategy(_strategy, messages) [mixin L271-285, 15行]
       分派到 TextStrategy/ToolsStrategy.call()
       内部：messages(List[Dict]) → llm_client.chat/chat_with_tools


三、Observation 注入流程
完整路径（base_react.py L805-850）：


工具执行完成，execution_result 拿到后：

1. MessageBuilder.build_observation_text(execution_result)     [message_builder L113-120, 8行]
   |  委托给 _format_llm_observation(execution_result)          [tool_result_formatter L78-111, 34行]
   |  输出格式： "Observation: success/error - {message}\n数据: {json}"
   |  优先使用 llm_data 字段，fallback 到 data 字段
   |  追加 next_actions 推荐操作文本
   |
2. 附加内容质量检测（write_file专属）                         [base_react L810-821]
   |  _check_write_content_quality() -> quality_warning
   |  附加写入内容摘要
   |
3. 缓存命中前缀拼接                                          [base_react L833-835]
   |  observation_prefix + observation_text
   |
4. 更新缓存+失败计数                                         [base_react L837-840]
   |  message_builder.update_execution_cache()
   |  返回 fail_warning，拼接到 observation_text
   |
5. FC协议判断                                                 [base_react L843-849]
   |  if self._strategy == "tools" 且 llm_client._last_chat_response.tool_calls 存在:
   |    构建 fc_context = {"tool_calls": [...], "tool_call_id": "call_xxx"}
   |  否则 fc_context = None
   |
6. message_builder.add_observation(observation_text, llm_call_count, fc_context=fc_context)
   |                                                           [message_builder L75-94, 20行]
   |  a. _get_observation_budget(llm_call_count)   计算截断预算(20K~50K)
   |  b. _smart_truncate()                         如果超预算，头60%+尾40%截断
   |  c. _normalize_observation_prefix()           确保[Observation]前缀
   |  d. if fc_context:                            FC协议注入
   |       conversation_history.append({"role":"assistant", "content":None, "tool_calls":[...]})
   |       conversation_history.append({"role":"tool", "content":text, "tool_call_id":"call_xxx"})
   |     else:                                     文本模式注入
   |       conversation_history.append({"role":"system", "content":text})
   |  e. trim_history()                            容量感知裁剪
   |     → _dedup_by_fingerprint()                 去重（FC消息跳过去重，防配对断裂）
   |     → _trim_fc_pairs()                        FC配对裁剪（assistant每个tool_call.id必须有对应role:tool）
