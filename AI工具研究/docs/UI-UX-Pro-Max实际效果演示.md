# UI UX Pro Max 实际效果演示

**创建时间**: 2026-02-05 01:23:52

---

## 一、演示概述

本文档展示UI UX Pro Max Skill的实际输出效果，帮助你了解这个工具能为你做什么。

---

## 二、行业设计系统演示

### 2.1 Beauty Spa Website（美容SPA网站）

```
======================================================================
TARGET: Beauty Spa Website - DESIGN SYSTEM
======================================================================

PATTERN: Soft Wellness

STYLE: Minimalism
  Keywords: 

COLORS:
  Primary:    #F472B6        (粉色)
  Secondary:  #EC4899        (深粉色)
  Background: #FDF2F8        (极浅粉)
  Text:       #831843        (深玫红)
  Accent:     #D4AF37        (金色)

TYPOGRAPHY:
  Heading: Inter
  Body:    Open Sans
  Google Fonts: Inter|Open Sans

KEY EFFECTS:
  gentle shadows

AVOID:
  harsh lines

======================================================================
```

**分析**：
- 配色采用粉色系，符合美容、SPA的女性化、柔和风格
- 金色点缀增添高级感
- 效果要求gentle shadows（柔和阴影）
- 避免harsh lines（硬朗线条）

---

### 2.2 Healthcare App（医疗健康App）

```
======================================================================
TARGET: Healthcare App - DESIGN SYSTEM
======================================================================

PATTERN: Trust-Focused

STYLE: Accessible & Ethical
  Keywords: education

COLORS:
  Primary:    #10B981        (翠绿色)
  Secondary:  #059669        (深绿色)
  Background: #ECFDF5        (极浅绿)
  Text:       #064E3B        (深绿黑)
  Accent:     #F59E0B        (琥珀色)

TYPOGRAPHY:
  Heading: Inter
  Body:    Open Sans
  Google Fonts: Inter|Open Sans

KEY EFFECTS:
  calm colors

AVOID:
  aggressive CTAs

======================================================================
```

**分析**：
- 绿色系传达健康、信任、生命力
- Pattern选择Trust-Focused（信任导向）
- 避免aggressive CTAs（激进号召）
- calm colors（平静的配色）

---

### 2.3 Fintech Banking App（金融银行App）

```
======================================================================
TARGET: Banking App - DESIGN SYSTEM
======================================================================

PATTERN: Executive Dashboard

STYLE: Minimalism
  Keywords: 

COLORS:
  Primary:    #0F766E        (深青色)
  Secondary:  #115E59        (更深青色)
  Background: #F0FDF4        (极浅青)
  Text:       #134E4A        (深青黑)
  Accent:     #F59E0B        (琥珀色)

TYPOGRAPHY:
  Heading: IBM Plex Sans
  Body:    IBM Plex Sans
  Google Fonts: IBM Plex Sans

KEY EFFECTS:
  real-time updates

AVOID:
  cluttered UI

======================================================================
```

**分析**：
- 青色系传达专业、可信、稳定
- Pattern是Executive Dashboard（高管仪表板）
- 强调real-time updates（实时更新）
- 避免cluttered UI（杂乱的界面）

---

## 三、组件代码演示

### 3.1 React Button Component

```tsx
// React Button Component
// Generated by UI UX Pro Max

import React from 'react';

interface ButtonProps {
  children: React.ReactNode;
  variant?: 'primary' | 'secondary' | 'danger';
  onClick?: () => void;
  disabled?: boolean;
}

export const Button = ({ 
  children, 
  variant = 'primary', 
  onClick,
  disabled = false 
}: ButtonProps) => {
  const variants = {
    primary: 'bg-blue-600 hover:bg-blue-700 text-white rounded-lg px-4 py-2',
    secondary: 'bg-gray-200 hover:bg-gray-300 text-gray-800 rounded-lg px-4 py-2',
    danger: 'bg-red-600 hover:bg-red-700 text-white rounded-lg px-4 py-2',
  };

  return (
    <button
      className={`
        font-medium transition-colors duration-200
        ${variants[variant]}
        ${disabled ? 'opacity-50 cursor-not-allowed' : ''}
      `}
      onClick={onClick}
      disabled={disabled}
    >
      {children}
    </button>
  );
};

export default Button;
```

**特点**：
- TypeScript类型定义完整
- 支持三种变体（primary/secondary/danger）
- 禁用状态自动处理
- Tailwind CSS样式

---

### 3.2 Vue Card Component

```vue
<!-- Vue Card Component -->
<!-- Generated by UI UX Pro Max -->

<template>
  <div class="card" :class="{ 'dark-mode': isDark }">
    <div class="card-header">
      <slot name="header" />
    </div>
    <div class="card-body">
      <slot />
    </div>
    <div class="card-footer">
      <slot name="footer" />
    </div>
  </div>
</template>

<script setup lang="ts">
interface Props {
  isDark?: boolean;
  variant?: 'default' | 'glass' | 'outlined';
}

const props = withDefaults(defineProps<Props>(), {
  isDark: false,
  variant: 'default'
});
</script>

<style scoped>
.card {
  @apply bg-white rounded-xl p-6 shadow-lg;
  transition: all 0.3s ease;
}

.card:hover {
  @apply transform -translate-y-1;
}

.card.glass {
  @apply bg-white/20 backdrop-blur-lg;
}

.card.outlined {
  @apply bg-transparent border border-gray-200 shadow-none;
}

.card.dark-mode {
  @apply bg-gray-800 text-white;
}
</style>
```

**特点**：
- Vue 3 Composition API
- TypeScript支持
- 三种变体（default/glass/outlined）
- 深色模式支持
- Scoped CSS

---

### 3.3 Flutter Button Widget

```dart
// Flutter Button Widget
// Generated by UI UX Pro Max

import 'package:flutter/material.dart';

enum ButtonVariant { primary, secondary, danger }

class CustomButton extends StatelessWidget {
  final String text;
  final VoidCallback onPressed;
  final ButtonVariant variant;
  final bool isLoading;

  const CustomButton({
    super.key,
    required this.text,
    required this.onPressed,
    this.variant = ButtonVariant.primary,
    this.isLoading = false,
  });

  Color _getBackgroundColor(BuildContext context) {
    final colorScheme = Theme.of(context).colorScheme;
    switch (variant) {
      case ButtonVariant.primary:
        return colorScheme.primary;
      case ButtonVariant.secondary:
        return colorScheme.secondary;
      case ButtonVariant.danger:
        return Colors.red;
    }
  }

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: isLoading ? null : onPressed,
      style: ElevatedButton.styleFrom(
        backgroundColor: _getBackgroundColor(context),
        foregroundColor: _getForegroundColor(context),
        padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 12),
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(8),
        ),
        elevation: 2,
      ),
      child: isLoading
          ? const SizedBox(
              width: 20,
              height: 20,
              child: CircularProgressIndicator(
                strokeWidth: 2,
                color: Colors.white,
              ),
            )
          : Text(
              text,
              style: const TextStyle(
                fontWeight: FontWeight.medium,
                fontSize: 16,
              ),
            ),
    );
  }
}
```

**特点**：
- Flutter原生Dart
- Material Design集成
- 加载状态支持
- 主题适配
- 枚举类型安全

---

## 四、使用场景示例

### 场景1：创业公司官网

**需求**：
```
"创建一个SaaS创业公司的营销落地页"
```

**输出**：
- Pattern: Conversion-Optimized
- Style: Minimalism & Swiss Style
- Colors: Modern SaaS（蓝色系）
- Typography: Modern Sans
- Key Effects: smooth transitions

---

### 场景2：移动健康App

**需求**：
"设计一个健康管理的移动App界面"
```

**输出**：
- Pattern: Trust-Focused
- Style: Accessible & Ethical
- Colors: Healthcare（绿色系）
- Typography: Clean & Readable
- Key Effects: calm colors

---

### 场景3：电商产品展示

**需求**：
"创建一个电商网站的产品展示卡片"
```

**输出**：
- Pattern: Product Showcase
- Style: Minimal & Direct
- Colors: Modern SaaS或E-commerce
- Typography: Creative
- Key Effects: large images

---

### 场景4：游戏平台

**需求**：
"设计一个游戏交易平台的界面"
```

**输出**：
- Pattern: Immersive
- Style: Cyberpunk UI
- Colors: Gaming（紫色系）
- Typography: Tech Modern
- Key Effects: neon effects

---

## 五、总结

### 5.1 UI UX Pro Max 能做什么

| 功能 | 说明 |
|-----|------|
| **行业设计系统** | 根据行业自动生成完整设计规范 |
| **配色方案** | 行业特定的配色板 |
| **字体搭配** | Google Fonts最佳搭配 |
| **组件代码** | React/Vue/Flutter多框架代码 |
| **最佳实践** | 效果建议和反模式警告 |

### 5.2 使用方式

**在OpenCode中直接说**：
```
"创建一个红色的登录页面"
"用Glassmorphism设计卡片"
"为医疗App设计界面"
"用Flutter做个按钮"
```

### 5.3 文件位置

| 项目 | 位置 |
|-----|------|
| **全局Skill** | `%APPDATA%\.opencode\skills\ui-ux-pro-max\` |
| **CLI工具** | `uipro-cli` (npm全局) |
| **安装文档** | `D:\2bktest\MDview\docs\UI-UX-Pro-Max-Skill安装使用指南.md` |

---

**文档更新时间**: 2026-02-05 01:23:52
**文档类型**: 效果演示
**版本**: v1.0