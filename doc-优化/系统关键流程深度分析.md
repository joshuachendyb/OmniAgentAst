# 系统关键流程深度分析

**创建时间**: 2026-06-08 15:19:13  
**更新时间**: 2026-06-27 10:00:00  
**作者**: 小欧  
**版本**: v5.0  
**存放位置**: `G:\OmniAgentAs-desk\doc-优化\`

---

## 版本历史

| 版本 | 时间 | 签名 | 更新内容 |
|------|------|------|---------|
| v1.0 | 2026-06-08 15:19:13 | 小欧 | 初始版本，梳理10大关键流程 |
| v2.0 | 2026-06-25 13:14:51 | 小欧 | 全面重写：更新12大关键流程，新增测试要点、测试方法、风险矩阵 |
| v3.0 | 2026-06-25 17:10:00 | 小欧 | 修正流程9（删除强制保留机制、budget公式配平user）、行号对齐(流程3/4)、重定义F9-05为budget计算验证、总数89项 |
| v4.0 | 2026-06-25 22:30:00 | 小健 | 修正11处文档错误（文件路径6处、函数名2处、逻辑描述3处）、补充10个缺失测试要点、修正SSE type分类、更新风险矩阵、总数99项 |
| v5.0 | 2026-06-27 10:00:00 | 小欧 | 修正所有行号（7处）、修正文件路径（使用完整相对路径）、补充3个缺失流程（消息构建、工具注册、步骤发射）、补充4个缺失测试要点、修正风险矩阵统计、总数118项 |

---

## 一、概述

OmniAgentAs-desk 是全栈（React+FastAPI）AI Agent 桌面应用。核心模式是 **用户输入 → 意图检测 → Agent工厂 → ReAct循环 → SSE流式返回**。

本文深度梳理系统中 15 个关键流程，从入口到出口追踪完整数据流，并为每个流程定义测试要点和测试方法。

---

## 二、15 大关键流程

### 流程1：HTTP请求入口与SSE流式初始化

**文件链**: `app/api/v1/chat/chat_openai.py:43` → `chat_stream()` → `run_sse_stream()`

**当前状态**: `chat_router.py` 已合并入 `chat_openai.py`（2026-06-23 北京老陈）

```
Frontend fetch POST /api/v1/chat/stream
  │
  ▼
chat_stream_endpoint(request: ChatRequest)          ← chat_openai.py:43
  │
  ▼
chat_stream(request)                                 ← chat_openai.py:87
  ├── 1. user_input = request.messages[-1].content
  ├── 2. session_id = request.session_id or uuid4()
  ├── 3. task_id = uuid4()
  ├── 4. _current_task_id.set(task_id)               ← ContextVar设置
  ├── 5. next_step = create_step_counter()
  ├── 6. register_task(task_id, ai_service)           ← task_registry.py:41
  ├── 7. task_interrupt_check(task_id)                ← 中断检查
  ├── 8. step_start()                                ← 发出start SSE事件
  ├── 9. run_sse_stream(...)                         ← Agent执行核心
  │       │
  │       ├── _cancel_poller()                       ← asyncio.create_task
  │       │     └── 每秒检查check_cancelled()
  │       │
  │       └── async for sse_chunk in sse_stream:
  │             ├── task_pause_check_and_yield()     ← 暂停检查
  │             └── task_cancel_check_and_yield()    ← 取消检查
  │
  └── finally: task_cleanup(task_id, llm_call_count)
```

**关键设计**:
- task 生命周期在 `chat_stream` 的 `generate()` 内管理，finally 保证 cleanup
- `_cancel_poller` 是独立 asyncio task，每秒轮询取消状态
- cancel 检测到后调用 `sse_stream.aclose()` 关闭生成器

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F1-01 | 空消息列表返回错误 | P1 | 发送 `messages=[]` 请求 |
| F1-02 | task_id 正确设置到 ContextVar | P1 | Mock `_current_task_id` 验证 set 调用 |
| F1-03 | register_task 被调用 | P1 | Mock register_task 验证调用参数 |
| F1-04 | 中断检查拦截已取消任务 | P1 | Mock task_interrupt_check 返回 True |
| F1-05 | step_start 发出 start 事件 | P1 | Mock step_start 验证 yield |
| F1-06 | cancel_poller 检测到取消后关闭流 | P0 | 设置 cancelled=True，验证 aclose() 被调用 |
| F1-07 | finally 中 task_cleanup 必定执行 | P0 | 抛异常后验证 cleanup 被调用 |
| F1-08 | 异常时 yield router_error SSE | P1 | Mock generate 内部抛异常 |

---

### 流程2：Agent生命周期与状态管理

**文件链**: `app/services/agent/core_agent/base_agent.py:32` → `BaseAgent.__init__` → `UniversalAgent.__init__`

**当前状态**: 所有 intent 共用 `UniversalAgent`（2026-06-07 重构，删除 DesktopAgent 等子类）；AgentInitializer 类已删除，逻辑内联到 BaseAgent.__init__

```
UniversalAgent.__init__(llm_client, task_id, max_steps, initial_categories)
   │
   ├── BaseAgent.__init__()                                    ← base_agent.py:32
   │     ├── # 原 AgentInitializer._init_llm                  ← LLM客户端赋值
   │     ├── # 原 AgentInitializer._init_state                 ← 状态初始化
   │     │     └── self.llm_call_count = 0
   │     │     └── self.task_id = task_id
   │     ├── # 原 AgentInitializer._init_messages              ← MessageBuilder
   │     │     └── self.message_builder = MessageBuilder()
   │     ├── ToolLoader(self).init_tools(initial_categories)   ← 工具加载（按category，无META_TOOL_NAMES）
   │     │     └── 加载 initial_categories 指定分类的工具
   │     ├── ToolRetryEngine(self._tools_dict)                 ← 重试引擎
   │     ├── # 原 AgentInitializer._init_task_tracking         ← 任务追踪
   │     └── StepEmitter(self)                                 ← 步骤发射器
   │
   ├── self._loaded_categories = set(initial_categories)       ← 已注入分类
   │     └── 初始: {FUNDAMENTAL, SHELL, FILE}
   │
   ├── self._tool_cache = TTLCache(ttl=300)                   ← 工具缓存
   └── self._patch_search_desc()                               ← 动态更新tool_search描述
```

**AgentStatus 枚举** (`types/agent_status.py`):
- `IDLE` → `THINKING` → `EXECUTING` → `COMPLETED` / `FAILED` / `RETRYABLE_ERROR` / `CANCELLED`

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F2-01 | task_id 为空时抛 ValueError | P1 | 构造 UniversalAgent(task_id="") |
| F2-02 | 初始 categories 包含 3 个分类 | P1 | 验证 `_loaded_categories == {FUNDAMENTAL, SHELL, FILE}` |
| F2-03 | fundamental 工具（tool_search）被加载 | P1 | 验证 `_tools_dict` 包含 tool_search |
| F2-04 | Agent 状态流转正确 | P1 | Mock 运行，验证 IDLE→THINKING→EXECUTING→COMPLETED |
| F2-05 | MessageBuilder 每次 run 重置 | P1 | 调用 reset_per_run 后验证 history 为空 |
| F2-06 | ToolRetryEngine 绑定当前工具字典 | P1 | 验证 `_retry_engine._tools == _tools_dict` |
| F2-07 | patch_search_desc 动态更新描述 | P1 | 加载新分类后验证 tool_search 描述变化 |

---

### 流程3：ReAct循环（核心推理引擎）

**文件链**: `app/services/agent/core_agent/react_cycle.py:246` → `run_react_cycle()` → `_process_single_step()` (react_cycle.py:112) → handlers

**当前状态**: FC模式 + FC降级fallback（call_llm_with_fallback），tool_calls 原生消费（无 JSON roundtrip）

```
run_react_cycle(agent, task, context, max_steps, task_id)
   │
   ├── initialize_run_state(agent, task, task_id, context)
   │     ├── agent.steps = []
   │     ├── agent.message_builder.reset_per_run()
   │     ├── agent.status = AgentStatus.THINKING
   │     ├── agent.llm_call_count = 0
   │     ├── _inject_conversation_history(agent, context)    ← 多轮对话
   │     └── return ChunkBuffer(MAX_CONSECUTIVE_CHUNKS)
   │
   ├── agent.status = AgentStatus.EXECUTING
   │
   ├── while agent.llm_call_count < max_steps:
   │     │
   │     └── async for event in _process_single_step(agent, chunk_buffer):
   │           │
   │           ├── 1. agent.llm_call_count += 1
   │           ├── 2. agent.message_builder.trim_history()   ← 在_process_single_step内
   │           ├── 3. messages = prepare_messages_for_llm()  ← 在_process_single_step内
   │           ├── 4. openai_tools = get_openai_tools(agent)  ← 在_process_single_step内
   │           │
   │           ├── 5. call_llm_with_fallback(agent, messages, openai_tools)  ← llm_stream.py:153
   │           │     ├── call_llm_stream(agent, messages, openai_tools)       ← llm_stream.py:76
   │           │     │     ├── tool_choice = "auto" if openai_tools else None
   │           │     │     ├── async for chunk in request_stream(...):
   │           │     │     │     ├── chunk.tool_calls → 累积到 tool_calls_result
   │           │     │     │     ├── chunk.content → is_reasoning? reasoning : content
   │           │     │     │     ├── chunk.is_done → break + 收集usage
   │           │     │     │     └── chunk.stream_error → break
   │           │     │     ├── stream_error → 丢弃tool_calls_result, yield error
   │           │     │     ├── tool_calls_result → yield ("response", {type: "action"})
   │           │     │     └── 否则 → yield ("response", {type: "answer"})
   │           │     │
   │           │     └── FC降级: FC_MAX_RETRIES次重试 → FC_FALLBACK_ENABLED→Text模式
   │           │
   │           ├── 6. 检查 llm_response 是否有效
   │           ├── 7. 检查 _cancelled 状态
   │           ├── 8. 检查 _should_retry_truncated_tool()     ← 截断检测
   │           │
   │           └── 9. _dispatch_handler(agent, llm_response, chunk_buffer)
   │                 ├── "action" → handle_action()
   │                 └── "answer" → handle_answer()
   │
   ├── 检查 chunk_buffer.should_force_stop()                ← 累积超时
   │
   └── finally:
         ├── _ensure_failed_final_step()                   ← FAILED/RETRYABLE_ERROR补发FinalStep
         └── _finalize_cycle()                             ← 状态回调+任务追踪
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F3-01 | max_steps 兜底防死循环 | P0 | 设置 max_steps=3，验证不超过3轮 |
| F3-02 | LLM 返回空响应时处理 | P1 | Mock call_llm_with_fallback 返回 None |
| F3-03 | _cancelled 检测中断循环 | P1 | 设置 `_cancelled=True`，验证 yield interrupted |
| F3-04 | 截断检测触发重试 | P1 | Mock LLM 返回短 answer + 历史有未完成 tool_calls |
| F3-05 | action 分派到 handle_action | P1 | Mock LLM 返回 `{"type": "action", ...}` |
| F3-06 | answer 分派到 handle_answer | P1 | Mock LLM 返回 `{"type": "answer", ...}` |
| F3-07 | FAILED 时补发 FinalStep | P1 | Mock agent.status=FAILED，验证 FinalStep 被 yield |
| F3-08 | chunk_buffer 累积超时强制停止 | P1 | Mock should_force_stop 返回 True |
| F3-09 | finally 中 _finalize_cycle 执行 | P1 | 正常结束验证 _on_after_loop 被调用 |
| F3-10 | FC降级fallback触发 | P0 | Mock call_llm_stream 抛 FCFormatError，验证降级到Text模式 |
| F3-11 | RETRYABLE_ERROR状态转换正确 | P0 | Mock FCFormatError，验证 agent.status==RETRYABLE_ERROR |

---

### 流程4：工具执行管线（注册→安全→执行→结果）

**文件链**: `app/services/agent/core_agent/handlers/action_handler.py:366` → `handle_action()` → `app/services/agent/tool_executor.py:14` → `app/services/agent/tool_retry_engine.py:83`

**当前状态**: 三层架构：安全检查 → 工具执行 → 结果构建

```
handle_action(agent, parsed, chunk_buffer)              ← action_handler.py:366
   │
   ├── _build_call_list(parsed)                         ← action_handler.py:313 构建调用列表
   │     ├── tool_name, tool_params, fc_context
   │     ├── pending_calls（并行工具）
   │     └── all_calls = [主调用] + pending_calls
   │
   ├── yield ThoughtStep                               ← 思考步骤
   │
   ├── check_safety_and_confirm(agent, all_calls, step) ← action_handler.py:47
   │     ├── ToolSafetyChecker.check_before_execute()  ← tool_safety_checker.py:46 返回SafetyResult dataclass
   │     │     ├── _is_skip_safety()                    ← 模块级函数，security.enabled检查
   │     │     ├── tool_registry.get_tool()             ← 工具注册检查
   │     │     ├── _check_known_risks()                 ← 路径越权/写入保护/代码注入
   │     │     ├── _get_needs_confirmation()            ← 确认策略
   │     │     └── tool_meta.check_fn()                 ← 自定义检查
   │     │
   │     ├── blocked → ErrorStep + FAILED
   │     └── requires_confirmation → MetaStep + wait_for_confirmation_result
   │
   ├── execute_tools(agent, all_calls, is_parallel, ...)
   │     ├── 并行: asyncio.gather(*tasks, return_exceptions=True)，失败重试一次
   │     └── 串行: execute_tool(agent, tool_name, tool_params)
   │           └── ToolRetryEngine.execute_tool_with_retry()
   │                 ├── _validate_params()             ← 参数验证
   │                 └── _execute_with_retry()          ← 带重试执行
   │
   ├── _log_tool_results(results, ...)                 ← prompt_logger + task_tracker
   │
   ├── build_observation(ctx)                           ← 构建observation
   │     ├── ActionStep（每个工具）
   │     ├── build_observation_text()                   ← observation文本
   │     ├── message_builder.add_observation()          ← 加入对话历史
   │     └── ObservationStep（合并）
   │
   └── return_direct → FinalStep（⚠️仅检查results[0]，并行场景可能遗漏）
```

**⚠️ 已知问题**: `chunk_buffer` 参数在 handle_action 签名中存在但从未使用（遗留参数）

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F4-01 | blocked 工具拒绝执行 | P0 | Mock safety 返回 blocked=True |
| F4-02 | 需确认工具等待用户确认 | P1 | Mock safety 返回 requires_confirmation=True |
| F4-03 | 用户拒绝后 FAILED | P1 | Mock confirmation 返回 confirmed=False |
| F4-04 | 并行工具 asyncio.gather | P1 | 传入 _pending_calls，验证并行执行 |
| F4-05 | 工具异常时 build_observation 处理 | P1 | Mock execute_tool 抛异常 |
| F4-06 | return_direct 触发 FinalStep | P1 | Mock result.other_data.return_direct=True |
| F4-07 | 参数验证拒绝非法参数 | P1 | 传入 schema 外的参数 |
| F4-08 | 参数验证拒绝缺失必需参数 | P1 | 传入缺少 required 字段的参数 |
| F4-09 | 重试引擎在可重试错误时重试 | P1 | Mock 工具抛可重试异常 |
| F4-10 | observation 文本被截断 | P1 | 传入超长 result，验证截断 |
| F4-11 | 并行工具return_direct仅检查results[0] | P0 | 并行场景下第二个工具return_direct=True被忽略 |
| F4-12 | chunk_buffer参数未使用 | P1 | 验证handle_action中chunk_buffer从未被引用 |

---

### 流程5：文件安全（备份→执行→记录→回滚）

**文件链**: `app/services/safety/tool_safety_checker.py:46` → `app/services/safety/path_validator.py:75` → `app/services/safety/file_safety/`

**当前状态**: 安全检查器二元模式（safe/dangerous），返回 SafetyResult dataclass；路径校验统一在 `validate_path()`

```
ToolSafetyChecker.check_before_execute(tool_name, params) → SafetyResult
   │
   ├── _is_skip_safety()                                ← 模块级函数（非类方法），security.enabled=false时跳过
   │
   ├── tool_registry.get_tool(tool_name)                ← 工具注册检查
   │     └── 未注册 → blocked
   │
   ├── _check_known_risks(tool_name, params)
   │     │
   │     ├── 文件工具 → validate_path(path)             ← path_validator.py:75
   │     │     ├── ALLOWED_PATHS（path_validator.py:38，Windows: A-J盘符 + home）
   │     │     ├── .. 检测 → rejected
   │     │     └── 相对路径检查
   │     │
   │     ├── write_text_file → 写入大小保护
   │     │     └── 新内容 < 原内容20% → blocked
   │     │
   │     └── shell工具 → DANGEROUS_PATTERNS 检测
   │           └── 仅检查 _CODE_INJECTION_RISK_TOOLS & shell_tools 交集
   │
   ├── tool_meta.check_fn(params)                       ← 自定义检查函数
   │
   └── _get_needs_confirmation(tool_meta, params)
         ├── action_confirmation 优先
         └── tool_meta.needs_confirmation 兜底
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F5-01 | security.enabled=false 跳过检查 | P1 | Mock config 返回 security.enabled=false |
| F5-02 | 未注册工具被 blocked | P1 | 调用不存在的工具名 |
| F5-03 | 路径越权（..）被 blocked | P0 | 传入 `path="../../etc/passwd"` |
| F5-04 | 合法路径通过检查 | P1 | 传入 `path="E:/test/file.txt"` |
| F5-05 | 写入大小保护触发 | P1 | 传入新内容远小于原内容 |
| F5-06 | 代码注入检测触发 | P1 | 传入包含 DANGEROUS_PATTERNS 的 command |
| F5-07 | check_fn 异常时 blocked | P1 | Mock check_fn 抛异常 |
| F5-08 | action_confirmation 覆盖默认策略 | P1 | 传入 action 参数，验证使用 action_confirmation |
| F5-09 | 空路径被拒绝 | P0 | 传入 `path=""`，验证被 blocked |
| F5-10 | 路径带空格在 Windows 正确处理 | P0 | 传入 `path="E:/my folder/file.txt"` |

---

### 流程6：SSE事件流（后端→前端）

**文件链**: `app/services/react_sse_wrapper/run_sse_stream.py:91` → `format_agent_sse()` → 前端

**当前状态**: 直接 `to_dict()` 不经 SSE 字符串往返（2026-06-08 P1修复）

```
run_sse_stream(llm_client, task_id, ...)
   │
   ├── agent = UniversalAgent(llm_client, task_id)
   ├── llm_client.set_stop_check(_stop_check)          ← 注入停止回调（check_cancelled + check_paused）
   ├── context["previous_messages"] = _load_previous_messages(session_id)
   │     └── _load_previous_messages 已拆分为3个函数（_parse_tool_calls, _parse_observations, 主函数）
   │
   └── async for event in agent.run_react_cycle(task, context, task_id):
         │
         ├── event_dict = event if isinstance(event, dict) else event.to_dict()
         │
         ├── current_execution_steps.append(event_dict) ← 累积步骤
         ├── prompt_logger.log_step_yield()             ← 日志记录
         │
         ├── event_type == "final" → stream_state.current_content = content
         ├── event_type == "chunk" → stream_state.current_content += chunk
         │
         └── sse_data = format_agent_sse(event_dict)   ← 格式化SSE
               yield sse_data
```

**异常路径**:

```
CancelledError:
   ├── yield MetaStep(type="interrupted")
   ├── yield FinalStep(response="任务已被中断")          ← R3-3修复：补发FinalStep
   └── agent.status = CANCELLED

Exception:
   ├── yield ErrorStep(error_type="agent_operation_error")
   ├── yield FinalStep(response="执行异常: ...")        ← 小健2026-06-19修复：补发FinalStep
   └── agent.status = FAILED

finally:
   ├── save_execution_steps_to_db(session_id, steps, content)
   │     └── 最多重试2次
   └── stream_state.llm_call_count = agent.llm_call_count
```

**SSE type 分类**:

| Type | 含义 | Step类 | 类型 |
|------|------|--------|------|
| `thought` | LLM思考过程 | ThoughtStep | 固定 |
| `chunk` | AI流式回复片段 | ChunkStep | 固定 |
| `action_tool` | 工具调用 | ActionStep | 固定 |
| `observation` | 工具执行结果 | ObservationStep | 固定 |
| `final` | 最终回答 | FinalStep | 固定 |
| `error` | 错误信息 | ErrorStep | 固定 |
| `start` | 开始，含模型/安全信息 | MetaStep(type="start") | 动态 |
| `interrupted` | 中断 | MetaStep(type="interrupted") | 动态 |
| `paused`/`resumed` | 暂停/恢复 | MetaStep(type=...) | 动态 |
| `retrying` | 重试 | MetaStep(type=...) | 动态 |
| `authorization_required` | HITL确认 | MetaStep(type=...) | 动态 |

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F6-01 | 正常流程产出 start→...→final | P1 | Mock agent.run_react_cycle yield 完整步骤 |
| F6-02 | CancelledError 产出 interrupted+final | P1 | Mock run_react_cycle 抛 CancelledError |
| F6-03 | Exception 产出 error+final | P1 | Mock run_react_cycle 抛 Exception |
| F6-04 | finally 中 DB 保存必定执行 | P0 | 验证 save_execution_steps_to_db 被调用 |
| F6-05 | DB 保存失败不影响 SSE 流 | P1 | Mock save 抛异常，验证 SSE 仍正常 |
| F6-06 | DB 保存重试机制（最多2次） | P1 | Mock save 第1次失败第2次成功 |
| F6-07 | stream_state.current_content 正确更新 | P1 | Mock chunk+final 事件，验证 content 累积 |
| F6-08 | format_agent_sse 返回有效 SSE 格式 | P1 | 验证返回值包含 "data:" 前缀 |
| F6-09 | previous_messages 正确加载 | P1 | Mock _load_previous_messages 返回历史 |
| F6-10 | _stop_check 注入到 llm_client | P1 | 验证 llm_client.set_stop_check 被调用 |
| F6-11 | Exception后补发FinalStep | P0 | Mock run_react_cycle 抛 Exception，验证 yield ErrorStep + FinalStep |
| F6-12 | CancelledError后补发FinalStep | P0 | Mock run_react_cycle 抛 CancelledError，验证 yield MetaStep + FinalStep |

---

### 流程7：上下文变量传播

**文件链**: `app/services/context_vars.py` → `_current_task_id` → 工具执行链路

**当前状态**: 单一 ContextVar `_current_task_id`（内部名 `"tool_task_id"`），用于工具执行链路追踪

```
chat_stream()
  ├── _current_task_id.set(task_id)                   ← 设置
  │
  └── run_sse_stream()
        └── agent.run_react_cycle()
              └── _execute_tool()
                    └── 工具函数读取 _current_task_id.get()
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F7-01 | ContextVar 在不同协程隔离 | P1 | 两个协程各自 set 不同值，验证互不影响 |
| F7-02 | 工具执行时 task_id 可读 | P1 | Mock 工具函数读取 _current_task_id.get() |
| F7-03 | 未设置时默认为 None | P1 | 验证 _current_task_id.get() is None |
| F7-04 | 并发任务 task_id 不混淆 | P0 | 两个并发请求，验证工具读到正确的 task_id |

---

### 流程8：LLM客户端与流式通信

**文件链**: `_process_single_step` (react_cycle.py:112) → `call_llm_with_fallback()` (llm_stream.py:153) → `call_llm_stream()` (llm_stream.py:76) → `llm_client.request_stream()`

**当前状态**: FC模式 + FC降级fallback（call_llm_with_fallback），tool_calls 原生消费（无 JSON roundtrip）

```
_process_single_step(agent, chunk_buffer)               ← react_cycle.py:112
   │
   ├── 1. agent.llm_call_count += 1
   ├── 2. agent.message_builder.trim_history()
   ├── 3. messages = agent.message_builder.prepare_messages_for_llm()
   ├── 4. openai_tools = get_openai_tools(agent)
   │
   └── 5. async for item in call_llm_with_fallback(agent, messages, openai_tools):
         │
         ├── call_llm_stream(agent, messages, openai_tools)  ← llm_stream.py:76
         │     ├── tool_choice = "auto" if openai_tools else None
         │     ├── async for chunk in request_stream(messages, tools, tool_choice):
         │     │     ├── chunk.tool_calls → 累积到 tool_calls_result
         │     │     ├── chunk.content → is_reasoning? full_reasoning += : full_content +=
         │     │     ├── chunk.is_done → break + 收集usage
         │     │     └── chunk.stream_error → break
         │     ├── stream_error → 丢弃tool_calls_result, yield error response
         │     ├── tool_calls_result → yield ("response", {type: "action", ...})
         │     └── 否则 → yield ("response", {type: "answer", ...})
         │
         └── FC降级fallback:                              ← llm_stream.py:153
               ├── FC_MAX_RETRIES次重试（捕获FCFormatError）
               └── FC_FALLBACK_ENABLED → call_llm_stream(openai_tools=None) Text模式
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F8-01 | trim_history 在 call_llm_with_fallback 之前执行 | P1 | Mock trim_history，验证在 _process_single_step 中被调用 |
| F8-02 | tool_calls 原生消费（无 JSON roundtrip） | P1 | Mock chunk.tool_calls 返回结构化数据 |
| F8-03 | stream_error 时丢弃未完成 tool_calls | P1 | Mock stream_error + tool_calls_result |
| F8-04 | CancelledError 时日志已累积内容 | P1 | Mock request_stream 抛 CancelledError |
| F8-05 | 无工具时 tool_choice=None | P1 | 验证 openai_tools=[] 时 tool_choice=None |
| F8-06 | usage 数据正确传递 | P1 | Mock chunk.usage，验证出现在响应中 |
| F8-07 | reasoning 内容与 content 分离累积 | P1 | Mock chunk.is_reasoning=True，验证 full_reasoning 不混入 full_content |
| F8-08 | FC降级fallback触发 | P0 | Mock call_llm_stream 抛 FCFormatError，验证降级到Text模式 |
| F8-09 | FC降级禁用时产出error | P1 | 设置 FC_FALLBACK_ENABLED=False，验证error response |

---

### 流程9：历史管理与裁剪

**文件链**: `app/services/agent/message_builder.py:148` → `trim_history()` → `_trim_to_budget()`

**当前状态**: 容量感知裁剪，80%阈值触发，纯预算裁剪无强制保留

```
trim_history()
   ├── total = _total_chars(conversation_history)
   ├── if total < MAX_CONTEXT_CHARS * 0.8: return       ← 未达阈值
   ├── if len(history) <= 2: return                      ← 太短不裁
   │
   ├── _classify_messages()                              ← 分类（4组）
   │     ├── system_msgs: role=system
   │     ├── user_msgs: role=user
   │     ├── obs_list: role=tool
   │     └── assistant_msgs: role=assistant
   │
   ├── available_budget = max(10000, int(MAX_CONTEXT_CHARS * 0.7) - system_chars - user_chars)
   │     └── 最低10000字符保护
   │
   ├── _trim_to_budget(obs_list, assistant_msgs, budget)
   │     ├── 建立 tool_to_assistant 配对映射
   │     ├── 按 conversation_history 顺序排列 obs+assistant
   │     ├── 从最新往最旧遍历
   │     ├── tool消息找配对assistant一起保留（计算pair_chars）
   │     └── budget用完停止
   │
   └── _rebuild_and_validate(system_msgs, user_msgs, trimmed)
         ├── rebuilt = system_msgs + user_msgs + trimmed
         ├── _trim_fc_pairs()                           ← FC配对完整性
         └── len(rebuilt) < 2 → 前2+后8 或 返回None不裁剪
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F9-01 | 未达80%阈值不裁剪 | P1 | 构造小于阈值的 history |
| F9-02 | history<=2时不裁剪 | P1 | 构造2条消息的 history |
| F9-03 | system消息不被裁剪 | P1 | 验证 system 消息始终保留 |
| F9-04 | tool/assistant配对完整性 | P0 | 裁剪后验证每个 tool 都有配对 assistant |
| F9-05 | budget计算正确（含max(10000,...)保护） | P1 | 验证 available_budget = max(10000, MAX*0.7 - (system+user)_chars) |
| F9-06 | tool_calls JSON计入字符数 | P1 | 验证 assistant 消息的 tool_calls 被计入 |
| F9-07 | temp_history容量限制 | P1 | 构造超50000字符的 temp_history |
| F9-08 | 裁剪后消息不足时的回退策略 | P1 | 构造rebuilt<2的场景，验证取前2+后8或返回None |
| F9-09 | user_msgs独立分类不被裁剪 | P1 | 验证 user 消息与 system 消息同样保留 |

---

### 流程10：操作记录与回滚

**文件链**: `app/services/agent/core_agent/step_emitter.py:60` → `task_tracker` → `task_tracker.add_operation()`

**当前状态**: 通过 StepEmitter 透传到 TaskTracker，无独立的 operation_recorder；**record_operation 失败时 logger.warning 记录但不抛异常**

```
agent.record_operation(operation_type, status=status, error=error)
   │
   └── StepEmitter.record_operation()
         └── task_tracker.add_operation(tracked_task_id, operation_type, ...)
               ├── 记录到内存（非DB）
               └── except Exception → logger.warning（记录但不抛异常）
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F10-01 | record_operation 正确透传 | P1 | Mock task_tracker.add_operation 验证参数 |
| F10-02 | 无 tracker 时不报错 | P1 | 验证 task_tracker=None 时不崩溃 |
| F10-03 | complete_task 记录完成状态 | P1 | Mock complete_task 验证 success 参数 |
| F10-04 | 操作记录包含 status 和 error | P1 | 验证 status="success"/"failed" 正确传递 |
| F10-05 | add_operation异常被静默吞掉 | P1 | Mock add_operation 抛异常，验证不崩溃但无感知 |

---

### 流程11：错误处理与恢复

**文件链**: 多处异常处理 → ErrorStep → SSE error

**当前状态**: 分层错误处理，最终都产出 ErrorStep；新增 error_handler.py 模块处理 FC格式错误和网络错误

```
错误处理层级:
   │
   ├── L0: ReAct循环内错误分类（error_handler.py:15）
   │     ├── handle_react_error(agent, error, step)     ← 模块级函数
   │     ├── FCFormatError → RETRYABLE_ERROR + ErrorStep(recoverable=True)
   │     ├── 网络错误 → RETRYABLE_ERROR + ErrorStep(recoverable=True)
   │     └── 其他 → FAILED + ErrorStep
   │
   ├── L1: LLM调用异常
   │     └── llm_stream.py:60 → _yield_error_response()
   │
   ├── L2: 工具执行异常
   │     └── ToolRetryEngine._execute_single_attempt() → build_error()
   │
   ├── L3: 安全检查失败
   │     └── action_handler.py:60 → ErrorStep(blocked)
   │
   ├── L4: ReAct循环异常
   │     └── react_cycle.py:245 → handle_react_error()
   │
   ├── L5: SSE流异常
   │     └── run_sse_stream.py:178 → _yield_error_sse() + FinalStep
   │
   └── L6: 路由层异常
         └── chat_openai.py:164 → create_error_response(router_error)
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F11-01 | LLM异常产出error SSE | P1 | Mock request_stream 抛异常 |
| F11-02 | 工具异常被重试引擎捕获 | P1 | Mock 工具抛可重试异常 |
| F11-03 | 重试耗尽产出error response | P1 | Mock 工具持续失败 |
| F11-04 | 安全blocked产出ErrorStep | P1 | Mock safety 返回 blocked |
| F11-05 | 用户拒绝产出ErrorStep | P1 | Mock confirmation 返回 rejected |
| F11-06 | 路由异常产出router_error | P1 | Mock generate 内部抛异常 |
| F11-07 | 所有错误路径都补发FinalStep | P0 | 验证每个异常路径都有 FinalStep yield |
| F11-08 | FCFormatError设置RETRYABLE_ERROR | P0 | Mock FCFormatError，验证 agent.status==RETRYABLE_ERROR |
| F11-09 | 网络错误设置RETRYABLE_ERROR | P0 | Mock 网络异常，验证 ErrorStep(recoverable=True) |
| F11-10 | error_handler模块级函数分派正确 | P1 | 验证 handle_react_error 的 if/elif 分派 |

---

### 流程12：工具缓存与动态注入

**文件链**: `app/services/agent/tool_cache_manager.py:14` → `get_openai_tools()` → `patch_search_desc()`

**当前状态**: TTL缓存(300s) + 动态分类注入 + 实例属性描述覆盖（不修改全局ts_meta）

```
get_openai_tools(agent)
   ├── cached = agent._tool_cache.get()                  ← TTL缓存
   ├── if cached: return cached
   │
   └── tools = tool_registry.to_openai_tools(categories=agent._loaded_categories)
         ├── 如果 agent._tool_search_desc_override 存在 → 覆盖tool_search的description
         ├── agent._tool_cache.set(tools)
         └── return tools

patch_search_desc(agent)
   ├── unloaded = [cat for cat in ToolCategory if cat not in _loaded_categories and cat not in {FUNDAMENTAL, SHELL}]
   ├── 如果没有未加载分类 → agent._tool_search_desc_override = None
   ├── base_desc = _get_original_search_desc()           ← 剥离旧后缀
   └── agent._tool_search_desc_override = base_desc + "\n\n当前未加载分类:\n" + ...
         └── 设置实例属性，不修改全局ts_meta

auto_inject_from_search(agent, result)                  ← tool_executor.py:38
   ├── 从 result.data.matches 收集 category
   ├── agent._loaded_categories.update(new_categories)
   ├── agent._tool_loader.load_category(cat)
   ├── invalidate_tool_cache(agent)
   └── patch_search_desc(agent)
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F12-01 | 缓存命中时不重新构建 | P1 | 二次调用 get_openai_tools 验证缓存 |
| F12-02 | invalidate 后重新构建 | P1 | 调用 invalidate_tool_cache 后验证 |
| F12-03 | patch_search_desc 不重复追加（实例属性模式） | P0 | 多次调用验证 _tool_search_desc_override 不重复追加 |
| F12-04 | auto_inject 注入整个工具类 | P1 | Mock tool_search 结果，验证整个类被注入 |
| F12-05 | 新分类工具可执行 | P1 | 注入 NETWORK 分类后验证工具可调用 |
| F12-06 | 缓存过期后重建 | P1 | 等待 TTL 过期后验证缓存失效 |
| F12-07 | _get_original_search_desc 正确剥离 | P1 | 构造含后缀的描述，验证剥离 |

---

### 流程13：消息构建流程

**文件链**: `app/services/agent/message_builder.py:200` → `prepare_messages_for_llm()` → `build_system_message()`

**当前状态**: MessageBuilder管理对话历史，支持system/user/assistant/tool消息构建

```
prepare_messages_for_llm()
   │
   ├── messages = []
   │
   ├── 1. 构建system消息
   │     └── system_msg = build_system_message()
   │           ├── 基础system prompt
   │           ├── 工具使用说明（如有）
   │           └── 当前时间戳
   │     └── messages.append({"role": "system", "content": system_msg})
   │
   ├── 2. 添加对话历史
   │     └── messages.extend(self.conversation_history)
   │
   ├── 3. 添加临时历史（如有）
   │     └── if self.temp_history:
   │           └── messages.extend(self.temp_history)
   │
   └── return messages
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F13-01 | system消息包含基础prompt | P1 | 验证返回的messages[0]["role"]=="system" |
| F13-02 | 对话历史正确追加 | P1 | 构造conversation_history，验证追加到messages |
| F13-03 | temp_history正确追加 | P1 | 构造temp_history，验证追加到messages末尾 |
| F13-04 | 空历史时仅返回system消息 | P1 | 清空历史，验证messages长度为1 |
| F13-05 | 工具使用说明正确注入 | P1 | 加载工具后验证system包含工具说明 |

---

### 流程14：工具注册流程

**文件链**: `app/tools/registry.py:50` → `ToolRegistry` → `register_tool()` → `get_tool()`

**当前状态**: 单例ToolRegistry，按category注册工具，支持OpenAI格式转换

```
ToolRegistry (单例)
   │
   ├── register_tool(tool_name, tool_meta, category)
   │     ├── 验证tool_name唯一性
   │     ├── self._tools[tool_name] = tool_meta
   │     └── self._categories[category].add(tool_name)
   │
   ├── get_tool(tool_name) → ToolMeta
   │     └── return self._tools.get(tool_name)
   │
   ├── to_openai_tools(categories=None) → List[dict]
   │     ├── 筛选指定categories的工具
   │     ├── 转换为OpenAI function calling格式
   │     │     ├── type: "function"
   │     │     ├── function.name
   │     │     ├── function.description
   │     │     └── function.parameters (JSON Schema)
   │     └── return tools_list
   │
   └── get_all_tools() → Dict[str, ToolMeta]
         └── return self._tools
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F14-01 | 工具注册成功 | P1 | 注册工具后验证get_tool返回正确meta |
| F14-02 | 重复注册抛异常 | P1 | 注册同名工具，验证抛ValueError |
| F14-03 | 按category筛选工具 | P1 | 注册多个category，验证to_openai_tools筛选正确 |
| F14-04 | OpenAI格式转换正确 | P1 | 验证转换后的格式符合OpenAI规范 |
| F14-05 | 未注册工具返回None | P1 | 调用get_tool("不存在")，验证返回None |

---

### 流程15：步骤发射流程

**文件链**: `app/services/agent/core_agent/step_emitter.py:56` → `StepEmitter.emit()` → `record_operation()`

**当前状态**: StepEmitter负责发射Step并记录操作，透传到task_tracker

```
StepEmitter(agent)
   │
   ├── emit(step: Step) → Step
   │     ├── 记录step到agent.steps
   │     ├── 调用record_operation记录
   │     └── return step
   │
   ├── record_operation(operation_type, status, error)
   │     ├── if self.task_tracker:
   │     │     └── task_tracker.add_operation(task_id, operation_type, status, error)
   │     └── except Exception → logger.warning（不抛异常）
   │
   └── exit_with_error(step_count, error_type, error_message) → ErrorStep
         ├── 构建ErrorStep
         ├── 调用emit发射
         └── return ErrorStep
```

**测试要点**:

| 编号 | 测试点 | 风险等级 | 测试方法 |
|------|--------|---------|---------|
| F15-01 | emit正确记录step | P1 | 调用emit后验证agent.steps包含该step |
| F15-02 | emit调用record_operation | P1 | Mock record_operation，验证被调用 |
| F15-03 | record_operation透传参数正确 | P1 | 验证task_tracker.add_operation参数正确 |
| F15-04 | record_operation异常不崩溃 | P1 | Mock add_operation抛异常，验证不崩溃 |
| F15-05 | exit_with_error返回ErrorStep | P1 | 调用exit_with_error，验证返回ErrorStep |

---

## 三、流程依赖关系图

```
                         ┌──────────────┐
                         │   Frontend    │
                         │  (React SPA)  │
                         └──────┬───────┘
                                │ POST /chat/stream
                                ▼
                    ┌─────────────────────┐
                    │   chat_openai.py    │ ← 流程1
                    │   (API 入口层)       │
                    └────────┬────────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
     ┌─────────────┐ ┌─────────────┐ ┌──────────────┐
     │ register    │ │ step_start  │ │ ContextVar   │ ← 流程7
     │ _task       │ │ (start)     │ │ _task_id     │
     └──────┬──────┘ └──────┬──────┘ └──────┬───────┘
            │               │               │
            ▼               ▼               ▼
     ┌───────────────────────────────────────────┐
     │          run_sse_stream                    │ ← 流程6
     │  ┌─────────────────────────────────────┐   │
     │  │      UniversalAgent                 │ ← 流程2
     │  │         ↓                           │   │
     │  │    run_react_cycle()                │ ← 流程3
     │  │    ┌──────────────────────┐         │   │
     │  │    │ call_llm_with_       │ ← 流程8 │   │
     │  │    │ _dispatch_handler    │         │   │
     │  │    │ handle_action →      │         │   │
     │  │    │ check_safety_and_    │ ← 流程5 │   │
     │  │    │   confirm            │         │   │
     │  │    │ execute_tools →      │ ← 流程4 │   │
     │  │    │   ToolRetryEngine    │         │   │
     │  │    │ build_observation    │         │   │
     │  │    │ yield Step           │ ← 流程15│   │
     │  │    └──────────────────────┘         │   │
     │  │    ┌──────────────────────┐         │   │
     │  │    │ MessageBuilder       │ ← 流程9 │   │
     │  │    │ prepare_messages_    │ ← 流程13│   │
     │  │    │   for_llm()          │         │   │
     │  │    │ trim_history()       │         │   │
     │  │    └──────────────────────┘         │   │
     │  │    ┌──────────────────────┐         │   │
     │  │    │ ToolRegistry         │ ← 流程14│   │
     │  │    │ get_openai_tools()   │ ← 流程12│   │
     │  │    └──────────────────────┘         │   │
     │  └─────────────────────────────────────┘   │
     └───────────────────────────────────────────┘
                             │
                             ▼ yield SSE
                    ┌─────────────────────┐
                    │  format_agent_sse   │
                    │  → SSE 字符串        │
                    └─────────────────────┘
                             │
                             ▼
                    ┌─────────────────────┐
                    │   Frontend SSE      │
                    │  processSSEData     │
                    └─────────────────────┘
```

---

## 四、关键架构决策

| 决策 | 说明 | 文件位置 | 当前状态 |
|------|------|---------|---------|
| 配置驱动 Agent | 所有 intents 共用 UniversalAgent | `app/services/agent/core_agent/universal_agent.py:29` | ✅ 生效 |
| FC模式 + 降级fallback | tool_calls 原生消费，FC失败降级Text | `app/services/agent/llm_stream.py:153` | ✅ 生效 |
| 薄调度重构 | react_cycle 只做分派，业务逻辑在 handlers | `app/services/agent/core_agent/react_cycle.py:112` | ✅ 生效 |
| SSE 直接 to_dict | 避免 Step→SSE→dict 往返解析 | `app/services/react_sse_wrapper/run_sse_stream.py:91` | ✅ 生效 |
| 批量保存+重试 | final 时走一次 save_execution_steps_to_db，最多2次重试 | `app/services/react_sse_wrapper/run_sse_stream.py:193` | ✅ 生效 |
| 工具缓存 TTL | 300秒缓存，invalidate 后重建 | `app/services/agent/tool_cache_manager.py:14` | ✅ 生效 |
| 动态分类注入 | tool_search 结果自动注入整个工具类 | `app/services/agent/tool_executor.py:38` | ✅ 生效 |
| 二元安全检查 | safe/dangerous，返回SafetyResult dataclass | `app/services/safety/tool_safety_checker.py:46` | ✅ 生效 |
| 停止检查回调 | llm_client.set_stop_check 注入 | `app/services/react_sse_wrapper/run_sse_stream.py:114` | ✅ 生效 |
| 实例属性描述覆盖 | patch_search_desc 不修改全局ts_meta | `app/services/agent/tool_cache_manager.py:60` | ✅ 生效 |

---

## 五、风险矩阵

| 流程 | P0风险 | P1风险 | 测试覆盖 |
|------|--------|--------|---------|
| 流程1 HTTP入口 | cancel_poller竞态、cleanup未执行 | 空消息处理 | 8项 |
| 流程2 Agent生命周期 | - | 状态流转、初始分类 | 7项 |
| 流程3 ReAct循环 | max_steps兜底、FC降级fallback、RETRYABLE_ERROR | 截断检测、chunk超时 | 11项 |
| 流程4 工具执行管线 | blocked拒绝、并行return_direct遗漏 | 参数验证、并行执行、重试逻辑、chunk_buffer未使用 | 12项 |
| 流程5 文件安全 | 路径越权、空格路径、空路径 | 大小保护、代码注入 | 10项 |
| 流程6 SSE事件流 | DB保存必定执行、Exception/CancelledError后补发FinalStep | stream_state更新、DB重试 | 12项 |
| 流程7 ContextVar | 并发任务task_id混淆 | 默认值、隔离性 | 4项 |
| 流程8 LLM通信 | FC降级fallback | tool_calls消费、stream_error、reasoning分离 | 9项 |
| 流程9 历史裁剪 | FC配对完整性 | 阈值触发、budget保护、回退策略 | 9项 |
| 流程10 操作记录 | - | 透传正确性、无tracker容错、静默吞异常 | 5项 |
| 流程11 错误处理 | 所有路径补发FinalStep、RETRYABLE_ERROR | 各层异常捕获、error_handler分派 | 10项 |
| 流程12 工具缓存 | patch_search_desc不重复（实例属性） | 缓存TTL、动态注入 | 7项 |
| 流程13 消息构建 | - | system消息、历史追加、temp_history | 5项 |
| 流程14 工具注册 | - | 注册成功、格式转换、category筛选 | 5项 |
| 流程15 步骤发射 | - | emit记录、record_operation透传 | 5项 |
| **合计** | **11项P0** | **107项P1** | **118项测试** |

---

## 六、总结

系统共 15 大关键流程，核心是 **chat_openai → UniversalAgent → ReAct循环 → SSE流式** 的完整链路。

| 流程编号 | 流程名称 | 核心文件 | 测试项数 |
|---------|---------|---------|---------|
| 1 | HTTP请求入口 | `app/api/v1/chat/chat_openai.py` | 8 |
| 2 | Agent生命周期 | `app/services/agent/core_agent/universal_agent.py` | 7 |
| 3 | ReAct循环 | `app/services/agent/core_agent/react_cycle.py` | 11 |
| 4 | 工具执行管线 | `app/services/agent/core_agent/handlers/action_handler.py` | 12 |
| 5 | 文件安全 | `app/services/safety/tool_safety_checker.py` | 10 |
| 6 | SSE事件流 | `app/services/react_sse_wrapper/run_sse_stream.py` | 12 |
| 7 | ContextVar传播 | `app/services/context_vars.py` | 4 |
| 8 | LLM通信 | `app/services/agent/llm_stream.py` | 9 |
| 9 | 历史裁剪 | `app/services/agent/message_builder.py` | 9 |
| 10 | 操作记录 | `app/services/agent/core_agent/step_emitter.py` | 5 |
| 11 | 错误处理 | 多处 | 10 |
| 12 | 工具缓存 | `app/services/agent/tool_cache_manager.py` | 7 |
| 13 | 消息构建 | `app/services/agent/message_builder.py` | 5 |
| 14 | 工具注册 | `app/tools/registry.py` | 5 |
| 15 | 步骤发射 | `app/services/agent/core_agent/step_emitter.py` | 5 |
| **合计** | | | **118项** |

**已知高风险区域**（需重点关注测试）:
1. SSE `aclose()` 与 main coroutine 竞态（流程1 F1-06）
2. `patch_search_desc` 实例属性覆盖模式正确性（流程12 F12-03）
3. DB 保存失败丢失执行历史（流程6 F6-04）
4. FC 配对裁剪后完整性（流程9 F9-04）
5. 并发任务 ContextVar 混淆（流程7 F7-04）
6. 所有错误路径补发 FinalStep（流程11 F11-07）
7. 并行工具 return_direct 仅检查 results[0]（流程4 F4-11）
8. FC降级fallback触发（流程3 F3-10）
9. RETRYABLE_ERROR 状态处理（流程3 F3-11）
10. 路径越权检查（流程5 F5-03）
11. 空路径拒绝（流程5 F5-09）
