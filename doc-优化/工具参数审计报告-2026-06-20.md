# 工具参数审计报告

**创建时间**: 2026-06-20 04:35:00
**更新时间**: 2026-06-20 05:15:00
**编写人**: 小欧
**变更人**: 小健
**审计范围**: 全部10组77个工具的Schema参数
**审计目标**: 每个参数的名称、含义、必填/可选、是否必要

---

## 一、fundamental（7个工具）

### 1.1 tool_search — 搜索工具

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| query | 搜索关键词，BM25全文检索，支持中英文。如'读取Word文档' 'SQL查询 数据库' | 必填 | ✅ 必要 |

### 1.2 time_now — 获取当前时间

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| （无参数） | — | — | — |

> 【2026-06-20 小健】删除 format、timezone——LLM不关心格式和时区，默认值足够

### 1.3 time_add — 时间偏移计算

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| delta | 偏移量。正数=增加，负数=减少 | 必填 | ✅ 必要 |
| start | 基准时间，默认当前时间 | 可选 | ✅ 必要——多数场景用默认值 |
| unit | 偏移单位：days/hours/minutes/seconds/months | 可选 | ✅ 必要——默认days，但需让用户知道可选 |

### 1.4 time_diff — 计算时间差

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| start | 起始时间 | 必填 | ✅ 必要 |
| end | 结束时间，默认当前时间 | 可选 | ✅ 必要——多数场景用默认值 |

### 1.5 query_calendar — 日历查询

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| name | 节日名称，如端午节/春节/中秋节 | 可选 | ✅ 必要——推荐方式 |
| year | 查询年份，默认当年 | 可选 | ⚠️ 多数情况用默认值 |
| date | 日期值 | 可选 | ✅ 必要——替代方式 |
| check_type | 检查类型：weekend/holiday/workday/next_workday | 可选 | ✅ 必要——替代方式 |
| n | 第N个工作日，仅next_workday有效 | 可选 | ✅ 必要——next_workday时必需 |

### 1.6 get_system_info — 获取系统信息

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| info_type | 系统信息类型：basic/cpu/内存/磁盘/网络/all | 可选 | ✅ 必要——默认all |

### 1.7 send_notification — 发送通知

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| title | 通知标题 | 必填 | ✅ 必要 |
| message | 通知正文 | 必填 | ✅ 必要 |
| duration | 通知显示时长（秒），默认5秒 | 可选 | ⚠️ 多数情况用默认值 |


---

## 二、file（15个工具）

### 2.1 read_text_file — 读取文本文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_path | 文件路径（绝对路径） | 必填 | ✅ 必要 |
| head | 读取前N行 | 可选 | ⚠️ 可选，大文件时有用 |
| tail | 读取后N行 | 可选 | ⚠️ 可选，大文件时有用 |
| offset | 起始行号，配合limit分页 | 可选 | ⚠️ 可选，大文件时有用 |
| limit | 最大读取行数，配合offset分页 | 可选 | ⚠️ 可选，大文件时有用 |
| encoding | 文件编码，默认utf-8 | 可选 | ✅ 必要——中文文件可能需要gbk |

### 2.2 write_text_file — 写入文本文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_path | 文件完整路径（绝对路径） | 必填 | ✅ 必要 |
| content | 要写入的文本内容 | 必填 | ✅ 必要 |
| encoding | 文件编码，默认utf-8 | 可选 | ✅ 必要——中文文件可能需要gbk |
| append | 是否追加，True=追加 False=覆盖 | 可选 | ✅ 必要——日志场景常用 |

> 【2026-06-20 小健】删除 create_parents、unescape——LLM用不好，内部默认True

### 2.3 read_media_file — 读取媒体文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_path | 媒体文件路径。支持图片/音频/视频 | 必填 | ✅ 必要 |

### 2.4 edit_text_file — 编辑文本文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_path | 目标文件绝对路径 | 必填 | ✅ 必要 |
| old_string | 待替换的旧字符串 | 必填 | ✅ 必要 |
| new_string | 替换的新字符串，空字符串表示删除 | 可选 | ✅ 必要——删除时传空 |
| replace_all | 是否替换所有匹配项，默认False | 可选 | ⚠️ 多数情况用默认值 |
| encoding | 文件编码，默认utf-8 | 可选 | ✅ 必要 |

> 【2026-06-20 小健】删除 dry_run——LLM几乎不用，内部默认False

### 2.5 list_directory — 列出目录内容

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| dir_path | 目录路径（绝对路径） | 必填 | ✅ 必要 |
| recursive | 是否递归列出子目录。| 可选 | ✅ 必要——查看深层结构时用 |
| sort_by | 排序方式：name/size/mtime | 可选 | ✅ 必要 |
| include_hidden | 是否显示隐藏文件 | 可选 | ⚠️ 多数情况不需要 |

> 【2026-06-20 小健】sortBy→sort_by(修复命名不一致)；删除 max_depth、page_token；删除 format/tree（与recursive重叠，recursive=True即树形输出）

### 2.6 search_files — 搜索文件名

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| pattern | 文件名匹配模式，支持glob通配符 | 必填 | ✅ 必要 |
| search_dir | 搜索起始目录（绝对路径） | 必填 | ✅ 必要 |
| recursive | 是否递归搜索子目录 | 可选 | ✅ 必要——默认True |
| ignore_case | 是否忽略大小写，默认True | 可选 | ⚠️ 多数情况用默认值 |
| type | 搜索类型过滤：file/directory | 可选 | ⚠️ 多数情况不需要 |

> 【2026-06-20 小健】删除 max_depth、page_token

### 2.7 grep_file_content — 搜索文件内容

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| pattern | 正则表达式搜索模式 | 必填 | ✅ 必要 |
| search_dir | 搜索路径（绝对路径） | 可选 | ⚠️ 可选，默认当前目录 |
| glob | 文件过滤通配符，如 "*.py" | 可选 | ✅ 必要——限定文件类型时用 |
| ignore_case | 是否忽略大小写，默认True | 可选 | ⚠️ 多数情况用默认值 |

> 【2026-06-20 小健】删除 page_token、multiline、head_limit；删除 output_mode（默认content已够用，需文件名列表时content返回里含文件名）、context（默认返回匹配行已够用，需更多上下文时调read_text_file）

### 2.8 compress_files — 压缩文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| source | 要压缩的文件/目录路径 | 必填 | ✅ 必要 |
| destination | 输出压缩包路径 | 必填 | ✅ 必要 |
| format | 压缩格式：zip/tar/tar.gz/tar.bz2 | 可选 | ✅ 必要——默认zip |
| password | ZIP加密密码 | 可选 | ⚠️ 特殊场景才用 |
| overwrite | 是否覆盖已存在文件 | 可选 | ⚠️ 多数情况用默认值 |
| exclude_patterns | 排除的文件/目录模式列表 | 可选 | ⚠️ 复杂场景才用 |

> 【2026-06-20 小健】删除 compression_level——LLM无法判断0-9该选哪个

### 2.9 extract_archive — 解压文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| source | 压缩包路径 | 必填 | ✅ 必要 |
| destination | 解压目标目录，默认自动创建同名目录 | 可选 | ✅ 必要——多数场景用默认值 |
| password | 解密密码，仅ZIP支持 | 可选 | ⚠️ 特殊场景才用 |
| overwrite | 是否覆盖已存在文件 | 可选 | ⚠️ 多数情况用默认值 |

### 2.10 move_file — 移动文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| source | 源文件路径（绝对路径） | 必填 | ✅ 必要 |
| destination | 目标路径（绝对路径） | 必填 | ✅ 必要 |
| overwrite | 是否覆盖目标文件 | 可选 | ⚠️ 多数情况用默认值 |

### 2.11 copy_file — 复制文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| source | 源文件路径（绝对路径） | 必填 | ✅ 必要 |
| destination | 目标路径（绝对路径） | 必填 | ✅ 必要 |
| recursive | 复制目录时需True | 可选 | ✅ 必要——复制目录时必需 |
| overwrite | 是否覆盖目标文件 | 可选 | ⚠️ 多数情况用默认值 |

> 【2026-06-20 小健】删除 preserve_metadata——LLM不理解元数据概念

### 2.12 delete_file — 删除文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| source | 要删除的文件/目录路径 | 必填 | ✅ 必要 |
| recursive | 删除非空目录时需True | 可选 | ✅ 必要——删除目录时必需 |
| force | True=跳过回收站永久删除 | 可选 | ⚠️ 多数情况用默认值 |

### 2.13 rename_file — 重命名文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| source | 原文件/目录路径（绝对路径） | 必填 | ✅ 必要 |
| destination | 新名称（仅文件名，不含目录路径） | 必填 | ✅ 必要 |

### 2.14 read_data_file — 读取结构化配置文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_path | 文件路径（绝对路径） | 必填 | ✅ 必要 |
| format | 强制指定格式：json/yaml/toml/ini/xml/properties | 可选 | ⚠️ 可选，默认自动检测 |

> 【2026-06-20 小健】删除 encoding——结构化配置99%是utf-8

### 2.15 write_data_file — 写入结构化配置文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_path | 文件路径（绝对路径） | 必填 | ✅ 必要 |
| data | 写入数据，dict或list | 必填 | ✅ 必要 |
| format | 强制指定格式：json/yaml/toml | 可选 | ⚠️ 可选，默认自动检测 |

> 【2026-06-20 小健】删除 encoding、indent——结构化配置99%是utf-8，缩进用默认值

---

## 三、shell（4个工具）

### 3.1 execute_shell_command — 执行Shell命令

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| command | 要执行的命令 | 必填 | ✅ 必要 |
| shell_type | 执行环境：powershell/cmd | 可选 | ⚠️ 默认powershell，Windows环境几乎不用改 |
| timeout | 超时毫秒数，默认30000(30秒) | 可选 | ⚠️ 多数情况用默认值 |
| run_in_background | 是否后台运行 | 可选 | ✅ 必要——长期服务时用 |
| cwd | 命令执行的工作目录(绝对路径) | 可选 | ⚠️ 需在特定目录执行命令时设置，不设置则使用当前目录 |

> 【2026-06-20 小健】删除 env_vars（LLM不应设置环境变量）；cwd 改描述（原描述"Agent智能设置"太模糊）

### 3.2 find_command — 查找命令路径

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| command | 要查找的命令名称 | 必填 | ✅ 必要 |
| all_paths | False=返回第一个，True=返回全部 | 可选 | ⚠️ 多数情况用默认值 |

### 3.3 shell_session — 后台Shell会话管理

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| shell_id | 后台会话ID | 必填 | ✅ 必要 |
| action | 操作类型：output(读取输出)/terminate(终止) | 可选 | ✅ 必要——默认output |
| filter | 输出过滤正则表达式 | 可选 | ⚠️ 可选，大数据量时用 |
| max_lines | 最大返回行数，默认1000 | 可选 | ⚠️ 多数情况用默认值 |
| force | 强制终止 | 可选 | ⚠️ 多数情况用默认值 |

### 3.4 execute_code — 执行代码

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| code | 要执行的代码字符串 | 必填 | ✅ 必要 |
| language | 语言类型：python/javascript | 可选 | ✅ 必要——默认python |
| timeout | 超时秒数，默认30秒 | 可选 | ⚠️ 多数情况用默认值 |
| working_dir | 工作目录 | 可选 | ⚠️ 可选，默认当前目录 |

> 【2026-06-20 小健】删除 safety_check——安全风险参数，LLM不应有权关闭安全检查，内部硬编码True

---

## 四、network（5个工具）

### 4.1 http_request — HTTP请求

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| url | 请求目标URL | 必填 | ✅ 必要 |
| method | HTTP方法：GET/POST/PUT/DELETE/PATCH/HEAD/OPTIONS | 可选 | ✅ 必要——默认GET |
| headers | 请求头字典 | 可选 | ✅ 必要——认证等场景用 |
| params | URL查询参数 | 可选 | ✅ 必要——GET参数时用 |
| json_body | JSON请求体（POST/PUT/PATCH时用） | 可选 | ✅ 必要——POST时用 |

> 【2026-06-20 小健】删除 timeout（底层控制，LLM无法判断合理值）、proxy（LLM不知代理地址，应从环境变量自动读取）、retry（底层控制，LLM无法判断重试次数）

### 4.2 download_file — 下载文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| url | 要下载文件的URL | 必填 | ✅ 必要 |
| destination_path | 文件保存完整路径 | 必填 | ✅ 必要 |

> 【2026-06-20 小健】删除 headers（下载极少自定义请求头）、timeout（底层控制）、proxy（同http_request）

### 4.3 fetch_webpage — 抓取网页

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| url | 网页URL | 必填 | ✅ 必要 |
| prompt | 提取指令，未指定时返回完整内容 | 可选 | ✅ 必要——精准提取时用 |
| extract_format | 提取格式：markdown/html/text | 可选 | ✅ 必要——默认markdown |

> 【2026-06-20 小健】删除 max_tokens（底层控制）、js_render（LLM不知页面是否SPA）、timeout（底层控制）、proxy（同http_request）

### 4.4 search_web — 搜索网页

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| query | 搜索查询字符串 | 必填 | ✅ 必要 |

> 【2026-06-20 小健】删除 allowed_domains/blocked_domains（LLM极少用域名过滤）、num_results（默认10够用）、proxy（同http_request）

### 4.5 network_diagnose — 网络诊断

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| host | 目标主机地址（域名或IP） | 必填 | ✅ 必要 |
| mode | 诊断模式：ping(主机级)/port(服务级) | 可选 | ✅ 必要——默认ping |
| port | 目标端口号（mode=port时必填） | 可选 | ✅ 必要——端口检测时必需 |

> 【2026-06-20 小健】删除 count（默认4次够用）、timeout（底层控制）

---

## 五、document（8个工具）

### 5.1 read_pdf — 读取PDF

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_name | 文件名+路径(.pdf) | 必填 | ✅ 必要 |

### 5.2 read_docx — 读取Word

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_name | 文件名+路径(.docx/.doc) | 必填 | ✅ 必要 |

### 5.3 read_pptx — 读取PPT

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_name | 文件名+路径(.pptx) | 必填 | ✅ 必要 |

### 5.4 read_xlsx — 读取Excel

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_name | 文件名+路径(.xlsx/.csv/.xls) | 必填 | ✅ 必要 |

### 5.5 write_docx — 写入Word

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_name | 文件名+路径(.docx) | 必填 | ✅ 必要 |
| title | 文档标题 | 可选 | ⚠️ 可选 |
| paragraphs | 正文内容（支持纯文本/混合内容/表格） | 可选 | ✅ 必要——写入内容时必需 |

### 5.6 write_xlsx — 写入Excel

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_name | 文件名+路径(.xlsx) | 必填 | ✅ 必要 |
| data | 写入数据（支持3种格式） | 可选 | ✅ 必要——写入内容时必需 |
| sheet_name | 工作表名 | 可选 | ⚠️ 默认Sheet1 |

### 5.7 write_pdf — 写入PDF

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_name | 文件名+路径(.pdf) | 必填 | ✅ 必要 |
| title | 文档标题 | 可选 | ⚠️ 可选 |
| paragraphs | 正文内容（支持纯文本/混合内容/表格） | 可选 | ✅ 必要——写入内容时必需 |

### 5.8 write_pptx — 写入PPT

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| file_name | 文件名+路径(.pptx) | 必填 | ✅ 必要 |
| slides | 幻灯片列表（支持封面/内容/两栏/表格） | 可选 | ✅ 必要——写入内容时必需 |

---

## 六、dataanalysis（6个工具）

### 6.1 generate_chart — 生成图表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| data | 图表数据（JSON格式） | 必填 | ✅ 必要 |
| chart_type | 图表类型：bar/line/pie/scatter | 可选 | ✅ 必要——默认bar |
| title | 图表标题 | 可选 | ⚠️ 可选 |
| output_path | 输出图片路径 | 可选 | ⚠️ 默认自动生成临时路径 |

> 【2026-06-20 小健】删除 x_label/y_label（LLM不关心轴标签）

### 6.2 analyze_data — 数据分析

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| data | 要分析的数据（数组或文件路径） | 必填 | ✅ 必要 |
| group_by | 分组统计的列名 | 可选 | ✅ 必要——分组分析时用 |
| sort_by | 排序的列名，按此列升序排列 | 可选 | ⚠️ 可选 |
| top_n | 只返回前N条结果 | 可选 | ✅ 必要——限制结果数 |

> 【2026-06-20 小健】删除 operations（默认全部统计够用）、max_rows（底层控制）

### 6.3 filter_data — 数据筛选

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| data | 要筛选的数据（数组或文件路径） | 必填 | ✅ 必要 |
| conditions | 筛选条件列表 | 必填 | ✅ 必要 |
| select_columns | 选择返回的列 | 可选 | ✅ 必要——只返回指定列时用 |
| sort_by | 排序的列名，按此列升序排列 | 可选 | ⚠️ 可选 |
| top_n | 只返回前N条结果 | 可选 | ✅ 必要——限制结果数 |

> 【2026-06-20 小健】删除 max_rows（底层控制）

### 6.4 query_sql — SQL查询（只读）

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| sql | SQL查询语句（仅SELECT/SHOW/DESCRIBE） | 必填 | ✅ 必要 |
| connection_type | 数据库类型：sqlite/mysql/postgresql | 可选 | ✅ 必要——默认sqlite |
| connection_string | MySQL/PostgreSQL连接字符串（connection_type=mysql/postgresql时必填） | 可选 | ✅ 必要——非sqlite时必需 |
| db_path | SQLite数据库文件路径（connection_type=sqlite时必填） | 可选 | ✅ 必要——sqlite时必需 |

> 【2026-06-20 小健】删除 limit（SQL里可写LIMIT）、timeout（底层控制）

### 6.5 execute_sql — SQL执行（写入）

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| sql | SQL写入语句（INSERT/UPDATE/DELETE/DDL） | 必填 | ✅ 必要 |
| dry_run | 预检模式，True=只校验不执行 | 可选 | ⚠️ 可选 |
| connection_type | 数据库类型：sqlite/mysql/postgresql | 可选 | ✅ 必要——默认sqlite |
| connection_string | MySQL/PostgreSQL连接字符串（connection_type=mysql/postgresql时必填） | 可选 | ✅ 必要——非sqlite时必需 |
| db_path | SQLite数据库文件路径（connection_type=sqlite时必填） | 可选 | ✅ 必要——sqlite时必需 |

> 【2026-06-20 小健】删除 timeout（底层控制）

### 6.6 get_db_schema — 获取数据库结构

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| table_name | 指定表名，仅获取该表结构 | 可选 | ✅ 必要——查看指定表时用 |
| connection_type | 数据库类型：sqlite/mysql/postgresql | 可选 | ✅ 必要——默认sqlite |
| connection_string | MySQL/PostgreSQL连接字符串（connection_type=mysql/postgresql时必填） | 可选 | ✅ 必要——非sqlite时必需 |
| db_path | SQLite数据库文件路径（connection_type=sqlite时必填） | 可选 | ✅ 必要——sqlite时必需 |

> 【2026-06-20 小健】删除 db_name（绝大多数用当前库）、filter_pattern（与table_name重叠）

---

## 七、system（5个工具）

### 7.1 event_log — 获取系统事件日志

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| log_name | 日志名称：System/Application/Security | 可选 | ✅ 必要——默认System |
| max_events | 最大返回事件数，默认50 | 可选 | ⚠️ 多数情况用默认值 |
| level | 日志级别过滤：critical/error/warning/info | 可选 | ✅ 必要——默认error |
| source | 事件来源/应用名过滤 | 可选 | ⚠️ 可选 |
| time_range | 时间范围：10m/1h/24h/7d | 可选 | ✅ 必要——默认1h |

### 7.2 create_task — 创建计划任务

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| task_name | 计划任务名称 | 必填 | ✅ 必要 |
| command | 要执行的命令或程序路径 | 必填 | ✅ 必要 |
| schedule | 计划执行时间（HH:MM/HH:MM /day N/HH:MM /monthly DD） | 必填 | ✅ 必要 |
| interval | 重复间隔分钟数 | 可选 | ⚠️ 可选 |

> 【2026-06-20 小健】删除 start_time——与 schedule 语义重叠

### 7.3 delete_task — 删除计划任务

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| task_name | 要删除的计划任务名称 | 必填 | ✅ 必要 |

### 7.4 list_tasks — 列出计划任务

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| task_name | 按任务名称过滤（模糊匹配） | 可选 | ⚠️ 可选 |
| state | 状态过滤：ready/running/disabled/all | 可选 | ✅ 必要——默认all |


---

## 八、desktop（16个工具）

### 8.1 window_info — 获取窗口列表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| include_minimized | 是否包含最小化的窗口 | 可选 | ⚠️ 默认False |
| filter_title | 按窗口标题过滤（大小写不敏感） | 可选 | ✅ 必要——查找特定窗口时用 |

### 8.2 window_focus — 聚焦窗口

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |

### 8.3 window_resize — 调整窗口大小

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |
| width | 窗口宽度（像素） | 可选 | ✅ 必要——调整大小时必需 |
| height | 窗口高度（像素） | 可选 | ✅ 必要——调整大小时必需 |

### 8.4 window_maximize — 最大化窗口

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |

### 8.5 window_minimize — 最小化窗口

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |

### 8.6 window_restore — 恢复窗口

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |

### 8.7 window_topmost — 置顶窗口

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |

### 8.8 window_unpin — 取消置顶窗口

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |

### 8.9 mouse_click — 鼠标点击

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| x | X坐标（不传则在当前位置点击） | 可选 | ⚠️ 可选 |
| y | Y坐标（不传则在当前位置点击） | 可选 | ⚠️ 可选 |
| button | 鼠标按钮：left/right/middle | 可选 | ✅ 必要——默认left |

### 8.10 mouse_move — 鼠标移动

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| x | 目标X坐标 | 必填 | ✅ 必要 |
| y | 目标Y坐标 | 必填 | ✅ 必要 |

### 8.11 mouse_scroll — 鼠标滚动

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| direction | 滚动方向：up/down | 可选 | ✅ 必要——默认down |
| amount | 滚动单位 | 可选 | ✅ 必要——默认3 |

### 8.12 mouse_position — 获取鼠标位置

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| （无参数） | — | — | — |

### 8.13 keyboard_control — 键盘控制

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| action | 键盘操作：type(输入文本)/shortcut(快捷键)/combo(组合键) | 必填 | ✅ 必要 |
| text_or_keys | 输入内容（type时为文本，shortcut时为ctrl+c，combo时为ctrl,shift,esc） | 必填 | ✅ 必要 |
| interval | 每个字符间隔（秒），仅ASCII有效 | 可选 | ⚠️ 极少用 |

### 8.14 screen_capture — 截屏

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| output_path | 输出文件路径 | 可选 | ⚠️ 默认自动生成临时路径 |
| region | 截取区域：{x, y, width, height} | 可选 | ⚠️ 可选 |
| display | 显示器编号，1=主显示器 | 可选 | ⚠️ 多显示器时用 |

### 8.15 clipboard_read — 读取剪贴板

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| （无参数） | — | — | — |

### 8.16 clipboard_write — 写入剪贴板

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| content | 要写入剪贴板的内容 | 必填 | ✅ 必要 |

> 【2026-06-20 小健】删除 screen_record、ocr、send_notification 三个死Schema——工具已删/已迁，Schema残留

---

## 九、win_registry（3个工具）

### 9.1 registry_read — 读取注册表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| key_path | 注册表键路径 | 必填 | ✅ 必要 |
| value_name | 值名称（不填则读取默认值） | 可选 | ✅ 必要——读取特定值时用 |
| hive | 注册表根键：HKCU/HKLM/HKCR/HKU/HKCC | 可选 | ✅ 必要——默认HKCU |

### 9.2 registry_write — 写入注册表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| key_path | 注册表键路径 | 必填 | ✅ 必要 |
| value_name | 值名称 | 必填 | ✅ 必要 |
| value | 值数据 | 必填 | ✅ 必要 |
| value_type | 值类型：auto_detect/REG_SZ/REG_EXPAND_SZ/REG_DWORD等 | 可选 | ✅ 必要——默认auto_detect |
| hive | 注册表根键 | 可选 | ✅ 必要——默认HKCU |

### 9.3 registry_delete — 删除注册表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| key_path | 注册表键路径 | 必填 | ✅ 必要 |
| value_name | 值名称（不填则删除整个键） | 可选 | ✅ 必要——删除特定值时用 |
| hive | 注册表根键 | 可选 | ✅ 必要——默认HKCU |
| recursive | 递归删除子键 | 可选 | ✅ 必要——删除非空键时必需 |

---

## 十、timer（3个工具）

### 10.1 timer_set — 设置定时器

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| delay | 延迟秒数（1~86400即最长24小时） | 必填 | ✅ 必要 |
| callback | 定时器触发内容（文本消息） | 必填 | ✅ 必要 |

### 10.2 timer_clear — 清除定时器

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| timer_id | 定时器ID（由timer_set返回） | 必填 | ✅ 必要 |

### 10.3 timer_list — 列出定时器

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| （无参数） | — | — | — |

---

## 十一、问题汇总

### 11.1 已修复问题

| # | 工具 | 参数 | 问题 | 修复方式 | 修复人 |
|---|------|------|------|---------|--------|
| 1 | list_directory | sortBy | 参数名应为 sort_by | ✅ 重命名为 sort_by | 小健 |
| 2 | search_files | page_token | 多余——LLM不需要分页 | ✅ 删除 | 小健 |
| 3 | grep_file_content | page_token | 多余——LLM不需要分页 | ✅ 删除 | 小健 |
| 4 | grep_file_content | multiline | 多余——LLM几乎不用 | ✅ 删除 | 小健 |
| 5 | grep_file_content | head_limit | LLM无法预判合理限制值 | ✅ 删除 | 小健 |
| 6 | execute_shell_command | env_vars | LLM不应设置环境变量 | ✅ 删除，内部默认os.environ | 小健 |
| 7 | fetch_webpage | max_tokens | LLM无法判断截断值 | ✅ 删除 | 小健 |
| 8 | query_sql/execute_sql/get_db_schema | db_path | 描述写"必填"但类型Optional | ✅ 改为条件必填说明 | 小健 |
| 9 | analyze_data/filter_data | sort_by | 描述"需搭配top_n"条件依赖 | ✅ 简化描述，删除条件依赖 | 小健 |
| 10 | execute_code | safety_check | 安全风险参数，LLM不应有权关闭 | ✅ 删除，内部硬编码True | 小健 |
| 11 | desktop | ScreenRecordInput/OcrInput/SendNotificationInput | 死Schema残留 | ✅ 删除 | 小健 |
| 12 | write_text_file | create_parents/unescape | LLM用不好 | ✅ 删除，内部默认True | 小健 |
| 13 | compress_files | compression_level | LLM无法判断0-9该选哪个 | ✅ 删除，内部默认6 | 小健 |
| 14 | copy_file | preserve_metadata | LLM不理解元数据概念 | ✅ 删除，内部默认True | 小健 |
| 15 | write_data_file | encoding/indent | 结构化配置99%是utf-8 | ✅ 删除 | 小健 |
| 16 | read_data_file | encoding | 结构化配置99%是utf-8 | ✅ 删除 | 小健 |
| 17 | list_directory/search_files | max_depth | LLM无法判断合理递归深度 | ✅ 删除，内部默认值 | 小健 |
| 18 | get_env | expand_vars | LLM不理解环境变量展开概念 | ✅ 删除，内部默认True | 小健 |
| 19 | create_task | start_time | 与schedule语义重叠 | ✅ 删除 | 小健 |
| 20 | dataanalysis | 3×(connection_type+connection_string+db_path) | DRY违反 | ✅ 提取_DbConnectionMixin基类 | 小健 |
| 21 | list_directory | format/tree | 与recursive重叠 | ✅ 删除tree，recursive=True即树形输出 | 小健 |
| 22 | grep_file_content | output_mode | 默认content已够用，files_with_matches场景content返回里含文件名 | ✅ 删除，内部默认content | 小健 |
| 23 | grep_file_content | context | 默认返回匹配行已够用，需更多上下文时调read_text_file | ✅ 删除 | 小健 |
| 24 | execute_shell_command | env_vars | LLM不应设置环境变量 | ✅ 删除，内部默认os.environ | 小健 |
| 25 | execute_shell_command | cwd | 描述"Agent智能设置"太模糊 | ✅ 改描述为明确说明何时使用 | 小健 |
| 26 | http_request | timeout/proxy/retry | 底层控制参数，LLM无法判断合理值 | ✅ 删除，内部默认值 | 小健 |
| 27 | download_file | headers/timeout/proxy | 下载极少自定义请求头+底层控制 | ✅ 删除，内部默认值 | 小健 |
| 28 | fetch_webpage | js_render/timeout/proxy | LLM不知是否SPA+底层控制 | ✅ 删除，内部默认值 | 小健 |
| 29 | search_web | allowed_domains/blocked_domains/num_results/proxy | LLM极少用域名过滤+默认10够用+底层控制 | ✅ 删除，内部默认值 | 小健 |
| 30 | network_diagnose | count/timeout | 默认4次够用+底层控制 | ✅ 删除，内部默认值 | 小健 |
| 31 | generate_chart | x_label/y_label | LLM不关心轴标签 | ✅ 删除，内部默认值 | 小健 |
| 32 | analyze_data | operations/max_rows | 默认全部统计够用+底层控制 | ✅ 删除，内部默认值 | 小健 |
| 33 | filter_data | max_rows | 底层控制 | ✅ 删除，内部默认值 | 小健 |
| 34 | query_sql | limit/timeout | SQL里可写LIMIT+底层控制 | ✅ 删除，内部默认值 | 小健 |
| 35 | execute_sql | timeout | 底层控制 | ✅ 删除，内部默认值 | 小健 |
| 36 | get_db_schema | db_name/filter_pattern | 绝大多数用当前库+与table_name重叠 | ✅ 删除，内部默认值 | 小健 |
| 37 | list_processes/kill_process/service_control | 整个工具 | LLM不应直接操控进程和服务，安全风险高 | ✅ 从Schema/Tools/Register全部删除 | 小健 |
| 38 | net_connections | 整个工具 | shell命令可替代，LLM不需要专用工具 | ✅ 从Schema/Tools/Register/Constants全部删除 | 小健 |
| 39 | get_env/set_env | 整个工具 | LLM不应操作环境变量，安全风险高 | ✅ 从Schema/Tools/Register全部删除 | 小健 |

### 11.2 不修复问题（经3遍分析确认为非问题）

| # | 工具 | 参数 | 原报告问题 | 不修复理由 |
|---|------|------|-----------|-----------|
| 1 | fetch_webpage | max_tokens | 名称误导 | 已删除，属于底层控制参数而非名称问题 |

---

**报告完成时间**: 2026-06-20 04:50:00
**更新时间**: 2026-06-20 05:15:00
**编写人**: 小欧
**变更人**: 小健
