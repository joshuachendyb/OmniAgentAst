# 系统代码问题深度分析报告

**编写人**: 小欧 2026-09-07 16:37:28

| 版本 | 更新时间 | 更新内容 | 更新人 |
|------|----------|----------|--------|
| v1.1 | 2026-09-07 17:05:00 | 问题2证伪标注、问题3b病根确认并修复、修复要点补充 | 小欧 |

---

## 一、概述

分析范围: `app_2026-09-05.log`(10.5MB) / `app_2026-09-06.log`(9.3MB) / `app_2026-09-07.log`(3.1MB)，共 22.9MB 日志。

| 指标 | 09-05 | 09-06 | 09-07 | 合计 |
|------|-------|-------|-------|------|
| ERROR 行数 | 822 | 720 | 31 | **1573** |
| Traceback 堆栈数 | 14 | 20 | 1 | **35** |

---

## 二、真实系统代码问题（根因分析）

### 问题1: `_process_single_step` 返回值异常 — `TypeError` 两连环

**严重度: 高 | 状态: 已修复(09-06) | 影响: 任务异常终止**

#### 病根1a: `'NoneType' object is not iterable`
- **位置**: `react_loop.py:163` → `for event in await _process_single_step(...)`
- **触发条件**: `_process_single_step` 连续截断上限取消分支(原 L404-421)存在**裸 `return`**
- **机理**: `async def` 函数裸 `return` 返回 `None`，主循环 L163 对 `None` 做 `for event in None` → `TypeError`
- **连锁故障**: `_is_recoverable_error(TypeError)=False` → 外层 `except Exception` → `set_failed` **无条件覆盖终态** → 本应 `cancelled`(final已publish) 实际 status=FAILED → `chat_tasks.status` 落 failed，与 `chat_task_steps` 终态=cancelled **自相矛盾**，SSE 多刷一条 error
- **修复**: 裸 `return` → `return []`(react_step.py L429)
- **日志证据**: 09-06 日志 `react_loop.py:242 [run_react_cycle] 不可恢复异常: 'NoneType' object is not iterable`

#### 病根1b: `'coroutine' object is not iterable`
- **位置**: 同 `react_loop.py:163`
- **触发条件**: `_process_single_step` 从 async generator 改为普通 async 返 List 的过渡期（4C 重构），部分调用路径未正确 await
- **机理**: 如果 `_process_single_step` 内部某路径返回 coroutine 而非 list，`for event in coroutine` → TypeError
- **修复**: 已在 4C(5.8.2) 中统一所有 return 路径为 `return []`（共 5 处 return，全部 return []）
- **日志证据**: 09-06 多次出现，09-07 **未再出现**（修复生效验证）

**根因总结**: 重构(async generator → async List)时**遗漏了1个 return 路径**，且单元测试盲区（直调 `_process_single_step` 读 event_log 不看返回值）未捕获。

---

### 问题2: `_process_single_step` 无可用工具 【证伪 — 测试构造场景】

**严重度: 中 | 状态: 证伪(非真实bug, 09-07核实) | 影响: 无**

- **位置**: `react_step.py:218` `[_process_single_step] 无可用工具`
- **日志证据**: 09-05: 81次 / 09-06: 140次
- **三遍核实结论**: 抽样全部伴随 `model=<MagicMock ...>`（如 `model=<MagicMock name='mock._task_llm_model.model'>`）或测试构造特征（`test-model`、`boom`、`weird`、`same tool warn=w`、`.pytest-*` 临时文件）——**全部是 pytest 测试直接构造 UniversalAgent 但不注册真实工具所得**
- **判据**: ①全部上下文为测试夹具/`pytest-147`；②09-07 真实运行 0 次；③真实会话工具由 `_loaded_categories` 初始 `{FUNDAMENTAL, SHELL, FILE}` 保证非空
- **结论**: **不是真实系统 bug，不修复**

---

### 问题3: LLM 流式取消与 429 限流

**严重度: 中 | 状态: 已修复(3b, 09-07) + 设计如此(3a) | 影响: 任务中断**

#### 3a: HTTP 429 限流耗尽
- **位置**: `llm_response_builder.py:99` / `client_sdk.py:226`
- **日志证据**: 09-07 `API配额/速率限制已耗尽(rpm exhausted)` → `quota_exceeded`
- **机理**: 调用 LLM API 超出 TPM/RPM 配额，`error_classifier.py:163` 分类为 `RATE_LIMIT`
- **当前处理**: `_is_recoverable_error(LLMResponseError)=True` → 可重试，但如果连续耗尽则最终失败

#### 3b: `Request cancelled` 取消风暴（病根确认+已修复）
- **位置**: `llm_call.py:228` 放行元组 / `llm_call.py:124-127` 取消中断异常 / `handle_answer.py:96-102` 取消终态
- **日志证据**: 09-07 真实 session `134656ce-3569-4dbb-bcc0-163c83984de0`：16:24:24 用户取消 → 16:24:41 `[Retry][L2] 第1/2次: LLM流式错误: Request cancelled` → 16:25:21 `第2/2次` → 16:25:22 `[FC降级] 降级到Text模式` → 16:27:13 才终态 FAILED（白白拖约 3 分钟）
- **病根（三遍核实）**: 用户取消 → `BaseAIService.cancel()` 置 `_cancelled=True` → LLM 流返回 `create_cancelled_chunk`（`stream_error="Request cancelled"`, `stream_error_type="cancelled"`）→ `err_type="cancelled"` **不在** `llm_call.py:228` 放行元组 `("quota_exceeded","rate_limit","idle_timeout","client")` → 被当可恢复错误 → L2 重试2次 + FC降级Text再调1次，**每次重试回来仍是 cancelled，纯白打3+次LLM**
- **终态矛盾**: 取消后 error payload 走到 `handle_answer.py` 旧 error 分支一律 `emit_failed_final` → `set_failed` → **覆盖已定的取消终态** → `chat_tasks.status=failed` 与 `chat_task_steps=cancelled` 自相矛盾
- **修复（2026-09-07 小欧）**:
  1. `llm_call.py:228` 放行元组补 `"cancelled"` → 取消 error 直接 yield 放行不重试不降级
  2. `llm_call.py:124-127` 取消中断异常由静默 return 改产 error payload(`exc_type="cancelled"`) → 杜绝空响应被 set_failed 误标
  3. `handle_answer.py:96-102` 新增 `err_type=="cancelled"` 特判 → `emit FinalStep(outcome="cancelled")` → react_dispatch 状态推断置 **CANCELLED**
- **验证**: 19项 llm_call + 28项 handler/react_step 测试全部通过，无回归

---

### 问题4: Windows 文件锁定 Error 32

**严重度: 低 | 状态: 已有重试机制 | 影响: 删除操作失败**

- **位置**: `delete_file.py:171` `[_force_delete_sync] 删除失败(重试3次后放弃)`
- **日志证据**: 09-07 出现 **20次**，涉及 `pytest-of-chend` 临时目录
- **病根**: Windows 文件锁定机制，进程仍持有文件句柄时删除失败
- **根因**: 测试（pytest）运行时，被测文件仍被其他测试进程/线程打开
- **当前处理**: 3次重试 + send2trash fallback + 永久删除 fallback（三层降级）
- **注意**: 09-07 日志中这些全部是 `pytest-of-chend` 路径，属于**测试自身**的文件锁竞争

---

### 问题5: Shell 命令执行异常（PowerShell 脚本块错误）

**严重度: 低 | 状态: LLM构造命令问题 | 影响: 单次执行失败**

- **位置**: `execute_shell_command.py:555`
- **日志证据**: 09-07 `python -c` 多行脚本在 PowerShell 中执行失败，`ScriptBlock should only be specified as a value of the Command parameter`
- **机理**: LLM 生成的 Python 单行脚本包含换行符，PowerShell 解析 `python -c` 时将多行参数误判为 ScriptBlock
- **根因**: LLM 不了解 Windows PowerShell 与 Bash 的差异，生成了 Bash 风格的多行命令
- **影响范围**: `rc=-1`（进程启动失败），`rc=2`（命令语法错误）

---

### 问题6: `LLMResponseError` FC 持续失败

**严重度: 中 | 状态: 仍存在 | 影响: 工具调用功能降级**

- **位置**: `llm_call.py:271` → `call_llm_with_fallback` / `llm_call.py:79` → `call_llm_stream`
- **日志证据**: 09-06 多次 `FC持续失败` / `server err` / `server: fc` / `server: bad fc format`
- **机理**: LLM 返回的 function call 格式不合法 → FC 解析失败 → 降级重试 → 重试耗尽抛 `LLMResponseError`
- **根因**: LLM 服务端不稳定，返回的 tool_call JSON 格式异常（可能是模型输出质量问题）
- **当前处理**: `_is_recoverable_error(LLMResponseError)=True` → 可重试

---

### 问题7: 遥测落库失败（降级不阻塞）

**严重度: 低 | 状态: 已有降级 | 影响: 遥测数据丢失**

- **日志证据**: 09-05 `致整表遥测落库失败(一行序列化拖垮全部指标, 与"降级不阻塞"声明相悖`
- **机理**: `agent_telemetry.py` 的 `finalize_and_persist()` 中，单条异常数据导致整表写入失败
- **根因**: 序列化时未隔离异常行，`json.dumps` 遇到不可序列化对象导致整个 INSERT 失败

---

## 三、问题严重度与修复建议汇总

| # | 问题 | 严重度 | 状态 | 修复建议 |
|---|------|--------|------|----------|
| 1 | `_process_single_step` return None | **高** | 已修复 | 单测补盲(验证所有return路径) |
| 2 | 无可用工具 | **中** | **证伪(测试构造)** | 不修复 |
| 3a | LLM 429 限流 | **中** | 设计如此 | 评估配额是否够用/增加退避时间 |
| 3b | LLM 取消风暴+终态矛盾 | **中** | **已修复(09-07)** | 放行元组补cancelled + handle_answer取消终态 |
| 4 | 文件锁定 Error32 | 低 | 已有重试 | 测试代码需 `with` 管理文件句柄 |
| 5 | Shell PowerShell 兼容 | 低 | LLM问题 | system prompt 增加 Windows 命令规范 |
| 6 | FC 持续失败 | **中** | 未修复 | 增加 FC 解析容错（JSON 修复/重发） |
| 7 | 遥测落库序列化 | 低 | 已有降级 | `json.dumps` 加 `default=str` 隔离异常行 |

---

## 四、统计结论

3天日志共 1573 条 ERROR，其中：
- **E2E 测试构造的异常** (占约 70%): `always fail` / `fail` / `corrupt write` / `disk full` / `mock offline` / `builder宕机` / `FC持续失败` / `MagicMock` / 各种 `unknown` 错误类型 — 这些是测试框架刻意构造的，**非真实 bug**
- **真实系统代码问题** (占约 15%): 问题 1-7 如上所述
- **LLM 外部依赖问题** (占约 10%): 429 限流、server err、取消
- **测试环境问题** (占约 5%): 文件锁、pytest 临时目录

**核心结论**: 真正需要修复的系统代码问题有 **2个高优先级**（问题2和问题6），其余已修复或有降级机制。
