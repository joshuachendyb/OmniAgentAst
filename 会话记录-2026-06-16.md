# 会话记录-2026-06-16

**创建时间**: 2026-06-16 13:46:35
**存放位置**: G:\OmniAgentAs-desk\

---

## 1 三系统工具注册架构对比分析 【完整分析文档】2026-06-16 13:46:35

### 1.1 Goal

对三个系统（Hermes、OmniAgentAs-desk、OpenCode）的工具注册架构进行深度对比分析，经过3轮代码复核修正后的最终版本。

### 1.2 Accomplished

经3轮代码复核，修正了6处差异，完成51个工具的完整清单。

---

## 2 系统概览

| 维度 | Hermes | OmniAgentAs-desk | OpenCode |
|------|--------|-----------------|----------|
| **注册方式** | 声明式 `@tool` 装饰器 + 函数式 `register()` | 纯函数式 `registry.register()`（无装饰器） | 函数式 `newTool()` / `Tool.make()` |
| **工具总数** | **35** | **51** | **~20**（built-in，插件不计） |
| **分类体系** | Toolset（9类） | ToolCategory（6类） | 无严格分类（按 domain 分） |
| **安全等级** | 无（通过 check_fn 自定义） | ToolSafetyLevel（5级） | 无（隐式信任） |
| **Schema 定义** | Pydantic v2 + 动态 schema 生成 | Pydantic v2 BaseModel + input_schema dict | Zod + TypeScript 类型 |
| **语言** | Python | Python | TypeScript（Hathora） |
| **所在目录** | `backend/app/services/tools/`（旧结构） | `backend/app/services/tools/` | `packages/plugin/src/` + `cli/` |

---

## 3 注册机制详细对比

### 3.1 Hermes 注册机制

**实际代码路径**: `OmniAgentAs-desk/backend/app/services/tools/registry.py`
**注意**: 当前代码库中 Hermes 已合并到 OmniAgentAs-desk，注册机制统一。

**注册方式**：纯函数式 `registry.register()` + 废弃的 `@register_tool()` 装饰器

**`register()` 方法签名**（共 15 个参数）：

```python
def register(
    self,
    name,                    # 必填
    description,             # 必填
    category,                # 必填
    implementation,          # 必填
    version = "1.0.0",       # 可选
    dependencies = None,     # 可选
    input_model = None,      # 可选
    input_schema = None,     # 可选
    output_schema = None,    # 可选
    examples = None,         # 可选
    expose_to_llm = True,    # 可选
    next_actions = None,     # 可选
    failure_hint_fn = None,  # 可选
    safety_level = SAFE,     # 可选
    action_safety_map = None,# 可选
) -> Dict[str, Any]:
```

**关键修正**: 15个参数（4必填+11可选），非13个。

**遗留的 `@register_tool()` 装饰器**（第 244-290 行）：仍然定义但**不再使用**，最后一批工具在 2026-05-02 重构期间已迁移至显式调用。

### 3.2 OmniAgentAs-desk 注册机制

同上（与 Hermes 共用同一 registry.py）。

**分类映射**（CATEGORY_MODULES，定义于 `tool_constants.py`）：

| 旧分类名 | 映射到模块 | 注册函数 |
|---------|-----------|---------|
| `"file"` | `app.services.tools.file` | `_register_file_tools` |
| `"shell"` | `app.services.tools.shell` | `_register_shell_tools` |
| `"network"` | `app.services.tools.network` | `_register_network_tools` |
| `"system"` | `app.services.tools.system` | `_register_system_tools` |
| `"desktop"` | `app.services.tools.desktop` | `_register_desktop_tools` |
| `"document"` | `app.services.tools.document` | `_register_document_tools` |
| `"meta"` | `app.services.tools.meta` | `_register_meta_tools` |

所有注册函数签名：`_register_*_tools() -> None`，无参数，内部循环调用 `tool_registry.register()`。

### 3.3 OpenCode 注册机制

**架构特征**（基于 Hathora 函数式架构）：

| 维度 | 说明 |
|------|------|
| **核心类型** | `Tool`（不可变记录）、`ToolDefinition`（序列化格式）、`PluginTool`（插件形式） |
| **注册方式** | `Tool.make()` 工厂方法（4个重载）+ `newTool()` 便捷函数 |
| **Schema** | Zod 对象 + `JsonSchema7Type` 转换 |
| **插件工具** | `PluginTool` 从 `plugin.json` 读取，通过 `pluginToolDefToDefinition()` 转换 |
| **LLM 格式化** | `lowerTool()` → OpenAI tool_choice 格式 |
| **运行时派发** | `Effect.forEach(tools, runTool)` → 并行执行 |
| **工具来源** | 内建 `built-in/` + 插件 `plugins/` + 用户自定义 |

---

## 4 分类体系对比

### 4.1 OmniAgent 分类（ToolCategory，6类）

| 类别 | value | order | name_cn | 工具数 |
|------|-------|-------|---------|--------|
| FILE | `"file"` | 0 | 文件操作工具 | 10 |
| FUND_RUNTIME | `"fund_runtime"` | 1 | 基础运行时工具 | 10 |
| NET_PROCESS | `"net_process"` | 2 | 网络与进程工具 | 6 |
| SCREEN | `"screen"` | 3 | 屏幕交互工具 | 7 |
| DOC_CONTENT | `"doc_content"` | 4 | 文档内容工具 | 9 |
| SYSTEM | `"system"` | 5 | 系统管理工具 | 9 |
| **总计** | | | | **51** |

### 4.2 OpenCode 分类（松散分组）

| 分组 | 说明 | 工具数 |
|------|------|--------|
| **built-in** | 内建工具，编译进 CLI | ~10 |
| **plugin tools** | 从插件 plugin.json 声明 | 动态 |
| **MCP tools** | MCP server 工具 | 动态 |

无严格分类枚举，按 domain 自然分组。

---

## 5 安全体系对比

### 5.1 OmniAgent 安全体系（ToolSafetyLevel）

**5 级安全等级**（定义于 `tool_types.py:60-70`）：

| 级别 | value | needs_confirmation | needs_safety_check |
|------|-------|-------------------|-------------------|
| READ_ONLY | `"read_only"` | false | false |
| SAFE | `"safe"` | false | false |
| DESTRUCTIVE | `"destructive"` | true | true |
| DANGEROUS_SANDBOX | `"dangerous_sandbox"` | true | true |
| DANGEROUS | `"dangerous"` | true | true |

**action_safety_map 机制**：工具可对不同 action 设置不同安全级别。

**执行检查链**：
1. `ToolExecutor.execute()` → 检查 safety_level
2. 需要确认 → 发送确认请求
3. 需要安全检查 → `command_security.py` 黑名单检查
4. 通过后执行 → 返回结果

### 5.2 OpenCode 安全体系

无显式安全等级系统。安全性通过以下方式保障：

| 机制 | 说明 |
|------|------|
| **permit 字段** | 部分工具标记 `permit: true` 需用户确认 |
| **Effect 隔离** | 函数式 Effect 确保无副作用泄漏 |
| **MCP 协议** | 通过子进程运行 MCP server，sandbox 隔离 |

---

## 6 工具数量详细清单

### 6.1 OmniAgent 全量工具清单（51个）

#### FILE（10个）
read_text_file, write_text_file, read_media_file, edit_text_file, list_directory, search_files, grep_file_content, archive_tool, file_operation, data_file_format

#### FUND_RUNTIME（10个）
- Shell（4）: execute_shell_command, find_command, shell_session, execute_code
- Meta（6）: tool_search, time_now, time_add, time_diff, query_calendar, timer

#### NET_PROCESS（6个）
http_request, download_file, fetch_webpage, search_web, network_diagnose, net_connections

#### SYSTEM（9个）
get_system_info, event_log, list_processes, get_env, kill_process, service_control, task_control, set_env, registry_control

#### SCREEN（7个）
window_info, window_control, mouse_control, keyboard_control, screen_capture, clipboard_control, send_notification

#### DOC_CONTENT（9个）
read_document, write_document, convert_document, analyze_data, filter_data, generate_chart, query_sql, execute_sql, get_db_schema

### 6.2 OpenCode 内建工具清单

Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch, Task, Question

---

## 7 关键修正记录

| # | 最初结论 | 复核后修正 | 修正原因 |
|---|---------|-----------|---------|
| 1 | `register()` 有13个参数 | 实际**15个**（4必填+11可选） | 漏计 `action_safety_map` 和 `safety_level` |
| 2 | ToolSafetyLevel 有5级 | ✅ **确认5级** | 首次即正确 |
| 3 | CATEGORY_MODULES 有6项 | 实际**7项**（shell+meta 分开） | 误以为合并后也合并映射 |
| 4 | Hermes 工具数 21 | OmniAgent 统一后 **51** | Hermes已合并到OmniAgent代码库 |
| 5 | OpenCode 无安全机制 | 有 `permit` 字段 + Effect 隔离 | 阅读源码后确认 |
| 6 | OpenCode `Tool.make()` 2重载 | 实际**4重载**（2 typed + 2 dynamic） | 阅读源码后确认 |

---

## 版本历史

| 版本 | 时间 | 签名 | 更新内容 |
|------|------|------|---------|
| v1.0 | 2026-06-16 13:46:35 | 小沈 | 初始版本，三系统工具注册架构对比分析，经3轮代码复核修正 |

