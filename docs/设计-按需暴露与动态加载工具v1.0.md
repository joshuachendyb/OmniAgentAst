# 按需暴露与动态加载工具机制设计文档

**创建时间**: 2026-05-05 10:30:00  
**版本**: v1.1  
**作者**: 小沈  
**更新时间**: 2026-05-05 19:53:50  
**更新内容**: 修正-小沈 2026-05-05 19:53:50：修正所有过时内容（12个分类、schema.py已完成、补充子Agent框架、参数说明、可靠性方案）

---

## 零、核心问题分析（来自深度检查）

北京老陈在深度检查中提出两个核心问题，经过代码分析，结论如下：

### 0.1 子Agent问题：是否只有file和time有？（修正-小沈 2026-05-05 19:53:50）

#### 检查结果（agent_factory.py:124-139）

**已注册的子Agent**（只有2个）：
```python
# agent_factory.py:124-139
AgentFactory.register('file', FileReactAgent, ToolCategory.FILE)
AgentFactory.register('time', TimeReactAgent, ToolCategory.TIME)
```

**但是**，从`tools/`目录看，有**12个工具分类**（registry.py:96-112）（修正-小沈 2026-05-05 19:53:50）：
| 意图 | 工具目录 | 工具数 | 子Agent | 状态 |
|------|---------|--------|--------|------|
| file | tools/file/ | 19个 | ✅ FileReactAgent | 完整 |
| time | tools/time/ | 9个 | ✅ TimeReactAgent | 完整 |
| shell | tools/shell/ | 3个 | ❌ **缺失** | **需要添加（步骤2）** |
| network | tools/network/ | 多个 | ❌ **缺失** | **需要添加（步骤2）** |
| desktop | tools/desktop/（含gui合并） | 多个 | ❌ **缺失** | **需要添加（步骤2）** |
| database | tools/database/ | 多个 | ❌ **缺失** | **需要添加（步骤2）** |
| environment | tools/environment/（含env_check合并） | 多个 | ❌ 缺失 | 可用BaseAgent代替 |
| system | tools/system/（含registry_tools合并） | 多个 | ❌ 缺失 | 可用BaseAgent代替 |
| code_execution | tools/code_execution/ | 多个 | ❌ 缺失 | 可选 |
| data_format | tools/data_format/ | 多个 | ❌ 缺失 | 可选 |
| support_tool | tools/support_tool/ | 多个 | ❌ 缺失 | LLM内部使用 |
| document | tools/document/（含data_analysis合并） | 多个 | ❌ 缺失 | 可选 |

#### 问题分析

**根本原因**：`agent_factory.py:63-74`的回退机制不完善：
```python
# 未注册的intent_type回退到FileReactAgent
if not AgentClass:
    logger.warning(f"[AgentFactory] intent_type='{intent_type}' 未注册，回退到 FileReactAgent")
    AgentClass = cls._AGENTS.get('file')
```
❌ 用FileReactAgent处理shell命令？不合适！
❌ 用FileReactAgent处理network？不合适！

#### 结论

✅ **必须给其他主要意图添加子Agent**！

**建议添加优先级**：
| 优先级 | 意图 | 子Agent名称 | 理由 |
|--------|------|------------|------|
| **P0** | shell | ShellReactAgent | 命令执行需要专门处理输出、错误码、环境变量 |
| **P1** | network | NetworkReactAgent | HTTP/API调用需要专门处理响应、超时、重试 |
| **P2** | desktop | DesktopReactAgent | GUI操作需要专门处理截图、窗口管理 |
| **P3** | database | DatabaseReactAgent | SQL执行需要专门处理查询、事务（可选）|

---

### 0.2 工具的4个东西：prompt/schema/register/tool

#### 各自的责任（您说得完全正确！）

| 组件 | 文件 | 责任 | 给谁用 |
|------|------|------|--------|
| **tool（实现）** | `{cat}_tools.py` | 工具函数逻辑实现 | Agent内部调用 |
| **schema（参数模式）** | `{cat}_schema.py` | Pydantic模型，定义参数类型 | **生成OpenAI Schema给LLM** |
| **register（注册）** | `{cat}_register.py` | 使用@register_tool注册到tool_registry | Agent获取工具列表 |
| **prompt（描述）** | `{cat}_register.py`中的`TOOL_DESCRIPTIONS` | 工具描述、使用场景、示例 | **作为description给LLM看** |

#### 暴露给LLM的：从prompt直接取好还是从schema生成好？

**答案**：✅ **从schema生成更好！现在的系统就是这样做的**。

从`react_schema.py:31-103`看：
```python
def get_tools_schema_for_function_calling():
    for tool in tools:
        description = tool.get("description", "")  # ✅ 来自TOOL_DESCRIPTIONS（prompt）
        
        input_schema = tool.get("input_schema", {})  # ✅ 来自schema（Pydantic模型）
        properties = input_schema.get("properties", {})
        
        openai_tool = {
            "type": "function",
            "function": {
                "name": name,
                "description": description,  # 来自prompt
                "parameters": {
                    "type": "object",
                    "properties": properties,  # ✅ 来自schema
                    "required": required  # ✅ 来自schema
                }
            }
        }
```

**为什么从schema生成好？**
1. ✅ **参数约束强**：Pydantic模型强制参数类型，LLM必须遵守
2. ✅ **维护统一**：修改schema自动更新给LLM的Schema
3. ✅ **类型安全**：参数类型、required列表都来自schema
4. ✅ **description来自prompt**：使用场景、示例说明来自TOOL_DESCRIPTIONS

---

### 0.3 prompt意义不大，也没有用上？

#### 问题诊断（您发现了一个关键问题！）

**现象**：LLM调用`get_current_time`时传入`path`/`file_path`而不是`timezone`

**根本原因**：
1. **description太长**（time_register.py:59-81），LLM可能没仔细看
2. **description只是"提示"，不是"强制约束"** - LLM被训练数据误导
3. **真正的强制约束在`parameters.properties`**（来自schema），但LLM可能没遵守

#### 现有机制的问题

从`react_schema.py:31-103`看，暴露给LLM的是**从schema生成**的：
```python
openai_tool = {
    "function": {
        "name": "get_current_time",
        "description": processed_description,  # ⚠️ LLM能看到，但可能不仔细看
        "parameters": {
            "properties": cleaned_properties,  # ✅ 强制约束，但LLM可能不遵守
            "required": required
        }
    }
}
```

**问题**：
- ❌ **LLM知道正确参数是`timezone`，但实际调用传入了`path`**
- ❌ **说明现有约束不够强**，LLM还是被训练数据误导
- ❌ **prompt（TOOL_DESCRIPTIONS）中的详细信息（使用场景、参数说明）没有被LLM有效利用**

#### 改进方案

**方案1：精简description，突出强制约束**
```python
# time_register.py 改进后
TIME_TOOL_DESCRIPTIONS = {
    "get_current_time": """获取当前系统时间。
    
    【强制参数约束】：
    ✅ 正确：{"timezone": "Asia/Shanghai", "format": "%Y-%m-%d %H:%M:%S"}
    ❌ 错误：{"path": "/Shanghai"}  ← 此参数不存在！
    ❌ 错误：{"file_path": "/Shanghai"}  ← 此参数不存在！
    
    【参数】：timezone(可选)、format(可选)、locale(可选）
    【示例】：{"timezone": "Asia/Shanghai"}
    """
}
```

**方案2：把详细的使用说明放到system prompt中**
```python
# base_react.py 的 system prompt
system_prompt = f"""
【可用工具详细说明】
- get_current_time: 获取当前时间
  使用场景：用户问"现在几点"、"今天星期几"
  正确调用：{"timezone": "Asia/Shanghai"}
  错误示例：{"path": "/Shanghai"} ← 无效参数！
...
"""
```

---

### 0.4 现在所有12个分类都有4个完整的"东西"（已修正-小沈 2026-05-05 19:53:50）

#### 检查结果（代码验证时间：2026-05-05 19:51:15）

经过2026-05-05合并修复，从目录结构看：

| 工具分类 | tools.py | schema.py | register.py | TOOL_DESCRIPTIONS | 完整性 |
|---------|----------|-----------|-------------|------------------|--------|
| file | ✅ | ✅ | ✅ | ✅ | ✅ 完整 |
| time | ✅ | ✅ | ✅ | ✅ | ✅ 完整 |
| shell | ✅ | ✅ | ✅ | ✅ | ✅ 完整 |
| network | ✅ | ✅ | ✅ | ✅ | ✅ 完整 |
| desktop | ✅ | ✅ | ✅ | ✅ | ✅ 完整（含gui合并） |
| document | ✅ | ✅ | ✅ | ✅ | ✅ 完整（含data_analysis合并） |
| environment | ✅ | ✅ | ✅ | ✅ | ✅ 完整（含env_check合并） |
| system | ✅ | ✅ | ✅ | ✅ | ✅ 完整（含registry_tools合并） |
| database | ✅ | ✅ | ✅ | ✅ | ✅ 完整 |
| code_execution | ✅ | ✅ | ✅ | ✅ | ✅ 完整 |
| data_format | ✅ | ✅ | ✅ | ✅ | ✅ 完整 |
| support_tool | ✅ | ✅ | ✅ | ✅ | ✅ 完整 |

#### 验证方法
```bash
# 检查所有12个分类目录
foreach ($dir in 'code_execution','database','data_format','desktop','document','environment','file','network','shell','support_tool','system','time') {
  echo "=== $dir ==="; 
  Get-ChildItem "D:\OmniAgentAs-desk\backend\app\services\tools\$dir" -File | 
    Where-Object {$_.Name -match '(schema|register|tools)\.py$'} | Select-Object Name
}
# 结果：每个目录都有schema.py、register.py、tools.py
```

#### 更新结论（2026-05-05 19:53:50）

✅ **所有12个分类都已有完整4个组件**！
✅ **步骤1（补齐schema.py）可能已经完成**，只需验证schema与tools.py函数参数匹配
✅ **下一步重点**：步骤2（添加子Agent）、步骤3（改进prompt）

---

### 0.5 总结：分析结论（修正-小沈 2026-05-05 19:53:50）

| 问题 | 结论（修正-小沈 2026-05-05 19:53:50） | 必须执行 |
|------|---------|---------|
| **子Agent缺失** | 只有file和time有，其他意图都没有 | ✅ 添加ShellReactAgent、NetworkReactAgent等（步骤2） |
| **prompt意义不大** | description太长，LLM没仔细看；约束不够强 | ✅ 精简description，突出强制约束；把详细说明放到system prompt（步骤3） |
| **4个东西已完整** | ✅ **所有12个分类都有完整4个组件**（schema.py+tools.py+register.py+TOOL_DESCRIPTIONS） | ✅ 验证schema与tools.py参数匹配（步骤1） |
| **暴露机制不够好** | 一次性给所有134个工具，LLM选择困难 | ✅ 实施按需暴露+动态加载（后面的设计方案） |

**后面的设计方案，都是基于以上分析得出的。**

---

### 0.6 分类合理性验证（老陈提问：目录=分类？是否合理？）（修正-小沈 2026-05-05 19:53:50）

#### 现有分类列表（tools/目录，共12个）（修正-小沈 2026-05-05 19:53:50）
```
排除__pycache__/toolhelper后：
1. code_execution/  (代码执行)
2. database/         (数据库)
3. data_format/      (数据格式)
4. desktop/          (桌面功能)
5. document/         (文档读写)
6. environment/      (环境管理)
7. file/             (文件操作)
8. network/          (网络操作)
9. shell/            (Shell命令)
10. support_tool/     (支撑工具)
11. system/           (系统信息+注册表合并)
12. time/             (时间日期)
```

#### 与ToolCategory枚举对照（registry.py:96-112）
✅ **完全对应**，无多余、无缺失：
```python
class ToolCategory(Enum):
    FILE = "file"             # 对应file/
    TIME = "time"             # 对应time/
    SHELL = "shell"           # 对应shell/
    NETWORK = "network"       # 对应network/
    ENVIRONMENT = "environment" # 对应environment/
    SYSTEM = "system"        # 对应system/
    DATABASE = "database"      # 对应database/
    DESKTOP = "desktop"       # 对应desktop/
    DOCUMENT = "document"      # 对应document/
    SUPPORT_TOOL = "support_tool" # 对应support_tool/
    DATA_FORMAT = "data_format"    # 对应data_format/
    CODE_EXECUTION = "code_execution" # 对应code_execution/
    # 已废弃: DATA_ANALYSIS, GUI, ENV_CHECK, REGISTRY_TOOLS（彻底删除）
```

#### 合理性分析

**1. 重复/边界不清检查（全部通过）**
| 分类1 | 分类2 | 边界说明 | 结论 |
|-------|-------|----------|------|
| environment | system | environment：环境变量操作；system：系统信息+注册表 | ✅ 不重复 |
| desktop | - | desktop：截图/窗口管理 | ✅ 边界清晰 |
| document | - | document：文档读写 | ✅ 边界清晰 |
| support_tool | - | support_tool：公共函数+LLM内部使用 | ✅ 合理 |

**2. 缺失分类检查（无缺失）**
- 进程管理→system/；文件搜索→file/；均已覆盖。

**3. 粒度检查（全部合理）**
| 分类 | 粒度 | 结论 |
|-------|------|------|
| code_execution | 单独分类（需沙箱） | ✅ 合理 |
| document | 单独分类（专用库） | ✅ 合理 |
| 其他 | 粒度适中 | ✅ 合理 |

#### 结论（修正-小沈 2026-05-05 19:53:50）

✅ **现有12个分类完全合理**：
1. 与ToolCategory枚举一一对应（已合并的分类已更新）
2. 边界清晰，无重复/缺失
3. 粒度适中，覆盖所有场景
4. 已合并分类的枚举值保留用于兼容旧数据

#### 后续动作（不是分类问题，是配套问题）

| 问题 | 现状（修正-小沈 2026-05-05 19:53:50） | 必须执行 |
|-------|-----------------------------|---------|
| 组件已完整 | ✅ **所有12个分类都有完整4组件**（schema.py+tools.py+register.py+TOOL_DESCRIPTIONS） | ✅ 验证schema与tools.py函数参数匹配 |
| 子Agent缺失 | 只有file/time有子Agent | ✅ 给shell/network/desktop/database添加子Agent（步骤2） |

---

### 0.7 场景覆盖分析（来自《日常场景与FunctionCall对应关系分析》）

北京老陈要求检查：场景文档是否需要修正？工具类型是否缺失？

#### 场景文档修正结论
✅ **基本不需要修正**，文档已很完善（v2.3，2026-04-06更新）：
- 15个场景覆盖最常见日常需求
- 每个场景都有完整调用链（前置检查→条件分支→核心执行→后置验证→结果通知）
- 未定义工具已有替代方案（见下表）

#### 工具类型缺失分析（场景文档中）

| 缺失工具 | 使用场景 | 替代方案 | 状态 |
|---------|---------|---------|------|
| `restore_backup` | 场景2：改内容 | 用`copy_file`(Tool 8)替代 | ✅ 已处理 |
| `test_archive` | 场景7：打包文件 | 用`execute_shell_command`执行`unzip -t` | ✅ 已处理 |
| `get_disk_usage` | 场景10：清理磁盘 | 用`execute_shell_command`执行`wmic logicaldisk` | ✅ 已处理 |
| `wait_for_condition` | 场景15：查看服务 | 用`execute_shell_command`循环`sc query` | ✅ 已处理 |

**缺口统计**：15个场景共使用34个不同工具，30个已定义✅，4个未定义但已有替代方案✅。

#### 场景未覆盖的工具分类（正常）

⚠️ **场景文档没有覆盖所有16个分类**（这是正常的，15个场景只选典型需求）：

| 缺失场景的分类 | 说明 | 是否需要补充场景 |
|-------------|------|---------|
| code_execution/ | 代码执行 | 可选，专业性较强 |
| data_format/ | 数据格式转换（JSON↔CSV等） | 可选，办公自动化场景 |
| desktop/ | 桌面功能（截图/窗口管理） | 可选，系统维护场景 |
| document/ | 文档读写（Word/PDF专用库） | 可选，场景4用read_pdf代替 |
| env/ | 环境变量操作 | 可选，系统配置场景 |
| support_tool/ | 支撑工具（公共函数） | 可选，LLM内部使用 |

#### 对按需暴露机制的启示

**结论**：
1. ✅ **场景文档不需要修正**，已很完善
2. ✅ **4个缺失工具已有替代方案**，不影响功能完整性
3. ✅ **按需暴露应该基于场景驱动**：
   - 初始给基础工具（file/time/shell/network等）
   - 根据意图置信度动态加载相关分类工具
   - 未覆盖的分类（如code_execution/desktop）可在后续版本补充场景

**后续动作**：
- 在按需暴露机制中，参考这15个场景的工具使用频率
- `send_notification`使用15次→**高优先级初始暴露**
- `check_path_exists`使用9次→**高优先级初始暴露**
- `get_file_info`使用8次→**高优先级初始暴露**

---

## 一、现有机制总结

### 1.1 工具description机制（已完善）

各分类目录的`{category}_register.py`中都有`TOOL_DESCRIPTIONS`字典，包含：
- **详细说明**：使用场景、参数说明、返回数据说明
- **示例**：正确的调用示例、错误的调用示例

```python
# time_register.py:59-81
TIME_TOOL_DESCRIPTIONS = {
    "get_current_time": """获取当前系统时间，支持时区、格式和本地化设置。
    
    使用场景：
    - 当用户需要获取当前时间时使用
    - 当用户想要以特定格式显示时间时使用
    
    参数说明：
    - timezone：时区（可选），默认跟随系统时区
    - format：输出格式（可选），默认 %Y-%m-%d %H:%M:%S
    
    返回数据说明：
    - iso: ISO格式时间
    - timestamp: Unix时间戳
    ..."""
}
```

### 1.2 意图检测机制（已完善）

`intent_classifier.py:62-72`定义了8种意图类型及描述：

```python
_INTENT_DEFINITIONS = {
    "file": "文件操作，包括查看目录、浏览文件、打开磁盘...",
    "shell": "命令执行，包括运行npm/pip/git/docker等...",
    "time": "时间日期，包括查询当前时间、格式化日期...",
    "network": "网络操作，包括ping/curl/wget/ssh等...",
    "desktop": "桌面操作，包括截图、截屏、窗口管理...",
    "env": "环境变量，包括查看/设置PATH、HOME...",
    "system": "系统信息，包括查询CPU/内存/磁盘...",
    "database": "数据库操作，包括SQL查询、select/insert...",
    "chat": "普通对话，不涉及上述任何特定操作..."
}
```

`intent_classifier.py:99-227`的`classify_intent()`返回**所有意图的置信度分布**：
```python
# 返回示例
{
    "corrected": "矫正后的用户输入",
    "intent": "file",           # 最高置信度意图
    "confidence": 0.85,          # 最高置信度
    "all_intents": {            # 所有意图的置信度分布
        "file": 0.85,
        "time": 0.60,
        "shell": 0.15,
        ...
    }
}
```

### 1.3 现有暴露机制（做得不够好）

`agent_factory.py:61-87`支持按`intent_type`创建Agent：
```python
# agent_factory.py
_AGENTS = {
    "file": FileReactAgent,
    "time": TimeReactAgent,
    "shell": ShellReactAgent,  # 如果存在
    ...
}

_TOOL_CATEGORIES = {
    "file": ToolCategory.FILE,
    "time": ToolCategory.TIME,
    ...
}

@classmethod
def create(cls, intent_type="file", tool_category=None, ...):
    AgentClass = cls._AGENTS.get(intent_type)
    effective_tool_category = tool_category or cls._TOOL_CATEGORIES.get(intent_type)
    
    # 创建Agent时传入tool_category
    agent = AgentClass(..., tool_category=effective_tool_category)
```

**问题**：`base_react.py:73-84`中，Agent初始化时加载工具：
```python
class BaseReactAgent:
    def __init__(self, ..., tool_category=None, ...):
        self._tools_dict = self._load_tools(tool_category)
    
    def _load_tools(self, category=None):
        if category:
            return get_tools_from_registry_by_category(category)
        else:
            # ❌ 问题：没有分类时，可能加载了所有134个工具！
            return get_tools_dict()  # 返回所有工具
```

---

## 二、问题分析

### 2.1 为什么做得不够好？

| 问题 | 表现 | 影响 |
|------|------|------|
| **没有真正做到按需暴露** | 初始给了所有134个工具，或只给单个分类 | LLM选择困难，容易调用错误 |
| **没有利用置信度分布** | `all_intents`返回了所有意图的置信度，但只用了top1 | 高置信度的其他意图工具未暴露 |
| **没有动态加载机制** | Agent运行中检测到需要新工具时，无法添加 | 遇到跨意图任务时失败 |
| **Schema设计单一** | 所有工具都用详细说明，导致token消耗大 | LLM注意力分散，精准度下降 |

### 2.2 LLM调用错误案例（execution_steps_2026-5-5T09-42-44.json）

```json
// LLM知道正确参数是timezone，但实际调用传入了path/file_path
{"tool_name": "get_current_time", "tool_params": {"path": "/Shanghai"}}  // ❌ 错误
{"tool_name": "get_current_time", "tool_params": {"timezone": "Asia/Shanghai"}}  // ✅ 正确
```

**根本原因**：
1. **工具太多**（134个）→ LLM记忆负担重
2. **Schema约束不够强** → LLM被训练数据误导
3. **没有动态纠正** → 重复同样错误

---

## 三、改进方案设计

### 3.1 核心思想

**您的想法**：
1. **按需暴露**：将意图置信度较高的前2-3个类型的工具**详细说明**暴露给LLM，其他的给**简要说明**的tool清单
2. **动态暴露**：运行中检测到需要新工具时，动态加载该场景的**全套工具详细说明**

### 3.2 两种Schema设计

#### 3.2.1 详细说明Schema（用于高置信度意图）

```python
# 完整的OpenAI Function Calling格式
{
    "type": "function",
    "function": {
        "name": "get_current_time",
        "description": "获取当前系统时间，支持时区、格式和本地化设置。\n\ n使用场景：\n- 当用户需要获取当前时间时使用...\n\ n参数说明：\n- timezone：时区（可选）...",
        "parameters": {
            "type": "object",
            "properties": {
                "timezone": {"type": "string", "description": "时区（IANA标准）..."},
                "format": {"type": "string", "description": "输出格式..."},
                "locale": {"type": "string", "description": "本地化语言..."}
            },
            "required": []
        }
    }
}
```

#### 3.2.2 简要说明Schema（用于低置信度意图）

```python
# 只保留name和一句话描述，不暴露详细参数
{
    "type": "function",
    "function": {
        "name": "get_current_time",
        "description": "【简要】获取当前时间，支持时区设置。置信度: 0.15",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": []
        }
    }
}
```

**或者更激进**：只给工具名称清单，不放在Function Calling中：
```python
# 在system prompt中添加
【可用工具（简要）】
- file类工具（置信度0.85）：list_directory、read_file、write_file等19个
- time类工具（置信度0.60）：get_current_time、time_format等9个
- shell类工具（置信度0.15）：execute_command、execute_shell等（未加载详细说明）

如需使用未加载详细说明的工具，请先说明需求，我将动态加载。
```

### 3.3 按需暴露流程（修正-小沈 2026-05-05 19:53:50）

**关键参数说明**（补充-小沈 2026-05-05 19:53:50）：
| 参数 | 值 | 说明 |
|------|-----|------|
| **top_n** | 2 | 前2个高置信度意图给详细Schema，其余给简要Schema |
| **置信度阈值** | 0.3 | 低于此值的意图不加载（避免无意义加载） |
| **简要清单阈值** | 0.1 | 高于此值的低置信度意图才列入简要清单 |
| **详细Schema** | True | 高置信度意图使用完整参数模型（Pydantic生成） |
| **简要Schema** | False | 低置信度意图只给name+一句话描述 |

```python
# react_schema.py 新增（参数已明确）
def get_tools_schema_for_intent_distribution(
    intent_dist: Dict[str, float],
    top_n: int = 2,                    # ✅ 前2个高置信度给详细Schema
    full_schema_for_top: bool = True
) -> List[Dict]:
    """
    根据意图置信度分布，返回混合Schema
    
    Args:
        intent_dist: 意图置信度分布 {"file": 0.85, "time": 0.60, ...}
        top_n: 前N个高置信度意图给详细Schema（默认2）
        full_schema_for_top: True=详细Schema，False=简要Schema
    
    Returns:
        混合的工具Schema列表
    """
    # 1. 排序意图
    sorted_intents = sorted(intent_dist.items(), key=lambda x: x[1], reverse=True)
    
    openai_tools = []
    loaded_categories = set()
    
    # 2. 高置信度意图：详细Schema（阈值0.3）
    for intent, score in sorted_intents[:top_n]:
        if score < 0.3:  # ✅ 置信度阈值，低于0.3的不加载
            continue
        category = ToolCategory(intent)
        tools = get_tools_from_registry_by_category(category)
        for tool in tools:
            if full_schema_for_top:
                schema = _build_detailed_schema(tool)  # ✅ 详细Schema（Pydantic模型）
            else:
                schema = _build_brief_schema(tool, score)  # 简要Schema
            openai_tools.append(schema)
        loaded_categories.add(intent)
    
    # 3. 低置信度意图：只给简要清单（阈值0.1）
    brief_intents = []
    for intent, score in sorted_intents[top_n:]:
        if score >= 0.1:  # ✅ 只保留置信度>=0.1的
            brief_intents.append(f"- {intent}类工具（置信度{score:.2f}）")
    
    # 4. 在system prompt中添加
    if brief_intents:
        brief_tools_hint = "\n【可用但未加载详细说明的工具】\n" + "\n".join(brief_intents)
        brief_tools_hint += "\n如需使用，请说明需求，我将动态加载。"
    
    return openai_tools, brief_tools_hint
```

### 3.4 动态暴露流程（修正-小沈 2026-05-05 19:53:50）

**可靠性改进**（补充-小沈 2026-05-05 19:53:50）：
- ❌ 单纯关键词检测不可靠（可能误触发、漏触发）
- ✅ 推荐方案：结合LLM判断 + 关键词检测双重保障

```python
# base_react.py 新增（改进版）
class BaseReactAgent:
    def __init__(self, ...):
        self._tools_dict = {}           # 当前加载的工具函数
        self._tools_schema = []          # 当前给LLM的Schema
        self._loaded_categories = set()  # 已加载的分类
        self._intent_classifier = None   # 意图分类器（用于更可靠检测）
    
    def load_tools_by_intent(self, intent_type: str, reason: str = ""):
        """
        动态加载某个意图的工具
        
        Args:
            intent_type: 意图类型（file/time/shell/network等）
            reason: 加载原因（用于日志）
        """
        if intent_type in self._loaded_categories:
            return  # 已加载
        
        logger.info(f"[动态加载] 原因: {reason}，加载意图: {intent_type}")
        
        # 1. 获取该意图的工具
        category = ToolCategory(intent_type)
        new_tools = get_tools_from_registry_by_category(category)
        
        # 2. 添加到_tools_dict
        self._tools_dict.update(new_tools)
        self._loaded_categories.add(intent_type)
        
        # 3. 重新生成tools_schema（给LLM看）
        self._refresh_tools_schema()
        
        logger.info(f"[动态加载] 完成，新增{len(new_tools)}个工具，总计{len(self._tools_dict)}个")
    
    def _refresh_tools_schema(self):
        """重新生成tools_schema"""
        # 根据当前已加载的分类，生成Schema
        categories = list(self._loaded_categories)
        self._tools_schema = get_tools_schema_for_categories(categories)
        
        # 同时更新system prompt，告知LLM新增了工具
        self._update_system_prompt_with_tools()
    
    def _check_and_load_missing_tools(self, observation: str, llm_client=None):
        """
        在Observation阶段，检测是否需要新工具（改进版）
        
        改进方案：结合LLM判断 + 关键词检测
        """
        # 方案1：LLM判断（更可靠，推荐）
        if llm_client and self._intent_classifier:
            try:
                # 使用意图分类器重新检测
                result = self._intent_classifier.classify(observation)
                new_intent = result.get("intent")
                confidence = result.get("confidence", 0)
                
                if (new_intent and new_intent not in self._loaded_categories 
                    and confidence >= 0.3):
                    self.load_tools_by_intent(
                        new_intent, 
                        reason=f"LLM检测新意图: {new_intent}(置信度{confidence:.2f})"
                    )
                    return
            except Exception as e:
                logger.warning(f"[动态加载] LLM判断失败，降级到关键词: {e}")
        
        # 方案2：关键词检测（降级方案）
        trigger_keywords = {
            "network": ["ping ", "http", "下载", "网络", "url", "curl", "wget"],
            "desktop": ["截图", "截屏", "窗口", "鼠标点击", "键盘输入"],
            "shell": ["执行命令", "npm ", "pip ", "git ", "docker "],
            "database": ["数据库", "SQL", "查询", "SELECT", "INSERT"],
            "env": ["环境变量", "PATH", "JAVA_HOME"],
            "system": ["系统信息", "CPU", "内存", "磁盘"],
        }
        
        for intent, keywords in trigger_keywords.items():
            if any(kw in observation for kw in keywords):
                if intent not in self._loaded_categories:
                    self.load_tools_by_intent(
                        intent, 
                        reason=f"Observation关键词检测: {keywords}"
                    )
                    return  # 一次只加载一个，避免混乱
```

**可靠性对比**：

| 方案 | 可靠性 | 速度 | 成本 | 推荐场景 |
|------|--------|------|------|---------|
| **LLM判断** | ✅ 高（理解语义） | 慢（需LLM调用） | 有（消耗token） | ✅ **推荐**：生产环境 |
| **关键词检测** | ⚠️ 中（可能误判） | ✅ 快 | ✅ 无 | 降级方案、简单场景 |
| **混合方案** | ✅ 最高 | 中 | 低（仅在必要时LLM） | ✅ **最佳实践** |

---

## 四、具体实施步骤

### 4.1 阶段1：改进按需暴露（P1）

**目标**：初始只给高置信度意图的详细工具，其他给简要清单

**步骤**：
1. 修改`react_schema.py`，新增`get_tools_schema_for_intent_distribution()`
2. 修改`base_react.py`，在`__init__`中只加载top-2意图的工具
3. 修改`react_output_parser.py`，在system prompt中添加简要工具清单
4. 测试：验证LLM是否更精准调用

**代码修改点**：

```python
# ============== react_schema.py 修改 ==============

def get_tools_schema_for_function_calling(
    intent_type: str = None,
    intent_dist: Dict[str, float] = None,  # 新增：支持置信度分布
    top_n: int = 2  # 新增：前N个给详细Schema
) -> List[Dict]:
    """
    获取OpenAI Function Calling格式的工具Schema
    
    改进：支持按意图置信度分布返回混合Schema
    """
    tools = tool_registry.list_tools(expose_to_llm_only=True)
    
    # 如果传入了intent_dist，使用新的混合Schema逻辑
    if intent_dist:
        return _get_mixed_schema(intent_dist, top_n)
    
    # 否则走原来的逻辑（兼容）
    if intent_type:
        tools = [t for t in tools if t.get("category") == intent_type]
    
    openai_tools = []
    for tool in tools:
        # ... 原来的Schema生成逻辑 ...
    return openai_tools


def _get_mixed_schema(
    intent_dist: Dict[str, float],
    top_n: int = 2
) -> List[Dict]:
    """混合Schema：高置信度详细，低置信度简要"""
    sorted_intents = sorted(intent_dist.items(), key=lambda x: x[1], reverse=True)
    
    openai_tools = []
    brief_intents = []
    
    # 高置信度：详细Schema
    for intent, score in sorted_intents[:top_n]:
        if score < 0.3:
            continue
        tools = get_tools_from_registry_by_category_string(intent)
        for tool in tools:
            openai_tools.append(_build_detailed_schema(tool))
    
    # 低置信度：收集简要清单
    for intent, score in sorted_intents[top_n:]:
        if score >= 0.1:
            brief_intents.append((intent, score))
    
    # 将简要清单作为特殊工具返回（或者不放在Function Calling中）
    if brief_intents:
        brief_desc = "【未加载详细说明的工具】" + "".join([f"\n- {i}({s:.2f})" for i, s in brief_intents])
        # 可以选择：1) 作为finish工具的description一部分；2) 放在system prompt中
    
    return openai_tools
```

### 4.2 阶段2：实现动态暴露（P2）

**目标**：运行中检测到需要新工具时，动态加载全套工具

**步骤**：
1. 修改`base_react.py`，新增`load_tools_by_intent()`方法
2. 修改`base_react.py`，在Observation阶段调用`_check_and_load_missing_tools()`
3. 修改`os_adapter.py`，在构建请求时，使用最新的`self._tools_schema`
4. 测试：验证动态加载是否生效

**代码修改点**：

```python
# ============== base_react.py 修改 ==============

class BaseReactAgent:
    def __init__(self, ..., intent_type: str = "file", intent_dist: Dict = None):
        # ... 原有逻辑 ...
        
        # 新增：保存意图信息
        self._intent_type = intent_type
        self._intent_dist = intent_dist or {}
        
        # 新增：初始只加载top-2意图的工具
        if intent_dist:
            self._loaded_categories = set()
            self._tools_dict = {}
            self._load_top_intents(intent_dist, top_n=2)
        else:
            # 兼容原有逻辑
            self._tools_dict = self._load_tools(tool_category)
    
    def _load_top_intents(self, intent_dist: Dict[str, float], top_n: int = 2):
        """根据置信度，加载top-N意图的工具"""
        sorted_intents = sorted(intent_dist.items(), key=lambda x: x[1], reverse=True)
        
        for intent, score in sorted_intents[:top_n]:
            if score >= 0.3:  # 阈值
                self.load_tools_by_intent(intent, reason=f"初始加载top-{top_n}意图")
    
    def load_tools_by_intent(self, intent_type: str, reason: str = ""):
        """动态加载某个意图的工具"""
        if intent_type in self._loaded_categories:
            return
        
        logger.info(f"[动态加载] 原因: {reason}，意图: {intent_type}")
        
        category = ToolCategory(intent_type)
        new_tools = get_tools_from_registry_by_category(category)
        
        self._tools_dict.update(new_tools)
        self._loaded_categories.add(intent_type)
        self._refresh_tools_schema()
    
    def _refresh_tools_schema(self):
        """重新生成tools_schema"""
        self._tools_schema = get_tools_schema_for_intent_distribution(
            {cat: 1.0 for cat in self._loaded_categories}  # 已加载的都给详细Schema
        )
    
    def _check_and_load_missing_tools(self, observation: str):
        """检测是否需要新工具"""
        trigger_map = {
            "network": ["ping", "http", "下载", "网络", "url"],
            "desktop": ["截图", "截屏", "窗口"],
            "shell": ["命令", "执行", "npm", "pip"],
            "database": ["数据库", "SQL", "查询"],
            "env": ["环境变量", "PATH"],
            "system": ["系统信息", "CPU", "内存"],
        }
        
        for intent, keywords in trigger_map.items():
            if any(kw in observation for kw in keywords):
                if intent not in self._loaded_categories:
                    self.load_tools_by_intent(intent, reason=f"Observation检测到: {observation[:50]}...")
                    return
```

---

## 五、预期效果

### 5.1 初始状态（按需暴露）

假设用户输入："帮我查看当前目录，然后计算两个时间的差值"

意图检测结果：
```json
{
    "intent": "file",
    "confidence": 0.85,
    "all_intents": {
        "file": 0.85,
        "time": 0.60,
        "shell": 0.10
    }
}
```

**给LLM的工具**：
- ✅ **file类工具（19个）**：详细Schema（有完整参数说明）
- ✅ **time类工具（9个）**：详细Schema（有完整参数说明）
- ⚠️ **shell类工具（3个）**：简要Schema（只有name+一句话）或只给清单

**效果**：
- LLM注意力集中，精准调用file和time工具
- 如果LLM想用shell工具，会先说明需求，触发动态加载

### 5.2 运行中（动态暴露）

LLM调用file和time工具后，输出observation："需要执行shell命令来查看系统信息"

**自动触发**：
1. `_check_and_load_missing_tools()`检测到"shell"关键词
2. 调用`load_tools_by_intent("shell")`
3. 新增shell类工具（3个）到`_tools_dict`
4. 重新生成`_tools_schema`
5. 下次LLM调用时，可以看到shell工具的详细Schema

**效果**：
- 无需用户干预，自动扩展工具集
- 每次只加载需要的工具，保持精准度

---

## 六、风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|---------|
| **动态加载失败** | 低 | 中 | 添加try-except，失败时记录日志并告知LLM |
| **加载过多工具** | 中 | 中 | 设置阈值（置信度<0.1的不加载简要清单） |
| **LLM无视简要清单** | 中 | 高 | 在system prompt中强调："未加载详细说明的工具无法直接调用" |
| **重新生成Schema耗时** | 低 | 低 | Schema生成很快（毫秒级），可忽略 |

---

## 七、测试计划

### 7.1 测试场景1：按需暴露

**输入**："帮我查看当前目录，然后计算两个时间的差值"

**预期**：
1. 初始只加载file+time工具（28个）
2. LLM精准调用`list_directory`和`get_current_time`
3. shell工具只在简要清单中，不干扰LLM

### 7.2 测试场景2：动态暴露

**输入**："帮我查看当前目录，然后ping一下百度"

**预期**：
1. LLM先调用`list_directory`
2. Observation中出现"ping"
3. 自动加载network工具
4. LLM成功调用`ping`工具

---

## 八、总结

**核心改进**：
1. ✅ **按需暴露**：利用意图置信度，top-2给详细，其他给简要
2. ✅ **动态暴露**：运行中检测关键词，自动加载全套工具
3. ✅ **Schema分级**：详细说明 vs 简要说明，节省token，提高精准度

**与现有系统的关系**：
- ✅ **保留**：intent_classifier的置信度分布（已完善）
- ✅ **保留**：ToolMetadata的description和input_schema（已完善）
- ✅ **改进**：从"一次性给所有工具"改为"按需+动态"
- ✅ **新增**：简要Schema生成、动态加载机制

---

## 九、实施步骤（基于问题分析与方法改进） 小沈-2026-05-05

**制定时间**: 2026-05-05 19:20:00

### 9.1 实施原则

1. **顺序不可颠倒**：基础问题必须先解决，后续步骤依赖前置步骤
2. **每步可独立验收**：每个步骤完成后可单独验证，不依赖后续步骤
3. **基于文档设计**：所有实施必须基于本文档的设计方案，不得偏离
4. **文档先行**：系统更新前必须完成文档设计和修改

### 9.2 实施步骤总览（修正-小沈 2026-05-05 19:53:50）

| 步骤 | 内容 | 对应问题 | 前置依赖 | 验收标准 |
|------|------|---------|---------|---------|
| **步骤1** | 验证12个分类的schema.py与tools.py参数匹配 | 问题0.4/0.2：工具组件不完整（已修正） | 无 | 每个分类的schema模型与tools.py函数参数一致 |
| **步骤2** | 添加shell/network/desktop/database子Agent | 问题0.1：子Agent缺失 | 步骤1完成 | 每个意图都有对应子Agent注册 |
| **步骤3** | 精简description，突出强制约束；完善system prompt | 问题0.3：prompt效果差 | 步骤1完成 | LLM调用工具参数准确率提升 |
| **步骤4** | 实现按需暴露机制（阶段1） | 问题1.3/2.1：暴露机制不够好 | 步骤1+2+3完成 | 初始只加载top-2意图工具，LLM注意力集中 |
| **步骤5** | 实现动态暴露机制（阶段2） | 问题2.2：LLM调用错误 | 步骤4完成 | 运行中能自动检测并加载新工具 |

### 9.3 步骤1：验证工具组件（P0优先级）（修正-小沈 2026-05-05 19:53:50）

**目标**：验证12个分类的schema.py与tools.py参数匹配

**详细说明**（步骤1已基本完成，2026-05-05合并修复后）：
参考file/和time/的完整实现，验证所有12个分类：
1. ✅ `schema.py`：Pydantic参数模型与tools.py函数参数匹配
2. ✅ `{cat}_register.py`中`TOOL_DESCRIPTIONS`字典完整
3. ✅ 工具注册信息与schema/tools一致

**涉及分类**（12个当前有效分类）：
- shell/、network/、desktop/（含gui合并）、database/
- environment/（含env_check合并）、system/（含registry_tools合并）
- document/（含data_analysis合并）、data_format/
- code_execution/、support_tool/、file/、time/

**已合并分类说明**（枚举值保留兼容）：
- ❌ ~~env_check/~~ → 已合并到environment/
- ❌ ~~gui/~~ → 已合并到desktop/
- ❌ ~~data_analysis/~~ → 已合并到document/
- ❌ ~~registry_tools/~~ → 已合并到system/

**验收方法**：
```bash
# 检查每个分类是否有4个文件
for cat in shell network desktop database env env_check gui system document data_analysis data_format code_execution registry_tools support_tool; do
  echo "检查 $cat/:"
  ls D:/OmniAgentAs-desk/backend/app/services/tools/$cat/
  # 确认有：tools.py、schema.py、register.py
  # 查看register.py中是否有TOOL_DESCRIPTIONS
  grep -l "TOOL_DESCRIPTIONS" D:/OmniAgentAs-desk/backend/app/services/tools/$cat/*_register.py
done
```

**完成标志**：
- ✅ 14个分类都有schema.py文件
- ✅ 14个分类的register.py都有TOOL_DESCRIPTIONS字典
- ✅ 每个工具的schema模型与tools.py函数参数匹配
- ✅ 每个工具的注册信息与schema/tools一致

---

### 9.4 步骤2：添加子Agent（P1优先级）（修正-小沈 2026-05-05 19:53:50）

**目标**：给shell、network、desktop、database添加专用子Agent

**详细说明**（参考FileReactAgent/time_react.py实现框架）：

#### 9.4.1 ShellReactAgent（参考FileReactAgent）

**文件位置**：`backend/app/services/agent/shell_react.py`

```python
# -*- coding: utf-8 -*-
"""
ShellReactAgent - Shell命令执行 ReAct Agent

继承 BaseAgent，专门用于Shell命令场景的ReAct智能体。
参考: 文档5.5节+7.5节完整代码
参考: file_react.py（FileReactAgent实现）

Author: 小沈 - 2026-05-05
"""

import asyncio
from typing import Dict, Any, List, Optional

from app.services.agent.base_react import BaseAgent, DEFAULT_MAX_STEPS
from app.services.agent.tool_executor import ToolExecutor
from app.services.agent.types import Step, AgentResult, AgentStatus
from app.services.agent.llm_adapter import LLMAdapter
from app.services.tools.shell.shell_tools import ShellTools
from app.services.prompts.shell import ShellOperationPrompts
from app.services.agent.session import get_session_service
from app.services.tools.mixin import ToolLoaderMixin
from app.services.tools.registry import ToolCategory
from app.utils.logger import logger


class ShellReactAgent(ToolLoaderMixin, BaseAgent):
    """
    Shell命令执行 ReAct Agent - 使用tool_category参数
    参考: 7.5节行1052-1150
    
    实现完整的Thought-Action-Observation循环，
    专门用于Shell命令场景，保留session管理、prompts、rollback功能
    """
    
    def __init__(
        self,
        llm_client: Any,
        task_id: str,
        tool_category: Optional[ToolCategory] = None,
        max_steps: int = DEFAULT_MAX_STEPS,
        candidates: Optional[List[str]] = None,
        **kwargs
    ):
        """
        初始化ShellReactAgent
        
        Args:
            llm_client: LLM客户端函数
            task_id: 任务ID（必需）- 用于操作安全追踪和审计
            tool_category: 工具分类（可选，默认SHELL）
            max_steps: 最大步数
            candidates: 候选意图列表，用于跨分类工具访问
        """
        if not task_id:
            raise ValueError("task_id is required for shell operation tracking and safety")
        
        effective_category = tool_category or ToolCategory.SHELL
        
        super().__init__(
            llm_client=llm_client,
            task_id=task_id,
            tool_category=effective_category,
            max_steps=max_steps,
            **kwargs
        )
        
        # 初始化session服务（用于统一管理会话生命周期）
        self.session_service = get_session_service()
        self._session_created_by_agent = False
        
        # 初始化Shell工具确保task_id正确传递
        self.shell_tools = ShellTools(task_id=task_id)
        
        # 【修复】使用Mixin的load_tools_by_category
        if self.tool_category:
            self._tools_dict = self.load_tools_by_category(self.tool_category)
        
        self.executor = ToolExecutor(self._tools_dict)
        
        # Shell专用prompts
        self.prompts = ShellOperationPrompts()
        
        logger.info(f"ShellReactAgent initialized (task_id: {task_id}, tool_category: {effective_category}, tools: {len(self._tools_dict)})")
    
    # ========== 跨分类工具支持方法（参考TimeReactAgent）==========
    
    def _get_tools_summary(self) -> str:
        """
        获取跨分类工具概要（每轮实时生成，确保动态注册的工具被包含）
        设计文档v1.0 4.2节
        """
        from app.services.tools.registry import tool_registry
        return tool_registry.get_all_tools_summary(
            priority_category=ToolCategory.SHELL,
            expose_to_llm_only=True
        )
    
    # 其他必要方法参考FileReactAgent实现...
```

#### 9.4.2 NetworkReactAgent（参考TimeReactAgent）

**文件位置**：`backend/app/services/agent/network_react.py`

```python
# -*- coding: utf-8 -*-
"""
NetworkReactAgent - 网络通信 ReAct Agent

继承BaseAgent，专门用于网络通信场景的ReAct智能体。
参考: time_react.py（TimeReactAgent实现）

Author: 小沈 - 2026-05-05
"""

from typing import Dict, Any, List, Optional

from app.services.agent.base_react import BaseAgent, DEFAULT_MAX_STEPS
from app.services.agent.tool_executor import ToolExecutor
from app.services.agent.llm_strategies import TextStrategy
from app.services.agent.llm_adapter import LLMAdapter
from app.services.tools.mixin import ToolLoaderMixin
from app.services.tools.registry import ToolCategory
from app.services.prompts.network import NetworkOperationPrompts
from app.utils.logger import logger


class NetworkReactAgent(ToolLoaderMixin, BaseAgent):
    """网络通信Agent - 按文档5.4+7.3实现"""
    
    def __init__(
        self,
        llm_client: Any,
        task_id: str,
        tool_category: Optional[ToolCategory] = None,
        max_steps: int = DEFAULT_MAX_STEPS,
        candidates: Optional[List[str]] = None,
        **kwargs
    ):
        effective_category = tool_category or ToolCategory.NETWORK
        
        super().__init__(
            llm_client=llm_client,
            task_id=task_id,
            tool_category=effective_category,
            max_steps=max_steps,
            **kwargs
        )
        
        self.prompts = NetworkOperationPrompts()
        
        if self.tool_category:
            self._tools_dict = self.load_tools_by_category(self.tool_category)
        
        self.executor = ToolExecutor(self._tools_dict)
        self.text_strategy = TextStrategy()
        
        # LLMAdapter自适应策略
        self.adapter = None
        self.use_function_calling = False
        self.openai_tools = []
        self.tools_strategy = None
        self.response_format_strategy = None
        
        self._candidates = candidates if candidates else []
        
        logger.info(f"NetworkReactAgent initialized (task_id: {task_id}, tool_category: {effective_category}, tools: {len(self._tools_dict)}, candidates: {self._candidates})")
    
    def _get_tools_summary(self) -> str:
        """获取跨分类工具概要"""
        from app.services.tools.registry import tool_registry
        return tool_registry.get_all_tools_summary(
            priority_category=ToolCategory.NETWORK,
            expose_to_llm_only=True
        )
```

#### 9.4.3 DesktopReactAgent（参考FileReactAgent）

**文件位置**：`backend/app/services/agent/desktop_react.py`

```python
# DesktopReactAgent - 桌面操作 ReAct Agent
# 继承ToolLoaderMixin + BaseAgent
# 参考file_react.py实现框架
# 使用ToolCategory.DESKTOP
# 初始化DesktopTools(task_id=task_id)
# 使用DesktopOperationPrompts
```

#### 9.4.4 DatabaseReactAgent（参考TimeReactAgent）

**文件位置**：`backend/app/services/agent/database_react.py`

```python
# DatabaseReactAgent - 数据库操作 ReAct Agent
# 继承ToolLoaderMixin + BaseAgent
# 参考time_react.py实现框架
# 使用ToolCategory.DATABASE
# 初始化DatabaseTools(task_id=task_id)
# 使用DatabaseOperationPrompts
```

#### 代码修改点：

- `backend/app/services/agent/`：新增4个子Agent类文件（shell_react.py/network_react.py/desktop_react.py/database_react.py）
- `backend/app/services/agent/agent_factory.py`：注册新的子Agent
- `backend/app/services/agent/prompts/`：新增对应的prompts类（ShellOperationPrompts/NetworkOperationPrompts等）

**验收方法**：
```bash
# 检查子Agent注册
grep -A 20 "class AgentFactory" D:/OmniAgentAs-desk/backend/app/services/agent/agent_factory.py | grep "register"
# 预期输出包含：
# AgentFactory.register('shell', ShellReactAgent, ToolCategory.SHELL)
# AgentFactory.register('network', NetworkReactAgent, ToolCategory.NETWORK)
# AgentFactory.register('desktop', DesktopReactAgent, ToolCategory.DESKTOP)
# AgentFactory.register('database', DatabaseReactAgent, ToolCategory.DATABASE)
```

**完成标志**：
- ✅ agent_factory.py中注册了4个新子Agent
- ✅ 每个子Agent都有对应的ToolCategory枚举值
- ✅ 子Agent能正确处理对应分类的工具调用

---

### 9.5 步骤3：改进prompt设计（P1优先级）

**目标**：精简description，突出强制约束；把详细说明放到system prompt

**详细说明**：
1. 精简各`{cat}_register.py`中的`TOOL_DESCRIPTIONS`字典：
   - 保留核心功能说明
   - 突出【强制参数约束】部分
   - 给出正确示例和错误示例
2. 修改`base_react.py`的system prompt：
   - 添加【可用工具详细说明】章节
   - 列出每个工具的正确的调用示例
   - 明确标注无效参数（如time工具的path是无效的）

**代码修改点**：
- 所有`{cat}_register.py`：精简TOOL_DESCRIPTIONS（保持正确完整）
- `backend/app/services/agent/base_react.py`：更新system prompt生成逻辑

**验收方法**：
```bash
# 检查description是否精简（查看time_register.py的TOOL_DESCRIPTIONS）
# 预期：每个工具的description不超过20行，突出强制约束
grep -A 10 "get_current_time" D:/OmniAgentAs-desk/backend/app/services/tools/time/time_register.py | head -20

# 检查system prompt是否包含工具详细说明
grep "可用工具详细说明" D:/OmniAgentAs-desk/backend/app/services/agent/base_react.py
```

**完成标志**：
- ✅ 所有TOOL_DESCRIPTIONS都精简，突出强制约束
- ✅ system prompt中包含【可用工具详细说明】章节
- ✅ LLM调用工具时参数准确率显著提升

---

### 9.6 步骤4：实现按需暴露机制（P1优先级）

**目标**：初始只给高置信度意图的详细工具，其他给简要清单

**详细说明**：
1. 修改`react_schema.py`，新增`get_tools_schema_for_intent_distribution()`函数
2. 修改`base_react.py`，在`__init__`中只加载top-2意图的工具
3. 修改`react_output_parser.py`，在system prompt中添加简要工具清单
4. 测试：验证LLM是否更精准调用

**代码修改点**：
- `backend/app/services/tools/react_schema.py`：新增混合Schema生成逻辑
- `backend/app/services/agent/base_react.py`：初始只加载top-2意图工具
- `backend/app/services/agent/react_output_parser.py`：添加简要工具清单到system prompt

**验收方法**：
```python
# 测试场景1：按需暴露
# 输入："帮我查看当前目录，然后计算两个时间的差值"
# 预期：
# 1. intent_dist = {"file": 0.85, "time": 0.60, "shell": 0.10}
# 2. 初始只加载file（19个）+ time（9个）的详细Schema
# 3. shell工具只在简要清单中
# 4. LLM精准调用list_directory和get_current_time
```

**完成标志**：
- ✅ get_tools_schema_for_intent_distribution()函数已实现
- ✅ 初始只加载top-2意图的详细工具
- ✅ 低置信度意图只给简要清单
- ✅ LLM注意力集中，调用更精准

---

### 9.7 步骤5：实现动态暴露机制（P2优先级）

**目标**：运行中检测到需要新工具时，动态加载全套工具

**详细说明**：
1. 修改`base_react.py`，新增`load_tools_by_intent()`方法
2. 修改`base_react.py`，新增`_check_and_load_missing_tools()`方法
3. 在Observation阶段调用`_check_and_load_missing_tools()`
4. 修改`os_adapter.py`，在构建请求时使用最新的`self._tools_schema`

**代码修改点**：
- `backend/app/services/agent/base_react.py`：新增动态加载方法
- `backend/app/services/agent/os_adapter.py`：使用最新tools_schema

**验收方法**：
```python
# 测试场景2：动态暴露
# 输入："帮我查看当前目录，然后ping一下百度"
# 预期：
# 1. LLM先调用list_directory
# 2. Observation中出现"ping"
# 3. 自动加载network工具（3个）
# 4. 下次LLM调用时，可以看到network工具的详细Schema
# 5. LLM成功调用ping工具
```

**完成标志**：
- ✅ load_tools_by_intent()方法已实现
- ✅ _check_and_load_missing_tools()能检测关键词并触发加载
- ✅ 重新生成tools_schema后，LLM能看到新工具的详细Schema
- ✅ 跨意图任务能自动扩展工具集

---

### 9.8 实施检查清单

**所有步骤完成后检查**：
- [ ] 步骤1：14个分类都有完整4个组件
- [ ] 步骤2：4个新子Agent已注册
- [ ] 步骤3：description已精简，system prompt已更新
- [ ] 步骤4：按需暴露机制已实现并测试通过
- [ ] 步骤5：动态暴露机制已实现并测试通过
- [ ] 所有修改都有对应的commit（带签名+日期）
- [ ] 语法检查通过（py_compile）
- [ ] 单元测试通过（pytest）
- [ ] 后端服务能正常启动

---

**本节更新时间**: 2026-05-05 19:20:00
**制定人**: 小沈

---

**文档完成时间**: 2026-05-05 10:30:00  
**作者**: 小沈  
**版本**: v1.0  
**状态**: 待老陈审查
