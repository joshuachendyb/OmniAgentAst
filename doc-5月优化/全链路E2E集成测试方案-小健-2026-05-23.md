# 全链路端到端集成测试方案（Full-Link E2E Integration Test）

**创建时间**: 2026-05-23 01:00:44  
**版本**: v1.0  
**编写人**: 小健

---

## 版本记录

| 版本 | 时间 | 签名 | 更新内容 |
|------|------|------|---------|
| v1.0 | 2026-05-23 01:00:44 | 小健 | 初始版本 |

---

## 一、测试定义

### 1.1 测试名称

**全链路端到端集成测试**（Full-Link End-to-End Integration Test）

### 1.2 为什么叫这个名

| 关键词 | 含义 | 在这个测试中的体现 |
|--------|------|-------------------|
| 全链路 | 覆盖系统所有组件 | 前端API→后端→Agent→LLM(Ollama)→工具执行→数据库→SSE响应 |
| 端到端 | 从用户发消息到收到完整结果 | POST /chat → SSE事件流 → final事件 |
| 集成 | 验证各组件对接是否正确 | API路由、SSE封装、Agent循环、LLM调用、工具执行、数据库写入 |

### 1.3 与现有测试的区别

| 测试类型 | 测试什么 | 用什么驱动 | 能发现什么问题 |
|---------|---------|-----------|--------------|
| 单元测试（现有~200个） | 单个工具函数 | Mock/Patch | 函数级bug |
| MessageBuilder测试（43个） | Prompt构建逻辑 | Mock | Prompt构建bug |
| 现有Ollama E2E（7个） | LLM连通性+基础对话 | 真实Ollama | LLM通不通 |
| **全链路E2E（本方案）** | **Agent行为+系统集成** | **真实Ollama+真实后端** | **死循环、工具选错、参数传错、会话污染、日志异常、数据库异常** |

### 1.4 测试目标

**唯一目标**：用真实LLM模拟真实用户操作，把系统中隐藏的问题炸出来。

**具体来说**：
1. Agent在真实LLM下能否正确完成用户请求
2. Agent在异常情况下（LLM返回垃圾/空内容/错误工具）能否容错
3. 系统各组件的串联是否完整（API→Agent→LLM→工具→DB）
4. 日志和数据库记录是否完整准确

---

## 二、测试架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      测试脚本（pytest）                          │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    测试执行引擎                            │   │
│  │  ① 初始化测试环境                                         │   │
│  │  ② 通过httpx发送POST /chat → 接收SSE事件流               │   │
│  │  ③ 逐事件解析、记录、断言                                  │   │
│  │  ④ 查询日志文件，检查ERROR/WARNING                        │   │
│  │  ⑤ 查询SQLite数据库，检查session/messages/steps          │   │
│  │  ⑥ 清理测试残留数据                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│         ↓ HTTP POST /chat           ↓ SSE event stream          │
│    ┌──────────────────────────────────────────────────┐         │
│    │              后端服务（uvicorn）                   │         │
│    │  main.py → chat_router.py → Agent → LLM → 工具   │         │
│    │                          ↓                        │         │
│    │                    SQLite DB / 日志文件             │         │
│    └──────────────────────────────────────────────────┘         │
│                        ↑                                        │
│                真实LLM调用（Ollama）                              │
│                http://localhost:11434/v1                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、完整测试流程

每个测试用例都严格按照以下步骤执行。

### 3.1 前置条件

| 条件 | 要求 | 如何检查 |
|------|------|---------|
| Ollama已启动 | 必须运行，监听11434 | `curl http://localhost:11434/api/tags` 返回200 |
| qwen2.5:1.5b已就绪 | 模型已拉取，可调用 | `ollama list` 能看到模型 |
| 后端已启动 | uvicorn运行中，监听8000 | `curl http://localhost:8000/docs` 返回200 |
| 测试数据就绪 | 测试目录、测试文件已创建 | `Test-Path` 检查路径存在 |
| 日志目录就绪 | `backend/logs/prompt-logs/`可写 | `Test-Path` 检查目录 |
| 数据库路径就绪 | `~/.omniagent/chat_history.db`可写 | 确认无权限问题 |

### 3.2 测试执行（核心流程）

```
步骤 1: 记录测试起始状态
─────────────────────────────────────────────────────────────────
  · 记录当前时间 T0
  · 记录数据库当前会话数（base count）
  · 记录日志文件当前大小

步骤 2: 发送用户请求
─────────────────────────────────────────────────────────────────
  · 用 httpx 发送 POST /chat
  · 请求体：{"messages": [{"role": "user", "content": "用户输入"}]}
  · 设置 stream=True 以接收 SSE
  · 记录请求时间 T1

步骤 3: 接收并解析 SSE 事件流
─────────────────────────────────────────────────────────────────
  · 逐行读取 SSE 响应
  · 按 event: xxx 和 data: xxx 解析每个事件
  · 记录每个事件的：
    - 事件类型（start/thought/action/observation/chunk/final/error/interrupted）
    - 时间戳
    - step编号
    - data内容（工具名、参数、结果等）
  · 持续接收直到 final 事件或超时
  · 记录接收完成时间 T2

步骤 4: 验证事件流正确性
─────────────────────────────────────────────────────────────────
  · [必须] 最终收到了 final 事件 → 正常结束
  · [必须] 没有收到 error 事件 → 没有异常中断
  · [必须] 事件总数在合理范围内（如 < 50个） → 没有死循环
  · [警告] 如果事件总数 > 20，可能存在空转/无意义循环
  · [检查] final 事件中是否包含结果内容
  · [检查] 事件顺序是否正确（start→thought→action→observation→...→final）

步骤 5: 验证数据库记录
─────────────────────────────────────────────────────────────────
  · 查询 chat_history.db 的 sessions 表
    - 对应当前测试的 session 记录是否存在
    - session 的开始时间、结束时间、状态是否正常
  · 查询 messages 表
    - 对应的消息记录是否存在
    - user message 和 assistant response 是否都保存了
  · 查询 execution_steps 表
    - 步骤数量与 SSE 事件流中的 action 数量是否一致
    - 每个 step 的 type、tool、status 是否正常

步骤 6: 验证日志文件
─────────────────────────────────────────────────────────────────
  · 扫描后端日志文件
    - 是否有 ERROR 级别日志（有则失败）
    - 是否有异常 traceback（有则失败）
    - 记录 WARNING 级别数量（过多则标记）
  · 扫描 prompt-logs/
    - 检查本次请求的 LLM 调用日志是否已保存
    - 检查 LLM 调用次数（过多则可能死循环）

步骤 7: 记录测试完成状态
─────────────────────────────────────────────────────────────────
  · 记录测试总耗时 T2 - T1
  · 记录 LLM 调用次数
  · 记录 SSE 事件总数
  · 记录最终结论（通过/失败/警告）
```

### 3.3 验证标准总表

| 验证点 | 通过标准 | 失败标准 | 验证来源 |
|--------|---------|---------|---------|
| 最终事件 | 收到 final | 未收到 final（超时或中断） | SSE事件流 |
| 错误事件 | 无 error 事件 | 任意 error 事件 | SSE事件流 |
| step数量 | < 50 | >= 50（可能死循环） | SSE事件流 |
| LLM调用次数 | < 10 | >= 10（可能死循环） | prompt-logs / 事件计数 |
| 数据库记录 | session + messages + steps 完整 | 缺少任意记录 | SQLite DB |
| 日志级别 | 无 ERROR | 有 ERROR | 后端日志文件 |
| 异常traceback | 无 | 有 | 后端日志文件 |
| 响应时间 | < 300s（CPU推理） | >= 300s | 计时 |
| 执行工具 | 与预期一致 | 调用了不相关的工具 | SSE action事件 |

### 3.4 后置清理

| 操作 | 说明 |
|------|------|
| 清理测试数据库记录 | 删除本次测试产生的session记录 |
| 清理测试文件 | 删除测试过程中创建的文件 |
| 恢复被修改的配置 | 还原 config.yaml 等 |
| 记录测试报告 | 写入测试结果到报告文件 |

---

## 四、测试场景清单

### 4.1 P0：已知Bug回归测试（务必通过这些才能继续）

| 编号 | 场景 | 用户输入 | 预期行为 | 炸什么雷 |
|------|------|---------|---------|---------|
| E2E-P0-01 | 闲聊不匹配CRSS | `"hello"` | 直接finish，不调任何工具，step <= 3 | 空工具名死循环（§9.1已知bug） |
| E2E-P0-02 | 无关聊天 | `"你好，今天天气怎么样"` | finish或调search_web，step <= 5 | Agent把闲聊当工具调用 |
| E2E-P0-03 | 空输入 | `""` | 直接finish，不调任何工具 | 空字符串处理 |
| E2E-P0-04 | 特殊符号 | `"!@#$%^&*()"` | 直接finish，不崩溃 | 特殊字符导致解析器崩溃 |

### 4.2 P1：核心功能场景（核心功能必须通过）

| 编号 | 场景 | 用户输入 | 预期工具 | 预期step数 |
|------|------|---------|---------|-----------|
| E2E-P1-01 | 列出目录 | `"列出当前目录有哪些文件"` | list_directory | 2-5 |
| E2E-P1-02 | 读文件 | `"把readme.md读出来"` | read_file | 2-5 |
| E2E-P1-03 | 网络检查 | `"检查一下百度能不能访问"` | check_network_connectivity | 2-5 |
| E2E-P1-04 | 系统信息 | `"CPU占用多少"` | get_system_info | 2-5 |
| E2E-P1-05 | 搜索 | `"帮我搜一下2026年奥运会"` | search_web | 2-5 |

### 4.3 P2：多步推理场景（复杂任务必须通过）

| 编号 | 场景 | 用户输入 | 预期行为 | 预期step数 |
|------|------|---------|---------|-----------|
| E2E-P2-01 | 先查后读 | `"列出当前目录，然后读第一个文件"` | 先list_directory，再read_file | 3-8 |
| E2E-P2-02 | 搜索+获取 | `"帮我查一下某个新闻的详情"` | 先search_web，再fetch_webpage | 3-8 |
| E2E-P2-03 | 递进操作 | `"看看D盘根目录有什么文件"` | list_directory（正确路径） | 2-5 |

### 4.4 P3：异常与容错场景（系统健壮性验证）

| 编号 | 场景 | 方法 | 预期行为 |
|------|------|------|---------|
| E2E-P3-01 | 路径不存在 | 请求读不存在的文件 | Agent报错后finish，不崩溃 |
| E2E-P3-02 | 连续请求 | 连续发5次不同请求 | 每次独立，不被前一次污染 |
| E2E-P3-03 | 并发请求 | 同时发3个请求 | 各自正常结束，session隔离 |
| E2E-P3-04 | 长输入 | 输入1000字长文本 | Agent能处理，不OOM |

---

## 五、实现方式

### 5.1 测试框架选择

使用 **pytest**，与现有测试体系一致。

### 5.2 测试文件结构

```
backend/tests/
  ├── test_e2e_full_link.py       ← 主测试文件（本方案的核心）
  ├── conftest.py                  ← fixtures（ollama_client、backend_client、db检查等）
```

### 5.3 测试辅助函数

每个测试都需要以下辅助能力：

```python
async def send_chat_and_collect_events(
    user_input: str,
    base_url: str = "http://localhost:8000"
) -> dict:
    """
    发POST /chat，接收SSE事件流，返回结构化结果。
    
    返回值：
    {
        "events": [...],         # 所有SSE事件列表
        "final_event": {...},    # 最后一个final事件
        "has_error": bool,
        "total_steps": int,
        "tool_calls": [...],     # 调用了哪些工具
        "llm_call_count": int,   # LLM调用次数
        "total_time_ms": int,    # 总耗时
        "response_text": str     # final中的回复文本
    }
    """
```

```python
def check_database(session_id: str) -> dict:
    """
    检查数据库记录，返回完整性报告。
    
    {
        "session_exists": bool,
        "session_status": str,
        "messages_count": int,
        "execution_steps_count": int
    }
    """
```

```python
def check_logs(test_id: str, start_time: datetime) -> dict:
    """
    检查日志文件，返回异常报告。
    
    {
        "errors": [...],
        "warnings": [...],
        "tracebacks": [...]
    }
    """
```

### 5.4 测试用例模板

```python
@pytest.mark.e2e_full_link
@pytest.mark.asyncio
async def test_e2e_p0_01_chat_hello():
    """P0-01: 闲聊输入"hello"，Agent应直接finish不调工具"""
    
    # 1. 记录起始状态
    test_start = datetime.now()
    base_session_count = get_session_count()
    
    # 2. 发送请求并接收SSE事件流
    result = await send_chat_and_collect_events("hello")
    
    # 3. 验证事件流正确性
    assert result["has_error"] == False, "不应有error事件"
    assert result["final_event"] is not None, "必须有final事件"
    assert result["total_steps"] <= 3, f"step不应超过3，实际={result['total_steps']}"
    
    # 4. 验证没有调用任何工具（闲聊不应调工具）
    assert len(result["tool_calls"]) == 0, f"不应调任何工具，实际调了{result['tool_calls']}"
    
    # 5. 验证响应时间
    assert result["total_time_ms"] < 300000, f"不应超时，实际={result['total_time_ms']}ms"
    
    # 6. 验证数据库
    db_check = check_database(result["final_event"]["session_id"])
    assert db_check["session_exists"], "session应已保存"
    
    # 7. 验证日志
    log_check = check_logs("e2e-p0-01", test_start)
    assert len(log_check["errors"]) == 0, f"日志不应有ERROR: {log_check['errors']}"
```

---

## 六、执行计划

### 6.1 前提条件

```bash
# 1. 确保 Ollama 已启动
curl http://localhost:11434/api/tags

# 2. 确保后端已启动
start python -m uvicorn app.main:app --reload

# 3. 运行测试
pytest -m e2e_full_link -v --tb=short
```

### 6.2 执行时间预估

| 级别 | 测试数 | 单个耗时 | 总耗时 |
|------|--------|---------|--------|
| P0 | 4 | 30s-2min | ~5min |
| P1 | 5 | 30s-3min | ~10min |
| P2 | 3 | 1min-5min | ~10min |
| P3 | 4 | 1min-5min | ~10min |
| **合计** | **16** | | **~35min** |

### 6.3 标记

所有测试使用 `@pytest.mark.e2e_full_link` 标记，与现有 `e2e_ollama` 标记区分。

执行命令：
```bash
# 跑全部全链路测试
pytest -m e2e_full_link -v --tb=short

# 只跑P0（快速验证已知bug是否修复）
pytest -m e2e_full_link -k "p0" -v --tb=short
```

---

## 七、测试报告格式

每个测试运行后输出：

```
=== E2E-P0-01: hello ===
  用户输入: "hello"
  状态: ✅ 通过
  耗时: 12.3s
  SSE事件数: 3 (start→thought→final)
  工具调用: 无
  LLM调用次数: 1
  数据库: ✅ session已保存
  日志: ✅ 无ERROR
```

所有测试跑完后输出汇总：

```
=== 全链路E2E测试汇总 ===
  总测试数: 16
  通过: 12
  失败: 2
  跳过: 2
  
  ❌ 失败详情:
    E2E-P0-01: hello → 死循环（step=47, LLM调用=12） ← 已知bug未修复
    E2E-P1-01: 列出目录 → 超时（>300s） ← CPU推理太慢
  
  ⚠️ 跳过详情:
    E2E-P3-03: 并发请求 → 依赖未就绪
    E2E-P3-04: 长输入 → 依赖未就绪
```

---

## 八、与其他测试的关系

```
所有测试体系
│
├── L1: Mock单元测试（~200个, ~10秒）
│   ├── 单工具函数测试（file/shell/network/system/desktop/document/meta）
│   ├── MessageBuilder测试（43个）
│   └── 运行时集成测试（14个）
│
├── L2: Ollama E2E基础测试（7个, ~5分钟）
│   ├── 连通性测试（2个）
│   ├── LLM Core基础对话（4个）
│   └── 单场景ReAct（1个）
│
└── L3: 全链路E2E集成测试（16个, ~35分钟）← 本方案
    ├── P0: 已知bug回归（4个）
    ├── P1: 核心功能场景（5个）
    ├── P2: 多步推理场景（3个）
    └── P3: 异常容错场景（4个）
```

**L3是最接近用户手工测试的自动化版本**。之前L1和L2覆盖不到的问题（空工具名死循环、hello死循环、超时不处理），L3都能抓到。

---

**更新时间**: 2026-05-23 01:00:44  
**版本**: v1.0  
**编写人**: 小健
