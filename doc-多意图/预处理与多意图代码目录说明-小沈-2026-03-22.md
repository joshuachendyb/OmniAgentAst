# 预处理与多意图代码目录说明

**创建时间**: 2026-03-22 11:00:00
**版本**: v1.0
**编写人**: 小沈

---

## 目录结构与核心函数

```
backend/app/services/
│
├── agent/                          ← 通用多意图Agent引擎
│   │
│   ├── agent.py                    → IntentAgent类
│   │     核心函数:
│   │       __init__(llm_client, session_id, intent_type="file", ...)
│   │         - intent_type参数: file/desktop/network
│   │         - 根据intent_type加载对应tools/prompts
│   │       run(task, context, system_prompt) -> AgentResult
│   │         - 同步运行入口
│   │       run_stream(task, context, system_prompt) -> AsyncGenerator
│   │         - 流式运行入口
│   │       rollback(step_number) -> bool
│   │         - 回滚操作
│   │       _run_with_session(task, context, system_prompt) -> AgentResult
│   │         - 内部ReAct循环
│   │         - 调用preprocessor.process()预处理
│   │         - 调用intent_registry.get()获取意图定义
│   │       _get_llm_response() -> str
│   │         - 统一LLM调用入口
│   │         - 根据adapter策略选择调用方式
│   │       _get_llm_response_text(message, history_dicts) -> str
│   │         - 文本模式调用
│   │       _get_llm_response_with_tools(message, history_dicts) -> str
│   │         - Function Calling模式调用
│   │       _execute_tool(action, action_input) -> dict
│   │         - 执行工具调用
│   │       _register_default_intents()
│   │         - 注册file和network意图
│   │
│   ├── base.py                     → BaseAgent抽象基类
│   │     核心函数:
│   │       __init__(max_steps, use_function_calling)
│   │       run_with_tools(task, context, system_prompt) -> AgentResult
│   │         - 公开运行入口
│   │       run_stream(task, context, system_prompt, max_steps) -> AsyncGenerator
│   │         - 流式运行入口
│   │       _get_llm_response_text(message, history_dicts) -> str  [抽象]
│   │       _get_llm_response_with_tools(message, history_dicts) -> str  [抽象]
│   │       _execute_tool(action, action_input) -> dict  [抽象]
│   │       _format_observation(observation) -> str
│   │       _add_observation_to_history(observation)
│   │       _execute_with_retry(action, action_input, max_retries) -> dict
│   │
│   ├── tool_parser.py              → ToolParser类
│   │     核心函数:
│   │       parse_response(response) -> dict
│   │         - 解析LLM响应为{content, reasoning, action_tool, params}
│   │       _extract_from_text(text) -> dict
│   │         - 从文本提取JSON
│   │       _extract_action_tool(data) -> tuple
│   │         - 提取action_tool和params
│   │
│   ├── tool_executor.py            → ToolExecutor类
│   │     核心函数:
│   │       execute(action, params) -> dict
│   │         - 执行工具调用
│   │         - finish动作直接返回
│   │         - 未知工具返回错误
│   │       _normalize_params(action, params) -> dict
│   │         - 参数名容错(path→file_path, source→source_path)
│   │       _format_result(result, action) -> dict
│   │         - 统一结果格式{status, summary, data}
│   │
│   ├── llm_strategies.py           → LLM调用策略
│   │     核心函数:
│   │       TextStrategy.call(llm_client, message, history_dicts, conversation_history) -> str
│   │         - 直接调用llm_client返回文本
│   │       ToolsStrategy.call(llm_client, message, history_dicts, conversation_history) -> str
│   │         - 调用llm_client.chat_with_tools()
│   │         - 解析tool_calls为Agent格式
│   │       ToolsStrategy._format_tool_calls(tool_calls) -> str
│   │         - 格式化tool_calls为JSON
│   │       ResponseFormatStrategy.call(llm_client, message, history_dicts, conversation_history) -> str
│   │         - 调用llm_client.chat_with_response_format()
│   │         - 解析JSON响应
│   │
│   ├── llm_adapter.py              → LLMAdapter类
│   │     核心函数:
│   │       __init__(api_base, api_key, model)
│   │       ensure_capability() -> SelectedStrategy
│   │         - 探测LLM能力并选择最佳策略
│   │       _detect_capability() -> LLMProbeResult
│   │         - 调用CapabilityDetector探测
│   │
│   ├── os_adapter.py               → OSAdapter类
│   │     核心函数:
│   │       __init__()
│   │         - 检测当前系统(Windows/Linux/Mac)
│   │       get_system_prompt() -> str
│   │         - 返回系统相关提示
│   │       get_tool_descriptions() -> dict
│   │         - 返回工具描述
│   │
│   ├── capability.py               → 能力枚举
│   │     核心类:
│   │       LLMCapability枚举(NONE, RESPONSE_FORMAT, TOOLS, STREAMING, REASONING)
│   │       LLMFeature数据类(supports_tools, supports_response_format, ...)
│   │       LLMProbeResult数据类(tools_works, response_format_works, ...)
│   │
│   ├── capability_detector.py      → CapabilityDetector类
│   │     核心函数:
│   │       __init__(api_base, api_key, model)
│   │       detect() -> LLMProbeResult
│   │         - 探测response_format支持
│   │         - 探测tools支持
│   │         - 探测reasoning特征
│   │
│   ├── strategy_selector.py        → StrategySelector类
│   │     核心函数:
│   │       select(capability) -> SelectedStrategy
│   │         - tools支持→tools策略
│   │         - response_format支持→response_format策略
│   │         - 都不支持→prompt策略
│   │
│   ├── session.py                  → FileOperationSessionService类
│   │     核心函数:
│   │       create_session(agent_id, task_description) -> str
│   │         - 创建会话，返回session_id
│   │       complete_session(session_id, success) -> None
│   │         - 完成会话
│   │       get_session(session_id) -> dict
│   │         - 获取会话信息
│   │
│   ├── session_base.py             → SessionServiceBase抽象基类
│   │     核心函数:
│   │       create_session(agent_id, task_description) -> str  [抽象]
│   │       complete_session(session_id, success) -> None  [抽象]
│   │       get_session(session_id) -> dict  [抽象]
│   │       get_recent_sessions(limit) -> list  [抽象]
│   │       _generate_session_id() -> str
│   │         - 生成sess-{uuid}格式ID
│   │
│   ├── intent/
│   │   ├── registry.py             → IntentRegistry类
│   │   │   核心函数:
│   │   │     register(intent) -> None
│   │   │       - 注册意图到注册表
│   │   │     get(name) -> Intent
│   │   │       - 获取指定名称的意图
│   │   │     list_all() -> list
│   │   │       - 列出所有已注册意图
│   │   │     get_all_names() -> list
│   │   │       - 获取所有意图名称
│   │   │
│   │   └── classifier.py           → IntentClassifier类
│   │       核心函数:
│   │         classify(preprocessed, context) -> list
│   │           - 置信度>=0.7: 使用GLiClass结果
│   │           - 置信度<0.7: 回退到关键词匹配
│   │           - 都失败: 返回空列表
│   │         _keyword_match(text) -> list
│   │           - 关键词匹配，可返回多个意图
│   │
│   ├── preprocessing/
│   │   ├── corrector.py            → TextCorrector类
│   │   │   核心函数:
│   │   │     correct(text) -> tuple
│   │   │       - 调用pycorrector修正文本
│   │   │       - 返回(修正后文本, 修正记录)
│   │   │       - None/空输入返回("", [])
│   │   │
│   │   ├── intent_classifier.py    → GLiClass意图检测
│   │   │   核心函数:
│   │   │     classify(text, labels) -> dict
│   │   │       - 返回{intent, confidence, all_intents}
│   │   │       - 空文本返回unknown
│   │   │
│   │   └── pipeline.py             → PreprocessingPipeline类
│   │       核心函数:
│   │         process(text, intent_names) -> dict
│   │           - 整合corrector和classifier
│   │           - 返回{original, corrected, errors, intent, confidence, all_intents}
│   │           - 校对失败使用原始文本
│   │           - 分类失败返回unknown
│   │
│   └── types/
│       ├── step_types.py           → Step数据类
│       │     核心类:
│       │       Step(step_number, thought, action, action_input, ...)
│       │       ThoughtStep(内容, 推理, 标签)
│       │       ActionToolStep(工具名, 参数, 状态)
│       │       ObservationStep(观察内容, 状态)
│       │
│       ├── result_types.py         → AgentResult数据类
│       │     核心类:
│       │       AgentResult(success, message, steps, total_steps, ...)
│       │
│       └── react_schema.py         → 工具Schema生成
│             核心函数:
│               get_tools_schema_for_function_calling() -> list
│                 - 生成所有工具的OpenAI格式Schema
│               get_tool_schema(func) -> dict
│                 - 生成单个工具的Schema
│               validate_tool_call(call) -> bool
│                 - 验证tool_calls格式
│
├── tools/
│   └── file/
│       ├── file_tools.py           → FileTools类
│       │     核心函数:
│       │       read_file(file_path, offset, limit, encoding) -> dict
│       │       write_file(file_path, content, encoding) -> dict
│       │       list_directory(dir_path, recursive, max_depth, page_size, page_token) -> dict
│       │       delete_file(file_path, recursive) -> dict
│       │       move_file(source_path, destination_path) -> dict
│       │       search_files(pattern, path, file_pattern, use_regex, max_results) -> dict
│       │       generate_report(output_dir) -> dict
│       │
│       └── file_schema.py          → Pydantic输入模型
│             核心模型:
│               ReadFileInput(file_path, offset, limit, encoding)
│               WriteFileInput(file_path, content, encoding)
│               ListDirectoryInput(dir_path, recursive, max_depth, page_size)
│               DeleteFileInput(file_path, recursive)
│               MoveFileInput(source_path, destination_path)
│               SearchFilesInput(pattern, path, file_pattern, use_regex, max_results)
│               GenerateReportInput(output_dir)
│
├── safety/
│   └── file/
│       └── file_safety.py          → FileOperationSafety类
│             核心函数:
│               record_operation(session_id, operation_type, source_path) -> str
│               execute_with_safety(session_id, operation_type, func, ...) -> dict
│               rollback_operation(operation_id) -> bool
│               rollback_session(session_id) -> dict
│               get_session_operations(session_id) -> list
│               get_operation(operation_id) -> dict
│
├── intents/
│   └── definitions/
│       └── file/
│           ├── file_intent.py      → FileIntent模型
│           │     字段:
│           │       name="file"
│           │       description="文件读写、目录管理、文件搜索等文件操作"
│           │       keywords=["文件","读取","写入","删除","移动","搜索",...]
│           │       tools=["read_file","write_file","list_directory","delete_file","move_file","search_files","generate_report"]
│           │       safety_checker="file_safety"
│           │
│           └── file_stats.py       → FileSessionStats模型
│                 字段:
│                   rolled_back_count: int = 0
│                   report_generated: bool = False
│                   report_path: str = None
│                   report_type: str = None
│
└── prompts/
    ├── base.py                     → BasePrompts抽象基类
    │     核心函数:
    │       get_system_prompt() -> str  [抽象]
    │       get_available_tools_prompt() -> str  [抽象]
    │       get_task_prompt(task, context) -> str
    │       get_observation_prompt(observation) -> str
    │       get_safety_reminder() -> str
    │       get_parameter_reminder() -> str
    │       build_full_system_prompt() -> str
    │
    └── file/
        └── file_prompts.py         → FileOperationPrompts类(继承BasePrompts)
              核心函数:
                get_system_prompt() -> str
                  - 返回文件操作Agent系统提示
                get_available_tools_prompt() -> str
                  - 返回7个工具的描述
                get_task_prompt(task, context) -> str
                  - 返回任务提示
                get_observation_prompt(observation) -> str
                  - 返回观察结果提示
                get_parameter_reminder() -> str
                  - 返回参数命名规则
                get_safety_reminder() -> str
                  - 返回安全提醒
```

---

## 重构完成度

### 12.2节目录结构

```
设计要求                          实际状态
──────────────────────────────────────────────
agent/                            ✅ 存在
├── preprocessing/                ✅ 存在
│   ├── corrector.py              ✅ 存在
│   ├── intent_classifier.py      ✅ 存在
│   └── pipeline.py               ✅ 存在
├── intent/                       ✅ 存在
│   ├── registry.py               ✅ 存在
│   └── classifier.py             ✅ 存在
├── types/                        ✅ 存在
│   ├── step_types.py             ✅ 存在
│   ├── result_types.py           ✅ 存在
│   └── react_schema.py           ✅ 存在
├── tool_parser.py                ✅ 存在
├── tool_executor.py              ✅ 存在
├── llm_strategies.py             ✅ 存在
├── agent.py                      ✅ 存在(已改名IntentAgent)
├── llm_adapter.py                ✅ 存在
├── capability.py                 ✅ 存在
├── capability_detector.py        ✅ 存在
├── strategy_selector.py          ✅ 存在
├── session.py                    ✅ 存在
└── session_base.py               ✅ 存在

tools/file/                       ✅ 存在
├── file_tools.py                 ✅ 存在
└── file_schema.py                ✅ 存在

safety/file/                      ✅ 存在
└── file_safety.py                ✅ 存在

intents/definitions/file/         ✅ 存在
├── file_intent.py                ✅ 存在
└── file_stats.py                 ✅ 存在

prompts/                          ✅ 存在
├── base.py                       ✅ 存在
└── file/
    └── file_prompts.py           ✅ 存在
```

### 12.4节拆分要求

```
设计要求                          实际状态
──────────────────────────────────────────────
FileOperationAgent→IntentAgent    ✅ 已重命名
agent.py精简(840→662行)            ✅ 已精简
ToolParser拆分                    ✅ 已拆分
ToolExecutor拆分                  ✅ 已拆分
LLM策略拆分                       ✅ 已拆分
session_base.py新建               ✅ 已新建
intent_type参数                   ✅ 已实现
预处理流水线集成                   ✅ 已集成
意图注册表集成                     ✅ 已集成
向后兼容层删除                     ✅ 已删除
```

---

**更新时间**: 2026-03-22 11:00:00
**版本**: v1.0
