# 会话记录-2026-05-31

**创建时间**: 2026-05-31 19:30:36  
**存放位置**: G:\OmniAgentAs-desk\

---

## 1 问题28（OCP统一）最终改造 【北京老陈要求6处定义全部统一到一个文件】2026-05-31 19:30:36

### 1.1 Goal

完成后端代码 10 大原则深度核查报告中的问题 28：将散布在 `tool_types.py` + `intent_mapper.py` + `crss_scorer.py` 的 6 处分类/意图/关键词定义，全部统一到 `tool_types.py` 一个文件，实现 **OCP（开闭原则）**——新增分类/意图只需改一个文件。

### 1.2 Instructions

老陈要求：
> "６个地方不是都应该统一到一个地方来吗？"
> 全部搬到 `tool_types.py`，`intent_mapper.py` 只做重新导出

### 1.3 Accomplished

#### 步骤 1：`tool_types.py` 扩展（单一定义源）

1. **新增 `IntentType` 枚举** — 从 `intent_mapper.py` 搬迁过来，加 `category` 属性自动派生 `ToolCategory`
2. **新增 `_CRSS_REGISTRY` 字典** — 一个地方定义所有 CRSS 意图：
   ```python
   _CRSS_REGISTRY: Dict[str, tuple[str, dict]] = {
       "FILE": ("FILE", {"keywords": [...], "chinese_keywords": [...]}),
       "SHELL": ("SYSTEM", {...}),
       ...
   }
   ```
3. **`INTENT_MAPPING` 自动派生** — `{name: IntentType[type_name] for name, (type_name, _) in _CRSS_REGISTRY.items()}`
4. **`CRSS_TYPE_KEYWORDS` 自动派生** — `{name: kw for name, (_, kw) in _CRSS_REGISTRY.items()}`
5. **更新 `__all__`** — 加入 `IntentType`、`INTENT_MAPPING`、`CRSS_TYPE_KEYWORDS`

#### 步骤 2：`intent_mapper.py` 瘦身

- **删除** `IntentType` 枚举定义
- **删除** `INTENT_MAPPING` 字典定义  
- **删除** `CRSS_TYPE_KEYWORDS` 字典定义
- **保留** `INTENT_ALIASES`（用户输入别名映射，不属于 OCP 数据）
- **保留** 所有映射函数（`_map_intent_to_agent`、`get_crss_intent_names` 等）
- **新增** `from app.services.tools.tool_types import ToolCategory, IntentType, INTENT_MAPPING`

#### 步骤 3：更新 3 个文件的 import 路径

| 文件 | 变更 |
|------|------|
| `crss_scorer.py` | `INTENT_MAPPING, CRSS_TYPE_KEYWORDS` 改从 `tool_types` 导入 |
| `agent_config.py` | `IntentType` 改从 `tool_types` 导入，函数仍从 `intent_mapper` 导入 |
| `build_intent_prompt.py` | `IntentType` 改从 `tool_types` 导入 |

#### 验证

- ✅ `pytest tests/ -x --tb=short -q` → **13 passed**
- ✅ 无导入错误、无循环依赖

### 1.4 In Progress

- 无

### 1.5 Remaining Tasks

无（39/39 问题全部归零，进入下一阶段开发）

### 1.6 当前文件结构（问题28后）

```
tool_types.py（单一定义源，6个定义全部在此）
├── IntentType 枚举
├── ToolCategory 枚举（__new__携带元数据）
├── _CRSS_REGISTRY 字典（新增，驱动下面2个）
├── INTENT_MAPPING          ← 自动派生
├── CRSS_TYPE_KEYWORDS      ← 自动派生
├── INTENT_TO_CATEGORY      ← 自动派生
├── CATEGORY_ORDER          ← 自动派生
├── CATEGORY_NAMES          ← 自动派生
└── ToolMetadata

intent_mapper.py（仅保留 INTENT_ALIASES + 映射函数）
└── INTENT_ALIASES
└── _map_intent_to_agent / resolve_category / ...
```

**新增分类/意图流程（OCP）**：编辑 `tool_types.py` 的 `_CRSS_REGISTRY` + `ToolCategory` → 所有派生数据自动更新

---

**更新时间**: 2026-05-31 19:30:36
