# 全链路端到端集成测试方案（Full-Link E2E Integration Test）

**创建时间**: 2026-05-23 01:00:44  
**版本**: v2.3  
**编写人**: 小健

---

## 版本记录

| 版本 | 时间 | 签名 | 更新内容 |
|------|------|------|---------|

| v1.8 | 2026-06-15 12:43:30 | 小健 | P3-SY全部13个PASSED(累计32个零失败)；新增assert_stream_ended()统一流结束验证；send_chat新增partial_result参数(超时/中断保留数据)；write_test_record新增resp_has_error(回复含错误关键词标FAIL)+fallback DB check(db空自动补查)；Stream结束三种方式(final/error/中断)全部视为有效结束；has_error从MUST降为SHOULD WARN(不阻塞只记录)；错误关键词精确化(避免"超时时间"误报)；3.4/3.5/3.6/4.4/4.12/5.3/5.4/5.5/7对应同步更新 |
| v1.9 | 2026-06-18 19:20:00 | 小欧 | 3.2新增步骤6单用例执行后强制检查门禁(调用链分析+测试记录验证，两项都正常才能进下一个case)；write_test_record核心函数新增[RECORD OK]/[RECORD FAIL]输出+调用链输出+返回文件路径；步骤编号修正(4.5→5, 4.6→6, 原5-9→7-11)；步骤5-9从3.3移入3.2统一编号 |
| v2.0 | 2026-06-18 19:50:00 | 小欧 | 核心代码已实现的步骤(2/3/4/7/8/10/11)在e2e_helpers.py加注释标明手册对照；文档3.2/3.3删除已实现步骤的详细要求，只保留未实现/需人工的步骤(1/5/6/9) |
| v2.1 | 2026-06-18 20:00:00 | 小欧 | 新增record_test_baseline()(步骤1 DB base count+日志大小)、verify_response_quality()(3.4回复长度检查)、verify_response_time()(3.4响应时间阈值检查)；文档3.2/3.4同步更新标记 |
| v2.2 | 2026-06-18 20:10:00 | 小欧 | 3.4删除已实现的14项验证标准(只保留工具选择+参数正确性2项需人工)；3.5删除已实现的铁律+模板(只保留一行引用)；e2e_helpers.py文件头docstring补充3.4/3.5全部已实现项对照 |
| v2.3 | 2026-06-18 20:40:00 | 小欧 | 按北京老陈4步流程重构：3.1检查→3.2编写脚本→3.3执行脚本→3.4检查结果→3.5错误处理，合并原3.1ABC+3.2+3.3碎片为统一结构，删除所有旧编号(步骤1/5/6/9) |

---

## 一、测试定义

### 1.1 测试名称

**全链路端到端集成测试**（Full-Link End-to-End Integration Test）

**唯一目标**：用真实LLM（后端从config.yaml读取配置）模拟真实用户操作，把系统中隐藏的问题炸出来。

**具体来说**：
1. Agent在真实LLM下能否正确完成用户请求
2. Agent在异常情况下（LLM返回垃圾/空内容/错误工具）能否容错
3. 系统各组件的串联是否完整（API→Agent→LLM→工具→DB）
4. 日志和数据库记录是否完整准确


### 1.2 为什么叫这个名

| 关键词 | 含义 | 在这个测试中的体现 |
|--------|------|-------------------|
| 全链路 | 覆盖系统所有组件 | 前端API→后端→Agent→LLM→工具执行→数据库→SSE响应 |
| 端到端 | 从用户发消息到收到完整结果 | POST /chat → SSE事件流 → final事件 |
| 集成 | 验证各组件对接是否正确 | API路由、SSE封装、Agent循环、LLM调用、工具执行、数据库写入 |

### 1.3 前置环境依赖分析（v1.7 - 小健 - 2026-06-15 核实）

| 依赖项 | 手册要求 | 当前状态 | 需要准备 |
|--------|---------|---------|---------|
| 后端服务 | uvicorn在8000端口 | 已运行 | 每次测试前需确认存活 |
| config.yaml | security.enabled=false | 需每次测试前设置 | 测试脚本已自动处理 |
| E:\test_dir | 测试根目录 | 已存在 | OK |
| E:\test_dir\test.txt | 内容"hello world" | **"hello world"** | OK（已核实 2026-06-15） |
| E:\test_dir\test_old.txt | 内容"old content" | **"old content"** | OK（已核实 2026-06-15） |
| E:\test_dir\backup | 空目录 | **空目录** | OK（已核实 2026-06-15） |
| E:\test_dir\data.csv | 含2026年数据 | 5行数据含3条2026年 | OK |
| E:\test_dir\test.docx | 简单Word文档 | 已存在 | OK |
| E:\test_dir\empty_dir | 空目录 | 已存在且为空 | OK |
| 数据库 | chat_history.db可写 | 后端内部可写 | e2e_helpers已改用API |
| 日志目录 | logs/可写 | 已存在 | OK |

### 1.4 与现有测试的区别

| 测试类型 | 测试什么 | 用什么驱动 | 能发现什么问题 |
|---------|---------|-----------|--------------|
| 单元测试（现有~200个） | 单个工具函数 | Mock/Patch | 函数级bug |
| MessageBuilder测试（43个） | Prompt构建逻辑 | Mock | Prompt构建bug |
| **全链路E2E（本方案）** | **Agent行为+系统集成** | **真实后端（读取config.yaml的LLM配置）+ 真实SQLite** | **死循环、工具选错、参数传错、会话污染、日志异常、数据库异常** |


### 1.5 核心测试原则（铁律）

**铁律1：测试的目的是发现问题，不是跑脚本。**

**铁律2：严禁批量跑测试用例。一次只能跑一个测试用例。**

| 允许 | 禁止 |
|------|------|
| ✅ 写一个脚本 → 跑一个 → 观察 → 检查 | ❌ 一次写完所有脚本 |
| ✅ 一个用例跑通后再写下一个 | ❌ 一次批量跑多个用例 |
| ✅ 每次跑完仔细观察系统运行过程 | ❌ 只看pass/fail就过 |
| ✅ 失败了停下来分析修复 | ❌ 失败了跳过继续下一个 |

**铁律3：所有测试统一基于真实后端运行，禁止Mock：**

| 环节 | 要求 | 禁止 |
|------|------|------|
| **后端** | 真实uvicorn启动，从config.yaml读取LLM配置 | ❌ 不启动后端、Mock后端 |
| **LLM** | 真实调用config.yaml中配置的provider/model | ❌ Mock LLM返回、假响应 |
| **工具** | 真实执行文件操作、命令、网络请求等 | ❌ Mock工具返回值 |
| **数据库** | 真实读写SQLite（~/.omniagent/chat_history.db） | ❌ Mock数据库、内存数据库 |
| **SSE** | 真实解析后端返回的SSE事件流 | ❌ 假造SSE事件 |

**一句话：测试输入就是用户会说的话，测试过程就是真实系统的完整运行。**

---

## 二、测试架构
###  2.1  测试架构
```
┌─────────────────────────────────────────────────────────────────┐
│                      测试脚本（pytest）                          │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    测试执行引擎                            │   │
│  │  ① 初始化测试环境                                         │   │
│  │  ② 通过httpx发送POST /chat → 接收SSE事件流               │   │
│  │  ③ 逐事件解析、记录、断言                                  │   │
│  │  ④ 查询日志文件，检查ERROR/WARNING                        │   │
│  │  ⑤ 查询SQLite数据库，检查session/messages/steps          │   │
│  │  ⑥ 清理测试残留文件（DB记录保留供检查）                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│         ↓ HTTP POST /chat           ↓ SSE event stream          │
│    ┌──────────────────────────────────────────────────┐         │
│    │              后端服务（uvicorn）                   │         │
│    │  main.py → chat_router.py → Agent → LLM → 工具   │         │
│    │        ↑                                         │         │
│    │   从 config.yaml 读取 LLM provider/model        │         │
│    │                          ↓                        │         │
│    │                    SQLite DB / 日志文件             │         │
│    └──────────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```
###  2.2  测试架构E2E测试模板目录

模板目录位置: `backend/e2emodel/`

```
backend/e2emodel/
├── test_e2e_p0_01_introduce_self.py    ← 模板A: 无工具调用
├── test_e2e_p0_02_tool_call.py         ← 模板B: 单工具调用
├── test_e2e_p0_03_multi_step.py        ← 模板C: 多步推理
├── test_e2e_p0_04_data_persistence.py  ← 模板D: 数据持久化
└── e2e_helpers.py                      ← 公共/核心函数库(唯一来源)
```
**四类模板说明**:

| 模板 | 适用场景 | 典型用例 | 特点 |
|------|---------|---------|------|
| **模板A (P0-01)** | LLM直接回复，不调用工具 | 自我介绍、闲聊、知识问答 | 无工具断言，侧重回复语义验证 |
| **模板B (P0-02)** | LLM调用单个工具完成任务 | 创建文件、读取文件 | 单工具调用，侧重SSE含action事件+tool_calls验证 |
| **模板C (P0-03)** | LLM多步推理 | 先读文件再回复内容 | 验证多step记录+tool_name非空+observation存在 |
| **模板D (P0-04)** | LLM调用工具后验证DB三表 | 目录列表、数据持久化 | 侧重DB三表完整验证+steps字段完整性 |
---

## 三、完整测试流程

每个测试用例严格按 **4步流程** 执行：检查 → 编写脚本 → 执行脚本 → 检查结果。

---

### 3.1 第1步：检查

**每次执行测试前，逐项检查，全部通过才能开始。**

```
检查项 0: 先杀死旧的 uvicorn 进程,重启后端服务（每次测试前必须执行）
  · 先杀死旧的 uvicorn 进程
    Windows: taskkill /F /IM python.exe /FI "WINDOWTITLE eq uvicorn*"
    或找到占用 8000 端口的进程杀掉
  · 重新启动后端
    cd backend && python -m uvicorn app.main:app --reload
  · 等待启动完成（约3-5秒）
  · 检查后端是否正常:
    curl http://localhost:8000/docs 返回 200
  · 失败处理: 检查端口占用/代码错误/配置问题,修复后重新启动
  · 即使使用 --reload,也必须确认后端正常响应才能继续

检查项 1: 后端服务已确认正常
  · 接检查项0的结果,确认返回200

检查项 2: LLM 配置
  · config.yaml 中 ai.provider 是否已配置有效 provider
  · 检查方法: 后端启动日志无 LLM 连接错误
  · 失败处理: 检查 config.yaml 的 ai 配置段

检查项 3: 安全开关
  · config.yaml 中 security.enabled 是否为 false（纯功能验证模式）
  · 检查方法: grep "enabled" config/config.yaml
  · 失败处理: 修改 config.yaml 设 security.enabled: false

检查项 4: 数据库
  · ~/.omniagent/chat_history.db 是否存在且可写
  · 检查方法: sqlite3 查询 SELECT count(*) FROM sessions
  · 失败处理: 检查目录权限

检查项 5: 日志目录
  · backend/logs/ 和 backend/logs/prompt-logs/ 是否可写
  · 检查方法: 尝试写入测试文件
  · 失败处理: 创建目录或修复权限

检查项 6: 测试数据完备性（铁律：每个用例跑前必须自检）
  · 测试用的目录和文件是否就绪
  · 检查方法: Test-Path 检查关键路径
  · 失败处理: 创建测试目录和文件

以上全部通过 → 进入第2步
任一项未完成 → 不开始测试
```

---

### 3.2 第2步：编写用例脚本

**铁律：一次只写一个，写完跑通再写下一个。**
测试case的脚本 必须在目录: `backend/tests/` 下创建
```
禁止一次性编写所有脚本！
  · 一次性写完 → 无法验证每个脚本是否正确
  · 一次性写完 → 错误会累积,难以定位
  · 批量测试 = 自欺欺人,发现不了真正的问题

正确做法: 一个用例一个脚本,写完跑通再写下一个

  第1个: 看测试用例 E2E-P0-01 "详细介绍自己"
         → 要测什么? (API→SSE通路)
         → 要发什么指令? ("详细介绍一下你自己，你能做什么")
         → 要验证什么? (收到final、无error、DB有记录)
         → 编写 test_e2e_p0_01_introduce_self()
         → 运行 → 观察 → 检查
         → 通过 → 进入第2个
         → 失败 → 分析错误、修复、重跑、通过后进入第2个

  第2个: 看测试用例 E2E-P0-02 "创建文件"
         → 编写 → 运行 → 观察 → 检查
         → 通过 → 进入第3个

  ...逐个推进...

编写顺序: P0 → P1 → P2 → P5 → P6 → P7 → P8 → P9 → P10 → P3 → P4
  · 先写最简单的（P0 hello）
  · 确认基础流程通了再写复杂的
  · 每写完一个,立即运行观察系统行为
```
脚本分为:用例case脚本和核心测试过程的脚本,有典型性问题修改都必须修改 e2e模板库的 核心e2e_helpers.py代码,
---

### 3.3 第3步：执行脚本

**每个用例执行时，记录起始时间，跑完后过门禁。**

```
记录测试起始时间 T0（测试脚本手动记录）

### 3.4 第4步：检查结果

**用例执行完毕后，逐项验证，全部通过才算完成。**

```
用例执行完毕
      │
      ▼
┌─────────────────────────────────────┐
│ 验证项1: 代码错误异常检查（铁律）    │
│  · 检查日志中是否有traceback/ERROR   │
│  · 检查SSE中是否有error事件          │
│  · 有 → 立即停止,走3.5修复流程       │
│  · 无 → 继续验证项1                  │
│  · 原则: 代码错误优先级最高,必须先修  │
└─────────────────────────────────────┘
      │
      │ (无代码错误)
      ▼
┌─────────────────────────────────┐
│ 验证项2: 调用链分析（手动）       │
│  · 对照手册"预期调用链"逐个核对   │
│  · 工具选择是否合理               │
│  · 调用顺序是否合理               │
│  · LLM调用次数是否合理            │
│  · 输出: [CALL CHAIN] tool→tool  │
│  · 正常标准: 与预期一致或语义等价  │
└─────────────────────────────────┘
      │
      │
      ▼
┌─────────────────────────────────┐
│ 验证项3: 参数正确性（人工）       │
│  · tool_params中的路径/关键词正确 │
└─────────────────────────────────┘
      │
      ▼
   全部通过？
    ├─ 是 → 本用例完成，进入下一个
    └─ 否 → 执行错误处理（见3.5）
```

### 3.5 分析的错误处理报告和处理流程（第4步发现FAIL时的必须流程）

```
错误分两类:
  A类(运行时): LLM超时/工具崩溃/死循环/SSE断流 → 查日志traceback
  B类(数据不一致): session缺失/messages丢失/steps不匹配 → 对比SSE vs DB

处理流程:
  ① 分类 → A类还是B类
  ② 定位 → 查后端日志 + SSE事件 + prompt-logs,定位到文件:行号
  ③ 分析 → 是代码bug还是测试用例问题
  ④ 修复 → 遵守10大规范 (SRP/DRY/KISS/YAGNI,禁止backward compat)
  ⑤ 复测 → 重跑该用例 + 跑关联测试确认无回归
  ⑥ 记录 → 写入测试报告

严重等级:
  P0(致命): 系统崩溃/死循环 → 立即停止,修复后从P0重跑
  P1(严重): 功能异常但不崩溃 → 修复后重跑该用例
  P2(一般): 非核心异常 → 记录,批量修复
  P3(警告): 性能偏差 → 记录,不阻塞

禁止事项:
  ❌ 看到FAIL就跳过   ❌ 只改测试不改代码
  ❌ 修复时不跑单元测试  ❌ 不记录直接修复
```

## 四、测试场景用例清单（共57个工具覆盖，1个异步工具专项测试）

**设计原则：**
1. 每个测试必须至少覆盖1个工具，所有57个工具至少被覆盖1次（timer异步工具单独测试）
2. 单工具任务（P1-P4）验证每个工具在真实LLM下能否被正确识别和调用
3. 复合任务（P5）验证多工具串联、多步推理能力
4. 异常任务（P6）验证系统容错和恢复能力
5. 覆盖率追踪：每节末尾标注已覆盖工具数和累计覆盖率

**前置准备（测试数据统一创建）：**

在开始所有测试前，先统一创建测试所需的基础文件和目录：

```
【基础目录结构】
· E:\test_dir\                     — 测试根目录
· E:\test_dir\backup\              — 空目录（供复制/归档测试）
· E:\test_dir\empty_dir\           — 空目录（供空结果测试）
· E:\test_dir\reports\             — 空目录（供汇总报告存放）

【基础测试文件】
· E:\test_dir\test.txt             — 内容"hello world"（供读取/搜索/编辑测试）
· E:\test_dir\test_old.txt         — 内容"old content"（供压缩测试）
· E:\test_dir\data.csv             — 内容如下（供数据分析测试）:
  
· E:\test_dir\test.docx            — 简单Word文档，内容"这是测试文档内容"（供文档读取测试）

【系统环境要求】
· python命令在PATH中（开发环境检查用）
· git命令在PATH中（开发环境检查用）
· 网络连通（网络诊断/搜索/获取网页用）
· 记事本窗口打开（窗口管理流程用，可手动打开或脚本自动打开）
· chat_history.db存在且有数据（数据库分析用，后端启动后自动创建）

【缺失数据补充方案】
1. python+git环境: 测试前执行`where python`和`where git`确认
2. 网络连通: 测试前执行`ping baidu.com`确认
3. 记事本窗口: 测试前手动打开记事本，或在脚本中用`os.system("notepad")`自动打开
4. chat_history.db数据: 先启动后端并发几条消息产生数据，或手动INSERT测试数据

【每个用例的数据清理规则】

```

---

### 4.1 P0：核心链路验证（4个任务）

| 编号 | 任务 | 用户输入 | 前置数据 | 预期过程 | 通过标准 | 失败标准 |
|------|------|---------|---------|---------|---------|---------|
| E2E-P0-01 | LLM对话通路 | `"详细介绍一下你自己，你能做什么"` | 无 | LLM直接回复，不调用工具 | 收到final事件；回复语义完整；无error事件 | 超时未收到final；回复为空或胡言乱语；收到error |
| E2E-P0-02 | 工具调用通路 | `"在E盘根目录创建一个e2e_test_p0.txt，内容为hello"` | E盘根目录可写 | Agent调用write_text_file，返回成功 | SSE包含action事件；DB有execution_steps记录；文件存在且内容正确 | 未调用工具；文件未创建；DB无记录 |
| E2E-P0-03 | 多步推理通路 | `"读取E:\test_dir\test.txt的内容，然后告诉我里面写了什么"` | test.txt存在且内容="hello world" | 先调read_file，再回复内容 | 调用了read_file；回复中包含文件内容；DB有2条steps | 未调用read_file；回复中无文件内容 |
| E2E-P0-04 | 数据持久化通路 | `"列出E:\test_dir下的所有文件"` | E:\test_dir\目录存在且有文件(test.txt等) | 调用list_directory，返回文件列表 | sessions表有记录；messages表有user+assistant；execution_steps有步骤记录 | 任一表无记录；记录不完整 |

---

### 4.2 P1：FILE工具多任务场景（15个工具 → 5个真实工作场景）

**设计原则**：每个case都是真实工作场景，至少调用3-5个工具，多轮交互完成。

| 编号 | 工作场景 | 用户输入 | 前置数据 | 预期调用链(多工具) | 通过标准 | 失败标准 |
|------|---------|---------|---------|-------------------|---------|---------|
| E2E-P1-01 | 项目初始化流程 | `"我要在E:\test_dir下创建一个新项目myproject，帮我：①创建项目目录 ②创建README.md写入项目说明 ③创建src子目录 ④在src下创建main.py骨架 ⑤列出项目结构确认都创建好了，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | E:\test_dir\可写；用例后清理myproject目录 | write_text_file(README)→write_text_file(main.py)→list_directory→write_text_file(汇总报告) | myproject目录存在；README.md和main.py都存在；回复包含项目结构 | 任一文件未创建 |
| E2E-P1-02 | 日志分析流程 | `"E:\test_dir\下有个test.txt文件，帮我：①先读出来看看内容 ②搜索里面有没有error或warning关键词 ③把内容和搜索结果整理成分析报告 ④保存到E:\test_dir\analysis_report.txt ⑤确认报告文件已生成，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | test.txt存在，内容="hello world"；用例后删除analysis_report.txt | read_text_file→grep_file_content→write_text_file→read_text_file(确认) | analysis_report.txt存在且包含原文内容和搜索结果 | 分析报告未生成 |
| E2E-P1-03 | 代码重构流程 | `"我要重构E:\test_dir\test.txt，帮我：①先备份到test_backup.txt ②把hello改成你好 ③读取修改后的内容确认改对了 ④对比备份和修改后的差异 ⑤把重构过程记录到refactor_log.txt，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | test.txt存在，内容="hello world"；用例后恢复test.txt为"hello world"，删除test_backup.txt和refactor_log.txt | copy_file→edit_text_file→read_text_file→write_text_file | test_backup.txt存在；test.txt内容含"你好"；refactor_log.txt存在 | 任一步骤失败 |
| E2E-P1-04 | 数据归档流程 | `"帮我把E:\test_dir下的文件整理归档：①列出所有文件 ②把test.txt和test_old.txt打包成archive.zip ③把zip移动到backup目录 ④解压验证内容完整 ⑤清理临时文件，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | test.txt和test_old.txt存在；backup/为空；用例后清理backup下zip和解压文件 | list_directory→compress_files→move_file→extract_archive→list_directory(验证) | backup下有archive.zip；解压后有原文件 | 归档失败 |
| E2E-P1-05 | 配置管理流程 | `"我要管理项目配置：①读取E:\test_dir\data.csv看看数据格式 ②创建config.json配置文件记录数据源路径 ③创建settings.yaml记录处理参数 ④列出目录确认配置文件都创建了 ⑤把配置信息汇总报告，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | data.csv存在；用例后删除config.json和settings.yaml | read_text_file→write_data_file(json)→write_data_file(yaml)→list_directory→write_text_file | config.json和settings.yaml都存在且可读 | 配置文件未创建 |

**覆盖率**：5个场景覆盖15个FILE工具(read/write/edit/copy/move/compress/extract/list/search/grep/read_data/write_data)

---

### 4.3 P2：SHELL+NETWORK多任务场景（9个工具 → 4个真实工作场景）

| 编号 | 工作场景 | 用户输入 | 前置数据 | 预期调用链(多工具) | 通过标准 | 失败标准 |
|------|---------|---------|---------|-------------------|---------|---------|
| E2E-P2-01 | 开发环境检查流程 | `"我要检查开发环境是否就绪：①查找python命令路径 ②查找git命令路径 ③执行python --version确认版本 ④执行git --version确认版本 ⑤把环境检查结果保存到E:\test_dir\env_check.txt，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | python和git在PATH中；用例后删除env_check.txt | find_command(python)→find_command(git)→execute_shell_command(python)→execute_shell_command(git)→write_text_file | env_check.txt存在且包含python和git路径及版本 | 任一命令找不到 |
| E2E-P2-02 | 网络诊断修复流程 | `"网络好像有问题，帮我诊断修复：①ping测试baidu.com连通性 ②如果通了就获取百度首页内容 ③检查当前网络连接状态 ④把诊断结果和网页内容保存报告 ⑤给出网络状态建议，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 网络连通；用例后删除网络报告 | network_diagnose(ping)→fetch_webpage→net_connections→write_text_file | 报告包含ping结果、网页内容、连接状态 | 诊断失败 |
| E2E-P2-03 | 技术调研流程 | `"我要调研Python异步编程：①搜索Python asyncio最佳实践 ②打开第一个结果的网页详细内容 ③执行一段asyncio示例代码验证 ④把调研结果和代码运行结果保存报告 ⑤列出报告文件确认，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 网络连通；用例后删除调研报告 | search_web→fetch_webpage→execute_code→write_text_file→list_directory | 报告包含搜索结果、网页内容、代码执行结果 | 任一步骤失败 |
| E2E-P2-04 | 后台任务监控流程 | `"我要启动后台任务并监控：①启动后台ping baidu.com持续5秒 ②等2秒后查看输出 ③再等2秒查看最新输出 ④终止后台任务 ⑤把监控日志保存报告，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 网络连通；用例后删除监控报告 | execute_shell_command(后台)→shell_session(读输出)→shell_session(读输出)→shell_session(终止)→write_text_file | 报告包含多次ping输出 | 后台任务管理失败 |

**覆盖率**：4个场景覆盖9个SHELL+NETWORK工具(execute/find/session/code/search/fetch/diagnose/http/connections)

---

### 4.４ P4：DOCUMENT+DATAANALYSIS多任务场景（15个工具 → 5个真实工作场景）

| 编号 | 工作场景 | 用户输入 | 前置数据 | 预期调用链(多工具) | 通过标准 | 失败标准 |
|------|---------|---------|---------|-------------------|---------|---------|
| E2E-P4-01 | 文档处理流程 | `"我要处理一批文档：①读取E:\test_dir\test.docx内容 ②创建新的Word文档写入处理结果 ③把原文档转成PDF格式 ④列出目录确认文件都生成了 ⑤记录处理过程，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | test.docx存在；用例后删除生成的文档和PDF | read_docx→write_docx→convert_document→list_directory→write_text_file | 新Word文档和PDF都存在 | 任一步骤失败 |
| E2E-P4-02 | 数据分析报告流程 | `"我要分析销售数据并生成报告：①读取E:\test_dir\data.csv数据 ②分析数据的统计特征(均值/最大/最小) ③筛选2026年的数据 ④生成柱状图可视化 ⑤把分析结果保存到Word报告，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | data.csv存在，含数值列和2026年数据；用例后删除图表和报告 | read_text_file→analyze_data→filter_data→generate_chart→write_docx | 图表和Word报告都存在 | 分析失败 |
| E2E-P4-03 | 数据库分析流程 | `"我要分析应用数据库：①获取chat_history.db的表结构 ②查询sessions表最近5条记录 ③统计messages表的消息数量 ④把数据库分析结果保存报告 ⑤列出报告文件确认，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | chat_history.db存在且有数据；用例后删除分析报告 | get_db_schema→query_sql(sessions)→query_sql(messages)→write_text_file→list_directory | 报告包含表结构和查询结果 | 查询失败 |
| E2E-P4-04 | 时间规划流程 | `"我要规划下周的工作：①获取当前时间 ②计算从今天到下周一是几天 ③查询下周一是不是工作日 ④计算从今天到月底还有多少天 ⑤把时间规划保存报告，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 无；用例后删除规划报告 | time_now→time_diff→query_calendar(工作日)→time_diff(月底)→write_text_file | 报告包含时间计算结果 | 时间计算错误 |
| E2E-P4-05 | 综合数据处理流程 | `"我要综合处理多种数据：①读取E:\test_dir\data.csv数据 ②分析统计特征 ③生成折线图 ④把图表和统计数据写入Excel报告 ⑤创建Word文档总结分析结论，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | data.csv存在；用例后删除图表/Excel/Word | read_text_file→analyze_data→generate_chart→write_xlsx→write_docx | 图表/Excel/Word都存在 | 任一步骤失败 |

**覆盖率**：5个场景覆盖15个DOCUMENT+DATAANALYSIS工具



### 4.５ P5：复合多步任务（多tool、多类型、多步骤，18个任务）

这些任务验证Agent多步推理和跨分类调用能力。每个任务独立执行，不依赖其他任务。

#### 4.５.1 同分类复合任务（6个）

| 编号 | 任务 | 用户输入 | 前置数据 | 预期调用链 | 通过标准 | 失败标准 |
|------|------|---------|---------|-----------|---------|---------|
| E2E-P5-01 | 文件全流程 | `"帮我在E:\test_dir下创建一个e2e_mc01.txt，里面写上'测试数据——2026年6月'，然后读出来看看写对了没有，再搜一下里面有没有'测试'这个词，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | E:\test_dir\可写；用例后清理e2e_mc01*.txt | write→read→grep | 三步都执行；文件内容包含"测试" | 任一步骤失败 |
| E2E-P5-02 | 文件管理链 | `"刚才创建的e2e_mc01.txt我想改个名叫e2e_mc02.txt，改完再复制一份到backup目录备份一下，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | e2e_mc01.txt存在（需先执行MC-01创建）；backup/为空；用例后清理 | rename→copy | 新文件在backup目录存在 | 操作未完成 |
| E2E-P5-03 | Shell管理链 | `"帮我查一下notepad.exe装在哪了，查到了帮我把它打开，我懒得自己去找，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | notepad在系统中存在 | find→execute | 两步都返回结果 | 任一步骤失败 |
| E2E-P5-04 | 网络查询链 | `"帮我搜一下今天科技圈有什么大新闻，搜到了把第一条结果的详细内容打开给我看看，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 网络连通 | search_web→fetch_webpage | 返回网页内容 | 获取失败 |
| E2E-P5-05 | 系统诊断链 | `"电脑开机越来越慢了，帮我查一下CPU和内存的占用情况，再看看哪些进程在吃资源，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行 | get_system_info→list_processes | 两步都返回系统数据 | 任一步骤失败 |
| E2E-P5-06 | 桌面操作链 | `"帮我截个当前屏幕的图，然后看看屏幕上有哪些文字，把识别出来的内容告诉我，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行，屏幕有文字 | screen_capture→ocr | 返回识别出的文字 | OCR失败 |

#### 4.５.2 跨分类复合任务（6个）

| 编号 | 任务 | 用户输入 | 前置数据 | 预期调用链（跨分类） | 通过标准 | 失败标准 |
|------|------|---------|---------|-------------------|---------|---------|
| E2E-P5-07 | 时间写文件 | `"帮我看看现在几点了，然后以当前时间做文件名（比如20260615_1430.txt）创建个文件放到E:\test_dir，里面写上当前的时间信息，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | E:\test_dir\可写；用例后清理创建的文件 | get_time→write_text_file | 文件名包含时间；内容有时间信息 | 文件未创建 |
| E2E-P5-08 | 网页存文件 | `"帮我查一下今天北京的天气怎么样，搜到的结果保存到E:\test_dir\weather.txt里，回头我好出门前看一眼，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 网络连通+E:\test_dir\可写；用例后删除weather.txt | search_web→write_text_file | weather.txt存在且包含天气信息 | 文件未创建 |
| E2E-P5-09 | 系统报告存文件 | `"帮我把这台电脑的系统信息（CPU、内存、操作系统版本）查出来，整理成一份报告保存到E:\test_dir\sys_report.txt，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行+E:\test_dir\可写；用例后删除sys_report.txt | get_system_info→write_text_file | sys_report.txt存在且包含系统信息 | 文件未创建 |
| E2E-P5-10 | 搜索发通知 | `"帮我搜一下2026年AI领域的最新进展，挑重点用Windows通知弹给我看看，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 网络连通+系统正常运行(低风险副作用) | search_web→send_notification | 通知发送成功 | 通知失败 |
| E2E-P5-11 | SQL结果存文件 | `"帮我查一下sessions表里前3条会话记录是什么样的，查到的结果保存到E:\test_dir\query_result.txt，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | chat_sessions表有数据+E:\test_dir\可写；用例后删除query_result.txt | query_sql→write_text_file | 文件包含查询结果 | 文件未创建 |
| E2E-P5-12 | 综合报告 | `"帮我收集三样信息：①当前时间 ②系统CPU和内存使用情况 ③E:\test_dir下的文件列表，汇总成一份综合报告保存到E:\test_dir\report.txt，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行+E:\test_dir\有文件；用例后删除report.txt | get_time→get_system_info→list_directory→write_text_file | report.txt包含三类信息 | 任一步骤失败 |

#### 4.５.3 新增跨分类复合任务（6个，每个跨越4~5种tool类型）

| 编号 | 任务 | 用户输入 | 前置数据 | 预期调用链（跨分类） | 通过标准 | 失败标准 |
|------|------|---------|---------|-------------------|---------|---------|
| E2E-P5-13 | 网络搜索+系统诊断+文件存档 | `"帮我从网上搜一下Windows系统时间同步的方法，然后用systeminfo命令查一下我这台电脑的详细系统信息，把搜索结果和系统信息汇总保存到E:\test_dir\sys_research.txt，最后用dir确认文件已经在了，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 网络连通+系统正常运行；用例后清理sys_research.txt | search_web→execute_shell_command(systeminfo)→write_text_file→execute_shell_command(dir) | 文件存在且包含搜索内容和系统信息 | 任一步骤失败 |
| E2E-P5-14 | 计时+系统监控+截屏+报告归档 | `"帮我看看当前时间，查一下CPU和内存的使用情况，再截个当前屏幕的图，然后把时间和系统状态写成一个报告保存到E:\test_dir\monitor_report.txt，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行+E:\test_dir\可写；用例后清理monitor_report.txt和截图 | get_time→get_system_info→screen_capture→write_text_file | 报告文件存在且包含时间和系统信息 | 任一步骤失败 |
| E2E-P5-15 | 命令查询+系统分析+文档生成+文件验证 | `"用systeminfo命令查一下这台电脑的初始安装日期，然后把硬件信息整理成一份Word文档放到E:\test_dir\hardware_report.docx，再用dir命令确认文档已生成，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行+E:\test_dir\可写；用例后删除hardware_report.docx | execute_shell_command(systeminfo)→write_document→execute_shell_command(dir) | Word文档存在且可读；dir输出包含文件名 | 任一步骤失败 |
| E2E-P5-16 | 网络搜索+文件保存+时间记录+通知提醒 | `"帮我搜一下今天AI行业的热点新闻，把新闻摘要保存到E:\test_dir\ai_news.txt，然后告诉我现在几点几分，最后弹个Windows通知告诉我全部搞定了，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 网络连通+E:\test_dir\可写；用例后删除ai_news.txt | search_web→write_text_file→get_time→send_notification | 文件存在且包含新闻内容；通知发送成功 | 任一步骤失败 |
| E2E-P5-17 | 文档读取+文字提取+日期计算+通知完成 | `"帮我把E:\test_dir\test.docx的内容读出来，把里面的文字提取出来保存到E:\test_dir\doc_content.txt，再帮我算一下从2026年1月1日到今天过了多少天，最后发个通知告诉我提取完成，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | test.docx存在且可读+E:\test_dir\可写；用例后删除doc_content.txt | read_document→write_text_file→time_diff→send_notification | 文件存在且包含docx文字内容；天数计算正确；通知发送成功 | 任一步骤失败 |
| E2E-P5-18 | 计时+网络搜索+系统监控+文件汇总+通知完成 | `"帮我做一件事：①看看现在几点了 ②搜一下今天的科技新闻 ③查一下CPU使用率 ④把①②③的信息汇总成一个综合报告保存到E:\test_dir\daily_report.txt ⑤搞定后弹个Windows通知告诉我，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 网络连通+系统正常运行+E:\test_dir\可写；用例后删除daily_report.txt | get_time→search_web→get_system_info→write_text_file→send_notification | 报告文件包含时间+新闻+系统信息；通知发送成功 | 任一步骤失败 |

---

### 4.６ P6：异常与容错场景（E2E测试，通过send_chat触发，8个场景）

**测试目标**：验证Agent在遇到错误输入、不存在的资源、不支持的操作时，能否正常处理而不崩溃、不死循环。

**测试方法**：每个场景通过`send_chat`发送一条用户指令，验证Agent返回合理响应。

| 编号 | 测试目标 | 用户输入 | 前置数据 | 预期行为 | 验证标准 | 能发现的问题 |
|------|---------|---------|---------|---------|---------|-------------|
| E2E-P6-01 | 文件不存在时Agent的容错 | `"读取E:\test_dir\no_such_file.txt"` | no_such_file.txt不存在 | Agent调用read_file，工具返回错误，Agent回复告知用户 | final事件存在；回复包含"不存在"/"找不到"/"无法"等提示；不死循环（steps<50） | Agent对文件不存在的异常处理逻辑是否正确 |
| E2E-P6-02 | 目录不存在时Agent的容错 | `"列出E:\test_dir\no_such_dir下的文件"` | no_such_dir不存在 | Agent调用list_directory，工具返回错误，Agent回复告知用户 | final事件存在；回复包含错误提示；不死循环 | Agent对目录不存在的异常处理逻辑 |
| E2E-P6-03 | 网络请求失败时Agent的容错 | `"访问http://this-does-not-exist-12345.com"` | 无 | Agent尝试访问，网络工具返回连接失败，Agent回复告知用户 | final事件存在；回复包含"无法访问"/"连接失败"/"不存在"等提示；不死循环 | Agent对网络异常的处理，是否会陷入重试循环 |
| E2E-P6-04 | SQL查询错误时Agent的容错 | `"帮我查一下chat_history.db里有没有user_settings这张表"` | chat_history.db存在 | Agent调用工具查询，返回表不存在，Agent回复告知用户 | final事件存在；回复包含"不存在"/"没有"等提示；不死循环 | Agent对数据库查询异常的处理 |
| E2E-P6-05 | 长文本输入时Agent的处理 | `"把下面这段文字保存到E:\test_dir\long_text.txt：这是一段测试文字，用于验证Agent对长输入的处理能力。" + "重复内容x100` | E:\test_dir\可写；用例后删除long_text.txt | Agent调用write_text_file，正确写入长文本，回复确认 | final事件存在；E:\test_dir\long_text.txt存在且内容长度>1000；不死循环 | Agent对大payload的处理能力，是否OOM或超时 |
| E2E-P6-06 | 不同session的消息隔离 | 用asyncio.gather同时发2个请求：A=`"现在几点了"` B=`"今天星期几"` | 无 | 两个请求各自独立处理，各自有自己的session | 两个都收到final；A的session中无B的消息，B的session中无A的消息；各自DB记录独立 | session隔离机制是否正确，是否有消息串扰 |
| E2E-P6-07 | 不支持的操作Agent的响应 | `"帮我把E:\test_dir\test.txt加密一下"` | test.txt存在 | Agent说明无法加密（没有加密工具），不崩溃 | final事件存在；回复说明无法执行/不支持；不死循环 | Agent对不支持操作的处理，是否会尝试不存在的工具 |
| E2E-P6-08 | 空结果场景Agent的响应 | `"在E:\test_dir\empty_dir中搜索所有.txt文件"` | empty_dir/为空目录 | Agent调用搜索工具，返回空结果，正确报告"未找到" | final事件存在；回复包含"未找到"/"没有"/"空"等提示；不死循环 | Agent对空搜索结果的处理，是否会反复重试 |

---

### 4.７ P7：任务控制API风险测试（API测试，通过httpx直接调用，5个场景）

**测试目标**：发现任务状态机的真实bug——状态不一致、资源泄漏、竞态条件、LLM中断有效性。

**测试方法**：直接调用API端点（不通过send_chat），因为send_chat是阻塞的无法在任务执行中进行控制。

| 编号 | 风险点 | 测试操作 | 前置数据 | 预期行为 | 验证标准 | 能发现的bug |
|------|--------|---------|---------|---------|---------|------------|
| E2E-P7-01 | 状态机非法转换 | 1. 创建任务 2. cancel 3. pause | session_id + 一个简单任务 | 第3步pause应拒绝（已取消的任务不能暂停） | 状态机拒绝非法转换；返回错误；任务状态不变 | 状态机未校验前置状态，允许非法转换 |
| E2E-P7-02 | pause后LLM是否真的停 | 1. 创建长任务 2. pause 3. 等5秒 4. 查看步骤数 | session_id + 长时间任务（如搜索+读写） | pause后步骤数不再增加 | pause前步骤数 < pause后步骤数（5秒内无新增） | pause只是改了状态标记，LLM流没真正中断 |
| E2E-P7-03 | cancel后资源是否释放 | 1. 创建任务 2. cancel 3. 查询任务列表 | session_id + 一个任务 | cancel后任务从列表消失或标记已取消 | task_id在活跃任务列表中不存在 | cancel只改状态，没清理任务引用/内存 |
| E2E-P7-04 | 并发cancel竞态 | 1. 创建长任务 2. 同时发2个cancel请求 | session_id + 一个长任务 | 只有一个成功，另一个幂等返回 | 2个请求都返回成功/已取消；task状态一致；不崩溃 | 并发修改无锁保护，状态不一致 |
| E2E-P7-05 | 状态变更后消息一致性 | 1. 创建多步任务 2. 等2步后cancel 3. 查session消息 | session_id + 多步任务 | cancel后消息记录完整，有start+thought+observation+final | 消息数>=3（start+至少1步+final/cancel）；无空消息 | 状态变更时消息写入不完整，中间步骤丢失 |

---

### 4.８ P8：会话管理API风险测试（API测试，通过httpx直接调用，5个场景）

**测试目标**：发现会话管理的真实bug——数据残留、竞态条件、性能问题、注入风险。

**测试方法**：直接调用API端点。每个用例使用独立的session_id，不依赖其他用例。

| 编号 | 风险点 | 测试操作 | 前置数据 | 预期行为 | 验证标准 | 能发现的bug |
|------|--------|---------|---------|---------|---------|------------|
| E2E-P8-01 | 并发更新竞态 | 同时发2个update请求改同一个会话名 | 一个已有会话 | 只有一个生效，不崩溃 | 会话名是最终值之一；服务不返回500；无数据损坏 | 并发update无锁，后写覆盖先写，数据丢失 |
| E2E-P8-02 | 删除后数据残留 | 1. 创建会话+发几条消息 2. 删除会话 3. 查消息列表 | 会话id + 消息 | 删除后消息列表为空或404 | 消息数=0 或 返回404 | 删除会话没级联删除消息，数据残留 |
| E2E-P8-03 | 大消息量性能 | 1. 创建会话 2. 插入50条消息 3. 获取消息列表 | 会话id + 50条消息 | 列表正常返回，不超时 | 响应时间<5秒；消息数=50；支持分页 | 消息列表没分页，全量加载导致慢查询/超时 |
| E2E-P8-04 | 特殊字符处理 | 创建会话名：`<script>alert(1)</script>` | 无 | 会话名原样保存，不被解析执行 | 返回的会话名与输入一致；不触发XSS | 会话名未转义，存储型XSS风险 |
| E2E-P8-05 | 并发创建冲突 | 同时发3个创建会话请求（同名或不同名） | 无 | 全部成功或合理处理冲突 | 返回的会话列表>=1且<=3；不崩溃；无重复ID | 并发创建无隔离，可能创建重复会话或崩溃 |

---

### 4.９ P9：ReAct循环异常处理（E2E测试，通过send_chat触发，4个场景）

**测试目标**：验证Agent在异常输入（空输入、乱码、模糊意图、中英混合）下的处理能力。

| 编号 | 测试目标 | 用户输入 | 前置数据 | 预期行为 | 验证标准 | 能发现的问题 |
|------|---------|---------|---------|---------|---------|-------------|
| E2E-P9-01 | 空输入Agent的处理 | `"   "` | 后端+LLM可用 | Agent收到空消息，回复或finish | final事件存在；不崩溃；不死循环 | Agent对空输入的边界处理 |
| E2E-P9-02 | 乱码输入Agent的处理 | `"asdfghjkl12345!@#$%^&*"` | 后端+LLM可用 | Agent尝试理解，回复说明无法理解或尝试回答 | final事件存在；有回复内容；不死循环 | Agent对无法理解输入的处理 |
| E2E-P9-03 | 模糊意图Agent的处理 | `"帮我处理一下那个文件的事情"` | 后端+LLM可用 | Agent追问具体文件或尝试合理推断 | final事件存在；回复内容合理（追问或尝试）；不死循环 | Agent对模糊指令的理解和澄清能力 |
| E2E-P9-04 | 中英混合输入Agent的处理 | `"create一个新文件叫e2e_mixed.txt在E:\test_dir"` | 后端+LLM可用+E:\test_dir\可写；用例后删除e2e_mixed.txt | Agent理解中英混合指令并执行 | final事件存在；E:\test_dir\e2e_mixed.txt存在；不死循环 | Agent对中英混合输入的理解能力 |

---

### 4.10 P10：工具执行容错（E2E测试，通过send_chat触发，2个场景）

**测试目标**：验证工具在执行失败时（权限不足、命令不存在）Agent的容错能力。

| 编号 | 测试目标 | 用户输入 | 前置数据 | 预期行为 | 验证标准 | 能发现的问题 |
|------|---------|---------|---------|---------|---------|-------------|
| E2E-P10-01 | 读取权限受限文件的容错 | `"读取C:\Windows\System32\config\SAM"` | Windows系统，SAM文件存在但无权限 | Agent调用read_file，工具报权限错误，Agent回复告知用户 | final事件存在；回复包含"权限"/"拒绝"/"无法访问"；不死循环 | read_file工具对权限不足的异常处理 |
| E2E-P10-02 | 执行无效命令的容错 | `"执行命令 invalid_command_xyz_12345"` | 无 | Agent调用execute_shell_command，工具报错，Agent回复告知用户 | final事件存在；回复包含"找不到"/"不是内部或外部命令"；不死循环 | execute_shell_command工具对无效命令的异常处理 |

---

### 4.11 P3：SYSTEM+DESKTOP多任务场景（19个工具 → 5个真实工作场景）

| 编号 | 工作场景 | 用户输入 | 前置数据 | 预期调用链(多工具) | 通过标准 | 失败标准 |
|------|---------|---------|---------|-------------------|---------|---------|
| E2E-P3-01 | 系统健康检查流程 | `"电脑感觉不太对劲，帮我全面检查：①查CPU和内存使用率 ②列出占用资源最多的前10个进程 ③检查网络连接状态 ④查看系统日志最近的错误 ⑤生成健康检查报告保存到E:\test_dir\health_check.txt，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行；用例后删除health_check.txt | get_system_info→list_processes→net_connections→event_log→write_text_file | health_check.txt包含CPU/内存/进程/网络/日志信息 | 任一检查失败 |
| E2E-P3-02 | 环境配置诊断流程 | `"Python环境好像有问题，帮我诊断：①查看PATH环境变量 ②查找python命令路径 ③查看PYTHONPATH环境变量 ④读取注册表中Python安装路径 ⑤把诊断结果保存报告，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行；用例后删除诊断报告 | get_env(PATH)→find_command(python)→get_env(PYTHONPATH)→registry_read→write_text_file | 报告包含PATH/python路径/注册表信息 | 任一步骤失败 |
| E2E-P3-03 | 桌面自动化流程 | `"我要自动化操作桌面：①列出当前所有窗口 ②截取当前屏幕 ③读取剪贴板内容 ④发送一个测试通知 ⑤把操作记录保存报告，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行，有窗口打开；用例后删除截图和报告 | window_info→screen_capture→clipboard_read→send_notification→write_text_file | 报告包含窗口列表/截图路径/剪贴板内容 | 任一步骤失败 |
| E2E-P3-04 | 窗口管理流程 | `"窗口太乱了我整理一下：①列出所有窗口 ②找到记事本窗口并最大化 ③截屏看看效果 ④把记事本还原 ⑤记录操作过程，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行，有记事本窗口打开；用例后删除截图和报告 | window_info→window_maximize→screen_capture→window_restore→write_text_file | 报告包含窗口操作记录 | 窗口操作失败 |
| E2E-P3-05 | 系统监控告警流程 | `"我要监控系统并告警：①获取当前时间作为监控开始时间 ②查看CPU使用率 ③查看内存使用率 ④如果CPU>80%或内存>90%就发通知告警 ⑤把监控数据保存报告，把操作步骤和获取的信息分析汇总后保存到目录reports下的文件中"` | 系统正常运行；用例后删除监控报告 | time_now→get_system_info(cpu)→get_system_info(memory)→send_notification(条件)→write_text_file | 报告包含时间和系统资源数据 | 监控失败 |

**覆盖率**：5个场景覆盖19个SYSTEM+DESKTOP工具

---


---

### 4.12 测试场景总计

| 级别 | 场景数 | 测试方法 | 覆盖范围 | 设计特点 |
|------|--------|---------|---------|---------|
| P0 核心链路 | 4 | E2E(send_chat) | API→Agent→LLM→DB→SSE完整链路 | 基础通路验证 |
| P1 FILE工具 | 5 | E2E(send_chat) | 文件操作15个工具全覆盖 | **每个场景5步，多工具串联** |
| P2 SHELL+NETWORK | 4 | E2E(send_chat) | 命令执行+网络请求9个工具全覆盖 | **每个场景5步，多工具串联** |
| P5 复合多步 | 18 | E2E(send_chat) | 多工具串联+跨分类调用 | **跨分类复杂场景，4-5种工具** |
| P6 异常容错 | 8 | E2E(send_chat) | 错误输入+不存在资源+不支持操作+边界条件 | 异常处理验证 |
| P7 任务控制API | 5 | API直接调用 | 状态机bug/资源泄漏/竞态条件 | 风险测试 |
| P8 会话管理API | 5 | API直接调用 | 数据残留/竞态条件/性能/注入 | 风险测试 |
| P9 ReAct异常 | 4 | E2E(send_chat) | 空输入/乱码/模糊意图/中英混合 | 边界条件 |
| P10 工具容错 | 2 | E2E(send_chat) | 权限不足/无效命令 | 异常处理 |
| P3 SYSTEM+DESKTOP | 5 | E2E(send_chat) | 系统+桌面19个工具全覆盖 | **每个场景5步，多工具串联** |
| P4 DOCUMENT+DATA | 5 | E2E(send_chat) | 文档+数据分析15个工具全覆盖 | **每个场景5步，多工具串联** |
| **合计** | **65** | | | **真实工作场景，多任务多轮** |

**设计改进说明**:
- P1/P2/P3/P4从单一工具调用改为**真实工作场景**
- 每个场景至少调用**3-5个工具**
- 每个场景都是**多轮交互完成复杂任务**
- 场景数从88个精简到65个，但测试深度大幅提升
- 覆盖率不变：所有77个工具仍被完整覆盖

---

## 五、测试脚本编写规则

### 5.1 铁律：1个用例 = 1个函数

```
每个 E2E-XX-XX 编号对应一个 async def test_e2e_xx_xx_xxx()
每个函数只发一条用户指令
每个函数独立验证、独立清理
函数名 = test_ + 编号小写（如 test_e2e_p0_01_introduce_self）

为什么不能合并：
  · 合并后无法定位哪个场景失败
  · 合并后无法单独重跑失败的用例
  · 合并后一个场景的残留数据会影响下一个场景
```

### 5.2 验证分层

```
MUST层   → assert 硬断言   → 无死循环、DB有记录、文件存在
SHOULD层 → if...assert    → DB steps数偏差在2以内
MAY层    → if...print     → 工具名差异仅记录日志
```

### 5.3 测试记录写入（铁律）

```
每个测试用例跑完后，无论通过还是不通过，都必须自动生成测试记录文件。

触发位置：测试脚本的 finally 块中调用 write_test_record()
即使失败也要写：assert失败/异常退出时，记录文件必须写入（含失败原因）
文件位置：G:\OmniAgentAs-desk\notes\测试记录-{用例编号}-{日期}.md

脚本结构：
  passed = False; r = None; sid = None; ...
  try:
      # ... 测试逻辑 ...
      passed = True
  except Exception as e:
      passed = False; raise
  finally:
      write_test_record("用例编号","任务描述",u,r or {},db,ci,si,lc,passed,...)

注意事项：
  · 所有变量必须在try前初始化默认值（防止assert失败后未定义）
  · write_test_record必须在finally中调用（确保无论通过/失败都写入）
```

---

## 六、执行计划

### 6.1 前提条件与操作步骤

```bash
# 1. 关闭安全检查（纯功能验证）
#    编辑 config.yaml，将 security.enabled 改为 false
#    文件路径: config/config.yaml
#    修改前: security:
#              enabled: true
#    修改后: security:
#              enabled: false

# 2. 杀死旧的后端进程（每次测试前必须）
#    找到占用8000端口的进程并杀掉
#    Windows: netstat -ano | findstr :8000 → taskkill /F /PID <pid>

# 3. 重新启动后端
cd backend
python -m uvicorn app.main:app --reload
# 等待3-5秒启动完成
# 验证: curl http://localhost:8000/docs 返回200

# 4. 运行测试（另一个终端）
cd backend
pytest -m e2e_full_link -v --tb=short

# 5. 测试完成后恢复安全检查
#    编辑 config.yaml，将 security.enabled 改回 true
#    修改后: security:
#              enabled: true
```

### 6.2 测试数量与时间预估

> **注意**：测试的目的=发现问题。时间长不是问题，跑100天也应该。每个测试都值得认真观察系统行为。

### 6.3 标记与分批执行

所有测试使用 `@pytest.mark.e2e_full_link` 标记。

```bash
# 跑全部全链路测试（约4-5小时）
pytest -m e2e_full_link -v --tb=short

# 只跑P0（核心链路验证，~5分钟）
pytest -m e2e_full_link -k "p0" -v --tb=short

# 只跑P1-P2（快速单工具，FILE+SHELL+NETWORK，~20分钟）
pytest -m e2e_full_link -k "p1 or p2" -v --tb=short

# 只跑P5-P6（复合+异常，~60分钟）
pytest -m e2e_full_link -k "p5 or p6" -v --tb=short

# 只跑P7-P10（生命周期+API+容错，~30分钟）
pytest -m e2e_full_link -k "p7 or p8 or p9 or p10" -v --tb=short

# 只跑P3-P4（低优先级，SYSTEM+DESKTOP+DOCUMENT+META，~70分钟）
pytest -m e2e_full_link -k "p3 or p4" -v --tb=short

# 跑指定单个测试
pytest -m e2e_full_link -k "e2e_p0_01" -v --tb=short
```

---

## 七、测试报告格式

每个测试运行后输出（控制台`print_report`生成）：

```
=== E2E-P0-01: 核心链路验证 ===
  Status: PASSED
  Elapsed: 15.2s (15200ms)
  SSE events: 3 (start->thought->final)
  LLM calls: 1
  DB: [OK] session, [OK] is_valid, [OK] order, [OK] msgs, [WARN] steps, [OK] fields
  Consistency: [OK] (0 issues)
  Step reason: [OK] (0 issues)
  Logs: [OK] no-ERROR, [OK] no-TB, [OK] sess-records, [WARN] SSE-records
  Tools: []
  Conclusion: PASSED
```

**v1.8变更：`流结束方式`不再作为PASS/FAIL判定依据，改为记录具体结束方式。**
测试记录文件中的验证结果表新增"流结束"行，显示 final / error / 中断。`has_error`从MUST降为SHOULD WARN，不再阻断。回复中含错误关键词由`write_test_record`自动标FAIL。

所有测试跑完后输出汇总：

```
=== 全链路E2E测试汇总 ===
    总测试数: 98
    通过: X
    失败: X
    跳过: X
    
    ❌ 失败详情:
      [实际失败列表]
    
    ⚠️ 跳过详情:
      [实际跳过列表]
```

---

## 八、与其他测试的关系

```
所有测试体系
│
├── L1: Mock单元测试（~200个, ~10秒）
│   ├── 单工具函数测试（file/shell/network/system/desktop/document/meta）
│   ├── MessageBuilder测试（43个）
│   └── 运行时集成测试（14个）
│
├── L2: 全链路E2E集成测试（88个, ~4-5小时）← 本方案主测
│   ├── P0: 核心链路验证（4个）
│   ├── P1: FILE工具全覆盖（8个）
│   ├── P2: SHELL+NETWORK全覆盖（7个）
│   ├── P5: 复合多步任务（18个）
│   ├── P6: 异常容错场景（8个）
│   ├── P7: 任务控制API（5个）
│   ├── P8: 会话管理API（5个）
│   ├── P9: ReAct异常处理（4个）
│   ├── P10: 工具执行容错（2个）
│   ├── P3: SYSTEM+DESKTOP全覆盖（13个）
│   └── P4: DOCUMENT+META全覆盖（14个）
│
└── L3: 辅助集成测试（22个, ~30分钟）← 本方案辅助测
    ├── ReAct循环异常（Mock LLM，8个）
    ├── 工具重试容错（Mock工具，6个）
    └── SSE流管道异常（构造非法请求，8个）
```

**L2覆盖三层验证**：
- **工具层**（P1-P4）：57个工具在真实LLM下能否被正确调用
- **流程层**（P5-P7）：多步推理、任务控制、系统串联是否完整
- **容错层**（P6/P9-P10）：异常输入、工具失败、服务中断时能否优雅处理

---

## 九、辅助集成测试（需Mock，非真实LLM）

**本章与L2的区别**：L2用真实后端+真实LLM，本章用Mock模拟内部异常，验证系统内部机制。

**适用场景**：需要构造正常用户输入无法触发的内部异常状态。

### 9.1 ReAct循环异常（Mock LLM返回）

| 编号 | 场景 | Mock方式 | 预期行为 |
|------|------|---------|---------|
| INT-E2E-P9-01 | LLM返回空内容 | Mock LLM返回 `""` | Agent报错"LLM返回为空"，正常finish |
| INT-E2E-P9-02 | LLM返回不可解析内容 | Mock LLM返回 `"这不是JSON"` | Agent走parse_error分支，正常finish |
| INT-E2E-P9-03 | LLM返回未知type | Mock LLM返回 `{"type":"foo_bar"}` | Agent走unknown handler，正常finish |
| INT-E2E-P9-04 | LLM返回空工具名 | Mock LLM返回 `{"tool_name":"","tool_params":{}}` | Agent报错，不死循环 |
| INT-E2E-P9-05 | LLM返回不存在的工具 | Mock LLM返回 `{"tool_name":"fake_tool","tool_params":{}}` | Agent报错"工具不存在"，不死循环 |
| INT-E2E-P9-06 | LLM返回缺少必要参数 | Mock LLM返回 `{"tool_name":"read_file","tool_params":{}}` | Agent报错"参数缺失"，不死循环 |
| INT-E2E-P9-07 | 循环中取消任务 | 发送请求 → 取消 → Mock LLM继续返回action | Agent检测到取消标志，停止循环 |
| INT-E2E-P9-08 | 超过max_steps | Mock LLM每次都返回action（永不answer） | Agent在max_steps后强制停止，返回"步骤上限" |

### 9.2 工具执行重试与容错（Mock工具抛异常）

| 编号 | 场景 | Mock方式 | 预期行为 |
|------|------|---------|---------|
| INT-E2E-P7-01 | 工具抛可重试异常 | Mock read_file第一次抛ConnectionError，第二次成功 | 重试后成功，Agent继续 |
| INT-E2E-P7-02 | 工具抛不可重试异常 | Mock read_file抛ValueError | 立即失败，不重试 |
| INT-E2E-P7-03 | 重试耗尽 | Mock read_file每次都抛异常 | 达到最大重试次数后失败，Agent报错 |
| INT-E2E-P7-04 | 工具执行超时 | Mock read_file永不返回（sleep 300s） | 超时中断，Agent报错 |
| INT-E2E-P7-05 | 工具返回None | Mock read_file返回None | Agent处理空结果，不崩溃 |
| INT-E2E-P7-06 | 同步工具通过to_thread执行 | Mock一个同步函数作为工具 | 正常执行，不阻塞事件循环 |

### 9.3 SSE流管道异常（构造非法请求）

| 编号 | 场景 | 构造方式 | 预期行为 |
|------|------|---------|---------|
| INT-SSE-01 | 空messages列表 | `POST /chat` 带 `{"messages":[]}` | 返回错误SSE事件，不崩溃 |
| INT-SSE-02 | 缺少role字段 | `POST /chat` 带 `{"messages":[{"content":"hi"}]}` | 返回参数错误 |
| INT-SSE-03 | 缺少content字段 | `POST /chat` 带 `{"messages":[{"role":"user"}]}` | 返回参数错误 |
| INT-SSE-04 | messages不是数组 | `POST /chat` 带 `{"messages":"hello"}` | 返回类型错误 |
| INT-SSE-05 | 无效session_id格式 | `POST /chat` 带 `{"session_id":"!!!invalid"}` | 返回格式错误或自动清理 |
| INT-SSE-06 | 不存在的session_id | `POST /chat` 带 `{"session_id":"00000000-0000-0000-0000-000000000000"}` | 返回session不存在或自动创建 |
| INT-SSE-07 | 超大messages | `POST /chat` 带100条messages | 正常处理或返回限制提示 |
| INT-SSE-08 | 并发取消同一任务 | 同时发2次cancel同一task_id | 一个成功，一个提示已取消 |

---

**更新时间**: 2026-06-15 12:43:30  
**版本**: v1.8  
**更新人**: 小健  
**原编写人**: 小健
