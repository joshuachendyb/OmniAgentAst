# OmniAgentAst. 阶段3.1 设计文档

**版本**: v1.0
**创建时间**: 2026-02-22 01:29:47
**负责人**: AI助手小沈
**目标**: 将Web版打包为桌面应用

---

## 一、阶段概述

### 1.1 阶段目标

将已完成的Web版应用打包为Windows桌面应用，实现：
- 系统托盘常驻
- 全局快捷键唤醒
- 一键安装/便携运行

### 1.2 前置条件

| 前置阶段 | 状态 | 说明 |
|---------|------|------|
| 阶段2.1 | ✅ 已完成 | Web对话界面+配置+安全 |
| 阶段2.2 | ✅ 已完成 | 工具集+历史管理 |

### 1.3 交付物

| 交付物 | 说明 |
|--------|------|
| 便携版 | 解压即用，需本地Python环境 |
| 安装版 | PyInstaller打包，独立运行 |
| EXE安装程序 | 一键安装（可选后续） |

---

## 二、技术选型

### 2.1 桌面框架

| 项目 | 选择 | 理由 |
|------|------|------|
| 桌面框架 | **Tauri 2.0** | 体积小、性能好、现代技术 |
| 前端框架 | React (已有) | 复用Web版代码 |
| 后端框架 | FastAPI (已有) | 复用Web版代码 |

### 2.2 打包方案

| 阶段 | 方案 | 用户群体 |
|------|------|---------|
| **便携版** | 用户本地Python | 开发者/测试 |
| **安装版** | PyInstaller打包 | 普通用户 |

### 2.3 安装程序

| 类型 | 选择 | 理由 |
|------|------|------|
| 先做 | 便携版 | 方便测试迭代 |
| 后做 | EXE安装包 | 制作简单，用户友好 |

---

## 三、功能设计

### 3.1 Tauri集成

#### 3.1.1 项目结构

```
OmniAgentAs-desk/
├── src-tauri/              # Tauri后端（Rust）
│   ├── src/
│   │   ├── main.rs         # 主入口
│   │   └── lib.rs          # 核心逻辑
│   ├── Cargo.toml          # Rust依赖
│   ├── tauri.conf.json     # Tauri配置
│   └── icons/              # 应用图标
│
├── frontend/               # React前端（已有）
│   ├── src/
│   ├── package.json
│   └── vite.config.ts
│
├── backend/                # Python后端（已有）
│   ├── app/
│   ├── requirements.txt
│   └── main.py
│
└── dist/                   # 打包输出
    ├── portable/           # 便携版
    └── installer/          # 安装版
```

#### 3.1.2 Tauri配置

```json
// tauri.conf.json
{
  "build": {
    "beforeBuildCommand": "npm run build",
    "beforeDevCommand": "npm run dev",
    "devPath": "http://localhost:5173",
    "distDir": "../frontend/dist"
  },
  "package": {
    "productName": "OmniAgentAst",
    "version": "0.4.0"
  },
  "tauri": {
    "allowlist": {
      "all": false,
      "shell": {
        "all": true,
        "execute": true,
        "sidecar": true
      },
      "window": {
        "all": true
      },
      "globalShortcut": {
        "all": true
      }
    },
    "bundle": {
      "active": true,
      "targets": ["msi", "nsis"],
      "identifier": "com.omniagent.desktop",
      "icon": [
        "icons/32x32.png",
        "icons/128x128.png",
        "icons/icon.ico"
      ]
    },
    "systemTray": {
      "iconPath": "icons/icon.ico",
      "iconAsTemplate": true
    },
    "windows": [
      {
        "title": "OmniAgentAst",
        "width": 1200,
        "height": 800,
        "minWidth": 800,
        "minHeight": 600,
        "resizable": true,
        "fullscreen": false,
        "visible": true,
        "center": true
      }
    ]
  }
}
```

---

### 3.2 系统托盘

#### 3.2.1 托盘功能

| 功能 | 说明 |
|------|------|
| 托盘图标 | 显示应用图标 |
| 右键菜单 | 显示/隐藏窗口、退出 |
| 最小化到托盘 | 关闭窗口时最小化而非退出 |
| 托盘提示 | 显示应用状态 |

#### 3.2.2 托盘菜单设计

```
┌─────────────────────┐
│ ▶ 显示主窗口        │
│ ─────────────────── │
│ ⚙️ 设置             │
│ 📋 打开日志目录      │
│ ─────────────────── │
│ ❌ 退出             │
└─────────────────────┘
```

#### 3.2.3 托盘实现代码

```rust
// src-tauri/src/lib.rs
use tauri::{
    Manager, 
    SystemTray, 
    SystemTrayEvent, 
    SystemTrayMenu, 
    SystemTrayMenuItem, 
    CustomMenuItem
};

pub fn create_system_tray() -> SystemTray {
    let show = CustomMenuItem::new("show".to_string(), "显示主窗口");
    let settings = CustomMenuItem::new("settings".to_string(), "设置");
    let logs = CustomMenuItem::new("logs".to_string(), "打开日志目录");
    let quit = CustomMenuItem::new("quit".to_string(), "退出");

    let tray_menu = SystemTrayMenu::new()
        .add_item(show)
        .add_native_item(SystemTrayMenuItem::Separator)
        .add_item(settings)
        .add_item(logs)
        .add_native_item(SystemTrayMenuItem::Separator)
        .add_item(quit);

    SystemTray::new().with_menu(tray_menu)
}

pub fn handle_tray_event(event: SystemTrayEvent) {
    match event {
        SystemTrayEvent::MenuItemClick { id, .. } => {
            match id.as_str() {
                "show" => { /* 显示窗口 */ },
                "settings" => { /* 打开设置 */ },
                "logs" => { /* 打开日志目录 */ },
                "quit" => { /* 退出应用 */ },
                _ => {}
            }
        }
        _ => {}
    }
}
```

---

### 3.3 全局快捷键

#### 3.3.1 快捷键设计

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `Ctrl+Shift+O` | 唤醒/隐藏窗口 | 全局可用 |
| `Ctrl+Shift+Q` | 退出应用 | 全局可用 |
| `Esc` | 最小化到托盘 | 窗口内 |

#### 3.3.2 快捷键实现

```rust
// src-tauri/src/lib.rs
use tauri::GlobalShortcutManager;

pub fn register_shortcuts(app: &tauri::AppHandle) {
    let manager = app.global_shortcut_manager();
    
    // 唤醒窗口快捷键
    manager.register("Ctrl+Shift+O", move || {
        // 切换窗口显示/隐藏
        if let Some(window) = app.get_window("main") {
            if window.is_visible().unwrap() {
                window.hide().unwrap();
            } else {
                window.show().unwrap();
                window.set_focus().unwrap();
            }
        }
    }).unwrap();
    
    // 退出快捷键
    manager.register("Ctrl+Shift+Q", move || {
        app.exit(0);
    }).unwrap();
}
```

---

### 3.4 Python后端打包

#### 3.4.1 打包方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| **用户本地Python** | 体积小(~10MB)、调试方便 | 需用户安装Python |
| **PyInstaller** | 独立运行、用户友好 | 体积大(~100MB) |

#### 3.4.2 PyInstaller打包配置

```python
# backend/build.spec
# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['app/main.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('config', 'config'),  # 包含配置文件
    ],
    hiddenimports=[
        'uvicorn.logging',
        'uvicorn.loops',
        'uvicorn.loops.auto',
        'uvicorn.protocols',
        'uvicorn.protocols.http',
        'uvicorn.protocols.http.auto',
        'uvicorn.protocols.websockets',
        'uvicorn.protocols.websockets.auto',
        'uvicorn.lifespan',
        'uvicorn.lifespan.on',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='OmniAgentAst-Backend',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,  # 显示控制台窗口（调试用）
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
```

#### 3.4.3 打包脚本

```bash
#!/bin/bash
# build_backend.sh

cd backend

# 安装依赖
pip install pyinstaller

# 打包
pyinstaller build.spec

# 输出位置
# dist/OmniAgentAst-Backend.exe
```

---

### 3.5 Tauri启动Python后端

#### 3.5.1 Sidecar配置

```json
// tauri.conf.json
{
  "tauri": {
    "bundle": {
      "externalBin": [
        "binaries/backend"
      ]
    }
  }
}
```

#### 3.5.2 启动后端代码

```rust
// src-tauri/src/lib.rs
use tauri::api::process::{Command, CommandEvent};

pub async fn start_backend() {
    let (mut rx, _child) = Command::new_sidecar("backend")
        .expect("failed to create backend command")
        .spawn()
        .expect("Failed to spawn backend process");

    // 监听后端输出
    tauri::async_runtime::spawn(async move {
        while let Some(event) = rx.recv().await {
            if let CommandEvent::Stdout(line) = event {
                println!("[Backend] {}", line);
            }
        }
    });
}
```

---

### 3.6 单实例运行

#### 3.6.1 防止多开

```rust
// src-tauri/src/lib.rs
use tauri::Manager;

pub fn ensure_single_instance(app: &tauri::AppHandle) -> bool {
    let identifier = "com.omniagent.desktop";
    
    match app.get_window("main") {
        Some(window) => {
            // 已有实例，激活窗口
            window.show().unwrap();
            window.set_focus().unwrap();
            false
        }
        None => true,
    }
}
```

---

## 四、便携版设计

### 4.1 便携版结构

```
OmniAgentAst-Portable/
├── OmniAgentAst.exe        # Tauri桌面应用
├── backend/                 # Python后端
│   ├── app/
│   ├── config/
│   ├── requirements.txt
│   └── run.bat             # 启动脚本
├── data/                    # 用户数据
│   ├── config/
│   ├── logs/
│   └── knowledge/
├── run.bat                  # 一键启动
└── README.txt               # 使用说明
```

### 4.2 启动脚本

```batch
@echo off
chcp 65001 >nul
echo ========================================
echo   OmniAgentAst 便携版启动器
echo ========================================
echo.

REM 检查Python环境
python --version >nul 2>&1
if errorlevel 1 (
    echo [错误] 未检测到Python环境
    echo 请安装 Python 3.11 或更高版本
    echo 下载地址: https://www.python.org/downloads/
    pause
    exit /b 1
)

echo [信息] 检测到Python环境
echo.

REM 检查依赖
echo [信息] 检查依赖...
cd backend
pip install -r requirements.txt -q

REM 启动后端
echo [信息] 启动后端服务...
start /b python -m uvicorn app.main:app --host 127.0.0.1 --port 8000

REM 等待后端启动
timeout /t 3 /nobreak >nul

REM 启动前端
echo [信息] 启动桌面应用...
cd ..
start OmniAgentAst.exe

echo.
echo [成功] OmniAgentAst 已启动
echo [提示] 关闭此窗口不会退出应用
echo.
pause
```

### 4.3 便携版优点

| 优点 | 说明 |
|------|------|
| 体积小 | ~50MB（不含Python运行时） |
| 方便测试 | 修改代码即可测试 |
| 调试方便 | 可看到后端日志输出 |

### 4.4 便携版使用要求

- Python 3.11+ 已安装
- pip 可用
- 网络正常（首次需下载依赖）

---

## 五、安装版设计

### 5.1 安装版结构

```
OmniAgentAst-Installer/
├── OmniAgentAst.exe        # Tauri桌面应用
├── backend.exe              # PyInstaller打包的后端
├── resources/               # 资源文件
│   ├── config/
│   └── icons/
├── data/                    # 用户数据
└── uninstall.exe           # 卸载程序
```

### 5.2 PyInstaller打包

```bash
# 打包后端
cd backend
pyinstaller --onefile --name backend --distpath ../dist app/main.py

# 打包前端（Tauri自动处理）
cd ..
npm run tauri build
```

### 5.3 安装版优点

| 优点 | 说明 |
|------|------|
| 独立运行 | 无需安装Python |
| 用户友好 | 双击即可使用 |
| 一键安装 | 包含所有依赖 |

### 5.4 安装版体积估算

| 组件 | 体积 |
|------|------|
| Tauri前端 | ~15MB |
| Python运行时 | ~40MB |
| 后端依赖 | ~30MB |
| 资源文件 | ~5MB |
| **总计** | **~90MB** |

---

## 六、开发计划

### 6.1 开发步骤

```
第1步：Tauri项目初始化（2小时）
   ├── 创建src-tauri目录
   ├── 配置tauri.conf.json
   └── 测试前后端通信

第2步：系统托盘实现（3小时）
   ├── 托盘图标和菜单
   ├── 最小化到托盘
   └── 托盘事件处理

第3步：全局快捷键（2小时）
   ├── 注册快捷键
   ├── 唤醒窗口
   └── 退出应用

第4步：Python后端集成（3小时）
   ├── PyInstaller配置
   ├── Sidecar集成
   └── 自动启动后端

第5步：便携版制作（2小时）
   ├── 目录结构
   ├── 启动脚本
   └── 使用文档

第6步：测试验证（3小时）
   ├── 功能测试
   ├── 兼容性测试
   └── 性能测试

第7步：安装版打包（可选，3小时）
   ├── Tauri打包
   ├── NSIS安装程序
   └── 发布测试
```

### 6.2 时间估算

| 阶段 | 时间 | 累计 |
|------|------|------|
| Tauri初始化 | 2h | 2h |
| 系统托盘 | 3h | 5h |
| 全局快捷键 | 2h | 7h |
| 后端集成 | 3h | 10h |
| 便携版 | 2h | 12h |
| 测试验证 | 3h | 15h |
| 安装版（可选） | 3h | 18h |

**总计**：15-18小时

---

## 七、验收标准

### 7.1 功能验收

| 功能 | 验收标准 |
|------|---------|
| 桌面窗口 | 双击启动，窗口正常显示 |
| 系统托盘 | 托盘图标显示，右键菜单可用 |
| 最小化到托盘 | 关闭窗口后托盘图标仍在 |
| 全局快捷键 | Ctrl+Shift+O可唤醒窗口 |
| Python后端 | 后端自动启动，API可用 |
| 便携版 | 解压后运行run.bat可启动 |
| 安装版 | 双击exe可独立运行 |

### 7.2 性能验收

| 指标 | 标准 |
|------|------|
| 启动时间 | < 5秒 |
| 内存占用 | < 200MB |
| 便携版体积 | < 100MB |
| 安装版体积 | < 150MB |

---

## 八、风险与对策

### 8.1 潜在风险

| 风险 | 影响 | 对策 |
|------|------|------|
| PyInstaller打包失败 | 安装版无法使用 | 先发布便携版 |
| Tauri兼容性问题 | 部分系统无法运行 | 多系统测试 |
| 杀毒软件误报 | 用户无法安装 | 申请白名单 |
| 后端启动失败 | 功能不可用 | 添加重试机制 |

### 8.2 回退方案

如果安装版打包失败：
1. 继续使用便携版
2. 提供详细的Python环境配置文档
3. 后续优化安装版

---

## 九、后续规划

### 9.1 本阶段完成后

- [ ] 便携版发布
- [ ] 用户反馈收集
- [ ] 安装版优化

### 9.2 下一阶段（3.2）预告

- Planner规划器
- Refiner优化器
- 浏览器自动化
- 知识库

---

**文档创建时间**: 2026-02-22 01:29:47
**文档版本**: v1.0
**下次更新**: 根据开发进展更新
