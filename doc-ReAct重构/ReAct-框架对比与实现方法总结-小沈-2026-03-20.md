# ReAct 框架对比与实现方法总结报告

**文档版本**: v1.0
**创建时间**: 2026-03-20 05:55:00
**编写人**: 小沈
**研究框架数量**: 5 个（LangChain, LlamaIndex, CrewAI, AutoGen, Haystack）
**研究资料总数**: 140+ 篇
**存放位置**: D:\OmniAgentAs-desk\doc-ReAct重构\

---

## 1 执行摘要

### 1.1 研究目的

深入研究主流 AI Agent 框架的 ReAct 实现，提炼各框架的字段定义、核心流程、设计模式，为 omniAgent 的 ReAct 重构提供参考。

### 1.2 研究框架

| 框架 | Stars | 语言 | 定位 |
|------|-------|------|------|
| LangChain | 100K+ | Python/JS | 最流行的 LLM 应用框架 |
| LlamaIndex | 30K+ | Python | 专注 RAG 的数据框架 |
| CrewAI | 46K+ | Python | 多 Agent 协作框架 |
| AutoGen | 55K+ | Python/C# | 微软多 Agent 框架 |
| Haystack | 15K+ | Python | 开源 RAG 框架 |

---

## 2 ReAct 标准流程

### 2.1 原始 ReAct 论文定义

```
Thought → Action → Observation → Thought → Action → Observation → ... → Final Answer
```

### 2.2 标准 Prompt 格式

```
Answer the following questions as best you can. You have access to tools provided.

Use the following format:

Question: the input question you must answer
Thought: you should always think about what to do
Action: the action to take
Action Input: the input to the action
Observation: the result of the action
... (this Thought/Action/Action Input/Observation can repeat N times)
Thought: I now know the final answer
Final Answer: the final answer to the original input question

Begin!
```

---

## 3 各框架字段对比

### 3.1 核心数据结构对比

| 概念 | LangChain | LlamaIndex | CrewAI | AutoGen | Haystack |
|------|-----------|------------|--------|---------|----------|
| **Thought** | `AgentAction.log` | `ActionReasoningStep.thought` | 内部封装 | Prompt 格式 | 内部封装 |
| **Action** | `AgentAction.tool` | `ActionReasoningStep.action` | 内部封装 | Prompt 格式 | 内部封装 |
| **Action Input** | `AgentAction.tool_input` | `ActionReasoningStep.action_input` | 内部封装 | Prompt 格式 | Tool 参数 |
| **Observation** | `intermediate_steps` 元组 | `ObservationReasoningStep.observation` | 内部封装 | Prompt 格式 | Tool 返回 |
| **Final Answer** | `AgentFinish.return_values` | `ResponseReasoningStep.response` | Task 输出 | Prompt 格式 | `last_message` |

### 3.2 LangChain 字段定义

```python
# AgentAction
class AgentAction(NamedTuple):
    tool: str                    # 工具名称
    tool_input: Union[str, dict]  # 工具输入
    log: str                      # LLM 原始思考日志

# AgentFinish
class AgentFinish(NamedTuple):
    return_values: dict          # 返回值 {"output": "..."}
    log: str                      # 最终日志

# 中间步骤
intermediate_steps: List[Tuple[AgentAction, str]]
# AgentAction + Observation (str) 配对
```

### 3.3 LlamaIndex 字段定义

```python
# ActionReasoningStep
@dataclass
class ActionReasoningStep(ReasoningStep):
    thought: str              # 思考内容
    action: str              # 工具名称
    action_input: dict       # 工具参数

# ObservationReasoningStep
@dataclass
class ObservationReasoningStep(ReasoningStep):
    observation: str         # 观察结果

# ResponseReasoningStep (Final Answer)
@dataclass
class ResponseReasoningStep(ReasoningStep):
    reasoning: str           # 完整推理
    response: str           # 最终回答
```

---

## 4 核心机制对比

### 4.1 Output Parser

| 框架 | Parser 类型 | 正则模式 |
|------|------------|----------|
| LangChain | `ReActSingleInputOutputParser` | `Thought\s*(.*?)\nAction\s*(.*?)\nAction Input\s*(.*?)` |
| LlamaIndex | `ReActOutputParser` | 类似正则，解析 Thought/Action |
| CrewAI | 内部封装 | 不暴露 |
| AutoGen | 内部解析 | 不暴露 |
| Haystack | 内部解析 | 不暴露 |

### 4.2 Stop Sequence

| 框架 | 实现方式 |
|------|---------|
| LangChain | `stop_sequence=True` 或自定义列表 |
| LlamaIndex | 内部处理 |
| CrewAI | 内部处理 |
| AutoGen | Chat completion 控制 |
| Haystack | ChatGenerator 控制 |

### 4.3 上下文管理

| 框架 | 实现 | 说明 |
|------|------|------|
| LangChain | `trim_intermediate_steps()` | 保留最近 N 步 |
| LlamaIndex | `ChatMemoryBuffer` | 内存管理 |
| CrewAI | Task 结果传递 | 任务间共享 |
| AutoGen | `Cache` | LLM 响应缓存 |
| Haystack | `State` | 共享状态 |

---

## 5 关键设计模式

### 5.1 Agent Loop 模式

**LangChain 实现**:

```python
def run(self, input_str: str, max_iterations: int = 50) -> dict:
    intermediate_steps = []
    inputs = {"input": input_str, "intermediate_steps": []}

    for _ in range(max_iterations):
        result = self.agent.invoke(inputs)

        if isinstance(result, AgentFinish):
            return {"output": result.return_values["output"]}

        # 执行工具
        observation = self._execute_tool(result)
        intermediate_steps.append((result, observation))
        inputs = {
            "input": input_str,
            "intermediate_steps": intermediate_steps,
        }

    return {"output": "Max iterations reached"}
```

**LlamaIndex Workflow 实现**:

```python
class ReActAgentWorkflow(Workflow):
    @step
    async def handle_llm_input(self, ctx, ev) -> ToolCallEvent | StopEvent:
        response = await self.llm.achat(chat_history)
        reasoning_step = self.output_parser.parse(response.message.content)
        
        if reasoning_step.is_done:
            return StopEvent(result={"response": reasoning_step.response})
        elif isinstance(reasoning_step, ActionReasoningStep):
            return ToolCallEvent(tool_calls=[...])
```

### 5.2 工具注册模式

**AutoGen 实现**:

```python
register_function(
    search_tool,          # 函数
    caller=assistant,     # 调用方
    executor=user_proxy,    # 执行方
    name="search_tool",    # 工具名
    description="..."       # 描述
)
```

**Haystack 实现**:

```python
Tool(
    name="search",
    description="...",
    parameters={...},  # JSON Schema
    function=search_func
)
```

### 5.3 多 Agent 协作模式

**CrewAI 实现**:

```python
crew = Crew(
    agents=[researcher, writer],
    tasks=[research_task, write_task],
    process="sequential"  # 或 "hierarchical"
)
```

**AutoGen 实现**:

```python
user_proxy.initiate_chat(assistant, message=...)
```

---

## 6 ReAct Prompt 对比

### 6.1 LangChain / AutoGen 标准格式

```
Question: the input question you must answer
Thought: you should always think about what to do
Action: the action to take
Action Input: the input to the action
Observation: the result of the action
... (this Thought/Action/Action Input/Observation can repeat N times)
Thought: I now know the final answer
Final Answer: the final answer to the original input question
```

### 6.2 LlamaIndex 格式

```
Thought: your thought
Action: tool_name
Action Input: {{ "input": "input value" }}
```

### 6.3 CrewAI 内部格式

```
<start_of_thought>
Thought: your thought
Action: tool_name
Action Input: {{ "param1": "value1" }}
<end_of_thought>
```

---

## 7 各框架优缺点总结

### 7.1 LangChain

| 优点 | 缺点 |
|------|------|
| ✅ 完整 ReAct 实现 | ❌ 抽象层级多 |
| ✅ 高度可定制 | ❌ 版本变化快 |
| ✅ 丰富生态 | ❌ 学习曲线陡 |

### 7.2 LlamaIndex

| 优点 | 缺点 |
|------|------|
| ✅ Workflow 灵活 | ❌ ReAct 文档分散 |
| ✅ 类型安全 | ❌ 学习曲线中等 |
| ✅ 异步支持 | |

### 7.3 CrewAI

| 优点 | 缺点 |
|------|------|
| ✅ 多 Agent 原生 | ❌ ReAct 不透明 |
| ✅ Role-Based 设计 | ❌ 灵活性低 |
| ✅ 简化开发 | |

### 7.4 AutoGen

| 优点 | 缺点 |
|------|------|
| ✅ 代码执行 | ❌ 复杂度高 |
| ✅ Human-in-Loop | ❌ 资源消耗大 |
| ✅ 微软支持 | |

### 7.5 Haystack

| 优点 | 缺点 |
|------|------|
| ✅ RAG 集成 | ❌ Agent 支持较弱 |
| ✅ 组件化 | ❌ ReAct 不透明 |
| ✅ 确认策略 | |

---

## 8 omniAgent 参考建议

### 8.1 字段设计建议

基于 LangChain/LlamaIndex 的最佳实践：

```python
# Thought + Action
@dataclass
class ThoughtActionStep:
    type: str = "thought_action"
    thought: str = ""
    action: str = ""
    action_input: dict = field(default_factory=dict)
    timestamp: int = 0

# Observation
@dataclass
class ObservationStep:
    type: str = "observation"
    content: str = ""
    timestamp: int = 0

# Final Answer
@dataclass
class FinalStep:
    type: str = "final"
    content: str = ""
    timestamp: int = 0
```

### 8.2 Loop 实现建议

```python
class ReActLoop:
    def __init__(self, llm, tools):
        self.llm = llm
        self.tools = {t.name: t for t in tools}
        self.parser = ReActOutputParser()
    
    async def run(self, task: str) -> List[dict]:
        steps = []
        prompt = self._build_prompt(task)
        
        while len(steps) < MAX_ITERATIONS:
            # 1. 调用 LLM
            response = await self.llm.invoke(prompt, stop=["\nObservation:"])
            
            # 2. 解析输出
            step = self.parser.parse(response.content)
            
            if step.type == "final":
                steps.append(step.to_dict())
                break
            
            elif step.type == "thought_action":
                steps.append(step.to_dict())
                
                # 3. 执行工具
                observation = await self._execute_tool(step.action, step.action_input)
                steps.append({"type": "observation", "content": observation})
                
                # 4. 更新 prompt
                prompt += f"\n{step.to_text()}\nObservation: {observation}"
        
        return steps
```

### 8.3 关键实现要点

| 要点 | 说明 |
|------|------|
| **Stop Sequence** | 使用 `\nObservation:` 防止 LLM 幻觉 |
| **Output Parser** | 正则解析 Thought/Action/Action Input |
| **中间步骤管理** | 保留 (Action, Observation) 配对 |
| **上下文裁剪** | 保留最近 N 步防止 overflow |
| **工具注册** | 统一的工具定义格式 |

---

## 8 补充：当前项目现状对照

> **补充时间**: 2026-03-20 08:45:00
> **补充人**: 小健
> **说明**: 基于小健对文档的评审意见，补充当前项目实际实现与文档建议的对照

### 8.4 当前项目实际字段定义

根据 `doc/流式ReAct的type和API重构设计说明书.md` 和实际代码实现：

#### 8.4.1 thought 字段（实际）

```python
{
    "type": "thought",
    "step": 1,                    # 步骤序号
    "timestamp": 1710892800000,    # 时间戳
    "content": "思考内容",        # 思考文本
    "reasoning": "推理过程",      # 推理内容（新增字段）
    "action_tool": "list_directory",  # LLM选择的工具
    "params": {"dir_path": "..."}     # LLM生成的参数
}
```

| 字段 | 类型 | 说明 | 来源 |
|------|------|------|------|
| type | `"thought"` | 固定值 | 标准 |
| step | number | 步骤序号 | 项目扩展 |
| timestamp | number | 时间戳 | 项目扩展 |
| content | string | 思考内容 | 标准log |
| **reasoning** | string | 推理内容 | **项目特有** |
| **action_tool** | string | 工具名 | **项目特有** |
| **params** | dict | 工具参数 | **项目特有** |

#### 8.4.2 action_tool 字段（实际）

```python
{
    "type": "action_tool",
    "step": 1,
    "timestamp": 1710892800000,
    "name": "list_directory",         # 工具名称
    "params": {"dir_path": "..."}    # 工具参数
}
```

#### 8.4.3 observation 字段（实际）

```python
{
    "type": "observation",
    "step": 2,
    "timestamp": 1710892801000,
    "content": "原始结果...",        # 原始结果
    "summary": "摘要..."             # 长结果摘要（项目特有）
}
```

| 字段 | 类型 | 说明 | 来源 |
|------|------|------|------|
| type | `"observation"` | 固定值 | 标准 |
| step | number | 步骤序号 | 项目扩展 |
| timestamp | number | 时间戳 | 项目扩展 |
| content | string | 原始结果 | 标准 |
| **summary** | string | 摘要 | **项目特有** |

#### 8.4.4 额外的 type（项目特有）

除了标准 ReAct 的 4 种 type，项目还有以下扩展：

| type | 说明 | 用途 |
|------|------|------|
| **start** | 任务开始 | 记录用户输入和意图分析 |
| **chunk** | 流式中间输出 | 前端渲染用 |
| **final** | 最终回答 | ReAct 退出点 |
| **error** | 错误信息 | 错误处理 |
| **incident** | 状态事件 | pause/resume/interrupted等 |

### 8.5 术语对照表

| 文档术语 | 项目实际 | 说明 |
|---------|---------|------|
| `action` | `action_tool` | 项目使用更明确的命名 |
| `action_input` | `params` | 项目使用 params 更直观 |
| `observation.content` | `observation.content` | 一致 |
| 无 | `reasoning` | 项目特有的推理传导字段 |
| 无 | `summary` | observation长结果摘要 |
| 无 | `step` | 步骤序号，便于追踪 |
| 无 | `start/chunk/error/incident` | 项目扩展的type |

### 8.6 实施进度

| 模块 | 文档设计 | 当前实现 | 状态 | 文件 |
|------|---------|---------|------|------|
| start | 未设计 | ✅ 完成 | 包含LLM意图分析 | `process_start.py` |
| thought | 基础版 | ✅ 完成 | Thought2版本带LLM | `process_thought.py` |
| action_tool | 基础版 | ✅ 完成 | 标准实现 | `process_action.py` |
| observation | 基础版 | ✅ 完成 | 带summary字段 | `process_observation.py` |
| final | 基础版 | ✅ 完成 | 标准实现 | `process_final.py` |
| chunk | 未设计 | ✅ 完成 | 流式中间输出 | 前端sse.ts |
| error | 未设计 | ✅ 完成 | 错误码体系 | `process_error.py` |
| incident | 未设计 | ✅ 完成 | 状态事件 | `process_incident.py` |

### 8.7 下一步行动项

| 优先级 | 建议项 | 当前状态 | 实施难度 | 负责人 |
|-------|-------|---------|---------|--------|
| P0 | 统一字段命名与文档一致 | 部分不一致 | 低 | 待定 |
| P1 | 增强Output Parser | 当前较弱 | 中 | 待定 |
| P2 | 上下文裁剪(trim_steps) | 未实现 | 高 | 待定 |
| P3 | scratchpad格式化 | 未实现 | 中 | 待定 |

### 8.8 当前项目参考文档

| 文档 | 说明 |
|------|------|
| `doc/流式ReAct的type和API重构设计说明书.md` | 当前项目完整设计 |
| `doc-ReAct重构/ReAct-Loop-字段级详细设计-小沈-2026-03-20.md` | 字段级详细设计 |
| `doc-ReAct重构/LLM工具调用参数命名规范-实现说明文档.md` | 工具参数规范 |

---

## 9 各框架学习报告索引

| 报告文件 | 说明 |
|---------|------|
| `LangChain-ReAct-学习报告-小沈-2026-03-20.md` | LangChain ReAct 详细分析 |
| `LlamaIndex-ReAct-学习报告-小沈-2026-03-20.md` | LlamaIndex ReAct 详细分析 |
| `CrewAI-ReAct-学习报告-小沈-2026-03-20.md` | CrewAI ReAct 详细分析 |
| `AutoGen-ReAct-学习报告-小沈-2026-03-20.md` | AutoGen ReAct 详细分析 |
| `Haystack-ReAct-学习报告-小沈-2026-03-20.md` | Haystack ReAct 详细分析 |

---

## 10 参考资源汇总

### 10.1 官方文档

| 框架 | 文档链接 |
|------|---------|
| LangChain | https://python.langchain.com/docs/modules/agents/ |
| LlamaIndex | https://docs.llamaindex.ai/en/stable/examples/agent/react_agent/ |
| CrewAI | https://docs.crewai.com/ |
| AutoGen | https://microsoft.github.io/autogen/stable/ |
| Haystack | https://docs.haystack.deepset.ai/ |

### 10.2 GitHub 仓库

| 框架 | 仓库 |
|------|------|
| LangChain | https://github.com/langchain-ai/langchain |
| LlamaIndex | https://github.com/run-llama/llama_index |
| CrewAI | https://github.com/crewAIInc/crewAI |
| AutoGen | https://github.com/microsoft/autogen |
| Haystack | https://github.com/deepset-ai/haystack |

### 10.3 ReAct 从头实现

| 项目 | 说明 |
|------|------|
| [react-from-scratch](https://github.com/arunpshankar/react-from-scratch) | 从头实现 ReAct |
| [agent-from-scratch](https://github.com/hendrixer/agent-from-scratch) | TypeScript 实现 |
| [ai-agents-from-scratch](https://github.com/pguso/ai-agents-from-scratch) | JavaScript 实现 |

---

## 11 版本记录

| 版本 | 时间 | 更新内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-03-20 05:55:00 | 初始版本，包含各框架 ReAct 实现对比和总结 | 小沈 |

---

**文档结束**
