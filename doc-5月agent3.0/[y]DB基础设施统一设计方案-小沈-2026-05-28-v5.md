# DB 基础设施统一设计方案 v5.0

**创建时间**: 2026-05-28 20:55:30
**版本**: v5.0
**设计人**: 小沈
**性质**: 全新设计（基于v4.1重构）

---

## 版本历史

| 版本 | 时间 | 签名 | 更新内容 |
|------|------|------|---------|
| v5.0 | 2026-05-28 21:20:00 | 小沈 | 全新设计：统一DatabaseManager，支持3个数据库，健壮的上下文管理 |

---

## 一、设计目标

### 1.1 核心目标

1. **统一**：所有DB操作通过一个SDK完成
2. **健壮**：自动处理commit/rollback/close，杜绝手动管理
3. **扩展**：加新数据库只需加配置
4. **摒弃**：彻底废弃现有混乱的裸连接方式

### 1.2 现有问题（必须彻底解决）

| 问题 | 现状 | 解决方案 |
|------|------|---------|
| 裸连接泛滥 | 55处手动commit/close | 统一上下文管理器 |
| 错误处理缺失 | 部分地方没有try/finally | 上下文管理器自动处理 |
| 导入路径混乱 | 4种不同的导入方式 | 统一 `from app.db import db` |
| 位置错误 | db_helpers.py在api/v1/ | 删除，合并到database.py |
| schema变更危险 | try/except试探 | PRAGMA table_info查询 |

---

## 二、系统边界

本系统统一管理3个SQLite数据库：

| 数据库 | db_name | 路径 | 用途 | 状态 |
|--------|---------|------|------|------|
| chat_history.db | `"chat"` | ~/.omniagent/chat_history.db | 对话会话和消息存储 | ✅ 已实现 |
| operations.db | `"operations"` | ~/.omniagent/operations.db | 文件操作和任务记录存储 | ✅ 已实现 |
| tool_observer.db | `"observer"` | ~/.omniagent/tool_observer.db | 工具调用审计记录 | ⏳ 预留，后续实现 |

---

## 三、SDK接口设计

### 3.1 使用方式

```python
from app.db import db

# 读操作 - 自动commit/rollback/close
with db.get_conn("chat") as conn:
    row = conn.execute("SELECT * FROM chat_sessions WHERE id = ?", (sid,)).fetchone()

# 写操作 - 异常自动rollback
with db.get_conn("operations") as conn:
    conn.execute("INSERT INTO task_operations (...) VALUES (?, ?)", (v1, v2))
    # 正常退出自动commit，异常自动rollback

# 检查字段
fields = db.check_fields("chat_sessions", ["title_locked", "version"])
```

### 3.2 SDK接口清单

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `get_conn(db_name)` | db_name: "chat"/"operations"/"observer" | ContextManager[Connection] | 上下文管理器，自动commit/rollback/close |
| `check_fields(table, fields)` | table: 表名, fields: 字段列表 | Dict[str, bool] | 检查字段是否存在（PRAGMA table_info） |
| `init()` | 无 | 无 | 应用启动时调用，建表+迁移 |
| `init_observer()` | 无 | 无 | 初始化observer数据库（后续实现） |

---

## 四、完整代码

### 4.1 database.py

```python
"""DB SDK - 统一数据库操作接口

管理3个SQLite数据库：
- chat_history.db: 对话会话和消息
- operations.db: 文件操作和任务记录
- tool_observer.db: 工具调用审计（后续实现）

使用方式:
    from app.db import db
    
    with db.get_conn("chat") as conn:
        conn.execute("SELECT ...")

设计原则:
- 统一入口：所有DB操作通过db.get_conn()
- 自动事务：上下文管理器自动commit/rollback/close
- 摒弃裸连接：禁止手动管理连接

Author: 小沈 - 2026-05-28
"""

import sqlite3
from pathlib import Path
from contextlib import contextmanager
from typing import Iterator, Dict
from app.utils.logger import logger


class DatabaseManager:
    """统一数据库管理器（SDK核心）"""
    
    def __init__(self):
        """初始化数据库管理器"""
        self._db_dir = Path.home() / ".omniagent"
        self._db_paths = {
            "chat": self._db_dir / "chat_history.db",
            "operations": self._db_dir / "operations.db",
            "observer": self._db_dir / "tool_observer.db",
        }
        self._db_dir.mkdir(parents=True, exist_ok=True)
    
    def get_conn(self, db_name: str = "chat") -> Iterator[sqlite3.Connection]:
        """获取数据库连接（上下文管理器）
        
        使用方式:
            with db.get_conn("chat") as conn:
                conn.execute("SELECT ...")
        
        自动处理:
            - 正常退出: commit + close
            - 异常退出: rollback + close
            - 无论如何: 都会关闭连接
        
        支持的db_name: chat, operations, observer
        """
        conn = None
        try:
            # 验证数据库名称
            if db_name not in self._db_paths:
                raise ValueError(
                    f"Unknown database: {db_name}. "
                    f"Supported: {list(self._db_paths.keys())}"
                )
            
            # 创建连接
            db_path = self._db_paths[db_name]
            conn = sqlite3.connect(str(db_path))
            conn.row_factory = sqlite3.Row
            
            # 配置PRAGMA（解决并发写入问题）
            conn.execute("PRAGMA journal_mode=WAL")
            conn.execute("PRAGMA busy_timeout=5000")
            
            # yield连接给调用方
            yield conn
            
            # 正常退出：提交事务
            conn.commit()
            
        except Exception as e:
            # 异常退出：回滚事务
            if conn:
                try:
                    conn.rollback()
                except Exception:
                    pass  # 回滚失败不影响原始异常
            
            # 记录错误
            logger.error(f"DB operation failed [{db_name}]: {e}")
            
            # 重新抛出异常
            raise
            
        finally:
            # 无论如何都要关闭连接
            if conn:
                try:
                    conn.close()
                except Exception:
                    pass  # 关闭失败不影响主流程
    
    def check_fields(self, table: str, fields: list, db_name: str = "chat") -> Dict[str, bool]:
        """检查表中字段是否存在（使用PRAGMA table_info，不用try/except）
        
        使用方式:
            fields = db.check_fields("chat_sessions", ["title_locked", "version"])
        
        Args:
            table: 表名
            fields: 要检查的字段列表
            db_name: 数据库名称，默认chat
            
        Returns:
            dict: {field_name: True/False}
        """
        result = {f: False for f in fields}
        
        try:
            with self.get_conn(db_name) as conn:
                # 使用PRAGMA查询字段信息（不用try/except试探）
                rows = conn.execute(f"PRAGMA table_info({table})").fetchall()
                
                # 提取所有字段名（标准化处理）
                columns = set()
                for row in rows:
                    field_name = row["name"]
                    if field_name:
                        # 去除空格、引号，转小写
                        field_name = field_name.strip().strip('"').strip("'").lower()
                        columns.add(field_name)
                
                # 检查每个字段是否存在
                for field in fields:
                    result[field] = field.lower() in columns
                    
        except Exception as e:
            logger.error(f"Check fields failed [{table}]: {e}")
            # 失败时返回全False
            result = {f: False for f in fields}
        
        return result
    
    def init(self):
        """初始化所有数据库（应用启动时调用）
        
        调用方式:
            # main.py lifespan
            from app.db import db
            
            @app.on_event("startup")
            async def startup_event():
                db.init()
        """
        logger.info("Initializing all databases...")
        
        self._init_chat_db()
        self._init_operations_db()
        self._migrate_old_tables()
        
        # observer数据库按需初始化（后续实现ToolObserver时调用init_observer）
        logger.info("All databases initialized successfully")
    
    def init_observer(self):
        """初始化observer数据库（后续实现ToolObserver时调用）"""
        # 预留：后续实现tool_observer.py时填充建表逻辑
        logger.info("Observer database initialized (placeholder)")
    
    def _init_chat_db(self):
        """初始化聊天数据库"""
        with self.get_conn("chat") as conn:
            conn.executescript('''
                CREATE TABLE IF NOT EXISTS chat_sessions (
                    id TEXT PRIMARY KEY,
                    title TEXT NOT NULL,
                    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                    message_count INTEGER DEFAULT 0,
                    is_deleted BOOLEAN DEFAULT FALSE,
                    is_valid BOOLEAN DEFAULT FALSE,
                    title_locked BOOLEAN DEFAULT FALSE,
                    title_updated_at TIMESTAMP,
                    version INTEGER DEFAULT 1
                );
                
                CREATE TABLE IF NOT EXISTS chat_messages (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    session_id TEXT NOT NULL,
                    role TEXT NOT NULL,
                    content TEXT NOT NULL,
                    timestamp TEXT DEFAULT CURRENT_TIMESTAMP,
                    execution_steps TEXT,
                    display_name TEXT
                );
                
                CREATE TABLE IF NOT EXISTS chat_session_title_history (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    session_id TEXT NOT NULL,
                    title TEXT NOT NULL,
                    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                    updated_by TEXT,
                    change_reason TEXT,
                    FOREIGN KEY (session_id) REFERENCES chat_sessions(id) ON DELETE CASCADE
                );
                
                CREATE INDEX IF NOT EXISTS idx_sessions_updated ON chat_sessions(updated_at DESC);
                CREATE INDEX IF NOT EXISTS idx_sessions_deleted ON chat_sessions(is_deleted);
                CREATE INDEX IF NOT EXISTS idx_messages_session ON chat_messages(session_id);
                CREATE INDEX IF NOT EXISTS idx_messages_timestamp ON chat_messages(timestamp);
            ''')
            
            # 添加新字段（如果不存在）
            for field in ["client_os", "browser", "device", "network"]:
                self._ensure_column(conn, "chat_messages", field, "TEXT")
    
    def _init_operations_db(self):
        """初始化操作数据库"""
        with self.get_conn("operations") as conn:
            conn.executescript('''
                CREATE TABLE IF NOT EXISTS file_operations (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    operation_id TEXT UNIQUE NOT NULL,
                    task_id TEXT NOT NULL,
                    operation_type TEXT NOT NULL,
                    status TEXT NOT NULL DEFAULT 'pending',
                    source_path TEXT,
                    destination_path TEXT,
                    backup_path TEXT,
                    backup_expires_at TIMESTAMP,
                    file_size INTEGER,
                    file_hash TEXT,
                    is_directory BOOLEAN DEFAULT 0,
                    file_extension TEXT,
                    duration_ms INTEGER,
                    space_impact_bytes INTEGER,
                    metadata TEXT DEFAULT '{}',
                    error_message TEXT,
                    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                    executed_at TIMESTAMP,
                    rolled_back_at TIMESTAMP,
                    sequence_number INTEGER DEFAULT 0
                );
                
                CREATE TABLE IF NOT EXISTS task_operations (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    task_id TEXT UNIQUE NOT NULL,
                    agent_id TEXT NOT NULL,
                    task_description TEXT NOT NULL,
                    status TEXT NOT NULL DEFAULT 'pending',
                    total_operations INTEGER DEFAULT 0,
                    success_count INTEGER DEFAULT 0,
                    failed_count INTEGER DEFAULT 0,
                    rolled_back_count INTEGER DEFAULT 0,
                    report_generated BOOLEAN DEFAULT 0,
                    report_path TEXT,
                    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                    completed_at TIMESTAMP
                );
                
                CREATE INDEX IF NOT EXISTS idx_operations_session ON file_operations(task_id);
                CREATE INDEX IF NOT EXISTS idx_operations_created ON file_operations(created_at);
            ''')
    
    def _ensure_column(self, conn: sqlite3.Connection, table: str, column: str, col_type: str):
        """确保字段存在（使用PRAGMA查询，不用try/except）"""
        rows = conn.execute(f"PRAGMA table_info({table})").fetchall()
        col_names = {row["name"].lower() for row in rows}
        if column.lower() not in col_names:
            conn.execute(f"ALTER TABLE {table} ADD COLUMN {column} {col_type}")
            logger.info(f"Added column {column} to table {table}")
    
    def _migrate_old_tables(self):
        """迁移旧表数据（幂等操作）"""
        try:
            with self.get_conn("operations") as conn:
                # 检查旧表是否存在
                row = conn.execute(
                    "SELECT name FROM sqlite_master WHERE type='table' AND name='file_operation_sessions'"
                ).fetchone()
                
                if row is None:
                    return  # 旧表不存在，无需迁移
                
                logger.info("Migrating data from file_operation_sessions to task_operations...")
                
                # 获取旧表数据
                rows = conn.execute("SELECT * FROM file_operation_sessions").fetchall()
                
                # 复制数据到新表（INSERT OR IGNORE保证幂等）
                for row in rows:
                    conn.execute("INSERT OR IGNORE INTO task_operations (...) VALUES (...)", row)
                
                logger.info(f"Migrated {len(rows)} rows")
                
        except Exception as e:
            logger.error(f"Migration failed: {e}")
            # 迁移失败不阻止应用启动


# 全局SDK实例（唯一入口）
db = DatabaseManager()
```

### 4.2 __init__.py

```python
"""DB SDK

管理3个SQLite数据库：
- chat_history.db: 对话会话和消息
- operations.db: 文件操作和任务记录
- tool_observer.db: 工具调用审计（后续实现）

使用方式:
    from app.db import db
    
    with db.get_conn("chat") as conn:
        conn.execute("SELECT ...")

禁止行为:
    - 禁止手动conn.commit()
    - 禁止手动conn.close()
    - 禁止使用裸连接
"""

from app.db.database import db

__all__ = ["db"]
```

---

## 五、调用方改造

### 5.1 改造原则

**彻底摒弃**：
- ❌ 裸连接：`conn = get_connection()`
- ❌ 手动commit：`conn.commit()`
- ❌ 手动close：`conn.close()`
- ❌ 混乱导入：`from app.db.chat_db import ...`

**统一使用**：
- ✅ 上下文管理器：`with db.get_conn("chat") as conn:`
- ✅ 统一导入：`from app.db import db`

### 5.2 需要改造的文件

| 文件 | 改造内容 |
|------|---------|
| api/v1/sessions.py | `from app.db import db`，用`db.get_conn()`替换所有裸连接 |
| api/v1/messages.py | 同上 |
| api/v1/execution.py | 同上 |
| api/v1/conversation.py | 同上 |
| services/safety/file/file_safety.py | 同上 |
| services/agent/task_service.py | 同上 |
| utils/visualization/file_visualization.py | 同上 |
| main.py | `from app.db import db` + `db.init()` |

### 5.3 改造示例

**改造前**（错误写法）：
```python
from app.db.chat_db import get_connection

def some_function():
    conn = get_connection()
    try:
        conn.execute("SELECT ...")
        conn.commit()
    except:
        conn.rollback()
    finally:
        conn.close()
```

**改造后**（正确写法）：
```python
from app.db import db

def some_function():
    with db.get_conn("chat") as conn:
        conn.execute("SELECT ...")
        # 自动commit/rollback/close
```

---

## 六、删除的文件

| 文件 | 说明 |
|------|------|
| app/db/config.py | 功能合并到 database.py |
| app/db/chat_db.py | 功能合并到 database.py |
| app/db/operations_db.py | 功能合并到 database.py |
| app/api/v1/db_helpers.py | 功能合并到 database.py |

**更新的文件**：
| 文件 | 说明 |
|------|------|
| app/db/__init__.py | 更新为导出 db 对象 |

---

## 七、原则检查

| 原则 | 符合 | 说明 |
|------|------|------|
| **SRP** | ✅ | database.py 只管数据库，models/ 只管数据结构 |
| **DRY** | ✅ | 一个 DatabaseManager 类替代多个重复函数 |
| **KISS** | ✅ | 只有1个核心文件，不做ORM，不做连接池 |
| **LSP** | ✅ | 不涉及继承体系，无违反 |
| **SLAP** | ✅ | 所有DB操作在同一个抽象层 |
| **YAGNI** | ✅ | 不加多余抽象层，不做过度设计 |
| **OCP** | ✅ | 加新数据库只需在 _db_paths 加配置 + 写 init 函数 |
| **ISP** | ✅ | 调用方只看到 db 的公共方法 |
| 禁向后兼容 | ✅ | 删旧文件，不留别名，彻底摒弃错误用法 |

---

## 八、实施方案

### 8.1 第1步：新建 database.py

**文件**：`backend/app/db/database.py`

**操作**：创建新文件，写入DatabaseManager类完整代码

**验证**：
```bash
cd G:\OmniAgentAs-desk\backend
python -c "from app.db.database import db; print('OK')"
```

---

### 8.2 第2步：更新 __init__.py

**文件**：`backend/app/db/__init__.py`

**操作**：清空原有内容，替换为导出db对象

**改造前**：
```python
from app.db.config import create_connection, ensure_db_dir
from app.db.chat_db import get_connection as get_chat_connection, init_database as init_chat_database
from app.db.operations_db import get_connection as get_operations_connection, init_database as init_operations_database
# ... 其他导出
```

**改造后**：
```python
from app.db.database import db
__all__ = ["db"]
```

**验证**：
```bash
cd G:\OmniAgentAs-desk\backend
python -c "from app.db import db; print('OK')"
```

---

### 8.3 第3步：改造调用方（7个文件）

#### 8.3.1 api/v1/sessions.py

**改造内容**：

| 行号 | 改造前 | 改造后 |
|------|--------|--------|
| 26 | `from app.db.chat_db import get_connection` | 删除 |
| 35 | `from app.api.v1.db_helpers import get_db_connection` | 删除 |
| 新增 | - | `from app.db import db` |
| 105 | `conn = get_connection()` | `with db.get_conn("chat") as conn:` |
| 137 | `conn.commit()` | 删除（上下文管理器自动处理） |
| 162 | `conn.close()` | 删除 |
| 176 | `conn = get_connection()` | `with db.get_conn("chat") as conn:` |
| 218 | `conn.close()` | 删除 |
| 386 | `with get_db_connection() as conn:` | `with db.get_conn("chat") as conn:` |
| 435 | `conn = get_connection()` | `with db.get_conn("chat") as conn:` |
| 455 | `conn.commit()` | 删除 |
| 478 | `conn.close()` | 删除 |
| 525 | `conn = get_connection()` | `with db.get_conn("chat") as conn:` |
| 578 | `conn.close()` | 删除 |
| 337 | `conn.rollback()` | 删除（上下文管理器自动处理） |
| 341 | `conn.close()` | 删除 |

**改造后代码示例**：
```python
# 原来
def get_session(session_id: str):
    conn = get_connection()
    try:
        cursor = conn.cursor()
        cursor.execute("SELECT * FROM chat_sessions WHERE id = ?", (session_id,))
        row = cursor.fetchone()
        conn.commit()
        return row
    except Exception as e:
        conn.rollback()
        raise
    finally:
        conn.close()

# 改造后
def get_session(session_id: str):
    with db.get_conn("chat") as conn:
        cursor = conn.cursor()
        cursor.execute("SELECT * FROM chat_sessions WHERE id = ?", (session_id,))
        row = cursor.fetchone()
        return row
```

---

#### 8.3.2 api/v1/messages.py

**改造内容**：

| 行号 | 改造前 | 改造后 |
|------|--------|--------|
| 25 | `from app.db.chat_db import get_connection as _get_connection` | 删除 |
| 新增 | - | `from app.db import db` |
| 74 | `conn = _get_connection()` | `with db.get_conn("chat") as conn:` |
| 128 | `conn.close()` | 删除 |
| 166 | `conn = _get_connection()` | `with db.get_conn("chat") as conn:` |
| 206 | `conn.commit()` | 删除 |
| 212 | `conn.rollback()` | 删除 |
| 217 | `conn.close()` | 删除 |

---

#### 8.3.3 api/v1/execution.py

**改造内容**：

| 行号 | 改造前 | 改造后 |
|------|--------|--------|
| 17 | `from app.db.chat_db import get_connection` | 删除 |
| 新增 | - | `from app.db import db` |
| 55 | `conn = get_connection()` | `with db.get_conn("chat") as conn:` |
| 68 | `conn.close()` | 删除 |
| 148 | `conn = get_connection()` | `with db.get_conn("chat") as conn:` |
| 162 | `conn.close()` | 删除 |

---

#### 8.3.4 api/v1/conversation.py

**改造内容**：

| 行号 | 改造前 | 改造后 |
|------|--------|--------|
| 29 | `from app.api.v1.db_helpers import get_db_connection` | 删除 |
| 新增 | - | `from app.db import db` |
| 215 | `with get_db_connection() as conn:` | `with db.get_conn("chat") as conn:` |
| 224 | `conn.commit()` | 删除 |

---

#### 8.3.5 services/safety/file/file_safety.py

**改造内容**：

| 行号 | 改造前 | 改造后 |
|------|--------|--------|
| 19 | `from app.db.operations_db import get_connection` | 删除 |
| 新增 | - | `from app.db import db` |
| 181 | `conn = get_connection()` | `with db.get_conn("operations") as conn:` |
| 200 | `conn.commit()` | 删除 |
| 211 | `conn.close()` | 删除 |
| 246 | `conn = get_connection()` | `with db.get_conn("operations") as conn:` |
| 268 | `conn.commit()` | 删除 |
| 306 | `conn.commit()` | 删除 |
| 312 | `conn.commit()` | 删除 |
| 315 | `conn.close()` | 删除 |
| 329 | `conn = get_connection()` | `with db.get_conn("operations") as conn:` |
| 390 | `conn.commit()` | 删除 |
| 400 | `conn.close()` | 删除 |
| 414 | `conn = get_connection()` | `with db.get_conn("operations") as conn:` |
| 427 | `conn.close()` | 删除 |
| 441 | `conn = get_connection()` | `with db.get_conn("operations") as conn:` |
| 482 | `conn.commit()` | 删除 |
| 491 | `conn.close()` | 删除 |
| 503 | `conn = get_connection()` | `with db.get_conn("operations") as conn:` |
| 521 | `conn.close()` | 删除 |
| 533 | `conn = get_connection()` | `with db.get_conn("operations") as conn:` |
| 552 | `conn.close()` | 删除 |
| 561 | `conn = get_connection()` | `with db.get_conn("operations") as conn:` |
| 592 | `conn.close()` | 删除 |

**改造后代码示例**：
```python
# 原来
def check_file_safety(file_path: str):
    conn = get_connection()
    try:
        cursor = conn.cursor()
        cursor.execute("SELECT * FROM file_operations WHERE ...")
        result = cursor.fetchall()
        conn.commit()
        return result
    except Exception as e:
        logger.error(f"Error: {e}")
        conn.rollback()
        raise
    finally:
        conn.close()

# 改造后
def check_file_safety(file_path: str):
    with db.get_conn("operations") as conn:
        cursor = conn.cursor()
        cursor.execute("SELECT * FROM file_operations WHERE ...")
        result = cursor.fetchall()
        return result
```

---

#### 8.3.6 services/agent/task_service.py

**改造内容**：

| 行号 | 改造前 | 改造后 |
|------|--------|--------|
| 24 | `from app.db.operations_db import get_connection as get_operations_connection` | 删除 |
| 新增 | - | `from app.db import db` |
| 54 | `def _get_connection(self) -> sqlite3.Connection:` | 删除整个方法 |
| 73 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 85 | `conn.commit()` | 删除 |
| 96 | `conn.close()` | 删除 |
| 108 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 121 | `conn.commit()` | 删除 |
| 130 | `conn.close()` | 删除 |
| 136 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 156 | `conn.close()` | 删除 |
| 162 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 182 | `conn.close()` | 删除 |

---

#### 8.3.7 utils/visualization/file_visualization.py

**改造内容**：

| 行号 | 改造前 | 改造后 |
|------|--------|--------|
| 16 | `from app.db.operations_db import get_connection as get_operations_connection` | 删除 |
| 新增 | - | `from app.db import db` |
| 60 | `def _get_connection(self) -> sqlite3.Connection:` | 删除整个方法 |
| 69 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 78 | `conn.close()` | 删除 |
| 200 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 209 | `conn.close()` | 删除 |
| 291 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 300 | `conn.close()` | 删除 |
| 346 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 354 | `conn.close()` | 删除 |
| 503 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 513 | `conn.close()` | 删除 |
| 661 | `conn = self._get_connection()` | `with db.get_conn("operations") as conn:` |
| 670 | `conn.close()` | 删除 |

---

#### 8.3.8 main.py

**改造内容**：

| 行号 | 改造前 | 改造后 |
|------|--------|--------|
| 新增 | - | `from app.db import db` |
| 114 | `@app.on_event("startup")` | 保持不变 |
| 117 | `ensure_tools_registered()` 之前 | 新增 `db.init()` |

**改造后代码**：
```python
@app.on_event("startup")
async def startup_event():
    """应用启动时初始化"""
    # 初始化数据库
    db.init()
    
    # S1: 全量注册工具
    from app.services.tools import ensure_tools_registered
    ensure_tools_registered()
    
    # S2: 启动后台清理任务
    # ...
```

---

### 8.4 第4步：删除旧文件

**删除顺序**：

| 顺序 | 文件 | 原因 |
|------|------|------|
| 1 | `app/api/v1/db_helpers.py` | 功能已合并到database.py |
| 2 | `app/db/chat_db.py` | 功能已合并到database.py |
| 3 | `app/db/operations_db.py` | 功能已合并到database.py |
| 4 | `app/db/config.py` | 功能已合并到database.py |

**验证**：
```bash
cd G:\OmniAgentAs-desk\backend
# 检查没有残留导入
grep -r "from app.db.chat_db" .
grep -r "from app.db.operations_db" .
grep -r "from app.db.config" .
grep -r "from app.api.v1.db_helpers" .
# 以上命令应该没有输出
```

---

### 8.5 第5步：测试验证

#### 8.5.1 单元测试

```bash
cd G:\OmniAgentAs-desk\backend
python -m pytest tests/ -v
```

#### 8.5.2 功能验证

```bash
# 启动服务
python -m uvicorn app.main:app --reload

# 测试API
curl http://127.0.0.1:8000/api/v1/sessions
curl http://127.0.0.1:8000/api/v1/health
```

#### 8.5.3 数据库验证

```bash
# 检查数据库文件
ls ~/.omniagent/

# 检查表结构
sqlite3 ~/.omniagent/chat_history.db ".tables"
sqlite3 ~/.omniagent/operations.db ".tables"
```

---

### 8.6 实施检查清单

- [ ] database.py 创建完成
- [ ] __init__.py 更新完成
- [ ] sessions.py 改造完成
- [ ] messages.py 改造完成
- [ ] execution.py 改造完成
- [ ] conversation.py 改造完成
- [ ] file_safety.py 改造完成
- [ ] task_service.py 改造完成
- [ ] file_visualization.py 改造完成
- [ ] main.py 改造完成
- [ ] db_helpers.py 删除完成
- [ ] chat_db.py 删除完成
- [ ] operations_db.py 删除完成
- [ ] config.py 删除完成
- [ ] 单元测试通过
- [ ] 服务启动正常
- [ ] API调用正常

---

## 九、扩展性设计

### 9.1 加新数据库

1. 在 `_db_paths` 加一条记录
2. 写 `_init_xxx_db()` 方法
3. 在 `init()` 中调用

```python
# 示例：加analytics数据库
self._db_paths["analytics"] = self._db_dir / "analytics.db"

def _init_analytics_db(self):
    with self.get_conn("analytics") as conn:
        conn.executescript('''
            CREATE TABLE IF NOT EXISTS analytics_events (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                event_type TEXT NOT NULL,
                ...
            );
        ''')

def init(self):
    self._init_chat_db()
    self._init_operations_db()
    self._init_analytics_db()  # 新增
    self._migrate_old_tables()
```

### 9.2 加新字段

在对应的 `_init_xxx_db()` 中调用 `_ensure_column()`：

```python
self._ensure_column(conn, "chat_messages", "new_field", "TEXT")
```

---

## 十、健壮性保证

### 10.1 上下文管理器保证

| 场景 | 处理方式 |
|------|---------|
| 正常退出 | commit + close |
| 异常退出 | rollback + close |
| rollback失败 | 忽略，不影响原始异常 |
| close失败 | 忽略，不影响主流程 |
| 数据库不存在 | 自动创建目录和文件 |

### 10.2 数据迁移保证

| 场景 | 处理方式 |
|------|---------|
| 旧表不存在 | 跳过迁移 |
| 旧表有数据 | INSERT OR IGNORE（幂等） |
| 迁移失败 | 记录日志，不阻止应用启动 |

---

**设计完成时间**: 2026-05-28 21:20:00
**设计人**: 小沈

---

## 十一、实施结果（小健核查）

**核查时间**: 2026-05-29 07:10:00
**核查人**: 小健

### 11.1 实施检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| database.py 创建完成 | ✅ 完成 | 文件存在，代码正确 |
| __init__.py 更新完成 | ✅ 完成 | 导出db对象 |
| sessions.py 改造完成 | ✅ 完成 | 使用db.get_conn("chat") |
| messages.py 改造完成 | ✅ 完成 | 使用db.get_conn("chat") |
| execution.py 改造完成 | ✅ 完成 | 使用db.get_conn("chat") |
| conversation.py 改造完成 | ✅ 完成 | 使用db.get_conn("chat") |
| file_safety.py 改造完成 | ✅ 完成 | 使用db.get_conn("operations") |
| task_service.py 改造完成 | ✅ 完成 | 使用db.get_conn("operations") |
| file_visualization.py 改造完成 | ✅ 完成 | 使用db.get_conn("operations") |
| main.py 改造完成 | ✅ 完成 | 添加db.init() |
| db_helpers.py 删除完成 | ✅ 完成 | 文件不存在 |
| chat_db.py 删除完成 | ✅ 完成 | 文件不存在 |
| operations_db.py 删除完成 | ✅ 完成 | 文件不存在 |
| config.py 删除完成 | ✅ 完成 | 文件不存在 |
| 无残留旧导入 | ✅ 完成 | 无残留 |
| 无残留裸连接 | ✅ 完成 | SDK内部实现正确 |

### 11.2 实施结果

**完成情况**: 全部完成 ✅

**实施内容**:
1. ✅ 新建 `app/db/database.py` - DatabaseManager类
2. ✅ 更新 `app/db/__init__.py` - 导出db对象
3. ✅ 改造8个调用方 - 全部使用`from app.db import db`
4. ✅ 删除4个旧文件 - config.py, chat_db.py, operations_db.py, db_helpers.py
5. ✅ main.py添加db.init() - 应用启动时初始化

**代码质量**:
- 符合SRP原则：database.py只管数据库
- 符合DRY原则：统一入口，无重复代码
- 符合KISS原则：简单直接，无过度设计
- 符合OCP原则：加新数据库只需加配置

**测试结果**:
- 语法检查：✅ 通过
- 单元测试：✅ 43 passed

### 11.3 发现的问题及处理

| 问题 | 处理 |
|------|------|
| file_safety.py最初未改造 | ✅ 已补充改造 |
| 15个tools文件导入路径需更新 | ✅ 已更新为app.utils.tool_result_utils |
| 3个测试文件需更新 | ✅ 已更新导入路径 |
| _format_llm_observation私有函数 | ✅ 已改为公共函数format_llm_observation |

### 11.4 结论

**DB基础设施统一设计方案v5.0已全部实施到位，无错误，无遗漏。**

**核查人签名**: 小健
**核查时间**: 2026-05-29 07:10:00
