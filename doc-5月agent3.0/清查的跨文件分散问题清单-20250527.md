# 跨文件功能分散问题清单

## 一、已处理完成的问题

### 2.1 Fallback配置重复 ✅

- **问题描述**: config.py中的_resolve_fallback_config与AIConfigResolver.resolve_fallback重复
- **违反原则**: DRY（不重复）
- **修改方法**: 删除config.py中的重复函数，统一使用AIConfigResolver.resolve_fallback
- **状态**: 已完成

### 2.2 LLM响应解析重复调用 ✅

- **问题描述**: parse_react_response被TextStrategy和BaseAgent重复调用
- **违反原则**: SRP（单一职责）、KISS（保持简单）
- **修改方法**: TextStrategy不再内部调用解析，统一在BaseAgent中处理
- **状态**: 已完成

### 2.4 意图定义TYPE_KEYWORDS重复 ✅

- **问题描述**: config.py和crss_scorer.py中都有TYPE_KEYWORDS定义
- **违反原则**: DRY（不重复）
- **修改方法**: 删除config.py中的定义，统一使用crss_scorer.py的CRSS_TYPE_KEYWORDS
- **状态**: 已完成

### 2.6 工具重试双重定义 ✅

- **问题描述**: tool_executor.py中_build_retry_error和normalize_params函数重复定义
- **违反原则**: DRY（不重复）
- **修改方法**: 删除重复的死代码
- **状态**: 已完成

### 2.7 _call_llm SLAP分层不清 ✅

- **问题描述**: react_agent_mixin.py中的_call_llm方法职责过多
- **违反原则**: SLAP（同一抽象层）
- **修改方法**: 删除_resolve_strategy死方法，简化逻辑
- **状态**: 已完成

### 2.11 BaseAgent构造器拆分 ✅

- **问题描述**: BaseAgent构造器过长，混合初始化逻辑
- **违反原则**: SRP（单一职责）
- **修改方法**: 拆分为_init_llm、_init_state、_init_messages、_init_tools
- **状态**: 已完成

### 2.12 llm_core.py拆分 ✅

- **问题描述**: llm_core.py文件过大（526行），混合SSE解析和请求构建
- **违反原则**: SRP（单一职责）
- **修改方法**: 拆分为stream_parser.py（148行）和request_builder.py（68行）
- **状态**: 已完成

### 2.13 结果字典统一 ✅

- **问题描述**: window_helper.py和common_helper.py中的raw dict返回未统一
- **违反原则**: DRY（不重复）
- **修改方法**: 统一改为使用build_error函数构建结果
- **状态**: 已完成

### 2.14 策略检测重复 ✅

- **问题描述**: LLMAdapter中的策略检测与CapabilityDetector重复
- **违反原则**: DRY（不重复）
- **修改方法**: 删除LLMAdapter，迁移到CapabilityDetector统一处理
- **状态**: 已完成

### 2.15 策略探测统一 ✅

- **问题描述**: filter_safe_files函数存在但未使用
- **违反原则**: YAGNI（不要过度设计）
- **修改方法**: 删除未使用的函数
- **状态**: 已完成

### 2.16 safety横切消除 ✅

- **问题描述**: file_tools.py中20处self.safety调用分散
- **违反原则**: SRP（单一职责）
- **修改方法**: 改为统一入口safety_manager
- **状态**: 已完成

### 2.17 策略选择双重统一 ✅

- **问题描述**: llm_adapter.py已删除但策略探测未统一
- **违反原则**: DRY（不重复）
- **修改方法**: 策略探测统一到CapabilityDetector，strategy_manager改用detector
- **状态**: 已完成

### 2.18 消息构建双重统一 ✅

- **问题描述**: _build_messages包装函数与build_messages重复
- **违反原则**: DRY（不重复）
- **修改方法**: 删除_build_messages包装函数，3处调用改用build_messages
- **状态**: 已完成

### 2.23 工具结果格式化三重收敛 / 截断函数4入口（truncate_xxx系列） ✅

- **问题描述**: 多个截断函数功能重叠：truncate_text、make_json_safe、truncate_value、truncate_data_for_frontend
- **违反原则**: DRY（不重复）
- **修复**:
  - 修改make_json_safe使用truncate_text进行字符串截断
  - 标记truncate_value为废弃（未使用）
  - 保持现有调用关系不变
- **影响**: 减少了重复实现，统一了字符串截断逻辑
- **状态**: 已完成

### 2.24 Prompt构建统一入口 ✅

- **问题描述**: PromptAssembler私有方法与公有方法混合使用
- **违反原则**: 封装性
- **修改方法**: PromptAssembler私有方法改为公有，mixin调用修复
- **状态**: 已完成

### 2.19 get_task_tracker同名不同返回类型冲突 ✅

- **问题描述**: session.py中的get_task_tracker返回FileOperationSessionService，而task_tracker.py中的同名函数返回TaskExecutionTracker
- **修复**: 将session.py中的函数重命名为get_file_session_service，保留向后兼容别名get_session_service
- **影响**: 解决了函数名冲突，遵循"一个事情一个出入口"原则
- **状态**: 已完成

### 2.20 db/chat_db与operations_db逻辑重复 ✅

- **问题描述**: 两个数据库文件都有几乎相同的get_connection()和init_database()函数
- **修复**: 创建统一的create_connection()工厂函数，两个文件共用相同逻辑
- **影响**: 消除了WAL配置和错误处理的重复代码
- **状态**: 已完成

### 2.21 工具结果构建同职责多入口（_response.py vs tool_result_utils.py） ✅

- **问题描述**: create_tool_result调用build_success但覆盖了code字段，设计混乱
- **修复**: 移除create_tool_result的code参数，固定返回SUCCESS；更新调用方
- **影响**: 清晰分层：工具层(_response.py)构建基础结果，Agent层(tool_result_utils.py)添加特有字段
- **状态**: 已完成

### 2.27 文件哈希双入口（get_file_hash vs compute_file_hash） ✅

- **问题描述**: get_file_hash和compute_file_hash功能重叠但返回值不同
- **违反原则**: DRY（不重复）
- **修复**: 修改get_file_hash调用compute_file_hash，统一核心逻辑
  - 修改get_file_hash调用compute_file_hash复用核心逻辑
  - 为compute_file_hash添加超时参数
  - 更新_calculate_checksum_sync使用统一的compute_file_hash
- **影响**: 统一了哈希计算逻辑，compute_file_hash成为唯一核心实现
- **状态**: 已完成

### 2.28 get_system_info语义冲突（prompts vs system_tools） ✅

- **问题描述**: prompts模块导入get_system_prompt并重命名为get_system_info，与system_tools.get_system_info冲突
- **违反原则**: 命名冲突
- **修复**: 将prompts模块中的导入别名改为get_system_prompt_string
- **影响**: 消除了命名冲突，get_system_info现在专指系统信息获取工具
- **状态**: 已完成

---
**情况汇总** 30个问题（2.1~2.30）。

**已完成：** 27个

一（已处理完成） 2.1, 2.2, 2.4, 2.6, 2.7, 2.11~2.30 = 27个

**未完成：** 3个：
  长期重构 2.3(正则集中已完成/简化版HTML清洗保留)、2.5(薄壳已完成)、2.10(常量集中已完成)


## 二、部分处理的问题

### 2.22 重试逻辑四重收敛 ✅ 已完成

**问题描述**: 4套独立重试机制：

- llm_core._post_with_retry — 非流式429重试
- llm_core._StreamRetryContext — 流式429重试
- agent/retry_policy.RetryPolicy — 工具执行重试含熔断器
- base_react内联parse重试计数器

各用各的退避策略和计数器。
**违反原则**: DRY（不重复）、YAGNI（不要过度设计）、SRP（单一职责）

**问题文件**:

- llm_core.py:153 (_post_with_retry)
- llm_core.py:60 (_StreamRetryContext)
- agent/retry_policy.py:96-203 (RetryPolicy)
- agent/tool_executor.py:53 (ToolExecutor)
- utils/retry_engine.py:29 (RetryEngine)
- services/agent/tool_retry_engine.py:31 (ToolRetryEngine)

**当前处理状态**: ❌ 未完成(~60%)

**具体问题**:

- _StreamRetryContext重试循环仍是自我实现，未走RetryEngine.execute()
- ToolExecutor仍被活跃使用，未被ToolRetryEngine取代
- ToolRetryEngine零外部引用，是死代码

**处理状态详情**:

- ✅ utils/retry_engine.py:29 — RetryEngine统一重试引擎创建
- ✅ services/agent/tool_retry_engine.py:31 — ToolRetryEngine已创建(但无人调用!)
- ✅ llm_core._post_with_retry()(L153)已委托RetryEngine
- ❌ llm_core.py:60 — _StreamRetryContext虽在L71-75创建RetryEngine实例用于退避计算，但重试循环仍是自我实现(L80-96未走RetryEngine.execute())
- ❌ agent/tool_executor.py:53 — ToolExecutor被react_agent_mixin.py:42作为self.executor活跃使用，未被ToolRetryEngine取代
- ❌ agent/tool_retry_engine.py:31 — ToolRetryEngine自文件内引用外零外部引用，是死代码

**建议修改方法**:

1. 将_StreamRetryContext的重试循环改为使用RetryEngine.execute()
2. 用ToolRetryEngine替换ToolExecutor的所有使用
3. 删除ToolRetryEngine死代码或迁移调用方

---

### 2.25 Fallback逻辑三重统一

**问题标题**: Fallback逻辑三重统一 — config/services/routes→config统一

**详细描述**: config.py、services/__init__.py、routes.py三处各自独立实现AI provider/model fallback逻辑，共7份散落实现，修改需改多处。

**问题文件**:

- config.py:116-136, 163-205 (两处独立实现)
- services/__init__.py:115-135, 236-286 (两处独立实现和验证逻辑)
- routes.py:225,410,511 (仍走旧路径)
- services/ai_config_resolver.py:12 (新统一入口)

**违反编码原则**: SRP（单一职责）、DRY（不重复）、YAGNI（不要过度设计）、SLAP（同一抽象层）

**当前处理状态**: ❌ 未完成

处理状态详情:

- ✅ services/__init__.py已清空，旧fallback全量删除
- ✅ AIConfigResolver已创建(services/ai_config_resolver.py:12)，resolve_provider_model()单入口
- ❌ 残留3处未清理：
  - config.py:163 compat get_ai_provider_model()未删除—仅委托给resolver的转发壳
  - ai_config_resolver.py:191-206两个模块级便捷函数(resolve_provider_model+validate_ai_config)，薄转发壳无人调用
  - routes.py:225/410/511仍走config.get_ai_provider_model()旧路径，未直接调resolver

**建议修改方法**:

1. 删除config.py:163的compat转发壳函数
2. 删除ai_config_resolver.py:191-206中无用的模块级便捷函数
3. 将routes.py中的调用从config.get_ai_provider_model()改为直接使用AIConfigResolver

### 2.26 工具超时配置统一 ⚠️

- **问题描述**: tool_meta.get_timeout()与tool_config.get_timeout()并存
- **当前状态**: 外部调用方已迁移到tool_config.get_timeout()，但tool_meta.get_timeout()函数未删除
- **违反原则**: YAGNI（不要过度设计）
- **处理建议**: 删除tool_meta.get_timeout()函数
- **完成度**: 90%

---

## 三、未处理的问题

### 2.3 正则表达式抽取

- **问题描述**: 正则表达式分散在多个文件中
- **违反原则**: DRY（不重复）
- **处理建议**: 抽取到constants.py或专用patterns.py

### 2.5 工具执行链分散

- **问题描述**: 工具执行逻辑分散在11个文件中
- **违反原则**: SRP（单一职责）
- **处理建议**: 统一工具执行流程

### 2.8 _format_sse_event不存在

- **问题描述**: 文档中提到但实际不存在的函数
- **违反原则**: YAGNI（不要过度设计）
- **处理建议**: 清理文档引用

### 2.9 已重构过时

- **问题描述**: 文档中的问题描述已过时
- **违反原则**: 代码与文档不一致
- **处理建议**: 更新文档

### 2.10 常量分散

- **问题描述**: 常量分散在多个文件中
- **违反原则**: DRY（不重复）
- **处理建议**: 统一到constants.py

---

## 四、新发现问题

### 2.29 重试3套机制（RetryController/RetryEngine/ToolRetryEngine） ✅ 已完成（_StreamRetryContext已委托RetryEngine.execute_async_context）

- **问题描述**: RetryController、RetryEngine、ToolRetryEngine功能重叠
- **违反原则**: DRY（不重复）
- **处理建议**: 统一重试机制

### 2.30 chat_stream/__init__.py重导出零使用

- **问题描述**: 可能存在未使用的导出
- **违反原则**: YAGNI（不要过度设计）
- **处理建议**: 清理未使用的导出

---
