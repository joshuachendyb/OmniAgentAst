# [72] 设置界面 API Key 链路问题核实与整改方案

**版本**: v2.0
**创建时间**: 2026-09-26 01:01:38
**更新时间**: 2026-09-26 05:35:00
**编写人**: 小欧

## 版本历史

| 版本 | 时间 | 签名 | 修改内容 |
|------|------|------|---------|
| v3.0 | 2026-09-26 08:19:24 | 小欧 | 北京老陈要求「全文熟读 10 遍、检查上下逻辑一致性」→ **10 遍审查发现 6 个问题并全部修复**：**①【实质矛盾】** 第十章删除理由「与第五章重复」与第十二章「与第五章不重复、可独立成立」**同一逻辑两头用**（两者都是看明文）→ 澄清为：第十章/第十二章**与第五章均不重复**（第五章=配置文件全文粗放、它们=单个 key 精细），第十章被删的真实原因是**被第十二章替代**（形态更优），已修正 4 处表述；**②【契约冲突】** 第七章「两键恒定」与第十二章「三键恒定」冲突 → 明确最终契约为三键、第七章为其子集、**两处改同一函数须合并一次实施**；**③【表述含糊】** 第十一章「保留 GET /config」未界定边界 → 补全 5 个 GET 端点全部保留（含第五章依赖的 /config/read）；**④【改法分散】** 第二章与第八章都要求删 resolver.py:211 → 补交叉说明、附录已合并为一条；**⑤【编号断裂】** 第六章小节从 6.3 起、缺 6.1/6.2 → 重排为 6.1~6.7；**⑥【风格不统一】** 25 处 `###` 标题未编号 → 统一为 X.Y 格式 |
| v3.1 | 2026-09-26 08:32:10 | 小欧 | 北京老陈裁定「重复的、没有用的可以删除，留着干嘛，重新编排章节」→ **删除原第十章占位**（速查表占位行 + 3 处解释其删除理由的冗余说明块），**章节重排为连续 12 章**：原十一章(key 检测)→**第十章**、原十二章(旧通道)→**第十一章**、原十三章(key 显示与眼睛)→**第十二章**；小节号同步（11.x→10.x、12.x→11.x、13.x→12.x）、速查表与全部交叉引用同步更新。现正文一~十二连续无缺口，小节编号全部连续，速查表 12 行与正文一一对应 |
| v3.2 | 2026-09-26 08:41:22 | 小欧 | 北京老陈裁定「还有什么要确认的」→ **不再设待确认项，7 项全部按建议定死并落进正文，删除附录三**：① `/health` 豁免鉴权、② 暗号存 `config.yaml` 的 `security.api_token` 并支持环境变量、③ 第十章支持测未保存 key、④ 第十章建议先做、⑤ 第十一章整条删端点+删 3 个死测试+迁移并发护栏、⑥ 第二章在 `resolver.py` 抛专属异常+`stream_orchestrator.py` 专属 catch+`ErrorDetail.tsx` 补 `config_error` 中文标签、⑦ 第十二章须待第九章鉴权落地。**其中第 6 项原只在附录三、正文未落，已补为第二章 2.5「报错如何送达前端」+2.7 验收**（列出 `ERROR_TYPE_LABELS` 现有 21 项证明缺 `config_error`、说明 `formatErrorType` 为 `LABELS[type] \|\| type` 故不补则显示英文）。原附录四顺延为附录三 |
| v2.9 | 2026-09-26 08:08:07 | 小欧 | 北京老陈对「token 分发」不解且要求不必反复提问 → **第九章新增 9.5「token 分发是什么（大白话）」**：以"暗号"比喻讲清三件事——token=给服务设的暗号、知道才放行不知则 401；「分发」就是把暗号告诉需要用的人；给出 4 步最简操作（设置里填暗号→前端弹框输入→输对记住→泄露就换）＋实际场景对照表＋成本提示（84 个自动化测试须带暗号）。待确认第 1 项由"分发方式"改为**已定：登录页输入暗号**（后续 9.5.4 记录实施细节） |
| v2.8 | 2026-09-26 08:01:34 | 小欧 | 北京老陈问「服务端在 B 机器、客户端在其他机器，这个不好吗？」→ 追问确认真实部署为**局域网多机共用 + 多人/半公开** → **第九章整章推翻重写，纠正小欧重大误判**：① 原建议「改 `--host 127.0.0.1`」在多机部署下会**直接使其他机器连不上、服务作废**，已撤回；② `--host 0.0.0.0` **必须保留**；③ 「全面鉴权」由"建议单独立项"**升级为必做**。新增 token 鉴权实施方案。**第五章 5.3 前提同步修正**：其"明文可接受"依赖第九章鉴权而非监听地址 |
| v2.7 | 2026-09-26 07:47:50 | 小欧 | 北京老陈就第八章提出**与原方案对立的正确主张**：「URL 不能空（加 provider 必填）→ 空是错误状态要红色提示 → 保存就是保存」→ **第八章推翻重写**。纠正小欧两处不实结论：① 原称"清空后静默走官方直连"——实测 `_DEFAULT_URLS` 仅 deepseek/qwen/ollama 三家有硬编码地址，且 `:268` 注释明写"当前工作路径均不触发"；② 级别"中"→**低**。新方案：**base_url 必填 + 空则红色错误提示 + 拒绝保存 + 删除 `_default_base_url` 兜底** |
| v2.6 | 2026-09-26 07:29:47 | 小欧 | 北京老陈新增需求：「有 key 时显示前 4 位 + 后 4 位（如 `sk-1****7890`），用眼睛按钮切换隐藏/真实」→ **新增第十二章**。三项决策已定：①点眼睛**要二次确认 + 30 秒自动恢复**；②**接受泄露面 4 位→8 位**；③**先写文档，实施先后待定**。核实两个技术前提：后端现仅返 `suffix`、**无前缀**；明文前端从未持有。另识别**双眼睛冲突**与**短 key 前后缀重叠**两个实现坑 |
| v2.5 | 2026-09-26 07:20:10 | 小欧 | 北京老陈对第六章「三态」仍不解 → **新增 6.3 大白话解释**（三态=不修改/设置新值/清空；根源是空字符串 `''` 被赋予"别动它"与"设成空"两种相反含义）与 6.4 两个具体坏例子、6.5 潜伏成因与第三章的关系；**方案定为 B**（北京老陈拍板）：secret 项**统一收口到 `model_service` provider 通道**复用第三章三态，`settings_service` 写路径改为**显式拒绝**并加注册自检，**不在通用通道另开一套**。待确认项 8 → 6 条 |
| v2.4 | 2026-09-26 07:12:33 | 小欧 | 北京老陈裁定：**「关于→配置文件」显示明文全文没有问题，明文就明文** → ① **第五章降级为「不改」**，保留风险说明并绑定前置条件；② **第十章「查看明文」删除**（前提不成立，功能与第五章重复）；③ **第九章升为必做前置**；④ 第十章保留。待确认项由 9 条减至 7 条 |
| v2.3 | 2026-09-26 06:35:51 | 小欧 | 北京老陈指出第二章「没看懂」并给出正确逻辑「key 空白就是空白，不能瞎改，调用不通直接报错」→ **第二章判定修正**：补上 `_pv_key=None` 两种性质相反的情况（**A 同 provider** 保留 fallback、**B 跨 provider** 直接报错）；方案由「记 WARNING + 继续跑」改为「**情况 B 直接报错**」；补 env 覆盖不会误报的核实 |
| v2.2 | 2026-09-26 06:16:29 | 小欧 | 北京老陈追问「settings2 前端还在用 `PUT /config` 吗」「只是测试用的就一起删，不能为测试留垃圾」→ **改第十一章结论**：settings2 写配置全走 `PUT /settings`，5 个功能逐项已迁移 → `PUT /config` 写端点为纯死代码可整条删；依赖测试为 3 个文件不是 2 个，3 个死测试随端点删、1 个并发锁护栏须迁移 |
| v2.1 | 2026-09-26 06:10:39 | 小欧 | 北京老陈追问「旧通道 `config_helpers.py:414` 能否废弃、还有谁在用」→ 三遍核实后**新增第十一章**：含完整调用链、引用核实证据、3 处阻碍、分步废弃方案 |
| v2.0 | 2026-09-26 05:35:00 | 小欧 | **全文重排**：删自我说明；改「速查表→11章自包含详情→附录」；修清 7 处数字/引用矛盾（原 v1.2 增量修改残留：9章/11章、4个E2E/6个E2E、顺序自相矛盾等）；每章补「涉及文件」「验收」使其自包含 |
| v1.2 | 2026-09-26 05:35:00 | 小欧 | 补充两项链路缺口：第十章「查看明文」、第十章「key 正确性检测」；正文由 9 章扩为 11 章 |
| v1.1 | 2026-09-26 01:01:38 | 小欧 | 按要求重排为「一问题一章」；总览类内容收口不占章号 |
| v1.0 | 2026-09-26 01:01:38 | 小欧 | 初稿：9 个问题定位 + 实施修改方法 |

---

# 一、速查表

| 章 | 问题 / 功能 | 级别 | 一句话 |
|---|------------|------|-------|
| 一 | api_key 未去空格即落盘 | 中 | 同一个保存函数里，base_url 去了空格、api_key 没去 |
| 二 | 跨 provider 场景下 key 空白却偷用别的 provider 的 key | **高** | 用 A 的模型发 B 的 key，**无日志**；同 provider 场景是正常的，不算问题 |
| 三 | 后端收到空字符串直接清空 key | **高** | 绕过界面传个空值，密钥被擦掉还返回"成功" |
| 四 | clear 请求覆盖刚输入的新 key | 低 | 后端缺互斥校验，会静默丢弃（界面触发不了） |
| 五 | 配置文件明文 key 全屏可复制 | **不改** | **北京老陈裁定：明文就明文**（本机查看合理）。代价：第九章变成必做前置 |
| 六 | secret 项写入路径无三态保护 | 中 | 潜伏。当前无 secret 项，一旦启用即静默清空 |
| 七 | 掩码契约三套 + 类型撒谎 + 死契约 | 低 | 前端 TS 类型说 suffix 必填，后端不返回该键 |
| 八 | base_url 可空 + 静默兜底默认地址 | 低 | **方案已定（v2.7）：必填 + 空则红色报错 + 拒绝保存 + 去兜底** |
| 九 | 全路由零鉴权（多人共用） | **高·必做** | `--host 0.0.0.0` **必须保留**（多机部署）；要加的是 **token 鉴权**。**第五章明文可接受以此为前提** |
| 十 | **新增功能**：key 正确性检测 | — | 现在没有测试连接；本方案加，且要分四档错误 |
| 十一 | 旧通道 `PUT /config` 整条可删 | 中 | **第二个能清空密钥的入口**（第三章漏修）；settings2 已全迁移，是纯死代码 |
| 十二 | **新增功能**：key 显示前4+后4位 + 眼睛切换明文 | — | 现在只显示末4位看不出开头；**明文接口须待第九章落地后上线** |

**建议实施批次**：
- **必做**：第九章 **token 鉴权**（多人共用场景下任何人任何程序均可调任何接口）——因第五章保留明文，此项为其前提；**注意 `--host 0.0.0.0` 必须保留，不可收窄**（多机部署）
- **P0**：二、三、九
- **P1**：一、四、八
- **P2**：六、七、十二
- **新增功能**：第十章（key 检测，建议先做）、第十二章（key 显示与眼睛切换，须待第九章鉴权后）

> 第一 ~ 九章、十二章均为**逐条三遍核实**确认的问题（读原始代码行 → 追完整调用链 → 交叉比对同类实现）。第十章、十二章为新增功能，现状亦已核实。

---

# 二、问题详情

## 第一章 api_key 未去空格即落盘

**级别**：中

### 1.1

同一个 `doSave` 函数内，**base_url 去了空格、api_key 没去**——是遗漏，不是设计：

| 环节 | 代码 | 去空格 |
|------|------|-------|
| 前端落盘 | `ProviderConfig.tsx:108` `patch.api_key = apiKey` | **否**（`trim()` 只用于该行条件判断） |
| 同函数对照 | `ProviderConfig.tsx:109` `patch.base_url = baseUrl.trim()` | **是** |
| 后端落盘 | `model_service.py:431` `if k in key_map and v is not None` | **否** |
| 旧通道落盘 | `config_helpers.py:414` `config_data[...]['api_key'] = api_key.strip()` | **是** |
| 会话覆盖消费 | `resolver.py:193` `(...).strip() or None` | **是** |
| 全局默认消费 | 单例 `self.api_key` ← `config.py:112` / YAML 原值 | **否** |
| SDK 组 header | `client_sdk.py:279` → `adapters/base.py:17` `Bearer {api_key}` | **否**（原样透传） |

### 1.2

粘贴带首尾空格或换行的 key → 界面提示"保存成功" → 实际调用 401。**界面无任何提示指向空格**，用户反复重输同一串 key 仍失败。

且**行为分叉**：同一份配置、同一串 key，走会话覆盖路径会被 strip 因而可用，走全局默认路径就 401。

### 1.3

去空格必须放**后端**（最后防线，所有调用方必经），前端同步做仅为体验一致。

- **后端（主）** `model_service.py` `update_provider_config`：`key_map` 循环内对 `api_key` 特殊处理，写入前 `str(v).strip()`。使落盘值与旧通道 `config_helpers.py:414` 对齐，消除双通道不一致。
- **前端（辅）** `ProviderConfig.tsx:108`：`patch.api_key = apiKey` → `patch.api_key = apiKey.trim()`，与 `:109` 写法统一。
- **消费端兜底（建议一并做）** `client_sdk.py:279`：组 header 前对 `api_key` 再做一次 `.strip()`，使**已落盘的历史脏 key 也能工作**，避免要求用户手工重录。

### 1.4

`backend/app/services/model/model_service.py`、`backend/app/llm/client_sdk.py`、`frontend/src/features/settings2/components/ProviderConfig.tsx`

### 1.5

- 写入带首尾空格与换行的 key → 保存 → 实际 LLM 调用成功
- 已中招的历史脏 key（YAML 中带空格）→ 不重录也能调用成功

---

## 第二章 跨 provider 场景下 key 空白，却偷用别的 provider 的 key ★最高优先

**级别**：**高**

> **v2.3 判定修正**：v2.0/v2.2 把本章描述为"空白 key 静默回落到全局默认 key"，**遗漏了一个关键区分**——`_pv_key=None` 有两种**性质相反**的情况，其中一种是正常的。北京老陈指出：「key 空白的就是空白的，不能瞎改，调用不通的时候直接报错就是了」。本章据其修正：**该报错的报错，不该报错的必须保留原行为**。

### 2.1 病根

两处代码叠加：

```
resolver.py:189   if _ov.provider and _ov.provider != ai_service.llm_model.provider:
                  │  ← 只有【跨 provider】才进这个分支去查目标 provider 的配置
resolver.py:193       _pv_key = (_pv_cfg.get("api_key") or "").strip() or None
                                                    └─ 空白 key → "" → or None → None
base_service.py:208   api_key=api_key or self.api_key
                                           └─ None → 沿用单例 self.api_key（全局默认 provider 的 key）
```

### 2.2 关键区分：`_pv_key=None` 的两种情况（v2.3 补，原文遗漏）

| 情况 | 触发条件 | 代码路径 | 现在的行为 | 判定 |
|------|---------|---------|-----------|------|
| **A 同 provider** | 会话只换了**模型名**，provider 未变（如 sensenova 的 A 模型 → B 模型） | **不进入** `resolver.py:189` 的 if，`_pv_key` 保持 None → `base_service.py:208` 用单例 key | 用单例 key | **正常，必须保留**。同一个 provider 本来就该用同一个 key |
| **B 跨 provider** | 会话切到**另一个** provider（全局默认 sensenova → 会话选 glm），而该 provider 的 key 是空白 | 进入 if → 查 glm 配置 → 空白 → `_pv_key=None` → `base_service.py:208` 用单例 key | **偷用 sensenova 的 key** | **瞎改，必须报错** |

→ **同一个 `None`，在 A 里是设计如此，在 B 里是错误。** 原文档把两者混为一谈，是表述错误。

### 2.3 后果（仅情况 B）

会话切到 provider B（key 空白）时：

- 请求 **B 的模型名 + B 的 api_base**，但 `Authorization` 头带的是 **A（全局默认 provider）的 key**
- **多数情况**：401（key 与端点不匹配）
- **最坏情况**：若 A 与 B 指向**同一个网关且 key 通用** → **请求成功**。用户以为配好了，实际一直在用 A 的 key，**完全不知情**
- **全程无一条日志**——本批最难查的一个

**对照**：`model_service.py:537`（拉远程模型列表）`os.environ.get(...) or str(p.get("api_key") or "")` 传空字符串，**不回落**。→ 两条链路对"空 key"处理相反。

### 2.4 改法：情况 B 直接报错，情况 A 原样不动

**原则**（北京老陈）：**key 空白就是空白，不能瞎改；调用不通就明确报错，不要偷偷换一个 key 顶替。**

- **主修 `resolver.py:193-193`**：在**跨 provider 分支内**（即 `:189` 的 if 体内）区分"查到了配置但 key 空白"与"配置本身查不到"
  - `_pv_cfg` 取到、但 `_pv_key` strip 后为空 → **抛错**（复用现有 `except` 之外的中断路径），文案明确指向配置项：「会话切换到 provider **B**，但该 provider 未配置 api_key，请先在设置页填写，或设置环境变量 B_API_KEY」
  - **必须带上 provider 名**（来自 `_ov.provider`），让用户知道该去配哪一个
  - `_pv_cfg` **取不到**（`:197` except 分支）→ 维持现状走 `_default_snapshot`（`:203-207`），此处已有 WARNING 日志，**不动**
- **不动情况 A**：`resolver.py:189` 之外的所有 fallback **保持原样**。同 provider 用单例 key 是正确行为，严禁一并"修掉"
- **补 env 误报核实（已确认安全）**：`get_service_config`（`resolver.py:191`）→ `get_ai_config`（返回 `self._config["ai"]`）→ `AIConfig` 已执行 `config.py:102-112` `_apply_env_overrides`，env 提供的 key **已写入配置**
  → **用环境变量提供 key 的 provider 不会被误判为"未配置"**，改报错是安全的
- **一致性** `model_service.py:537`：该路径传空串**不回落**（行为正确），无需改动；但需在注释中写明"此处不回落，与 resolver 情况 B 报错共同构成 'key 空白即失败' 的一致语义"，避免后人误加 fallback
- **配套（DRY）** `lifecycle/validation.py:52` 已有 `api_key.strip() == ""` 判定分支，可抽为公用判定函数供 resolver 复用，避免第三次出现不一致

### 2.5 报错如何送达前端（已定：编排层专属 catch + 补中文标签）

**背景**：`resolve_session_client` 由 `stream_orchestrator.py:410` 调用，其异常会被 `:536` 的 `except Exception` 兜住，统一包成 `router_error`（文案「路由异常: {原始异常}」）。`stream_orchestrator.py:162-169` 的 [70] 修订记录已批评这种做法**"泄露内部异常文案"**。

若只在 `resolver.py` 抛错，用户会看到「路由异常: xxx」，与 ConnectionScope 等系统错误混在一起，**分不出是配置问题还是系统问题**。故采用专属 catch：

| 层 | 改动 |
|----|------|
| `resolver.py` | 抛**专属异常类型**（如 `ProviderKeyMissingError`），消息带 provider 名，不混进通用 `Exception` |
| `stream_orchestrator.py:410` 外 | 单独 catch 该类型 → `create_error_response(error_type="config_error", ...)`（`:185` 已 import），人话文案，**不让它掉进 `:536` 的 `router_error` 兜底** |
| 前端 `ErrorDetail.tsx` | **必须补中文标签**：`ERROR_TYPE_LABELS` 现无 `config_error`（现有 21 项：`empty_response`/`timeout`/`network_error`/`server_error`/`rate_limit`/`authentication_error`/`authorization_error`/`validation_error`/`not_found`/`internal_error`/`quota_exceeded`/`idle_timeout`/`cancelled`/`client`/`server`/`unknown`/`circuit_open`/`llm_error`/`unknown_response`/`agent_operation_error` 等），`formatErrorType` 逻辑是 `LABELS[type] \|\| type`，**不补则前端直接显示英文 `config_error`**；同时在 `ERROR_COLORS_MAP` 补配色（现有分组：`security_error`/`agent`/`network`/`validation`/`file_system`/`security`/`unknown`/`default`） |
| 前端展示形态 | 走 `PipelineRenderer.tsx:391` 的**对话流错误块**（非 toast），与现有错误一致 |

**文案风格对齐** `stream_orchestrator.py:240-244` 已有先例（`f"配置验证失败: {...}"`），不新造一套。

**为什么不在首个 401 响应处报错**：401 无法区分"key 错"与"其它 401"（如网关鉴权、账号欠费），且此时已发出请求、浪费一次配额。`resolver` 阶段抛错可**早失败**且能直指配置项。

### 2.6 涉及文件

`backend/app/services/model/resolver.py`（主改）、`backend/app/services/model/model_service.py`（仅加注释）、`backend/app/services/lifecycle/validation.py`（抽公用判定，可选）

### 2.7 验收

- **情况 B**：会话切到 key 空白的 provider → **明确报错**，文案含 provider 名与"未配置 api_key"；**日志无"用别的 key"的痕迹**
- **报错呈现**：前端对话流显示**中文标签 + 人话文案**（非英文 `router_error`、非"路由异常"）
- **情况 B 变体**：provider 的 key 配成**纯空格** → 同样报错（不静默用别的 key）
- **情况 B env 变体**：provider 的 key 由 `B_API_KEY` 环境变量提供 → **正常放行**，不误报
- **情况 A 回归**：同 provider 只换模型名 → **仍正常使用**（防"修过头"，这是本章最易引入的退化）
- 拉远程模型列表路径行为**不变**

---

## 第三章 后端收到空字符串直接清空 key

**级别**：**高**

### 3.1

- `model_service.py:431` `if k in key_map and v is not None` —— **空字符串不是 None**，判定通过，`api_key: ""` 被写进配置
- 校验链不拦：`config_helpers.py:276-277` `if 'api_key' not in provider_config` —— **只校验字段存在**，空字符串也算存在，照过
- 前端有保护（`ProviderConfig.tsx:108` 拦截空值不发送），**后端完全裸奔**

### 3.2

任何非界面途径传入空字符串即**擦除密钥**：`PUT /api/v1/config` 旧通道、脚本 / curl / 将来编写的 E2E。接口返回 `{"ok": true}`，**调用方以为设置成功**。

"留空=保持原值"这个语义**只存在于前端**，后端无对应实现——契约保护放错了层。

### 3.3

三态语义在后端做实，且**成为唯一权威**（前端保护降级为体验优化）。

| 传入 | 语义 | 行为 |
|------|------|------|
| 字段不出现 / `null` | 不修改 | 跳过，不写 |
| `""` 或纯空白 | **不修改**（与前端"留空=保持原值"对齐） | 跳过，不写 |
| 非空字符串 | 设置 | `.strip()` 后写入 |
| `clear: true` | 清空 | 显式写 `""`（唯一允许清空的途径） |

- **主修** `model_service.py` `update_provider_config`：`key_map` 循环前对 `api_key` 做三态判定——空/空白值 `continue` 跳过（不进入 `node`），仅非空值赋值。`clear` 分支（`:437`）保持显式清空，但需与设置分支互斥（见第四章）。
- **配套** `config_helpers.py:276-277`：该校验仅在"字段完全缺失"时报警。由于界面上 `ProviderConfig.tsx:201` 明确显示"未配置，留空=保持原值"，说明**允许未配置状态**，故**空值合法、不应报警**。此处维持现状，仅补注释说明，避免后人误加校验导致无法保存未配置状态。
- **契约文档化** `model_routes.py:76-81`：在 `ProviderConfigUpdate` 字段说明中写明 `api_key` 三态（None/"" = 不修改），避免调用方误用。

### 3.4

`backend/app/services/model/model_service.py`、`backend/app/api/v1/model_routes.py`

### 3.5

- 对已配 key 的 provider 提交 `api_key: ""` → 配置**未被清空**，返回 ok
- 提交 `clear: true` → key 被清空
- 提交非空 key → 正常写入

---

## 第四章 clear 请求覆盖刚输入的新 key

**级别**：低

### 4.1

`model_service.py:437-438` 排在 api_key 赋值**之后**且**无条件覆盖**：

```python
for k, v in fields.items():
    if k in key_map and v is not None:
        node[key_map[k]] = v          # ← 先写用户输入的新 key
...
if fields.get("clear") is True:
    node["api_key"] = ""             # ← 无条件清空，静默丢弃上面的新 key
```

无冲突校验、无报错。

### 4.2

当前**界面流程触发不了**：清空走 `ModelActions.tsx` 独立按钮 → `onClearApiKey`（有 `configured && !envManaged` 门控），与 `doSave` 是**两个独立请求**，不会同时携带 api_key 与 clear。

→ 定性为**后端缺防御**，非现行界面 bug。但后端 API 是公开的，不能依赖前端不同时发。

### 4.3

- **主修** `model_service.py`：增加互斥校验——`clear is True` 且 `api_key` 为非空字符串 → 抛 `ValueError`（由现有 `handle_api_errors` 转 400），文案明确"clear=true 与 api_key 设置值不能同时提交"，**不静默丢弃任何一方**。
- **测试补齐**：`test_model_service_tdd.py` 的 `TestUpdateProviderClear08`（`:148-153`）追加两用例：① 同时传 `api_key`+`clear` → 报错且配置未被修改；② 只传 `clear` → 正常清空。

### 4.4

`backend/app/services/model/model_service.py`、`backend/tests/test_model_service_tdd.py`

### 4.5

- 同传 `api_key`+`clear=true` → 返回 400，配置未变
- 只传 `clear=true` → 正常清空

---

## 第五章 配置文件明文 key 全屏显示且可一键复制 —— **裁定：不改**

**级别**：~~高~~ → **不改（业务决定）** | **裁定人**：北京老陈 2026-09-26 07:12

> **裁定原文**：「设置 → 关于 → 点"配置文件" → 弹窗打开 config.yaml 全文——这个地方看到全文没有问题啊，明文就明文吧。」
>
> 本机查看自己的配置文件，明文是合理需求（与用 VSCode 打开 config.yaml 无异）。**本章不实施修改**，原"逐行脱敏"方案作废。下文保留核实结论与**风险边界**，供后续追溯。

### 5.1 现状（保留核实记录）

- `config_service.py:330-334`：`content = f.read()` —— 整个 config.yaml 明文原样返回，零脱敏
- `config_routes.py:139-142` → `GET /api/v1/config/read` 直接透出
- `AboutFiles.tsx:92-93` 渲染全文，`:124-129` `copyAll` 写剪贴板
- 行为已被测试固化：`settings2-about-files.test.tsx:47` 测试名"能正常查看配置文件内容, 调 readConfigFile 并在 Modal 展示全文"

### 5.2 风险边界（不改的代价，必须记录）

**本机使用无风险**——用户看自己的配置，属正常需求。

**风险来自"外部可达"，不属于本章，属第九章范围**：

```
① 界面显示明文（已裁定接受）
② GET /api/v1/config/read 无身份验证      ← 第九章
③ 监听 0.0.0.0，同局域网任意设备可达      ← 第九章
```

②+③ 使风险从"自己能看到"放大为**"局域网任何设备一个 curl 就能拿走全部密钥"**，且**无需打开界面**。

### 5.3 绑定前置条件（因本章不改而新增）

**部署事实**：局域网多机共用 + 多人/半公开（v2.8 确认）。

**风险来源不是"本机查看"，而是"多人可达"**：

```
① 界面显示明文（已裁定接受）
② GET /api/v1/config/read 无身份验证      ← 第九章
③ 服务绑 0.0.0.0，多台客户端可访问        ← 第九章（多机部署必需，不可改）
```

在**单人本机**场景下，②③ 无害（只有自己能访问）。
但在**多人共用**场景下，②③ 使任何客户端 `curl` 一次即取走**全部** provider 明文密钥。

**→ 本章"不改"成立的前提是第九章 token 鉴权落地**（见 9.5 逻辑链）：
只有持 token 者能调接口后，"明文可接受"的前提恢复成立。

> **v2.8 修正**：v2.4 曾把本章前提写为"第九章第一步（监听地址收窄）"，该前提**错误**——多机部署下不能收窄监听地址。正确前提是**第九章的 token 鉴权**。

### 5.4 连带影响

- 若未来仍想要"更安全的单 key 查看"（二次确认 / 自动隐藏 / 审计日志），需重新立项，不在本报告范围

### 5.5 涉及文件

无（不修改任何文件）

### 5.6 验收

- `settings2-about-files.test.tsx:47` 保持原样，**测试全绿**
- `GET /config/read` 仍返回明文全文（预期行为）
- 确认第九章第一步已落地（`netstat` 不再监听 `0.0.0.0:8000`）

---

## 第六章 secret 设置项写入无「三态」保护（潜伏）

**级别**：中（潜伏未触发） | **方案**：**B 已定**（北京老陈 2026-09-26 07:20）

### 6.1 病根（现状：读路径已掩码，写路径未实现）

| 位置 | 事实 |
|------|------|
| `settings_registry.py:116` | `_item(... secret: bool = False ...)` 有该字段 |
| `settings_registry.py` 约 60 处 `_item()` 调用 | **零个传 `secret=True`** |
| `settings_service.py:145-146` | **只在读路径**调 `mask_secret_value` 掩码 |
| `settings_service.py:218-276` `_validate_value` | **无 secret 分支**——不校验类型、不处理空串 |
| `settings_service.py:296-311` `_to_stored_value` | **无 secret 分支**——value 原样转存储值 |
| `SettingRow.tsx:120,129` | 前端会提交 `''`（留空）与 `{clear:true}`（清空） |

### 6.2 什么是「三态」（大白话解释）

一个密钥输入框，用户有**三种不同意图**，但代码只用一种方式表达：

| 状态 | 用户做什么 | 他心里的意思 | 代码表示 |
|------|-----------|------------|---------|
| **状态1** | 什么都不填，直接点保存 | **"别动它"** | `''`（空字符串）或**不传该字段** |
| **状态2** | 输入 `sk-新值` | **"换成这个"** | `'sk-新值'`（非空字符串） |
| **状态3** | 点「清空」按钮 | **"删掉"** | `{clear: true}` |

**「三态」= 状态1 + 状态2 + 状态3。**

**问题根源：空字符串 `''` 被赋予了两种相反的含义**

```
用户心里：  我留空 = "这个字段我不想改"   → 状态1
代码理解：  收到空字符串 = "把值设成空"  → 状态3
```

**同一个 `''`，两种完全相反的意思。** 修"三态"的本质就是**把这两种意图拆开表达**：

| 意图 | 正确表示 |
|------|---------|
| 不修改 | **不传该字段**（或传 `null`） |
| 设置新值 | 传非空字符串 |
| 清空 | 传 `clear: true` |

### 6.3 两个具体坏例子

**例子 1：留空 = 擦掉**

```
某 provider 已配 key: "sk-真密钥"
用户打开设置页，只想改超时，密码框没碰，直接点保存
  → 前端送 api_key: ''        （含义："我没改它"）
  → 后端当普通值写入空串      （理解成"把 key 设成空"）  ← 反了
  → key 被擦掉，且界面显示"保存成功"
```

**例子 2：点清空 = 配置文件结构损坏**

```
用户点「清空」按钮
  → 前端送 clear: true              （一个 dict 对象）
  → `_to_stored_value` 当普通值处理
  → YAML 里写入：api_key: {clear: true}    ← 配置文件结构损坏
```

### 6.4 为什么叫「潜伏」

**因为现在还没有任何一个设置项开了 secret**：

```
settings_registry.py 约 60 处 _item() 调用 → 零个传 secret=True（已逐条 grep 确认）
```

所以**现在不会出错**。但这是个埋着的坑——以后有人给某个设置项加一行 `secret=True`，上面两个 bug **立刻生效**，且界面会自动显示"留空=保持原值"（`SettingRow.tsx` 走 secret 分支），**用户会被误导**。

**与第三章的关系**：同一个坑，已中一次、未中第二次。

| | 位置 | 状态 |
|---|------|------|
| 第三章 | `model_service.py:431` | **已实现**（key 就在用）→ 现行 bug，必须修 |
| 第六章 | `settings_service.py` 写路径 | **只预留字段、未实现** → 潜伏，尚未炸 |

修完第三章，key 写入路径就有三态了。第六章是同一件事在另一套系统里还没做。

### 6.5 改法（**已定方案 B**，北京老陈 2026-09-26 07:20）

**方案 B：secret 项统一收口到 `model_service` provider 通道**（复用第三章已实现的三态），**不在 settings 通用通道另开一套**。

- **主线** `settings_service.py` 写路径：`_validate_value`（`:218-276`）与 `_to_stored_value`（`:296-311`）**增加显式拒绝**——遇 `secret=True` 的项**直接报"该敏感项不支持经 /settings 写入，请用 provider 通道"**，不做隐式兜底
- **provider 通道** `model_service.py` `update_provider_config`：按第三章三态实现，作为 secret 写入的**唯一权威**
- **防半吊子（关键）** `settings_registry.py`：在 `get_item` / 注册收口处加**自检**——若某项 `secret=True` 而其写入路径未指向 provider 通道，**开发期即报错**，杜绝"开了 secret 却没实现写保护"
- **前端** `SettingRow.tsx`：secret 项的"清空"按钮改为调 `modelApi.updateProvider({clear:true})`（provider 通道），而非 `settingsApi.updateSettings`
- **测试补齐**：现有 2 个死测试**重写**为真断言——构造 secret 项，验证 ①读被掩码 ②写空串不覆盖原值 ③写 clear 清空生效
  > `test_settings_service_tdd.py:66-72` 还 mock 了 `_raw_config` 却**从未调用被测函数** `_item_data`，属无效 mock，一并清理。

### 6.6 涉及文件

| 文件 | 改动 |
|------|------|
| `backend/app/services/settings/settings_service.py` | 写路径遇 secret 项显式拒绝，指引 provider 通道 |
| `backend/app/services/settings/settings_registry.py` | 加 secret 自检（secret 项写入路径必须已实现） |
| `backend/app/services/model/model_service.py` | provider 通道三态（与第三章同一处改动，**不重复实现**） |
| `frontend/.../settings2/components/SettingRow.tsx` | secret 项清空改走 `modelApi.updateProvider` |
| `backend/tests/test_settings_registry_tdd.py` | 重写死测试为真断言 |
| `backend/tests/test_settings_service_tdd.py` | 重写死测试 + 清理无效 mock |

### 6.7 验收

- 构造 secret 项：读被掩码 ✓
- 对该 secret 项写空串 → **不覆盖原值**（不擦掉）
- 对该 secret 项写 `clear: true` → 清空生效，且 **YAML 中写入的是空串而非 dict 对象**
- 注册 secret 项但写入路径未实现 → **开发期报错**
- `pytest` 全量全过（含第三章的 E2E-KEY-02）

---

## 第七章 掩码契约三套并存 + 类型撒谎 + 死契约

**级别**：低

### 7.1

| # | 实现 | 返回形状 | 使用方 |
|---|------|---------|--------|
| 1 | `config_helpers.py:611-617` `mask_secret_value` | `{"configured": bool, "suffix": str?}` | `/models`、`/settings` |
| 2 | `config_service.py:153-157` `_mask_api_key` | `str`（`"****"+末4位`） | `/config/full` |
| 3 | `config_service.py:182-183` | `bool`（`api_key_configured`） | `/config` |

**问题 A：前端类型撒谎**
`model.api.ts:34` 声明 `api_key: { configured: boolean; suffix: string }` —— `suffix` **必填**；但后端 `config_helpers.py:615` 未配置时返回 `{"configured": False}`，**根本没有 suffix 键**。类型与实际不符。

**问题 B：第 2 套是死契约**
`/config/full` 的唯一前端消费方 `useModelLayer.ts:46-48` **只取 `cfg.current_model_ref`，从不读 api_key**。`config_service.py:154-155` 注释宣称的"前端 slice(-4) 兼容"**无任何对应代码**。纯维护负担。

### 7.2

三套形状并存，后续新增消费方易取错形状；前端类型与后端实际不符，埋下运行时 `undefined` 隐患；死契约让人误以为"前端在用"，改动时误判影响面。

### 7.3

收敛到唯一权威 `mask_secret_value`，删除无消费者的死契约。

- **修类型撒谎** `config_helpers.py:615`：未配置分支返回 `{"configured": False, "suffix": ""}`，**补齐 suffix 键**，使返回形状恒定（与 `:617` 一致）。前端 `model.api.ts:34` 类型即变诚实。
  > **与第十二章的合并实施（v3.0 审计发现）**：第十二章要加 `prefix` 字段，最终契约为**三键恒定** `{configured, prefix, suffix}`。**本章的"补 suffix 键"是其中的子集，两处改动落在同一函数（`config_helpers.py:611-617`），必须合并一次做完，不可分两次改同一函数。** 实施顺序：先做第十二章的三键改造，本章随之自动满足；单独做本章则会被第十二章覆盖。
  > 完整契约（以第十二章 12.5 为准）：已配且 `len>=8` → `{configured:true, prefix:s[:4], suffix:s[-4:]}`；`4<=len<8` → `{configured:true, prefix:"", suffix:s[-4:]}`；未配 → `{configured:false, prefix:"", suffix:""}`。
- **删死契约** `config_service.py:153-157`：实施前先确认 `config.api.ts:32,44` 声明的 `api_key: string` 字段**是否真被消费**（本次核实未见消费方，实施时须复核）。确认无消费后删除该函数，`/config/full` 相应字段改用第 1 套 `mask_secret_value`（形状变为对象）。
- **第 3 套保留**：它回答"是否已配置"这个**布尔问题**，与掩码语义不同，非重复。保留，仅在 `config_schemas.py:43` 字段说明中写清与前两者的区别。

### 7.4

`backend/app/services/model/config_helpers.py`、`backend/app/services/model/config_service.py`、`frontend/src/services/api/model.api.ts`

### 7.5

- 未配置 key 时，响应含 `suffix: ""` 键，形状恒定（**注**：若第十二章的三键改造已实施，此项自动满足，且契约以 12.5 为准）
- `/config/full` 不再返回 `"****xxx"` 字符串形态
- `npm run test` 中依赖 api_key 形状的用例全过

---

## 第八章 base_url 可空 + 静默兜底默认地址 —— **方案已定：必填 + 空即报错**

**级别**：~~中~~ → **低**（v2.7 降级，见 8.4 纠正） | **方案**：**已定**（北京老陈 2026-09-26 07:47）

> **v2.7 推翻重写**。原方案主张「留空=恢复默认地址，保留兜底 + 加提示」，**与北京老陈方案根本对立**。北京老陈主张：**URL 不能空 → 空是错误状态（红色提示）→ 保存就保存**。本章据其重写。

### 8.1 病根

**`base_url` 与同表单其它字段的留空语义相反**（`ProviderConfig.tsx:106-112`）：

| 字段 | 代码 | 留空语义 |
|------|------|---------|
| `api_key` | `:108` `if (apiKey.trim() !== '')` | **不修改**（保持原值） |
| `label` | `:110` `if (label.trim() !== '')` | **不修改**（保持原值） |
| `base_url` | `:109` `patch.base_url = baseUrl.trim()` | **清空**（无条件发送空串） |

**落盘确认**：`model_service.py:431` 空串通过（`'' is not None`）→ `api_base: ""` 写入配置（`:417` `key_map` 映射 `base_url → api_base`）。

**根子上的问题**：`base_url` **被设计成可空**，但它其实是 provider 的必要配置——没地址就无法调用。

### 8.2 现状：兜底机制是半成品

`client_sdk.py:304-315` `_default_base_url` 三段逻辑：

```
1. 读 get_config()["llm"]["provider_urls"][provider]   ← 自定义覆盖（可为空）
2. 回落 self._DEFAULT_URLS.get(provider, "")
```

`_DEFAULT_URLS` 的**全部内容**（`:311-315`）：

```python
_DEFAULT_URLS = {
    "deepseek": "https://api.deepseek.com",
    "qwen":     "https://dashscope.aliyuncs.com/compatible-mode",
    "ollama":   "http://localhost:11434",
}
```

**只有 3 个 provider 有地址**，其余（sensenova / glm / opencodeZen 等）**得空串**。

且 `client_sdk.py:268` 注释明写：

> "留空不填会向 httpx 传空 base_url（指向不存在的地址），**当前工作路径均不触发**"

→ **"默认地址"这套机制本身是半成品**：既不完整（只覆盖 3 家），也未被实际触发。**把"留空"解释为"恢复默认"是一个假便利。**

### 8.3 后果（准确版）

| provider | 清空 `api_base` 后实际行为 | 是否静默 |
|----------|--------------------------|---------|
| **ollama** | 变 `http://localhost:11434` | ⚠️ **静默**。远程 ollama 配的是 `http://192.168.x.x:11434`，清空后打向**本机**，请求自己的机器 |
| **deepseek / qwen** | 变硬编码官方地址 | ⚠️ **静默**。可能绕过用户想走的中转/代理，直连官方计费 |
| **sensenova / glm / opencodeZen** | 得**空串** → httpx 请求失败 | ✅ **会报错**，非静默 |

> 与第二章同一原则：**空就是空，不要瞎兜底。**

### 8.4 ⚠️ 对 v2.6 两处不实结论的纠正（小欧 2026-09-26）

**纠正一**：v2.6 写"清空后**静默改走官方直连**"，**表述过宽**。经核实（8.2/8.3），静默**仅 deepseek/qwen/ollama 三家**成立；sensenova/glm/opencodeZen 得空串会 fail-fast。且注释明写该路径"当前工作路径均不触发"。

**纠正二**：基于纠正一，危害范围大幅缩小，**级别由「中」降为「低」**。真正剩下的缺陷是**设计层**——`base_url` 被设计为可空，而它本应必填。

### 8.5 改法（**已定方案**，北京老陈 07:47）

**原则**：URL 是 provider 的必要配置，**不允许为空**；空即错误状态，明确报错，不做兜底。

**1. 加 Provider 时 base_url 必填**
- `ModelModals.tsx`（添加 Provider 弹窗）：base_url 输入框加必填校验，未填**不允许提交**
- 后端 `model_service.py` `add_provider`（`:396-409`）同步校验：`api_base` 为空 → 400，明确文案"api_base 必填"

**2. 回显：空 = 红色错误提示**
- `ProviderConfig.tsx`：检测 `config.api_base` 为空时，在输入框下方（复用 `:198-202` 的 `EXTRA_STYLE` 辅助文案行）显示**红色警示**：
  ```
  [ 空                                    ]
  ⚠️ URL 为空，此 Provider 无法调用（错误状态）
  ```
- **原样回显**——不做任何改写，用户填什么就显示什么（与 api_key 只显末 4 位不同，base_url 本身不是密钥，无需打码）

**3. 保存：空就拒绝**
- 前端 `doSave`：base_url 为空时**不提交**，就地红字提示
- 后端 `update_provider_config`（`:412-442`）：收到 `base_url` 为空/纯空白 → **拒绝并报错**（400），不再"清空后照存"
  > 注意：与 `api_key` 的三态语义**方向相反**——`api_key` 空=不修改（第三章），`base_url` 空=**错误**（本章）。两者差异源于：`api_key` 允许"先建后填"，`base_url` 不可。

**4. 去掉兜底**
- `client_sdk.py:269` 的 `or self._default_base_url(llm_model.provider or "openai")` **删除**，改为：`_base_url` 为空 → **构造即报错**，文案"provider X 的 URL 为空，请到设置页填写"
- `_default_base_url`（`:304-315`）与 `_DEFAULT_URLS`（`:311-315`）**一并删除**（删后无人调用；`provider_urls` 自定义覆盖能力如有需要，应显式配置而非靠兜底）
- `resolver.py:211` 的 `... or ai_service.llm_model.api_base`（回落到全局默认地址）**同样删除**，避免跨 provider 时用别的 provider 的地址
  > **与第二章的同处改动（v3.0 审计补充）**：该行**同时被第八章与第二章要求删除**——第八章的理由是"不该用别的 provider 的地址"；第二章改为跨 provider 且 key 空白即报错后，这条 `or` 回落**同样不应再兜底**。两处**同一行、同一目的**，附录一已合并为一条（`resolver.py` 关联章标「二、八」），**实施时一次改掉，不可分两次改同一行**。

**5. 兼容历史坏配置**
- 若已有 provider 的 `api_base` 为空（手改坏 / 依赖过 ollama 兜底）→ 回显**红色提示**引导补填；调用时**明确报错**，不再静默指向 localhost 或官方地址
- 上线前需**先扫一遍现网配置**列出 URL 为空的 provider，并逐一确认其真实地址

### 8.6 涉及文件

| 文件 | 改动 |
|------|------|
| `backend/app/llm/client_sdk.py` | 删兜底（`:269`）+ 删 `_default_base_url`（`:304-315`）与 `_DEFAULT_URLS`；空 URL 构造即报错 |
| `backend/app/services/model/resolver.py` | `:211` 删 `or` 回落，跨 provider 不用全局默认地址 |
| `backend/app/services/model/model_service.py` | `add_provider` 必填校验；`update_provider_config` 拒空 base_url |
| `frontend/.../settings2/components/ModelModals.tsx` | 添加 Provider 弹窗 base_url 必填校验 |
| `frontend/.../settings2/components/ProviderConfig.tsx` | 回显空 URL 红色提示；保存前校验拒绝提交 |

### 8.7 验收

- 添加 Provider 时不填 URL → **不允许提交**（前端拦截 + 后端 400）
- 某 provider 的 URL 为空 → 回显**红色警示**行，明确"无法调用"
- 保存时 URL 为空 → **拒绝保存并报错**，配置**不被清空**
- URL 为空时调用 LLM → **明确报错**「provider X 的 URL 为空，请到设置页填写」，**不再静默**指向 localhost / 官方地址
- ollama 远程地址场景：清空 URL 后调用 → **报错**，不再打向本机
- `_default_base_url` / `_DEFAULT_URLS` 全项目 grep → 零残留

---

## 第九章 全路由零鉴权（多人共用场景下的必做项）

**级别**：**高** | **状态**：**必做** | **部署前提**：**局域网多机共用 + 多人/半公开**（北京老陈 2026-09-26 08:01 确认）

> **v2.8 整章推翻重写，纠正小欧重大误判。**
>
> **误判**：v2.4~v2.7 建议「把 `--host 0.0.0.0` 改为 `127.0.0.1`」，理由是"同机使用不受影响"。该建议**基于"后端只在本机使用"的未确认假设**。北京老陈实际部署为**后端在 B 机器、局域网多机客户端访问**——若照此修改，**其他机器将全部无法连接，服务直接作废**。该建议**已撤回**。
>
> **纠正**：`--host 0.0.0.0` **必须保留**（多机访问的硬性前提）。真正的问题是**零身份验证**。

### 9.1 部署事实（本方案的前提）

| 项 | 事实 |
|---|---|
| 后端运行位置 | **B 机器**（非本机） |
| 客户端 | **局域网内多台机器** |
| 使用人数 | **多人 / 半公开** |
| 监听地址 | `--host 0.0.0.0`（`AGENTS.md:123`、`README.md:675`）——**多机访问必需，不可改** |

### 9.2 病根：门开着 + 没锁

| 项 | 事实 |
|----|------|
| 全局中间件 | `main.py:97-103` **唯一** `add_middleware` 是 CORS |
| 路由依赖 | 10 个 `APIRouter()`（`config`/`health`/`messages`/`metrics`/`model`/`sessions`/`settings`/`task_queries`/`token_usage`/`tool`）**全部裸挂**，`Depends(` **零命中** |
| 鉴权代码 | `main.py` 无 `Security`、无 token 校验 |
| 前端 | `client.ts:77-86` 仅"若 localStorage 有 token 则附带"，**后端从不校验** |
| CORS | `main.py:97-103` `allow_methods=["*"]`、`allow_headers=["*"]`、`allow_credentials=True` |

→ **监听地址是"多人可达"的前提（合理），但没有任何身份验证是缺陷（不合理）。二者必须分开看。**

### 9.3 后果（多人场景）

局域网内**任何人的浏览器、任何程序**都能直接调用：

| 动作 | 接口 | 后果 |
|------|------|------|
| 读明文 key | `GET /api/v1/config/read` | **全部 provider 密钥被拿走**（见第五章） |
| 改 / 清空 key | `PUT /api/v1/providers/{name}` | **密钥被改或被擦除**，服务不可用 |
| 读配置结构 | `GET /api/v1/models` | 末4位、内网地址、模型清单泄漏 |
| 读写业务数据 | 消息 / 会话 / 任务等接口 | 越权访问他人会话与消息 |

**多人共用场景下，这不是"理论风险"**——同事浏览器装个插件、某个网页发个跨域请求，都可能命中。

### 9.4 「门开着」与「没锁」必须分开看

| 现象 | 性质 | 处置 |
|------|------|------|
| `--host 0.0.0.0`（多人可达） | **合理**——多机部署的硬性前提 | **保持不变，不可收窄** |
| 零身份验证（任何请求都放行） | **缺陷**——多人场景下无法区分"谁有权用" | **必须修**（见 9.6） |

> v2.4~v2.7 把这两件事混为一个"绑定问题"，并给出"收窄监听地址"的方案——**该方案在多机部署下有害，已撤回**（见本章开头误判声明）。

### 9.5 token 分发是什么（大白话说明）

> 本节为北京老陈 2026-09-26 要求补充的解释层。**token = 暗号，「分发」= 把暗号告诉需要用的人。** 没有更复杂的含义。

#### 9.5.1 token 是什么

**给这个服务设的一个"暗号"。知道暗号的人才能用，不知道的一律拒绝。**

```
请求带对暗号   → 放行，正常返回
请求没带/带错  → 401 拒绝（看不到、进不去）
```

暗号本身就是一个普通字符串（如 `omni-2026-abc`），存在配置文件里，服务端比对。

**为什么需要它**：现在局域网里任何设备都能连你的服务，能看你的密钥、能改你的设置。加暗号后，这些都被挡住。

#### 9.5.2 「分发」是什么意思

**把暗号告诉需要用这个服务的人。分发 = 告诉。**

你的实际情况：

| 谁 | 需要知道暗号吗 |
|----|--------------|
| 你的笔记本 | 要（你告诉它） |
| 你的手机 | 要（你告诉它） |
| 同事的电脑 | 要（你告诉它同一个） |
| 任何不知道暗号的人 / 任何自动运行的程序 | **不要，挡住** |

#### 9.5.3 最简单的四步操作

**第 1 步**：在设置里填一个暗号，比如 `omni-2026-abc`
（该字段存在配置文件里，你自己填一次即可）

**第 2 步**：别人打开网页时，弹一个框让他输暗号

```
┌─────────────────────────┐
│  请输入访问口令          │
│  [________________]     │
│         [ 进入 ]         │
└─────────────────────────┘
```

**第 3 步**：输对了就记住（存在浏览器 localStorage），以后不用再输；输错或压根没输，就是进不去

**第 4 步**：暗号泄露了怎么办 → **你在设置里改成一个新暗号，所有旧暗号立刻作废**

#### 9.5.4 实际使用效果

| 场景 | 结果 |
|------|------|
| 你的电脑输对暗号 | 正常用 |
| 同事输对暗号 | 正常用 |
| 同事浏览器里的恶意插件想偷密钥 | **被挡住**（没有暗号） |
| 某个网页偷偷发跨源请求 | **被挡住**（没有暗号） |
| 你的暗号被别人知道了 | 你改暗号 → 别人的旧暗号**立即失效** |

#### 9.5.5 成本

- **后端**：新增一个校验暗号的依赖（见 9.6 第 1 条）
- **前端**：加一个"输入暗号"的页面（`client.ts` 已具备带 token 的逻辑，需核实）
- **必须同步做**：项目里 **76 个后端 E2E + 8 个前端 E2E = 84 个自动化测试**都要带上这个暗号才能跑通，否则全部 401 失败。改的地方只有两处（`e2e_helpers.py` 与 `api-proxy.ts` 统一注入），但**这一步不能漏**。

#### 9.5.6 其它分发方式为何不选

| 办法 | 做法 | 问题 |
|------|------|------|
| 后端某接口直接返回暗号 | 调一下就拿到 | **等于没有鉴权**——任何人都能调这个接口拿到暗号 |
| 让人手工改配置文件 | 每人手动改 `config.yaml` | 易改坏配置，还涉及文件权限 |
| 打开网页自动放行 | 不要暗号 | **等于没做鉴权** |
| IP 白名单 | 配置允许的 IP 段 | 换 WiFi / 换设备就进不去 |

**结论：用登录页让用户输入暗号**——门槛低（粘一下）、真起效（服务端真校验）、不改配置文件、泄露可换。

### 9.6 改法：加 token 鉴权（唯一正解）

> **不动的部分**：`--host 0.0.0.0` 保持（多机必需）、CORS 保持（前端跨源访问必需）、第五章明文显示保持（见 9.7 逻辑）。

**1. 后端：统一鉴权依赖（DRY，不逐路由重复）**

- 新增 `app/api/v1/deps.py`，实现一个 `Depends` 鉴权依赖：校验请求头 token，失败返回 401
- token 存于服务端配置（如 `security.api_token`），支持从环境变量读取（便于多机部署时统一配置）
- **挂载方式**：在 `main.py` 的 `include_router` 处统一加 `dependencies=[Depends(verify_token)]`，一处生效，不去改 10 个 router 的定义（KISS）
- **豁免项**：`/api/v1/health` 建议豁免（便于探活与排障），其余全部需鉴权

**2. 前端：token 注入**

- `client.ts:77-86` 已有"若 localStorage 有 token 则附带"的逻辑，需**核实其是否真的在发**（本次核实只确认了"后端不校验"，前端是否正确发出需实施时复核）
- 补充：token 获取入口（登录页 / 首次配置输入），存入 localStorage
- 若 token 失效（401），前端应跳登录页而非静默失败

**3. E2E 统一注入（工作量最大的前置）**

- 76 个后端 E2E + 8 个前端 E2E 的**所有请求**都要带 token
- 统一在两处注入，不逐个用例改：
  - 后端：`e2etests/e2emodel/e2e_helpers.py` 的 HTTP 调用封装处统一加 header
  - 前端：`e2e_front_lib/` 与 `e2e_case/api-proxy.ts` 统一转发时注入
- **不做这一步，全量测试会 100% 失败**（401）

**4. 部署配套（多机场景）**

- token 需分发给各客户端；建议提供**简易登录页**而非让用户手工改 localStorage
- token 泄露时的轮换手段（改配置即轮换）

### 9.7 与第五章的关系（逻辑链）

```
第五章「配置明文可接受」的前提 = 只有你能访问
        ↓
第九章加 token 鉴权
        ↓
只有持 token 者能调接口
        ↓
前提恢复成立 ✅ → 第五章保持明文不改
```

**结论：鉴权是第五章"明文可接受"的前置条件，而非替代。** 二者不冲突，是先后关系。

### 9.8 涉及文件

| 文件 | 改动 |
|------|------|
| `backend/app/api/v1/deps.py`（新增） | token 鉴权 `Depends` 依赖 |
| `backend/app/main.py` | `include_router` 处统一挂 `dependencies=[Depends(verify_token)]`；`/health` 豁免 |
| `backend/app/services/model/config_helpers.py` | 新增 `security.api_token` 配置项（支持 env 覆盖） |
| `frontend/src/services/api/client.ts` | 核实并修正 token 注入；401 跳登录 |
| `frontend/src/features/**`（新增登录页或 token 配置入口） | token 获取与持久化 |
| `backend/e2etests/e2emodel/e2e_helpers.py` | **E2E 统一注入 token**（76 个用例） |
| `frontend/e2e_front_lib/` + `e2e_case/api-proxy.ts` | **E2E 统一注入 token**（8 个用例） |
| `AGENTS.md` / `README.md` | **保持 `--host 0.0.0.0` 不变**（仅补鉴权说明） |

### 9.9 验收

- 无 token 请求 `GET /api/v1/models` → **401**
- 错误 token → **401**
- 正确 token → **200**（正常返回）
- `GET /api/v1/health` → **豁免**，无需 token 即可访问
- 无 token 请求 `GET /api/v1/config/read` → **401**（明文密钥不再对所有人敞开）
- 无 token 请求 `PUT /api/v1/providers/{name}` → **401**（无法改/擦他人密钥）
- **76 个后端 E2E + 8 个前端 E2E 全过**（token 注入正确）
- 局域网另一台机器**持正确 token** 可正常使用（多机部署未受影响）

# 三、新增功能

## 第十章 功能：key 正确性检测（测试连接）

**类型**：新增功能（非缺陷） | **实施顺序**：待定

### 10.1 现状：能力已存在，但不能直接用

**没有**"检测 key"的按钮。核实：`ModelLibraryTab.tsx:141` 调 `fetchRemoteModels`，目的是**拉模型列表**，不是**验证 key**。

但底层探测能力已具备：

```
model_service.py:537   api_key = os.environ.get(f"{name.upper()}_API_KEY") or str(p.get("api_key") or "")
model_service.py:538   headers = get_provider_adapter(name).static_headers(api_key)
adapters/base.py:17    return {"Authorization": f"Bearer {api_key}"}
model_service.py:488   await client.get(f"{api_base.rstrip('/')}/models", headers=headers)
```

→ 带真实 key 请求远端 `/models`，**这本身就是一次 key 有效性探测**。

### 10.2 病根：直接复用会误报

| # | 障碍 | 代码 | 后果 |
|---|------|------|------|
| 1 | status_code 丢失 | `model_service.py:543-552` `_fail()` **远端失败统一 HTTP 200 + ok:false** | 调用方拿不到状态码 |
| 2 | status_code 只进文案 | `_parse_remote_models_body:497-509` `message = f"HTTP {resp.status_code}"` 拼进字符串即丢 | 无法程序化区分错误类型 |
| 3 | **无错误分类** | 同上 | 401 / 404 / 超时 全变成同一句话 |

**第 3 条最要命**：部分 provider 的 `/models` 端点不存在或路径不同，若拿 404 判"key 无效"→ **给用户误报**。用户会反复改 key，浪费排查时间，且**可能把本来正确的 key 改坏**。

### 10.3 改法

**后端：先补错误分类（前提）**

- `fetch_remote_models` 的失败返回增加 `status_code` 字段，`_fail()` 增加该参数（`:543-552`）
- 增设分类判定：

| 分类 | 触发 | 含义（**注意第 3 行不能判 key 无效**） |
|------|------|------|
| `ok` | 2xx | key 有效 |
| `key_invalid` | **401 / 403** | key 无效或无权限 |
| `endpoint_unsupported` | **404 / 405 / 501** | 该 provider 无 `/models` 端点 → **key 可能是对的，不得判其无效** |
| `network_error` | 超时 / 连接失败 | base_url 或网络问题，与 key 无关 |

- **两个维度必须分开报**：
  - **base_url 通不通** —— 连不上 → 地址写错或网络不通
  - **连上后 key 对不对** —— 401 → key 写错
  → 若一律提示"连接失败"，用户无法判断该改地址还是改 key，等于没说

**支持测未保存的 key（建议）**

- 新增参数允许传入待测 key（不入库、仅本次探测）
- 理由：用户**正在输入框里新输 key** 时是"保存前验证"的最自然场景；若只能测已保存值，验证时机太晚
- 注意：传入的 key 走内存即可，**严禁落盘、严禁进日志**

**前端**

- `ProviderConfig.tsx` 加"测试连接"按钮，按上述分类给**不同**提示文案（不得统一显示"失败"）
- 明确区分"地址问题"与"key 问题"两套文案

**安全前提（必须先确认）**

- `model_service.py:490` `logger.error(f"拉取失败: {e}")` 把 httpx 异常**原样打进日志**
- **该异常字符串是否回显请求 header，尚未实测**，不能假设安全
- 做本功能前**必须先实测确认**：若会回显，须先过滤再落日志，否则"测试连接"本身会成为泄露点

### 10.4 涉及文件

`backend/app/services/model/model_service.py`、`frontend/src/features/settings2/components/ProviderConfig.tsx`

### 10.5 验收

- 有效 key + 正确 base_url → 提示"连接成功"
- 无效 key → 401 → 提示"key 无效或无权限"（**不提示"测试连接失败"**）
- 有效 key 但该 provider 无 `/models` 端点 → 404 → 提示"该 provider 不支持模型列表检测，key 未验证"（**不得说 key 无效**）
- base_url 写错 → 超时/连接错 → 提示"无法连接，请检查 base_url"（**不得说 key 有问题**）
- 日志中**不含**明文 key

---

# 四、补充问题

## 第十一章 旧通道 `PUT /config` 整条可废弃，且是第二个能清空密钥的入口

**级别**：中（死代码清理 + 安全补漏） | **来源**：北京老陈 2026-09-26 追问「旧通道能否废弃、还有谁在用」+「settings2 还在用吗」+「只是测试用的一起删，不能为测试留垃圾」

### 11.1 病根

第三章只修了 `model_service.py` 的 `update_provider_config` 一处。但**同一条链路上还有第二个能擦掉密钥的入口**——旧通道 `PUT /api/v1/config` 的 `provider_api_keys` 字段，它同样**不做空串保护**。

**完整调用链（三遍核实，逐步确认）**：

```
PUT /api/v1/config                          config_routes.py:82-83
  → update_config_service(config_update)    config_routes.py:83
  → config_service.py:81  update_config()   config_service.py:81
  → handler map 键 "provider_api_keys"     config_helpers.py:452
  → _update_api_keys()                      config_helpers.py:411-416
      config_data['ai'][provider]['api_key'] = api_key.strip()   ← :414
```

**关键点**：`:414` 的 `api_key.strip()` 作用是**去空格**，**不是空串保护**。传入 `""` 或纯空白 → `.strip()` 得 `""` → **照样写入配置，照样清空密钥**，且该函数 `:415` 还会 `logger.info("更新Provider API Key成功")` —— **擦掉密钥还记成功日志**。

与第三章 `model_service.py:431` `if k in key_map and v is not None` 是**完全相同的缺陷**（空串非 None 就写入），只是发生在另一个入口。

**v2.2 结论升级**：该缺陷所在的整条 `PUT /config` 写端点**已是纯死代码**（11.2），因此正确处置是**整条删除**而非"补保护后保留"——补保护只是 v2.1 的保守方案，删除后不需要再补。

### 11.2 谁在用：结论是 `PUT /config` 写端点**整条**可删

**核实一：settings2 前端完全不调它**（三遍确认）

| 查了什么 | 结论 |
|---------|------|
| settings2 的写接口 | 全是 `settingsApi.updateSettings` → **`PUT /settings`**；模型类走 `modelApi.*` → `/models` `/providers` |
| `configApi` 在 settings2 的出现 | 仅 `AboutFiles.tsx:93,102` 的 `readConfigFile`/`readVersionFile`——**都是 GET，只读**，不写 |
| 前端 `updateConfig` 方法 | `config.api.ts:85-88` **存在但零调用** |
| 注释佐证 | `config.api.ts:5` 与 `Layout/index.tsx:31` 明写"已 DRY 掉 `updateConfig+ai_model_ref` 拼装" |

**核实二：5 个 handler 的功能已全部迁移，无一遗漏**

| `PUT /config` handler | `PUT /settings` registry 对应项 | 覆盖 |
|---------------------|-------------------------------|------|
| `theme`（`:453`） | `settings_registry.py:226` `app.theme` | ✅ |
| `language`（`:454`） | `:224` `app.language` | ✅ |
| `max_steps`（`:455`） | `:134` `agent.max_steps` | ✅ |
| `security`（`:456`） | `:158,160,162,164` `security.enabled/confirmDangerousOps/auto_confirm_delay/hitl_timeout` | ✅ |
| `project_root`（`:457`） | `:129` `workspace.project_root` | ✅ |

**核实三：迁移进程有代码佐证**

`settings_service.py:8` 注释："v4.19 Phase 1/2：settings_service 改**直连后端** `update_config_service`"——**它以前直连旧通道，现已切开**。

**结论**：前端零调用 + 功能全迁移 + 迁移进程已闭合 → **`PUT /config` 写端点是纯死代码，可整条删除**（推翻 v2.1 的"端点保留"结论）。

### 11.3 后果

- **安全**：只要有人手工 curl `PUT /api/v1/config` 带 `{"provider_api_keys": {"xxx": ""}}`，即可擦除密钥，且 `:415` 记 `logger.info("更新Provider API Key成功")`——**擦掉密钥还报成功**。第九章已核实该端点**无鉴权**且**绑 0.0.0.0**，局域网可达。
- **维护**：`_update_api_keys` 是三套 key 写入逻辑中的第三套（三套见 11.6），每多一套就多一个不一致点。

### 11.4 依赖它的测试：3 个文件 4 处，3 个随端点删、1 个须迁移

> v2.1 曾说"2 个测试"，第 3 遍全项目扫描后发现是 **3 个文件 4 处**，其中一处是 `provider_api_keys` 唯一的回归护栏。

| # | 测试位置 | 测什么 | 处理 | 理由 |
|---|---------|--------|------|------|
| 1 | `test_api_comprehensive.py:86-87` | `PUT /api/v1/config` 空 body 200 + **`ai_provider:123` 返 422** | **删** | 这是 `provider_api_keys` 坏 provider 分支的**唯一回归护栏**；端点没了留着必挂 |
| 2 | `test_bug_hunt_round4_red.py:178-187` `TestR4_FieldHandlerProjectRoot` | `FIELD_HANDLERS["project_root"]` 必须写 `workspace.project_root` 不写 `app.project_root` 死键 | **删** | 死键修复的护栏，handler 随端点删，**测试前提消失** |
| 3 | `test_bug_hunt_round4_red.py:145-170` `TestR4_UpdateConfigLock` | **filelock 并发保护**：`cs.update_config(theme)` 与 `merge_region_patch(security)` 双线程并发，两个域都不丢 | **迁移，不可删** | 见下方 11.5 |
| 4 | `test_bug_hunt_round5_red.py:108-132` `TestR5_UpdateConfigNoAiKey` | 注释自称"**证伪验证**"：配置无 ai 段时 `update_config` 不 500 | **删** | 端点没了，场景不再存在 |

**第 3 项为什么不能一起删**（这条最容易被误判为"为测试留垃圾"）：

该测试的断言是 `final["app"]["theme"] == "dark"` **且** `final["security"]["enabled"] is True`——即**两个通道同时写配置都不丢更新**。它守护的能力是 `config_helpers.py:557` 的 **filelock**，`PUT /config`、`PUT /settings`、`PUT /providers` **三条链路的落盘都经过它**。

删掉旧端点后**能力仍在**（新通道照样用），但**新通道侧目前没有任何并发锁测试**——已扫：`merge_region_patch` 的测试（`test_bug_fixes.py:35,54`、`test_bug_round2.py:42`）**全是单线程**。

→ 跟着旧端点删掉它 = **并发护栏净损失**。这是**迁移测试**（换被测通道），**不是为测试留垃圾代码**。

### 11.5 改法（四步，顺序不可颠倒）

**第 1 步：删干净旧通道**

| 删除对象 | 位置 |
|---------|------|
| `PUT /config` 写端点 | `config_routes.py:82-84` |
| 5 个 field handler | `config_helpers.py:453-457`（`theme`/`language`/`max_steps`/`security`/`project_root`） |
| `_update_api_keys` 函数 | `config_helpers.py:411-416` |
| 其 map 注册项 | `config_helpers.py:452` |
| `provider_api_keys` 字段 | `config_schemas.py:32` |
| 前端 `updateConfig` 方法 | `config.api.ts:85-92`（零调用，删了无影响） |
| 前端 2 处 mock | `rendersmoke.test.tsx:40`、`review-bugs.test.tsx:42` |

> 注：`config_routes.py:63` 的 `GET /config` **保留**（读接口仍有前端在用），只删写端点。
>
> **端点边界澄清（v3.0 审计补充）**：本章**只删写端点 `PUT /config`**。`config_routes.py` 下的**所有 GET 端点一律保留**，包括：
> - `GET /api/v1/config`（系统配置数据，含 `api_key_configured: bool`）——第五章、第三章依赖
> - `GET /api/v1/config/read`（配置文件全文明文）——**第五章明确依赖且裁定不改**（见 5.6）
> - `GET /api/v1/config/full`（完整配置，掩码）——第七章依赖
> - `GET /api/v1/config/validate`、`/fix`、`/path` 等
>
> 换言之：**删的是"能通过 `PUT /config` 改配置"这条能力，不是整个 `/config*` 路由组**。

**第 2 步：删 3 个死测试**（上表 #1 #2 #4），逐条删除，**不留空壳类、不留注释占位**。

**第 3 步：迁移并发护栏**（上表 #3）

把 `TestR4_UpdateConfigLock` 的被测通道从"旧 + 新"改为"**新 + 新**"：

- 线程一：`merge_region_patch({"app.theme": "dark"}, scope="settings")`（原 `cs.update_config(ConfigUpdate(theme="dark"))`）
- 线程二：`merge_region_patch({"security.enabled": True}, scope="settings")`（原样保留）
- 断言全保留：两个域最终都落盘，不丢更新

> 能力、断言强度、并发强度**均不降**，只换被测入口。

**第 4 步：回归**

- `pytest` 全量
- 全项目 grep `update_config` / `provider_api_keys` / `FIELD_HANDLERS` → **零残留**（除迁移后的测试中不再出现旧通道名）
- 手工验证 `PUT /settings` 改 theme/language/security、`PUT /providers` 改 key 仍正常

### 11.6 为什么本条不只是删代码（三套写入逻辑收敛为两套）

| # | key 写入逻辑 | 状态 |
|---|------------|------|
| 1 | `model_service.py:412-442` `update_provider_config`（`PUT /providers/{name}`） | **在用**（settings2 `modelApi.updateProvider`） |
| 2 | `config_helpers.py:411-416` `_update_api_keys`（`PUT /config`） | **死代码，本章删除** |
| 3 | `config_helpers.py:414` 的 `.strip()` 行为 | 随 #2 一并消失 |

删除后 key 写入路径从 3 条收敛为 1 条，**第一章"双通道 strip 不一致"的根因随之消失**（届时第一章只需保留 `model_service` 的三态改造）。

### 11.7 涉及文件

| 文件 | 改动 |
|------|------|
| `backend/app/api/v1/config_routes.py` | 删 `PUT /config` 端点（`:82-84`），保留 `GET /config` |
| `backend/app/services/model/config_helpers.py` | 删 `_update_api_keys`（`:411-416`）+ map 注册项（`:452`）+ 5 个 field handler（`:453-457`） |
| `backend/app/api/v1/config_schemas.py` | 移除 `provider_api_keys`（`:32`）及其余随之失效的旧字段 |
| `backend/tests/test_api_comprehensive.py` | 删 `:86-87` 两条 `PUT /api/v1/config` 检查 |
| `backend/tests/test_bug_hunt_round4_red.py` | 删 `:178-187` 死键测试；`:145-170` 并发测试**迁移为新通道** |
| `backend/tests/test_bug_hunt_round5_red.py` | 删 `TestR5_UpdateConfigNoAiKey`（`:103-132`） |
| `frontend/src/services/api/config.api.ts` | 删 `updateConfig`（`:85-92`） |
| `frontend/src/tests/unit/rendersmoke.test.tsx` | 删 `:40` mock |
| `frontend/src/tests/unit/review-bugs.test.tsx` | 删 `:42` mock |

### 11.8 验收

- `PUT /api/v1/config` → 端点不存在（405/404）；`GET /api/v1/config` → **仍 200**
- `PUT /settings` 改 theme/language/security → 正常生效
- `PUT /providers/{name}` 改 key → 正常生效
- **并发护栏**：两线程并发调 `merge_region_patch` 写不同域 → 两个域都落盘不丢（迁移后的测试通过）
- 全项目 grep `update_config` / `provider_api_keys` / `FIELD_HANDLERS` → 零残留
- `pytest` 全量 + `npm run test` 全绿

---

# 五、新增功能（二）

## 第十二章 功能：key 显示「前 4 位 + 后 4 位」+ 眼睛按钮切换明文

**类型**：新增功能（非缺陷） | **来源**：北京老陈 2026-09-26 07:29 新增需求
**实施顺序**：**待定**（明文接口须待第九章第一步落地后方可上线）

### 12.1 需求（北京老陈原话）

> 「对 api_key 不空、就是有 key 的时候，key 的显示前面 4 个字符串 + 后面 4 个字符串。在你的基础上再加一点需求：回显的时候这样显示，用眼睛按钮切换隐藏和真实。」

**三项决策（已定）**：

| # | 事项 | 裁定 |
|---|------|------|
| 1 | 眼睛切换要不要二次确认 | **要确认 + 30 秒自动恢复** |
| 2 | 前 4 位泄露（泄露面 4 位 → 8 位） | **接受**（前 4 位多为 `sk-1`/`sk-p`，泄露价值低） |
| 3 | 与第九章的先后 | **先写文档，实施时再定**（明文接口须待第九章第一步落地） |

### 12.2 目标效果

```
平时（打码）:  [ sk-1**********7890  ] 👁     ← 前 4 + 后 4
点眼睛:        [ sk-1234567890abc     ] 👁     ← 明文
再点眼睛:      [ sk-1**********7890  ] 👁     ← 恢复打码
30 秒后:       自动恢复打码
env 接管时:    整块只读，不显示（维持现状，见 5.x env 分支）
```

### 12.3 现状与两个技术前提（核实结论）

**前提一：前 4 位后端根本没返回**

```
现返回:  { "configured": true, "suffix": "7890" }        ← config_helpers.py:611-617
                                        ↑ 只有末 4 位
目标:    { "configured": true, "prefix": "sk-1", "suffix": "7890" }
```

必须扩 `mask_secret_value`（`config_helpers.py:611-617`）增加 `prefix` 字段。

**前提二：明文前端从未持有**

打码态前端有数据（末4位），但**真实明文前端从来没有过**——`get_models`（`model_service.py:179`）只返掩码。要切换显示真实值，**必须后端新开接口**传明文。

### 12.4 两个实现坑（识别于设计阶段，须在实施时处理）

**坑 1：双眼睛冲突**

`ProviderConfig.tsx:187-195` 现用 AntD `Input.Password`，其内置"眼睛" toggle（`:193` `autoComplete`、默认 `visibilityToggle`）作用是**显示用户刚输入的字符**——因为输入框初值恒为 `''`（`:87`），它**看不到已保存的 key**。

若再加一个"看已保存明文"的眼睛，会出现**两个眼睛按钮**，用户无法分辨。

- **处理**：`Input.Password` 设 `visibilityToggle={false}` 关闭自带眼睛，改用**自定义眼睛**，明确职责：
  - **自定义眼睛** = 查看**已保存**的密钥（本章新增，走后端接口）
  - 若还需查看刚输入内容，靠 `autoComplete="new-password"` + 选中即全选替代，或保留自带 toggle 但在其旁加文字说明"（仅显示本次输入）"

**坑 2：短 key 前后缀重叠**

`mask_secret_value:617` 现有规则是 `len(s) >= 4` 才给 `suffix`，否则置空。加 `prefix` 后需明确短 key 行为：

| key 长度 | 现状 | 本章须定 |
|---------|------|---------|
| ≥ 8 位 | 显示 prefix+suffix | `prefix` 前 4 + `suffix` 后 4，**不重叠** |
| 4~7 位 | 只给 suffix | **只给 suffix，prefix 置空**（否则 `12345` → prefix=`1234`、suffix=`2345`，中间 3 位重复展示，等于泄露 7/8 位） |
| < 4 位 | suffix 置空 | 维持（`configured: true`、`prefix`/`suffix` 均 `""`） |

> 注：`len(s) >= 8` 判定须在实现时用测试锁住，避免后人改回 `>= 4` 导致短 key 泄露。

### 12.5 改法

**后端**

- **扩掩码结构** `config_helpers.py:611-617` `mask_secret_value`：
  - 已配 key：返回 `{configured: true, prefix: s[:4], suffix: s[-4:]}`，**但仅当 `len(s) >= 8`**
  - `4 <= len(s) < 8`：返回 `{configured: true, prefix: "", suffix: s[-4:]}`
  - 未配：`{configured: false, prefix: "", suffix: ""}`（**三键恒定**，与第七章 7.3「形状恒定」同一目标）
  - 与第七章同批改，避免同一函数改两遍
- **新增明文接口** `model_routes.py`：`GET /api/v1/providers/{name}/api-key`，返回 `{"api_key": "<明文>"}`
  - **必须记审计日志**：provider 名、时间戳、来源 IP（`request.client.host`）——读密钥属敏感操作
  - **env 接管时一律拒绝返回明文**，文案"该 provider 由 {NAME}_API_KEY 环境变量接管，密钥不在配置文件中，无法查看"
    > 理由：env 优先消费（`config.py:112`），YAML 里那个可能**不是生效值**，给用户看是假象——与第二章"静默回落"同类陷阱
  - 复用已有 env 判定（`model_service.py:176`），不新造轮子
- **前端类型** `model.api.ts:34`：`api_key` 加 `prefix: string`，与后端形状对齐

**前端** `ProviderConfig.tsx`

- 默认显示 `sk-1**********7890`（由 `prefix` + 打码 + `suffix` 拼接）
- 自定义眼睛按钮 → **二次确认弹窗**（"将显示明文密钥，请勿截图或分享"）→ 调接口 → 明文填入输入框
- **30 秒自动恢复打码**（或窗口失焦即恢复），用定时器/事件实现，组件卸载时清理定时器防内存泄漏
- 明文**不写 localStorage**；恢复打码时立即从 state 清除明文；保存后复用现有 `setApiKey('')`（`:120`）
- env 接管时按钮不显示（整块只读，`:128-154` 早退逻辑已覆盖）

**明确不做**：不提供"永久显示""导出密钥文件"；不做"记住我的选择"（不能关掉自动恢复）。

### 12.6 依赖与上线条件

| 项 | 说明 |
|----|------|
| **第九章 token 鉴权为硬依赖** | 明文接口无鉴权时，局域网内任何客户端均可直接调走明文密钥。**第九章鉴权落地前，本章的明文接口不得上线**（`0.0.0.0` 在多机部署下必须保留，不能靠收窄监听地址解决） |
| 第五章（配置文件明文） | 已裁定不改，本章为补充，不冲突 |

### 12.7 涉及文件

| 文件 | 改动 |
|------|------|
| `backend/app/services/model/config_helpers.py` | `mask_secret_value` 加 `prefix`，三键恒定，长度分档（与第七章同批） |
| `backend/app/api/v1/model_routes.py` | 新增明文接口 + 审计日志 + env 拒绝 |
| `frontend/src/services/api/model.api.ts` | `api_key` 类型加 `prefix`；新增明文接口方法 |
| `frontend/.../settings2/components/ProviderConfig.tsx` | 打码显示改前后 4 位；关闭自带眼睛；自定义眼睛 + 二次确认 + 30 秒恢复 |
| `frontend/src/tests/unit/settings2-*.test.tsx` | 补短 key 分档断言、补二次确认与自动恢复断言 |

### 12.8 验收

- 配了 8 位以上 key → 显示 `sk-1****7890`（前 4 + 后 4）
- 配了 4~7 位 key → **只显示末 4 位，prefix 为空**（不重叠泄露）
- 点眼睛 → 弹二次确认 → 确认后显示明文
- **30 秒后 / 失焦后自动恢复打码**，明文从 state 清除
- env 接管 provider → 眼睛按钮不显示，接口拒绝返回明文
- 明文接口访问有审计日志（provider、时间、IP）
- **同局域网另一设备**调用明文接口 → 连接失败（证明第九章第一步已生效）

---

# 六、附录

## 附录一 改动文件清单

| 序号 | 文件 | 改动 | 关联章 |
|------|------|------|-------|
| 1 | `backend/app/services/model/model_service.py` | api_key 三态 + strip + clear 互斥；错误分类 + status_code；测试连接 | 一、三、四、十一 |
| 2 | `backend/app/services/model/resolver.py` | 跨 provider 且 key 空白 → 明确报错（情况 A 同 provider 保持原样）；**删 `:211` 的 `or` 回落** | 二、八 |
| 3 | `backend/app/llm/client_sdk.py` | **删 `_default_base_url` 兜底 + `_DEFAULT_URLS`**，空 URL 构造即报错；header 前 strip | 一、八 |
| 4 | `backend/app/services/model/config_helpers.py` | `mask_secret_value` 加 `prefix`、三键恒定（与十三章同批）；删 `_update_api_keys` + map 注册项 + 5 个 field handler | 七、十二、十三 |
| 5 | `backend/app/services/model/config_service.py` | 删死契约（`_mask_api_key`） | 七 |
| 6 | `backend/app/services/settings/settings_service.py` | secret 写路径三态 | 六 |
| 7 | `backend/app/api/v1/model_routes.py` | api_key 三态契约文档化；**新增明文查看接口 + 审计日志 + env 拒绝** | 三、十三 |
| 8 | `backend/app/api/v1/config_routes.py` | **删 `PUT /config` 写端点（`:82-84`）**，保留 `GET /config` | 十二 |
| 9 | `backend/app/api/v1/config_schemas.py` | 移除 `provider_api_keys` 字段（`:32`） | 十二 |
| 10 | `backend/tests/test_api_comprehensive.py` | **删 `:86-87` 两条 `PUT /api/v1/config` 检查** | 十二 |
| 11 | `backend/tests/test_bug_hunt_round4_red.py` | **删 `:178-187` 死键测试**；`:145-170` 并发测试**迁移为新通道** | 十二 |
| 12 | `backend/tests/test_bug_hunt_round5_red.py` | **删 `TestR5_UpdateConfigNoAiKey`（`:103-132`）** | 十二 |
| 13 | `frontend/.../settings2/components/ProviderConfig.tsx` | api_key trim；**base_url 空则红色提示 + 拒绝保存**；测试连接按钮；**前后4位打码显示 + 自定义眼睛 + 二次确认 + 30秒恢复** | 一、八、十一、十三 |
| 14 | `frontend/.../settings2/components/ModelModals.tsx` | **添加 Provider 弹窗 base_url 必填校验** | 八 |
| 15 | `frontend/src/services/api/model.api.ts` | 类型与后端对齐（加 `prefix`）；新增明文接口方法 | 七、十三 |
| 16 | `frontend/src/services/api/config.api.ts` | 删除零调用的 `updateConfig` 方法（`:85-92`） | 十二 |
| 17 | `frontend/src/tests/unit/rendersmoke.test.tsx` | 清理 `updateConfig` mock（`:40`） | 十二 |
| 18 | `frontend/src/tests/unit/review-bugs.test.tsx` | 清理 `updateConfig` mock（`:42`） | 十二 |
| 19 | `AGENTS.md` / `README.md` | **保持 `--host 0.0.0.0` 不变**（多机部署必需），补 token 鉴权说明 | 九 |
| 20 | `frontend/src/tests/unit/settings2-about-files.test.tsx` | **不改**（第五章裁定保留明文，测试保持原样） | 五 |
| 21 | `frontend/src/tests/integration/settings2-live-about.test.ts` | **不改**（同 19） | 五 |
| 22 | `backend/tests/test_model_service_tdd.py` | 补 clear 互斥等用例 | 三、四 |
| 23 | `backend/tests/test_settings_*_tdd.py` | 重写 2 个死测试 | 六 |
| 24 | `frontend/src/tests/unit/settings2-*.test.tsx` | 补短 key 长度分档断言；二次确认与 30 秒自动恢复断言 | 十三 |

## 附录二 回归验证要求

**每章改完必做**

| 项 | 要求 |
|----|------|
| 语法 | 后端 `python -m py_compile`；前端 `npm run lint` + `npm run format:check` |
| 后端测试 | `pytest` 全量全过；重点 `test_model_service_tdd`、`test_settings_*_tdd`、`test_bug_fixes`、`test_bug_round2`、`test_bug_round3_new_code`（掩码相关不可回归） |
| 前端测试 | `npm run test` 全量；重点 `settings2-about-files`、`settings2-live-write2`、`settings2-live-probe` |
| 禁止 | 任何"改测试让通过"的做法。第五章涉及的两处测试改动是**改期望语义**，须在 commit 说明中写清 |

**整改后必须补的 E2E（当前完全空白）**

| 用例 | 验证点 | 关联章 |
|------|--------|-------|
| E2E-KEY-01 | 写入带空格 key → 保存 → 实际 LLM 调用成功 | 一 |
| E2E-KEY-01B | 会话切到 **key 空白的跨 provider** → **明确报错**（文案含 provider 名）；同 provider 只换模型名 → **仍正常** | 二 |
| E2E-KEY-02 | 对已配 key 的 provider 提交 `api_key: ""` → 配置**未被清空** | 三 |
| E2E-KEY-03 | 提交 `api_key` + `clear=true` → 返回 400 且配置未变 | 四 |
| ~~E2E-KEY-04~~ | ~~`GET /config/read` 正文不含明文 api_key~~ → **取消**（第五章裁定不改） | 五 |
| E2E-KEY-05 | 查看明文：正常 provider 返回明文 + 记审计日志；**env 接管时拒绝** | 十 |
| E2E-KEY-06 | 测试连接：401→key 无效、404→端点不支持（**不判 key 无效**）、超时→网络问题 | 十一 |
| E2E-KEY-07 | `PUT /api/v1/config` → 端点不存在（405/404）；`GET /api/v1/config` → **仍 200** | 十二 |
| E2E-KEY-08 | `PUT /settings` 改 theme/language/security、`PUT /providers` 改 key → 均正常生效 | 十二 |
| E2E-KEY-09 | 两线程并发调 `merge_region_patch` 写不同域 → 两个域都落盘不丢更新（迁移后的并发护栏） | 十二 |
| E2E-KEY-10 | 8 位以上 key 显示 `sk-1****7890`；4~7 位 key **只显示末 4 位**（prefix 空，不重叠） | 十三 |
| E2E-KEY-11 | 点眼睛 → 弹二次确认 → 显示明文；**30 秒/失焦后自动恢复打码**且明文从 state 清除 | 十三 |
| E2E-KEY-12 | env 接管 provider：眼睛按钮不显示 + 明文接口拒绝返回 + 有审计日志 | 十三 |

> 测试代码不纳入本次整改提交范围，按项目规范另行提交。

**整体验收标准**：后端 `pytest` 全绿 + 前端 `npm run test` 全绿；6 个新 E2E 全过；手工验证同一局域网另一设备访问 `http://<本机IP>:8000/api/v1/models` **应连接失败**（第九章第一步生效）。

## 附录三 本报告不覆盖的范围

- git 历史中 29 处明文 key 的清洗（涉及历史改写决策，另案）
- `/api/v1` token 鉴权的**代码实施**已列入第九章（v2.8 由"另立项"升级为必做）；本报告仅覆盖其方案与验收
- E2E 用例的编写（整改后另行提交）

---

**编写人**: 小欧
**创建时间**: 2026-09-26 01:01:38
**更新时间**: 2026-09-26 05:35:00
**版本**: v2.0
