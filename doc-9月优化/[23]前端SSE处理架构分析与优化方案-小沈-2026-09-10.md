# 前端 SSE 处理架构分析与优化方案

> 文档名称: 前端SSE处理架构分析与优化方案-小沈-2026-09-10.md
> 编写: 小沈  2026-09-10
> 范围: `frontend/src` 下 SSE 接收/解析/分发/状态/渲染全链路
> 基线: 2026-09-09 小欧日志治理后版本
> 更新时间: 2026-09-10 11:29:23（小欧：逐小节标注各问题对文档[25]的采纳情况）

---

## 一、概要

### 1.1 问题清单（9 项，按严重度排序）

| 编号 | 类型 | 问题 | 严重度 | 修复依赖 |
|------|------|------|--------|----------|
| P1 | 时序 bug | onResumed 回放 error 时 isPausedRef 仍 true → error 重吞缓冲死循环 | 致命 | 无 |
| P2 | 架构缺陷 | seq 权威去重未用于 step，反用脆弱指纹去重且放错层 | 致命 | 无 |
| P3 | 时序 bug | onError(from_backend) 清同名 ref 失效，历史 step 残入下任务 | 致命 | 无 |
| P4 | 时序 bug | 空闲超时调 reconnect 与 reader.read() 竞态，AbortError 被静默 | 高 | 无 |
| P5 | 时序 bug | cancelInProgress 放行双 final，两 final step 同入列表 | 高 | 无 |
| P6 | 架构缺陷 | executionSteps 三源真相，onComplete "取较长者"补丁 | 高 | P2 缓解 |
| P7 | 架构缺陷 | setExecutionSteps updater 内做副作用，违反 React 契约 | 中 | 无 |
| P8 | 架构缺陷 | responseBufferRef / streamingContentRef 双缓冲混用 | 中 | 无 |
| P9 | 死代码 | reconnectConfigRef.enabled 设了从不读，误导维护 | 低 | 无 |

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】问题清单总评**：9 项中 **6 项问题归属文档[25] 3.5 问题清单（P1→3.5.2.3、P2→3.5.1.2、P3→3.5.1.1、P5→3.5.2.3、P7→3.5.2.2、P8→3.5.2.1 叠加因素、P9→3.5.2.4），P6 问题分析未入 3.5 清单（只在第四章 Q9 + 方案 S7）、P4 主体被判定夸大不成立**（残留窗口走 S9）、"五、未列入项"中的 metaFrames 与 handlers 重建两项性能项未入 3.5。判定详见各 P 节与第四、五章标注。

### 1.2 修复策略

- **P1/P3/P4/P5** 为确凿运行时 bug，优先修，各自独立可实施。
- **P2** 是架构倒置的根，修后 P6 自然缓解，第二优先。
- **P6/P7/P8** 为补丁消除与职责归一，第三优先。
- **P9** 死代码清理，随手修。
- 全部 diff 均为**手动编辑**（遵循铁规：禁止脚本改代码），逐文件逐 hunk 实施。

### 1.3 涉及文件

```
frontend/src/features/chat/services/sseParser.ts      # P2 P7
frontend/src/hooks/useSSE.ts                           # P2 P4 P7 P9
frontend/src/features/chat/hooks/useChatCallbacks.ts  # P1 P2 P3 P5 P6 P8
```

---

## 二、架构现状速览

数据流（7 层）：

```
fetch+ReadableStream (useSSE.ts)
  → 逐行 data: JSON.parse (sseParser.ts processSSEData)
    → switch(type) 16 分支
      → setExecutionSteps(useSSE state) + executionStepsRef 同步写 + saveStepsToStorage
      → onStep/onChunk/onComplete/onError/... 回调
        → useChatCallbacks: setMessages 追加到 message.executionSteps
        → useChatStreaming: denied 聚合
          → stepFilter: 业务/元二分
            → PipelineRenderer 渲染
```

技术选型（fetch+ReadableStream、指数退避+Full Jitter、after_seq 续传）**合理**。问题全在状态归属与数据流时序。

---

## 三、实施章节

### P1 · onResumed 回放 error 死循环（致命时序 bug）

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[采纳]**。问题**真实**（文档25 3.1 P1 判定"真实"），已纳入文档25 **3.5.2.3**（复位逻辑依赖隐式时序，:148 明列该死循环）；方案（先复位 isPausedRef 再回放，:747 上移至回放循环之前）与文档25 **S5** 完全一致（"useChatCallbacks.ts:747 `isPausedRef.current = false` 上移到 :739 回放循环之前"），直接采纳，无修正。注意：此缺陷属暂停恢复场景的独立时序 bug，**不引起用户三症状之一**（三症状为重复/冻结/丢尾），但确为真实致命 bug，随 S5 一并根治。

**根因**：`onResumed` 先 forEach 回放缓冲 + 循环调 `onError`，**末尾**才 `isPausedRef.current = false`。而 `onError` 开头 `if (isPausedRef.current) { push 回缓冲; return; }`。回放期间 isPausedRef 仍 true → error 被 onError 重新 push 回 `displayBufferRef` → 下次 resumed 又回放又吞回，**error 永远不被处理且每次 resumed 重复**。

**现状** `useChatCallbacks.ts:706-754`：
```ts
setMessages((prev) => { ... forEach 回放 ... });
for (const err of errorItems) { onError(err); }   // ← isPausedRef 仍 true
displayBufferRef.current = [];
isPausedRef.current = false;                        // ← 末尾才复位
setIsPaused(false);
```

**diff**（先解除暂停态，再回放）：

```diff
--- a/frontend/src/features/chat/hooks/useChatCallbacks.ts
+++ b/frontend/src/features/chat/hooks/useChatCallbacks.ts
@@ onResumed
     console.log(
       '▶️ [onResumed] 收到恢复事件，缓冲区长度:',
       displayBufferRef.current.length
     );

+    // P1 修复(小沈 2026-09-10): 先复位 isPausedRef 再回放, 否则回放期间调 onError
+    //   会因 isPausedRef 仍 true 把 error 重新 push 回 displayBuffer → 死循环(每次 resumed 重复回放同一 error)
+    isPausedRef.current = false;
+    setIsPaused(false);
+
     const errorItems: Array<string | SSEError> = [];

     setMessages((prev) => {
@@ onResumed 末尾
     // 清空缓冲区
     displayBufferRef.current = [];

-    // 2026-08-27 小欧 修复#2: 同步复位 isPausedRef.current, 否则恢复后分块仍进缓冲而丢失
-    isPausedRef.current = false;
-    // 更新暂停状态
-    setIsPaused(false);
-
     // 通知流式组件恢复接收
     if (streaming?.setIsReceiving) {
       streaming.setIsReceiving(true);
     }
```

**验证**：暂停期间产生 error → resumed → error 进入 errorItems → onError 执行时 isPausedRef===false → error 正常处理不回缓冲。再次 resumed 时 displayBuffer 已空，不重复。

---

### P2 · seq 权威去重上移到解析层（致命架构倒置）

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[采纳·方案需按S3修正]**。问题**真实**（文档25 3.1 P2 判定"真实"），是"用户症状①重复输出"的核心架构根因，已纳入文档25 **3.5.1.2**（SSE 事件流无单调序防线）+ **Q6**（去重防线只拦一半，三星核心问题）+ **S3**（seq 权威守卫上移）。方案须修正两点：
>
> 1. **首帧死角**：本 diff 守卫写在 onSeq 之前、判定用 `rawData.seq <= lastConsumedSeqRef.current`，若 ref 初始值为 0，**首帧 seq=0 会因 `0<=0` 被误拦**。文档25 6.1 专门点名此死角，S3 改为 next-seq 语义：守卫放在 **onSeq 之后**、推进 `lastSeqRef = Math.max(lastSeqRef, seq+1)`、判 `seq < next`，使"首帧 seq=0"与"重放帧 seq=0"分离互不误伤。
> 2. **不可删除指纹去重**：本 diff 第 (3) 步把 onStep 指纹去重整体删除，文档25 S3 明确**保留指纹作内容级兜底**（覆盖"极端无 seq 老式事件"），与 seq 守卫双保险、职责分明——删除部分不采纳。
> 3. 另本 diff 只覆盖 useSSE 两处调用（done 分支与 for 循环分支），未及 sseParser 全部 6 处无条件持久化追加分支（详见 P7 标注），S3 以"入口单点拦截"一次覆盖。问题与方案主体采纳，按上述修正实施。

**根因**：后端每帧带 `seq`，但 `sseParser.ts:158-160` 只回传 `onSeq` 供重连 after_seq，**仅 usage 分支用 seq 去重**（`:267-270`），其余 15 个 step 类型无 seq 去重。转而在消费层 `useChatCallbacks.ts:161-171` 用 `type|step|preview|content前64` 指纹去重。指纹判据脆弱（chunk 同前缀误杀），且**去重在 onStep（消费层）而非 sseParser（解析层）**：被去重跳过的是 onStep 回调（不进 message），但 sseParser 的 `setExecutionSteps` 已执行（进了 useSSE state）→ 两源数据不一致。

**diff 分三处**：

**(1) sseParser.ts — handlers 类型加 lastConsumedSeqRef，入口加 seq 去重**

```diff
--- a/frontend/src/features/chat/services/sseParser.ts
+++ b/frontend/src/features/chat/services/sseParser.ts
@@ handlers 类型定义
     usageAccumRef?: React.MutableRefObject<{
       prompt: number;
       completion: number;
       total: number;
     }>;
     lastUsageSeqRef?: React.MutableRefObject<number>;
+    // P2 修复(小沈 2026-09-10): 已消费事件 seq 上限, 入口权威去重, 替代 onStep 指纹去重
+    lastConsumedSeqRef?: React.MutableRefObject<number>;
   },
@@ processSSEData 入口, rawData 解析后
     const rawData = JSON.parse(jsonStr);

     const frameTime = Date.now();
+
+    // P2 修复(小沈 2026-09-10): seq 权威去重——后端重连重放/重复行时, 已消费 seq 直接跳过。
+    //   一处去重, useSSE state 与 message 两源自然一致; 无 seq 帧(meta)不参与去重照常处理。
+    if (
+      typeof rawData.seq === 'number' &&
      handlers.lastConsumedSeqRef &&
+      rawData.seq <= handlers.lastConsumedSeqRef.current
+    ) {
+      return;
+    }

     // 【北京老陈 2026-07-12 小欧】回传后端事件 seq，断线重连时用于 after_seq 续传
     if (typeof rawData.seq === 'number' && onSeq) {
       onSeq(rawData.seq);
     }
```

> 说明：`onSeq` 回调仍保留（`useSSE.ts:795-797` 里 `if (s > lastSeqRef.current) lastSeqRef.current = s`），它负责更新 `lastSeqRef` 供 after_seq 续传。入口去重用同一 `lastSeqRef`（通过 `lastConsumedSeqRef` 传入），语义一致（已消费最大 seq）。去重在 onSeq 之前执行，重复帧先跳过、不更新。

**(2) useSSE.ts — 两处 processSSEData 调用补传 lastConsumedSeqRef**

```diff
--- a/frontend/src/hooks/useSSE.ts
+++ b/frontend/src/hooks/useSSE.ts
@@ done 分支调用处 (约 :773-803)
                 onSeq: (s: number) => {
                   if (s > lastSeqRef.current) lastSeqRef.current = s;
                 },
                 setMetaFrames,
                 usageAccumRef,
                 lastUsageSeqRef,
+                lastConsumedSeqRef: lastSeqRef,
               },
               isProcessingRef
@@ for 循环内调用处 (约 :813-845)
                 onSeq: (s: number) => {
                   if (s > lastSeqRef.current) lastSeqRef.current = s;
                 },
                 setMetaFrames,
                 usageAccumRef,
                 lastUsageSeqRef,
+                lastConsumedSeqRef: lastSeqRef,
               },
               isProcessingRef
```

**(3) useChatCallbacks.ts — 删除指纹去重，seq 去重已在解析层兜底**

```diff
--- a/frontend/src/features/chat/hooks/useChatCallbacks.ts
+++ b/frontend/src/features/chat/hooks/useChatCallbacks.ts
@@ import 行
-import { useCallback, useRef } from 'react'; // 2026-09-09 小欧 A1: 加useRef(任务内指纹去重Set) — 小欧-2026-09-09
+import { useCallback } from 'react';
@@ onStep 开头的指纹去重块 (约 :145-171)
-  // ---- A1(2026-09-09 小欧): 任务内步骤指纹去重 ----
-  const onStepFingerprintRef = useRef<Set<string>>(new Set());
-  const _dbgRoundRef = useRef(0); // [DEBUG-4] 轮次计数器
-
   const onStep = useCallback(
     (step: ExecutionStep) => {
-      // [DEBUG-4] 2026-09-09 执行路径追踪(轮次)
-      if (step.type === 'thought-start') _dbgRoundRef.current++;
-      console.log(`[DBG-4] → ${step.type}/R${_dbgRoundRef.current}`);
-      // A1(2026-09-09 小欧): 指纹去重——同 type|step|preview|content(前64) 事件视为重放/重复行跳过
-      const fingerprint = [
-        step.type,
-        step.step ?? '',
-        step.preview ? 'p' : '',
-        (step.content ?? '').slice(0, 64),
-      ].join('|');
-      if (onStepFingerprintRef.current.has(fingerprint)) {
-        return;
-      }
-      onStepFingerprintRef.current.add(fingerprint);
       // 【北京老陈 2026-07-12 小欧】统一取消语义：interrupted → cancelled
@@ onComplete 终态清理 (约 :531-532)
       streamingStepsRef.current = [];
       executionStepsRef.current = [];
-      // A1(2026-09-09 小欧): 终态清空任务内指纹去重Set, 供下一任务重新计数 — 小欧-2026-09-09
-      onStepFingerprintRef.current.clear();
     },
@@ onError 终态清理 (约 :667-668)
       streamingStepsRef.current = [];
       executionStepsRef.current = [];
-      // A1(2026-09-09 小欧): 终态清空任务内指纹去重Set, 供下一任务重新计数 — 小欧-2026-09-09
-      onStepFingerprintRef.current.clear();
@@ onError from_backend 分道 (约 :579-581)
         streamingStepsRef.current = [];
         executionStepsRef.current = [];
-        // A1(2026-09-09 小欧): from_backend 错误后会话终止, 同步清空任务内指纹Set
-        onStepFingerprintRef.current.clear();
         return;
```

**验证**：重连 GET after_seq=N 续传，后端重放 ≤N 的事件 → 入口 `rawData.seq <= lastSeqRef.current` 直接 return，不进 setExecutionSteps 也不进 onStep → 两源一致。合法 chunk（不同 seq）不受影响。

---

### P3 · onError(from_backend) 清同名 ref 失效（致命时序 bug）

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[部分采纳]**。问题**洞察对、理由偏**（文档25 3.1 P3："双 executionStepsRef 同名碰撞实锤，但'ref 渲染到 UI'非主因，主因是 onError(:578 清 useChatState 的) 与 onComplete(:517 读 useSSE 的) 操作不同 ref"）。问题分析已纳入文档25 **3.5.1.1 病根**（"清的对象与读的对象错位"）。方案**不采纳**：本 diff "from_backend 错误不清 executionStepsRef" 是留脏堵漏，不清会让历史步骤残留继续存在；文档25 **S2** 的正确解法是把五源收敛到同一 ref（清的对象=读的对象），onError/onComplete 操作同一个 useSSE ref，清空才真正生效。随 S2 一并根治。

**根因**：`useChatState.ts:259` 的 `executionStepsRef` 与 `useSSE.ts:384` 的 `executionStepsRef` **同名碰撞**。`useChatCallbacks` 解构的 `executionStepsRef` 来自 `useChatState`，而 `sseParser` 透传给 onComplete 的 `executionStepsFromSSE` 来自 `useSSE` 的 ref。`onError` 的 from_backend 分道清的是 useChatState 的 ref，useSSE 的没清 → 后续 final 的 onComplete 取 `executionStepsFromSSE`（useSSE ref，含全部历史）→ 历史 step 残入下任务。

**现状** `useChatCallbacks.ts:572-582`：
```ts
if (errorObj.from_backend === true) {
  streamingContentRef.current = '';
  streamingStepsRef.current = [];
  executionStepsRef.current = [];   // ← 清的是 useChatState 的, useSSE 的没清
  return;
}
```

**diff**（from_backend 错误不清 executionStepsRef——最终终态由随后 final 承担，清了反而让 final 拿到空）：

```diff
--- a/frontend/src/features/chat/hooks/useChatCallbacks.ts
+++ b/frontend/src/features/chat/hooks/useChatCallbacks.ts
@@ onError from_backend 分道
       if (errorObj.from_backend === true) {
         console.info(
           '[onError] 后端业务错误: 只进P3, 不弹窗/不替换消息/不停计时 (6.3.3)'
         );
         streamingContentRef.current = '';
         streamingStepsRef.current = [];
-        executionStepsRef.current = [];
-        // A1(2026-09-09 小欧): from_backend 错误后会话终止, 同步清空任务内指纹Set——查漏补洞:
-        //   防"错误后异常断链(无 final/无 onComplete)致 Set 残留, 下一任务同 step 同 content 被误拦" — 小欧-2026-09-09
-        onStepFingerprintRef.current.clear();
+        // P3 修复(小沈 2026-09-10): 不清 executionStepsRef——此处清的是 useChatState 的 ref,
+        //   而 onComplete 取的 executionStepsFromSSE 来自 useSSE 的 ref(同名碰撞), 清了无效且
+        //   让随后 final 拿到空 steps。最终终态由随后 final 承担, 保留 steps 供 final 完整透传。
         return;
       }
```

**验证**：from_backend 错误后 → 不清 executionStepsRef → 随后 final 的 onComplete 取 `executionStepsFromSSE`（useSSE ref，完整）→ PipelineRenderer 正常渲染全部步骤，无历史残留。

---

### P4 · 空闲超时竞态双流（高优先时序 bug）

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[未采纳·主体不成立]**。文档25 3.1 P4 判定"**夸大**"：①"链路被自己短路"不成立——实码复核 useSSE.ts:692 新 fetch 在 sendMessageInternal 后部独立建立，不会被本次 disconnect abort；②idle 假中断已由后端 25s 心跳化解（3.4：活连接每 25s read() 返回、60s idle 永不触发）。真实残留仅"disconnect abort 窗口期内旧流数据丢弃"竞态，已吸收为文档25 **S9**（abort 前 flush buffer，:651 与 :862-875 两处）。本 diff（idleTimeout 直接 abort 改法）**未采纳**，且不在三症状（重复/冻结/丢尾）之列。

**根因**：`useSSE.ts:744-765` idleTimeout 回调直接调 `reconnect()`，reconnect→sendMessageInternal→开头 `disconnect(false,...)`→设 `intentionalAbortRef=true`+abort controller。原 while 循环还阻塞在 `await reader.read()`，abort 触发 AbortError 进 catch，catch 里 `intentionalAbortRef.current===true` → **AbortError 被静默短路**（`:862-876`），重连链路被自己短路；同时 reconnect 的 setTimeout 又起新 fetch，旧 reader 抛 error 与新 fetch 短暂并存。

**diff**（空闲超时不经 reconnect/disconnect，直接 abort 让 reader 抛 error 进 catch 走正常 handleSSEError 重连，不设 intentionalAbortRef）：

```diff
--- a/frontend/src/hooks/useSSE.ts
+++ b/frontend/src/hooks/useSSE.ts
@@ readStream while 循环内 idleTimeout 回调 (约 :744-765)
         idleTimeoutRef.current = window.setTimeout(() => {
           // 2026-09-03 小欧 P1-3: HITL等待期暂停IDLE计时
           if (isHitlWaitingRef.current) return;
           const timeSinceLastData = Date.now() - lastDataTimeRef.current;
           if (timeSinceLastData >= IDLE_TIMEOUT && isReceivingRef.current) {
             console.warn(
               `[SSE] 空闲超时：已经${timeSinceLastData / 1000}秒未收到数据，判定连接断开`
             );
             console.warn('[SSE] ⏱️ 空闲超时触发上下文:', {
               timeSinceLastData,
               thresholdMs: IDLE_TIMEOUT,
               receiving: isReceivingRef.current,
               hitlWaiting: isHitlWaitingRef.current,
               nowIso: new Date().toISOString(),
             });
             onError?.('SSE 空闲超时：长时间未收到数据');
-            // 2026-08-29 小强 修复#26: 空闲超时走重连路径而非disconnect(true)绕过重连, 确保自动重连发生
-            reconnect();
+            // P4 修复(小沈 2026-09-10): 直接 abort 让 reader 抛 AbortError 进 catch 走统一
+            //   handleSSEError 重连。不经 disconnect(不设 intentionalAbortRef), 避免 AbortError
+            //   被静默短路导致重连链路自死; reader 抛错后 while 退出, 新 fetch 在 catch 重连中
+            //   发起, 无双流并存窗口。
+            if (abortControllerRef.current) {
+              try {
+                abortControllerRef.current.abort();
+              } catch (_e) {
+                /* ignore */
+              }
+            }
           }
         }, IDLE_TIMEOUT);
```

**验证**：60s 无数据 → abort → reader.read() 抛 AbortError → catch 里 `intentionalAbortRef.current===false`（未经 disconnect）→ 不进静默分支 → `handleSSEError` 正常分类重连。while 已退出无竞态。

---

### P5 · cancelInProgress 放行双 final（高优先时序 bug）

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[采纳]**。问题**真实**（文档25 3.1 P5 判定"真实"），已纳入文档25 **3.5.2.3**（取消行为"放行取消后的 completed 终态，final 帧数量不定"，:148）+ **Q5**（cancel 双 final）+ **Q5b**（指纹对 final 无区分）+ **S6**。方案与本 diff 一致：取消态只认 `final(cancelled)`，`final(completed)` 一律不放行（文档25 S6："取消态只认 final(cancelled) 与 error(cancelled) 帧"）。直接采纳，无修正。

**根因**：`useChatCallbacks.ts:184-190` 取消中放行 `isCancelEvent`(final+cancelled) 和 `step.type==='final'`(含 completed)。若先到 final(cancelled) 后到 final(completed)，**两个 final step 都入 executionSteps**。

**diff**（取消中只放行 final+cancelled，final+completed 也吞）：

```diff
--- a/frontend/src/features/chat/hooks/useChatCallbacks.ts
+++ b/frontend/src/features/chat/hooks/useChatCallbacks.ts
@@ onStep cancelInProgress 守卫 (约 :184-190)
       // ✅ 如果正在取消中，跳过非取消且与终态无关的事件（避免旧 chunk/步骤污染 UI）
-      // 2026-08-27 小欧 修复#1/10: 取消进行中允许 final 终态步骤通过, 否则 final(completed)被吞导致消息无内容
       if (cancelInProgressRef.current) {
-        if (!isCancelEvent && step.type !== 'final') {
+        // P5 修复(小沈 2026-09-10): 取消中只放行 final+cancelled, final+completed 亦吞。
+        //   取消语义下 completed 是竞态残留, 放行会导致两个 final step 同入列表。
+        if (!isCancelEvent) {
           console.log(`[取消] 忽略取消过程中收到的事件: ${step.type}`);
           return;
         }
-        // 是取消事件或 final 终态，继续处理（显示到 UI）
+        // 是取消终态 final+cancelled，继续处理（显示到 UI）
       }
```

**验证**：用户取消 → cancelInProgressRef=true → final(cancelled) 到达放行 → 若后续 final(completed) 残留到达被吞 → executionSteps 只有一个 final。

---

### P6 · onComplete "取较长者"补丁消除（高优先架构缺陷）

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[部分采纳]**。问题**真实、方案不彻底**（文档25 3.1 P6：按长度比较取源（useChatCallbacks.ts:454-459），重连重放混入旧源）。注意：**P6 问题是三份报告中唯一"3.5 清单级"分析缺口**，文档25 3.5 未单列该条（仅在叠加因素 :138 的 finalContent 兜底提及），其实体分析在第四章 **Q9**、终态由 **S7** 处理（:431"取长补丁消失"）。方案"无条件信 executionStepsFromSSE"**部分采纳**：方向（抛弃长度比较）与 S7 一致，但前提依赖 S2/S3（单一真源+seq 守卫）完成后两源才一致、才可无条件信权威源；且文档25 S7 以"统一缓冲 + 保留 `finalResponse || streamingContentRef` 兜底"更彻底，未照搬"只信 executionStepsFromSSE"。

**根因**：executionSteps 三源（useSSE state / message.executionSteps / executionStepsFromSSE），onComplete 做 `executionStepsFromSSE.length > lastMessage.executionSteps.length ? 前者 : 后者`。"较长"≠"正确"，重连重放时两源含不同/乱序 final，取较长者取到脏数据。P2 修复后 seq 去重保证两源一致，此处可无条件信权威源。

**diff**（无条件优先用 sseParser 传入的 executionStepsFromSSE，message 仅作 fallback）：

```diff
--- a/frontend/src/features/chat/hooks/useChatCallbacks.ts
+++ b/frontend/src/features/chat/hooks/useChatCallbacks.ts
@@ onComplete 内 finalSteps 计算 (约 :452-459)
-          // 2026-09-09 小欧 bug-2修复: sseParser final分支在调onComplete前已将final step追加到ref(:499-500),
-          //   并作为executionStepsFromSSE(:520)传入; 此处优先用它(含final), 防React批处理prev旧态覆盖
-          const finalSteps =
-            executionStepsFromSSE &&
-            executionStepsFromSSE.length >
-              (lastMessage.executionSteps?.length || 0)
-              ? executionStepsFromSSE
-              : lastMessage.executionSteps || [];
+          // P6 修复(小沈 2026-09-10): 无条件信 sseParser 传入的 executionStepsFromSSE(权威源,
+          //   sseParser final 分支 :498-500 已更新 ref 含 final, :520 透传)。P2 seq 去重后
+          //   两源一致, 不再做长度比较(较长≠正确, 重连重放取到脏数据)。message 仅作 fallback。
+          const finalSteps =
+            executionStepsFromSSE ?? lastMessage.executionSteps ?? [];
```

**验证**：final 到达 → sseParser 更新 ref 含 final → 透传 executionStepsFromSSE → onComplete 直接用，含 final step。React 批处理 prev 旧态不再影响（不读 lastMessage.executionSteps 做比较）。

---

### P7 · setExecutionSteps updater 纯函数化（中优先架构缺陷）

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[采纳]**。问题**真实**（文档25 3.1 P7 判定"真实"，sseParser 六分支 updater 内写 ref+持久化），已纳入文档25 **3.5.2.2**（React updater 回调内部执行副作用）+ **Q3** + **S4**。方案方向一致：副作用移出 updater（此处为 ref 权威 + 非 updater setExecutionSteps；文档25 S4 为纯 updater `appendSteps(prev, steps)` + useSSE 层 useEffect 300ms 防抖持久化），两者均根治 StrictMode 双调用。执行细则：**final 分支需保 ref 先行**（onComplete 读全量）两种写法都已保留，一致。补充：appendStep 仍是 `[...ref, s]` O(n) 整数组复制，**不解症状②冻结**（冻结主因 = 文档25 Q12 双路全量序列化 + 同步 setItem，S12 根治），本 P 只做副作用归位。

**根因**：`sseParser.ts` 多处在 `setExecutionSteps((prev) => { ref 写; saveStepsToStorage; return newSteps; })` updater 内做副作用。React 18 严格模式 updater 调两次、concurrent rendering 下 updater 可能在 render 期间执行污染并发 render。

**方案**：提取 `appendStep` helper，用 ref 当权威源算 newSteps，`setExecutionSteps(newSteps)`（非 updater 形式），副作用在 setExecutionSteps 外。一处定义，替换 thought/chunk/action/observation/paused/resumed/retrying/thought-start 全部分支。

**diff**（sseParser.ts）：

```diff
--- a/frontend/src/features/chat/services/sseParser.ts
+++ b/frontend/src/features/chat/services/sseParser.ts
@@ processSSEData 内, switch 之前 (约 :209 switch 上方)
+    // P7 修复(小沈 2026-09-10): 集中 appendStep——以 executionStepsRef 为权威源算 newSteps,
+    //   setExecutionSteps 用非 updater 形式(纯值传入), 副作用(ref 写/save)在 set 外,
+    //   不违反 React updater 必须纯函数的契约。替代各分支 updater 内写 ref+save 的重复副作用。
+    const appendStep = (s: ExecutionStep) => {
+      const newSteps = [...handlers.executionStepsRef.current, s];
+      handlers.executionStepsRef.current = newSteps;
+      setExecutionSteps(newSteps);
+      try {
+        saveStepsToStorage?.(newSteps);
+      } catch (e) {
+        console.warn('[SSE] sessionStorage 保存失败，可能容量不足:', e);
+      }
+    };
+
     switch (rawData.type) {
@@ thought-start 分支 (约 :255-260)
         setExecutionSteps((prev) => {
           const next = [...prev, ts];
           handlers.executionStepsRef.current = next;
-          // 2026-08-27 小欧 三堂会审: thought-start为瞬时光标信号, 仅内存帧不落sessionStorage, 故无持久化逻辑
           return next;
         });
+        // P7: thought-start 为瞬时光标, 用 appendStep 但不落库——直接 ref+state, 跳过 save
+        handlers.executionStepsRef.current = [...handlers.executionStepsRef.current, ts];
+        setExecutionSteps(handlers.executionStepsRef.current);
```

> thought-start 不落 sessionStorage，单独处理（直接 ref+state，不调 appendStep 的 save）。其余 thought/chunk/action/observation/paused/resumed/retrying 统一改用 `appendStep(step)`。

**thought 分支示例**（`:380-390`）：

```diff
-        setExecutionSteps((prev) => {
-          const newSteps = [...prev, step];
-          handlers.executionStepsRef.current = newSteps;
-          // 2026-09-09 小欧: 防抖已在useSSE内部处理, 此处直接调用, 去掉setTimeout(0)包裹
-          try {
-            saveStepsToStorage?.(newSteps);
-          } catch (e) {
-            console.warn('[SSE] sessionStorage 保存失败，可能容量不足:', e);
-          }
-          return newSteps;
-        });
+        appendStep(step);
```

**chunk / action / observation / paused/resumed/retrying 分支同改 `appendStep(step)`**（各自删掉 `setExecutionSteps((prev) => {...})` 整块，替换为 `appendStep(step);`）。

**final 分支**（`:498-511`）特殊：需在 onComplete 前更新 ref，保留显式写 ref + setExecutionSteps，但去掉 updater 形式：

```diff
-        const updatedSteps = [...handlers.executionStepsRef.current, step];
-        handlers.executionStepsRef.current = updatedSteps;
-
-        setExecutionSteps((prev) => {
-          const newSteps = [...prev, step];
-          try {
-            saveStepsToStorage?.(newSteps);
-          } catch (e) {
-            console.warn('[SSE] sessionStorage 保存失败，可能容量不足:', e);
-          }
-          return newSteps;
-        });
+        // P7: final 用 appendStep 统一更新(ref 已含 final 供 onComplete :517 读取)
+        appendStep(step);
```

**验证**：`npm run check`（tsc + eslint）通过；React 18 严格模式 updater 不再被二次执行副作用；executionStepsRef 与 executionSteps state 同步（appendStep 内先写 ref 再 set）。

---

### P8 · 双缓冲混用消除（中优先架构缺陷）

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[部分采纳]**。问题**真实**（responseBufferRef:sseParser.ts:401 vs streamingContentRef:useChatCallbacks.ts:313 双累积），已纳入文档25 **3.5.2.1 叠加因素之二** + **Q7**（双缓冲分裂）。方案**方向相反，不采纳**：本 diff 选 responseBufferRef 为权威（`finalContent = finalResponse`）并删除 streamingContentRef 回退；文档25 **S7 相反**——删除 responseBufferRef、以 streamingContentRef 为唯一累积缓冲并**补暂停期间累积**、保留 `finalResponse || streamingContentRef.current` 兜底（不再比长）。本 diff 删兜底会让 finalResponse 缺失时丢内容，风险不为文档25 采纳。随 S7 实施。

**根因**：`responseBufferRef`（useSSE，onComplete 的 fullResponse 源）与 `streamingContentRef`（useChatState，onChunk 实时显示源）各自累积同一份 chunk。暂停时 onChunk 走缓冲不累积 streamingContentRef，但 responseBufferRef 仍累积 → 两源不一致。onComplete 混用 `finalResponse || streamingContentRef.current`。

**diff**（onComplete 只信 sseParser 传入的 fullResponse，删 streamingContentRef 回退）：

```diff
--- a/frontend/src/features/chat/hooks/useChatCallbacks.ts
+++ b/frontend/src/features/chat/hooks/useChatCallbacks.ts
@@ onComplete 内 finalContent (约 :451)
-      // 2026-08-27 小欧 修复#6: 优先用服务端最终 fullResponse(含暂停期间缓冲分块), 避免暂停分块因 streamingContentRef 未累积而丢失
-      const finalContent = finalResponse || streamingContentRef.current;
+      // P8 修复(小沈 2026-09-10): 只信 sseParser 传入的 fullResponse(responseBufferRef 累积,
+      //   含暂停期间 chunk——sseParser chunk 分支无条件累积 responseBufferRef, 不受 onPaused 影响)。
+      //   streamingContentRef 仅用于 onChunk 实时显示, 职责分离, 不在终态混用。
+      const finalContent = finalResponse;
@@ onComplete 末条非 assistant 新建消息 (约 :490)
         const newAssistantMessage: Message = {
           id: (Date.now() + 1).toString(),
           role: 'assistant',
-          content: finalResponse || streamingContentRef.current,
+          content: finalResponse,
           isStreaming: false,
```

**验证**：暂停期间 chunk → sseParser 仍累积 responseBufferRef（`:401` 无条件 `+=`）→ final 的 fullResponse 完整 → onComplete 正确显示。streamingContentRef 仅 onChunk 实时用，暂停时不累积不影响终态。

---

### P9 · 删 enabled 死代码（低优先）

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[采纳]**。问题**真实**（useSSE.ts:597-601 设 enabled 从未读，canRetry 走 errorConfig.retryable:217-218），已纳入文档25 **3.5.2.4**（死代码与语义失真的配置）+ **Q11** + **S1**。方案一致（手动断开靠 pendingMessageRef=null 阻止重连，enabled 删除无行为变化），可直接落地。

**根因**：`useSSE.ts:597-601` 设 `reconnectConfigRef.current.enabled = false`，3 秒后恢复 true。但全文件 `enabled` **从未被读**（`handleSSEError` 的 `canRetry` 只看 `errorConfig.retryable && attempts < max`，不读 enabled）。手动断开实际靠 `pendingMessageRef.current = null`。enabled 是误导性死代码。

**diff**：

```diff
--- a/frontend/src/hooks/useSSE.ts
+++ b/frontend/src/hooks/useSSE.ts
@@ disconnect 内 manualDisconnect 分支 (约 :595-602)
       // 手动中断时清除 pendingMessage 并阻止重连
       if (manualDisconnect) {
         pendingMessageRef.current = null;
-        reconnectConfigRef.current.enabled = false;
-        // 3秒后恢复重连功能（避免永久禁用）
-        setTimeout(() => {
-          reconnectConfigRef.current.enabled = true;
-        }, 3000);
+        // P9 修复(小沈 2026-09-10): 删 enabled 死代码——全文件从未读 enabled,
+        //   canRetry 以 errorConfig.retryable && attempts<max 为准。手动断开靠
+        //   pendingMessageRef=null(reconnect 入口 if(!pendingMessageRef) return)。
       }
```

**验证**：手动停止 → pendingMessageRef=null → reconnect 入口 `if (!pendingMessageRef.current) return` 阻止重连。enabled 删除后无行为变化。

---

## 四、实施顺序与依赖

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】[部分采纳]**。总纪律采纳：分批实施+逐批回归（`npm run check` / `npm run test`）、严禁批量合入，与文档25 第八章"每次改动一条、跑对应 T 用例全绿再进下一条"一致。**排序须修正**，按文档25 第八章优先级：**S3(重复) ≥ S13(丢尾) ≥ S12(冻结)，先解决用户三症状再动质量项**——
>
> 1. **P2（seq 守卫）应提到最高优先**：它是症状①重复的核心根因，文档25 排 S3 为第一优先级；本文中放在第二批（依赖 P1/P3/P5/P9）顺序偏低。
> 2. **P4 项从批次中移除**：已判定主体不成立（P4 节标注），仅残留窗口 S9 保留。
> 3. **P1/P3/P5/P9 = 文档25 S5/S2/S6/S1 顺序的原则一致**（低风险先行），但 P3 的落地按 S2 而非本文 diff；P6/P8 依 S7、且须在 S2/S3 之后（文档25 顺序第 7 步），与本文"第三批（P2 完成后）"前提一致。

```
第一批(独立, 可并行):
  P1  useChatCallbacks.ts onResumed
  P3  useChatCallbacks.ts onError from_backend
  P4  useSSE.ts idleTimeout
  P5  useChatCallbacks.ts cancelInProgress
  P9  useSSE.ts disconnect

第二批(P2 是 P6 前提):
  P2  sseParser.ts + useSSE.ts + useChatCallbacks.ts  seq 去重上移
  P7  sseParser.ts  appendStep 提取

第三批(P2 完成后):
  P6  useChatCallbacks.ts onComplete  删取较长者补丁
  P8  useChatCallbacks.ts onComplete  删 streamingContentRef 回退
```

每批完成后执行验证：
```bash
# frontend/
npm run check      # tsc + eslint
npm run test       # vitest
```

重点回归用例（若有）：
- 暂停/恢复期间产生 error → 验证 P1（error 不重复回放）
- 断线重连 after_seq 续传 → 验证 P2（无重复 step）
- 后端业务错误后 final → 验证 P3（steps 完整）
- 60s 空闲超时 → 验证 P4（正常重连不被静默短路）
- 用户取消后 final(completed) 残留 → 验证 P5（无双 final）

---

## 五、未列入本方案的后续项

> **【文档[25] 采纳标注 v1.1｜小欧 2026-09-10 11:29:23】三项逐条判定：**
>
> 1. **executionSteps 彻底单一源（全以 message 为源）→ [不采纳·方向相反]**。问题的"多源"部分已入文档25 **3.5.1.1** + **S2**；但文档23 提议"onStep 不维护 useSSE state、全在 message"与文档25 S2 **相反**——S2 以 useSSE `executionSteps` state+ref 为唯一可写权威、message.executionSteps / sessionStorage / REST 全部降级只读快照。以 message 为权威会把它变成"渲染层既当显示又当真源"，且冲击 persistence/lifecycle/scroll 既有读取方，效应面更大，不采纳。
> 2. **metaFrames 高频 re-render → [未收录]**。性能项非 bug，文档25 未将其列为问题（3.5 清单与 Q 清单均无），也不在用户三症状机制内。维持现状，不做独立立项。
> 3. **handlers 对象循环内重建 → [未收录]**。GC 压力微优化，文档25 未采纳（未入 3.5/方案；3.5.2.5 的"handlers 20+ 字段直透"是职责问题而非性能问题）。维持现状；若后续做 6.1 函数级提取（文档25 3.6.1）时顺带评估提至循环外。（需单独评估）

| 项 | 说明 | 风险 |
|----|------|------|
| executionSteps 彻底单一源 | onStep 不维护 useSSE state，全在 message；删 useChatScroll 同步点 | 影响面极大（persistence/lifecycle/scroll 均读 ref），需单独排期 |
| metaFrames 高频 re-render | usage/stats 每轮 setMetaFrames 触发 facade re-render | 性能项，非 bug，可用 ref+手动触发优化 |
| handlers 对象循环内重建 | for(line) 每行构造 20+ 字段 handlers 对象 | GC 压力，可提至循环外闭包构造一次 |

本方案 9 项均为**确凿 bug 或确凿缺陷**，diff 基于真实代码逐行核对，可直接手动实施。

---

> 编写: 小沈  2026-09-10
> 复核要求: 实施前熟读对应文件 3 遍，每个 hunk 落盘后复核 5 遍，三堂会审（合规/合理/关联逻辑）