# 信任机制现状分析与前后端流程链条问题分析 - 小欧-2026-09-16

**创建时间**: 2026-09-16 03:43:28
**更新时间**: 2026-09-16 06:39:47

**版本历史**:

| 版本 | 时间 | 编写人 | 更新要点 |
|------|------|--------|---------|
| v0.1 | 2026-09-16 03:43:28 | 小欧 | 初稿: 基于代码核查的信任机制现状分析 + 前后端全链路梳理与问题点 |
| v0.2 | 2026-09-16 04:05:44 | 小欧 | 回答老陈"自动确认为何不进信任列表": 补 v1.5 定案历史与恒空四环节链条; 修正"bypass 不弹窗"表述错误; 新增实证问题A/B/C |
| v0.3 | 2026-09-16 04:44:32 | 小欧 | 核查前端信任全链路, 补充前端3处协同/呈现缺陷 |
| v0.4 | 2026-09-16 05:03:47 | 小欧 | 三思三省复核: 问题4定性修正为"设计有意留白待定"; 结论降级; 问题2补充完整调用链 |
| v0.5 | 2026-09-16 05:15:41 | 小欧 | 全文重写: 严格区分"确凿问题"与"已确认非问题"; 非问题不再冠以"问题N"编号, 归入独立小节; 结构梳理为现状/问题/非问题/前端缺陷/结论五段 |
| v0.6 | 2026-09-16 05:27:00 | 小欧 | 新增第三章问题链路图解: 问题A落库/查询不对称(当前错误+正确设计)、问题C信任豁免vs沙箱(当前链路+三选项决策); 原第三/四章顺移 |
| v0.7 | 2026-09-16 05:45:00 | 小欧 | 新增第四章修改方法及策略 + 第五章代码修改实施diff(基于本地最新代码, 精准diff): 问题A+B后端3文件 + 前端3文件协同修正 |
| v0.8 | 2026-09-16 05:55:00 | 小欧 | 问题C决策落地: 北京老陈选定方案A(信任核心=工具+路径, 只跳确认弹框); 修正4.3决策状态 + 5.8文案精准化 |
| v0.9 | 2026-09-16 06:05:00 | 小欧 | 三思三省复核修复: ①5.6/5.7 regex bug(Windows反斜杠路径被误拒)→改^[A-Za-z]:[\\\/] ②DRY: _NON_FILE_TOOLS改为复用trust.py常量 ③5.4标题误导修正 |
| v1.0 | 2026-09-16 06:10:21 | 小欧 | 三思三省10遍核验重构第五章: ①5.1改extract_trust_path专用提取(不动_parse_paths保护conflict_detector并查集分组; 排除data→value非路径别名污染; 消除两分支DRY) ②NON_FILE_TRUST_TOOLS常量迁tool_constants单一来源(trust/trust_db/storage三方共用) ③删5.5冗余透传(YAGNI, insert/check已有tool_name直接复用, 不新增参数) ④新增storage.py撤销一致性同步(原漏, 防registry撤消失配) ⑤删5.6/5.7前端虚假校验(垃圾路径带盘符前端无法识别, 诚实不设; 根治在后端) ⑥原5.8文案保留顺移为5.7, 5.8改前端缺陷②核查结论(无代码改动+存量数据清理建议) |
| v1.1 | 2026-09-16 06:19:57 | 小欧 | 10遍核查一致性修订: ①4.1标题"后端4文件"→"后端5文件"(表内实列5文件: tool_constants/trust/trust_db/storage/hitl_gateway, 原漏计) ②2.4与3.2.1图"executor.py"引用补全为"sandbox/executor.py:295-298"(消除与tool_executor.py歧义, 行号已实证=registry静态分析恒passed分支) ③全文90+处源码行号与本地最新代码逐行复核一致(config运行值enabled:False/auto_confirm_delay:6/hitl_timeout:120与1.1吻合; PARAM_ALIASES registry_write data→value污染实证; 双份_norm_trust_path位置trust_db:17/storage:560实证; 无残留v0.9旧方案名词矛盾) |
| v1.2 | 2026-09-16 06:29:56 | 小欧 | 新增第六章"基于TDD的实施步骤": ①6.1测试资产盘点总览(修改=test_trust.py; 新增=test_t1~t5共5文件; 保持=7文件) ②6.2~6.6后端五步TDD(红→绿→回归, 每步含case名+目标行为断言+红阶段失败锚点+对应diff节号) ③6.7前端手动验证清单(实证frontend无组件单测基建, 不建DOM mock, YAGNI) ④6.8回归守护(保持清单必须全绿) ⑤6.9纪律(测试文件禁commit/空过不算绿/绿后全量) |
| v1.3 | 2026-09-16 06:39:47 | 小欧 | 10遍TDD复核修正3处: ①6.3的3个case(data_value_excluded/no_path/params_none)明确标注为"守护case"(当前已返回目标值None, 断言空过不是红, 防误解) ②6.3的test_registry_write_data_value_excluded红阶段描述修正(原写"空过→双保险"逻辑混乱, 改为说明"守护case: 验证data→value别名不污染路径提取") ③6.6的test_hitl_confirm_spec_path_priority红阶段描述精确化(明确"当前hitl_confirm调用_resolve_trust_path行71而非直接调用extract_trust_path, mock断言直接调用即失败") |

---

## 一、现状分析

### 1.1 当前运行模式: 自动确认(bypass)模式

实际 `config/config.yaml`:

```yaml
security:
  auto_confirm_delay: 6
  hitl_timeout: 120
  enabled: false      # ← 当前 false = bypass 自动确认模式
```

- `security.enabled: false` → `_is_skip_safety()` 返回 True(`tool_safety_checker.py:93-98`)。
- 本需确认的工具调用走 bypass 自动确认: **仍弹窗**(倒计时自动放行), `auto_confirm=True`(`tool_safety_checker.py:158-167`): paused 照发, 前端倒计时(默认 4s)到期自动 allow(`safety_gate.py:96-113`); 用户仍可在倒计时窗口内手动拒绝。
- **危险防护不受影响**: 路径越权/写入大小保护/系统禁区/delete R6 等无条件硬拦(`tool_safety_checker.py:131-155`)。

### 1.2 信任名单的对象: 工具名 + 路径(tool + path)

核心定案(2026-09-02 北京老陈), 落库表 `chat_session_trust(session_id, tool_name, path)`:

| 维度 | 说明 | 代码出处 |
|------|------|---------|
| tool_name | 规范化工具名(normalize_tool_name) | `trust.py:18,118` |
| path | 从调用参数提取的目标路径; 仅路径类参数参与, 非路径参数不进信任 | `trust.py:66-74` |
| path=NULL | 无路径参数工具 → 工具级通配行(整工具豁免) | `trust_db.py:27-34` |
| 豁免粒度 | 信任根 == 目标 / 是目标的祖先目录(前缀递归) | `trust_db.py:36-58` |

### 1.3 信任的产生渠道(唯一主通道)

```
前端确认(勾 trust_session=True)
  → POST /api/.../confirm  (chat_routes.py:89-96)
  → resolve_confirmation(confirm_id, confirmed=True, trust_session=True)  (hitl_confirmation.py:158-197)
  → save_session_trust(task_id, tool_name, entry.path)  (trust.py:106-120)
  → insert_session_trust(session_id, 规范化工具名, _norm_trust_path(path))  (trust_db.py:27-34)
```

防污染: bypass 模式下前端强制 `trustSession=false`(勾了也不落库), `index.tsx:152-158`。

> **v1.5 历史定案(bypass 不产生信任)**:
> 1. **确实曾经落库**。v1.5 前 bypass 无条件 `trust_session=True`, 每个自动放行工具都落信任——老陈记的"自动确认加入信任列表"是真实旧行为。
> 2. **2026-09-02 v1.5 定案移除**(`会话信任功能修复方案-小欧-2026-09-02.md` 5.4): bypass 直放不产生信任。理由: 清单污染(含用户从未同意的工具) + 长期豁免盲区(自动信任不过期, 切回 enabled:true 后仍命中)。
> 3. **恒空四环节**: ① `security.enabled: false` → bypass; ② 前端强制 `trustSession=false`; ③ `hitl_confirmation.py:187` 永不触发 `save_session_trust`; ④ bypass 分支仅 `grant_temp_auth` 不落表。
> 4. **结论**: 信任列表恒空是 v1.5 定案后的正确预期, 非 bug。

### 1.4 信任的消费(豁免判定)链路

```
safety_gate.check_safety_and_confirm(每 call)
  → resolve_skip(agent_task_id, tool_name, params)      # trust.py:81-99
  → check_session_trust(session_id, 规范化名, path)     # trust_db.py:36-58
  → check_before_execute(skip_confirmation=_skip)        # 豁免只跳确认不跳危险防护
```

豁免语义: `skip_confirmation=True` 只跳过确认询问, 已知风险/系统禁区仍 blocked。

### 1.5 信任的撤销

`DELETE /sessions/{id}/trust/{tool_name}?path=xxx` — path 传入精确删, 空则删通配行, 无匹配 404。

---

## 二、确凿问题(需修复)

### 2.1 全链路总览

```
[后端] safety_gate.check_safety_and_confirm
   │  ① trust.resolve_skip(会话信任预查)
   │  ② tool_safety_checker.check_before_execute(skip_confirmation)
   │        ├─ blocked → error 事件
   │        ├─ requires_confirmation → ③ hitl_gateway.hitl_confirm
   │        │      ├─ paused 事件(trust_path/auto_confirm/confirm_timeout/backend_timeout)
   │        │      ├─ wait_for_confirmation_result
   │        │      └─ resolve_confirmation 单点收口
   │        └─ 豁免/safe → sandbox 汇合 + grant_temp_auth
   │  ④ resolve_confirmation → confirmed+trust_session → save_session_trust
[前端] SSE paused → sseParser → useAuthorization → AuthorizationModal
   ├─ 用户点允许/勾信任 → confirm API(trust_session)
   ├─ 成功 → omni-trust-changed → TrustPanel 刷新
   └─ TrustPanel 查询/撤销
```

### 2.2 问题A: 落库/查询路径提取不对称 → 非文件工具路径级信任永不生效(实施错误)

**根因**: 落库侧 `_resolve_trust_path`(`hitl_gateway.py:36`) 复用了 sandbox 判断越界写意图的 6 键回落扫描, 但查询侧 `_parse_paths`(`trust.py:31-43`) 没有对应逻辑, 仅对 `FILE_OPERATION_TOOLS`/窗口工具提取 → **两侧不对称(DRY 违反)**。

**落库侧完整链路**(逐行验证):
1. `safety_gate.py:98`: `ConfirmSpec` 未传 `path` → `spec.path=None`
2. `hitl_gateway.py:71`: `_path = spec.path or await _resolve_trust_path(...)` → 走回落
3. `hitl_gateway.py:33`: `extract_trust_path` → registry_write 不在 `FILE_OPERATION_TOOLS` → None
4. `hitl_gateway.py:36`: **回落扫描** `("path","file_path",...)` → `params.get("path")` = `"HKCU\Software\X"` → 返回
5. `hitl_confirmation.py:107-108`: `create_confirmation` 存储 `entry.path = "HKCU\Software\X"`
6. `hitl_confirmation.py:190`: `resolve_confirmation` → `save_session_trust(task_id, "registry_write", entry.path)`
7. `trust_db.py:32`: `_norm_trust_path("HKCU\Software\X")` → `Path("HKCU\Software\X").resolve()` → **`F:\OmniAgentAs-repair\HKCU\Software\X`**(垃圾路径)入库

**查询侧完整链路**(逐行验证):
1. `safety_gate.py:71`: `_skip = await resolve_skip(...)`
2. `trust.py:93`: `_tgt = extract_trust_path("registry_write", params)` → `_parse_paths` 返回空集(None)
3. `trust_db.py:45-46`: `path is None` → 只查 `path IS NULL` 行
4. 落库的行 path = `"F:\...\HKCU\Software\X"`(非 NULL) → **永不命中**

**受影响工具**(三者共同特征: schema 参数名=`path` + `needs_confirmation=True` + 不在 `FILE_OPERATION_TOOLS`):

| 工具 | Schema出处 | 注册出处 | 症状 |
|------|-----------|---------|------|
| registry_write | `win_registry_schema.py:59` | `win_registry_register.py:70,91` | 路径级信任永不生效 + 垃圾路径入库 |
| registry_delete | `win_registry_schema.py:87` | `win_registry_register.py:70,91` | 同上 |
| execute_sql | `dataanalysis_schema.py:46` | `dataanalysis_register.py:135` | 路径级信任永不生效(路径本身正确但查询不命中) |

shell 类(参数 command/cwd 无 6 键)不受影响 → 工具级通配正常。

### 2.3 问题B: `_norm_trust_path` 对注册表键路径 `Path.resolve()` → 垃圾绝对路径入库(问题A的伴生症状)

`trust_db.py:17-24`: `_norm_trust_path` 对任意 path 做 `str(Path(path).resolve())`。

实测(`E:\Appsw\python31311\python.exe`): `Path(r"HKCU\Software\MyApp").resolve()` → `F:\OmniAgentAs-repair\HKCU\Software\MyApp` — 注册表键被当项目目录下相对文件路径解析。

组合问题A: registry 工具若真 HITL 勾选信任, 落库的是臆造的垃圾路径; 即便查询侧修复统一, 该路径也永远匹配不上真实注册表键。

建议: registry 类工具排除出文件路径信任域; `_norm_trust_path` 不应对非文件路径做文件系统 resolve。

### 2.4 问题C: 信任豁免 vs 沙箱 needs_ruling — 设计有意但产品语义未对齐(留白待定)

**代码链路**:
1. `tool_safety_checker.py:188-194`: skip_confirmation 分支 → `SafetyResult(sandbox_required=_needs)`, **auto_confirm 未设(默认 False)**
2. `safety_gate.py:168`(插入点③): `run_sandbox_gate` 缺省 `main_confirmed=False`
3. `sandbox_gate.py:88`: `pre.needs_ruling and safety_result.auto_confirm` → False → 不直通
4. `sandbox_gate.py:85`: `main_confirmed and pre.needs_ruling` → False → 不直通
5. → `sandbox_gate.py:101-113`: needs_ruling → 再弹 HITL

**但这是有意设计**: `tool_safety_checker.py:189` 注释"v1.6 会审F1: 信任豁免只跳确认不跳沙箱", 安全方向正确。

**触发面极窄**:
- registry 工具: `sandbox/executor.py:295-298` 静态分析始终 passed → 永不触发;
- delete/copy/move: 正常影子副本预演 → passed → 不弹; 仅源不存在/预检超时/环境性失败等边缘场景 → 二次弹。

**已决策(2026-09-16)**: 方案A — 信任核心=工具+路径, 信任只跳确认弹框, 沙箱未完成验证时仍需用户裁决。仅修正文案。

### 2.5 前端协同/呈现缺陷(3处)

> 前述问题A/B/C 的根因均在后端, 前端无法自行修复。但前端有 3 处协同/呈现缺陷:

**缺陷①(最重要, 老陈困惑的人因来源): bypass 弹窗勾选框静默失效无提示**
- `AuthorizationModal/index.tsx:296-310`: 勾选框始终渲染、可勾选;
- `:154-160`(handleConfirm): 对 `isBypass` 强制 `trustSession=false`;
- `isBypass` 前端已知(`:108`), 却不隐藏/禁用勾选框、不提示;
- 正是老陈"记着自动确认会进信任列表、列表却恒空"的 UI 误导来源。
- **建议**: bypass 模式下禁用勾选框并展示说明文案。

**缺陷②: trust_path 无校验原样展示**
- `index.tsx:301-303`: tooltip 显示 `{toolName} › {trustPath}，含子目录`;
- `TrustPanel.tsx:178`: 显示 `{path ?? '全局'}`, 撤销原样回传(`task.api.ts:227-238`);
- 后端垃圾路径原样展示给用户, 前端零校验。

**缺陷③: "含子目录"文案对非文件工具语义错误**
- `index.tsx:303`: 对 registry_write 等注册表键工具, "子目录"是文件系统语义, 注册表键层级 ≠ 文件目录树。
- **建议**: 按工具域区分文案。

---

## 三、问题链路图解与正确设计

### 3.1 问题A: 落库/查询路径提取不对称(图解)

#### 3.1.1 当前错误链路(根因: 落库侧有回落扫描, 查询侧没有)

```
                    ┌─────────────────────────────────────────────────────┐
                    │          安全网关入口(safety_gate.py:71)             │
                    │          工具: registry_write                       │
                    │          参数: path="HKCU\Software\X"              │
                    └───────────────┬──────────────────┬──────────────────┘
                                    │                  │
                    ┌───────────────▼──────┐  ┌────────▼─────────────────┐
                    │  落库侧(存储路径)      │  │  查询侧(豁免判定)         │
                    │  hitl_gateway.py:71   │  │  trust.py:93            │
                    │  _resolve_trust_path  │  │  extract_trust_path     │
                    └───────────────┬──────┘  └────────┬─────────────────┘
                                    │                  │
                    ┌───────────────▼──────┐  ┌────────▼─────────────────┐
                    │ extract_trust_path   │  │ _parse_paths             │
                    │ 不在FILE_OPERATION_   │  │ 不在FILE_OPERATION_      │
                    │ TOOLS → None         │  │ TOOLS → 空集 → None     │
                    └───────────────┬──────┘  └────────┬─────────────────┘
                                    │                  │
                    ┌───────────────▼──────┐  ┌────────▼─────────────────┐
                    │ ★ 回落扫描(BUG根因)   │  │ check_session_trust     │
                    │ hitl_gateway.py:36   │  │ trust_db.py:45-46       │
                    │ 6键 ("path",...)     │  │ path is None            │
                    │ params.get("path")   │  │ → 只查 path IS NULL 行  │
                    │ = "HKCU\Software\X" │  └────────┬─────────────────┘
                    │ ✅ 有值, 返回         │           │
                    └───────────────┬──────┘           │
                                    │                  │
                    ┌───────────────▼──────┐  ┌────────▼─────────────────┐
                    │ create_confirmation  │  │ 数据库查询结果             │
                    │ hitl_confirmation.py │  │ WHERE path IS NULL      │
                    │ :107-108             │  │                         │
                    │ entry.path =         │  │ 落库行 path =            │
                    │ "HKCU\Software\X"   │  │ "F:\...\HKCU\...\X"    │
                    └───────────────┬──────┘  │ (非 NULL)               │
                                    │         │ → 永不命中 ❌            │
                    ┌───────────────▼──────┐  └─────────────────────────┘
                    │ 用户确认(勾trust)     │
                    │ → resolve_confirm    │
                    │ → save_session_trust │
                    └───────────────┬──────┘
                                    │
                    ┌───────────────▼──────┐
                    │ insert_session_trust │
                    │ trust_db.py:32       │
                    │ _norm_trust_path     │
                    │ Path().resolve()     │
                    │ → "F:\...\HKCU\     │
                    │    Software\X"       │  ← 垃圾路径入库 ❌
                    └──────────────────────┘
```

**错误节点标注**:

| 节点 | 位置 | 错误类型 | 说明 |
|------|------|---------|------|
| ★ 回落扫描 | `hitl_gateway.py:36` | **根因** | 6键回落是为 sandbox 判断越界写意图复用, 不适用于信任路径提取 |
| `Path().resolve()` | `trust_db.py:17-24` | 伴生症状 | 注册表键被当文件路径解析, 产生垃圾绝对路径 |
| `path is None 查询` | `trust_db.py:45-46` | 不对称 | 查询侧无回落扫描, 路径级信任永远查不到 |

#### 3.1.2 正确设计(统一路径提取, 消除不对称)

```
                    ┌─────────────────────────────────────────────────────┐
                    │          安全网关入口(safety_gate.py:71)             │
                    │          工具: registry_write                       │
                    │          参数: path="HKCU\Software\X"              │
                    └───────────────┬──────────────────┬──────────────────┘
                                    │                  │
                    ┌───────────────▼──────┐  ┌────────▼─────────────────┐
                    │  落库侧(存储路径)      │  │  查询侧(豁免判定)         │
                    │  hitl_gateway.py:71   │  │  trust.py:93            │
                    └───────────────┬──────┘  └────────┬─────────────────┘
                                    │                  │
                    ┌───────────────▼──────┐  ┌────────▼─────────────────┐
                    │  extract_trust_path  │  │  extract_trust_path      │
                    │  (统一实现)           │  │  (同一函数)              │
                    └───────────────┬──────┘  └────────┬─────────────────┘
                                    │                  │
                    ┌───────────────▼──────┐  ┌────────▼─────────────────┐
                    │  extract_trust_path │  │  extract_trust_path      │
                    │  按工具域提取路径:    │  │  按工具域提取(同一函数):  │
                    │  - FILE 域 → 复用    │  │  - FILE 域 → 复用        │
                    │    _parse_paths     │  │    _parse_paths          │
                    │  - registry/sql →   │  │  - registry/sql →        │
                    │    专用提取(5.2)     │  │    专用提取(5.2)          │
                    └───────────────┬──────┘  └────────┬─────────────────┘
                                    │                  │
                    ┌───────────────▼──────┐  ┌────────▼─────────────────┐
                    │  _norm_trust_path   │  │  check_session_trust     │
                    │  按工具域规范化:       │  │  用同样的规范化路径查询   │
                    │  - FILE → resolve() │  │  → 路径匹配一致 ✅       │
                    │  - registry → 原样   │  └──────────────────────────┘
                    │  - sql → 原样        │
                    └───────────────┬──────┘
                                    │
                    ┌───────────────▼──────┐
                    │  落库: 正确路径 ✅    │
                    │  查询: 能命中 ✅      │
                    └──────────────────────┘
```

**正确设计要点**:
1. 落库侧删除 `hitl_gateway._resolve_trust_path`(6 键回落扫描 + 函数内联, `hitl_gateway.py:29-41,71`), 统一调用 `extract_trust_path`
2. `extract_trust_path` 扩展: FILE 域沿用 `_parse_paths` 原有逻辑, 非文件域(registry/sql)补专用提取(仅规范 `path` 及定向别名), **不动 `_parse_paths` 本身**(保护 conflict_detector 并查集分组与该函数为冲突检测而设的 FILE 域过滤)
3. `_norm_trust_path` 按工具域规范化: FILE 工具做 `resolve()`, registry/sql 工具原样 strip 存储; 落库/查询/**撤销**三侧一致(trust_db.py + storage.py 双份同步)
4. 查询侧不变, 已经用 `extract_trust_path` → 两侧对齐

### 3.2 问题C: 信任豁免 vs 沙箱 needs_ruling(图解)

#### 3.2.1 当前链路(有意设计, 非 bug)

```
                    ┌─────────────────────────────────────────────────────┐
                    │  工具调用命中信任 → skip_confirmation=True           │
                    │  safety_gate.py:71 (trust 豁免分支)                │
                    └───────────────┬─────────────────────────────────────┘
                                    │
                    ┌───────────────▼──────────────────────────────────┐
                    │  check_before_execute(skip_confirmation=True)    │
                    │  tool_safety_checker.py:188-194                  │
                    │  SafetyResult:                                   │
                    │    requires_confirmation = False  ← 跳确认 ✅   │
                    │    sandbox_required = _needs      ← 保留        │
                    │    auto_confirm = 未设(False)     ← 默认值       │
                    └───────────────┬──────────────────────────────────┘
                                    │
                    ┌───────────────▼──────────────────────────────────┐
                    │  run_sandbox_gate(safety_result)                  │
                    │  safety_gate.py:168 (插入点③)                    │
                    │  main_confirmed = False (缺省)                   │
                    └───────────────┬──────────────────────────────────┘
                                    │
                    ┌───────────────▼──────────────────────────────────┐
                    │  sandbox_gate.py:85-88                           │
                    │  pre.execute (静态分析)                           │
                    │                                                   │
                    │  ① registry 工具:                                 │
                    │     sandbox/executor.py:295-298 恒 passed=True   │
                    │     needs_ruling = False                          │
                    │     → 直接放行, 永不到二次弹窗节点 ✅             │
                    │                                                   │
                    │  ② delete/copy/move (正常情况):                   │
                    │     影子副本预演 → passed=True                     │
                    │     needs_ruling = False                          │
                    │     → 直接放行 ✅                                │
                    │                                                   │
                    │  ③ delete/copy/move (边缘场景):                   │
                    │     源不存在/超时/环境性失败 → passed=False        │
                    │     needs_ruling = True                           │
                    │     ┌─────────────────────────────────────────┐  │
                    │     │ sandbox_gate.py:85:                     │  │
                    │     │ main_confirmed(False) and needs_ruling  │  │
                    │     │ → 不直通                                │  │
                    │     │ sandbox_gate.py:88:                     │  │
                    │     │ needs_ruling and auto_confirm(False)    │  │
                    │     │ → 不直通                                │  │
                    │     │ → 落入 :101-113                         │  │
                    │     │ → ★ 二次弹 HITL                        │  │
                    │     └─────────────────────────────────────────┘  │
                    └──────────────────────────────────────────────────┘
```

**当前行为总结**:

| 工具域 | 静态分析结果 | needs_ruling | 是否二次弹窗 | 说明 |
|--------|-------------|-------------|-------------|------|
| registry | 始终 passed | False | **否** | 静态分析兜底, 永不到 needs_ruling |
| delete/copy/move (正常) | passed | False | **否** | 影子副本预演成功 |
| delete/copy/move (边缘) | failed | True | **是** | 源不存在/超时/环境性失败 |

#### 3.2.2 正确设计选项(✅ 已决策: 方案A)

```
  ┌──────────────────────────────────────────────────────────────────┐
  │  信任命中 → skip_confirmation=True                               │
  └─────────────────────────┬────────────────────────────────────────┘
                            │
                            ▼
  ┌──────────────────────────────────────────────────────────────────┐
  │  ✅ 选项 A: 保持当前保守行为(北京老陈 2026-09-16 选定)           │
  │  信任核心=工具+路径, 信任只跳确认弹框, 不跳沙箱                 │
  │                                                                   │
  │  信任只跳确认, 不跳沙箱 → needs_ruling 时再弹 HITL              │
  │  ✅ 安全方向正确(v1.6 会审F1)                                   │
  │  ✅ registry 永不触发(静态分析 passed)                           │
  │  ✅ 仅 delete/copy/move 边缘场景触发                            │
  │                                                                   │
  │  ⚠️ 需修正: TrustPanel 文案"免弹框" → "免确认弹框"             │
  └──────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────┐
  │  选项 B: 信任时 needs_ruling 也直放                              │
  │                                                                   │
  │  信任 = 完全豁免(跳确认 + 跳沙箱)                               │
  │  ✅ 用户体验好, 一次信任后完全不弹                               │
  │  ❌ 安全风险: 源不存在/超时等边缘场景无二次验证                  │
  │                                                                   │
  │  修改: sandbox_gate.py:88 加 needs_trusted 分支                  │
  └──────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────┐
  │  选项 C: 分层信任                                                │
  │                                                                   │
  │  工具级信任 → 只跳确认, 不跳沙箱(当前行为)                      │
  │  路径级信任 → 跳确认 + 跳沙箱(完全豁免)                        │
  │                                                                   │
  │  ✅ 精细化控制                                                   │
  │  ❌ 实现复杂, 需扩展 trust 表结构                                │
  └──────────────────────────────────────────────────────────────────┘
```
### 3.3 修复优先级建议


| 级别 | 数量 | 内容 |
|------|------|------|
| **严重实施问题**(必改) | 1个 | 问题A+问题B: 落库/查询路径提取不对称 + 垃圾路径入库 |
| **已决策: 方案A**(仅修文案) | 1个 | 问题C: 信任豁免 vs 沙箱 needs_ruling, 北京老陈选定方案A(信任核心=工具+路径, 只跳确认弹框) |
| **前端协同缺陷**(需修正) | 3处 | 勾选框静默失效 / 垃圾路径展示(根治在后端+存量数据清理) / "含子目录"误导 |
| **已确认非问题** | 7项 | bypass不产生信任 / 临时授权边界 / 工具级通配 / 前缀递归 / 反查回落 / 豁免不跳危险防护 / 双份解析 |

---

## 四、修改方法及策略

### 4.1 问题A+B: 统一路径提取 + 消除垃圾路径(后端5文件)

**策略**: 非文件信任域常量单一来源(`tool_constants.py`); 信任路径提取统一收敛到 `extract_trust_path`(不动 `_parse_paths`, 保护 conflict_detector 并查集分组); `_norm_trust_path` 按工具域选择规范化方式(非文件域跳过 `Path.resolve()`), 落库/查询/**撤销**三侧一致。

| 修改文件 | 修改内容 | 目的 |
|---------|---------|------|
| `tool_constants.py` | 第6节新增 `NON_FILE_TRUST_TOOLS`(`registry_write`/`registry_delete`/`execute_sql`) | 非文件信任域单一来源, trust/trust_db/storage 三方共用(DRY) |
| `trust.py` | `extract_trust_path` 在 `_parse_paths` 循环后新增非文件信任域专用提取(仅规范 `path` 及其定向别名 `key_path`/`db_path` 等, 排除 `data→value` 非路径别名) | 查询侧能提取 registry/sql 工具的 path, 使两侧对称; 不动 `_parse_paths` 避免越界改变冲突分组 |
| `trust_db.py` | `_norm_trust_path(path, tool_name=None)`; `insert_session_trust`/`check_session_trust` 内部直接复用已有 `tool_name`(不新增参数) | 消除垃圾路径入库, 非文件域原样 strip 存储 |
| `storage.py` | 同步 `_norm_trust_path(path, tool_name)` + `delete_session_trust` 透传 | 撤销链路一致性: 防 registry 原样路径被再次 resolve 成垃圾导致撤消失配(原漏, 必补) |
| `hitl_gateway.py` | 删除 6 键回落扫描; `_resolve_trust_path` 内联删除, 直接调 `extract_trust_path`(KISS 无透传函数) | 消除 DRY 违反与回落垃圾路径入口, 回归单一职责 |

### 4.2 前端协同缺陷修正(3处)

| 缺陷 | 修改文件 | 修改内容 |
|------|---------|---------|
| ①bypass勾选框静默失效 | `AuthorizationModal/index.tsx` | bypass 模式下 `disabled` 勾选框 + Tooltip 说明 |
| ②trust_path 垃圾路径展示 | 后端(4.1) + 存量数据清理 | **前端无法区分合法盘符路径与垃圾路径**(垃圾 `F:\...\HKCU\Software\X` 同样带盘符), 设前端校验=虚假校验; 根治在后端不 resolve 非文件路径; 存量垃圾行建议 SQL 清理(见5.8) |
| ③"含子目录"文案误导 | `AuthorizationModal/index.tsx` | 按工具域区分: registry 工具"含子键", 其余保持"含子目录" |

### 4.3 问题C: 信任豁免 vs 沙箱 needs_ruling(已决策: 方案A)

当前行为是 v1.6 会审 F1 有意设计, 非 bug。

**北京老陈决策(2026-09-16)**: 方案A — 信任核心=工具+路径, 信任只跳确认弹框, 沙箱未完成验证时仍需用户裁决。仅修正文案精准反映"工具+路径"语义。

| 选项 | 行为 | 安全性 | 修改量 |
|------|------|--------|--------|
| **A: 保持保守(✅ 已选)** | 信任只跳确认, needs_ruling 仍弹 | ✅ 最安全 | 仅修文案 |
| B: 信任完全豁免 | needs_ruling 也直放 | ❌ 有风险 | 改 sandbox_gate |
| C: 分层信任 | 路径级信任完全豁免 | ✅ 精细 | 改 trust 表结构 |

---

## 五、代码修改实施 diff

> 以下 diff 均基于本地最新代码, 精准到行号, 非伪代码。

### 5.1 tool_constants.py — 新增 NON_FILE_TRUST_TOOLS 常量(单一来源)

> 与 `FILE_OPERATION_TOOLS` 同置第6节。三方共用(trust 路径提取 / trust_db 落库·查询规范化 / storage 撤销规范化), 消除 v0.9 计划中"常量放 trust.py + `_get_non_file_tools()` 延迟 import 复用"的绕路(直接顶层导入即无环: trust.py 顶层只依赖 tools_alias_mapper/tool_constants, 不依赖 trust_db)。

```diff
 FILE_OPERATION_TOOLS: set[str] = {  # 【tool 级】使用对象: 文件操作类工具集合(安全/分批判定)
     "readtext", "writetext", "edittext",
     "move", "copy", "delete", "rename",
     "compress", "extract",
     # office 8工具(读写) — 小欧 2026-08-13: 与文本文件工具同机制参与路径冲突检测, 消除并行读写竞态
     "read_xlsx", "read_docx", "read_pdf", "read_pptx",
     "write_xlsx", "write_docx", "write_pdf", "write_pptx",
 }
+
+# 2026-09-16 小欧 问题A修复: 非文件信任域工具 — 共同特征 = schema参数名path + needs_confirmation=True +
+#   不在 FILE_OPERATION_TOOLS, 信任路径不能做文件系统 resolve。registry_read 只读不确认不入(与其他
+#   确认工具同源, YAGNI)。单一来源, trust.extract_trust_path / trust_db._norm_trust_path /
+#   storage._norm_trust_path(撤销) 三方共用(DRY) — 小欧-2026-09-16
+NON_FILE_TRUST_TOOLS: set[str] = {
+    "registry_write", "registry_delete",
+    "execute_sql",
+}
```

### 5.2 trust.py — extract_trust_path 专用提取非文件信任域路径(不动 _parse_paths)

> **为何不动 `_parse_paths`**(较 v0.9 计划改进): `_parse_paths` 被 `conflict_detector.py:46,82` 复用; `_partition_calls:82` 对**所有工具**无过滤调用它, 若让 registry/sql 在此被认 path, 会静默改变并发分组行为(注册表写操作并组串行)——信任修复不应越界改动冲突检测(SRP)。且 registry_write 的别名池含 `data→value` 非路径字段, 复用通用解析会把注册表值数据当路径(污染)。
> **改法**: 在 `extract_trust_path` 内, `_parse_paths` 循环之后补非文件信任域专用提取: 仅认规范 `path` 及其定向别名(`key_path`/`registry_key`/`db_path` 等 → `path`), 排除 value → 与 FILE 流程共用 `PARAM_ALIASES` 但只取规范 path, 精准无污染。

```diff
 from typing import Dict, Optional, Set
 from app.tools.tools_alias_mapper import PARAM_ALIASES, normalize_tool_name
-from app.tools.tool_constants import FILE_OPERATION_TOOLS
+from app.tools.tool_constants import FILE_OPERATION_TOOLS, NON_FILE_TRUST_TOOLS
```
```diff
 def extract_trust_path(name: str, params: Dict) -> Optional[str]:
     """提取工具调用用于信任落库/查询的目标路径(复用 _parse_paths, DRY) — 小欧 2026-09-02
     取首个非 window: 键的文件路径; 无路径参数/窗口工具/提取失败返回 None(=工具级通配)
+    2026-09-16 小欧 问题A修复: 非文件信任域(registry/sql)补专用提取 —— _parse_paths 按
+      FILE_OPERATION_TOOLS 过滤, registry_write 等不在其中致 trust_path 恒 None(查询侧永不命中);
+      此处仅认规范 path 及定向别名(key_path/registry_key/db_path → path), 排除 data→value 等
+      非路径别名, 防注册表值/数据污染信任路径; 不动 _parse_paths(保护 conflict_detector 并查集分组) — 小欧-2026-09-16
     完整复制自 action_handler.py:581-587，取首个非 window: 键的文件路径
     """
     for p in _parse_paths(name, params or {}):
         if not p.startswith("window:"):
             return p
+    if name in NON_FILE_TRUST_TOOLS:          # 小欧 2026-09-16
+        _params = params or {}
+        for _akey, _acn in PARAM_ALIASES.get(name, {}).items():
+            if _acn == "path":                # 仅规范 path 的定向别名(key_path/registry_key/db_path...)
+                _v = _params.get(_akey)
+                if isinstance(_v, str) and _v:
+                    return _v
+        _v = _params.get("path")              # 规范名兜底
+        return _v if isinstance(_v, str) and _v else None
     return None
```

### 5.3 trust_db.py — _norm_trust_path 按工具域规范化(复用已有 tool_name, 不新增参数)

> **较 v0.9 计划改进(YAGNI)**: insert/check 已有 `tool_name` 参数, 直接透传 `_norm_trust_path(path, tool_name)` 即可, 无需新增 `path_tool_name` 外部参数与 5.5 的透传层。tool_name=None 时 `None in NON_FILE_TRUST_TOOLS` 自然 False, 安全降级为原 resolve 行为。

```diff
 # -*- coding: utf-8 -*-
 # 编辑历史:
 # 2026-09-05 - 小欧 - 架构边界修正(三堂会审): 会话信任读写三函数从 app/services/chat/storage.py
 #   迁入本层 app/tools/trust_db.py —— 修复 app/tools 禁 app.services 依赖方向守护违规
+# 2026-09-16 小欧 - 问题B修复: _norm_trust_path 增加 tool_name 参数, 非文件信任域跳过 Path.resolve() — 小欧-2026-09-16
 """tools 层会话信任读写(纯 SQL 查询, 不依赖 services 层) — 小欧 2026-09-05"""
 from pathlib import Path
 from typing import Optional
 from sqlite3 import Connection

 from app.utils.time_utils import get_local_iso_timestamp  # 小欧 2026-08-08 全程统一本地时区: 本地ISO无Z入库
+from app.tools.tool_constants import NON_FILE_TRUST_TOOLS  # 小欧 2026-09-16 非文件信任域(与 storage:561 撤销侧同位)


-def _norm_trust_path(path: Optional[str]) -> Optional[str]:
-    """信任路径规范化(resolve 绝对化, 供落库/查询双侧一致) — 小欧 2026-09-02"""
+def _norm_trust_path(path: Optional[str], tool_name: Optional[str] = None) -> Optional[str]:
+    """信任路径规范化(resolve 绝对化, 供落库/查询双侧一致) — 小欧 2026-09-02
+    2026-09-16 小欧 问题B修复: 非文件信任域(registry/sql)跳过 Path.resolve() 原样 strip 存储——
+      注册表键(HKCU\Software\X)非文件系统路径, resolve 会臆造垃圾绝对路径(F:\...\HKCU\...\X);
+      execute_sql 的 db path 保持相对原样(两侧一致即正确); 复用 insert/check 已有 tool_name, 不新增参数 — 小欧-2026-09-16"""
     if not path:
         return None
     try:
+        if tool_name in NON_FILE_TRUST_TOOLS:
+            return path.strip()
         return str(Path(path).resolve())
     except Exception:
         return None


 def insert_session_trust(conn: Connection, session_id: str, tool_name: str, path: Optional[str] = None) -> None:
     """HITL"信任本次会话"落库（UNIQUE(session_id, tool_name, path) 幂等）— 小欧 2026-08-16; v1.5 增 path 参数
     path=None=无路径工具的工具级通配; 非空=该路径及子目录树递归豁免"""
     conn.execute(
         "INSERT OR IGNORE INTO chat_session_trust(session_id, tool_name, path, created_at) VALUES (?,?,?,?)",
-        (session_id, tool_name, _norm_trust_path(path), get_local_iso_timestamp()),
+        (session_id, tool_name, _norm_trust_path(path, tool_name), get_local_iso_timestamp()),
     )


 def check_session_trust(conn: Connection, session_id: str, tool_name: str, path: Optional[str] = None) -> bool:
     """工具安全检查豁免查询：会话已信任该 tool+path 则免二次 HITL 确认 — 小欧 2026-08-16; v1.5 增 path 前缀递归匹配
     匹配规则(北京老陈 2026-09-02 定案):
       path=None: 仅命中工具级通配行(path IS NULL);
       path 给定: 命中工具级通配行, 或任一行信任路径等于/为目标的父目录(前缀递归, 与 temp_auth 语义对齐)。"""
     rows = conn.execute(
         "SELECT path FROM chat_session_trust WHERE session_id=? AND tool_name=?",
         (session_id, tool_name),
     ).fetchall()
     if path is None:
         return any(r["path"] is None for r in rows)
-    target = _norm_trust_path(path)
+    target = _norm_trust_path(path, tool_name)
     if target is None:
         return False
     target_p = Path(target)
```

### 5.4 storage.py — 撤销链路一致性同步(原漏, 必补)

> **为何必改(v1.0 核验新增发现)**: `delete_session_trust`(storage.py:579-591) 也经 `_norm_trust_path` 规范化撤销路径。本文件若不随 5.3 同步, 修复后 registry 以原样键路径落库、撤销时又被 resolve 臆造成垃圾 → 撤消失配(404) → 引入新退化。该双份 `_norm_trust_path` 为 trust_db 迁移时的既有设计(文档二[44]1.3 已述), 双侧必须同步一致。

```diff
 import json
 import threading
 import types  # 11.1 冻结 token 零值常量, 防外部 mutate 污染全局 — 小欧 2026-08-20
 from pathlib import Path  # v1.5(2026-09-02 小欧): 信任路径 resolve 绝对化(前缀递归基础) — 小欧 2026-09-02
 from typing import Any, Dict, Optional, Tuple
 from sqlite3 import Connection
 
 from fastapi import HTTPException
 from pydantic import BaseModel, Field
 
 from app.logger import logger
 from app.db import db
 from app.db.models.chat_models import SessionModelOverride, ModelRef   # 归一: ModelRef=SessionModelOverride 别名 — 小欧 2026-08-22
 from app.utils.json_utils import safe_json_dumps, parse_json
 from app.utils.time_utils import get_local_iso_timestamp  # 小欧 2026-08-08 全程统一本地时区: 本地ISO无Z入库
+from app.tools.tool_constants import NON_FILE_TRUST_TOOLS  # 小欧 2026-09-16 撤销侧规范化同位(与 trust_db:15 同步)
```
```diff
-def _norm_trust_path(path: Optional[str]) -> Optional[str]:
-    """信任路径规范化(resolve 绝对化, 供落库/查询双侧一致) — 小欧 2026-09-02"""
+def _norm_trust_path(path: Optional[str], tool_name: Optional[str] = None) -> Optional[str]:
+    """信任路径规范化(resolve 绝对化, 供落库/查询双侧一致) — 小欧 2026-09-02
+    2026-09-16 小欧 问题B修复: 与 trust_db.py:17 同位同步非文件信任域跳过 resolve
+      (撤销侧一致性, 防 registry 原样键路径再被臆造 resolve 致撤消失配) — 小欧-2026-09-16"""
     if not path:
         return None
     try:
+        if tool_name in NON_FILE_TRUST_TOOLS:
+            return path.strip()
         return str(Path(path).resolve())
     except Exception:
         return None
```
```diff
     else:
         cur = conn.execute(
             "DELETE FROM chat_session_trust WHERE session_id=? AND tool_name=? AND path=?",
-            (session_id, tool_name, _norm_trust_path(path)),
+            (session_id, tool_name, _norm_trust_path(path, tool_name)),
         )
     return cur.rowcount > 0
```

### 5.5 hitl_gateway.py — 删除 6 键回落 + _resolve_trust_path 内联删除

> **较 v0.9 计划改进(KISS-DIRECT)**: 删除回落扫描后 `_resolve_trust_path` 只剩 `return extract_trust_path(...)`, 属"无透传函数"反例, 直接内联删除, `hitl_confirm:71` 直接调主链函数。回落键本就冗余(PARAM_ALIASES 已覆盖 file_path/source_path 等), 且是 registry 工具垃圾路径入库的直接入口。

```diff
-async def _resolve_trust_path(tool_name, params) -> Optional[str]:
-    from app.tools.trust import extract_trust_path        # 延迟import防环（真实位置tools/trust.py行66）
-    params = params or {}
-    # 网关内统一提取（原主路_extract_trust_path + sandbox回落扫描，收拢；回落键与sandbox_gate行101同源）
-    _trust = extract_trust_path(tool_name, params)
-    if _trust:
-        return _trust
-    for _k in ("path", "file_path", "source_path", "dest_path", "target", "dir_path"):
-        _v = params.get(_k)
-        if isinstance(_v, str) and _v:
-            return _v
-    return None
-
-
 def _desensitize(params) -> dict:
```
```diff
 async def hitl_confirm(agent, spec: ConfirmSpec, publish):
     """spec属性访问（与4.2同形）→verdict。网关内统一：超时计算/trust_path/脱敏/SUSPENDED→wait→EXECUTING。"""
     from app.services.task.hitl_confirmation import (            # 延迟import防环
         create_confirmation, wait_for_confirmation_result, resolve_confirmation)
-    _path = spec.path or await _resolve_trust_path(spec.tool_name, spec.params)
+    from app.tools.trust import extract_trust_path    # 延迟import防环（原 _resolve_trust_path 内联, KISS 无透传函数 — 小欧-2026-09-16）
+    _path = spec.path or extract_trust_path(spec.tool_name, spec.params)
```

### 5.6 AuthorizationModal/index.tsx — bypass 勾选框禁用 + 工具域文案(删虚假校验)

> **删除 isValidTrustPath**(v0.9 曾引入): 垃圾路径(`F:\...\HKCU\Software\X`)同样带盘符, 前端正则无法区分真假 → 设前端校验=虚假校验, 删。缺陷②根治在后端(5.3/5.4 不再 resolve 非文件路径 → 新信任正确入库)。此处仅保留真实可行的两项: ①bypass 禁用勾选框(缺陷①) ③按工具域区分文案(缺陷③)。

```diff
         <div style={{ marginBottom: 12 }}>
           <Checkbox
             checked={trustSession}
-            disabled={submitting}
+            disabled={submitting || isBypass}   // 2026-09-16 小欧 缺陷①修复: bypass 禁用勾选框, 防静默失效误导(原仅 handleConfirm 强改 false, UI 仍可勾) — 小欧-2026-09-16
             onChange={(e) => setTrustSession(e.target.checked)}
           >
-            {request.trustPath ? (
+            {isBypass ? (
+              <Tooltip title="自动确认模式下信任不落库，勾选无效">
+                <span>信任此操作（本次会话）</span>
+              </Tooltip>
+            ) : request.trustPath ? (
               <Tooltip
-                title={`${request.toolName} › ${request.trustPath}，含子目录`}
+                // 2026-09-16 小欧 缺陷③修复: 按工具域区分文案(registry→含子键, 其余→含子目录) — 小欧-2026-09-16
+                title={`${request.toolName} › ${request.trustPath}${
+                  request.toolName.startsWith('registry') ? '，含子键' : '，含子目录'
+                }`}
               >
                 <span>信任此操作（本次会话）</span>
               </Tooltip>
```

### 5.7 问题C 文案修正(TrustPanel Tooltip) — 方案A决策落地

> 信任核心=工具+路径。文案精准反映: 信任豁免的是"确认弹框", 非所有弹框; 沙箱未完成验证时仍可能弹窗。

```diff
-        <Tooltip title="会话级 tool+path 免审白名单：勾信任后同会话同工具、目标路径及其子目录免弹框，危险操作仍拦截，可×撤销">
+        <Tooltip title="会话级 tool+path 信任：同会话同工具+目标路径免确认弹框（沙箱预检未完成验证时仍可能弹窗），危险操作仍拦截，可×撤销">
```

(注: `TrustPanel.tsx:134` 触发文字 `信任(N)` 无变化, 仅 Tooltip 文案更新。)

### 5.8 前端缺陷②核查结论: 无代码改动 + 存量数据清理建议

**结论**: `TrustPanel.tsx:178` 展示 `${t.toolName} › ${t.path ?? '全局'}` 已忠实呈现数据库内容, 无需改动。垃圾路径展示的根治在本章 5.3/5.4(后端不再 resolve 非文件路径 → 新信任正确入库)。

**存量垃圾行**(修复前已入库的 `F:\...\HKCU\Software\X` 等臆造绝对路径): 撤销界面按原样键路径匹配不到这些行, 建议人工核对后清理:

```sql
-- 列举存量非文件工具信任(人工核对 path 是否为臆造绝对路径, 确认后删除) — 小欧-2026-09-16
SELECT id, tool_name, path FROM chat_session_trust
WHERE tool_name IN ('registry_write','registry_delete','execute_sql')
  AND path IS NOT NULL;
-- 确认后: DELETE FROM chat_session_trust WHERE id IN (...);
```

> 注: 正式运行环境当前为 bypass 模式, 信任不落库(v1.5 定案, 见1.3 恒空四环节), 存量行大概率为空; 此清理仅供回切 `enabled:true` 历史数据核对用。

## 六、基于 TDD 的实施步骤(红→绿→重构)

> 依赖编排: 5.8 为核查结论(无代码改动)不设步骤。执行顺序: 步骤1(常量)→步骤2(提取)→步骤3(落库规范化)→步骤4(撤销同步)→步骤5(网关删回落)→步骤6(前端文案)。前端缺陷②(垃圾路径展示)由步骤3/4完成后自动根治, 无独立步骤。
> 全部测试基于内存 SQLite + 三原语 mock(参照 test_trust_v15/test_grant_before_resolve 既有模式), 不启后端、不调 LLM(铁律)。

### 6.1 测试资产盘点: 新增 / 修改 / 保持 总览

| 动作 | 测试文件 | 内容 |
|------|---------|------|
| 修改 | `backend/tests/test_trust.py` | `TestExtractTrustPath` 类增补非文件信任域案例; `_parse_paths` 9 例与 `resolve_skip` 系列**原封不动**(查询链未改) |
| 新增 | `backend/tests/test_t1_trust_non_file_tools.py` | 步骤1: `NON_FILE_TRUST_TOOLS` 常量契约 |
| 新增 | `backend/tests/test_t2_trust_extract_non_file_domain.py` | 步骤2: `extract_trust_path` 非文件域专用提取 + 污染防护 |
| 新增 | `backend/tests/test_t3_trust_norm_domain.py` | 步骤3: `trust_db._norm_trust_path` 按工具域规范化 + 落库/查询集成 |
| 新增 | `backend/tests/test_t4_trust_revoke_consistency.py` | 步骤4: `storage._norm_trust_path` 撤销侧一致性(防撤消失配) |
| 新增 | `backend/tests/test_t5_trust_gateway_cleanup.py` | 步骤5: `hitl_gateway` 删回落 + 直接调主链(静态+行为) |
| 保持 | `test_trust_v15.py` | T1~T8 全保持(双参默认 `None` 兼容, 回归验证) |
| 保持 | `test_grant_before_resolve.py` | 网关单点 resolve 收口契约(删 `_resolve_trust_path` 不触及 `resolve_confirmation`) |
| 保持 | `test_path1_step2_hitl_gateway.py` | 网关静态契约(其锚点 `resolve_confirmation` 在 5.5 保留) |
| 保持 | `test_conflict_detector.py` | `_parse_paths` 并查集分组守护(方案不动它 → 该文件必须原样全绿) |
| 保持 | `test_sandbox_executor.py` | registry 静态分析/根键冒号归一(3.2 现状, 不涉本方案) |
| 保持 | `test_architecture_boundaries.py` | 依赖方向守护(storage→`app.tools` 为合法方向, 5.4 新 import 不破墙) |

### 6.2 步骤1: NON_FILE_TRUST_TOOLS 常量(对应 diff 5.1)

**先红**: 新建 test_t1_trust_non_file_tools.py, 锚定目标行为:

| case 名 | 目标行为断言 | 红阶段(现行为) |
|---------|-------------|----------------|
| test_non_file_trust_tools_exact_membership | `NON_FILE_TRUST_TOOLS == {"registry_write","registry_delete","execute_sql"}` | `ImportError`(常量未定义) |
| test_registry_read_not_included | `"registry_read"` 不在集合(只读不确认不信任, YAGNI) | `ImportError` |
| test_disjoint_with_file_operation_tools | `NON_FILE_TRUST_TOOLS & FILE_OPERATION_TOOLS == set()`(无双归属, 防 `_parse_paths` 侧漏) | `ImportError` |

> 背景实证(非 case): 三者均 `needs_confirmation=True`(`win_registry_register.py:70` CONFIRM_TOOLS、`dataanalysis_register.py:135`), 集合无幻觉。

**绿**: `tool_constants.py` 第 6 节 `FILE_OPERATION_TOOLS` 后追加常量(精确 diff 见 5.1)。
**验证**: `pytest -k test_t1_trust_non_file_tools` 全绿。

### 6.3 步骤2: extract_trust_path 非文件域专用提取(对应 diff 5.2)

**先改 test_trust.py(修改点)**: `TestExtractTrustPath.test_non_file_tool_returns_none`(`searchweb→None`)**保持不动** —— 它是"不越界"守护: 方案只认 `NON_FILE_TRUST_TOOLS`, searchweb 等仍须 None。同批追加"非信任域非文件工具仍 None"：query_sql / get_db_schema / registry_read → None。

**再红**(新建 test_t2 文件): 现 `extract_trust_path` 对 registry/sql 恒返回 None(循环被 `FILE_OPERATION_TOOLS` 过滤); 红判定必须锚定**具体目标值**, 严禁"断言 None"空过(参照 test_grant_before_resolve 对真空绿的教训):

| case 名 | 目标行为断言 | 红阶段(现行为) |
|---------|-------------|----------------|
| test_registry_write_path_returns_original | `extract("registry_write", {"path": "HKCU\\Software\\X"}) == "HKCU\\Software\\X"` | None → 断言失败 |
| test_registry_write_key_path_alias | `{"key_path": "HKCU\\Software\\X"}` → 返回同串(PARAM_ALIASES registry_write `key_path→path` 已实证) | None → 断言失败 |
| test_registry_write_registry_key_alias | `{"registry_key": ...}` → 返回(`registry_key→path` 已实证) | None → 断言失败 |
| test_registry_write_data_value_excluded | `{"data": "myValue", "value_name": "v"}` → None(`data→value` 非路径, 防值数据污染信任路径) | **守护case**: 当前已返回None(正确), 断言None空过; 目的验证data→value别名不污染路径提取(若实现有误则返回"myValue"→断言失败) |
| test_execute_sql_db_path_alias | `{"db_path": "D:/data.db"}` → `"D:/data.db"`(`execute_sql` 别名集 db_path/dbpath/database/database_path 已实证) | None → 断言失败 |
| test_execute_sql_no_path_returns_none | `{}` → None(无路径=工具级通配语义) | **守护case**: 当前已返回None(正确), 断言None空过; 语义确认 |
| test_non_file_domain_params_none | `extract("registry_write", None)` → None(与现有 test_none_params 同构) | **守护case**: 当前已返回None(正确), 断言None空过; 与现有case同构验证 |

> 定向别名范围实证: registry_write 别名池 = registry_key/reg_key/regpath/key_path/**key→path** + **data→value**(污染项); registry_delete = registry_key/reg_key/regpath/key_path/key 全 path、无 data; execute_sql = db_path/dbpath/database/database_path 全 path。5.2 diff 的"仅认 `_acn == "path"`"即精确取这些。

**绿**: `extract_trust_path` 的 `_parse_paths` 循环后补 NON_FILE_TRUST_TOOLS 专用提取(diff 5.2)。
**验证**: `pytest tests/test_trust.py tests/test_t2_trust_extract_non_file_domain.py -k "extract"`(9 例旧断言 + 新 case 全绿)。

### 6.4 步骤3: trust_db._norm_trust_path 按工具域(对应 diff 5.3)

**先红**(新建 test_t3): 现 `_norm_trust_path` 恒 `Path(path).resolve()`(问题B 直接原因), 红锚定目标原样值:

| case 名 | 目标行为断言 | 红阶段(现行为: 恒 resolve) |
|---------|-------------|----------------|
| test_norm_registry_returns_original | `_norm_trust_path("HKCU\\Software\\X", "registry_write") == "HKCU\\Software\\X"` | FAIL: resolve 臆造 `F:\...\HKCU\Software\X`(工作目录下垃圾绝对路径) |
| test_norm_registry_strip_surrounding_space | `" HKCU\\Software\\X "` → `"HKCU\\Software\\X"`(strip 后原样) | FAIL(未 strip 且臆造) |
| test_norm_execute_sql_relative_kept | `_norm_trust_path("data.db", "execute_sql") == "data.db"`(相对路径保留, 两侧一致即正确) | FAIL: resolve→`F:\...\data.db` |
| test_norm_file_still_resolve | `_norm_trust_path("D:/a", "delete") == str(Path("D:/a").resolve())`(工具域隔离: 文件域行为不退化) | 绿(保持) |
| test_norm_no_tool_name_still_resolve | `_norm_trust_path("HKCU\\x")` 单参 → 仍 resolve(None 不在常量, 兼容兜底) | 绿(现行为一致, 兼容旧签名守卫) |
| test_insert_registry_persists_original | 内存库 `insert_session_trust(s1, "registry_write", "HKCU\\Software\\X")` → 行 path == 原样且不含 `"F:"` 前缀 | 红: 落库臆造垃圾 |
| test_check_registry_prefix_recursive | 同库 `check(s1, "registry_write", "HKCU\\Software\\X")` True; `check(..., "HKCU\\Software\\X\\SubA")` True(前缀递归, 语义=子键树) | 红: 落库垃圾→查询命中为空 |

**绿**: trust_db.py 头部 import `NON_FILE_TRUST_TOOLS` + `_norm_trust_path` 加 `tool_name` 参数(默认 None)+ 非文件域 strip 分支 + insert/check 两处调用透传(diff 5.3)。
**验证**: `pytest -k "trust" tests/test_trust.py tests/test_trust_v15.py tests/test_t3_trust_norm_domain.py` —— test_trust_v15.py T1~T8 全绿即证明**签名兼容无退化**(已有调用全部单参)。

### 6.5 步骤4: storage._norm_trust_path 同步(对应 diff 5.4)

**先红**(新建 test_t4): 现 `storage.delete_session_trust` 撤销路径也被 resolve 臆造 → SQL `path=?` 匹配不到 registry 原样行 → `rowcount==0` 撤消失配(404)。

| case 名 | 目标行为断言 | 红阶段(现行为) |
|---------|-------------|----------------|
| test_storage_revoke_registry_exact | 预插 registry 行 → `storage.delete_session_trust(s1, "registry_write", "HKCU\\Software\\X") is True` 且该行删除 | FAIL: resolve 臆造→不匹配→False(撤消失配) |
| test_dual_norm_consistency | 对 registry_write/execute_sql/delete 三工具: `storage._norm_trust_path(p, tool) == trust_db._norm_trust_path(p, tool)`(双份防分裂) | 红: 双侧不一致(撤销侧仍 resolve) |
| test_storage_norm_file_not_regressed | `storage._norm_trust_path("D:/a", "delete")` 仍绝对化 | 绿(保持) |
| test_revoke_file_still_exact | delete 工具精确撤销仍匹配(原 test_trust_v15 T5 语义不退化) | 绿(保持) |

**绿**: storage.py 头部 import + `_norm_trust_path` 双参同步 + `delete_session_trust` 调用处透传(diff 5.4)。
**验证**: `pytest tests/test_trust_v15.py tests/test_t4_trust_revoke_consistency.py -k "delete or revoke or norm"`(T5/T8 原语义必须仍绿)。

### 6.6 步骤5: hitl_gateway 删回落 + 内联(对应 diff 5.5)

**先红**(新建 test_t5, 静态+行为混合, 风格同 test_path1_step2):

| case 名 | 目标行为断言 | 红阶段(现行为) |
|---------|-------------|----------------|
| test_gateway_no_resolve_trust_path_symbol | `"_resolve_trust_path"` not in `inspect.getsource(hitl_gateway)`(无透传函数, KISS-DIRECT) | 红: 符号存在 |
| test_gateway_six_key_fallback_removed | 源码不含回落键特证 `"file_path", "source_path", "dest_path", "target", "dir_path"`(6 键回落删除) | 红: 循环体存在 |
| test_gateway_direct_extract_trust_path_call | `"extract_trust_path("` in 源码且 `hitl_confirm` 直接调用(无中间透传) | 红: 仅 `_resolve_trust_path` 内部调用 |
| test_hitl_confirm_spec_path_priority | 三原语 mock(create/wait/resolve)+ mock `extract_trust_path`: `spec.path="X"` → 断言调用 `extract_trust_path(spec.tool_name, spec.params)` 且其结果仅作 `spec.path` 缺省 | **红**: 当前hitl_confirm调用`_resolve_trust_path`(行71), 而非直接调用`extract_trust_path`; mock断言直接调用即失败 |
| test_gateway_resolve_confirm_kept | `"resolve_confirmation"` in 源码(单点收口不退化, 与 test_path1_step2 锚点对齐) | 绿(保持) |

**红阶段注意**: 行为 case 的 mock 目标在 `hitl_confirmation`(三原语)与 `app.tools.trust.extract_trust_path`; 静态 case 用 `app.services.agent.handlers.hitl_gateway` 源码字符串锚定(避免 import 区 `wait_for_confirmation_result` 式误造红——锚点取符号与特证串, 不锚"首个`wait`")。

**绿**: 删除 `_resolve_trust_path`(含 6 键回落)+ `hitl_confirm:71` 内联直调(延迟 import 防环保留, diff 5.5)。
**验证**: `pytest tests/test_path1_step2_hitl_gateway.py tests/test_grant_before_resolve.py tests/test_t5_trust_gateway_cleanup.py` —— **前两文件必须全绿 = 不退化证明**(steps1 的锚点 `resolve_confirmation` 保留、gate 的 resolve 收口契约不受影响)。

### 6.7 步骤6: 前端 5.6 / 5.7(无自动测试基建声明 + 手动验证清单)

**现状实证**: `frontend/**/*.test.*` 0 命中(无 Vitest 组件单测); Playwright 仅有 `e2e_case/fre2e_*`(需真实后端+LLM), 且信任授权场景需 `config.yaml security.enabled: true`(当前运行 bypass)。故:
- **不新增**前端组件单测(改动为纯展示逻辑, 引入 DOM mock 基建属 YAGNI);
- **不扩展** fre2e(需改运行配置且触发条件苛刻, 收益低);
- 落地为**手动验证清单**(步骤内逐项勾验):

| # | 验证点 | 操作 | 预期 |
|---|--------|------|------|
| 1 | 5.6 bypass 禁用 | bypass 模式下授权弹框 → 勾选框 | `disabled`, Tooltip "自动确认模式下信任不落库，勾选无效" |
| 2 | 5.6 工具域文案(registry) | 触发 registry_write/registry_delete 确认 → 信任文案 | `… › HKCU\…，含子键` |
| 3 | 5.6 工具域文案(其余) | 触发 delete/writetext 确认 | `… › D:/…，含子目录` |
| 4 | 5.7 TrustPanel Tooltip | 悬停 `信任(N)` | 新文案(见 diff 5.7), 关键词: 免确认弹框/沙箱预检未完成验证时仍可能弹窗/危险操作仍拦截 |
| 5 | 撤销一致性(后端联调) | 授权 registry_write 后 TrustPanel × 撤销 | 撤销不 404, 列表该行消失(Tooltip 信任"N"随动) |

**验证命令**: `npm run check`(lint+format)+ `npm run dev` 冒烟勾验上表。

### 6.8 回归守护(保持清单必须全绿)

- **不改动**: test_trust_v15.py(T1~T8)、test_grant_before_resolve.py、test_path1_step2_hitl_gateway.py、test_conflict_detector.py、test_sandbox_executor.py、test_architecture_boundaries.py —— 全量必绿; 绿=不退化, 红=本方案引入退化即回退。
- **后端全量**: `python -m pytest -x --tb=short`(workdir=backend, pytest.ini 已收敛 testpaths=tests)。
- **前端**: `npm run check`。
- **提交范围铁律**: 本次施工仅提交生产代码 5.1~5.7 七个文件; 测试文件严禁 commit(铁规, 红→绿产物留在本地不进仓)。

### 6.9 TDD 纪律

1. **顺序不可逆**: 每步先写测试(红)→ 最小 diff 实现(绿)→ 立即 6.8 全量回归; 严禁先改代码后补测试(打补丁式)。
2. **空过不算绿**: 6.3 注, 断言必须锚定具体目标值(参照 test_grant_before_resolve.py 对"真空绿"的修正教训)。
3. **红判锚点精确**: 避免 import 区同名符号误造红(6.6 注, 参照 test_path1_step2 的 wait 修正教训)。
4. **一步一回归**: 步骤间测试相互掩盖歧义, 每步绿后全量。

---

**编写人**: 小欧
**时间**: 2026-09-16 06:39:47
