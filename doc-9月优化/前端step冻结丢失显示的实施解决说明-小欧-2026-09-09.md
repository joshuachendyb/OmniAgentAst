# 前端step冻结/丢失显示的实施解决说明

**创建时间**: 2026-09-09 20:56:47
**编写人**: 小欧
**版本**: v1.2

**版本历史**：
| 版本 | 时间 | 更新信息 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-09-09 20:56:47 | 初始创建：[17][18][19]三文档对照本地代码验证 | 小欧 |
| v1.1 | 2026-09-09 22:09:17 | 更新：记录bug-2修复+saveStepsToStorage防抖的实际代码变更、三堂会审结果、编辑历史修正 | 小欧 |
| v1.2 | 2026-09-09 22:10:28 | 文档更名：三文档对照验证报告 → 前端step冻结/丢失显示的实施解决说明 | 小欧 |
| v1.3 | 2026-09-09 22:12:00 | 补全：useSSE.ts useEffect cleanup 代码、签名版本修正 | 小欧 |

**关联文档**:
- [17]任务1任务2问题根因分析报告-小欧-2026-09-09.md
- [18]前端 UI显示缺陷分析之一-小欧-2026-09-09.md
- [19]前端渲染显示缺陷分析之二-小沈-2026-09-09.md

**依据**: 本地代码 `frontend/src/features/chat/` 全链路逐行核对

---

## 一、结论先行

| 文档 | 任务2结论 | 任务1结论 | 代码验证 |
|------|---------|---------|---------|
| [17] 小欧 | _hasFinal切历史 + 单次REST快照永不定 | 后端LLM挂起429零输出 | 任务2✅正确；任务1⚠️与用户实证冲突 |
| [18] 小欧 | onComplete读prev覆盖executionSteps | LLM处理20秒正常行为 | 任务2✅正确；任务1⚠️不涉及主线程阻塞 |
| [19] 小沈 | HITL缓冲不同步(待验证) | saveStepsToStorage O(N²)阻塞主线程 | 任务1✅代码层正确；任务2待验证 |

**综合判定**：任务2有两个独立bug需同时修；任务1的 saveStepsToStorage 阻塞机制成立。

---

## 二、任务2 对照验证

### 2.1 [17]结论验证：_hasFinal切历史 + 单次快照

| 结论文本 | 本地代码位置 | 验证结果 |
|---------|------------|---------|
| `_hasFinal` 在 final 进 liveSteps 瞬间翻转 | `RightViewer.tsx:130` `liveSteps.some(s => s.type === 'final')` | ✅ 正确 |
| `displaySteps` 由 live 切 history(空) | `RightViewer.tsx:312` `isCurrentLive ? liveSteps : historySteps` | ✅ 正确 |
| REST effect 首快照后永不重拉 | `RightViewer.tsx:296` 依赖 `[activeTaskId, sessionId, isCurrentLive, _hasFinal, serverTaskId]` | ✅ 正确 |
| A3 300ms 被击穿 → 滞后窗口>300ms | `RightViewer.tsx:286-287` | ✅ 正确 |

**行号偏差**：[17]说`:125`，实际`:130`（差5行）；`:285`实际`:312`（差27行）；`:269`实际`:296`（差27行）。原因：文档写完后北京老陈加了 `hasBusinessSteps` 铁证兜底（:136-145共10行），行号整体偏移。**逻辑结论正确**。

### 2.2 [18]结论验证：onComplete读prev覆盖executionSteps

| 结论文本 | 本地代码位置 | 验证结果 |
|---------|------------|---------|
| final分支先更新ref再调onComplete | `sseParser.ts:499-500` 更新ref → `:520` 读ref → `:522` `onComplete` | ✅ 正确 |
| onComplete内`lastMessage.executionSteps`读prev旧态 | `useChatCallbacks.ts:458` `lastMessage.executionSteps \|\| []` | ✅ 正确 |
| onStep+onComplete同tick调用React批处理 | `sseParser.ts:515` onStep → `:522` onComplete，React 18批处理prev为旧值 | ✅ 正确 |

**行号偏差**：`:432-467` 实际 `:448-483`（偏移16行），逻辑正确。

### 2.3 [19]结论验证：saveStepsToStorage阻塞（非任务2主因）

[19]的saveStepsToStorage阻塞机制对任务2解释力弱——任务2的病根是onComplete覆盖+切历史，不是主线程阻塞。

---

## 三、任务1 对照验证

### 3.1 [17]结论验证：后端LLM挂起429

- [17]说"后端零SSE输出"，但用户实证"action WAS sent to frontend but UI took ~10s to render"
- [17]说"最终时间戳15:46:29"，用户说"等待约10多秒"
- ⚠️ 与用户实证有冲突

### 3.2 [18]结论验证：LLM处理20秒正常行为

- [18]说"这是正常行为不需要修复"
- ⚠️ 用户明确说"冻住"是问题，不接受"正常行为"定性

### 3.3 [19]结论验证：saveStepsToStorage O(N²)阻塞主线程

| 结论文本 | 本地代码位置 | 验证结果 |
|---------|------------|---------|
| 三处setTimeout(0)+saveStepsToStorage同步写sessionStorage | thought:377-389, action:681-693, observation:834-845 | ✅ 正确 |
| JSON.stringify(steps)全量序列化+sessionStorage.setItem同步写入 | `useSSE.ts:486` | ✅ 正确 |
| N个事件→N个宏任务依次阻塞→O(N²) | steps线性增长，第N次序列化全量N步 | ✅ 逻辑正确 |

**[19]代码层结论正确**，且与用户"冻住→整批显示"现象吻合。

---

## 四、任务2 实际需要修两处

| bug | 文件:行号 | 机制 | 现有修复覆盖 | 修复状态 |
|-----|----------|------|------------|---------|
| **bug-1** | `RightViewer.tsx:312` | `_hasFinal`翻转→`displaySteps`切空`historySteps`→REST单次快照缺数据 | 北京老陈`hasBusinessSteps`兜底（:136-145）挡部分场景 | 部分修（已有兜底） |
| **bug-2** | `useChatCallbacks.ts:454-459` | `onComplete`读React旧态`prev`→覆盖含final的executionSteps→PipelineRenderer缺step5/step6 | **已修复（2026-09-09 22:09）** | ✅ 已修 |

bug-1 已有部分兜底（`hasBusinessSteps`），bug-2 已于本次修改中修复。

---

## 五、已完成的代码变更（2026-09-09 22:09）

### 5.1 修复 bug-2（onComplete覆盖）✅ 已完成

**文件**: `frontend/src/features/chat/hooks/useChatCallbacks.ts`
**行号**: :454-459
**编辑历史**: :28-30（文件顶部编辑历史块最底部）

```typescript
// 修改前（:449）：
const finalSteps = lastMessage.executionSteps || [];

// 修改后（:454-459）：
const finalSteps =
  executionStepsFromSSE &&
  executionStepsFromSSE.length >
    (lastMessage.executionSteps?.length || 0)
    ? executionStepsFromSSE
    : lastMessage.executionSteps || [];
```

**原理**: sseParser final分支在调用onComplete前已将final step追加到`executionStepsRef.current`（:499-500），并作为第3参数传入onComplete（:520,522）。用此参数可绕过React批处理的prev旧态问题。

### 5.2 修复 saveStepsToStorage 阻塞（任务1根治）✅ 已完成

**文件1**: `frontend/src/hooks/useSSE.ts:484-501`
**编辑历史**: :37-39（文件顶部编辑历史块最底部）

```typescript
// 修改前（:481-493）：
const saveStepsToStorage = useCallback((steps: ExecutionStep[]) => {
  if (steps.length > 0 && config.sessionId) {
    const storageKey = `${SSE_STORAGE_KEY}_${config.sessionId}`;
    try {
      sessionStorage.setItem(storageKey, JSON.stringify(steps));
    } catch (e) {
      console.warn('[SSE] 保存到 sessionStorage 失败:', e);
    }
  }
}, [config.sessionId]);

// 修改后（:484-501）：加300ms防抖 + 组件卸载清理timer
const saveStepsTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
const saveStepsToStorage = useCallback((steps: ExecutionStep[]) => {
  if (steps.length === 0 || !config.sessionId) return;
  if (saveStepsTimerRef.current !== null) clearTimeout(saveStepsTimerRef.current);
  saveStepsTimerRef.current = setTimeout(() => {
    const storageKey = `${SSE_STORAGE_KEY}_${config.sessionId}`;
    try {
      sessionStorage.setItem(storageKey, JSON.stringify(steps));
    } catch (e) {
      console.warn('[SSE] 保存到 sessionStorage 失败:', e);
    }
    saveStepsTimerRef.current = null;
  }, 300);
}, [config.sessionId]);
```

**新增 useEffect cleanup**（useSSE.ts:504-512）：组件卸载时清理防抖 timer，防止卸载后仍写 sessionStorage

```typescript
useEffect(() => {
  return () => {
    if (saveStepsTimerRef.current !== null) {
      clearTimeout(saveStepsTimerRef.current);
      saveStepsTimerRef.current = null;
    }
  };
}, []);
```

**文件2**: `frontend/src/features/chat/services/sseParser.ts`
**编辑历史**: :48-50（文件顶部编辑历史块最底部）
**改法**: 6处去掉外层 `setTimeout(() => { saveStepsToStorage?.(newSteps); }, 0)`，直接调 `saveStepsToStorage?.(newSteps)`（防抖已在内部处理）

| 分支 | 行号 |
|------|------|
| thought | :383-388 |
| chunk | :432-437 |
| final | :504-509 |
| action | :681-686 |
| observation | :832-837 |
| paused/resumed/retrying | :872-877 |

### 5.3 三堂会审结果

| 轮次 | 检查项 | 结果 |
|------|-------|------|
| 第1轮 合规 | ESLint 0 errors / TypeScript 0 errors / Prettier 全部通过 | ✅ |
| 第2轮 合理 | bug-2逻辑链（sseParser ref更新→onComplete传参→useChatCallbacks取参）正确；防抖逻辑链（取消旧timer→设新timer→300ms后写入）正确 | ✅ |
| 第3轮 关联 | clearStepsFromStorage不受影响 / sessionStorage恢复可接受300ms延迟 / 防抖timer有cleanup effect / 无功能退化 | ✅ |

### 5.4 编辑历史修正

初版编辑历史位置有误（useSSE.ts和sseParser.ts的编辑历史插在了中间），已修正至文件顶部编辑历史块最底部：

| 文件 | 修正前位置 | 修正后位置 | 状态 |
|------|----------|----------|------|
| `useChatCallbacks.ts` | :28-30（底部） | 无需修正 | ✅ |
| `useSSE.ts` | 函数旁（:481-483） | 文件顶部 :37-39 | ✅ 已修正 |
| `sseParser.ts` | 中间（:31-33） | 文件顶部 :48-50 | ✅ 已修正 |

---

## 六、编写人签名

**编写人**: 小欧
**编写时间**: 2026-09-09 22:12:00
**版本**: v1.2
