# [64]LLM补充采样参数设置说明

**创建时间**: 2026-09-22 22:19:22
**编写人**: 小欧
**版本历史**（按时间正序，旧条原文保留）:
- v1.0 2026-09-22 22:19:22 小欧 新建：缺什么常用参数 / 价值与场景与国内模型支持 / 可否设为模型无关通用参数 / 如何实施（四章节）
- v1.1 2026-09-22 22:34:52 小欧 分组定案（北京老陈拍板"整类迁 llm.sampling"）：采样参数进出调优组，新增 config.yaml 顶层 `llm.sampling` 域，UI=设置→通用；temperature/max_tokens 由 tuning.llm 迁入；注释清 context_limit 兜底=262144(256K) 属模型级（不进通用域）
- v1.2 2026-09-22 22:39:09 小欧 按北京老陈要求将本次新参配置化说明集中一处：新增 3.6 节（config.yaml 全量落盘一图看全，含新增 llm 域+迁移对照+留旧不动的 B 类/C 类提示）；3.2/3.5 两处散落的 yaml 代码块删除、改为引用 3.6
- v1.3 2026-09-22 22:43:15 小欧 按北京老陈要求补充"同步更新 config.example.yaml"事项：核实 config/ 现无活 example 文件（仅 2026-08-10 旧备份且结构脱节）；4.1 第 7 行加"example 需同步更新"、新增细节块（新建+全键同步+api_key 占位+旧备份保留+后续变更同步纪律）
- v1.4 2026-09-22 22:46:59 小欧 **纠错［北京老陈指正］**：v1.3 声称"config/ 无活 example"系误报——真身在 **`backend/config.yaml.example`**（与 config.yaml 同构、持续维护至 [62]v1.4）。修正：4.1 第 7 行与细节块改指 `backend/config.yaml.example`（追加 `llm` 域 6 键，非新建）；**新增 4.2 UI 位置定案**：通用组「最大步数」下方新增大分组「模型参数」（sectionOf 加 general 判断），tuning 组剔除采样项留 B 类。全文档 4-x 目录同步。
- v1.5 2026-09-23 03:56:06 小欧 **纠错［核验根因F1/F2］**：修正致命样板错选——4.1#4"仿 temperature 现写法"改为仿 max_tokens None 透传（实测 service.py:119 temperature 恒非 None 短路 base_service.py:116 全局兜底，tuning.llm.temperature 主链路是死键）；修正 3.1"temperature 本就全局生效"谎报（仅 max_tokens 真全局）；4.1#5 resolver 改为无需独立改（快照天然继承）；新增第五章代码 diff 说明。
- v1.6 2026-09-23 04:03:53 小欧 **纠错［全文复核］**：①推翻 v1.5 的"snapshot 无需改"——实测 base_service.py:184-197 snapshot 系显式构造，必须同步加 top_p 三参透传，否则切会话丢采样值；②"逐字节一致"改为"语义一致"（全局默认值 1.0/0 解析后会被拼进 body，字节必多键）；③4.1#4 澄清两段式（service.py 只读 provider_config，读全局的是 base_service）；④错字：北京老陳→北京老陈；行号 L190/274/335→L189/273/334。
- v1.7 2026-09-23 04:09:32 小欧 **补齐第五章**：新增 5.5 settings_registry（general 加 6 条目＋tuning 剔 2 键＋OLD_KEY_MAP 2 映射）、5.6 base_service（签名＋读键迁移＋request 三处透传）、5.7 service.py 解析缺省＋client_sdk 三签名、5.8 前端 SettingsGroup sectionOf 加 general「模型参数」分支。
- v1.8 2026-09-23 04:12:07 小欧 **纠错［第三章设计复核］**：①3.2 temperature 迁入仍死——service.py:119 必须同步改 None 透传（死键复活，默认值同为 0.7 故存量无感）；②llm 域混装澄清（A 类采样/生成＋1 个 C 类全局缺省）；③"统一下限"改"全局缺省"；④"旧键不读写"改"读旧写新一次、之后废弃"；⑤A 类改称"采样/生成"（max_tokens 属输出控制）。
- v1.9 2026-09-23 04:17:21 小欧 **纠错［第四章复核］**：①temperature 改法归属由#3移回#4；②#2 补 request/request_stream 两公有签名；③provider 计数 4→5（opencodeZen 按上游透传）；④#6 路径纠正为 services/agent/llm_call.py（L89 读 tuning，L94-96 不传采样参靠实例默认）；⑤P2 回退语更新；⑥4.2 键序明确前 5 照 3.2、第 6 殿后。
- v1.10 2026-09-23 04:28:29 小欧 **第五章纪律**：①5.1 补 temperature 改后行；②5.4 resolver 裸说明换调用点引用＋快照跟随断言；③5.8 tuning 裸说明换不动点代码引用（全章每节必带代码块）。
- v1.11 2026-09-23 04:36:01 小欧 **第四章对表补齐**：①5.5 值域按北京老陈拍板改 200000~2000000（3.5 同步回填）；②5.6 补 self 赋值三行；③新增 5.9（llm_call 不动点＋config 落盘确认）；④#2“llm 顶层”移备注。

---

## 一、缺什么常用参数

> 编写人：小欧 2026-09-22 22:19:22

### 1.1 现状请求体实际拼入的字段

后端 LLM 请求体构建唯一入口 `_build_request_body`（`backend/app/llm/client_sdk.py:51-89`）当前只拼：

| 字段 | 来源 |
|------|------|
| model / messages | 固定 |
| max_tokens | `provider_config.max_tokens` |
| temperature | `provider_config.temperature`（默认 0.7） |
| seed | `provider_config.seed`（可 None） |
| stream / stream_options | 流式开关 + tuning.llm.stream_options |
| tools / tool_choice / parallel_tool_calls | 工具调用 |
| extra_body | `model_params.{model}` 余量透传（服务层第二通道，绕过校验） |
| context_limit | ❌ 无请求体字段（**窗口基准，非 OpenAI 字段**）；`model_params.{model}.context_limit` 逐模型配置，未配置时兜底 `DEFAULT_CONTEXT_LIMIT=262144`(256K，`compaction_constants.py:41`) |

### 1.2 缺失的常用采样参数清单

| 参数 | OpenAI 兼容字段名 | 值域 | 是否在注册表中 | 当前能否传到请求体 |
|------|------------------|------|:---:|------|
| top_p | `top_p` | 0~1（默认 1） | ❌ | ⚠️ 仅能躲进 extra_body 手写 config.yaml |
| frequency_penalty | `frequency_penalty` | -2~2（默认 0） | ❌ | ⚠️ 同上 |
| presence_penalty | `presence_penalty` | -2~2（默认 0） | ❌ | ⚠️ 同上 |
| stop | `stop` | 字符串/数组 | ❌ | ❌ 完全不可达 |
| top_k | `top_k`（**非 OpenAI 标准**） | 1+ | ❌ | ⚠️ 部分国内厂商（如 minimax）支持 |
| n / logit_bias / user / response_format | — | — | ❌ | ❌（评估不建议加，见第三章 3.4） |

结论：**系统当前对"除 temperature/seed 外的采样控制"零正式支持**（temperature 挂在 tuning 调优组，语义错位），全靠 extra_body 透传逃生门（无类型校验、无 UI、有 400 风险）。这就是本文档要补的缺口。

> 关联项：上下文窗口兜底 262144(256K) 已核实，见第三章 3.5（北京老陈要求该值在 config.yaml 体现）。

---

## 二、有什么价值、什么场景有用、哪些国内模型支持

> 编写人：小欧 2026-09-22 22:19:22

### 2.1 各参数的价值与适用场景

| 参数 | 价值 | 典型场景（本系统视角） |
|------|------|------------------------|
| **top_p**（0~1） | 核采样：只从累计概率达标的最小候选集合里采样，等效"裁剪长尾噪声词" | ①**工具选择**：FC 模式下把 top_p 降到 0.8~0.9，减少模型在"多个可用工具"之间摇摆、误选伪工具名；②**代码/JSON 输出**：调低 top_p 提升结构化输出稳定性；③与 temperature 二选一（见 2.3） |
| **frequency_penalty**（-2~2） | 按"词/符号出现的频次"惩罚重复 | ①**Agent 长任务**：模型反复调用同一工具、反复输出同一路径/同一段代码——此项直接压制工具调用层面的重复；②结果汇总重复内容被有效降低 |
| **presence_penalty**（-2~2） | 按"是否出现过"惩罚（讨论过的话题整体降权） | ①长链推理中模型**绕圈子复述旧结论**；②多轮对话中换话题时甩不掉旧主题的粘性 |
| stop | 停止序列 | 输出到固定边界即止（如 JSON 收尾、表格结尾），需维护边界串列表，收益场景窄（本期不做，二期备选） |
| top_k | 截取概率最高的 K 个候选 | 仅对支持该非标字段的厂商有意义（如 minimax）；**OpenAI 兼容主流接口会 400 拒绝**（见 3.4 结论） |

### 2.2 哪些国内模型支持（按本项目实际配置的 provider 域）

本项目当前真实配置 5 个 provider（`config/config.yaml` ai 区，均走 OpenAI 兼容 /v1 接口）：**sensenova（商汤）、qiniu（七牛聚合：deepseek/glm/kimi/minimax）、zhipuai（智谱 GLM）、agnes、opencodeZen**。

| 参数 | DeepSeek 系 | 智谱 GLM 系 | Moonshot Kimi 系 | MiniMax 系 | 商汤 sense 系 | agnes / opencodeZen（聚合/桥接） |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| temperature | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| top_p | ✅ | ✅ | ✅ | ✅（另有 top_k） | ✅ | 🔶 按上游透传 |
| frequency_penalty | ✅ | 🔶 部分版本 | ✅ | ✅ | 🔶 需实测 | 🔶 按上游透传 |
| presence_penalty | ✅ | 🔶 部分版本 | ✅ | ✅ | 🔶 需实测 | 🔶 按上游透传 |
| seed | ✅ | 🔶 部分 | ✅ | ✅ | 🔶 需实测 | 🔶 按上游透传 |
| stop | ✅ | ✅ | ✅ | ✅ | 🔶 需实测 | 🔶 按上游透传 |

> **说明（诚信标注，不得夸大）**：
> 1. ✅ 表示各家官方 OpenAI 兼容文档明确支持；🔶 表示"厂商文档未明示/版本间不一致"，**必须按本系统一贯做法对目标 provider 发真实请求验证字段合法性**（同 [62] BY-08 对 reasoning_effort 的做法），不能仅凭推测上线。
> 2. **top_k 是特例**：DeepSeek/GLM/Kimi 的 OpenAI 兼容端点均**不认 top_k**（会 400），仅 MiniMax 型厂商接受 → 论证结论为"不做通用 top_k"（见 3.4）。
> 3. 所有 provider 最终都汇聚到本系统 `_build_request_body` 这一个拼体入口，参数是否生效取决于"拼进去了 + 对端认这个字段"两者同时满足。
> 4. temperature / top_p / frequency_penalty / presence_penalty / max_tokens 均为**模型无关**的采样/输出控制，已定案整类迁入顶层域 `llm.sampling`，归属"设置 → 通用"（见 3.2/3.3）。

### 2.3 temperature 与 top_p 的互斥提醒

OpenAI 官方建议：**temperature 与 top_p 不同时使用**（两者都作用于采样分布，同时改会产生叠加且互相掩盖）。落地时 UI 应给提示（如：修改 top_p 时提示"建议与 temperature 二选一为主控"），后端不做强制互斥（多做反而限制自由度，语义留给用户）。

---

## 三、可否设置为与模型无关的通用参数

> 编写人：小欧 2026-09-22 22:19:22

### 3.1 直接结论

**top_p / frequency_penalty / presence_penalty 可以，而且应当做成"模型无关的全局通用参数"。**

理由（OpenAI 兼容体系的事实）：
- 这三个字段在 OpenAI 兼容巨头（DeepSeek/GLM/Kimi/MiniMax）里是**同名同义同值域**的公共语言，不随模型变；
- 它们的语义（采样分布裁剪/重复惩罚）是"模型属性无关"的，不像 `context_limit`（受模型窗口硬约束）必须逐模型配置；
- 系统全局参数先例：`tuning.llm.max_tokens` 是跨模型全局生效的真先例（service.py:118 可 None → base_service.py:120 兜底）；`client_sdk.py:172` 早已预留 `llm.provider_urls` 顶层命名（`llm` 域有先例，本次落地）。

> ⚠️ **【v1.5 根因F2修正 2026-09-23 小欧】**：v1.0-v1.4 曾断言"temperature/max_tokens 本就是跨模型全局生效"——**temperature 半边是谎报，特此纠正**。实测 `service.py:119` 为 `float(provider_config.get("temperature", 0.7))` **恒非 None** → `base_service.py:116` 的 `if temperature is None` 全局兜底分支**永不触发** → `tuning.llm.temperature` 主链路是**死键**。只有 `max_tokens` 是真全局（`service.py:118` 可 None 透传 → `base_service.py:120` 兜底生效）。因此本方案采样三参的正确样板是 **仿 max_tokens（None 透传）**，切勿仿 temperature（见 4.1#4）。

### 3.2 落点：新增 config.yaml 顶层 `llm.sampling`（UI=设置→通用，模型无关）

北京老陈 2026-09-22 拍板：采样参数**不进"调优"组**，整类迁入新增顶层域 `llm.sampling`，UI 归"设置 → 通用"。完整配置文件落盘见 **3.6 集中落盘**。
> 【v1.8 澄清】`llm` 顶层含两部分：A 类采样/生成 5 键（模型无关）＋1 个 C 类全局缺省 `llm.context_limit_default`（模型相关，逐模型可覆盖；UI 通用组「模型参数」小节内单独标注"全局缺省"）。

| 键 | 类型 | 默认值 | 值域 | 来源 | 说明 |
|------|------|--------|------|------|------|
| `llm.sampling.temperature` | float | 0.7 | 0~2 | tuning.llm 迁入 | LLM 采样温度 |
| `llm.sampling.max_tokens` | int | 16384 | 1~100000 | tuning.llm 迁入 | 单次最大输出 token |
| `llm.sampling.top_p` | float | 1.0 | 0~1 | 新增 | 核采样阈值，1.0=不启用 |
| `llm.sampling.frequency_penalty` | float | 0 | -2~2 | 新增 | 频次重复惩罚 |
| `llm.sampling.presence_penalty` | float | 0 | -2~2 | 新增 | 话题重复惩罚 |

默认值语义【v1.6 修正】：top_p=1.0/penalty=0 在语义上等价于不启用 → **不设置的用户行为与现状语义一致（注意：请求体字节会多出显式的 top_p=1.0/penalty=0 键，不再逐字节一致）**。
迁移：`tuning.llm.temperature` / `tuning.llm.max_tokens` 走 `OLD_KEY_MAP` 平滑迁入（旧 yaml 值带入、不留新旧双源，禁 backward）。

### 3.3 与"调优组 / 模型级参数"的关系辨析（三分法不冲突）

| 类 | 配置域 | UI | 参数 | 模型无关 |
|----|--------|----|------|:---:|
| A 采样/生成 | `llm.sampling`（新增） | 设置 → 通用 | temperature / top_p / frequency_penalty / presence_penalty / max_tokens（注：max_tokens 属输出控制，随组同迁仅为收敛管理） | ✅ |
| B 调优 | `tuning.llm` / `tuning.llm_net`（现状） | 设置 → 调优 | stream_options / retries / response_* / 超时 / 连接池 | ✅ |
| C 模型级 | `ai.{provider}.model_params.{model}`（现状） | 模型管理（逐模型） | context_limit / reasoning_effort | ❌ 逐模型 |

**实施策略**：A 类走 `client_sdk._build_request_body` 显式拼参（默认即旁路），**不改成逐模型特殊处理**；B 类现状不动；C 类个别需求仍走 `model_params` 逃生门，本次不扩展。

### 3.4 明确"不做"的（模型无关性反面）

| 候选 | 结论 | 理由 |
|------|------|------|
| top_k | **不做通用参数** | 非 OpenAI 标准，主流 vendor 400 拒绝；若个别厂商（如 MiniMax）需要，应走 model_params 逃生门逐模型配，归模型级而非通用级 |
| n / logit_bias / user / response_format | 不做 | 无消费方 / 结构难 UI / 非采样需求，YAGNI |
| stop | 二期备选 | 需维护边界串，收益窄；若做同样入 llm.sampling |

### 3.5 context_limit 兜底 256K 的配置化（C 类关联项）

> 北京老陈 2026-09-22 拍板：兜底上下文大小必须在配置文件中体现，不能只藏在代码常量里。

**现状查证**：

| 项 | 位置 | 值 |
|----|------|-----|
| 兜底常量 | `backend/app/services/agent/compaction_constants.py:41` | `DEFAULT_CONTEXT_LIMIT = 262144`（**256K，不是 250K**） |
| 语义 | 同上 L39-41 | 上下文窗口默认上限；`model_params.{model}.context_limit` **未配置时**才用它兜底 |
| 消费链 | `service.py:99` `pop("context_limit", DEFAULT_CONTEXT_LIMIT)` → BaseAIService → 压缩/裁剪门限（start×0.5 / loop×0.75 / buffer 20K，`message_builder.py:100` 联动） | 窗口基准 |

**5 家 provider 实际命中情况**（对照 `config/config.yaml` ai 区）：

| provider | 是否命中 256K 兜底 | 生效窗口 |
|----------|:---:|------|
| sensenova（deepseek-v4-flash/glm-5.2/deepseek-v4-pro） | ❌ 配置优先 | 900000 |
| sensenova（sensenova-6.8-flash-lite） | ❌ 配置优先 | 200000 |
| opencodeZen（5 个模型） | ❌ 配置优先 | 10000 |
| agnes | ✅ **兜底 256K** | 262144 |
| qiniu / zhipuai | ✅ **兜底 256K** | 262144 |

**配置化落点**（C 类 → 不进 `llm.sampling`，归模型级窗口域；完整 yaml 见 **3.6 集中落盘**）：

| 候选键 | 归属 | 说明 |
|--------|------|------|
| （无） | 现状 | 256K 只在代码常量，**config.yaml 无体现** — 北京老陈要求补 |
| `llm.context_limit_default` | llm 顶层（C 类全局缺省） | 全局缺省默认 262144，值域 200000~2000000（北京老陈拍板）；`service.py:99` pop 缺省改读此键：`get_config().get("llm.context_limit_default", DEFAULT_CONTEXT_LIMIT)` |
| `ai.{provider}.model_params.{model}.context_limit` | 逐模型（已有） | 配置优先，覆盖全局兜底 — 不新增 |

**关键设计**：全局缺省为"所有未配置窗口的模型的统一缺省值"，逐模型配置仍优先（现状语义不变）；`llm.context_limit_default` 让"临时调大/调小全系统窗口"不用改代码，贴近老陈要"配置文件体现"的诉求。

### 3.6 本次新增/迁入配置集中落盘（一图看全）

> 北京老陈 2026-09-22 拍板：本次几个参数更新的配置文件**集中一处说明**，不分散在章节各处。
> 下方为 `config/config.yaml` 本次改动**全量汇总**：新增顶层 `llm` 域 + 逐模型窗口不动 + 调优组剔除 2 键。

```yaml
# ============ 本次新增：顶层 llm 域（模型无关通用，UI=设置→通用） ============
llm:
  sampling:                   # A类 采样/生成控制（5 键）
    temperature: 0.7          # 迁入：自 tuning.llm.temperature
    max_tokens: 16384         # 迁入：自 tuning.llm.max_tokens
    top_p: 1.0                # 新增：核采样阈值，1.0=不启用
    frequency_penalty: 0      # 新增：频次重复惩罚
    presence_penalty: 0       # 新增：话题重复惩罚
  context_limit_default: 262144   # C类全局兜底（256K）：model_params.{model}.context_limit 未配置时生效

# ============ 本次不动（已有域，仅提示归属） ============
ai:                           # 模型级（C 类，逐模型窗口优先于 llm.context_limit_default）
  <provider>:
    models: [...]             # 已有
    model_params:             # 已有；context_limit / reasoning_effort 留此，不出现在 llm 域
      <model>:
        context_limit: <N>    # 配置优先，覆盖 llm.context_limit_default

tuning:                       # B类 调优/协议（剔除 2 个迁出键，其余留原处）
  llm:
    tool_choice: auto         # 留原地【本期不迁】
    stream_options: ...       # 留原地【本期不迁】
    response_*                # 留原地【本期不迁】
    # temperature / max_tokens 已迁出到 llm.sampling（读旧写新一次：OLD_KEY_MAP 读旧 yaml 值带入，之后旧键废弃）
```

> 对应关系速查：**llm.sampling.*（A类采样）→ 设置→通用**；**tuning.llm / tuning.llm_net（B类调优/协议）→ 设置→调优，本期不动**；**ai.{provider}.model_params（C类模型级）→ 模型管理，逐模型配置优先于 llm.context_limit_default**。

---

## 四、如何实施

> 编写人：小欧 2026-09-22 22:19:22

> 遵循项目铁规：**能复制就复制、不重写**；TDD 红→绿→验证门全过再收尾；禁 commit 测试代码文件；代码加署名+日期注释。

### 4.1 改动清单（后端为主，前端迎合后端）

| # | 文件 | 改动 | 层 |
|---|------|------|-----|
| 1 | `backend/app/services/settings/settings_registry.py` | **新增 `llm.sampling` 子组 5 键**（temperature/max_tokens 迁入 + top_p/frequency_penalty/presence_penalty 新增）**+ `llm.context_limit_default`**；`tuning.llm.temperature/max_tokens` 从 tuning 组移除；OLD_KEY_MAP 加迁入映射；range_ 值域照 3.2/3.5 表 | 注册表（唯一 Schema 源） |
| 2 | `backend/app/llm/client_sdk.py` | `_build_request_body` 签名加 `top_p / frequency_penalty / presence_penalty: Optional[float]`，`if x is not None: body[key]=x`（仿 seed/max_tokens 的 None 透传写法；**切勿仿 temperature 的 float(get(...,0.7)) 恒非 None 写法**）；`request`（L184-186）/`request_stream`（L228-230）两公有签名同步加三参并透传给 `_build_request_body`（备注：`llm: {}` 顶层已读 provider_urls，属现状非改动） | 请求体拼装 |
| 3 | `backend/app/llm/base_service.py` | 构造参数加三字段 + `self.top_p=` 等（仿 L111-122 seed 先例）；`request` 三处调用点透传（L189-191/273-275/334-336）；**temperature/max_tokens 读键由 `tuning.llm.*` 改 `llm.sampling.*`**（L117/120）；采样三参兜底仿 max_tokens 写成 `if top_p is None` 才读全局 | 服务层 |
| 4 | `backend/app/services/lifecycle/service.py` | `create_service_instance` 两段式：本层只读 `provider_config.get("top_p")`（**仿 max_tokens 的 None 透传现写法**，可 None；provider_config 同名键覆盖优先于全局）；读 `llm.sampling.*` 全局兜底的是 `base_service`（见#3），本层不直读全局；**`service.py:119` temperature 同步改 None 透传**（`provider_config.get("temperature")` 可 None，死键复活，见 5.2）；**`parse_model_params` pop 缺省改 `get_config().get("llm.context_limit_default", DEFAULT_CONTEXT_LIMIT)`**（L99） | 实例装配 |
| 5 | `backend/app/services/model/resolver.py` + `backend/app/llm/base_service.py:184-197 snapshot` | **必须同步改 snapshot**【v1.6 推翻 v1.5】：snapshot 系显式构造（L184-197 已列 max_tokens/temperature/seed），必须加 `top_p=self.top_p` 等三行透传，否则 L2 跨 provider 切会话丢采样值；resolver.py 本体无需逐参逻辑，只确认快照不断链 | 会话覆盖 |
| 6 | `backend/app/services/agent/llm_call.py` | **无需改键**——实测 L89 `tool_choice` 读 `tuning.llm.tool_choice`，L94-96 调 `request_stream` 只传 messages/tools/tool_choice、不传采样参（靠实例默认值，采样三参自动跟随）；tool_choice / stream_options / response_* / stream_max_retries 均属 B 类留 `tuning.llm`；仅当后续 tool_choice 定案迁 `llm.sampling` 时才动 L89 | 调用链 |
| 7 | `config/config.yaml` + **`backend/config.yaml.example`（需同步更新）** | 落地 `llm.sampling.*` 5 键 + `llm.context_limit_default: 262144`；**`backend/config.yaml.example` 必须与 `config.yaml` 结构同步**（追加顶层 `llm` 域，见 3.6）；两份文件 api_key 一律占位符（**不含任何 api_key 明文**） | 配置 |

> 关键设计【v1.6 修正】：**默认值即旁路指语义旁路（top_p=1.0/penalty=0 等价于不启用），不是字节旁路**。三参在 create_service_instance 侧保持 None 可透传，base_service 侧 `is None` 才读 `llm.sampling.*` 全局；provider_config 显式键 > llm.sampling 全局 > 函数默认值。若仿 temperature 的 `float(get(...,缺省))` 恒非 None 写法，则全局兜底永不触发、新键全成死键——此为本次核验根因F1，切记避开。

> **注意【北京老陈拍板补充】**：`tuning.llm.tool_choice / stream_options / response_* / stream_max_retries` 暂**不迁**（B 类调优/协议留 tuning.llm），本期只迁温度与 token（纯采样语义）；llm_call.py 无需改键（tool_choice 留原地）。

> **细节同步更新 `backend/config.yaml.example`（北京老陈 2026-09-22 拍板）**：
> - 现状核查（v1.4 修正）：**example 真身在 `backend/config.yaml.example`**，与 config.yaml 同构、持续维护（截止 2026-09-22 已含 [61] tuning 31 键、[62]v1.4 zhipuai model_params/model_meta）；`config/` 下另有旧备份 `config.yaml.example.backup_20260810_080141`（早于单源收敛，勿作现行基线）。
> - 本次实施必须：**①在 `backend/config.yaml.example` 追加顶层 `llm` 域**（`llm.sampling.*` 5 键 + `llm.context_limit_default: 262144`，写法同 3.6）；**②api_key 一律占位符**（`sk_xxx...`/`ak_xxx...`，严禁任何真实密钥）；③文件头编辑历史按既有格式追加新行（版本号+时间+签名+改动说明，禁删旧条目）。
> - 后续维护纪律：**任何 config.yaml 结构变更，必须同步 example 文件**（同一次改动，不做落单）。

### 4.2 前端改动

> **UI 位置定案（北京老陈 2026-09-22 拍板）**：设置2版 → **通用** 组，在 **「最大步数」下方**新增独立分组 **「模型参数」**（对标系统组「运维日志/工程目录」小节式分组），集中展示本次 `llm.sampling.*` 5 键 + `llm.context_limit_default`；不复用调优组（采样类已整类迁出）。实现：`settings_registry.py` 通用组 items 在 `agent.max_steps` 之后追加 6 个新条目（前 5 键序照 3.2 表，`llm.context_limit_default` 第 6 殿后）；前端 `SettingsGroup.sectionOf` 对 `general` 组加一节：`llm.sampling.*` 与 `llm.context_limit_default` → **「模型参数」** 小节（既有系统组 3 小节机制复用，不改渲染主逻辑）。

| # | 改动 | 说明 |
|---|------|------|
| 1 | 通用组在「最大步数」下方新增大分组 **「模型参数」**，其下渲染 `llm.sampling.*`(5 键) + `llm.context_limit_default` | 由 `settings_registry.py` 通用组 items 添加 6 条目 + `SettingsGroup.sectionOf` 对 general 组判「模型参数」小节；其余无需改前端代码（对齐 [62] P9 既有事实） |
| 2 | **调优组剔除采样项**：tuning 组 items 移除 temperature/max_tokens（迁往通用「模型参数」），留存 tool_choice/stream_options/response_*/stream_max_retries 等 B 类项 | 与 3.3 三分法 B 类留 tuning 一致；tuning 组小节标题「LLM 语义参数」顺延不变 |
| 3 | 前端 notice 区显示互斥/旁路提示（文字来自注册表 notice 字段） | 无需硬编码 |

### 4.3 测试与验证（TDD，禁 commit 测试文件）

| 步骤 | 内容 |
|------|------|
| 红 | 写单测断言：`_build_request_body` 三参 None 时 body 不含对应键；设值时含且类型正确；`parse_model_params` 无 context_limit 时读 `llm.context_limit_default` |
| 绿 | 实现 4.1 改动后用例转绿 |
| 单测补充 | `parse_model_params`/快照构造透传断言 + OLD_KEY_MAP 迁移用例（tuning.llm.temperature 旧值迁入 llm.sampling.temperature） |
| Provider 真实验证 | 对 qiniu(deepseek)、zhipuai(glm)、sensenova、agnes、opencodeZen 5 家各发一条**真实请求**含 top_p=0.9，断言接受（opencodeZen 按上游透传，只断言不 400）；frequency/presence_penalty 按 2.2 表对 🔶 项实测，失败者记入"不支持清单"并在 notice 提示 |
| 全量回归 | `pytest` 后端全量 + 前端 `npm run check` + settings2 单测，无退化 |

### 4.4 实施顺序与风险

| 阶段 | 内容 | 验证门 | 风险 | 回退 |
|------|------|--------|------|------|
| P1 | 注册表新增 `llm.sampling` 5 键 + `llm.context_limit_default` + client_sdk/base_service 透传（默认旁路） | 单测红→绿；真实请求 5 provider 接受 top_p | 低（仿写既有写法） | 默认值语义旁路=零感知回退 |
| P2 | snapshot 加采样三参透传 + OLD_KEY_MAP 迁入 t/m_tokens + 全量单测 | 会话切换后参数仍生效；旧 yaml 温度值平滑迁入 | 中（迁移点） | snapshot 显式透传 self 值，无缺参分支；实例 None 则 __init__ 兜底读全局，不报错 |
| P3 | 不支持清单收集 + notice 文案 | 文档记录 + UI 展示 | 低 | 文案可改 |

### 4.5 最终效果（验收标准）

- 设置2版 → **通用** 组出现 `llm.sampling` 下 **temperature / max_tokens / top_p / frequency_penalty / presence_penalty** 与 **context_limit_default(256K)**，范围校验生效（负数/越界被拒，同 [62] #7 修复）；
- 保存后 `_build_request_body` 产物含采样三字段；不设置的用户请求语义与改动前一致（body 会显式带 top_p=1.0/penalty=0，不逐字节一致）；
- 兜底窗口在 `config.yaml` `llm.context_limit_default` 可见可改，逐模型 model_params 仍优先；
- 五个国内 provider 真实请求全部接受 top_p（opencodeZen 按上游透传，只断言不 400）；penalty 项在"已实测支持"厂商生效、"不支持"厂商有 notice 提示；
- 全量回归无退化。

---

## 五、代码修复 diff 说明（v1.5 新增，v1.11 全改动点必带代码块）

> 编写人：小欧 2026-09-23 03:56:06 ｜ 本章只给 diff 意图（不动代码，待实施时由后端照此落地）。

### 5.1 根因F1：service.py 采样三参必须仿 max_tokens（None 透传）

现状实测（`backend/app/services/lifecycle/service.py:118-119`）：

```python
max_tokens=provider_config.get("max_tokens"),          # 可 None → base_service 兜底活
temperature=float(provider_config.get("temperature", 0.7)),  # 恒非 None → base_service 兜底死
```

错误写法（照抄 temperature，**禁止**）：

```python
# ❌ 恒非 None → base_service 的 if top_p is None 永不触发 → llm.sampling.top_p 死键
top_p=float(provider_config.get("top_p", 1.0)),
```

正确写法（仿 max_tokens，**采用**）：

```python
# ✅ 可 None → None 时 base_service 读 llm.sampling.top_p 全局兜底（真旁路）— 小欧 2026-09-23
top_p=provider_config.get("top_p"),
frequency_penalty=provider_config.get("frequency_penalty"),
presence_penalty=provider_config.get("presence_penalty"),
temperature=provider_config.get("temperature"),  # ✅ v1.8 死键复活：去 float(...,0.7)恒非 None，改可 None（默认 0.7 由 llm.sampling.temperature 给）
```

### 5.2 根因F2：base_service.py 采样兜底读 llm.sampling（仿 max_tokens 分支）

现状实测（`backend/app/llm/base_service.py:116-120`）：temperature 分支永不触发，max_tokens 分支是活样板。新增三参照 max_tokens 分支仿写：

```python
# ✅ 仿 max_tokens 分支（L120）— 小欧 2026-09-23
if top_p is None:
    top_p = get_config().get("llm.sampling.top_p", 1.0)
if frequency_penalty is None:
    frequency_penalty = get_config().get("llm.sampling.frequency_penalty", 0)
if presence_penalty is None:
    presence_penalty = get_config().get("llm.sampling.presence_penalty", 0)
# temperature/max_tokens 读键由 tuning.llm.* 改 llm.sampling.*（OLD_KEY_MAP 保证旧值迁入）
# ⚠️【v1.8 死键复活】service.py:119 必须同步改 None 透传，否则迁入仍死：
#   改前：temperature=float(provider_config.get("temperature", 0.7))  # 恒非 None，兜底永死
#   改后：temperature=provider_config.get("temperature")  # 可 None，None 时 base 读 llm.sampling.temperature
# 死键复活说明：旧全局 tuning.llm.temperature 从未生效；新全局默认值同为 0.7，故存量用户无感；若用户改过旧全局值，迁入后首次生效，需在 notice 提示。
```

### 5.3 client_sdk.py 请求体拼装（仿 seed 的 None 判断）

```python
# ✅ 仿 seed 现写法：None 即不拼（旁路），有值才拼 — 小欧 2026-09-23
if top_p is not None:
    body["top_p"] = top_p
if frequency_penalty is not None:
    body["frequency_penalty"] = frequency_penalty
if presence_penalty is not None:
    body["presence_penalty"] = presence_penalty
```

### 5.4 snapshot 必须同步加三参（v1.6 推翻 v1.5“无需改”）

实测 `backend/app/llm/base_service.py:184-197` snapshot 系显式构造（已列 max_tokens/temperature/seed），**必须加三行**，否则 L2 切会话丢采样值：

```python
# ✅ v1.6 修正 — 小欧 2026-09-23
snap = BaseAIService(
    ...
    max_tokens=self.max_tokens,
    temperature=self.temperature,
    top_p=self.top_p,  # 新增
    frequency_penalty=self.frequency_penalty,  # 新增
    presence_penalty=self.presence_penalty,  # 新增
    seed=self.seed,
    ...
)
```

`resolver.py` 调用点现状引用＋快照跟随断言（本体零新增行，凭单测锁死不断链）：

```python
# ✅ resolver.py:205-209 调用点原样不动（快照三参由 base 侧带入，不在此逐参）— 小欧 2026-09-23
session_client = ai_service.snapshot(
    model_ref,
    extra_body_params=_pv_ebp,
    context_limit=_pv_ctx,
)
# ✅ 快照跟随断言（TDD 红→绿锁死）— 小欧 2026-09-23
def test_snapshot_carries_sampling():
    snap = svc.snapshot(model_ref)
    assert (snap.top_p, snap.frequency_penalty, snap.presence_penalty) == \
           (svc.top_p, svc.frequency_penalty, svc.presence_penalty)
```

### 5.5 settings_registry.py 注册表（general 加 6 条目＋tuning 剔 2 键＋OLD_KEY_MAP 迁入）

现状实测（`backend/app/services/settings/settings_registry.py:69-71` general 5 项止于 `agent.max_steps`；`:151-155` tuning.llm 7 键含 temperature/max_tokens；`:228` OLD_KEY_MAP）：

```python
# ✅ general 组 agent.max_steps 之后追加 6 条目 — 小欧 2026-09-23
_item("agent.max_steps", "int", "最大步数", 10000, range_=[1, 10000],
      notice="单任务最大执行步数，超限中止"),
_item("llm.sampling.temperature", "float", "采样温度", 0.7, range_=[0, 2],
      notice="LLM 采样温度，0=确定性，2=高随机（自 tuning.llm.temperature 迁入）"),
_item("llm.sampling.max_tokens", "int", "单次最大 token", 16384, range_=[1, 100000],
      notice="LLM 单次调用最大输出 token 数（自 tuning.llm.max_tokens 迁入）"),
_item("llm.sampling.top_p", "float", "核采样 top_p", 1.0, range_=[0, 1],
      notice="核采样阈值，1.0=不启用；建议与温度二选一为主控"),
_item("llm.sampling.frequency_penalty", "float", "频次惩罚", 0, range_=[-2, 2],
      notice="按词频惩罚重复，0=不启用"),
_item("llm.sampling.presence_penalty", "float", "存在惩罚", 0, range_=[-2, 2],
      notice="按是否出现过惩罚复述，0=不启用"),
_item("llm.context_limit_default", "int", "默认上下文窗口", 262144, range_=[200000, 2000000],
      notice="model_params 未配 context_limit 时的全局缺省（256K；北京老陈拍板值域 200K~2M）"),
# tuning 组删除 tuning.llm.temperature / tuning.llm.max_tokens 两行（L151-155 中的两条）
# OLD_KEY_MAP 加迁入映射（旧 yaml 值带入，不留双源）：
OLD_KEY_MAP = {
    ...
    "tuning.llm.temperature": "llm.sampling.temperature",  # 新增
    "tuning.llm.max_tokens": "llm.sampling.max_tokens",  # 新增
}
```

### 5.6 base_service.py 签名＋读键迁移＋request 三处透传（补 5.2 未覆盖）

现状实测（`backend/app/llm/base_service.py:103-113` 构造签名；`:116-120` 兜底；`:269-275` request 非流式；`:330-336` request_stream 流式；第三处为 fallback/重试内同形调用，按同规则加三参）：

```python
# ✅ __init__ 签名加三字段（仿 seed 先例 L111）— 小欧 2026-09-23
def __init__(
    self,
    ...
    max_tokens: Optional[int] = None,
    temperature: float = None,
    top_p: Optional[float] = None,  # 新增
    frequency_penalty: Optional[float] = None,  # 新增
    presence_penalty: Optional[float] = None,  # 新增
    seed: Optional[int] = None,
    ...
):
# ✅ 读键迁移＋新键兜底（L116-120 段内改写）
if temperature is None:
    temperature = get_config().get("llm.sampling.temperature", _D_TEMPERATURE)  # tuning.llm.temperature→llm.sampling.temperature
self.max_tokens = max_tokens if max_tokens is not None else get_config().get("llm.sampling.max_tokens", _D_MAX_TOKENS)  # 同上迁移
# ✅ request 三处调用点透传（以 L269-275 非流式为例，其余两处同形）
response = await self._llm_sdk.request(
    messages=messages,
    tools=tools,
    tool_choice=tool_choice,
    max_tokens=self.max_tokens,
    temperature=self.temperature,
    top_p=self.top_p,  # 新增
    frequency_penalty=self.frequency_penalty,  # 新增
    presence_penalty=self.presence_penalty,  # 新增
    seed=self.seed,
    extra_body=self.extra_body_params,
)
```
# ✅ 实例属性化三行（snapshot 读的正是这三个属性，仿 seed 的 self.seed=seed）— 小欧 2026-09-23
self.top_p = top_p  # 已兜底全局后的定值
self.frequency_penalty = frequency_penalty
self.presence_penalty = presence_penalty
```

### 5.7 service.py 解析缺省＋client_sdk 三签名（补 5.1/5.3 未覆盖）

现状实测（`backend/app/services/lifecycle/service.py:99` parse_model_params 内 pop；`backend/app/llm/client_sdk.py:51-63` _build_request_body 签名；`:184-186` request 签名；`:229-230` request_stream 签名）：

```python
# ✅ service.py:99 pop 缺省改读全局兜底 — 小欧 2026-09-23
extra_body_params, context_limit = parse_model_params(provider_config, final_model)
# parse_model_params 内部改写：
context_limit = model_params.pop("context_limit", get_config().get("llm.context_limit_default", DEFAULT_CONTEXT_LIMIT))
# ✅ client_sdk 三签名加三参（以 _build_request_body 为例，request/request_stream 同形加参并透传给它）
def _build_request_body(
    messages: List[Dict],
    model: str,
    max_tokens: Optional[int] = None,
    temperature: Optional[float] = None,
    top_p: Optional[float] = None,  # 新增
    frequency_penalty: Optional[float] = None,  # 新增
    presence_penalty: Optional[float] = None,  # 新增
    seed: Optional[int] = None,
    ...
```

### 5.8 前端 SettingsGroup sectionOf 加 general「模型参数」小节

现状实测（`frontend/src/features/settings2/components/SettingsGroup.tsx:33-52` sectionOf 仅认 system/tuning，general 无分支→渲染无小节标题）：

```tsx
// ✅ general 组加模型参数小节（仿 system/tuning 分支写法，能复制就复制）— 小欧 2026-09-23
function sectionOf(group: string, key: string): string | null {
  if (group === 'general') {
    if (key.startsWith('llm.sampling.') || key === 'llm.context_limit_default') return '模型参数';
    return null;  // 通用组其余项（workspace/logging/agent.*）保持无小节原样
  }
  if (group === 'system') { ... }  // 原样不动
  if (group === 'tuning') { ... }  // 原样不动（temperature/max_tokens 剔除后由 schema 自动消失，无代码）
  return null;
}
// tuning 分支原样不动点（temperature/max_tokens 剔除后由 schema 自动消失，凭此 5 行锁死无第二改动点）— 小欧 2026-09-23
  if (group === 'tuning') {
    if (key.startsWith('tuning.llm.')) return 'LLM 语义参数';
    if (key.startsWith('tuning.llm_net.')) return 'LLM 网络/超时/连接池';
```
// tuning.llm 剔除项与 general 新增项均由后端 schema 驱动（不动点凭上块锁死）— 小欧 2026-09-23

### 5.9 llm_call 不动点＋config 落盘确认（补 #6/#7 在第五章的零块）

```python
# ✅ llm_call.py:89/94-96 调用点原样不动（不传采样参，靠实例默认值跟随）— 小欧 2026-09-23
tool_choice = get_config().get("tuning.llm.tool_choice", _D_TOOL_CHOICE) if openai_tools else None
async for chunk in agent.llm_client.request_stream(
    messages=messages, tools=openai_tools, tool_choice=tool_choice,
):
```

```yaml
# ✅ config/config.yaml＋backend/config.yaml.example 落盘确认（写法同 3.6，api_key 一律占位）— 小欧 2026-09-23
llm:
  sampling: {temperature: 0.7, max_tokens: 16384, top_p: 1.0, frequency_penalty: 0, presence_penalty: 0}
  context_limit_default: 262144  # 值域 200000~2000000
```

---

> 更新人：小欧 2026-09-23 04:36:01
