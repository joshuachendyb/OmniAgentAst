# 01 前端3接收实时变量冗余重构方案

| 版本 | 时间 | 签名 | 修改简介 |
|------|------|------|---------|
| v1.5 | 2026-09-14 07:37:39 | 小欧 | 新增 5.5.3 E5 落地规格：模板库 API 签名、[DBG-1] 解析规则（recv 字段随改点E删除容错）、等待圈 selector、任务列表 aria-label、test 步骤→API 调用映射、监控探针函数设计（含禁止 commit 测试文件铁规） |
| v1.4 | 2026-09-14 07:34:03 | 小欧 | 复检四项非致命修正：B7 穷举 16→24 组（2×2×2×3）、5.5.2 总原则显式化"契约级可硬断言/失败模态只软诊断"、监控-B 改可观测窗口（A 已完成末段必为 final）、3.3 补 startinfo 先到时序前提 |
| v1.3 | 2026-09-14 07:23:27 | 小欧 | 十轮一致性检查修复：test02 标题 ②④→②③、R4 补 L331-341、B4 引用改双覆盖推出、取证探针 L237→L239、5.1 标题补全时间戳 |
| v1.2 | 2026-09-14 07:10:41 | 小欧 | 第五章增补：5.5.1 E5 专项测试用例详细设计（test01 历史切换 / test02 实时恒 true）、5.5.2 E5 双线监控要点（action-waiting 漏洞1 同步观测） |
| v1.1 | 2026-09-14 07:03:02 | 小欧 | 新增第五章：基于 TDD 的代码实施——测试资产盘点、case 处置总表（必须修改/必须重新写/必须新增）、A/B/C/D 四组单测 case 全量矩阵、E2E 回归清单、红-绿-重构实施顺序 |
| v1.0 | 2026-09-14 06:49:41 | 小欧 | 新建文档：streaming/isReceiving/receiving 冗余分析+解决方案+代码diff |

编写人：小欧

---

## 一、问题

前端三个状态变量 `streaming`、`isReceiving`、`receiving` 在实时期间**全部恒 true**，语义重叠严重，违反 DRY / KISS-DIRECT / 禁止backward 三大规范。

| 变量 | 定义出处 | 实时期间值 |
|------|---------|:----------:|
| `streaming` | RightViewer.tsx L472 `streaming={isCurrentLive}` | true |
| `isReceiving` | useSSE.ts L431 唯一真源 | true |
| `receiving` | useChatPanels.tsx L238 `receiving={isReceiving}` | true |

冗余链路 5 层绕圈：`isReceiving → receiving → badge → isCurrentLive → streaming → taskActive`，中间 2 层纯透传。

---

## 二、分析

### 2.1 三变量的真实语义

| 变量 | 真实语义 | 使用者数量 |
|------|---------|:----------:|
| `isReceiving` | SSE 连接在接收数据（全局，不绑定任务） | 20+ 处 |
| `receiving`（RightViewer 版） | **= isReceiving 纯改名直传** | 3 处 |
| `streaming`（= isCurrentLive） | 当前展示的任务是实时活跃任务 | 6 处 |

### 2.2 冗余判定

- `isReceiving` ↔ RightViewer `receiving` prop：**完全相等，纯改名**（useChatPanels L238 `receiving={isReceiving}`）→ DRY 违反
- `streaming` 的定义（isCurrentLive）内部又混入了 `receiving` 条件 → 重复计算
- `taskActive`（PipelineRenderer L282-286）= `streaming || highlight || badge` 并联 → streaming 与 badge 语义重叠

### 2.3 不能合并为1个变量的原因

| 场景 | isReceiving | streaming | 若只用 isReceiving |
|------|:-----------:|:---------:|-------------------|
| 实时收数、显示切到历史任务 | **true** | **false** | ❌ 历史回放被误加光标/齿轮 |
| final 到达、SSE 仍在发后续帧 | **true** | **false** | ❌ 统计区不显示 |
| SSE 断连、流水线仍停在屏幕 | **false** | **true** | ❌ 光标/打字机瞬间丢失 |

**结论：最小集合 = 2 个变量，无法缩到 1 个。**

---

## 三、解决方案

### 3.1 核心思路

| 保留 | 删除 | 改造 |
|------|------|------|
| `isReceiving`（唯一连接信号） | `receiving`（RightViewer 版纯改名层） | `isCurrentLive`（去 receiving 条件） |
| `streaming`（唯一展示信号） | | `taskActive`（去 streaming 条件） |
| TaskInfoBar `receiving`（秒表专属） | | `useTaskInfo`（删 receiving 参数） |

### 3.2 改造后变量格局

```
isReceiving ──→ 连接级：发送禁用 / beforeunload / 持久化 / 滚动 / 任务选择 / 秒表
streaming   ──→ 展示级：光标 / 打字机 / final跳过 / replay
taskActive  ──→ highlight || badge running/paused（权威驱动，不再重复 streaming）
```

### 3.3 关键改动说明

| 改动 | 为什么安全 |
|------|-----------|
| `useTaskInfo` 删 receiving 参数，startinfo 门改 `badge = 'running'` | startInfo 帧到达即"任务已开始"权威信号（后端 8.4.3），不再依赖连接状态；门只在 `badge==='idle'` 时执行，有步骤已由循环置 running 不受影响 |
| `isCurrentLive` 删 `receiving \|\|` | `hasBusinessSteps`（首业务步骤起恒 true）+ `liveBadge='running'`（startinfo 门无条件 running）双覆盖实时期间（**时序前提**：后端 startinfo 首帧先于业务步骤到达——后端 8.4.3 契约；若反向到达，thought-start 空窗的 live 语义由 A2/B2 边界锁定） |
| `taskActive` 删 `streaming \|\|` | taskActive 由 badge 权威驱动，streaming 保留给光标/打字机/final跳过/replay |
| TaskInfoBar `receiving` prop 保留 | 秒表启停 + shownElapsed 选择专属，与 badge 派生解耦 |

### 3.4 风险点及解决方案

| # | 风险 | 影响 | 解决方案 |
|---|------|------|---------|
| R1 | useTaskInfo 删 receiving 后，两个调用方参数顺序必须同步左移 | TS 编译报错 | 仅 RightViewer:209 和 TaskInfoBar:101 两处，已确认无第三方 |
| R2 | TaskInfoBar L101 的 liveError 是可选 prop，删 receiving 后第四参=detail（可选），签名匹配 | 无风险 | detail 可选、liveError 可选，useTaskInfo 新签名 `(steps, frames, detail?, liveError?)` 完全匹配 |
| R3 | isCurrentLive 去 receiving 后，startInfo→首步骤空窗内 badge 由 startinfo 门兜住 | 已验证 | startinfo 门改无条件 running，覆盖该空窗 |
| R4 | action-waiting 逻辑（L290-292/L331-341）保留，taskActive 语义变化后 thinking 流式中仍会追加 | 漏洞1行为不变 | 用户已确认本次不改 action-waiting，另行处理 |
| R5 | 编辑历史块插入位置 | 文档合规 | 每个文件的编辑历史块底部追加新条目 |

---

## 四、实施代码 diff

### 文件1：useTaskInfo.ts（3 处改动）

**改点 A — L83-89 签名删 receiving**
```typescript
// 当前
export const useTaskInfo = (
  steps: ExecutionStep[],
  frames: TaskMetaFrames,
  receiving: boolean,          // ← 删这一行
  detail?: TaskDetail | null,
  liveError?: LiveError | null
) => {

// 改为
export const useTaskInfo = (
  steps: ExecutionStep[],
  frames: TaskMetaFrames,
  detail?: TaskDetail | null,
  liveError?: LiveError | null
) => {
```

**改点 B — L244-250 startinfo 门**
```typescript
// 当前
    if (hasStartInfo && badge === 'idle') {
      console.log(
        `[DBG-3c] startinfo门: badge=idle, receiving=${receiving} → badge=${receiving ? 'running' : 'idle'}`
      );
      badge = receiving ? 'running' : 'idle';
    }

// 改为
    if (hasStartInfo && badge === 'idle') {
      // 2026-09-14 小欧 冗余重构: startInfo 帧到达即"任务已开始"权威信号,
      //   不再依赖连接信号 receiving — 小欧-2026-09-14
      console.log(`[DBG-3c] startinfo门: badge=idle → badge=running`);
      badge = 'running';
    }
```

**改点 C — L333 deps**
```typescript
// 当前
  }, [steps, frames, receiving, detail, liveError]);

// 改为
  }, [steps, frames, detail, liveError]);
```

**编辑历史**（头部块底部 L35 后追加）
```
// 编辑历史: 2026-09-14 小欧 - 前端3接收实时变量冗余重构(北京老陈批准): 删 receiving 参数, badge 派生不再依赖连接信号;
//   startinfo 门改 startInfo 帧到达即 running, SSE 断连窗不被压回 idle — 小欧-2026-09-14
```

---

### 文件2：RightViewer.tsx（5 处改动）

**改点 A — L141 Props 接口**
```typescript
// 当前
  receiving: boolean;

// 删除这一行
```

**改点 B — L158 解构**
```typescript
// 当前
  receiving,

// 删除这一行
```

**改点 C — L209-213 useTaskInfo 调用**
```typescript
// 当前
  const { badge: liveBadge } = useTaskInfo(
    liveSteps,
    frames ?? emptyMetaFrames(),
    receiving
  );

// 改为
  // 2026-09-14 小欧 冗余重构: 删 receiving 参数 — 小欧-2026-09-14
  const { badge: liveBadge } = useTaskInfo(
    liveSteps,
    frames ?? emptyMetaFrames()
  );
```

**改点 D — L233 isCurrentLive**
```typescript
// 当前
    (hasBusinessSteps ||
      receiving ||
      liveBadge === 'running' ||
      liveBadge === 'paused');

// 改为
    (hasBusinessSteps ||
      liveBadge === 'running' ||
      liveBadge === 'paused');
```

**改点 E — L239 debug 日志**
```typescript
// 当前
      `[DBG-1] live=${isCurrentLive} match=${activeTaskId === serverTaskId} final=${_hasFinal} biz=${hasBusinessSteps} recv=${receiving} badge=${liveBadge} live=${liveSteps.length} hist=${historySteps.length}`

// 改为
      `[DBG-1] live=${isCurrentLive} match=${activeTaskId === serverTaskId} final=${_hasFinal} biz=${hasBusinessSteps} badge=${liveBadge} live=${liveSteps.length} hist=${historySteps.length}`
```

**编辑历史**（头部块底部 L92 后追加）
```
// 编辑历史: 2026-09-14 小欧 - 前端3接收实时变量冗余重构(北京老陈批准): 删 receiving prop;
//   useTaskInfo 删 receiving 参; isCurrentLive 删 receiving 条件; DBG-1 去 recv — 小欧-2026-09-14
```

---

### 文件3：useChatPanels.tsx（1 处改动）

**改点 A — L238**
```typescript
// 当前
          <RightViewer
            activeTaskId={activeTaskId}
            sessionId={sessionId}
            serverTaskId={serverTaskId}
            receiving={isReceiving}    // ← 删这一行
            liveSteps={executionSteps}

// 改为
          <RightViewer
            activeTaskId={activeTaskId}
            sessionId={sessionId}
            serverTaskId={serverTaskId}
            liveSteps={executionSteps}
```

**编辑历史**（头部块底部追加）
```
// 编辑历史: 2026-09-14 小欧 - 前端3接收实时变量冗余重构(北京老陈批准): RightViewer 停传裸接收信号;
//   TaskInfoBar/ChatInput 传参不变 — 小欧-2026-09-14
```

---

### 文件4：TaskInfoBar.tsx（1 处改动）

**改点 A — L101**
```typescript
// 当前
  const info = useTaskInfo(steps, frames, receiving, detail, liveError);

// 改为
  const info = useTaskInfo(steps, frames, detail, liveError);
```

**编辑历史**（头部块底部追加）
```
// 编辑历史: 2026-09-14 小欧 - 前端3接收实时变量冗余重构(北京老陈批准): useTaskInfo 删 receiving 参;
//   receiving prop 保留(秒表启停) — 小欧-2026-09-14
```

---

### 文件5：PipelineRenderer.tsx（1 处改动）

**改点 A — L282-286 taskActive**
```typescript
// 当前
  const taskActive =
    streaming ||
    !!highlightToolName ||
    badge === 'running' ||
    badge === 'paused';

// 改为
  // 2026-09-14 小欧 前端3接收实时变量冗余重构(北京老陈批准): taskActive 删 streaming,
  //   收敛 highlight + badge 权威驱动 — 小欧-2026-09-14
  const taskActive =
    !!highlightToolName ||
    badge === 'running' ||
    badge === 'paused';
```

**编辑历史**（头部块底部追加）
```
// 编辑历史: 2026-09-14 小欧 - 前端3接收实时变量冗余重构(北京老陈批准): taskActive 删 streaming;
//   streaming 光标/打字机/final跳过/replay 不动 — 小欧-2026-09-14
```

---

### 修改总表

| # | 文件 | 改动数 | 改动内容 |
|---|------|:------:|---------|
| 1 | useTaskInfo.ts | 3 | 删 receiving 参数 / startinfo 门去 receiving / deps 去 receiving |
| 2 | RightViewer.tsx | 5 | 删 receiving prop / 解构 / useTaskInfo 调用 / isCurrentLive / debug |
| 3 | useChatPanels.tsx | 1 | 删 receiving={isReceiving} |
| 4 | TaskInfoBar.tsx | 1 | useTaskInfo 调用删 receiving |
| 5 | PipelineRenderer.tsx | 1 | taskActive 删 streaming |
| **合计** | **5 文件** | **11 处** | |

### 验证计划
1. `npm run check`（lint + prettier）
2. `tsc --noEmit` 全绿
3. 实机回归 4 场景：实时流水线 / 切历史任务 / SSE 断连重连 / HITL 暂停

---

## 五、基于 TDD 的代码实施与测试 case 计划

> 编写人：小欧 2026-09-14 07:03:02
> TDD 三支柱：先红（新契约测试先行，旧代码必失败）→ 变绿（实施 11 处 diff）→ 重构（门禁命令全过）。
> 本章回答两个问题：①哪些 case 必须修改 / 必须重新写；②针对本次 4 处行为改动点（删 receiving 参数 / startinfo 门无条件 running / isCurrentLive 去 receiving / taskActive 去 streaming）的全面深度测试矩阵。

### 5.1 测试资产现状盘点（2026-09-14 07:03:02 小欧 逐文件核实）

| 层 | 配置/位置 | 现状 | 结论 |
|----|----------|------|------|
| 单元测试 | `frontend/vitest.config.ts` → `include: ['src/tests/**/*.{test,spec}.{ts,tsx}']` | `src/tests/` 目录**不存在**；`setupFiles: ['./src/tests/setup.ts']` 文件**缺失** | 零单测资产；且 setup.ts 不建则 vitest 启动即报错 |
| 单元测试依赖 | `@testing-library/react ^14.1.2`（内置 renderHook）、`@testing-library/jest-dom ^6.2.0`、jsdom | 依赖齐备 | 可直接新建单测 |
| E2E | `frontend/e2e_case/` 4 个 Playwright 全链路 case | fre2e_01_reconnect_ui / 02_weather / 03_stock_quote / 04_file_dir_analysis + `e2e_front_lib/` 公共设施 | 既有资产可复用 |

**核实结论**：本次 5 文件 11 处 diff 的影响面仅动 `receiving/streaming` 两个内部 prop 与两个内联布尔表达式，**既有 4 个 e2e 断言全部是黑盒行为断言**（发送/接收/终态/重连/无重复正文），**没有一个断言引用 `receiving/streaming` prop 名或 `isCurrentLive/taskActive` 内部名**——详见 grep 取证（RightViewer `receiving` 9 处中代码引用 5 处全部在 diff 覆盖内、`useTaskInfo(` 调用方仅 2 处、PipelineRenderer `streaming` 仅 L283 被删）。

### 5.2 case 处置总表：必须修改 / 必须重新写 / 必须新增

| 处置 | 数量 | 明细 |
|------|:----:|------|
| **必须修改**（既有 case 断言变动） | **0** | 既有 4 个 e2e 断言零改动——黑盒行为不受 prop 改名影响，直接复用 |
| **必须重新写**（既有 case 语义失效） | **0** | 前端 `src/` 下无任何既有单元测试，不存在失效 case 可重写 |
| **必须新增**（本次重构防护） | **5 项** | ①`src/tests/setup.ts`（vitest.config.ts L22 已引用但缺失，不建则单测无法启动）②`src/tests/hooks/useTaskInfo.test.ts` ③`src/tests/components/pipeline/taskActive.test.ts` ④`src/tests/components/right/isCurrentLive.test.ts` ⑤`e2e_case/fre2e_05_history_switch.spec.ts`（新增 E2E 专项，见 5.5-E5） |

> 说明（诚实声明）：本章核心工作不是"改旧 case"，而是**为 4 处行为/契约变更新建回归防护单测**，并以 TDD 红-绿顺序驱动 11 处 diff 的落码。

### 5.3 测试判定函数载体决策（TDD 友好，2 方案待北京老陈定夺）

`taskActive`（PipelineRenderer L282-286）与 `isCurrentLive`（RightViewer L228-235）是组件内联布尔表达式，单测需可 import 的载体：

| 方案 | 做法 | 优点 | 代价 |
|------|------|------|------|
| **方案 A（推荐）** | 抽导出纯函数 `computeTaskActive(highlight, badge)`、`computeIsCurrentLive({activeTaskId, serverTaskId, hasFinal, hasBusinessSteps, liveBadge})`，渲染层只剩消费 | 判定逻辑提纯（SLAP），单测直线断言，最贴 TDD | diff 由 11 处增至 13 处（+2 个新增导出），**超出已批准范围，须老陈批准** |
| 方案 B | 组件渲染级 DOM 断言（renderPipelineRenderer 后检查 `.action-waiting-cursor` / `.waiting-cursor` 是否存在） | 零额外 diff | 需构造 buildSegments 全量 steps，断言绕、耦合渲染细节 |

> 推荐方案 A：本次重构语义就是"变量冗余收敛"，抽纯函数正是 DRY/SLAP 的正向目标，且 13 处 diff 每处均可单测钉死。若老陈不批，退方案 B（case 矩阵不变，断言载体改 DOM）。

### 5.4 单测 case 全量矩阵（4 处行为改动点 × 语义/契约全覆盖）

> 共同前置：码头 `renderHook` / 纯函数直调；`frames` 用 `emptyMetaFrames()` 打底；步骤用 `ExecutionStep` 最小字段（type/timestamp/content/outcome）。`console.log`（DBG-3b/3c 等）不影响断言。**改后契约立此存照：useTaskInfo 新签名四参 `(steps, frames, detail?, liveError?)`，不再有 receiving。**

#### A 组：useTaskInfo badge 派生（改动点 ①②：删 receiving 参数 + startinfo 门无条件 running）

| # | Case | 输入 | 期望 badge | 归属 | 防回归对象 |
|---|------|------|-----------|:----:|-----------|
| A1 | startinfo 已到、无业务步骤 | steps=[]、frames.startInfo 非 null | running | 改动点②（变化） | startInfo 帧即"任务已开始"权威信号 |
| **A2** | **startinfo 已到 + SSE 断连窗（旧 receiving=false）** | steps=[]、frames.startInfo 非 null | **running** | 改动点②（变化·核心） | **09-08「前端UI静默10秒整批显示」根治**：断连窗 badge 不再被压回 idle，isCurrentLive 不翻 false |
| A3 | startinfo 未到、空 steps → 不提前亮灯 | steps=[]、frames=empty | idle | 改动点②（等价） | B33"有帧才亮"既有语义 |
| A4 | 业务 step 到达（thought-start/action/observation） | steps=[{type:'action'}] | running | 改动点②（等价） | 09-08"业务step即证执行中"不被遮蔽 |
| A5 | paused 单事件 | steps=[{type:'paused'}] | paused | 改动点②（等价） | HITL 挂起徽标 |
| A6 | paused 后业务 step 回推 | steps=[{type:'paused'},{type:'observation'}] | running | 改动点②（等价） | 09-01 paused 瞬时化回推 |
| A7 | resumed 恢复 | steps=[{type:'paused'},{type:'resumed'}] | running | 改动点②（等价） | 恢复徽标 |
| A8 | final completed | steps=[{type:'final',outcome:'completed'}] | completed | 改动点②（等价） | 终态徽标 |
| A9a | final failed | steps=[{type:'final',outcome:'failed'}] | failed | 改动点②（等价） | quota_exceeded 失败徽标 |
| A9b | hasFailedFinal 兜底（frames 源） | frames.finalStats.final_status='failed'、steps=[] | failed | 改动点②（等价） | DB 滞后 status 覆盖 |
| A10 | final cancelled | steps=[{type:'final',outcome:'cancelled'}] | cancelled | 改动点②（等价） | 取消徽标 |
| A11a | liveError 非终态覆盖 failed | liveError={kind:'error',text,…} | failed | 改动点②（等价） | 位4 error 徽标 |
| A11b | liveError 后业务恢复（_badgeRecovered）不覆盖 | liveError + steps=[…business…] | running | 改动点②（等价） | 09-03 恢复守卫 |
| A12a | detail 分支 executing 无 duration | detail={status:'executing',duration:0} | running | 改动点①（等价·**证明删参不影响 detail**） | A3 历史详情派生 |
| A12b | detail 分支 DB 滞后兜底 | detail={status:'executing',duration:5} | completed | 改动点①（等价） | 09-11 DB 滞后兜底 |
| A13 | detail + hasFailedFinal → failed 覆盖 | detail + frames.finalStats failed | failed | 改动点①（等价） | detail 分支单真源 |
| A14 | started 过程事件 20 条保首 | 21+ 过程事件 + startInfo | processEvents 含 started、长 20 | 改动点②（等价） | 08-27 slice(-20) 保首 |
| A15 | usage 兜底 {0,0,0} → taskAccumulated 有值映射 | taskAccumulated null / 有值两种 | 0/0/0 或映射 | 改动点①（等价） | 09-12 P1-11 |
| A16 | liveMeta 合成后到者胜 + requestLevel | truncated + retrying + liveError | 时间大者胜、requestLevel 透传 | 改动点①（等价） | 09-02/08/09 位4 |

#### B 组：isCurrentLive 判定（改动点③：删 receiving 条件）

| # | Case | 输入（activeTaskId 匹配/serverTaskId/**hasFinal/business/liveBadge**） | 期望 | 归属 | 防回归对象 |
|---|------|------|:----:|:----:|-----------|
| B1 | 有业务步骤（thought-start/action/observation/chunk 任一） | match=true、final=false、business=true、badge=idle | **true** | 改动点③（等价） | 09-09 hasBusinessSteps 铁证兜底 |
| B2 | badge=running（startinfo 门无条件 running） | badge='running'、business=false | **true** | 改动点③（等价·与②协同） | 首屏 startinfo 撑 live |
| B3 | badge=paused（HITL 挂起） | badge='paused'、business=false | **true** | 改动点③（等价） | 挂起不切历史 |
| **B4** | **仅旧 receiving=true、其余全假（startinfo 未到+无业务步骤）** | business=false、badge=idle、match=true、final=false | **false** | 改动点③（**变化**） | 固化解锁：该窗口无 UI 载体，不再误判 live（§3.3 双覆盖推出） |
| B5 | final 已到 | final=true | **false** | 改动点③（等价） | 12.6 final 早退切历史 |
| B6 | 无匹配（activeTaskId=null / ≠serverTaskId） | match=false | **false** | 改动点③（等价） | 切历史/无任务 |
| B7 | 24 组真值表穷举 | match×final×business×badge∈{idle,running,paused} 全组合 | 与改后布尔式恒等 | 改动点③（等价） | 消灭隐式回归（2×2×2×3=24，含不可达组合在内穷举非等价判定） |

#### C 组：taskActive 判定（改动点④：删 streaming）

| # | Case | 输入（highlight/badge/streaming） | 期望 | 归属 | 防回归对象 |
|---|------|------|:----:|:----:|-----------|
| C1 | highlight 非空保底 | highlight='toolA'、badge=idle、streaming=false | **true** | 改动点④（等价） | HITL 确认时 waiting 保持 |
| C2 | badge=running | badge='running'、highlight=null、streaming=false | **true** | 改动点④（等价） | 实时中齿轮/波纹维持 |
| C3 | badge=paused | badge='paused'、highlight=null、streaming=false | **true** | 改动点④（等价） | 挂起保活 |
| **C4** | **仅旧 streaming=true、highlight=null、badge=idle（startinfo 未到+无步骤）** | streaming=true、highlight=null、badge=idle | **false** | 改动点④（**变化**） | 固化解锁：无 waiting 段/action-waiting 段，无 UI 载体 |
| C5 | 历史回放 | streaming=false、badge=undefined、highlight=null | **false** | 改动点④（等价） | 回放不显示等待圈（02-09 定案） |
| C6 | 实时正常时序等价性 | streaming=true + 有 thinking 段 ⇒ badge=running 或 highlight 非空 | **true**（改后表达式与改前恒等） | 改动点④（**等价性证明**） | 本改动唯一差异窗口=startInfo 未到，已在 C4 单独锁定 |
| C7 | action-waiting 追加门 | lastSeg.kind='thinking' + taskActive=true / false | 追加 / 不追加 | 改动点④（等价·**漏洞1 行为不变**） | 2026-09-13 特留逻辑（L290-292/L331-341）本次不动，回归钉死 |

#### D 组：调用链契约（tsc 门禁自动覆盖 + 人工核查点）

| # | Case | 验证方式 | 期望 | 说明 |
|---|------|---------|:----:|------|
| D1 | RightViewer:209 / TaskInfoBar:101 删参后编译 | `npx tsc --noEmit` | 0 error | 漏一处即 TS2554（实参/形参个数） |
| D2 | useChatPanels L238 已删、RightViewer props 已删 | tsc | 0 error | 残留多余 prop 即 TS2322 |
| D3 | TaskInfoBar `receiving` prop 保留 | 单测/人工核对 L116/133/136 | 秒表 interval 仍由 receiving 启停 | 合并 A 组渲染 + 断言 `liveElapsed` 变化（可选） |
| D4 | infoMaps:11 / PipelineRenderer:80 类型导入不受影响 | tsc | 0 error | `import type` 与函数签名解耦 |

### 5.5 E2E 回归清单

| # | Case | 验证点 | 与本次改动关系 | 必跑 |
|----|------|--------|---------------|:----:|
| E1 | fre2e_01_reconnect_ui | 断连→重连→after_seq 续传→UI 无重复/终态完整 | **专项覆盖改动点②③语义区**（断连窗 badge/isCurrentLive），断言零改动 | ✅ |
| E2 | fre2e_02_weather | 正常功能流（工具+正文） | 黑盒回归，断言零改动 | ✅ |
| E3 | fre2e_03_stock_quote | 正常功能流 | 黑盒回归 | ✅ |
| E4 | fre2e_04_file_dir_analysis | 长任务流 | 黑盒回归 | ✅ |
| E5（新增） | fre2e_05_history_switch.spec.ts | 实时任务后台执行时切看历史任务→右侧无残留、无 waiting/action-waiting 误显、切回实时恢复 live | **专项覆盖 B6/C5 端到端**（复用 e2e_front_lib 设施） | 单测绿后补跑 |

### 5.5.1 E5 专项测试用例详细设计（fre2e_05_history_switch.spec.ts）

> 编写人：小欧 2026-09-14 07:10:41
> 环境：普通会话流环境 `startNormalUiEnv`（前端 API 直连 :8000 跨域放行，**无 9000 代理**，本 case 不含断流）。前置：后端 :8000 预先手动启动（用例不拉起）。依赖 POM（`e2e_front_lib/chat-page.ts`）与公共设施（`normal-chat.ts` 的 `runChatFlow`、`stream-diag.ts` 的 `attachStreamDiag`）。**取证探针：RightViewer `[DBG-1]` 日志（L239 仅 isCurrentLive 状态变化时打印，改后即为该语义的直接证据），经 attachStreamDiag 的 consoleAll 采集。**

#### test 01｜实时任务后台运行中切历史任务 —— isCurrentLive/taskActive 语义端到端（E5 主线，对应 B6/C5）

| 步骤 | 动作 | 断言/取证 |
|------|------|----------|
| 1 | startNormalUiEnv + gotoChat | input 可见（60s） |
| 2 | 发任务 A（促 LLM 长思考、少调工具，缩短时长）→ runChatFlow 等 done | A 终态 >30 字符并记录其关键词 |
| 3 | 记录左侧 `.task-list-item` 项数（=1） | — |
| 4 | 发任务 B（独立话题）→ waitReceiving（停止钮=流已启动） | poll consoleAll 出现 `[DBG-1] live=true`（30s） |
| 5 | 记录 B 实时右侧正文基线 | — |
| 6 | 点击历史任务 A 项（从 `.task-list-item` 的 aria-label `任务 {id} {status}` 提取 task_id，点 ≠ serverTaskId 者） | — |
| 7 | **切走后校验** | ① poll 30s 出现 `[DBG-1] live=false`（isCurrentLive 正确翻 false）② 右侧正文含 A 终态关键词（历史渲染准确）③ 右侧无 `.waiting-cursor` / `.action-waiting-cursor` / `.tool-waiting-cursor`（历史回放无等待圈，C5） |
| 8 | 点回实时任务 B 项 | ① poll 30s 出现 `[DBG-1] live=true`（切回恢复 live）② 右侧正文恢复增长 / B 关键词出现 |
| 9 | B 流至终态 waitDone | 终态含 B 关键词 + 无 run-on（findAdjacentDup）+ >30 字符 |
| 10 | 通用区③ printDiag + keepBrowserOpenIfRequested | 失败归因落盘 |

#### test 02｜实时期间 isCurrentLive 恒 true 回归（改动点②③语义端到端）

| 步骤 | 动作 | 断言 |
|------|------|------|
| 1 | startNormalUiEnv + gotoChat | input 可见 |
| 2 | 发消息 C → waitReceiving | 停止钮可见 |
| 3 | 全程采集 `[DBG-1]` 序列（首帧→done） | 首次即 live=true；done 前除 `_hasFinal` 终帧外无 false 插入（删 receiving 后实时期 isCurrentLive 不再依赖连接信号，A2/B1/B2 撑住） |
| 4 | waitDone | 终态完整断言（>30 字符 + 无 run-on） |
| 5 | printDiag + keepBrowserOpenIfRequested | — |

### 5.5.2 E5 双线监控要点（action-waiting 漏洞1 同步观测）

> 编写人：小欧 2026-09-14 07:10:41
> 总原则：E5 **硬断言**只锁 isCurrentLive/taskActive 语义（切历史/切回/实时恒 true）；action-waiting 漏洞1（2026-09-13 特留逻辑，L290-292/L331-341）属**监控取证（软诊断）**，不作为 E2E 硬断言，避免两问题耦合导致 case 无法通过。**契约级与失败模态分立：契约级可硬断言（如历史回放无任何 waiting 圈=test01 步骤7③，锁 taskActive=false 的 C5 契约）；失败模态只软诊断（过早出现/残留/误显仅 [WARN] 采集归因）。**
> 监控目标：确认 action-waiting 波纹在不同窗口出现的时序与 taskActive 信号（highlight/badge）的关系，为漏洞1 根治提供证据链（三现象：过早出现/残留/误显）。

**（一）监控三时机**

| 时点 | 观测动作 | 疑似根因假设 |
|------|---------|-------------|
| A. B 实时 thinking 流式中 | 采集 `.action-waiting-cursor` 出现时刻 vs `[SSE]` thinking chunk 时间戳对照 | **过早出现**：思考未完就亮波纹——若波纹与打字机 cursor 同帧并存，说明追加条件 `lastSeg.kind==='thinking'` 把"流式中的 thinking 末段"算入，应加"thinking 已完结（非实时末段）"约束 |
| B. 切历史任务 A 回放时 | A 已完成末段必为 final（waiting 段/波纹不可渲染）→ 步骤7③ 硬断言右侧无任何 waiting 圈（**C5 契约断言**，可达可验证） | **误显可观测窗口实为 B 侧**：若 B 流式中 highlight 非空（授权 pending）+ 末段 thinking → 波纹出现，需对照 highlight 时效（recentConfirmedTool 2s）判定过早/误显，收敛 highlight 作用域 |
| C. B done 后 | 检测波纹是否消失 | **残留**：final 到达 badge=completed → taskActive=false → 波纹应灭；若 5s 后仍现，说明 taskActive 被 highlight 残留（recentConfirmedTool 2s）拉住，需限制 highlight 的时效/终态 |

**（二）监控取证实现**

- 库内新探针：轮询 `.action-waiting-cursor` 存在性+时标（T+Xms），与 consoleAll 中 `[SSE]` / `[DBG-1]` / `[DBG-3b]` 时序对齐归因
- 异常三态命中即打 `[WARN] action-waiting-<class>-<ts>`，连同 isCurrentLive/taskActive/badge 对照行并入 DIAG 落盘（`e2e_case/output/`）
- **对照分析表**：每次波纹事件记录 {时刻, 观察窗口, taskActive 相关信号(highlight/badge/streaming), 是否打字机同步} → 产出漏洞1 根因证据链

**（三）产出判定**

- 监控证实假设（过早=缺 thinking 完结约束 / 误显=highlight 历史作用域 / 残留=highlight 终态未清）→ 即漏洞1 的解法方向，取证结果汇报北京老陈后另行设计修复
- 本次改动（taskActive 删 streaming）与 action-waiting 有交叉影响 → 立即停，报告再定
- 监控接入点：test 01 步骤 4-9 每个交互窗口（B 流式中 / 切 A / 切回 B / B done），test 02 关注"实时期间 action-waiting 正常亮灭节奏"作为基线对照

### 5.5.3 E5 落地规格（照文可写码的调用细节证据，2026-09-14 07:37:39 小欧 逐文件只读取证）

> 编写人：小欧 2026-09-14 07:37:39
> 目的：把 5.5.1/5.5.2 的设计落到"库 API + DOM + 探针 + 解析规则"的具体规格，使后续编写 spec 时无需再翻库文件。
> 铁规提醒：**AGENTS.md 严令禁止 commit 任何测试相关代码文件** —— E5 spec 落盘 `frontend/e2e_case/` 但严禁提交。

**（一）模板库 API 签名（e2e_front_lib 逐文件核证）**

| API | 签名/返回 | 出处 |
|-----|----------|------|
| `startNormalUiEnv(frontendDir)` | 5173 有活 vite 服务直接复用，无则 `npm run dev` 起并等就绪（`ensureDevServer` 轮询 2xx-4xx）；不做 9000 代理、不注入 VITE_API_BASE_URL | normal-chat.ts L23-27 |
| `runChatFlow(chat, prompt, { diag?, waitDoneTimeout? })` | sendPrompt → waitReceiving(90s) → waitGreenDot(90s，非强制) → waitDone(timeout) → waitMs(1500) → getFinalText；任一步失败 printDiag 全量后抛错 | normal-chat.ts L32-60 |
| `attachStreamDiag(page)` | 返回 `DiagBundle{streamReqs[], reconnectLogs[], sseErrors[], consoleAll[], allFailed[], t0}`；consoleAll 条目格式 `[T+Xms] [类型] 文本` | stream-diag.ts L38-112 |
| `ChatPage` | `input`(占位符"输入消息，Shift+Enter 换行")、`sendBtn`("发送")、`stopBtn`("停止")、`greenDot`(`span.waiting-cursor[aria-label="等待下一个思考内容"]`)、`gotoChat/waitReceiving/waitGreenDot/waitDone/getFinalText(=body.innerText)/waitMs(pop)·sendPrompt` | chat-page.ts L10-67 |
| `printDiag(streamReqs, reconnectLogs, sseErrors, consoleAll, tail, allFailed, getCaseId())` | 网络 DIAG + 全 FAIL + 重连 + SSE 错误 + consoleAll 尾 40/60 + 后端日志段，落盘 `e2e_case/output/diag-<caseId>-<ts>.log` | stream-diag.ts L183-226 |
| `getTodayLogPath(BACKEND_DIR)` / `logBaseOf(BLOG)` / `readLogSince(BLOG, base)` | 当日后端日志 `logs\app_YYYY-MM-DD.log`；字符基线；自基线切片（utf8 字符索引） | stream-diag.ts L15-35 |
| `findAdjacentDup(text)` | 行内 6~30 步长 3 滑动窗相邻重复判定，返回命中上下文数组（普通会话软 DIAG 不 fail） | stream-diag.ts L119-141 |
| `keepBrowserOpenIfRequested(page)` | 双开关（命令行 `KEEP_BROWSER=1` > `e2e_case/e2e.config.ts` 的 `E2E_KEEP_BROWSER_OPEN`）true 时挂起不关浏览器 | stream-diag.ts L233-245 |

**（二）[DBG-1] 取证探针精确格式与解析规则（RightViewer.tsx L236-242 实码取证）**

- 格式：`[DBG-1] live=<bool> match=<bool> final=<bool> biz=<bool> recv=<bool> badge=<string> live=<n> hist=<n>`，仅 isCurrentLive 状态翻转时打印（prevIsCurrentLiveRef 变门）。
- **解析规则（关键容错）**：只取前缀四字段 `live / match / final / biz`，**不依赖 recv/badge/live/hist**。原因：改点E 的 debug 行（L239）与 isCurrentLive 同步删除 `recv=${receiving}`，改后该字段不存在；解析器若把 recv 当必选匹配即失配。断言判据：`final=false` 的帧必须 `live=true`（test02）；`live=false` 帧出现=切历史（test01 步骤7①）；`live=true` 帧=实时恢复（test01 步骤4/8①）。
- 轮询方式：记录 `base = diag.consoleAll.length` 后定点扫描新增条目（防配到旧轮次帧）。

**（三）等待圈 selector 三件套（WaitingIcons/index.tsx L13/34/62 + index.css 取证）**

| selector | aria-label | 语义 |
|----------|-----------|------|
| `.waiting-cursor` | "等待思考输出" | 思考打字机光标（注意：与 ChatPage.greenDot 的 `aria-label="等待下一个思考内容"` 是**不同元素**，两者 class 相同） |
| `.tool-waiting-cursor` | "等待工具完成" | 工具执行齿轮 |
| `.action-waiting-cursor` | "等待action到达" | G 波纹（CSS `@keyframes action-ripple-1/-2` 1.8s 交替 scale0.5→1.4 + opacity，index.css L159+） |

- test01 步骤7③ 硬断言：`page.evaluate` 查三者全无（C5 契约，历史回放无等待圈）。
- test02 基线：实时期间三件套正常亮灭节奏只采录不硬断言。

**（四）任务列表 DOM 与切换定位（TaskListPanel.tsx L127-142 实码取证）**

- 项元素：`div.task-list-item`（当前任务追加 ` active` 类）、`role="button"`、`tabIndex=0`、`aria-pressed`（active 布尔）、**`aria-label="任务 {task_id} {status}"`**、`onClick=onSelect(task_id)`。
- 提取 task_id：从 aria-label 用 `/^任务 (\S+) (\S+)$/` 解析第一组。
- 切换定位：发 B 后 A 为 `.task-list-item:not(.active)`；切到 A 后 B 为非 active 项 → 两类切换统一用 `.task-list-item:not(.active)` 点击。
- 计数断言（步骤3）：`page.locator('.task-list-item')` 应 count=1。

**（五）5.5.1 test 步骤 → 实际 API 调用映射**

| test01 步骤 | 实现要点 |
|------|---------|
| 1 环境+进页 | `startNormalUiEnv(FRONTEND_DIR)` → `chat.gotoChat()` → `expect(chat.input).toBeVisible({timeout:60_000})` |
| 2 任务 A done | `runChatFlow(chat, PROMPT_A, { diag })`，录 A 终态 ≥30 字符；存 `aTail = norm(A终态).slice(-40)`（norm=去空白归一）供步骤7② 匹配 |
| 3 左侧项数 | `page.locator('.task-list-item')` count=1 |
| 4 发 B 等 live=true | `chat.sendPrompt(PROMPT_B)` → `chat.waitReceiving()` → 自 base 起 poll 新增 `[DBG-1]...live=true`（30s）→ 录实时正文基线 `b5 = chat.getFinalText()` |
| 5 录 B 基线 | `b5` 长度 + `norm(b5).slice(-40)` 供步骤8② 恢复增长断言 |
| 6 点历史 A | `page.locator('.task-list-item:not(.active)').first().click()` |
| 7 切走校验 | ①自 base 起 poll `live=false`（30s）②`norm(getFinalText()).includes(aTail)` ③三件套 selector 全无（C5 硬断言） |
| 8 点回实时 B | 同步骤6 定位点击；①poll `live=true`（30s）②`len8 > len5 && norm8.includes(b5Tail)`（B 后台续收） |
| 9 B done | `chat.waitDone(420_000)` → 终态含 B 关键词 + `findAdjacentDup` 软 DIAG + ≥30 字符 |
| 10 通用区 | `printDiag(...)` + `[TOOL]` 尾 8 行 + `keepBrowserOpenIfRequested(page)` |

| test02 步骤 | 实现要点 |
|------|---------|
| 1 环境+进页 | 同上 |
| 2 发 C waitReceiving | `chat.sendPrompt(PROMPT_C)` → `chat.waitReceiving()` |
| 3 全程 [DBG-1] 扫描 | 规则：全部 `final=false` 帧必须 `live=true`（A2/B1/B2 撑住）；任一 `live=false && final=false` 即 FAIL |
| 4 waitDone | `chat.waitDone(420_000)` → 终态 ≥30 字符 + 无 run-on 软 DIAG |
| 5 通用区 | `printDiag(...)` + `keepBrowserOpenIfRequested(page)` |

**（六）监控探针函数设计（5.5.2 落地 → 未来随 spec 一起写入库，当前未实现）**

- `sampleWaitingCursors(page)` → `{action, thinking, tool}`（page.evaluate 三 selector 存在性）
- `monitorActionWaiting(page, windowMs, sampleMs=250)` → 抽样数组 `{ts, action, thinking, tool}`；同帧 `action && thinking` 打 `[WARN] action-waiting-premature-T+Xms`；期间 action 出现打 `[WARN] action-waiting-active-T+Xms` 供归因；返回样本供 DIAG 对照
- `checkWaitingLeftover(page, afterMs=5000)` → done 后 5s 采样，action 仍现打 `[WARN] action-waiting-leftover-T+Xms`（C 时机残留）
- 接入点：test01 W1（B 实时流式，live=true 后 ~6s）/ W2（A 回放窗 ~2s，硬断言已兜底）/ W3（B done 后，走 leftover）；test02 与 `runChatFlow` 并行 `monitorActionWaiting(page, 180_000)` 作基线
- 全部软诊断不参与断言（契约级与失败模态分立见 5.5.2 总原则）

### 5.6 TDD 实施顺序（红 → 绿 → 重构）与门禁命令

**第一步 RED（先写新契约测试，旧代码必失败）**
1. 新建 `src/tests/setup.ts`：`import '@testing-library/jest-dom';`
2. 新建 A/B/C 组单测（按 5.4 矩阵；若批方案 A 则先抽 2 纯函数再写）
3. 执行 `npm run test` → 预期**编译失败或断言红**（旧代码仍是三参 receiving 依赖 / 表达式含 streaming）——此红即 TDD 契约存档

**第二步 GREEN（实施 11 处 diff，测试变绿）**
4. 按第四章 diff 顺序实施 5 文件 11 处（useTaskInfo.L83-89/L244-250/L333 → RightViewer.L141/158/209-213/233/239 → useChatPanels.L238 → TaskInfoBar.L101 → PipelineRenderer.L282-286）
5. 每个文件编辑历史块底部追加新条目（R5 已定位置）
6. `npm run test` → 全绿；`npx tsc --noEmit` → 0 error

**第三步 REFACTOR（门禁 + 回归）**
7. `npm run check`（lint + prettier）→ 零告警零格式差
8. **【必须执行 · 不可跳过 · 最终验收门禁】E2E 全链路回归**：按 5.5 清单逐一跑 E1→E4（含每 case 三查：代码错误异常/调用链/参数正确性），单测全绿后补 E5。**任一 E2E 红 → 立即停止，回代码修复后全量重跑，严禁跳过、严禁拆批妥饰**（严格遵循 backend/e2etests/全链路E2E测试手册：一次只跑一个 case、Popen 落盘方式、真实后端+真实 LLM+真实 SQLite、禁止 Mock）
9. 实机 4 场景终验：实时流水线 / 切历史任务 / SSE 断连重连 / HITL 暂停

**通过判定 (DoD · 硬门槛，缺一不可)**：vitest 全绿 + tsc 0 error + `npm run check` 通过 + **E1-E4 全过（单测绿后含 E5）** + 实机 4 场景无异常 → 才允许提交与打 tag；任一不满足即回对应步骤重做，E2E 未全过视为任务未完成。
