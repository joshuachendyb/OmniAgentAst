# 前端SSE接收处理链路：三份报告交叉核查与根治优化方案

**创建时间**: 2026-09-10 09:19:31（PowerShell Get-Date 获取）
**更新时间**: 2026-09-10 12:12:25
**编写人**: 小欧

### 版本历史

| 版本 | 更新时间 | 更新内容 | 作者 |
|------|---------|---------|------|
| v1.0 | 2026-09-10 09:19:31 | 首版：三份报告交叉核查 + 后端心跳化解P4判定 + seq守卫初始值死角 + 根治优化方案 | 小欧 |
| v1.1 | 2026-09-10 09:27:52 | 北京老陈质疑后修订：Q6 升级为"⭐去重防线只拦一半"（最核心问题），补 sseParser 5 分支行号证据与三层累积后果；S3 补"守卫拦截四路短路"纵深说明；TDD 加 T8/T9；2.2 速览加第6条 | 小欧 |
| v1.3 | 2026-09-10 09:36:04 | 北京老陈再次质疑后深度核查（从症状回溯链路）补强：Q6 钉死"用户可见重复主源=executionSteps state 渲染区（Pipeline/RightViewer），气泡有指纹反而不重复；preview+canonical 双发进 state"；Q5 补"cancel 双 final 也进消息气泡(:266)"；新增 Q5b（指纹/取消守卫对 final 帧无区分，final 计数不定）；排除 3 条伪命题：final_stats 独立 case 无双 onComplete、request_level loading 悬挂被 handleSend finally(:169) 化解、P4 idle 被心跳化解 | 小欧 |
| v1.4 | 2026-09-10 09:47:04 | 北京老陈提供三症状实测（重复输出/短时冻结/丢最后几步）后架构级定性：新增 Q12（高频主线程阻塞：O(n)复制+双路全量stringify+同步setItem）、Q13（结束瞬时 live→REST 双源切换丢尾）；新增 S12/S13 架构根治；TDD 加 T10/T11；实施顺序升 S13/S12 优先级，S3≥S13≥S12 面向三症状 | 小欧 |
| v1.5 | 2026-09-10 09:50:51 | 按北京老陈要求新增 3.5 节：三维问题清单（架构 4 条 / 技术实现 4 条 / 技术选择 2 条），不用表格、逐条说明"是什么+危害"，并给出"架构维是根、实现维是放大、选择维仅一处轻误用"的定性结论 | 小欧 |
| v1.6 | 2026-09-10 09:55:00 | 按北京老陈要求重排 3.5 节编号：废弃"一/二/三"与"第一~第十"式编号，改为规范章节号——3 级标题 3.5、4 级标题 3.5.1/3.5.2/3.5.3、5 级标题 3.5.x.y 逐条锚定问题，并将原"结论"独立为 4 级标题 3.5.4；观点与行号证据不变 | 小欧 |
| v1.7 | 2026-09-10 10:06:28 | 按北京老陈要求，把文档21 的 8 个问题点逐一对照 3.5 检查后补齐：3.5.1.1 补"state/ref 手工双写普遍性+清错对象"病根、3.5.1.2 补"解析层三责合一（解析/状态变更/持久化）"病根、3.5.2.1 补"chunk 三处累积"叠加因素、3.5.2.3 补"错误处理三层链路"病根；新增 3.5.2.5（模块与接口职责失衡：useSSE 六大职责/handlers 20+ 字段直透）；sessionStorage 与三症状无关的"类型宽泛"不采纳不予加入；3.5.4 同步改为十一点/~3.5.2 五条 | 小欧 |
| v1.8 | 2026-09-10 12:02:34 | 第5章从伪代码改写为实施的完整准确代码 diff：19条 S（S1~S19），全部基于本地最新真实源码（useSSE.ts/sseParser.ts/useChatCallbacks.ts/useChatState.ts/useChatStreaming.ts/useChatPersistence.ts/RightViewer.tsx），行号经逐行比对确认；含 useStateWithRef 新增 hook、批量 commit 模式、settledSteps 快照、seq 权威守卫等完整 diff | 小欧 |
| v1.9 | 2026-09-10 12:06:26 | 全文一致性检查修复（16项）：①S4.5标注已被S12取代；②S13.3等长校验取消注释改为正式diff+S13.4独立小节；③引言S1~S13→S1~S19；④2.2#7"详见第七章"→"详见第五章S3"；⑤S3/S12补handlers类型声明；⑥S14 updater写ref改为外层同步；⑦Q10行号修正；⑧实施顺序表补S14/S15；⑨S4.4补ref先行(:498-499) | 小欧 |
| v1.9.1 | 2026-09-10 12:09:38 | 3.6.7 删除"短期/中期/长期"分级管理措辞，改为"一次到位原则"——所有修改同步实施，不留尾巴 | 小欧 |
| v2.0 | 2026-09-10 12:12:25 | 全面清理分期语言 + 补齐函数提取实施：①3.6.1/3.6.2/3.6.8 标注"已并入 S20/S21/S22"；②新增 S20（连接层函数提取：startIdleMonitor/clearIdleMonitor/clearAllTimers）；③新增 S21（解析层 type handler 提取：handleThought/handleChunk/handleFinal/handleAction/handleObservation/handleMeta + switch 分派）；④新增 S22（错误终态幂等清理 request_level 闸门）；⑤S4.5 删除回退方案措辞；⑥实施顺序表补 S20/S21/S22；⑦总原则改为"三症状+架构质量+长期维护性同步落地" | 小欧 |
| v2.1 | 2026-09-10 12:35:00 | 全面重写：①S10/S16/S17/S18/S19 伪代码补为真实diff；②第六章删除→内容并入S3.5/S20；③S3.5 补首帧陷阱验证单元测试diff（四场景）；④第六章TDD重写为22条S逐条验收用例（38个T用例）；⑤第七章重写为TDD驱动实施顺序+依赖链图+风险矩阵 | 小欧 |



## 一、严重UI实时渲染的问题及文档信息
前端3个严重的问题 都是 非定时必定出现的问题,都实时显示AI消息 过程中的 
1 是重复输出 
2 输出中短时间冻结被堵
3 丢失steploog最后几步的显示

| 项 | 内容 |
|----|------|
| 文档类型 | 编辑型（分析 + 优化方案） |
| 应用范围 | `frontend/src/hooks/useSSE.ts`、`frontend/src/features/chat/services/sseParser.ts`、`frontend/src/features/chat/hooks/useChatCallbacks.ts`、`useChatState.ts`、`useChatStreaming.ts`、`useChatFacade.ts` |
| 后端契约 | `backend/app/services/task/task_state.py`、`backend/app/services/chat/stream_orchestrator.py`、`backend/app/services/agent/step_emitter.py`、`backend/app/utils/sse_formatter.py` |
| 上游依据 | `[21]前端SSE处理架构深度分析报告-小李-2026-09-10.md`、`[22]前端SSE接收处理链路核查与优化实施说明-小草-2026-09-10.md`、`[23]前端SSE处理架构分析与优化方案-小沈-2026-09-10.md` |
| 核查强度 | 三遍核查（①完整读文件 ②逐条验证真伪+交叉验证 ③方案彻底性+死角排查） |

## 二、核查方法与证据采集

### 2.1 核查方法

1. **第1遍 - 完整读取**：前端6个关键文件逐行读完（useSSE.ts 1086行 / sseParser.ts 938行 / useChatCallbacks.ts / useChatState.ts / useChatStreaming.ts / useChatFacade.ts），后端4个契约文件（task_state.py / stream_orchestrator.py / step_emitter.py / sse_formatter.py）。
2. **第2遍 - 逐条验证**：对三份报告每个声明点，在本地代码中查证行号证据，判定"真实/夸大/理由不实/被化解"。
3. **第3遍 - 方案彻底性**：对每条方案验证是否"解决根因、无副作用、不引入新抽象"，并排查方案自身死角（如 seq 守卫首帧问题）。

### 2.2 核心结论速览（北京老陈可在 5 分钟内读完本段）

| # | 结论 | 性质 |
|---|------|------|
| 1 | 小沈[23]问题清单质量最高：P1/P2/P5 全部真实，P3 洞察对但理由偏，P4 有夸大 | 采纳其问题主体，剔除浮夸 |
| 2 | 小草[22]实施方式稳妥：O1 保留指纹兜底、O2 幂等清理方向正确，但漏了 P1/P5 两个致命时序链 | 采纳其稳妥实施路线 |
| 3 | 小李[21]架构观察准（单一真源缺失），但方案全错位：策略模式/EventEmitter/中间件是过度设计（违反 YAGNI） | 只取"问题陈述"，方案弃用 |
| 4 | **重大修正：小沈 P4"空闲超时假中断"已被后端 25s 心跳化解**（HEARTBEAT_INTERVAL=25s，`stream_reader` 发 `: ping`，前端 reader 循环每次 read() 返回重置 idle 计时器） | 未发现遗留风险 |
| 5 | **独家死角：seq 守卫方案若按小沈/小草写法上移，首帧 seq=0 会被误拦（小草式）或漏拦（小沈式）** | 详见第六章 |
| 6 | **最核心问题 Q6：去重防线只拦一半**——sseParser 解析层 5 分支对 executionSteps state/ref/sessionStorage 三处无条件追加（零防御），指纹只在 message 消息区拦截 → 重放时 UI 步骤区/刷新恢复源/onComplete 读源三处成倍累积 | 详见第四章 Q6 + 第五章 S3 |
| 7 | **北京老陈三症状实证：重复输出 / 短时冻结 / 丢最后几步 = 同一架构病根**——①重复：解析层无 seq 守卫无条件追加+preview/canonical 双发；②冻结：高频全量 O(n) 数组复制 + 300ms/500ms 双路全量 JSON.stringify+同步 setItem 阻塞主线程；③丢最后几步：final 到达 isCurrentLive 翻转，用 300ms 后 REST（DB 未落全）顶替 liveSteps 全量，切换源不同步丢尾 | 详见第四章 Q6/Q12/Q13 + 第五章 S3/S12/S13 |

## 三、三份上游报告交叉验证结论

### 3.1 小沈[23]声明逐条判定

| 声明 | 判定 | 行号证据 |
|------|------|---------|
| P1：onResumed error 死循环 | **真实** | useChatCallbacks.ts:739-741 回放 error 时 isPausedRef(:747) 才复位 → onError:600-603 命中暂停重推 buffer → 死循环 |
| P2：seq 权威去重未用 | **真实** | 后端 task_state.py:44-54 每事件带 seq（len 单调递增+持锁）；sseParser.ts:158-160 已提取 seq；但前端仅 usage 分支(:267-271)用 seq 去重，业务帧全依赖指纹 |
| P3：双 executionStepsRef 同名碰撞 | **洞察对，理由偏** | useSSE.ts:384 与 useChatState.ts:259 同名不同物实锤；但"ref 渲染到 UI"非主因，主因是 onError(:578 清 useChatState 的) 与 onComplete(:517 读 useSSE 的) 操作不同 ref |
| P4：断线/重连竞态 | **夸大** | "链路被自己短路"不成立：sendMessageInternal 调 disconnect 后仍建新 fetch(useSSE.ts:692)继续跑；真实残留仅"disconnect abort 窗口期内旧流数据丢弃"竞态；且 idle 假中断被后端心跳化解（见3.4） |
| P5：cancelInProgress 双 final | **真实** | useChatCallbacks.ts:184-190 `!isCancelEvent && step.type !== 'final'` → final(completed) 被放行 → 双击取消出现双完成帧 |
| P6：final 处理"取较长者" | **真实，方案不彻底** | useChatCallbacks.ts:454-459 长度比较取源；重连重放时混入旧源（详见第四章 Q9） |
| P7：updater 内副作用 | **真实** | sseParser.ts:380-390/429-439/502-511/678-688/829-839/869-879，textDecoder 每分支内写 ref+持久化 |

### 3.2 小草[22]声明逐条判定

| 声明 | 判定 | 说明 |
|------|------|------|
| O1：指纹去重 | **真实** | useChatCallbacks.ts:151-171 onStepFingerprintRef 内容级去重 |
| O2：错误态幂等清理 | **真实但不完整** | 清理方向对，但应区分 request_level（整个请求失败，断链无 final）与 stream_level（错误帧后有 final），小草未区分 |
| O3：useChatFacade 依赖复杂 | **真实** | useMemo 50+ 依赖互文，50+依赖(:252-418) |
| O4：fetchTimeout 只兜首响应 | **真实** | useSSE.ts:682 设 180s，:717-720 fetch 返回立即 clear → 全程超时语义不存在 |
| 遗漏 P1/P5 | **致命遗漏** | onResumed 死循环、cancel 双 final 两个真实链路缺陷未被覆盖 |

### 3.3 小李[21]声明判定

| 声明 | 判定 | 说明 |
|------|------|------|
| 架构观察：单一真源缺失 | **真实** | useSSE state/ref + useChatState ref + message.executionSteps + sessionStorage + REST historySteps 五份状态（详见第四章 Q1） |
| 方案：策略模式/EventEmitter/中间件 | **过度设计（YAGNI）** | 为 2 个入口、1 种去重场景引入抽象注册表；违反 AGENTS.md KISS-DIRECT |

### 3.4 重大修正：P4"空闲超时假中断"已被后端心跳化解

**证据链**：
1. 后端 `constants.py:127` `HEARTBEAT_INTERVAL = 25.0`（常量注释§6 明确"与前端 IDLE_TIMEOUT=60s 错开"）。
2. 后端 `stream_reader`（stream_orchestrator.py:460-470）：`cond.wait()` 每 25s 超时发 `: ping\n` 保活。
3. 前端 `useSSE.ts:767`：`lastDataTimeRef.current = Date.now()` 在 reader 循环顶部每次执行；`:740-742` 每次 read() 返回前先清除旧 idle 计时器。
4. 前端 `sseParser.ts:112-115`：非 `data: ` 前缀行直接 return 零副作用 —— `: ping` 不触碰业务数据，但 reader.read() 已返回、循环重设 idle。

**结论**：活连接下每 25s 一次 read() 返回，idle 计时器（60s）永远无法到期，假中断不发。P4 的真实残留仅剩：真断连检测时点（正确兜底）与 abort 窗口期（第八章 S10）。

### 3.5 问题三维清单：架构 / 技术选择 / 技术实现

> 按北京老陈的要求：不用表格罗列；问题点改用规范的章节号（3 级标题 3.5、4 级标题 3.5.x、5 级标题 3.5.x.y）来锚定，"是什么"与"危害是什么"以段落方式紧随各章节号之下。三个维度虽并列，但本核查的结论是：**危害最大的三条（重复输出、短时冻结、丢最后几步）全部源于"架构——状态与数据源设计"这一维，技术实现维的缺陷是放大与叠加因素，技术选择维整体正确、仅有一处轻度误用**。

#### 3.5.1 架构维度（状态与数据源设计）——用户三症状的根

##### 3.5.1.1 executionSteps 五份状态无单一真源

executionSteps 同时存在五份互相独立、读写不齐的状态。分别是 useSSE 的 React state（useSSE.ts:383）、useSSE 的伴生 ref（:384）、useChatState 的同名 ref（:259）、消息消息体里的 executionSteps（useChatCallbacks.ts:266）以及 sessionStorage 持久化副本（useSSE.ts:485-500）。它们的写入方与读取方各不相同：解析层写 state 与 useSSE ref，错误清理写 useChatState ref，完成回调读 useSSE ref，消息区另走一套追加。

**病根**：五源不是偶然堆积，而是全链路普遍采用"state 与 ref 手工双写"编码模式累积出的结构病——serverTaskId 靠 serverTaskIdRef + syncServerTaskId 手动同步（useSSE.ts:408-410），isReceiving 同理，没有任何封装保证同步完备。对普通字段而言，同步遗漏只是一次性 bug；对 executionSteps 这类"高频变更且带清空语义"的对象，后果被放大成病态：**清的对象与读的对象错位**——onError 清的是 useChatState 的 ref（useChatCallbacks.ts:578），onComplete 读的是 useSSE 的 ref（:370/:392），两者同名不同物，清空永远清不掉正在读的那一份；同时 sseParser 六个分支每帧同时写 state、useSSE ref（:382/:431/:680/:831/:871）与 sessionStorage 三处，正常无重放时三处尚强一致，一旦重连重放或清空错位立即背离。

**危害**：同一份业务步骤在五处漂移，任何一处不同步，用户就会在同一界面上看到同一批步骤有的地方重复、有的地方丢失；更严重的是清空与读取操作对象不一致时（清的是甲 ref、读的是乙 ref），清理永远不生效，步骤残留在界面上的现象反复出现。

##### 3.5.1.2 SSE 事件流无单调序防线，每帧无条件整数组追加

后端在发布事件时已为每一帧分配了唯一且单调递增的 seq（task_state.py:51，持锁原子），但前端解析层只把这把现成的标尺用在了 usage 分之上（sseParser.ts:267-271），其余业务帧一律不做顺序校验，直接 `[...prev, step]` 追加进 executionSteps state、ref 和 sessionStorage 三处。指纹去重只保护消息气泡区（useChatCallbacks.ts:161-171），左流水线/右查看区读的 state 毫无防线，而 action 的 preview 与 canonical 双发（sseParser.ts:663-666）都会进入 state。

**病根**：写入方 processSSEData 单函数同时承担**解析、状态变更、持久化**三重职责（sseParser.ts:68-936，约 900 行处理 17 种 type），每个事件分支都内联"构造步骤 + setExecutionSteps + 写 ref + saveStepsToStorage"（如 :380-390/:678-688/:829-839），解析层与状态层之间没有任何边界。这使"统一的顺序守卫"根本无处安放——要拦重放必须在每个分支各写一遍守卫，而现状是一处都没写；也使 preview+canonical 双发（:663-666）这类由解析层自己制造的重复源被原样灌入 state，解析层对自身产出的重复毫无感知。

**危害**：断线重连经 after_seq 续传时，序号低于当前进度的重放帧会被三次累积，用户面前重复输出；正常任务里单步骤也被 preview+canonical 双条化，重复感被放大。

##### 3.5.1.3 实时显示源与终态数据源两套数据流，结束时刻靠"猜"交接

执行过程中右侧查看区读的是实时 state（liveSteps，RightViewer.tsx:303），final 一到便切换为读 REST /getTaskSteps 的历史接口（:307）；而历史接口依赖后端把步骤落库成功，落库存在时延（:273-278 的注释明确承认"SSE final 先发、DB 写入稍后"）。

**危害**：结束瞬间如果数据库还没写全最新几步，REST 结果就以"缺尾巴的完整历史"顶替了实时全量，最后几步从界面上永久消失；300ms 缓冲只是拍脑袋的猜测，不是同步点，丢尾窗口在任何一次网络偏慢时都会重新打开。

##### 3.5.1.4 生命周期终态由多个布尔与标记拼接，无唯一判定

是否结束要看 isCurrentLive、_hasFinal、hasBusinessSteps、receiving、activeTaskId 与 serverTaskId 的匹配等多个分散标记的组合（useTaskInfo.ts:125-149、RightViewer.tsx:303），判断彼此存在竞态（例如 liveSteps 已含 final 时先判结束再切历史，与 REST 尚未返回的空窗重叠）。

**危害**：结束与切换的时序每次不完全一样，症状忽隐忽现、非定时必现，正是"修了无数次仍不彻底"的典型特征——每次碰的地方对了、但切换的源始终是猜测的。

#### 3.5.2 技术实现维度（并发与主线程卫生）——放大与叠加因素

##### 3.5.2.1 高频热路径叠加 O(n) 数组复制与双路全量 JSON 序列化

每一帧事件都执行 `[...prev, step]` 的整数组拷贝（sseParser.ts:380/429/678/829 等）并触发 setState，触发 Pipeline 全量重渲染；同时 saveStepsToStorage 每 300ms 防抖内做一次同步的 `JSON.stringify(整个步骤数组)` 加 `sessionStorage.setItem`（useSSE.ts:493），useChatPersistence 又每状态变化或 500ms 对整份 messages（含每条消息的 executionSteps）再做一次全量 JSON.stringify 与写入（useChatPersistence.ts:192-220）。sessionStorage.setItem 是同步阻塞 API。

**危害**：数组上千条时，两路大数组序列化加同步写周期性锁死主线程数百毫秒，打字机输出出现可感知的冻结与"被堵"停顿，且步骤越多、任务越长，冻结越频繁。

叠加因素之二：chunk 正文的累积点散落三处——sseParser.ts:401 的 responseBufferRef、useChatCallbacks.ts:313 的 streamingContentRef、:451 的 finalContent 兜底，同一份流式正文被三个地方各累积一遍；而 onChunk 每块到达都 setMessages 全量重建消息区（useChatCallbacks.ts:313-327），与双路序列化叠加，进一步抬高打字机渲染期的主线程占用。

##### 3.5.2.2 React updater 回调内部执行副作用

sseParser 的多个分支在 `setExecutionSteps(prev => {...})` 的 updater 函数体内写 executionStepsRef 与 saveStepsToStorage（sseParser.ts:380-390/429-439/502-511/678-688/829-839/869-879）。updater 应当是纯函数，React 在 StrictMode 下会将其执行两遍以暴露此类违规。

**危害**：updater 不是纯函数使副作用被执行次数不可控（StrictMode 双调用时 ref 写两次、持久化被触发两次），并使"更新 state 的同时依赖读取 ref"这种时间耦合常态化，后续任何一次 React 升级或批处理策略调整都可能让表现漂移。

##### 3.5.2.3 复位逻辑依赖隐式时序，存在死循环与终态数量不定

`isPausedRef` 的复位被安排在 error 回放循环之后（useChatCallbacks.ts:739-747），而 onError 在暂停期间会把错误重新推回缓冲（:600-603），形成回放时错误永远处理不掉、被反复重推的死循环；cancel 进行中的判断用 `!isCancelEvent && step.type !== 'final'` 放行了取消后的 completed 终态（:184-190），且指纹与取消守卫对 final 帧无法区分，最终消息里 final 帧数量不定。

**病根**：错误处理链路横跨三层、各自独立行事——useSSE 的 handleSSEError（分类与重连判断，useSSE.ts:179/:882/:930）、useChatStreaming 的 sseOnError（denied/blocked/timeout 聚合，useChatStreaming.ts:233）、useChatCallbacks 的 onError（业务错误分道与终端提示），三者之间无统一的内存序约定，复位动作（isPausedRef、聚合状态）散落各层；链路上任何一层在回放循环内提前或延后复位，即触发上文死循环。链路每增一层，时序类缺陷的排查面就翻一倍。

**危害**：暂停恢复会卡死，取消后会出现双完成帧或完成后又插入旧步骤，界面上出现明显违背预期的行为。

##### 3.5.2.4 死代码与语义失真的配置，加深维护误判

包括只被清空而无写入点的 streamingStepsRef（useChatState.ts:261）、设置后从未被读取的 reconnectConfigRef.enabled（useSSE.ts:597-601）、从未使用第 3 参数的 processSSEData（sseParser.ts:120）、名为 180 秒 fetch 超时实际只在首响应 resolve 后就清除的 fetchTimeout（useSSE.ts:682/717-720）以及 50 多个依赖项互相纠缠的 useMemo（useChatFacade.ts:252-418）。

**危害**：死代码让"终态应该清哪个 ref"的排查无从下手，名字失真的超时配置让后续维护者对系统保活能力产生系统性误判，依赖纠缠让任何单点改动都波及大范围重渲染。

##### 3.5.2.5 模块与接口职责失衡：useSSE 六大职责、handlers 20+ 字段直透

useSSE.ts 承载连接建立/断开、重连退避、空闲超时、sessionStorage 备份/恢复、轮询观察、意图标记六大职责（约 1086 行），违反 SRP 单一职责；processSSEData 的 handlers 参数塞入 20+ 字段——setExecutionSteps、executionStepsRef、saveStepsToStorage、onChunk、onComplete、disconnect、syncServerTaskId 等（useSSE.ts:777-778/817-818/:794/:836），把"改状态的能力"与"中断连接的能力"原样交给解析层，违反 ISP 接口隔离。

**危害**：职责不分的直接后果是状态归属混乱——executionSteps 之所以漂移成五份，正因为连接、存储、解析、回调各自握有其读写权且无人统一；handlers 直透让解析层随意触碰 ref、持久化乃至 disconnect，3.5.1.1（多源漂移）与 3.5.2.2（updater 副作用）都是这层失守的下游。它不直接制造三症状，却是三症状持续存在、修了无数次仍反复的管理性根因。

#### 3.5.3 技术选择维度（整体正确，仅一处轻度误用）

##### 3.5.3.1 fetch + ReadableStream 手动解析 SSE 的整体选型正确

它绕开 EventSource 无法携带自定义头与无法显式退避重连的短板，配合后端 25 秒心跳保活、断线按 after_seq 续传、动态退避最大三次重连与轮询兜底，构成了完整的断线自愈体系，这些选择本核查未发现错误，予以维持。

##### 3.5.3.2 sessionStorage 作"刷新恢复 + 双路高频持久化"载体属轻误用

作为一次性、按会话隔离的浏览器同源存储，它适合低频、小体积的会话级快照；而此处既承担高频防抖全量数组写入，又承担每个 messages 快照的全量序列化，其同步阻塞写与容量限制与高频 SSE 流形同水火。

**危害**：它不成比例地放大了症状二的冻结，且在数据量大时会静默抛容量不足告警（useSSE.ts:495 的存在即证据）而丢失恢复数据。正确的归属应是低频兜底快照或迁移到异步存储。

#### 3.5.4 三维定性结论

> 本节的用途：把前面 3.5.1~3.5.3 的 11 个问题点汇总成 4 条行动结论，回答"先修谁、修到什么程度算根治、为什么以前修不好"。每条结论对应一个维度或一个核心判断，单独签名如下。

**结论①架构维（3.5.1，共 4 条）＝三症状的病根，此维不修，症状必复发。**
- 3.5.1.1 executionSteps 五源无单一真源 → 直接造成重复输出、丢步。
- 3.5.1.2 SSE 无单调序防线 → 直接造成重复输出（重放+preview/canonical 双发）。
- 3.5.1.3 实时/终态双源靠"猜"交接 → 直接造成丢最后几步。
- 3.5.1.4 生命周期无唯一终态判定 → 三症状忽隐忽现、非定时必现的原因。
- 对应关系：**症状①重复 ≈ 3.5.1.1+3.5.1.2；症状②冻结 ≈ 结构上由 3.5.2 放大、源头在 3.5.1；症状③丢尾 ≈ 3.5.1.3+3.5.1.1。**

**结论②实现维（3.5.2，共 5 条）＝放大因素与独立缺陷，须一并清除，但单修此维不治根。**
- 3.5.2.1 O(n) 复制＋双路全量序列化 → 放大症状②冻结（叠加 chunk 三处累积）。
- 3.5.2.2 updater 内副作用 → 放大重复与冻结（StrictMode 双调用）。
- 3.5.2.3 复位逻辑依赖隐式时序 → 独立缺陷（暂停死循环、cancel 双 final）。
- 3.5.2.4 死代码与语义失真配置 → 独立缺陷（清错 ref、保活误判）。
- 3.5.2.5 模块与接口职责失衡 → 管理性根因（三症状修不干净的原因之一）。

**结论③选择维（3.5.3，共 2 条）＝技术选型结论。**
- 3.5.3.1 fetch + ReadableStream 手动解析 SSE：选型正确，予以维持。
- 3.5.3.2 sessionStorage 作高频持久化载体：轻误用，修正为低频兜底快照或异步存储。

**结论④行动优先级（用于实施排期）。**
- 第 1 步：按 3.5.1.2 / 3.5.1.3 / 3.5.1.1 / 3.5.1.4 顺序根治架构维（先解三症状）。
- 第 2 步：按 3.5.2.1 / 3.5.2.3 / 3.5.2.2 / 3.5.2.4 / 3.5.2.5 清除实现维。
- 第 3 步：修正 3.5.3.2 sessionStorage 职责。
- 验收标准：任何一条新修补，先对号入座属于 3.5.1 / 3.5.2 / 3.5.3 的哪一档再动手；3.5.1 不落地，三症状不消失，即不算根治。

### 3.6 吸收的其他文档的优秀的改进建议

> 本节收录 3.5 问题清单之外的**改进方法与方向层面**的认可项（当前来源：文档21 + 文档22 + 文档23 + 文档24；其他上游文档的优秀建议持续补充进本节）。方向认可，落地一律遵循文档25 方案 S1~S22 的"不加抽象、不 backward"原则。

#### 3.6.1 连接层函数级提取（来源：21 §4.1 / §7.1）→ 已并入 S20

将 useSSE.ts 中的 `startIdleMonitor`、`clearIdleMonitor`、`clearAllTimers` 等连接管理逻辑**提取为独立函数**，降低 1086 行单文件的 SRP 发散面。**已在 S20 中实施。**

#### 3.6.2 解析层按 type 提取处理函数（来源：21 §7.1）→ 已并入 S21

`processSSEData` 的每个 type 分支抽成独立处理函数，配合 S3 的**入口级 seq 守卫**（守卫置于 switch 之前）实现"单点出口"——守卫拦截后四路同时短路；各 type 函数只负责"构造步骤 + 产出事件"，不再内联 `setExecutionSteps` / `saveStepsToStorage`（对应 3.5.1.2 病根的整改）。**已在 S21 中实施。**

#### 3.6.3 handlers 接口化约束（来源：21 §4.3 / §7.1）

用 TypeScript 接口约束 handlers，并按"数据读写"与"连接/生命周期控制"分组（setExecutionSteps/onChunk/onComplete 归数据；disconnect/syncServerTaskId 归连接），为 S2 收敛五源、S3 减面提供编译期护栏。

#### 3.6.4 暂停缓冲区设置大小上限（来源：21 §5.5）

`displayBufferRef` 增加容量上限（如 500 条），超限按 seq 顺序丢弃最老条目并告警，防止长时间暂停时缓冲无限增长；作为 S5 修复"回放死循环"之后的**防御性约束**，非根因修复。

#### 3.6.5 IDLE_TIMEOUT 可配置化（来源：21 §5.2）

空闲超时时长由常量改为可配置项，但**最小取值必须 ≥26s**，与后端 25s 心跳保持错开（≤25s 会与心跳同频假断，对应 3.4/6.2 红线）；当前 60s 值维持不变。

#### 3.6.6 chunk 累积收敛为单一缓冲（来源：21 §4.5）

"统一由 useChatCallbacks 管理 chunk 累积、解析层只传原始 chunk"的方向认可，**已并入方案 S7**：`responseBufferRef`（sseParser.ts:401）删除、`streamingContentRef`（useChatCallbacks.ts:313）为唯一累积缓冲、onComplete 不再"取较长者"。此处仅记录来源对应。

#### 3.6.7 一次到位原则（来源：21 §8.3 改写）

所有修改**一次性到位**——函数提取、数据源收敛、公共库沉淀同步实施，不分短期/中期/长期。文档25 的 S1~S22 按实施依赖顺序排列（先清障再动刀），但每条改动在实施时必须做到位：该提取的函数立即提取、该收敛的数据源立即收敛、该补的类型声明立即补全，不留尾巴、不预留"以后再改"。

#### 3.6.8 错误终态幂等清理：request_level 判据闸门（来源：22 §五-2 O2）→ 已并入 S22

"断链时错误终态缺失"是 3.5.2.3（复位逻辑依赖隐式时序）病根的下游表现。以 `request_level`（sseParser.ts:599 读原始 `rawData.step === 0`，字段定义 types/sse.ts:109）为幂等清理闸门：**仅对请求级错误**（整个请求失败，断链后不可能再有 final）做 waitTimer/loading/refs 的幂等终态清理；**执行级错误**（blocked/timeout，任务仍等 HITL 或继续运行）保持"等 final"原语义不动。**已在 S22 中实施。**

#### 3.6.9 seq 守卫与后端"同 seq 覆写"不冲突的契约确认（来源：22 §三）

文档22 独有取证，经本核查复核后采纳并作为 **S3 安全性的依据**补记于此：后端 `agent_runner.py` 5.8.6 注释确认"短条覆写 publish 原位（保 seq）"——覆写只发生在事件缓冲（StreamBuffer）内，`stream_reader` 流读取端对每个 seq 只读一次，前端按 seq 单调消费**不会**把"同 seq 覆写"读成两遍。故 S3 入口级 seq 守卫（3.5.1.2）与后端契约完全自洽，无冲突。此为事实确认而非新方案，仅为防止后续维护者在此处产生系统误判而记录来源对应。

#### 3.6.10 seq 守卫单点拦截方法（来源：23 §P2）

去重防线建在解析层**入口**，一处拦截四路（executionSteps state / executionStepsRef / saveStepsToStorage / onStep 回调），无 seq 帧（meta）不参与去重照常处理——该入口单点拦截方法并入 S3（与文档21 §O1、文档22 §O1 为同向共识）。

#### 3.6.11 appendStep 副作用归位方法（来源：23 §P7）

将各分支内联的 `setExecutionSteps(prev => { 写ref+保存; return newSteps; })` 收敛为单一 append 辅助函数、副作用移出 updater，final 分支保证 ref 先行供 onComplete 读全量——该方法并入 S4。

#### 3.6.12 useStateWithRef 封装 state/ref 双写同步（来源：24 §缺陷1 / §4.1）

"state 与 ref 手工双写"是 3.5.1.1 点名的全链路结构病（serverTaskId 靠 serverTaskIdRef + syncServerTaskId 手动同步 useSSE.ts:408-410、isReceiving 靠 useEffect 异步同步 :405-415），S2/S4 只覆盖了 executionSteps 一类；对 serverTaskId/isReceiving 等**普通字段**，文档24 提供方法：新建 `useStateWithRef` hook（返回 `{state, ref, setValue}`，`setValue` 一次调用同时写 ref 与 state，`value instanceof Function` 时基于 ref.current 计算），替换各处手动双写与 useEffect 同步，异步回调一律读 ref 拿最新值。此方法补足 S2/S4 的覆盖面，落地时与 3.5.1.1 病根一并处置。

#### 3.6.13 sessionStorage 快照来源标记（来源：24 §缺陷4 / §4.5）

3.5.3.2 将 sessionStorage 定性为"高频持久化轻误用"，方向是降级为低频兜底快照；文档24 提供快照判据方法：写入时带 `{steps, source, timestamp, version}` 元数据外壳，恢复时校验来源并兼容旧格式（`parsed.steps || parsed`）。该元数据使"快照新鲜度/来源"可判别，与 S13"REST 结果与 settledSteps 等长校验、短则弃"同思路——降级为只读快照后，凭 timestamp/source 决定快照是否仍可信，作为 S2/S13 落地时的辅助判据。

#### 3.6.14 HITL 等待态 Map 计数管理（来源：24 §缺陷7 / §4.8）

`isHitlWaitingRef` 单布尔在**并发 HITL** 场景存在误判：一处分歧（wrappedOnResumed）即把整体等待态置 false，另一处仍在等时 IDLE 计时被错误恢复。方法：改用 `Map<key, boolean>` 计数（onPaused 后按 confirmId 增、onResumed 后删，等待数=size>0），并在组件卸载时 `clear()` 防状态残留——该方法支撑 3.5.1.4"生命周期终态拼接"的收敛，属防御性补充，非三症状根因修复。

---

## 四、本地代码确凿问题清单（行号证据）

### Q1：executionSteps 五份状态无单一真源

| # | 状态源 | 位置 | 读写方 |
|---|--------|------|--------|
| ① | `executionSteps` state | useSSE.ts:383 | sseParser upstreams 写、UI 读 |
| ② | `executionStepsRef` | useSSE.ts:384 | sseParser 写、onComplete:517 读 |
| ③ | `executionStepsRef`（同名不同物） | useChatState.ts:259 | onError:578 清空、onStep:266 读 |
| ④ | `message.executionSteps` | useChatCallbacks.ts:266 追加 | 消息流 |
| ⑤ | sessionStorage（300ms 防抖） | useSSE.ts:485-500 | 刷新恢复 |

**后果**：同一业务步骤在 5 处漂移，任何一处不同步就出现重复/丢失。

### Q2：双 executionStepsRef 同名碰撞

- useSSE.ts:384 与 useChatState.ts:259 同名不同物。
- **onError 清的是 ③**（useChatCallbacks.ts:578 解构自 useChatState），**onComplete 读的是 ②**（useSSE.ts:517）→ 同一个"清空"操作对另一份 ref 无效 → 步骤残留。

### Q3：updater 内副作用（React StrictMode 会双重执行）

sseParser.ts 下列分支的 `setExecutionSteps((prev) => { ...副作用; return [...] })`：

| 分支 | 行号 | 副作用 |
|------|------|--------|
| thought | 380-390 | 写 ref + saveStepsToStorage |
| chunk | 429-439 | 写 ref + saveStepsToStorage |
| final | 502-511 | 写 ref + onComplete 同步读 |
| action | 678-688 | 写 ref + saveStepsToStorage |
| observation | 829-839 | 写 ref + saveStepsToStorage |
| error/paused | 869-879 | 写 ref + setPaused 通知 |

### Q4：onResumed error 死循环（真 bug）

- useChatCallbacks.ts:739-741：`errors.forEach(→onError)` 回放时，isPausedRef 仍为 true（:747 才复位）。
- useChatCallbacks.ts:600-603：onError 内 `if (isPausedRef.current) { push 回 buffer; return; }`。
- **链路**：onResumed 回放 error → onError 命中暂停 → error 推回 buffer → 死循环不退出。

### Q5：cancelInProgress 双 final（真 bug）

- useChatCallbacks.ts:184-190：`if (!isCancelEvent && step.type !== 'final')` 放行。
- 取消后服务端发 `final(completed)` → 判断条件为 `type !== 'final'` 之外的 final 也被放行 → 双完成帧/完成后又插入旧步骤。
- **影响面**：双重——①UI 步骤区（state 渲染区）②消息气泡（onStep :266 直接把 final append 进 message.executionSteps）。取消后最终消息里出现两帧 final，内容重复。

### Q5b：fingerprint 与取消守卫对 final 无区分

- useChatCallbacks.ts:174-175 `isCancelEvent = step.type==='final' && step.outcome==='cancelled'`；指纹(:161-171)对 final 帧无 preview 位区分，两帧 `final|completed` 若是同 content 还会被指纹拦一个 → **cancel 后 final 计数不定**（0/1/2 皆可能），UI 状态依赖 final 帧数。

### Q6：⭐ 去重防线只拦一半——解析层对重放零防御（最核心问题之一）

**现状链路**（sseParser.ts 5处分支全部无条件追加）：

| 分支 | 行号 | 无条件追加到 |
|------|------|-------------|
| thought | 380-390 | setExecutionSteps + executionStepsRef + saveStepsToStorage |
| chunk | 429-439 | 同上 |
| final | 502-511 | 同上（+ onComplete 同步读 ref） |
| action | 678-688 | 同上 |
| observation | 829-839 | 同上 |

**三层都在累积，只有一层被拦**：
- 解析层（sseParser:380-390 等）：`setExecutionSteps(prev => [...prev, step])` **无条件追加** executionSteps **state** + **ref** + **sessionStorage** —— 对重放/重复行零防御。
- 消费层（useChatCallbacks.ts:161-171 onStep 指纹）：**仅保护 message.executionSteps（消息区）**。
- **后果**：断线重连 GET 重放时，消息区被指纹挡住不重复显示，但 executionSteps（UI 步骤展示区）、executionStepsRef（onComplete 读源）、sessionStorage（刷新恢复源 :473-474）**三处仍成倍累积** → 刷新后步骤翻倍、onComplete 拿重复数据写回消息、356步残留类问题复发。

**⭐ 用户可见重复的直接来源（本核查独有补强）**：
- 用户看到的"步骤重复显示"**主要来自 executionSteps state 渲染区**（PipelineRenderer / RightViewer / 查看区读的是 `executionSteps` state，useChatFacade:287/isReceiving 消费即 state），而消息气泡区（message.executionSteps）有指纹保护**反而不重复**。
- 意味着：**渲染管道被重复喂 data，消重只作用在气泡** → 气泡正常、流水线/查看区翻倍，用户第一眼看到的就是左侧流水线重复（与 2026-09-09 pipeline chunk step 重复显示修复文档同症状）。
- action preview+canonical 双发（sseParser:663 注释"preview/canonical 双发 list-back 时序"）都进 executionSteps state → 正常任务单步就占2条；重放再翻倍。

> 小沈[23] A1（"去重防线只拦一半"）正是此问题，本核查独立复核后确认属实，且影响面比报告所述更广：**消息区只是表象，UI 流水线/查看区才是重复主会场**。

**根因**：去重防线建在"消费层"，解析层无 seq 单调守卫。后端每帧唯一 seq（task_state.py:51）是现成的权威护栏，前端却只用于 usage 分支（sseParser:267-271），业务帧全靠内容指纹兜底。

### Q7：双缓冲分裂

- sseParser.ts:401 `responseBufferRef`（解析层无条件累积）
- useChatCallbacks.ts:313 `streamingContentRef`（回调层累积，暂停时 :302-309 不累积）
- 两缓冲互不感知，onComplete:451 混用 `finalResponse || streamingContentRef.current`。

### Q8：fetchTimeout 名不副实

- useSSE.ts:682 设置 180s，:717-720 在 `fetch()` resolve 后立即 clear → **只覆盖首响应等待，不覆盖流中途**。语义名不符实（应以"首响应超时"为名，靠 idle+心跳保活性）。

### Q9：onComplete"取较长者"补丁（重连重放脏）

- useChatCallbacks.ts:454-459 按长度比较 `finalResponse` vs `streamingContentRef.current` 选源 → 断线重放（after_seq 续传）时混入残留旧缓冲，导致内容漂移。

### Q10：双超时机制语义混乱

- idle 60s（useSSE.ts:444，已与心跳 25s 错开，正确）
- fetchTimeout 180s（名不副实，见 Q8）
- reconnect delay（useSSE.ts:520-610 disconnect 内退避逻辑，动态退避最大三次重连）
- 三者职责边界在代码注释中互引，但实际各管一段，读者易误判。

### Q11：死代码残留（YAGNI 违反）

| 死代码 | 位置 | 说明 |
|--------|------|------|
| `streamingStepsRef` | useChatState.ts:261 | 只被清空(=[])、无写入点 |
| `reconnectConfigRef.enabled` | useSSE.ts:597-601 | 设置后从未读取，实际开关走 errorConfig.retryable(:217-218) |
| `processSSEData` 第3参 `_isProcessingRef` | sseParser.ts:120 | 形参未使用 |
| useMemo 50+ 依赖 | useChatFacade.ts:252-418 | 互文依赖，无 memo 价值 |

### Q12：⭐ 高频输出时主线程阻塞（用户症状2"输出中短时冻结被堵"）

**三重同步阻塞叠加**（高频输出期每 300-500ms 发生一次）：

1. **sseParser 每 chunk/step `[...prev, step]` O(n) 数组全量复制**（:380/:429/:678/:829 等）——数组上千条时每次复制耗 C 级，且 setExecutionSteps 触发 React 全量重渲染 Pipeline。
2. **saveStepsToStorage 300ms 防抖内同步 `JSON.stringify(上1000条)+sessionStorage.setItem`**（useSSE.ts:493）——setItem 是同步阻塞 API，大字符串一次写几十~几百 ms。
3. **useChatPersistence.saveState 每状态变化/500ms 又全量 `JSON.stringify` 整个 messages 数组**（:192-220，含每消息 executionSteps）——与第2路叠加。

**后果**：LLM 输出高频期间，主线程周期性被 2 路大数组序列化+同步写锁死 → 打字机停顿数秒 → UI 冻结被堵，用户看得见。

### Q13：⭐ 结束瞬时 live→history 切换丢最后几步（用户症状3"丢失steplog最后几步"）

**症结**（RightViewer.tsx:303-307 + :235-287）：
- 执行中 `displaySteps = liveSteps`（executionSteps state，实时全量）。
- **final 到达瞬时 `_hasFinal` 翻转 → `isCurrentLive` 变 false → `displaySteps` 切 `historySteps`**（由 :273-278 的 300ms 延迟后 GET /getTaskSteps 填充）。
- 但 :273-278 注释表明确认 **DB 落库窗口存在**（"SSE final 先发、DB 写入稍后"）——300ms 缓冲是拍脑袋猜的，网络慢/SSE 快时 DB 仍缺尾 → **REST 顶替 live 后最后几步永久消失**。
- 若此时 liveSteps 已含 final（useTaskInfo:129-130），判终切历史 → live 中被 seq/指纹拦掉或 updater 未提交的尾步在此一并丢失。

**架构根因**：**实时数据源(liveSteps state)与终态数据源(REST historySteps)双源切换无同步点**——结束不是"把 live 快照交给终态"，而是"猜测 DB 落库后覆盖 live"，必然有丢尾窗口。

## 五、根治优化方案（不留死角）

> 合并小沈[23]问题主体 + 小草[22]稳妥实施 + 本核查独有修正（seq 初始值等）。每条方案对应到根因，绝不加抽象、绝不 backward。
> **所有代码 diff 均基于本地最新真实源码（2026-09-10 11:59:55 核实），行号经逐行比对确认。**

### S1：清理死代码（最先做，零风险）

#### S1.1 useSSE.ts — 删除 streamingStepsRef（Q11 死代码）

```diff
// useSSE.ts:260-261 — 删除无写入点的 streamingStepsRef
- const streamingStepsRef = useRef<ExecutionStep[]>([]);
- const streamingSteps = streamingStepsRef.current;
```

#### S1.2 useSSE.ts — 删除 reconnectConfigRef.enabled 死代码

```diff
// useSSE.ts:418-423 — reconnectConfigRef.enabled 设置后从未读取
- const reconnectConfigRef = useRef<ReconnectConfig>({
-   enabled: true,
-   maxAttempts: 3,
-   baseDelay: 1000,
-   maxDelay: 10000,
- });
+ const reconnectConfigRef = useRef<Omit<ReconnectConfig, 'enabled'>>({
+   maxAttempts: 3,
+   baseDelay: 1000,
+   maxDelay: 10000,
+ });

// useSSE.ts:597-601 — 删除 enabled 设置代码
- reconnectConfigRef.current.enabled = false;
- // 3秒后恢复重连功能（避免永久禁用）
- setTimeout(() => {
-   reconnectConfigRef.current.enabled = true;
- }, 3000);
```

#### S1.3 sseParser.ts — 删除未使用的 _isProcessingRef 形参

```diff
// sseParser.ts:120 — 形参未使用
- ): UseSSEReturn => {
-   // ...handlers,
-   _isProcessingRef: React.MutableRefObject<boolean>
+ ): UseSSEReturn => {
+   // ...handlers,
```

#### S1.4 useChatState.ts — 删除 streamingStepsRef 死代码

```diff
// useChatState.ts:261 — 只被清空无写入点
- const streamingStepsRef = useRef<ExecutionStep[]>([]);
```

### S2：收敛 executionSteps 单一真源（根治 Q1）

#### S2.1 useChatState.ts — 删除 executionStepsRef（只保留 useSSE 的 ref）

```diff
// useChatState.ts:259 — 删除同名不同物的 ref
- const executionStepsRef = useRef<ExecutionStep[]>([]);
```

#### S2.2 useChatCallbacks.ts — onError 改读 useSSE 的 executionStepsRef

```diff
// useChatCallbacks.ts:578 — 清空操作改为只清一个 ref
- executionStepsRef.current = [];
+ // 小欧 2026-09-10 S2: executionStepsRef 已统一为 useSSE 的 ref，此处不重复清空
+ // executionStepsRef 通过 props 注入，onComplete 读同一 ref，不再清错对象
```

#### S2.3 useChatStreaming.ts — 透传 useSSE executionStepsRef 到 useChatCallbacks

```diff
// useChatStreaming.ts:284-293 — 从 useSSE 解构 executionStepsRef
  const {
    streamingContentRef,
    streamingStepsRef,
    executionStepsRef,
    currentSessionIdRef,
    replyUserMessageIdRef,
    waitTimerRef,
  } = state;
+
+ // 小欧 2026-09-10 S2: useSSE 的 executionStepsRef 透传给 callbacks
+ // 消除 useChatState:259 同名 ref 的歧义（Q2）
```

#### S2.4 useChatState.ts — 删除 syncServerTaskId 手动同步函数

```diff
// useSSE.ts:408-412 — serverTaskId 手工双写
- const syncServerTaskId = useCallback((id: string | null) => {
-   serverTaskIdRef.current = id;
-   setServerTaskId(id);
- }, []);
+ // 小欧 2026-09-10 S2: 由 useStateWithRef(S14) 封装替代，见 S14
```

**彻底性**：五份状态收敛为一份权威 + 三份只读镜像，任何"清空/追加"只落在同一位置。

### S3：seq 权威守卫上移 sseParser 入口（根治 Q6 ⭐，三层一次获救）

#### S3.0 sseParser.ts handlers 类型 — 新增 lastSeqRef 字段

```diff
// sseParser.ts:68-120 handlers 类型声明
  handlers: {
    setExecutionSteps: ...;
    executionStepsRef: ...;
    saveStepsToStorage: ...;
    onStep: ...;
    // ... 其他已有字段
+   // 小欧 2026-09-10 S3: seq 权威守卫 — 传入 lastSeqRef 供守卫判定
+   lastSeqRef: React.MutableRefObject<number>;
  },
```

#### S3.1 sseParser.ts — onSeq 之后、switch 之前插入 seq 守卫
    if (typeof rawData.seq === 'number' && onSeq) {
      onSeq(rawData.seq);
    }
+
+   // 小欧 2026-09-10 S3: seq 权威守卫 — next-seq 语义(初始0)
+   // 首帧 seq=0 → 推进 next=1 → 0<1 放行; 重放 seq=0 → 0<1 拦截
+   // 守卫在 switch 前 return：state/ref/storage/onStep 四路同时短路
+   if (typeof rawData.seq === 'number' && handlers.lastSeqRef) {
+     if (rawData.seq < handlers.lastSeqRef.current) {
+       console.debug(`[SSE] seq守卫拦截: seq=${rawData.seq} < next=${handlers.lastSeqRef.current}`);
+       return;
+     }
+   }

    // 【小强修复 2026-03-18】统一处理timestamp转换
    let timestampValue = Date.now();
```

**精确定义**（超越三份报告的关键修正）：
- `lastSeqRef` 语义改为"**期望的下一 seq**"（next-seq），初始值 `0`。
- 守卫放 `onSeq()` 之后、switch 之前：先 `onSeq(rawData.seq)` 推进 `lastSeqRef = Math.max(lastSeqRef, rawData.seq+1)`，再 `if (rawData.seq < lastSeqRef.current) return;`。
- **seq=0 首帧与 seq=0 重放帧分离，互不误伤**。
- 重连时 lastSeqRef **不重置**（保持 next-seq），新请求才重置 `0`（useSSE.ts:678）；`after_seq=${lastSeqRef.current}`（:690）续传天然对齐。
- 指纹去重（O1）**保留**作内容级兜底，与 seq 守卫双保险。

#### S3.5 seq 守卫首帧陷阱——三份报告均未覆盖的死角

**陷阱**：seq=0 既可能是首帧（合法），也可能是重放帧（非法），两种方案均有致命缺陷：
- 小沈[23]：`if (_seq && _seq <= lastConsumedSeqRef?.current) return;` — `_seq` 为 `0` 时 falsy → **seq=0 重放帧永不拦截**（守卫对首帧退缩）。
- 小草[22]：若用 `seq <= lastSeq` 且 lastSeq 初始 0 → **首帧 seq=0 被误拦**，首条 thought/action 丢失。

**解法**：next-seq 语义 + 守卫在 onSeq 之后，两帧分离，双杀。验证矩阵：

| 帧 | lastSeq(next) | 守卫表达式 | 期望 |
|----|--------------|-----------|------|
| 首帧 seq=0 | 0→1 | `0 < 1` | 放行 ✓ |
| 正常 seq=1 | 1→2 | `1 < 2` | 放行 ✓ |
| 重放 seq=0 | 1 | `0 < 1` | 拦截 ✓ |
| 重连续传 seq=5.. | 1 | `5 < 1` 假 | 放行 ✓ |

#### S3.5.1 首帧陷阱验证代码——单元测试 diff

```diff
// sseParser.test.ts — 新增 seq 守卫首帧陷阱验证（四场景全覆盖）
+ // 小欧 2026-09-10 S3.5: seq 守卫首帧陷阱——三份报告均未覆盖的死角
+ describe('S3.5 seq 守卫首帧陷阱', () => {
+   let lastSeqRef: React.MutableRefObject<number>;
+   let handlers: ProcessHandlers;
+   beforeEach(() => {
+     lastSeqRef = { current: 0 }; // next-seq 初始值 0
+     handlers = createMockHandlers({ lastSeqRef });
+   });
+
+   // 场景1: 首帧 seq=0 → onSeq 推进 next=1 → 0<1 放行
+   it('首帧 seq=0 不被拦截', () => {
+     const rawData = { type: 'thought', seq: 0, step: 1, thought: 'test' };
+     processSSEData(JSON.stringify(rawData), handlers);
+     expect(handlers.onStep).toHaveBeenCalled(); // 放行 → 步骤正常产出
+     expect(lastSeqRef.current).toBe(1);         // next 推进到 1
+   });
+
+   // 场景2: 正常 seq=1 → onSeq 推进 next=2 → 1<2 放行
+   it('正常 seq=1 放行', () => {
+     lastSeqRef.current = 1; // 已有首帧
+     const rawData = { type: 'thought', seq: 1, step: 2, thought: 'test2' };
+     processSSEData(JSON.stringify(rawData), handlers);
+     expect(handlers.onStep).toHaveBeenCalled();
+     expect(lastSeqRef.current).toBe(2);
+   });
+
+   // 场景3: 重放 seq=0 → lastSeq=1 → 0<1 拦截
+   it('重放 seq=0 被拦截', () => {
+     lastSeqRef.current = 1; // 已有首帧，next=1
+     const rawData = { type: 'thought', seq: 0, step: 1, thought: 'replay' };
+     processSSEData(JSON.stringify(rawData), handlers);
+     expect(handlers.onStep).not.toHaveBeenCalled(); // 拦截 → 步骤不产出
+     expect(handlers.setExecutionSteps).not.toHaveBeenCalled();
+   });
+
+   // 场景4: 重连续传 seq=5 → lastSeq=1 → 5<1 假 → 放行
+   it('重连续传 seq=5 放行', () => {
+     lastSeqRef.current = 1; // lastSeq 未重置
+     const rawData = { type: 'thought', seq: 5, step: 6, thought: 'resumed' };
+     processSSEData(JSON.stringify(rawData), handlers);
+     expect(handlers.onStep).toHaveBeenCalled();
+     expect(lastSeqRef.current).toBe(6); // next 推进到 6
+   });
+ });
```

### S4：updater 纯函数化（根治 Q3）

#### S4.1 sseParser.ts — 提取纯 appendStep updater（6 个分支复用）

```diff
// sseParser.ts:140 后新增 — 纯函数 updater，零副作用
+   // 小欧 2026-09-10 S4: 纯 updater，StrictMode 安全
+   const appendStep = (prev: ExecutionStep[], step: ExecutionStep): ExecutionStep[] => {
+     return [...prev, step];
+   };
```

#### S4.2 sseParser.ts:380-390 thought 分支 — 纯化 updater

```diff
// sseParser.ts:380-390 — thought 分支
      setExecutionSteps((prev) => {
-       const newSteps = [...prev, step];
-       handlers.executionStepsRef.current = newSteps;
-       try {
-         saveStepsToStorage?.(newSteps);
-       } catch (e) {
-         console.warn('[SSE] sessionStorage 保存失败，可能容量不足:', e);
-       }
-       return newSteps;
+       return appendStep(prev, step);
      });
+     // 副作用移出 updater（见 S4.5 useEffect 统一处理）
```

#### S4.3 sseParser.ts:429-439 chunk 分支 — 同样纯化

```diff
// sseParser.ts:429-439 — chunk 分支
      setExecutionSteps((prev) => {
-       const newSteps = [...prev, step];
-       handlers.executionStepsRef.current = newSteps;
-       try {
-         saveStepsToStorage?.(newSteps);
-       } catch (e) {
-         console.warn('[SSE] sessionStorage 保存失败，可能容量不足:', e);
-       }
-       return newSteps;
+       return appendStep(prev, step);
      });
```

#### S4.4 sseParser.ts:498-511 / 678-688 / 829-839 / 869-879 — final/action/observation/paused 分支

```diff
// sseParser.ts:498-511 — final 分支（特殊：需先写 ref 供 onComplete 读全量）
- const updatedSteps = [...handlers.executionStepsRef.current, step];
- handlers.executionStepsRef.current = updatedSteps;
+ // 小欧 2026-09-10 S4: final 分支 ref 先行（供 onComplete 读全量）
+ handlers.executionStepsRef.current = appendStep(handlers.executionStepsRef.current, step);
- setExecutionSteps((prev) => {
-   const newSteps = [...prev, step];
-   try {
-     saveStepsToStorage?.(newSteps);
-   } catch (e) {
-     console.warn('[SSE] sessionStorage 保存失败，可能容量不足:', e);
-   }
-   return newSteps;
- });
+ setExecutionSteps((prev) => appendStep(prev, step));
  onStep?.(step);

// sseParser.ts:678-688 — action 分支
      setExecutionSteps((prev) => {
-       const newSteps = [...prev, step];
-       handlers.executionStepsRef.current = newSteps;
-       try {
-         saveStepsToStorage?.(newSteps);
-       } catch (e) {
-         console.warn('[SSE] sessionStorage 保存失败，可能容量不足:', e);
-       }
-       return newSteps;
+       return appendStep(prev, step);
      });

// sseParser.ts:829-839 — observation 分支（同上模式）
// sseParser.ts:869-879 — paused/resumed/retrying 分支（同上模式）
```

#### S4.5 useSSE.ts — useEffect 统一 ref 同步 + 防抖持久化

> **已被 S12 取消。** S12 的 `flushPendingSteps` 在批量 commit 时统一处理 ref 同步和持久化，此 useEffect 不再需要。删除。

```diff
// useSSE.ts:519 后新增
+ // 小欧 2026-09-10 S4: updater 副作用归位 — ref 同步 + 防抖持久化 统一单点
+ useEffect(() => {
+   executionStepsRef.current = executionSteps;
+   if (executionSteps.length > 0) {
+     saveStepsToStorage(executionSteps);
+   }
+ }, [executionSteps, saveStepsToStorage]);
```

**彻底性**：StrictMode 双重调用不再重复副作用；持久化收敛单点；onComplete 读 ref 始终是最新全量。

### S5：onResumed 先复位再回放（根治 Q4 死循环）

```diff
// useChatCallbacks.ts:738-747 — isPausedRef 复位提前到回放循环之前
- // error类型需单独处理（调用onError回调）
- for (const err of errorItems) {
-   onError(err);
- }
-
- // 清空缓冲区
- displayBufferRef.current = [];
-
- // 2026-08-27 小欧 修复#2: 同步复位 isPausedRef.current
- isPausedRef.current = false;

+ // 小欧 2026-09-10 S5: 先复位 isPausedRef 再回放 — 死循环根治
+ // 原 isPausedRef 复位在回放循环之后(:747)，onError(:600) 命中暂停态
+ // 将 error 推回 buffer → 死循环。提前到回放前，onError 不再命中暂停。
+ isPausedRef.current = false;
+
+ // error类型需单独处理（调用onError回调）
+ for (const err of errorItems) {
+   onError(err);
+ }
+
+ // 清空缓冲区
+ displayBufferRef.current = [];
```

**彻底性**：时序因果修正（isPausedRef 复位 → onError 不命中暂停 → error 不推回 buffer），非补 by-pass。

### S6：cancelInProgress 只放行取消帧（根治 Q5）

```diff
// useChatCallbacks.ts:184-190 — 取消态只认 cancelled 帧
      if (cancelInProgressRef.current) {
-       if (!isCancelEvent && step.type !== 'final') {
+       // 小欧 2026-09-10 S6: 取消态只放行 cancelled 帧
+       // 原条件 type !== 'final' 放行了 final(completed)，导致双 final
+       if (!isCancelEvent) {
          console.log(`[取消] 忽略取消过程中收到的事件: ${step.type}`);
          return;
        }
```

**彻底性**：取消后 `final(completed)` 帧被拦截，双 final 根除。

### S7：缓冲统一（根治 Q7）

#### S7.1 sseParser.ts — 删除 responseBufferRef

```diff
// sseParser.ts:401 — 解析层不再攒缓冲
- responseBufferRef.current += chunkContent;
- setCurrentResponse(responseBufferRef.current);
+ setCurrentResponse(chunkContent);
```

#### S7.2 useChatCallbacks.ts:451 — onComplete 不再比长

```diff
// useChatCallbacks.ts:451 — 只信 streamingContentRef
- const finalContent = finalResponse.length > streamingContentRef.current.length
-   ? finalResponse
-   : streamingContentRef.current;
+ // 小欧 2026-09-10 S7: streamingContentRef 为唯一累积缓冲，不再比长
+ const finalContent = finalResponse || streamingContentRef.current;
```

**彻底性**：双缓冲归并，取长补丁消失。

### S8：超时语义钉死（根治 Q8/Q10）

```diff
// useSSE.ts:443 — 重命名语义
- const fetchTimeoutRef = useRef<number | null>(null); // 180s fetch超时检测
+ // 小欧 2026-09-10 S8: 重命名 — 仅覆盖首响应等待(180s)，非全程超时
+ const firstChunkTimeoutRef = useRef<number | null>(null);

// useSSE.ts:682 — 设定处
- fetchTimeoutRef.current = window.setTimeout(
+ firstChunkTimeoutRef.current = window.setTimeout(
    () => controller.abort(),
    180000
- ); // 180s超时
+ ); // 首响应超时(180s) — 流中途活性由 idle(60s)+心跳(25s)保障

// useSSE.ts:717-720 — 清除处
- if (fetchTimeoutRef.current) {
-   clearTimeout(fetchTimeoutRef.current);
-   fetchTimeoutRef.current = null;
+ if (firstChunkTimeoutRef.current) {
+   clearTimeout(firstChunkTimeoutRef.current);
+   firstChunkTimeoutRef.current = null;
  }
```

### S9：abort 竞态窗口（根治 P4 残留）

```diff
// useSSE.ts:651 — disconnect 前 flush buffer
  disconnect(false, false, undefined, false, false);
+ // 小欧 2026-09-10 S9: abort 前 flush processBuffer，防止窗口期数据丢弃
+ if (processBufferRef.current && processBufferRef.current.length > 0) {
+   const pending = processBufferRef.current.splice(0);
+   pending.forEach(line => processSSEData(line, handlers));
+ }
```

### S10：sessionStorage 恢复路径预过滤 preview（根治 Q6 残余）

useSSE.ts:470-472 已有 preview 过滤，但仅在**恢复路径**生效；需确认**新增路径**（reader 循环产出 → setExecutionSteps → RightViewer）不写 preview 到 state。若恢复路径漏过滤，preview action 会在历史页多渲染一次空壳。

```diff
// useSSE.ts:470-472 — 恢复路径已实现 preview 过滤（验证确认）
  const parsedSteps: ExecutionStep[] = JSON.parse(raw);
+ // 小欧 2026-09-10 S10: 恢复路径预过滤 preview — 不写入 state，仅执行路径写入
  const filteredSteps = parsedSteps.filter(
    s => !(s.type === 'action' && s.preview === true)
  );
  executionStepsRef.current = filteredSteps;
  setExecutionSteps(filteredSteps);

// 新增路径（reader 循环产出）不需要过滤：preview action 由 S4 appendStep 的
// step.type === 'action' && step.preview === true 不会触发 setExecutionSteps 副作用
// （S12 的 pendingSteps.push + scheduleFlush 只 push 非 preview 步骤）
// 但需在 processSSEData 的 action 分支加显式 guard：
// sseParser.ts — action 分支（S21 handleAction 内）
+ // 小欧 2026-09-10 S10: 新增路径 guard — preview action 不进 pendingSteps
+ if (step.preview) return; // 仅弹 toast，不写 state
```

### S11：onStep 指纹清理——onComplete/onError 同步清 Set（根治重复渲染残留）

useChatCallbacks.ts:581 已在 `from_backend` 错误路径清 `onStepFingerprintRef.current.clear()`。需在 onComplete 路径补清。

```diff
// useChatCallbacks.ts:444-459 onComplete 内
  setMessages((prev) => {
    // ...
  });
+ // 小欧 2026-09-10 S11: onComplete 同步清指纹 Set，供下一任务重新计数
+ onStepFingerprintRef.current.clear();
```

### S12：主线程同步阻塞根除（根治 Q12 ⭐ 症状2）

#### S12.1 useSSE.ts — 批量 commit 模式（替代每帧 setExecutionSteps）

```diff
// useSSE.ts:383 后新增 — 批量提交队列
+ // 小欧 2026-09-10 S12: 批量 commit — 每帧只 append 到 pendingSteps
+ // requestAnimationFrame 合并多帧，单次 setExecutionSteps 批量更新
+ const pendingStepsRef = useRef<ExecutionStep[]>([]);
+ const flushScheduledRef = useRef(false);
+
+ const flushPendingSteps = useCallback(() => {
+   if (pendingStepsRef.current.length === 0) {
+     flushScheduledRef.current = false;
+     return;
+   }
+   const batch = pendingStepsRef.current.splice(0);
+   setExecutionSteps(prev => {
+     const newSteps = [...prev, ...batch];
+     executionStepsRef.current = newSteps;
+     return newSteps;
+   });
+   flushScheduledRef.current = false;
+ }, []);
+
+ const scheduleFlush = useCallback(() => {
+   if (!flushScheduledRef.current) {
+     flushScheduledRef.current = true;
+     requestAnimationFrame(flushPendingSteps);
+   }
+ }, [flushPendingSteps]);
```

#### S12.0 sseParser.ts handlers 类型 — 新增 pendingStepsRef / scheduleFlush 字段

```diff
// sseParser.ts:68-120 handlers 类型声明（接 S3.0）
+   // 小欧 2026-09-10 S12: 批量 commit — 传入 pendingStepsRef + scheduleFlush
+   pendingStepsRef: React.MutableRefObject<ExecutionStep[]>;
+   scheduleFlush: () => void;
```

#### S12.2 sseParser.ts — 所有分支改为 pendingSteps + scheduleFlush

```diff
// sseParser.ts:380-390 — thought 分支（所有分支同模式）
- setExecutionSteps((prev) => {
-   const newSteps = [...prev, step];
-   handlers.executionStepsRef.current = newSteps;
-   try {
-     saveStepsToStorage?.(newSteps);
-   } catch (e) {
-     console.warn('[SSE] sessionStorage 保存失败:', e);
-   }
-   return newSteps;
- });
+ // 小欧 2026-09-10 S12: 批量 append，零同步序列化
+ handlers.pendingStepsRef.current.push(step);
+ handlers.scheduleFlush();
```

#### S12.3 useSSE.ts — saveStepsToStorage 低频兜底（≥5s）

```diff
// useSSE.ts:485-500 — 防抖间隔从 300ms 改为 5s 兜底
  const saveStepsToStorage = useCallback(
    (steps: ExecutionStep[]) => {
      if (steps.length === 0 || !config.sessionId) return;
      if (saveStepsTimerRef.current !== null)
        clearTimeout(saveStepsTimerRef.current);
      saveStepsTimerRef.current = setTimeout(() => {
        const storageKey = `${SSE_STORAGE_KEY}_${config.sessionId}`;
        try {
          sessionStorage.setItem(storageKey, JSON.stringify(steps));
        } catch (e) {
          console.warn('[SSE] 保存到 sessionStorage 失败:', e);
        }
        saveStepsTimerRef.current = null;
-     }, 300);
+     }, 5000); // 小欧 2026-09-10 S12: 5s 兜底快照，去主线程同步阻塞
    },
    [config.sessionId]
  );
```

#### S12.4 useChatPersistence.ts — saveState 合并到同一防抖链

```diff
// useChatPersistence.ts:400-408 — 自动保存防抖从 1s 改为与 steps 共享
  useEffect(() => {
    const timer = setTimeout(() => {
      saveStateWithSSECheck();
-   }, 1000);
+   }, 5000); // 小欧 2026-09-10 S12: 与 steps 防抖同频，去双路全量 stringify
    return () => clearTimeout(timer);
  }, [messages, saveStateWithSSECheck]);
```

**彻底性**：主线程在高频期只做"批追加 + 内存态"，零同步序列化，打字机不冻结。

### S13：live→终态单一真源平滑交接（根治 Q13 ⭐ 症状3）

#### S13.1 RightViewer.tsx — 新增 settledSteps 快照

```diff
// RightViewer.tsx:118 后新增
+ // 小欧 2026-09-10 S13: live→终态快照 — final 到达时固化 executionStepsRef 全量
+ const [settledSteps, setSettledSteps] = useState<ExecutionStep[]>([]);
+ const settledRef = useRef<ExecutionStep[]>([]);
+
+ useEffect(() => {
+   if (_hasFinal && isCurrentLive) {
+     // final 到达瞬时：固化当前 liveSteps 为终态快照
+     const snapshot = executionStepsRef.current.length > 0
+       ? [...executionStepsRef.current]
+       : liveSteps;
+     settledRef.current = snapshot;
+     setSettledSteps(snapshot);
+   }
+ }, [_hasFinal, isCurrentLive, liveSteps]);
```

#### S13.2 RightViewer.tsx:303 — displaySteps 优先用 settledSteps

```diff
// RightViewer.tsx:303
- const displaySteps = isCurrentLive ? liveSteps : historySteps;
+ // 小欧 2026-09-10 S13: 结束瞬时先用 live 快照兜底，REST 成功且更长时再替换
+ const displaySteps = isCurrentLive
+   ? liveSteps
+   : (settledSteps.length > 0 ? settledSteps : historySteps);
```

#### S13.3 RightViewer.tsx:273-278 — REST 等长校验（替代 300ms 猜测）

```diff
// RightViewer.tsx:277-278 — 去掉 300ms 猜测缓冲
- if (_hasFinal && activeTaskId === serverTaskId) {
-   settleTimer = window.setTimeout(startFetch, 300);
- } else {
-   startFetch();
- }
+ // 小欧 2026-09-10 S13: 立即拉 REST，结果与 settledSteps 等长校验
+ startFetch();
```

#### S13.4 RightViewer.tsx startFetch 内 — REST 等长校验逻辑

```diff
// RightViewer.tsx startFetch 内部 — Promise.all 返回后增加等长校验
+   // 小欧 2026-09-10 S13.4: REST 结果与 settledSteps 等长校验——短则弃，保留 settledSteps 兜底
+   if (s.steps.length > 0 && settledRef.current.length > 0 && s.steps.length < settledRef.current.length) {
+     console.warn(`[RV] REST 步骤数(${s.steps.length}) < settledSteps(${settledRef.current.length})，弃用 REST`);
+     setHistorySteps(toExecutionSteps(settledRef.current));
+     return;
+   }
```

**彻底性**：结束即快照，REST 只作增量补齐，无窗口期可丢步，最后几步必显示。

### S14：useStateWithRef 封装 state/ref 双写同步（根治 3.5.1.1）

```diff
// 新增 hooks/useStateWithRef.ts
+ import { useState, useRef, useCallback, type MutableRefObject } from 'react';
+
+ // 小欧 2026-09-10 S14: 封装 state/ref 同步 — 替换手工双写
+ // 注意：ref 在 updater 外层同步（setState 回调只负责返回新值），违反 S4 纯函数原则
+ export function useStateWithRef<T>(
+   initialValue: T
+ ): [T, MutableRefObject<T>, (value: T | ((prev: T) => T)) => void] {
+   const [state, setState] = useState<T>(initialValue);
+   const ref = useRef<T>(initialValue);
+
+   const setValue = useCallback((value: T | ((prev: T) => T)) => {
+     setState(prev => {
+       const next = value instanceof Function ? value(prev) : value;
+       return next;
+     });
+     // ref 在 setState 回调外同步（下一帧 render 前 ref 已更新，异步回调可读最新值）
+     ref.current = value instanceof Function ? value(ref.current) : value;
+   }, []);
+
+   return [state, ref, setValue];
+ }
```

#### S14.1 useSSE.ts — 替换 serverTaskId/isReceiving 双写

```diff
// useSSE.ts:381-382 — isReceiving
- const [isReceiving, setIsReceiving] = useState(false);
- const isReceivingRef = useRef(false);
+ // 小欧 2026-09-10 S14: useStateWithRef 替换手工双写
+ const [isReceiving, isReceivingRef, setIsReceiving] = useStateWithRef(false);

// useSSE.ts:405-412 — serverTaskId
- const [serverTaskId, setServerTaskId] = useState<string | null>(null);
- const serverTaskIdRef = useRef<string | null>(null);
- const syncServerTaskId = useCallback((id: string | null) => {
-   serverTaskIdRef.current = id;
-   setServerTaskId(id);
- }, []);
+ const [serverTaskId, serverTaskIdRef, setServerTaskId] = useStateWithRef<string | null>(null);

// useSSE.ts:413-415 — 删除 useEffect 手动同步
- useEffect(() => {
-   isReceivingRef.current = isReceiving;
- }, [isReceiving]);
```

### S15：HITL Map 计数管理（来源：24 §缺陷7，防御性补充）

```diff
// useSSE.ts:446 — 改为 Map 计数
- const isHitlWaitingRef = useRef(false);
+ // 小欧 2026-09-10 S15: Map 计数 — 并发 HITL 场景防误判
+ const hitlWaitingKeysRef = useRef(new Set<string>());
+ const isHitlWaitingRef = { get current() { return hitlWaitingKeysRef.current.size > 0; } };

// useSSE.ts:447-454 — wrappedOnPaused/wrappedOnResumed 改为按 confirmId 增删
  const wrappedOnPaused = useCallback((confirmId?: string) => {
-   isHitlWaitingRef.current = true;
+   hitlWaitingKeysRef.current.add(confirmId ?? 'default');
    onPaused?.();
  }, [onPaused]);
  const wrappedOnResumed = useCallback((confirmId?: string) => {
-   isHitlWaitingRef.current = false;
+   hitlWaitingKeysRef.current.delete(confirmId ?? 'default');
    onResumed?.();
  }, [onResumed]);
```

### S16：appendStep 副作用归位（根治 S4 updater 内联 ref+持久化，见 S4 详述）

S4 的 updater 纯化过程中提取了 `appendStep` 公共函数，6 个分支统一调用。此处补充函数定义（实现于 sseParser.ts:130-140，被 S4.1~S4.4 的 diff 引用）：

```diff
// sseParser.ts — 新增 appendStep 公共函数（S4.1 的实现）
+ // 小欧 2026-09-10 S4/S16: appendStep — 纯 append，ref 同步由 S12 批量 commit 处理
+ function appendStep(prev: ExecutionStep[], step: ExecutionStep): ExecutionStep[] {
+   const newSteps = [...prev, step];
+   return newSteps;
+ }
```

**引用关系**：S4.1~S4.4 的每个 updater 分支将内联逻辑替换为 `return appendStep(prev, step)`；S12 的批量 commit 模式进一步收敛为 `pendingStepsRef.current.push(step)` + `scheduleFlush()`，appendStep 仅在 flush 路径被调用。

### S17：useStateWithRef 替换 isPausedRef / cancelInProgressRef 等（根治 3.5.1.1，见 S14 详述）

isPausedRef / cancelInProgressRef 等布尔 ref 只在 refs 层使用（不驱动渲染），维持 useRef 不变；useStateWithRef 仅用于"state 驱动渲染 + ref 供异步回调读最新值"场景。此处补充 S14 中未覆盖的 **isConnected / isHitting / hitlWaitingKeys** 替换 diff：

```diff
// useSSE.ts:405-412 — isConnected 双写替换（与 isReceiving 同模式）
- const [isConnected, setIsConnected] = useState(false);
- const isConnectedRef = useRef(false);
+ // 小欧 2026-09-10 S14/S17: useStateWithRef 替换手工双写
+ const [isConnected, isConnectedRef, setIsConnected] = useStateWithRef(false);

// useSSE.ts:446 — isHitting 双写替换
- const [isHitting, setIsHitting] = useState(false);
- const isHittingRef = useRef(false);
+ const [isHitting, isHittingRef, setIsHitting] = useStateWithRef(false);

// useEffect 手动同步删除（与 S14.1 同模式）
- useEffect(() => { isHittingRef.current = isHitting; }, [isHitting]);
- useEffect(() => { isConnectedRef.current = isConnected; }, [isConnected]);
```

**引用关系**：S14 覆盖 isReceiving / serverTaskId / executionSteps；本条补充 isConnected / isHitting 的同模式替换。

### S18：handlers 接口分组约束——编译期护栏（根治 S3/S4/S12 接口污染，见 S3.0 详述）

handlers 接口在 S3/S4/S12 中逐步收窄，此处补充 **S12 完成后的完整类型约束**（与 S3.0 同文件，同一类型声明块内）：

```diff
// sseParser.ts:68-120 handlers 类型声明 — 完整约束（S3.0 + S12 两轮修改后的最终形态）
+ // 小欧 2026-09-10 S3/S12/S18: handlers 接口最终约束 — 禁止暴露全量 ref，禁止同步写 state
  type ProcessHandlers = {
    executionStepsRef: React.MutableRefObject<ExecutionStep[]>;
    currentResponseRef: React.MutableRefObject<string>;
+   lastSeqRef: React.MutableRefObject<number>;           // S3: seq 守卫 next-seq
+   pendingStepsRef: React.MutableRefObject<ExecutionStep[]>; // S12: 批量 commit 队列
+   scheduleFlush: () => void;                              // S12: rAF 调度刷新
+   responseBufferRef: React.MutableRefObject<string>;     // S7: 唯一累积缓冲
+   setIsReceiving: (v: boolean) => void;                  // S12: 批量 commit 后通知
+   setIsConnected: (v: boolean) => void;                  // S12: batch 后通知
    onStep?: (step: ExecutionStep) => void;
    onChunk?: (content: string, isReasoning: boolean) => void;
    onComplete?: (...) => void;
    onError?: (error: string) => void;
    onPaused?: (...) => void;
    onResumed?: (...) => void;
    onMeta?: (...) => void;
    updateThinkingUI?: (id: string, update: ThinkingUpdate) => void;
    cancelConfirmUI?: (confirmId: string) => void;
    thinkingChunksRef: React.MutableRefObject<Map<string, string>>;
    pendingThinkingUpdateRef: React.MutableRefObject<Map<string, ThinkingUpdate>>;
-   _isProcessingRef?: React.MutableRefObject<boolean>;  // S3: 已删除
  };
```

**引用关系**：S3.0 添加 `lastSeqRef`，S12 添加 `pendingStepsRef` / `scheduleFlush`，本条补充完整类型快照并标注每个字段的归属。

### S19：sessionStorage 快照来源标记——写入元数据外壳（根治 S2/S13 辅助判据）

S2（恢复路径过滤 preview）和 S13（settledSteps 等长校验）已完成核心逻辑；此处补充**元数据外壳写入 diff**，使恢复路径能区分"本会话累积步骤"与"外部恢复步骤"：

```diff
// useSSE.ts saveStepsToStorage — 写入时包裹元数据外壳（S12 防抖链内）
  const saveStepsToStorage = useCallback(
    (steps: ExecutionStep[]) => {
      if (steps.length === 0 || !config.sessionId) return;
      if (saveStepsTimerRef.current !== null) clearTimeout(saveStepsTimerRef.current);
      saveStepsTimerRef.current = setTimeout(() => {
        const storageKey = `${SSE_STORAGE_KEY}_${config.sessionId}`;
        try {
+         // 小欧 2026-09-10 S19: 写入元数据外壳——恢复时按 source 区分累积 vs 外部
+         const envelope = {
+           steps: steps,
+           source: 'live' as const,
+           timestamp: Date.now(),
+         };
-         sessionStorage.setItem(storageKey, JSON.stringify(steps));
+         sessionStorage.setItem(storageKey, JSON.stringify(envelope));
        } catch (e) {
          console.warn('[SSE] 保存到 sessionStorage 失败:', e);
        }
        saveStepsTimerRef.current = null;
      }, 5000);
    },
    [config.sessionId]
  );

// useSSE.ts 恢复路径（470-472）— 解析时兼容新旧格式
  const parsedRaw = JSON.parse(raw);
+ // 小欧 2026-09-10 S19: 兼容旧格式（纯 steps 数组）和新格式（{steps, source, timestamp}）
+ const parsedSteps: ExecutionStep[] = Array.isArray(parsedRaw)
+   ? parsedRaw
+   : (parsedRaw?.steps ?? []);
+ const source: string = Array.isArray(parsedRaw) ? 'legacy' : (parsedRaw?.source ?? 'unknown');
+ console.info(`[SSE] 恢复 ${parsedSteps.length} 步, source=${source}`);
  const filteredSteps = parsedSteps.filter(
    s => !(s.type === 'action' && s.preview === true)
  );
```

**引用关系**：S2 过滤 preview 步骤（恢复路径已有，本条补充外壳写入）；S13 settledSteps 快照读取时也检查 `source` 字段，确保快照来源可追溯。

### S20：连接层函数提取——长期可维护性（来源：3.6.1）

useSSE.ts 1086 行承担连接/断开/重连/空闲/持久化/轮询六大职责（3.5.2.5），提取连接管理相关逻辑为独立函数，降低单文件 SRP 发散面，长期维护时单点修改不影响业务逻辑。

#### S20.1 useSSE.ts — 提取 startIdleMonitor / clearIdleMonitor

```diff
// useSSE.ts — 从 reader 循环(:744-765) 和 disconnect(:546-549) 中提取空闲监控
+ // 小欧 2026-09-10 S20: 空闲监控提取为独立函数——长期维护时只改一处
+ const startIdleMonitor = useCallback(() => {
+   if (idleTimeoutRef.current) clearTimeout(idleTimeoutRef.current);
+   idleTimeoutRef.current = window.setTimeout(() => {
+     if (isHitlWaitingRef.current) return;
+     const timeSinceLastData = Date.now() - lastDataTimeRef.current;
+     if (timeSinceLastData >= IDLE_TIMEOUT && isReceivingRef.current) {
+       console.warn('[SSE] 空闲超时');
+       onError?.('SSE 空闲超时：长时间未收到数据');
+       reconnect();
+     }
+   }, IDLE_TIMEOUT);
+ }, [IDLE_TIMEOUT, reconnect, onError]);
+
+ const clearIdleMonitor = useCallback(() => {
+   if (idleTimeoutRef.current) {
+     clearTimeout(idleTimeoutRef.current);
+     idleTimeoutRef.current = null;
+   }
+ }, []);
```

#### S20.2 useSSE.ts — 提取 clearAllTimers

```diff
// useSSE.ts — disconnect 和 sendMessageInternal 末尾清理散落的定时器清理由统一函数收敛
+ // 小欧 2026-09-10 S20: 统一定时器清理——长期维护新增定时器只改此处
+ const clearAllTimers = useCallback(() => {
+   clearIdleMonitor();
+   if (fetchTimeoutRef.current) { clearTimeout(fetchTimeoutRef.current); fetchTimeoutRef.current = null; }
+   if (reconnectTimeoutRef.current) { clearTimeout(reconnectTimeoutRef.current); reconnectTimeoutRef.current = null; }
+   if (saveStepsTimerRef.current) { clearTimeout(saveStepsTimerRef.current); saveStepsTimerRef.current = null; }
+   if (intentionalAbortTimerRef.current) { clearTimeout(intentionalAbortTimerRef.current); intentionalAbortTimerRef.current = null; }
+ }, [clearIdleMonitor]);
```

#### S20.3 useSSE.ts — disconnect / sendMessageInternal 调用 clearAllTimers

```diff
// useSSE.ts:528 disconnect 内部 — 替换散落的 clearTimeout
  const disconnect = useCallback((...) => {
    if (clearStorage) clearStepsFromStorage();
-   if (reconnectTimeoutRef.current) { clearTimeout(reconnectTimeoutRef.current); reconnectTimeoutRef.current = null; }
-   if (idleTimeoutRef.current) { clearTimeout(idleTimeoutRef.current); idleTimeoutRef.current = null; }
-   if (fetchTimeoutRef.current) { clearTimeout(fetchTimeoutRef.current); fetchTimeoutRef.current = null; }
+   clearAllTimers();
    // ... rest of disconnect
  }, [...]);

// useSSE.ts:850-858 sendMessageInternal 末尾 — 同理替换
- if (idleTimeoutRef.current) { clearTimeout(idleTimeoutRef.current); idleTimeoutRef.current = null; }
+ clearAllTimers();
```

### S21：解析层按 type 提取处理函数——长期可维护性（来源：3.6.2）

processSSEData 单函数 900 行处理 17 种 type（3.5.1.2/3.5.2.5），每个 type 分支内联构造步骤+setExecutionSteps+写ref+持久化+回调。提取为独立 handler 函数后：①长期新增 type 只加一个函数+一行 case；②S3 seq 守卫在 switch 前拦截，各 handler 不再重复判断；③每个 handler 只负责"构造步骤+产出事件"，不直接碰 ref/持久化（由 S12 的 pendingSteps 统一处理）。

#### S21.1 sseParser.ts — 提取 handleThought / handleChunk / handleFinal / handleAction / handleObservation / handleMeta

```diff
// sseParser.ts — 每个 type 分支提取为独立函数（以 thought 为例，其余同模式）
+ // 小欧 2026-09-10 S21: 按 type 提取处理函数——长期维护新增 type 只加一个函数+一行 case
+ const handleThought = (
+   rawData: Record<string, unknown>,
+   step: ExecutionStep,
+   handlers: ProcessHandlers,
+   frameTime: number
+ ): void => {
+   const stepNum = Number(rawData.step) || 1;
+   step.step = stepNum;
+   step.timestamp = frameTime;
+   step.thought = rawData.thought || '';
+   step.reasoning = rawData.reasoning || '';
+   step.tool_name = rawData.tool_name || '';
+   step.tool_params = rawData.tool_params || rawData.params || {};
+   handlers.pendingStepsRef.current.push(step);
+   handlers.scheduleFlush();
+   handlers.onStep?.(step);
+ };
+
+ const handleChunk = (
+   rawData: Record<string, unknown>,
+   step: ExecutionStep,
+   handlers: ProcessHandlers,
+   _frameTime: number
+ ): void => {
+   const is_reasoning = normalizeIsReasoning(rawData.is_reasoning);
+   const chunkContent = rawData.content || '';
+   handlers.onChunk?.(chunkContent, is_reasoning);
+   step.content = chunkContent;
+   handlers.pendingStepsRef.current.push(step);
+   handlers.scheduleFlush();
+   handlers.onStep?.(step);
+ };
+
+ const handleFinal = (
+   rawData: Record<string, unknown>,
+   step: ExecutionStep,
+   handlers: ProcessHandlers,
+   frameTime: number
+ ): void => {
+   step.step = Number(rawData.step) || 1;
+   step.timestamp = frameTime;
+   step.response = rawData.response || '';
+   step.content = step.response;
+   step.outcome = rawData.outcome;
+   step.error_type = rawData.error_type;
+   step.error_message = rawData.error_message;
+   // ref 先行供 onComplete 读全量
+   handlers.executionStepsRef.current = [...handlers.executionStepsRef.current, step];
+   handlers.pendingStepsRef.current.push(step);
+   handlers.scheduleFlush();
+   handlers.onStep?.(step);
+   const finalStepsWithCurrent = handlers.executionStepsRef.current;
+   handlers.onComplete?.(handlers.responseBufferRef.current, {
+     model: rawData.model,
+     provider: rawData.provider,
+     display_name: rawData.display_name,
+   }, finalStepsWithCurrent);
+   handlers.setIsReceiving(false);
+   handlers.setIsConnected(false);
+ };
```

#### S21.2 sseParser.ts — switch 改为分派调用

```diff
// sseParser.ts:209 switch — 改为调用独立 handler
    switch (rawData.type) {
      case 'start':
-       // ... 50 行内联逻辑
+       handleStart(rawData, handlers, frameTime);
+       break;
      case 'thought':
-       // ... 15 行内联逻辑
+       handleThought(rawData, step, handlers, frameTime);
+       break;
      case 'chunk':
-       // ... 20 行内联逻辑
+       handleChunk(rawData, step, handlers, frameTime);
+       break;
      case 'final':
-       // ... 40 行内联逻辑
+       handleFinal(rawData, step, handlers, frameTime);
+       break;
      case 'error':
-       // ... 50 行内联逻辑
+       handleError(rawData, step, handlers, frameTime);
+       break;
      case 'action':
-       // ... 60 行内联逻辑
+       handleAction(rawData, step, handlers, frameTime);
+       break;
      case 'observation':
-       // ... 80 行内联逻辑
+       handleObservation(rawData, step, handlers, frameTime);
+       break;
      case 'paused':
      case 'resumed':
      case 'retrying':
-       // ... 15 行内联逻辑
+       handleMeta(rawData, step, handlers, frameTime);
+       break;
    }
```

### S22：错误终态幂等清理——request_level 判据闸门（来源：3.6.8）

断链时请求级错误（整个请求失败，不可能再有 final）应做幂等终态清理；执行级错误（blocked/timeout，任务仍继续）保持"等 final"。loading 已由 handleSend finally(:169) 兜底，本条针对 waitTimer / 聚合状态。

#### S22.1 useChatCallbacks.ts onError — request_level 判据闸门

```diff
// useChatCallbacks.ts onError 内 — from_backend 分道之后、暂停缓冲之前
  if (errorObj.from_backend === true) {
    // ... 现有逻辑
    return;
  }

+ // 小欧 2026-09-10 S22: request_level 幂等清理闸门
+ // 仅请求级错误(整个请求失败, 不可能再有 final)做终态清理
+ // 执行级错误(blocked/timeout, 任务仍继续)保持"等 final"不动
+ const isRequestLevel = errorObj.step === 0 || errorObj.error_type === 'request_timeout';
+ if (isRequestLevel) {
+   console.info('[onError] 请求级错误: 幂等清理 waitTimer + 聚合状态');
+   if (waitTimerRef.current) { clearInterval(waitTimerRef.current); waitTimerRef.current = null; }
+   onStepFingerprintRef.current.clear();
+ }

  // ⭐ 暂停时存入缓冲区
  if (isPausedRef.current) {
```

**彻底性**：请求级错误立即清终态，执行级错误等 final，两路语义分明；幂等保证——即便随后 final 到达，onComplete 再清空一次无副作用。

## 六、TDD 级验证方案（每条 S 对应验收用例）

> 真实后端 + 真实 LLM + 真实工具 + 真实 SQLite（`~/.omniagent/chat_history.db`），禁 Mock。每条 S 实施后跑对应 T 用例全绿再进下一条。

### 6.1 S1 清死代码——零行为回归

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S1.1 | 启动前端 `npm run dev`，发一条消息 | `streamingStepsRef` / `reconnectConfigRef.enabled` / `_isProcessingRef` / useChatState `streamingStepsRef` 已删除，编译无报错 | Q11 |

### 6.2 S5 onResumed 复位时序——根治死循环

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S5.1 | 发消息 → HITL 暂停 → 服务端返回 error 帧 → 恢复 | console 无"推回 buffer"日志；恢复后步骤正常推进，无死循环 | Q4 |

### 6.3 S6 cancelInProgress 只放行取消帧——根治双 final

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S6.1 | 发消息 → 运行中点取消 → 等 final | 仅出现 `final(cancelled)` 一帧；无 `final(completed)` 双帧 | Q5 |
| T-S6.2 | 同 T-S6.1，检查 message.executionSteps | 消息气泡区 final 帧数 = 1 | Q5b |

### 6.4 S2 单一真源——executionSteps 五源收敛

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S2.1 | 发消息，F12 console 搜 `executionStepsRef` | 仅有 useSSE 的一个 ref；useChatState:259 已删除 | Q1/Q2 |
| T-S2.2 | 发消息触发 error（请求级），等终态 | onError 清的 ref 与 onComplete 读的 ref 为同一个——error 后 steps 残留数 = 0 | Q2 |

### 6.5 S3 seq 守卫——三层一次获救

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S3.1 | 首帧 seq=0（断点观察 first data） | 首条 action/thought 正常显示，不被拦截 | Q6 |
| T-S3.2 | 断连 5s → 恢复，观察重放帧 | console 搜 `seq守卫拦截` 有日志；executionSteps state 长度不翻倍 | Q6 |
| T-S3.3 | 同 T-S3.2，检查 sessionStorage | 刷新后恢复的步骤数 = 实际数，不翻倍 | Q6 |
| T-S3.4 | 同 T-S3.3，检查 message.executionSteps | 消息气泡区步骤数 = 实际数（双重保护：seq守卫 + 指纹） | Q6 |

### 6.6 S4 updater 纯化——StrictMode 安全

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S4.1 | 发消息，F12 Performance 录制 | updater 内无 `saveStepsToStorage` 调用（持久化由 S12 统一处理） | Q3 |
| T-S4.2 | React StrictMode 下（dev 模式）发消息 | steps 不翻倍；ref 写入次数 = event 数（无双重执行） | Q3 |

### 6.7 S7 缓冲统一 + S8 超时语义

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S7.1 | 发长消息，观察 streamingContent | SSE 流式过程只有一份累积缓冲；`responseBufferRef` 已删除 | Q7/Q9 |
| T-S8.1 | 发消息，F12 搜 `firstChunkTimeoutRef` | 变量名已改为 `firstChunkTimeoutRef`；超时 180s 仅覆盖首响应 | Q8/Q10 |

### 6.8 S9 abort flush——竞态窗口

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S9.1 | 发消息 → 快速点断开/取消 | disconnect 前 processBuffer 被 flush，无残留未处理行 | P4 |

### 6.9 S11 onStep 指纹清理

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S11.1 | 发消息 → final 到达 → 立即发第二条 | 第二条消息步骤不受第一条指纹影响，无遗漏渲染 | Q6 |

### 6.10 S13 live→终态交接——根治丢尾

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S13.1 | 长任务快结束（最后3步），观察 RightViewer | final 到达后仍显示最后 thought/action/observation 全部步骤 | Q13 |
| T-S13.2 | 同 T-S13.1，切到历史再切回 | 步骤不丢失，settledSteps 快照兜底生效 | Q13 |
| T-S13.3 | 同 T-S13.1，F12 console 搜 `REST 步骤数` | 无 "REST 步骤数 < settledSteps" 警告（等长校验通过） | Q13 |

### 6.11 S12 阻塞根除——主线程卫生

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S12.1 | 长答案流式输出，F12 Performance 记录主线程长任务 | 300-500ms 内无 >100ms 长任务；打字机无可见停顿 | Q12 |
| T-S12.2 | 同 T-S12.1，观察 sessionStorage 写入频率 | 写入间隔 ≥5s（非 300ms） | Q12 |
| T-S12.3 | 同 T-S12.2，检查 useChatPersistence.saveState | 保存间隔 ≥5s（与 steps 防抖同频） | Q12 |

### 6.12 S14 useStateWithRef——手工双写消除

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S14.1 | 发消息，F12 搜 `useEffect.*isReceivingRef\|useEffect.*isConnectedRef` | 无 useEffect 手动同步日志；isReceiving/isConnected 由 useStateWithRef 管理 | 3.5.1.1 |

### 6.13 S15 HITL Map 计数

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S15.1 | 并发 HITL：快速发两条消息触发两个 paused | 两个 confirmId 独立管理；一个 resume 后另一个仍在等待 | 3.5.1.4 |

### 6.14 S16 appendStep 函数——归位验证

| 用例 | 场景 | 验证点 | 对应 Q |
|------|------|--------|--------|
| T-S16.1 | 发消息，F12 console 搜 `appendStep` | 6 个分支（thought/chunk/final/action/observation/paused）全部调用 appendStep | 3.5.2.2 |

### 6.15 S20 连接层函数提取——回归验证

| 用例 | 场景 | 验证点 | 对应 3.6 |
|------|------|--------|---------|
| T-S20.1 | 发消息 → 等 60s 无数据 → 触发 idle 超时 | `startIdleMonitor` 被调用；idle 超时后自动重连 | 3.6.1 |
| T-S20.2 | 发消息 → 手动断开 | `clearAllTimers` 被调用；无残留 idle/fetch/reconnect/saveSteps timer | 3.6.1 |

### 6.16 S21 解析层 type handler 提取——回归验证

| 用例 | 场景 | 验证点 | 对应 3.6 |
|------|------|--------|---------|
| T-S21.1 | 发消息，观察 SSE 事件 | 每个 type（thought/chunk/final/error/action/observation/paused/resumed/retrying）正确分派到独立 handler | 3.6.2 |
| T-S21.2 | 同 T-S21.1，检查 steps 输出 | 每个 type 的步骤构造与原逻辑一致（无遗漏字段、无多余字段） | 3.6.2 |

### 6.17 S22 错误终态幂等清理——request_level 闸门

| 用例 | 场景 | 验证点 | 对应 3.6 |
|------|------|--------|---------|
| T-S22.1 | 发消息 → 服务端返回请求级错误（step=0） | console 搜 `请求级错误: 幂等清理` 有日志；waitTimer 被清空；loading = false | 3.6.8 |
| T-S22.2 | 发消息 → 服务端返回执行级错误（blocked/timeout） | console 无 `请求级错误` 日志；waitTimer 不变；等 final 到达 | 3.6.8 |

### 6.18 S10 + S17 + S18 + S19 辅助验证

| 用例 | 场景 | 验证点 | 对应 S |
|------|------|--------|--------|
| T-S10.1 | 发消息，检查 RightViewer 不显示 preview action 空壳 | preview action 不进 state（S10 guard 生效） | S10 |
| T-S17.1 | 发消息，F12 搜 `isConnectedRef\|isHittingRef` | 由 useStateWithRef 管理；无 useEffect 手动同步 | S17 |
| T-S18.1 | 编译 TypeScript：`npx tsc --noEmit` | handlers 类型无 any；`_isProcessingRef` 已从类型中删除 | S18 |
| T-S19.1 | 发消息 → 完成 → F12 搜 `恢复.*步骤.*source` | sessionStorage 写入带 `{steps, source, timestamp}` 外壳；恢复时打印 source=live | S19 |

## 七、实施顺序与依赖链（TDD 驱动）

> **铁律**：每步实施完跑第六章对应 T 用例全绿再进下一步，严禁批量跳步。

### 7.1 实施顺序

| 顺序 | S | 改动摘要 | 修改文件 | 风险 | 验证用例 | 依赖 |
|------|---|---------|---------|------|---------|------|
| 1 | S1 | 清死代码：streamingStepsRef / reconnectConfigRef.enabled / _isProcessingRef / useChatState streamingStepsRef | useSSE.ts, sseParser.ts, useChatState.ts | 低 | T-S1.1 | 无 |
| 2 | S5 | onResumed 复位提前到回放循环前 | useChatCallbacks.ts:738-747 | 低 | T-S5.1 | 无 |
| 3 | S6 | cancelInProgress 只放行 cancelled 帧 | useChatCallbacks.ts:184-190 | 低 | T-S6.1~T-S6.2 | 无 |
| 4 | S2 | executionSteps 单一真源：删 useChatState ref + props 注入 + 删除 syncServerTaskId | useChatState.ts, useChatCallbacks.ts, useChatStreaming.ts | 中 | T-S2.1~T-S2.2 | S1（删死代码后干净） |
| 5 | S3 | seq 守卫上移 sseParser 入口：handlers 新增 lastSeqRef + onSeq 后 switch 前守卫 | sseParser.ts, useSSE.ts | 中 | T-S3.1~T-S3.4 | S2（单一真源后守卫拦截有效） |
| 6 | S4 | updater 纯化：提取 appendStep + 副作用移出 updater | sseParser.ts | 中 | T-S4.1~T-S4.2 | S3（守卫在上游，updater 安全） |
| 7 | S7 | 缓冲统一：删 responseBufferRef + onComplete 不比长 | sseParser.ts, useChatCallbacks.ts | 低 | T-S7.1 | S4（updater 纯化后缓冲归一） |
| 8 | S8 | 超时语义：fetchTimeoutRef → firstChunkTimeoutRef | useSSE.ts | 低 | T-S8.1 | 无 |
| 9 | S9 | abort flush：disconnect 前 flush processBuffer | useSSE.ts:651 | 低 | T-S9.1 | 无 |
| 10 | S11 | onComplete 清指纹 Set | useChatCallbacks.ts:444-459 | 低 | T-S11.1 | 无 |
| 11 | S13 | settledSteps 快照 + REST 等长校验 | RightViewer.tsx | 中 | T-S13.1~T-S13.3 | S2（单一真源后 liveSteps 权威） |
| 12 | S12 | 阻塞根除：pendingSteps + scheduleFlush + 5s 防抖 | useSSE.ts, sseParser.ts, useChatPersistence.ts | 中 | T-S12.1~T-S12.3 | S4（appendStep 提供 flush 路径） |
| 13 | S14 | useStateWithRef：替换 isReceiving / serverTaskId / executionSteps 双写 | useStateWithRef.ts(新增), useSSE.ts | 低 | T-S14.1 | S2（单一真源后双写收敛） |
| 14 | S15 | HITL Map 计数：isHitlWaitingRef → hitlWaitingKeysRef(Set) | useSSE.ts:446-454 | 低 | T-S15.1 | 无 |
| 15 | S16 | appendStep 函数定义归位 | sseParser.ts:130-140 | 低 | T-S16.1 | S4（已在 S4 中实施） |
| 16 | S17 | isConnected / isHitting 双写替换 | useSSE.ts:405-412, :446 | 低 | T-S17.1 | S14（同模式封装） |
| 17 | S18 | handlers 完整类型约束 | sseParser.ts:68-120 | 低 | T-S18.1 | S3+S12（字段齐全后约束） |
| 18 | S19 | sessionStorage 元数据外壳 | useSSE.ts:485-500, :470-472 | 低 | T-S19.1 | S2（单一真源后外壳写入） |
| 19 | S20 | 连接层函数提取：startIdleMonitor / clearIdleMonitor / clearAllTimers | useSSE.ts | 低 | T-S20.1~T-S20.2 | S1（死代码清完后提取） |
| 20 | S21 | 解析层 type handler 提取：handleThought / handleChunk / handleFinal / handleAction / handleObservation / handleMeta + switch 分派 | sseParser.ts | 中 | T-S21.1~T-S21.2 | S12（pendingSteps 模式确定后提取） |
| 21 | S22 | 错误终态幂等清理：request_level 闸门 | useChatCallbacks.ts onError | 低 | T-S22.1~T-S22.2 | S5（onResumed 时序修正后错误链路干净） |

### 7.2 依赖链图

```
S1(清死代码) ─→ S2(单一真源) ─→ S3(seq守卫) ─→ S4(updater纯化) ─→ S7(缓冲统一)
                  │                                    │
                  ↓                                    ↓
              S14( useStateWithRef )            S12(阻塞根除)
                  │                                    │
                  ↓                                    ↓
              S17(双写替换)                      S21(type handler提取)
                  
S5(时序修正) ─→ S22(错误幂等)
S6(取消帧) ──→ 独立
S8/S9/S10/S11/S15/S16/S18/S19/S20 ─→ 各自独立或低依赖
```

### 7.3 风险矩阵

| S | 风险等级 | 风险描述 | 缓解措施 | 回滚方案 |
|---|---------|---------|---------|---------|
| S2 | 中 | executionSteps 五源收敛涉及 3 个文件 5 处引用点 | 先加 props 注入再删 ref，逐调用点替换 | 恢复 useChatState executionStepsRef |
| S3 | 中 | seq 守卫误拦首帧或合法重传 | next-seq 语义 + T-S3.1~T-S3.4 全覆盖 | 注释掉守卫 return，恢复无条件追加 |
| S4 | 中 | updater 纯化后副作用时序变化 | StrictMode 双重执行测试 + T-S4.1~T-S4.2 | 恢复 updater 内联副作用 |
| S12 | 中 | 批量 commit 改变 steps 更新频率 | F12 Performance 量化 + T-S12.1 | 恢复每帧 setExecutionSteps |
| S13 | 中 | settledSteps 快照时机与 REST 等长校验 | T-S13.1~T-S13.3 三场景覆盖 | 恢复 300ms 延迟 GET |
| S21 | 中 | type handler 提取遗漏字段或改变时序 | 每个 type 逐一对照原代码 + T-S21.1~T-S21.2 | 恢复 switch 内联 |
| 其余 | 低 | 纯重构或单点修改 | 对应回归用例 | 直接 revert |

**签名**: 小欧
**更新时间**: 2026-09-10 12:35:00