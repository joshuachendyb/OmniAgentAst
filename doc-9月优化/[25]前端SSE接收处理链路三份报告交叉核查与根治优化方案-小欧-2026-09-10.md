# 前端SSE接收处理链路：三份报告交叉核查与根治优化方案

**创建时间**: 2026-09-10 09:19:31（PowerShell Get-Date 获取）
**更新时间**: 2026-09-10 11:37:12
**编写人**: 小欧

### 版本历史

| 版本 | 更新时间 | 更新内容 | 作者 |
|------|---------|---------|------|
| v1.0 | 2026-09-10 09:19:31 | 首版：三份报告交叉核查 + 后端心跳化解P4判定 + seq守卫初始值死角 + 根治优化方案 | 小欧 |
| v1.1 | 2026-09-10 09:27:52 | 北京老陈质疑后修订：Q6 升级为"⭐去重防线只拦一半"（最核心问题），补 sseParser 5 分支行号证据与三层累积后果；S3 补"守卫拦截四路短路"纵深说明；TDD 加 T8/T9；2.2 速览加第6条 | 小欧 |
| v1.3 | 2026-09-10 09:36:04 | 北京老陈再次质疑后深度核查（从症状回溯链路）补强：Q6 钉死"用户可见重复主源=executionSteps state 渲染区（Pipeline/RightViewer），气泡有指纹反而不重复；preview+canonical 双发进 state"；Q5 补"cancel 双 final 也进消息气泡(:266)"；新增 Q5b（指纹/取消守卫对 final 帧无区分，final 计数不定）；排除 3 条伪命题：final_stats 独立 case 无双 onComplete、request_level loading 悬挂被 handleSend finally(:169) 化解、P4 idle 被心跳化解 | 小欧 |
| v1.4 | 2026-09-10 09:47:04 | 北京老陈提供三症状实测（重复输出/短时冻结/丢最后几步）后架构级定性：新增 Q12（高频主线程阻塞：O(n)复制+双路全量stringify+同步setItem）、Q13（结束瞬时 live→REST 双源切换丢尾）；新增 S12/S13 架构根治；TDD 加 T10/T11；实施顺序升 S13/S12 优先级，S3≥S13≥S12 面向三症状 | 小欧 |
| v1.5 | 2026-09-10 09:50:51 | 按北京老陈要求新增 3.5 节：三维问题清单（架构 4 条 / 技术实现 4 条 / 技术选择 2 条），不用表格、逐条说明"是什么+危害"，并给出"架构维是根、实现维是放大、选择维仅一处轻误用"的定性结论 | 小欧 |
| v1.6 | 2026-09-10 09:55:00 | 按北京老陈要求重排 3.5 节编号：废弃"一/二/三"与"第一~第十"式编号，改为规范章节号——3 级标题 3.5、4 级标题 3.5.1/3.5.2/3.5.3、5 级标题 3.5.x.y 逐条锚定问题，并将原"结论"独立为 4 级标题 3.5.4；观点与行号证据不变 | 小欧 |
| v1.7 | 2026-09-10 10:06:28 | 按北京老陈要求，把文档21 的 8 个问题点逐一对照 3.5 检查后补齐：3.5.1.1 补"state/ref 手工双写普遍性+清错对象"病根、3.5.1.2 补"解析层三责合一（解析/状态变更/持久化）"病根、3.5.2.1 补"chunk 三处累积"叠加因素、3.5.2.3 补"错误处理三层链路"病根；新增 3.5.2.5（模块与接口职责失衡：useSSE 六大职责/handlers 20+ 字段直透）；sessionStorage 与三症状无关的"类型宽泛"不采纳不予加入；3.5.4 同步改为十一点/~3.5.2 五条 | 小欧 |



## 一、严重UI实时渲染的问题及文档信息
前端3个严重的问题 都是 非定时必定出现的问题,都实时显示AI消息 过程中的 
1 是重复输出 
2 输出中短时间冻结被堵
3 丢失steploog最后几步的显示

| 项 | 内容 |
|----|------|
| 文档类型 | 编辑型（分析 + 优化方案） |
| 应用范围 | `frontend/src/hooks/useSSE.ts`、`frontend/src/features/chat/services/sseParser.ts`、`frontend/src/features/chat/hooks/useChatCallbacks.ts`、`useChatState.ts`、`useChatStreaming.ts`、`useChatFacade.ts` |
| 后端契约 | `backend/app/services/task/task_state.py`、`backend/app/services/chat/stream_orchestrator.py`、`backend/app/services/agent/step_emitter.py`、`backend/app/utils/sse_formatter.py` |
| 上游依据 | `[21]前端SSE处理架构深度分析报告-小李-2026-09-10.md`、`[22]前端SSE接收处理链路核查与优化实施说明-小草-2026-09-10.md`、`[23]前端SSE处理架构分析与优化方案-小沈-2026-09-10.md` |
| 核查强度 | 三遍核查（①完整读文件 ②逐条验证真伪+交叉验证 ③方案彻底性+死角排查） |

## 二、核查方法与证据采集

### 2.1 核查方法

1. **第1遍 - 完整读取**：前端6个关键文件逐行读完（useSSE.ts 1086行 / sseParser.ts 938行 / useChatCallbacks.ts / useChatState.ts / useChatStreaming.ts / useChatFacade.ts），后端4个契约文件（task_state.py / stream_orchestrator.py / step_emitter.py / sse_formatter.py）。
2. **第2遍 - 逐条验证**：对三份报告每个声明点，在本地代码中查证行号证据，判定"真实/夸大/理由不实/被化解"。
3. **第3遍 - 方案彻底性**：对每条方案验证是否"解决根因、无副作用、不引入新抽象"，并排查方案自身死角（如 seq 守卫首帧问题）。

### 2.2 核心结论速览（北京老陈可在 5 分钟内读完本段）

| # | 结论 | 性质 |
|---|------|------|
| 1 | 小沈[23]问题清单质量最高：P1/P2/P5 全部真实，P3 洞察对但理由偏，P4 有夸大 | 采纳其问题主体，剔除浮夸 |
| 2 | 小草[22]实施方式稳妥：O1 保留指纹兜底、O2 幂等清理方向正确，但漏了 P1/P5 两个致命时序链 | 采纳其稳妥实施路线 |
| 3 | 小李[21]架构观察准（单一真源缺失），但方案全错位：策略模式/EventEmitter/中间件是过度设计（违反 YAGNI） | 只取"问题陈述"，方案弃用 |
| 4 | **重大修正：小沈 P4"空闲超时假中断"已被后端 25s 心跳化解**（HEARTBEAT_INTERVAL=25s，`stream_reader` 发 `: ping`，前端 reader 循环每次 read() 返回重置 idle 计时器） | 未发现遗留风险 |
| 5 | **独家死角：seq 守卫方案若按小沈/小草写法上移，首帧 seq=0 会被误拦（小草式）或漏拦（小沈式）** | 详见第六章 |
| 6 | **最核心问题 Q6：去重防线只拦一半**——sseParser 解析层 5 分支对 executionSteps state/ref/sessionStorage 三处无条件追加（零防御），指纹只在 message 消息区拦截 → 重放时 UI 步骤区/刷新恢复源/onComplete 读源三处成倍累积 | 详见第四章 Q6 + 第五章 S3 |
| 7 | **北京老陈三症状实证：重复输出 / 短时冻结 / 丢最后几步 = 同一架构病根**——①重复：解析层无 seq 守卫无条件追加+preview/canonical 双发；②冻结：高频全量 O(n) 数组复制 + 300ms/500ms 双路全量 JSON.stringify+同步 setItem 阻塞主线程；③丢最后几步：final 到达 isCurrentLive 翻转，用 300ms 后 REST（DB 未落全）顶替 liveSteps 全量，切换源不同步丢尾 | 详见第七章 Q6/Q12/Q13，架构方案第六章 |

## 三、三份上游报告交叉验证结论

### 3.1 小沈[23]声明逐条判定

| 声明 | 判定 | 行号证据 |
|------|------|---------|
| P1：onResumed error 死循环 | **真实** | useChatCallbacks.ts:739-741 回放 error 时 isPausedRef(:747) 才复位 → onError:600-603 命中暂停重推 buffer → 死循环 |
| P2：seq 权威去重未用 | **真实** | 后端 task_state.py:44-54 每事件带 seq（len 单调递增+持锁）；sseParser.ts:158-160 已提取 seq；但前端仅 usage 分支(:267-271)用 seq 去重，业务帧全依赖指纹 |
| P3：双 executionStepsRef 同名碰撞 | **洞察对，理由偏** | useSSE.ts:384 与 useChatState.ts:259 同名不同物实锤；但"ref 渲染到 UI"非主因，主因是 onError(:578 清 useChatState 的) 与 onComplete(:517 读 useSSE 的) 操作不同 ref |
| P4：断线/重连竞态 | **夸大** | "链路被自己短路"不成立：sendMessageInternal 调 disconnect 后仍建新 fetch(useSSE.ts:692)继续跑；真实残留仅"disconnect abort 窗口期内旧流数据丢弃"竞态；且 idle 假中断被后端心跳化解（见3.4） |
| P5：cancelInProgress 双 final | **真实** | useChatCallbacks.ts:184-190 `!isCancelEvent && step.type !== 'final'` → final(completed) 被放行 → 双击取消出现双完成帧 |
| P6：final 处理"取较长者" | **真实，方案不彻底** | useChatCallbacks.ts:454-459 长度比较取源；重连重放时混入旧源（详见第四章 Q9） |
| P7：updater 内副作用 | **真实** | sseParser.ts:380-390/429-439/502-511/678-688/829-839/869-879，textDecoder 每分支内写 ref+持久化 |

### 3.2 小草[22]声明逐条判定

| 声明 | 判定 | 说明 |
|------|------|------|
| O1：指纹去重 | **真实** | useChatCallbacks.ts:151-171 onStepFingerprintRef 内容级去重 |
| O2：错误态幂等清理 | **真实但不完整** | 清理方向对，但应区分 request_level（整个请求失败，断链无 final）与 stream_level（错误帧后有 final），小草未区分 |
| O3：useChatFacade 依赖复杂 | **真实** | useMemo 50+ 依赖互文，50+依赖(:252-418) |
| O4：fetchTimeout 只兜首响应 | **真实** | useSSE.ts:682 设 180s，:717-720 fetch 返回立即 clear → 全程超时语义不存在 |
| 遗漏 P1/P5 | **致命遗漏** | onResumed 死循环、cancel 双 final 两个真实链路缺陷未被覆盖 |

### 3.3 小李[21]声明判定

| 声明 | 判定 | 说明 |
|------|------|------|
| 架构观察：单一真源缺失 | **真实** | useSSE state/ref + useChatState ref + message.executionSteps + sessionStorage + REST historySteps 五份状态（详见第四章 Q1） |
| 方案：策略模式/EventEmitter/中间件 | **过度设计（YAGNI）** | 为 2 个入口、1 种去重场景引入抽象注册表；违反 AGENTS.md KISS-DIRECT |

### 3.4 重大修正：P4"空闲超时假中断"已被后端心跳化解

**证据链**：
1. 后端 `constants.py:127` `HEARTBEAT_INTERVAL = 25.0`（常量注释§6 明确"与前端 IDLE_TIMEOUT=60s 错开"）。
2. 后端 `stream_reader`（stream_orchestrator.py:460-470）：`cond.wait()` 每 25s 超时发 `: ping\n` 保活。
3. 前端 `useSSE.ts:767`：`lastDataTimeRef.current = Date.now()` 在 reader 循环顶部每次执行；`:740-742` 每次 read() 返回前先清除旧 idle 计时器。
4. 前端 `sseParser.ts:112-115`：非 `data: ` 前缀行直接 return 零副作用 —— `: ping` 不触碰业务数据，但 reader.read() 已返回、循环重设 idle。

**结论**：活连接下每 25s 一次 read() 返回，idle 计时器（60s）永远无法到期，假中断不发。P4 的真实残留仅剩：真断连检测时点（正确兜底）与 abort 窗口期（第八章 S10）。

### 3.5 问题三维清单：架构 / 技术选择 / 技术实现

> 按北京老陈的要求：不用表格罗列；问题点改用规范的章节号（3 级标题 3.5、4 级标题 3.5.x、5 级标题 3.5.x.y）来锚定，"是什么"与"危害是什么"以段落方式紧随各章节号之下。三个维度虽并列，但本核查的结论是：**危害最大的三条（重复输出、短时冻结、丢最后几步）全部源于"架构——状态与数据源设计"这一维，技术实现维的缺陷是放大与叠加因素，技术选择维整体正确、仅有一处轻度误用**。

#### 3.5.1 架构维度（状态与数据源设计）——用户三症状的根

##### 3.5.1.1 executionSteps 五份状态无单一真源

executionSteps 同时存在五份互相独立、读写不齐的状态。分别是 useSSE 的 React state（useSSE.ts:383）、useSSE 的伴生 ref（:384）、useChatState 的同名 ref（:259）、消息消息体里的 executionSteps（useChatCallbacks.ts:266）以及 sessionStorage 持久化副本（useSSE.ts:485-500）。它们的写入方与读取方各不相同：解析层写 state 与 useSSE ref，错误清理写 useChatState ref，完成回调读 useSSE ref，消息区另走一套追加。

**病根**：五源不是偶然堆积，而是全链路普遍采用"state 与 ref 手工双写"编码模式累积出的结构病——serverTaskId 靠 serverTaskIdRef + syncServerTaskId 手动同步（useSSE.ts:408-410），isReceiving 同理，没有任何封装保证同步完备。对普通字段而言，同步遗漏只是一次性 bug；对 executionSteps 这类"高频变更且带清空语义"的对象，后果被放大成病态：**清的对象与读的对象错位**——onError 清的是 useChatState 的 ref（useChatCallbacks.ts:578），onComplete 读的是 useSSE 的 ref（:370/:392），两者同名不同物，清空永远清不掉正在读的那一份；同时 sseParser 六个分支每帧同时写 state、useSSE ref（:382/:431/:680/:831/:871）与 sessionStorage 三处，正常无重放时三处尚强一致，一旦重连重放或清空错位立即背离。

**危害**：同一份业务步骤在五处漂移，任何一处不同步，用户就会在同一界面上看到同一批步骤有的地方重复、有的地方丢失；更严重的是清空与读取操作对象不一致时（清的是甲 ref、读的是乙 ref），清理永远不生效，步骤残留在界面上的现象反复出现。

##### 3.5.1.2 SSE 事件流无单调序防线，每帧无条件整数组追加

后端在发布事件时已为每一帧分配了唯一且单调递增的 seq（task_state.py:51，持锁原子），但前端解析层只把这把现成的标尺用在了 usage 分之上（sseParser.ts:267-271），其余业务帧一律不做顺序校验，直接 `[...prev, step]` 追加进 executionSteps state、ref 和 sessionStorage 三处。指纹去重只保护消息气泡区（useChatCallbacks.ts:161-171），左流水线/右查看区读的 state 毫无防线，而 action 的 preview 与 canonical 双发（sseParser.ts:663-666）都会进入 state。

**病根**：写入方 processSSEData 单函数同时承担**解析、状态变更、持久化**三重职责（sseParser.ts:68-936，约 900 行处理 17 种 type），每个事件分支都内联"构造步骤 + setExecutionSteps + 写 ref + saveStepsToStorage"（如 :380-390/:678-688/:829-839），解析层与状态层之间没有任何边界。这使"统一的顺序守卫"根本无处安放——要拦重放必须在每个分支各写一遍守卫，而现状是一处都没写；也使 preview+canonical 双发（:663-666）这类由解析层自己制造的重复源被原样灌入 state，解析层对自身产出的重复毫无感知。

**危害**：断线重连经 after_seq 续传时，序号低于当前进度的重放帧会被三次累积，用户面前重复输出；正常任务里单步骤也被 preview+canonical 双条化，重复感被放大。

##### 3.5.1.3 实时显示源与终态数据源两套数据流，结束时刻靠"猜"交接

执行过程中右侧查看区读的是实时 state（liveSteps，RightViewer.tsx:303），final 一到便切换为读 REST /getTaskSteps 的历史接口（:307）；而历史接口依赖后端把步骤落库成功，落库存在时延（:273-278 的注释明确承认"SSE final 先发、DB 写入稍后"）。

**危害**：结束瞬间如果数据库还没写全最新几步，REST 结果就以"缺尾巴的完整历史"顶替了实时全量，最后几步从界面上永久消失；300ms 缓冲只是拍脑袋的猜测，不是同步点，丢尾窗口在任何一次网络偏慢时都会重新打开。

##### 3.5.1.4 生命周期终态由多个布尔与标记拼接，无唯一判定

是否结束要看 isCurrentLive、_hasFinal、hasBusinessSteps、receiving、activeTaskId 与 serverTaskId 的匹配等多个分散标记的组合（useTaskInfo.ts:125-149、RightViewer.tsx:303），判断彼此存在竞态（例如 liveSteps 已含 final 时先判结束再切历史，与 REST 尚未返回的空窗重叠）。

**危害**：结束与切换的时序每次不完全一样，症状忽隐忽现、非定时必现，正是"修了无数次仍不彻底"的典型特征——每次碰的地方对了、但切换的源始终是猜测的。

#### 3.5.2 技术实现维度（并发与主线程卫生）——放大与叠加因素

##### 3.5.2.1 高频热路径叠加 O(n) 数组复制与双路全量 JSON 序列化

每一帧事件都执行 `[...prev, step]` 的整数组拷贝（sseParser.ts:380/429/678/829 等）并触发 setState，触发 Pipeline 全量重渲染；同时 saveStepsToStorage 每 300ms 防抖内做一次同步的 `JSON.stringify(整个步骤数组)` 加 `sessionStorage.setItem`（useSSE.ts:493），useChatPersistence 又每状态变化或 500ms 对整份 messages（含每条消息的 executionSteps）再做一次全量 JSON.stringify 与写入（useChatPersistence.ts:192-220）。sessionStorage.setItem 是同步阻塞 API。

**危害**：数组上千条时，两路大数组序列化加同步写周期性锁死主线程数百毫秒，打字机输出出现可感知的冻结与"被堵"停顿，且步骤越多、任务越长，冻结越频繁。

叠加因素之二：chunk 正文的累积点散落三处——sseParser.ts:401 的 responseBufferRef、useChatCallbacks.ts:313 的 streamingContentRef、:451 的 finalContent 兜底，同一份流式正文被三个地方各累积一遍；而 onChunk 每块到达都 setMessages 全量重建消息区（useChatCallbacks.ts:313-327），与双路序列化叠加，进一步抬高打字机渲染期的主线程占用。

##### 3.5.2.2 React updater 回调内部执行副作用

sseParser 的多个分支在 `setExecutionSteps(prev => {...})` 的 updater 函数体内写 executionStepsRef 与 saveStepsToStorage（sseParser.ts:380-390/429-439/502-511/678-688/829-839/869-879）。updater 应当是纯函数，React 在 StrictMode 下会将其执行两遍以暴露此类违规。

**危害**：updater 不是纯函数使副作用被执行次数不可控（StrictMode 双调用时 ref 写两次、持久化被触发两次），并使"更新 state 的同时依赖读取 ref"这种时间耦合常态化，后续任何一次 React 升级或批处理策略调整都可能让表现漂移。

##### 3.5.2.3 复位逻辑依赖隐式时序，存在死循环与终态数量不定

`isPausedRef` 的复位被安排在 error 回放循环之后（useChatCallbacks.ts:739-747），而 onError 在暂停期间会把错误重新推回缓冲（:600-603），形成回放时错误永远处理不掉、被反复重推的死循环；cancel 进行中的判断用 `!isCancelEvent && step.type !== 'final'` 放行了取消后的 completed 终态（:184-190），且指纹与取消守卫对 final 帧无法区分，最终消息里 final 帧数量不定。

**病根**：错误处理链路横跨三层、各自独立行事——useSSE 的 handleSSEError（分类与重连判断，useSSE.ts:179/:882/:930）、useChatStreaming 的 sseOnError（denied/blocked/timeout 聚合，useChatStreaming.ts:233）、useChatCallbacks 的 onError（业务错误分道与终端提示），三者之间无统一的内存序约定，复位动作（isPausedRef、聚合状态）散落各层；链路上任何一层在回放循环内提前或延后复位，即触发上文死循环。链路每增一层，时序类缺陷的排查面就翻一倍。

**危害**：暂停恢复会卡死，取消后会出现双完成帧或完成后又插入旧步骤，界面上出现明显违背预期的行为。

##### 3.5.2.4 死代码与语义失真的配置，加深维护误判

包括只被清空而无写入点的 streamingStepsRef（useChatState.ts:261）、设置后从未被读取的 reconnectConfigRef.enabled（useSSE.ts:597-601）、从未使用第 3 参数的 processSSEData（sseParser.ts:120）、名为 180 秒 fetch 超时实际只在首响应 resolve 后就清除的 fetchTimeout（useSSE.ts:682/717-720）以及 50 多个依赖项互相纠缠的 useMemo（useChatFacade.ts:252-418）。

**危害**：死代码让"终态应该清哪个 ref"的排查无从下手，名字失真的超时配置让后续维护者对系统保活能力产生系统性误判，依赖纠缠让任何单点改动都波及大范围重渲染。

##### 3.5.2.5 模块与接口职责失衡：useSSE 六大职责、handlers 20+ 字段直透

useSSE.ts 承载连接建立/断开、重连退避、空闲超时、sessionStorage 备份/恢复、轮询观察、意图标记六大职责（约 1086 行），违反 SRP 单一职责；processSSEData 的 handlers 参数塞入 20+ 字段——setExecutionSteps、executionStepsRef、saveStepsToStorage、onChunk、onComplete、disconnect、syncServerTaskId 等（useSSE.ts:777-778/817-818/:794/:836），把"改状态的能力"与"中断连接的能力"原样交给解析层，违反 ISP 接口隔离。

**危害**：职责不分的直接后果是状态归属混乱——executionSteps 之所以漂移成五份，正因为连接、存储、解析、回调各自握有其读写权且无人统一；handlers 直透让解析层随意触碰 ref、持久化乃至 disconnect，3.5.1.1（多源漂移）与 3.5.2.2（updater 副作用）都是这层失守的下游。它不直接制造三症状，却是三症状持续存在、修了无数次仍反复的管理性根因。

#### 3.5.3 技术选择维度（整体正确，仅一处轻度误用）

##### 3.5.3.1 fetch + ReadableStream 手动解析 SSE 的整体选型正确

它绕开 EventSource 无法携带自定义头与无法显式退避重连的短板，配合后端 25 秒心跳保活、断线按 after_seq 续传、动态退避最大三次重连与轮询兜底，构成了完整的断线自愈体系，这些选择本核查未发现错误，予以维持。

##### 3.5.3.2 sessionStorage 作"刷新恢复 + 双路高频持久化"载体属轻误用

作为一次性、按会话隔离的浏览器同源存储，它适合低频、小体积的会话级快照；而此处既承担高频防抖全量数组写入，又承担每个 messages 快照的全量序列化，其同步阻塞写与容量限制与高频 SSE 流形同水火。

**危害**：它不成比例地放大了症状二的冻结，且在数据量大时会静默抛容量不足告警（useSSE.ts:495 的存在即证据）而丢失恢复数据。正确的归属应是低频兜底快照或迁移到异步存储。

#### 3.5.4 三维定性结论

> 本节的用途：把前面 3.5.1~3.5.3 的 11 个问题点汇总成 4 条行动结论，回答"先修谁、修到什么程度算根治、为什么以前修不好"。每条结论对应一个维度或一个核心判断，单独签名如下。

**结论①架构维（3.5.1，共 4 条）＝三症状的病根，此维不修，症状必复发。**
- 3.5.1.1 executionSteps 五源无单一真源 → 直接造成重复输出、丢步。
- 3.5.1.2 SSE 无单调序防线 → 直接造成重复输出（重放+preview/canonical 双发）。
- 3.5.1.3 实时/终态双源靠"猜"交接 → 直接造成丢最后几步。
- 3.5.1.4 生命周期无唯一终态判定 → 三症状忽隐忽现、非定时必现的原因。
- 对应关系：**症状①重复 ≈ 3.5.1.1+3.5.1.2；症状②冻结 ≈ 结构上由 3.5.2 放大、源头在 3.5.1；症状③丢尾 ≈ 3.5.1.3+3.5.1.1。**

**结论②实现维（3.5.2，共 5 条）＝放大因素与独立缺陷，须一并清除，但单修此维不治根。**
- 3.5.2.1 O(n) 复制＋双路全量序列化 → 放大症状②冻结（叠加 chunk 三处累积）。
- 3.5.2.2 updater 内副作用 → 放大重复与冻结（StrictMode 双调用）。
- 3.5.2.3 复位逻辑依赖隐式时序 → 独立缺陷（暂停死循环、cancel 双 final）。
- 3.5.2.4 死代码与语义失真配置 → 独立缺陷（清错 ref、保活误判）。
- 3.5.2.5 模块与接口职责失衡 → 管理性根因（三症状修不干净的原因之一）。

**结论③选择维（3.5.3，共 2 条）＝技术选型结论。**
- 3.5.3.1 fetch + ReadableStream 手动解析 SSE：选型正确，予以维持。
- 3.5.3.2 sessionStorage 作高频持久化载体：轻误用，修正为低频兜底快照或异步存储。

**结论④行动优先级（用于实施排期）。**
- 第 1 步：按 3.5.1.2 / 3.5.1.3 / 3.5.1.1 / 3.5.1.4 顺序根治架构维（先解三症状）。
- 第 2 步：按 3.5.2.1 / 3.5.2.3 / 3.5.2.2 / 3.5.2.4 / 3.5.2.5 清除实现维。
- 第 3 步：修正 3.5.3.2 sessionStorage 职责。
- 验收标准：任何一条新修补，先对号入座属于 3.5.1 / 3.5.2 / 3.5.3 的哪一档再动手；3.5.1 不落地，三症状不消失，即不算根治。

### 3.6 吸收的其他文档的优秀的改进建议

> 本节收录 3.5 问题清单之外的**改进方法与方向层面**的认可项（当前来源：文档21 + 文档22 + 文档23；其他上游文档的优秀建议持续补充进本节）。方向认可，落地一律遵循文档25 方案 S1~S13 的"不加抽象、不 backward"原则。

#### 3.6.1 连接层函数级提取（来源：21 §4.1 / §7.1）

将 useSSE.ts 中的 `pollSessionTaskStatus`、`calculateReconnectDelay`、idleTimeout 等连接管理逻辑**提取为独立函数**，降低 1086 行单文件的 SRP 发散面，方便 S2 收敛数据源、S12 改造热路径时单点注入。

#### 3.6.2 解析层按 type 提取处理函数（来源：21 §7.1）

`processSSEData` 的每个 type 分支抽成独立处理函数，配合 S3 的**入口级 seq 守卫**（守卫置于 switch 之前）实现"单点出口"——守卫拦截后四路同时短路；各 type 函数只负责"构造步骤 + 产出事件"，不再内联 `setExecutionSteps` / `saveStepsToStorage`（对应 3.5.1.2 病根的整改）。

#### 3.6.3 handlers 接口化约束（来源：21 §4.3 / §7.1）

用 TypeScript 接口约束 handlers，并按"数据读写"与"连接/生命周期控制"分组（setExecutionSteps/onChunk/onComplete 归数据；disconnect/syncServerTaskId 归连接），为 S2 收敛五源、S3 减面提供编译期护栏。

#### 3.6.4 暂停缓冲区设置大小上限（来源：21 §5.5）

`displayBufferRef` 增加容量上限（如 500 条），超限按 seq 顺序丢弃最老条目并告警，防止长时间暂停时缓冲无限增长；作为 S5 修复"回放死循环"之后的**防御性约束**，非根因修复。

#### 3.6.5 IDLE_TIMEOUT 可配置化（来源：21 §5.2）

空闲超时时长由常量改为可配置项，但**最小取值必须 ≥26s**，与后端 25s 心跳保持错开（≤25s 会与心跳同频假断，对应 3.4/6.2 红线）；当前 60s 值维持不变。

#### 3.6.6 chunk 累积收敛为单一缓冲（来源：21 §4.5）

"统一由 useChatCallbacks 管理 chunk 累积、解析层只传原始 chunk"的方向认可，**已并入方案 S7**：`responseBufferRef`（sseParser.ts:401）删除、`streamingContentRef`（useChatCallbacks.ts:313）为唯一累积缓冲、onComplete 不再"取较长者"。此处仅记录来源对应。

#### 3.6.7 改进方向分级管理机制（来源：21 §8.3）

短期（清理+函数级提取）/ 中期（收敛单一真源）/ 长期（沉淀公共库）三档分级管理后续迭代——文档25 的 S1~S13 即按此框架排期。

#### 3.6.8 错误终态幂等清理：request_level 判据闸门（来源：22 §五-2 O2）

"断链时错误终态缺失"是 3.5.2.3（复位逻辑依赖隐式时序）病根的下游表现，文档25 未为其单列独立 S 条款；此处收录文档22 的**方法层**建议——以 `request_level`（sseParser.ts:599 读原始 `rawData.step === 0`，字段定义 types/sse.ts:109）为幂等清理闸门：**仅对请求级错误**（整个请求失败，断链后不可能再有 final）做 waitTimer/loading/refs 的幂等终态清理；**执行级错误**（blocked/timeout，任务仍等 HITL 或继续运行）保持"等 final"原语义不动。落地要点：①先区分 request_level 与 stream_level 再实施；②loading 已由 handleSend finally(:169) 兜底，本闸门针对 waitTimer / 聚合状态的清理；③幂等保证——即便随后 final 到达，onComplete 再清空一次无副作用；④随 S5/S6 一并实施。

#### 3.6.9 seq 守卫与后端"同 seq 覆写"不冲突的契约确认（来源：22 §三）

文档22 独有取证，经本核查复核后采纳并作为 **S3 安全性的依据**补记于此：后端 `agent_runner.py` 5.8.6 注释确认"短条覆写 publish 原位（保 seq）"——覆写只发生在事件缓冲（StreamBuffer）内，`stream_reader` 流读取端对每个 seq 只读一次，前端按 seq 单调消费**不会**把"同 seq 覆写"读成两遍。故 S3 入口级 seq 守卫（3.5.1.2）与后端契约完全自洽，无冲突。此为事实确认而非新方案，仅为防止后续维护者在此处产生系统误判而记录来源对应。

#### 3.6.10 seq 守卫单点拦截方法（来源：23 §P2）

去重防线建在解析层**入口**，一处拦截四路（executionSteps state / executionStepsRef / saveStepsToStorage / onStep 回调），无 seq 帧（meta）不参与去重照常处理——该入口单点拦截方法并入 S3（与文档21 §O1、文档22 §O1 为同向共识）。

#### 3.6.11 appendStep 副作用归位方法（来源：23 §P7）

将各分支内联的 `setExecutionSteps(prev => { 写ref+保存; return newSteps; })` 收敛为单一 append 辅助函数、副作用移出 updater，final 分支保证 ref 先行供 onComplete 读全量——该方法并入 S4。

---

## 四、本地代码确凿问题清单（行号证据）

### Q1：executionSteps 五份状态无单一真源

| # | 状态源 | 位置 | 读写方 |
|---|--------|------|--------|
| ① | `executionSteps` state | useSSE.ts:383 | sseParser upstreams 写、UI 读 |
| ② | `executionStepsRef` | useSSE.ts:384 | sseParser 写、onComplete:517 读 |
| ③ | `executionStepsRef`（同名不同物） | useChatState.ts:259 | onError:578 清空、onStep:266 读 |
| ④ | `message.executionSteps` | useChatCallbacks.ts:266 追加 | 消息流 |
| ⑤ | sessionStorage（300ms 防抖） | useSSE.ts:485-500 | 刷新恢复 |

**后果**：同一业务步骤在 5 处漂移，任何一处不同步就出现重复/丢失。

### Q2：双 executionStepsRef 同名碰撞

- useSSE.ts:384 与 useChatState.ts:259 同名不同物。
- **onError 清的是 ③**（useChatCallbacks.ts:578 解构自 useChatState），**onComplete 读的是 ②**（useSSE.ts:517）→ 同一个"清空"操作对另一份 ref 无效 → 步骤残留。

### Q3：updater 内副作用（React StrictMode 会双重执行）

sseParser.ts 下列分支的 `setExecutionSteps((prev) => { ...副作用; return [...] })`：

| 分支 | 行号 | 副作用 |
|------|------|--------|
| thought | 380-390 | 写 ref + saveStepsToStorage |
| chunk | 429-439 | 写 ref + saveStepsToStorage |
| final | 502-511 | 写 ref + onComplete 同步读 |
| action | 678-688 | 写 ref + saveStepsToStorage |
| observation | 829-839 | 写 ref + saveStepsToStorage |
| error/paused | 869-879 | 写 ref + setPaused 通知 |

### Q4：onResumed error 死循环（真 bug）

- useChatCallbacks.ts:739-741：`errors.forEach(→onError)` 回放时，isPausedRef 仍为 true（:747 才复位）。
- useChatCallbacks.ts:600-603：onError 内 `if (isPausedRef.current) { push 回 buffer; return; }`。
- **链路**：onResumed 回放 error → onError 命中暂停 → error 推回 buffer → 死循环不退出。

### Q5：cancelInProgress 双 final（真 bug）

- useChatCallbacks.ts:184-190：`if (!isCancelEvent && step.type !== 'final')` 放行。
- 取消后服务端发 `final(completed)` → 判断条件为 `type !== 'final'` 之外的 final 也被放行 → 双完成帧/完成后又插入旧步骤。
- **影响面**：双重——①UI 步骤区（state 渲染区）②消息气泡（onStep :266 直接把 final append 进 message.executionSteps）。取消后最终消息里出现两帧 final，内容重复。

### Q5b：fingerprint 与取消守卫对 final 无区分

- useChatCallbacks.ts:174-175 `isCancelEvent = step.type==='final' && step.outcome==='cancelled'`；指纹(:161-171)对 final 帧无 preview 位区分，两帧 `final|completed` 若是同 content 还会被指纹拦一个 → **cancel 后 final 计数不定**（0/1/2 皆可能），UI 状态依赖 final 帧数。

### Q6：⭐ 去重防线只拦一半——解析层对重放零防御（最核心问题之一）

**现状链路**（sseParser.ts 5处分支全部无条件追加）：

| 分支 | 行号 | 无条件追加到 |
|------|------|-------------|
| thought | 380-390 | setExecutionSteps + executionStepsRef + saveStepsToStorage |
| chunk | 429-439 | 同上 |
| final | 502-511 | 同上（+ onComplete 同步读 ref） |
| action | 678-688 | 同上 |
| observation | 829-839 | 同上 |

**三层都在累积，只有一层被拦**：
- 解析层（sseParser:380-390 等）：`setExecutionSteps(prev => [...prev, step])` **无条件追加** executionSteps **state** + **ref** + **sessionStorage** —— 对重放/重复行零防御。
- 消费层（useChatCallbacks.ts:161-171 onStep 指纹）：**仅保护 message.executionSteps（消息区）**。
- **后果**：断线重连 GET 重放时，消息区被指纹挡住不重复显示，但 executionSteps（UI 步骤展示区）、executionStepsRef（onComplete 读源）、sessionStorage（刷新恢复源 :473-474）**三处仍成倍累积** → 刷新后步骤翻倍、onComplete 拿重复数据写回消息、356步残留类问题复发。

**⭐ 用户可见重复的直接来源（本核查独有补强）**：
- 用户看到的"步骤重复显示"**主要来自 executionSteps state 渲染区**（PipelineRenderer / RightViewer / 查看区读的是 `executionSteps` state，useChatFacade:287/isReceiving 消费即 state），而消息气泡区（message.executionSteps）有指纹保护**反而不重复**。
- 意味着：**渲染管道被重复喂 data，消重只作用在气泡** → 气泡正常、流水线/查看区翻倍，用户第一眼看到的就是左侧流水线重复（与 2026-09-09 pipeline chunk step 重复显示修复文档同症状）。
- action preview+canonical 双发（sseParser:663 注释"preview/canonical 双发 list-back 时序"）都进 executionSteps state → 正常任务单步就占2条；重放再翻倍。

> 小沈[23] A1（"去重防线只拦一半"）正是此问题，本核查独立复核后确认属实，且影响面比报告所述更广：**消息区只是表象，UI 流水线/查看区才是重复主会场**。

**根因**：去重防线建在"消费层"，解析层无 seq 单调守卫。后端每帧唯一 seq（task_state.py:51）是现成的权威护栏，前端却只用于 usage 分支（sseParser:267-271），业务帧全靠内容指纹兜底。

### Q7：双缓冲分裂

- sseParser.ts:401 `responseBufferRef`（解析层无条件累积）
- useChatCallbacks.ts:313 `streamingContentRef`（回调层累积，暂停时 :302-309 不累积）
- 两缓冲互不感知，onComplete:451 混用 `finalResponse || streamingContentRef.current`。

### Q8：fetchTimeout 名不副实

- useSSE.ts:682 设置 180s，:717-720 在 `fetch()` resolve 后立即 clear → **只覆盖首响应等待，不覆盖流中途**。语义名不符实（应以"首响应超时"为名，靠 idle+心跳保活性）。

### Q9：onComplete"取较长者"补丁（重连重放脏）

- useChatCallbacks.ts:454-459 按长度比较 `finalResponse` vs `streamingContentRef.current` 选源 → 断线重放（after_seq 续传）时混入残留旧缓冲，导致内容漂移。

### Q10：双超时机制语义混乱

- idle 60s（useSSE.ts:444，已与心跳 25s 错开，正确）
- fetchTimeout 180s（名不副实，见 Q8）
- reconnect delay（useSSE.ts:992，动态退避）
- 三者职责边界在代码注释中互引，但实际各管一段，读者易误判。

### Q11：死代码残留（YAGNI 违反）

| 死代码 | 位置 | 说明 |
|--------|------|------|
| `streamingStepsRef` | useChatState.ts:261 | 只被清空(=[])、无写入点 |
| `reconnectConfigRef.enabled` | useSSE.ts:597-601 | 设置后从未读取，实际开关走 errorConfig.retryable(:217-218) |
| `processSSEData` 第3参 `_isProcessingRef` | sseParser.ts:120 | 形参未使用 |
| useMemo 50+ 依赖 | useChatFacade.ts:252-418 | 互文依赖，无 memo 价值 |

### Q12：⭐ 高频输出时主线程阻塞（用户症状2"输出中短时冻结被堵"）

**三重同步阻塞叠加**（高频输出期每 300-500ms 发生一次）：

1. **sseParser 每 chunk/step `[...prev, step]` O(n) 数组全量复制**（:380/:429/:678/:829 等）——数组上千条时每次复制耗 C 级，且 setExecutionSteps 触发 React 全量重渲染 Pipeline。
2. **saveStepsToStorage 300ms 防抖内同步 `JSON.stringify(上1000条)+sessionStorage.setItem`**（useSSE.ts:493）——setItem 是同步阻塞 API，大字符串一次写几十~几百 ms。
3. **useChatPersistence.saveState 每状态变化/500ms 又全量 `JSON.stringify` 整个 messages 数组**（:192-220，含每消息 executionSteps）——与第2路叠加。

**后果**：LLM 输出高频期间，主线程周期性被 2 路大数组序列化+同步写锁死 → 打字机停顿数秒 → UI 冻结被堵，用户看得见。

### Q13：⭐ 结束瞬时 live→history 切换丢最后几步（用户症状3"丢失steplog最后几步"）

**症结**（RightViewer.tsx:303-307 + :235-287）：
- 执行中 `displaySteps = liveSteps`（executionSteps state，实时全量）。
- **final 到达瞬时 `_hasFinal` 翻转 → `isCurrentLive` 变 false → `displaySteps` 切 `historySteps`**（由 :273-278 的 300ms 延迟后 GET /getTaskSteps 填充）。
- 但 :273-278 注释表明确认 **DB 落库窗口存在**（"SSE final 先发、DB 写入稍后"）——300ms 缓冲是拍脑袋猜的，网络慢/SSE 快时 DB 仍缺尾 → **REST 顶替 live 后最后几步永久消失**。
- 若此时 liveSteps 已含 final（useTaskInfo:129-130），判终切历史 → live 中被 seq/指纹拦掉或 updater 未提交的尾步在此一并丢失。

**架构根因**：**实时数据源(liveSteps state)与终态数据源(REST historySteps)双源切换无同步点**——结束不是"把 live 快照交给终态"，而是"猜测 DB 落库后覆盖 live"，必然有丢尾窗口。

## 五、根治优化方案（不留死角）

> 合并小沈[23]问题主体 + 小草[22]稳妥实施 + 本核查独有修正（seq 初始值等）。每条方案对应到根因，绝不加抽象、绝不 backward。

### S1：清理死代码（最先做，零风险）

- 删除 `streamingStepsRef`（Q11）、`reconnectConfigRef.enabled`、`_isProcessingRef` 形参、useChatFacade 无用 refs。
- 理由：先清障再动刀，避免修复被死代码干扰判定。

### S2：收敛 executionSteps 单一真源（根治 Q1）

**方案**：统一唯一可写源 = `useSSE.ts:383 executionSteps` state + 其伴生 `executionStepsRef(:384)`。

- useChatState.ts:259 的 `executionStepsRef` **删除**，useChatCallbacks/useChatStreaming 一律通过 props 注入 useSSE 的 ref。
- `message.executionSteps`（useChatCallbacks.ts:266）、sessionStorage（:485-500）、REST historySteps 全部降级为**只读快照**，不再作为回写真源。
- onError(:578)、onComplete(:517) 操作同一个 ref。

**彻底性**：五份状态收敛为一份权威 + 三份只读镜像，任何"清空/追加"只落在同一位置。

### S3：seq 权威守卫上移 sseParser 入口（根治 Q6 ⭐，三层一次获救）

**精确定义**（此为超越三份报告的关键修正）：

- `lastSeqRef` 语义改为"**期望的下一 seq**"（next-seq），初始值 `0`。
- **守卫必须放在 `onSeq()` 之后、switch 之前**：先由 sseParser.ts:158-160 用 `onSeq(rawData.seq)` 推进 `lastSeqRef = Math.max(lastSeqRef, rawData.seq+1)`，再用 `if (rawData.seq < lastSeqRef.current) return;` 拦截重放帧。
- 这样：首帧 seq=0 → 推进 next=1 → `0 < 1` 通过；重放帧 seq=0 → `0 < 1` 拦截。**seq=0 首帧与 seq=0 重放帧分离，互不误伤**。
- 重连时 lastSeqRef **不重置**（保持 next-seq），新请求才重置 `0`（useSSE.ts:678 改为此语义）；`after_seq=${lastSeqRef.current}`（:690）续传天然对齐。
- **守卫拦截的纵深**：一旦 seq 命中拦截，在 switch 前 `return`——**executionSteps state、executionStepsRef、saveStepsToStorage、onStep 四路同时短路**，Q6"三层累积"一次根治。
- 指纹去重（O1）**保留**作内容级兜底（覆盖极端：无 seq 的老式事件），与 seq 守卫双保险。

**彻底性**：重放秒杀、首帧不丢、续传对齐，单点出口。

### S4：updater 纯函数化（根治 Q3）

- 提取纯 updater `appendSteps(prev, steps)`：仅返回新数组，**零副作用**。
- 副作用（写 ref、saveStepsToStorage）移到 useSSE 层：`useEffect` 监听 executionSteps 变化，300ms 防抖持久化（替代现有 sseParser 内散点持久化）。
- final 分支特殊处理：改为先写 ref + 触发 onComplete 再 setState，保持"onComplete 读全量"语义。

**彻底性**：StrictMode 双重调用不再重复副作用；持久化收敛单点。

### S5：onResumed 先复位再回放（根治 Q4 死循环）

- useChatCallbacks.ts:747 `isPausedRef.current = false` **上移到**:739 回放循环之前。
- 回放 errors 时 onError(:600-603) 不再命中暂停 → 正常处理不再推回 buffer → 死循环消除。

**彻底性**：时序因果修正，非补 by-pass。

### S6：cancelInProgress 只放行取消帧（根治 Q5）

- useChatCallbacks.ts:184-190 改为 `if (!isCancelEvent) return;` —— 取消态只认 `final(cancelled)` 与 `error(cancelled)` 帧，`final(completed)` 一律不放行。

**彻底性**：取消后 completion 帧不再渲染，双 final 根除。

### S7：缓冲统一（根治 Q7）

- `responseBufferRef`（sseParser.ts:401）删除，解析层只产出事件，不攒缓冲。
- `streamingContentRef`（useChatCallbacks.ts:313）为唯一累计缓冲，暂停时也累积（每次 paused 帧插入句柄）。
- onComplete:451 只信 `finalResponse || streamingContentRef.current`（不再比长）。

**彻底性**：双缓冲归并，取长补丁消失。

### S8：超时语义钉死（根治 Q8/Q10）

- `fetchTimeout` 重命名为 `firstChunkTimeout`（180s 覆盖首数据）；流中途活性全部交给 idle(60s)+心跳(25s)。
- 三超时各自注释职责边界，杜绝互引误读。

### S9：abort 竞态窗口（根治 P4 残留）

- useSSE.ts:651 disconnect（abort）前，若 `processBuffer` 非空先 flush 再 abort；catch:862-875 静默短路前同样先 flush buffer。
- 窗口期内已收到的数据不丢。

### S12：主线程同步阻塞根除（根治 Q12 ⭐ 症状2）

**架构层面拆三路**：
1. **去 O(n) 复制**：executionSteps 追加从 `[...prev, step]` 改为**队列头防抖批量 commit**——sseParser 全部改为「只 append 到不可变增量队列 + 单一 onSteps 批量回调」，每帧不再 setExecutionSteps；改为每 tick（requestAnimationFrame 或 50ms 批）合并 append。
2. **去同步 setItem**：sessionStorage 持久化移出防抖热路径，改 `navigator.storage`/**独立 Web Worker** 序列化或低频(≥5s)兜底快照；高频只更内存。
3. **去双路全量 stringify**：useChatPersistence.saveState 合并到同一条防抖链，去掉 500ms 独立全量序列化。

**彻底性**：主线程在高频期只做"批追加 + 内存态"，零同步序列化，打字机不冻结。

### S13：live→终态单一真源平滑交接（根治 Q13 ⭐ 症状3）

**架构原则**：**终态快照不应来自 REST 猜测，应直接来自 live 流最后状态**。
- final 到达时：把 `executionStepsRef.current`（含 final 的完整 live 全量）**直接固化**为终态快照 `settledSteps`。
- `displaySteps` 判定改为：`isCurrentLive ? liveSteps : (settledSteps ?? historySteps)`——**结束瞬间先用 live 快照兜底**，REST 成功且更长时再升级替换。
- RightViewer:273-278 的 300ms 猜测缓冲取消，改 REST 结果与 settledSteps **等长校验**（短则弃），彻底消除 DB 落库窗口丢尾。
- useTaskInfo:129-130 判终切历史同样改为优先 settledSteps。

**彻底性**：结束即快照，REST 只作增量补齐，无窗口期可丢步，最后几步必显示。

## 六、关键死角专项（本核查独有发现）

### 6.1 seq 守卫首帧地狱（三份报告均未覆盖）

- 小沈[23]方案：`if (_seq && _seq <= lastConsumedSeqRef?.current) return;` —— `_seq` 为 `0` 时 falsy → **seq=0 重放帧永不拦截**（守卫对首帧退缩）。
- 小草[22]方案：若用 `seq <= lastSeq` 且 lastSeq 初始 0 → **首帧 seq=0 被误拦**，首条 thought/action 丢失。
- 本方案（S3）：next-seq 语义 + 守卫在 onSeq 之后，两帧分离，双杀。

**验证用例**：
| 帧 | lastSeq(next) | 守卫表达式 | 期望 |
|----|--------------|-----------|------|
| 首帧 seq=0 | 0→1 | `0 < 1` | 放行 ✓ |
| 正常 seq=1 | 1→2 | `1 < 2` | 放行 ✓ |
| 重放 seq=0 | 1 | `0 < 1` | 拦截 ✓ |
| 重连续传 seq=5.. | 1 | `5 < 1` 假 | 放行 ✓ |

### 6.2 心跳-idle 错开已被后端固化（P4 化解答）

- 后端 `HEARTBEAT_INTERVAL=25.0`（constants.py:127），与前端 `IDLE_TIMEOUT=60s`（useSSE.ts:444）错开 35s。
- 结论：活连接不可能空闲判死；改前端时**严禁**把 IDLE_TIMEOUT 下调到 ≤25s（会与心跳同频共振造成假断）。

## 七、TDD 验证方案

> 实施 S2-S6 后，逐条验证下列用例。真实后端 + 真实 LLM + 真实工具 + 真实 SQLite，禁 Mock。

| # | 场景 | 操作 | 预期 |
|---|------|------|------|
| T1 | 正常对话 | 发一条消息 | 步骤单份无重复、无丢；final 仅一次 |
| T2 | 中间断网 5s | 断网→恢复 | after_seq 续传，无重复无丢失，最终 final 一次 |
| T3 | 暂停→恢复含错误 | HITL 暂停，错误帧回放 | 无死循环、无 error 残留、状态正确恢复 |
| T4 | 消息中取消 | 运行中发送中取消 | 仅 final(cancelled)，无 final(completed) 双帧 |
| T5 | 首帧 seq=0 | 断点观察 first data | 首条 action/thought 正常显示 |
| T6 | 重放帧 seq=0 | 断连重放 | 首条不重复渲染 |
| T7 | 短线任务 | 快速完成 | final 后无残留 idle timer 僵尸（useSSE.ts:850-856 回归） |
| T8 | ⭐ 重放三层不翻倍（Q6 验证） | 断连重连，断点检查 | 重放帧被 seq 守卫在 switch 前短路：executionSteps state 长度不翻倍、executionStepsRef 不翻倍、sessionStorage 恢复后的步骤数不翻倍、message.executionSteps 不重复 |
| T9 | 刷新恢复不重复（Q6 关联） | 完成后刷新，再发新消息 | 刷新恢复的步骤为单份；恢复源 + 新消息累积后仍无重复 |
| T10 | ⭐ 高频输出不冻结（Q12 验证） | 长答案流式输出，F12 Performance 记录主线程长任务 | 300-500ms 内无 >100ms 长任务；打字机无可见停顿 |
| T11 | ⭐ 结束不丢尾步（Q13 验证） | 长任务快结束（最后3步）观察 | final 到达后 RightViewer 仍显示最后 thought/action/observation 全部步骤，切历史无缺尾 |

## 八、实施顺序与风险

| 顺序 | 步骤 | 风险 | 缓解 |
|------|------|------|------|
| 1 | S1 清死代码 | 低 | 仅删除，无行为 |
| 2 | S5 onResumed 时序 | 低 | 单点顺序调整 |
| 3 | S6 cancelInProgress | 低 | 判断条件收紧 |
| 4 | S2 单一真源 | 中 | 先加 props 注入再删 useChatState ref，逐调用点替换 |
| 5 | S3 seq 守卫 | 中 | 先 T2 再 T5/T6 |
| 6 | S4 updater 纯化 | 中 | 先 T1 回归再全量 |
| 7 | S7 缓冲统一 / S8 超时改名 | 低 | 纯重构 |
| 8 | S9 abort flush | 低 | 单点 |
| 9 | S13 live→终态交接（症状3丢尾） | 中 | 先 settledSteps 兜底再动 RightViewer 数据源；T11 验证 |
| 10 | S12 阻塞根除（症状2冻结） | 中 | 分批 commit + Worker 序列化，T10 Performance 量化 |

**总原则**：每次改动一条，跑对应 T 用例全绿再进下一条，严禁批量。优先级：**S3(重复) ≥ S13(丢尾) ≥ S12(冻结)**，先解决用户三症状，再动架构质量项。

**签名**: 小欧
**更新时间**: 2026-09-10 09:36:04