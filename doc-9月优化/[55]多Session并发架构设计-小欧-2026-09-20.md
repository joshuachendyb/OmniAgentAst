# [55] 多 Session 并发架构设计文档

| 项目 | 内容 |
|------|------|
| **文档名称** | [55]多Session并发架构设计-小欧-2026-09-20.md |
| **编写人** | 小欧 |
| **时间** | 2026-09-20 08:31:46 |
| **版本** | v1.8.1 |
| **状态** | 待北京老陈审核 |

---

## 版本历史

| 版本 | 时间 | 更新内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-07-17 21:28:19 | 初稿：全系统并发分析+6层架构设计+三思三省 | 小欧 |
| v1.1 | 2026-07-17 22:15:00 | 补盲扫描：file_state/win_registry无锁、跨任务文件冲突、task_runtime缺失import、同会话串行化闸门、多轮加载竞态、chunk_buffer损坏、sequence_number竞态 | 小欧 |
| v1.2 | 2026-09-20 03:41:21 | 小欧 | 文档搬迁：doc-7月优化 → doc-9月优化，加编号[55]，文件名日期同步更新 |
| v1.3 | 2026-09-20 03:51:58 | 小欧 | 新增「十、实施状态核查」：对照本地最新代码逐项实证标注 X1-X9/C1-C3/H1-H5/M1-M6 落地现状（北京老陈指令「不许弄虚作假」后的二次实证） |
| v1.4 | 2026-09-20 04:06:43 | 小欧 | 追加「十二、多任务并行必要性分级与新方案骨架」：按「多任务并行」目标重分级未实施项，给出并行新骨架（北京老陈定：不新建文档，直接补充本文件尾部） |
| v1.5 | 2026-09-20 04:13:35 | 小欧 | 术语修正：北京老陈明确「多会话并行任务」= 各会话各自持有任务、跨会话并发执行，同会话内仍串行续聊；据此更正 12.3 X5/X6 先前「与并行目标相反」的错判（应保留实施）并更新 12.1~12.6 分级 |
| v1.6 | 2026-09-20 04:16:55 | 小欧 | 关键更正 12.1/12.4/12.5/12.6：实证确认 snapshot 仅覆盖「有sessionModel覆盖的会话」，无覆盖会话并行时仍共享全局单例（`_cancelled/_current_response/_stop_check` 单槽位竞态），C1 回调为必须修；最优架构=无条件 snapshot（状态分离）+ 共享 httpx 连接池（复用），新增 12.7 多会话并行最终架构 |
| v1.7 | 2026-09-20 04:22:00 | 小欧 | 新增「十三、多会话并行详细设计与真实代码 Diff」：按北京老陈指令「详细设计代码必须基于本地最新代码的代码diff」，逐字取证 task_runtime/resolver/base_service/agent_runner/stream_orchestrator/file_state/registry_read/session_service/task_registry/task_state 等真实源码后，写 13.1~13.7 共 P0-P5 六个落地项 + 落地顺序验证回滚说明，每项含 diff 全部基于真实当前代码；13.4 依北京老陈定案修订为「同会话串行 + 运行中注入（B 机制：inbox 队列，每轮 LLM 调用前合并吸收，机会②）」 |
| v1.8 | 2026-09-20 08:24:07 | 小欧 | 全篇一致性统一（北京老陈指令「整体体现最新设计与决策，前面章节不一致处修改或删除」）：① 第3章 6 层图 L4/L5 改为新定案（无条件 snapshot 复用共享池 + 同会话1串行注入，弃 Semaphore(3)）；② 决策4/决策10 改为 B 机制定案（旧 per-session 信号量、前端拦截/后端拒绝/排队方案废弃）；③ 第5章层号对齐 6 层；④ 第6章 5 遍验证更新（去掉 semaphore/session_semaphores，心跳对齐 `: ping`）；⑤ 第8章 Phase2 映射 12.6 P1-P5；⑥ 第9章风险矩阵引用改 13.x；⑦ 第10章 C1 状态更正为「部分缓解必须修」（同 12.1），10.3 改 P0-P6 对照；⑧ 第11章评分注记改为 v1.7 定案口径；⑨ 13.4.1 补 typing `List` import、13.4.3 补 orchestrator import + `request["content"]→user_input`（落地 NameError 预防） |
| v1.8.1 | 2026-09-20 08:31:46 | 小欧 | 北京老陈审查后确认：**第8章「实施方案（分3个阶段）」废弃清空**（已被 12.6 P0-P6 + 13.1-13.6 完全取代，无独立价值）；第8章留空并注记「落地以 12.6 + 13.x 为准」 |

---

## 一、文档目的

本文档基于**全系统逐行代码阅读**，对多 Session 并发场景进行深度分析，产出：
1. 全系统并发问题清单（含代码行号定位）
2. 6层分层架构设计
3. 逐决策点三思三省
4. 10大编码规范检验
5. 5遍交叉验证
6. 实施方案

**原则**：只审核设计，不盲目修改代码。所有设计必须经北京老陈审核后方可实施。

---

## 二、全系统并发问题清单（17个问题）

### 2.1 致命级（CRITICAL）— 3个

| # | 问题 | 代码位置 | 影响 |
|---|------|----------|------|
| C1 | **LLM Client 全局单例竞态** — `_cancelled`/`_stop_check`/`_current_response` 三个 per-request 状态共享在单例上 | `base_service.py:68-70`, `lifecycle/service.py:19-21,89-114` | Session A 取消会误杀 Session B 的流 |
| C2 | **config.yaml 读-改-写无锁** — 所有配置修改端点（8个）都用 `load_config()→改→save_config()` 模式，无文件锁/互斥锁 | `model_routes.py` 所有 PUT/POST/DELETE 端点 | 最后写入者胜，静默丢失配置 |
| C3 | **message_count 非原子递增** — `save_message` 用 `session["message_count"] + 1` 而非 SQL 原子递增 | `messages.py:136` | 并发保存消息时 message_count 少计 |

### 2.2 高风险级（HIGH）— 5个

| # | 问题 | 代码位置 | 影响 |
|---|------|----------|------|
| H1 | **Session 删除不取消运行中任务** — `delete_session` 不检查 `running_tasks`，不取消关联任务 | `sessions.py:260-280` | 任务继续写入已删除 session（孤儿数据） |
| H2 | **`get_service_for_model()` 无锁** — `cleanup_old_instance()` 不持锁，可与 `get_service()` 竞态导致实例泄漏 | `lifecycle/service.py:141-161,48-52` | httpx 连接池泄漏 |
| H3 | **rollback 非幂等** — 并发调用 `rollback_session()` 会重复回滚同一操作 | `task_queries.py:46-56` | 文件系统损坏 |
| H4 | **`reset()` 不清理运行中任务** — 关闭 LLM 服务时，运行中任务的 `ai_service` 引用指向已关闭对象 | `main.py:193-194`, `lifecycle/service.py:49-52` | 僵尸任务，后续 HTTP 调用失败 |
| H5 | **Session 切换时旧 SSE 回调污染新 Session** — `onChunk` 无 session_id 校验，旧 session 的 chunk 会追加到新 session | `useChatCallbacks.ts:272-317` | 消息列表出现幽灵 AI 内容 |

### 2.3 中风险级（MEDIUM）— 6个

| # | 问题 | 代码位置 | 影响 |
|---|------|----------|------|
| M1 | **httpx.AsyncClient 共享 headers/base_url** — client 级别的 `Authorization` header 和 `base_url` 在多 provider 场景下会冲突 | `client_sdk.py:107-120` | 当前单 provider 安全，多 provider 时认证错误 |
| M2 | **`_pending_confirmations` 无锁** — 依赖 CPython GIL + asyncio 单线程模型 | `hitl_confirmation.py:32` | 多 worker 部署时跨进程 confirmation 失败 |
| M3 | **SSE 心跳缺失** — `stream_reader` 无超时/心跳，长 LLM 调用时前端 60s idle timeout 断连 | `stream.py:99-113` | 长操作时前端假断连 |
| M4 | **DB step 逐条连接** — 每个 agent step 独立 `db.get_conn()` 开/关 | `agent_runner.py:152-161` | 20步=22次连接创建/销毁 |
| M5 | **`agent_streams` 无锁** — 全局 dict 无保护 | `task_state.py:38` | 多 worker 部署时竞态 |
| M6 | **`MetricsCollector` 无锁** — 全局 dict 无保护 | `collector.py:43` | 多线程部署时字典迭代崩溃 |

### 2.4 低风险级（LOW）— 3个

| # | 问题 | 代码位置 | 影响 |
|---|------|----------|------|
| L1 | **跨任务文件冲突未检测** — `_has_conflict()` 只检测单 action 内冲突，不检测跨任务 | `action_handler.py` | 两个任务同时写同一文件，数据丢失 |
| L2 | **SSE start 步骤不在 buffer 中** — 重连时丢失 start 步骤 | `openai.py:231-232` | 前端不显示"AI 正在思考"指示器 |
| L3 | **`_user_message_ids` 内存泄漏** — session 删除时不清理 | `storage.py:26` | 进程生命周期内内存增长 |

---

## 二-补：第二轮补盲扫描新发现问题（2026-07-17 22:15:00 补充）

> 第一轮扫描遗漏了**工具层安全状态、同会话并发、启动初始化、后台任务生命周期**等死角。第二轮 4 个探索任务补全，发现 **9 个新问题**，其中 2 个高风险、1 个已确认崩溃 bug。

### 2.5 高风险级（HIGH，新增）— 2个

| # | 问题 | 代码位置 | 影响 |
|---|------|----------|------|
| X1 | **`file_state._state` 无锁全局 dict** — `record_read/record_write/check_conflict` 读写跨任务共享的 `Dict[resolved_path→(mtime_ns,content_hash)]`，经 `asyncio.to_thread` 在线程池并行执行，完全无锁 | `tools/file/file_state.py:全局_var` | 两个任务写同一文件时 mtime 缓存互相污染，`check_conflict` 误报/漏报；check-then-act 复合操作非原子 |
| X2 | **跨任务文件冲突完全无检测** — `operation_*` 层按单 task 视角设计，无进程内/DB 级跨 task 互斥、文件锁、冲突检测；磁盘目标文件写无跨 task 保护 | `safety/operation_executor.py`、`file_state.py` | 两个任务并发写同一文件，后写覆盖前写，各自备份仅时间戳目录不撞名；回滚只能恢复各自备份点，可能产生静默数据丢失 |

### 2.6 已确认崩溃 bug（CONFIRMED CRASH）— 1个

| # | 问题 | 代码位置 | 影响 |
|---|------|----------|------|
| X3 | **`task_runtime.py` 缺失 `import asyncio`** — 第112行 `await asyncio.wait_for(...)`、第115行 `except asyncio.TimeoutError:` 引用未导入的 `asyncio`，必抛 `NameError` | `task_runtime.py:9-24`(无import)、`:112`、`:115` | 用户暂停任务且进入超时等待分支（300s）时 `NameError` 崩溃，暂停→恢复流程失败，可能中断 agent 任务 |

### 2.7 中风险级（MEDIUM，新增）— 3个

| # | 问题 | 代码位置 | 影响 |
|---|------|----------|------|
| X4 | **`win_registry._registry_session_backup` 无锁** — `Dict[root\sub→.reg备份路径]` 跨任务共享，经 `asyncio.to_thread` 并行执行，read-then-write 非原子 | `tools/win_registry/registry_read.py:33` | 注册表同键并发写/删时 dict 写入竞态与备份语义污染（碰撞概率低） |
| X5 | **同会话快速发两条消息无串行化闸门** — 后端对"同一 session_id 同时跑两个任务"无任何互斥/排队/拒绝；两条消息各自起独立 Agent、独立 task_id、独立 SSE 缓冲并行执行 | `openai.py:235-240` | 两个助手答案同时写库、上下文互相穿插的体验问题（非崩溃级）；AI 消息 ID 靠 SQLite 主键兜底不冲突 |
| X6 | **多轮上下文加载快照竞态** — `_load_previous_messages(session_id)` 在任务启动时一次性快照；消息2若在消息1 `finalize_message`（写 content/status）之前加载，读到不完整的消息1（空 content 或残缺 steps） | `stream.py:67-96`、`agent_runner.py:129` | 消息2基于残缺上下文作答；不丢数据（消息1最终完整落库），仅影响多轮一致性 |

### 2.8 低风险级（LOW，新增）— 3个

| # | 问题 | 代码位置 | 影响 |
|---|------|----------|------|
| X7 | **`sequence_number` 并发重复** — `add_operation` 用 `SELECT COALESCE(MAX(sequence_number),0)+1` 算 seq，非事务隔离保证；同 task 并发 add_operation 可能算出相同 seq | `task_db.py:89-94` | 回放/排序时步骤顺序错乱（非数据损坏，一致性弱化） |
| X8 | **`ensure_tools_registered` 缺双调防护** — 仅 `_registered_categories` set 去重，无"初始化中"互斥标志；若改为懒加载并发调用可能双注册 | `tools/registry.py:380-404` | 当前 startup 单次调用无触发，属架构健壮性短板 |
| X9 | **`chunk_buffer.py` 重复方法定义+误植代码** — `should_force_stop`/`clear` 各定义两遍，首个 `clear` 内误植 `self.buffer+=content`/`self.consecutive_count+=1`；因 Python 方法覆盖语义，坏代码被覆盖永不执行，功能正确不崩溃 | `agent/chunk_buffer.py:44-79` | 代码质量缺陷（违反 DRY/SRP），应清理；非并发 bug |

### 2.9 补盲扫描确认"已正确"的项（无需改动）

| # | 项 | 结论 |
|---|------|------|
| Y1 | StreamBuffer 多消费者（两标签页读同一 task_id） | ✅ 安全：`offset` 是消费者局部变量，`event_log` 单生产者 append-only，seq 唯一 |
| Y2 | 重连端点并发（两标签页同时重连） | ✅ 安全：幂等只读，不创建/写 buffer、不启动 agent |
| Y3 | 执行查看器 SSE 多消费者 | ✅ 安全：无状态、每次重开 DB 连接、不碰 agent_streams |
| Y4 | `message_builder.MAX_CONTEXT_CHARS` | ✅ 安全：实例属性，per-agent 隔离 |
| Y5 | `observation_formatter`/`initialize_run_state`/`fc_message_types` | ✅ 安全：纯函数/类型模块，无模块级可变状态 |
| Y6 | `_background_tasks`/`_agent_tasks` 裸 set | ✅ 安全：asyncio 单线程下 set.add/discard 原子 |
| Y7 | `_tool_cache`/`_loaded_categories`/`_tools_dict`/`_searchtool_desc_override` | ✅ 安全：全部 per-agent 实例属性 |
| Y8 | `display_name_cache`/`_user_message_ids` | ✅ 安全：全局但持 `threading.Lock`，单线程下功能安全 |
| Y9 | Timer 全套（`_timers`/`_timer_counter`/`_callback`/`_events`） | ✅ 安全：统一 `_timer_lock`(asyncio.Lock) 保护 |
| Y10 | `operation_backup._backup_path` | ✅ 已锁（`threading.Lock`），但无调用者（死代码） |
| Y11 | Desktop 全部工具 | ✅ 无全局状态，每次实时读 OS/落盘临时文件 |

---

## 三、6层分层架构设计

> **v1.7 修订注记（小欧 2026-09-20）**：本图原 v1.0 版 L4/L5 与新定案不符（L4「每次请求新建」、L5「Semaphore(3) per session」）。按北京老陈 2026-09-20 定案「同会话 1 任务串行 + 运行中注入（B 机制）」改写 L4/L5；L6 心跳已实施（M3），对齐现状；L1/L2 不变。详细实施见 12.7 / 13.2 / 13.4。

```
┌───────────────────────────────────────────────────────┐
│ Layer 6: SSE Heartbeat (心跳保活)                      │
│   已实施(M3)：stream_orchestrator.py:511 HEARTBEAT++": ping"│
├───────────────────────────────────────────────────────┤
│ Layer 5: Per-session Concurrency (并发控制)            │
│   同会话 1 任务串行 + 运行中注入(B机制: inbox 队列)      │
│   (弃 v1.0 asyncio.Semaphore(3) —— 13.4 定案)         │
├───────────────────────────────────────────────────────┤
│ Layer 4: Per-request BaseAIService (状态隔离)          │
│   每任务 snapshot() 无条件状态分离，复用共享连接池       │
│   (_cancelled/_stop_check/_current_response 独立, 13.2) │
├───────────────────────────────────────────────────────┤
│ Layer 3: Per-request LLMClient (薄包装)               │
│   每任务持有 LLMClient，其 _client 复用共享 AsyncClient │
├───────────────────────────────────────────────────────┤
│ Layer 2: Shared httpx.AsyncClient (连接池共享)         │
│   模块级单例，10 连接池，所有请求共用 (13.2.1 注入)      │
├───────────────────────────────────────────────────────┤
│ Layer 1: SQLite WAL (已有)                             │
│   busy_timeout=30s, WAL mode, 无需改动                 │
└───────────────────────────────────────────────────────┘
```

---

## 四、逐决策点三思三省

### 决策1：每任务独立 BaseAIService（Layer 4 状态隔离层 → 13.2 无条件 snapshot）

> **v1.8 注记（小欧 2026-09-20）**：决策1「per-request 独立服务」精神正确，落地形态以 13.2 为准——每任务调用前 `snapshot()` 无条件构造独立 BaseAIService，复用共享连接池（不再仅限有 sessionModel 覆盖的会话，12.1 C1）。

| 维度 | 思 | 省 |
|------|-----|-----|
| **正确性** | 每任务独立 `_cancelled`/`_stop_check`/`_current_response`，消除竞态（13.2 定案） | ✅ 通过 |
| **性能** | BaseAIService 是轻量包装器（无重资源），创建开销 ~1μs | ✅ 通过 |
| **内存** | httpx.AsyncClient 共享，不爆炸 | ✅ 通过 |
| **遗漏检查** | `_llm_sdk` 是懒初始化的，一旦创建就不变，可安全共享 | ✅ 无遗漏 |
| **异常安全** | 创建 BaseAIService 失败时，shared_client 由模块级管理，不泄漏 | ✅ 通过 |
| **关闭时机** | 请求结束时不需要 close shared_client（进程生命周期管理） | ✅ 通过 |
| **约束** | 单 provider 架构下安全；多 provider 时需 per-request header | ⚠️ 需标注约束 |

**结论**: ✅ 设计正确，符合 SRP + KISS 原则

---

### 决策2：Shared httpx.AsyncClient（Layer 2 连接池层）

| 维度 | 思 | 省 |
|------|-----|-----|
| **线程安全** | httpx 文档明确说 AsyncClient 是 asyncio 安全的 | ✅ 通过 |
| **连接池复用** | 避免 per-request 连接池爆炸（10 连接 vs N×10 连接） | ✅ 通过 |
| **超时配置** | 每个请求可传不同 `request_timeout`，覆盖 client 级别默认值 | ✅ 通过 |
| **⚠️ 认证头问题** | `headers={"Authorization": f"Bearer {api_key}"}` 是 client 级别的 | ⚠️ **发现设计约束** |
| **约束分析** | 当前系统只用一个 provider（单例模式），共享 client 安全 | ⚠️ **需标注约束** |
| **base_url 问题** | 同上，`base_url` 也是 client 级别的 | ⚠️ **同上约束** |

> **v1.8 改写（小欧 2026-09-20）**：初稿「修正设计：shared_client 不设 headers/base_url，由 LLMClient per-request 传入」已被 13.2.1 定案替代——**快照直接复用全局单例客户端（含 headers/base_url）**，共享连接池内部 handle 复用即够。初稿修正确认：

**结论**: ✅ 设计正确：13.2.1 snapshot 复用全局单例 client（含 headers），无需 per-request 传 header；单 provider 架构约束标注见 12.3 M1

---

### 决策3：register_task 增加 session_id（Layer 4 会话关联）

| 维度 | 思 | 省 |
|------|-----|-----|
| **必要性** | 同会话活跃任务判定（`has_active_task_in_session`）需要 session_id（13.4.1） | ✅ 通过 |
| **向后兼容** | 默认空串，不影响现有调用 | ✅ 通过 |
| **来源** | `request.session_id` 已在 `chat_stream()` 中 | ✅ 通过 |
| **遗漏检查** | running_tasks 已有 `created_at`、`status`、`ai_service`，session_id 是必要补充 | ✅ 通过 |

**结论**: ✅ 设计正确，符合 DRY 原则

---

### 决策4：同会话并发控制（原 per-session 信号量 → v1.7 改 B 机制）

> **v1.7 定案改写（小欧 2026-09-20）**：初稿设计 `asyncio.Semaphore(3) per session`（单会话最多 3 并发）已被北京老陈 2026-09-20 定案否决。最终定案 = **同会话 1 任务串行 + 运行中注入（B 机制，13.4）**，不再使用任何 per-session 信号量。下表为改写后语义。

| 维度 | 思 | 省 |
|------|-----|-----|
| **防资源耗尽** | 同会话同一时刻只跑 1 任务（串行），天然满足目标语义「同会话续聊不乱」 | ✅ 通过 |
| **独立性** | 同会话无并发、不同会话各自独立任务，互不影响；跨会话并发总量由 13.6 全局软配额兜底 | ✅ 通过 |
| **泄漏风险** | B 机制基于 `running_tasks` + inbox 队列，随任务一起清理，无独立信号量泄漏面 | ✅ 通过 |
| **session 清理** | 任务与会话同生命周期（13.5 H1 级联取消），无需额外 `cleanup_session_semaphore` | ✅ 通过 |
| **上限合理性** | 不再设 3 并发上限（会话内本就 1 任务）；全局软配额 `LLM_MAX_CONNECTIONS=10`（13.6）承接防击穿 | ✅ 通过 |
| **与 task cleanup 交互** | 任务 finalize 时 inbox 一并清空，无残留 | ✅ 通过 |
| **运行中插话** | 任务执行中收到新消息 → 注入 inbox，下一轮 LLM 前合并吸收（13.4），不打断、不拒绝、不排队 | ✅ 通过 |

**结论**: ✅ 定案 = 同会话 1 串行 + B 机制注入（13.4）；旧 `_session_semaphores` 设计废弃

---

### 决策5：SSE 心跳（Layer 6 可观测性层）

| 维度 | 思 | 省 |
|------|-----|-----|
| **防断连** | 30s 心跳 < 60s idle timeout，确保不断连 | ✅ 通过 |
| **格式** | 已实施（M3）：`HEARTBEAT_INTERVAL + ": ping\n"` 标准 SSE 注释格式（stream_orchestrator.py:511/521） | ✅ 通过 |
| **前端兼容** | `processSSEData` 会忽略非 `data:` 行 | ✅ 通过 |
| **不影响重连** | 心跳不进入 `event_log`，不影响 `after_seq` | ✅ 通过 |
| **不影响 prompt logger** | 心跳不通过 `_append()`，不记日志 | ✅ 通过 |
| **不影响 buffer done** | 心跳在 `buffer.cond.wait()` 超时时触发，不设置 `done` | ✅ 通过 |

**结论**: ✅ 设计正确，符合 KISS 原则

---

### 决策6：DB step 批量写入（Layer 1 数据层优化）

| 维度 | 思 | 省 |
|------|-----|-----|
| **减少开销** | 20 步从 22 次连接降到 1 次，减少 90% | ✅ 通过 |
| **连接生命周期** | `try/finally` 确保关闭 | ✅ 通过 |
| **异常安全** | 中途异常时连接正确关闭 | ✅ 通过 |
| **并发安全** | 单连接被单协程使用，无并发问题 | ✅ 通过 |
| **WAL 模式** | SQLite WAL 允许并发读，单连接写安全 | ✅ 通过 |

**结论**: ✅ 设计正确，符合 SLAP 原则

---

## 四-补：新增决策点三思三省（2026-07-17 22:15:00 补充）

### 决策7：为 `file_state._state` 加锁（修复 X1）

| 维度 | 思 | 省 |
|------|-----|-----|
| **正确性** | 该全局 dict 被 `asyncio.to_thread` 派发的同步函数并行访问，无锁必有竞态 | ✅ 必须加锁 |
| **锁类型** | 访问方是线程池中的同步函数（`asyncio.to_thread`），非协程 → 必须用 `threading.Lock` | ✅ 用 threading.Lock（非 asyncio.Lock） |
| **隔离粒度** | 当前跨 task 共享，应至少按 task 或按文件隔离；最简方案是全局 `threading.Lock` 保护所有读写 | ✅ 全局锁可接受（临界区极短） |
| **异常安全** | 锁内只做 dict 读写，无 I/O，不会死锁 | ✅ 通过 |
| **性能** | 文件工具并发度低，全局锁开销可忽略 | ✅ 通过 |

**结论**: ✅ 设计正确，用 `threading.Lock` 包裹 `_state` 全部读写

---

### 决策8：跨任务文件冲突检测（修复 X2）

| 维度 | 思 | 省 |
|------|-----|------|
| **必要性** | 两个任务并发写同一文件，磁盘目标无互斥，数据丢失风险真实 | ✅ 必须设计 |
| **方案选择** | A: 全局文件锁（fcntl/msvcrt）B: 跨 task 冲突检测（借鉴 `_has_conflict` 扩展到跨 task）C: 按文件粒度 `asyncio.Lock`/`threading.Lock` 字典 | ⚠️ 见下 |
| **A 方案** | 操作系统级文件锁，最彻底，但 Windows 下 `msvcrt.locking` 仅支持阻塞锁，且与 `asyncio.to_thread` 模型配合复杂 | ⚠️ 复杂度高 |
| **B 方案（推荐）** | 在 `file_state` 或 operation 层维护"正在写入的文件集"，新任务写文件前检查是否有其他 task 正在写同一文件 → 若有则排队或告警 | ✅ KISS，复用现有 `_state` 结构 |
| **C 方案** | 每个被写文件一个锁对象，并发写同文件串行化 | ✅ 精确但需管理锁生命周期 |
| **当前决策** | **12.6 P2 采用 B 方案落地为 `file_write_arbiter`（跨 task 冲突检测 + 锁 + 登记 + 告警/排队，13.3），标注为已知约束，不阻塞核心隔离实施** | ⚠️ 标注约束 |

**结论**: ⚠️ 设计采用"跨 task 冲突检测"轻量方案，完整文件锁留作后续增强

---

### 决策9：补全 `task_runtime.py` 缺失 import（修复 X3）

| 维度 | 思 | 省 |
|------|-----|------|
| **正确性** | `asyncio.wait_for`/`asyncio.TimeoutError` 引用未导入符号，超时分支必崩 | ✅ 必须补 `import asyncio` |
| **影响面** | 仅暂停流程超时分支（300s），正常即时恢复不触发 | ✅ 影响窄但致命 |
| **修复成本** | 一行 import，零风险 | ✅ 立即修复 |

**结论**: ✅ 一行 `import asyncio` 修复，纳入 12.6 P0 最高优先级（13.1）

---

### 决策10：同会话串行化闸门（X5/X6 → v1.7 改 B 机制注入）

> **v1.7 定案改写（小欧 2026-09-20）**：初稿的三方案讨论（A 后端拒绝 429、B 后端排队、C 前端拦截+后端拒绝）已是过时备选。北京老陈 2026-09-20 定案 **B 机制（13.4）**：同会话已有活跃任务时，新消息注入该任务 inbox，下一轮 LLM 调用前合并吸收。下文保留初稿三方案仅作历史演进记录，最终定案以 13.4 为准。

| 维度 | 思 | 省 |
|------|-----|------|
| **必要性** | 同 session 连续两条消息要求上下文不穿插、不丢上一轮响应 | ✅ 必须设计 |
| ~~A: 后端拒绝~~ | 同 session 有活跃 task 返回 429/错误 → 用户需手动重试，体验差 | ❌ 否决（v1.7） |
| ~~B: 后端排队~~ | 新消息等上一 task finalize 再启动 → 需管理会话级等待队列，复杂度高 | ❌ 否决（v1.7） |
| ~~C: 前端拦截~~ | 有活跃 streaming 时禁用发送按钮 + 后端防御 → 用户得先等完再发，同会话仍卡顿 | ❌ 否决（v1.7） |
| **B 机制（定案，13.4）** | 有活跃任务 → 注入 inbox，工具不打断、不拒绝、不排队；无活跃任务 → 正常新建任务 | ✅ 北京老陈 2026-09-20 定 |
| **前端配合** | 发送框不禁用，改"注入"模式（提示下轮吸收），由 `serverTaskId` + 响应 `error_type="injected"` 派生 | ✅ 最小契约变化 |
| **全局总量** | 跨会话并发峰值由 13.6 全局软配额（连接池感知）兜底，非会话级闸门 | ✅ 互补不冲突 |

**结论**: ✅ 定案 = B 机制注入（13.4）；前端拦截/后端拒绝/排队的旧 A-C 方案全部废弃

---

### 决策11：TaskTracker `sequence_number` 事务化（修复 X7）

| 维度 | 思 | 省 |
|------|-----|------|
| **正确性** | `SELECT MAX+1` 非原子，并发 add_operation 可能重复 seq | ✅ 需加固 |
| **方案** | 改用事务内 `INSERT` 后取 `last_insert_rowid`，或 DB 列加 `AUTOINCREMENT`/唯一序列 | ✅ 标准做法 |
| **影响** | 仅影响回放排序，非数据损坏；优先级低 | ⚠️ 可延后 |

**结论**: ⚠️ 标注为低优先加固项，纳入 12.6 P6 或后续处理

---

### 决策12：`ensure_tools_registered` 双调防护（修复 X8）

| 维度 | 思 | 省 |
|------|-----|------|
| **必要性** | 当前 startup 单次调用无触发，但缺防护属架构短板 | ⚠️ 预防性 |
| **方案** | 加 `_init_guard = asyncio.Lock()` 或 `_init_done` 原子标志 | ✅ 极简 |
| **影响** | 仅启动时一次，零运行时开销 | ✅ 通过 |

**结论**: ✅ 加 `_init_guard` 锁，纳入 12.6 P6 启动加固

---

### 决策13：清理 `chunk_buffer.py` 重复方法（修复 X9）

| 维度 | 思 | 省 |
|------|-----|------|
| **正确性** | 重复定义因 Python 覆盖语义功能正确不崩溃，但违反 DRY/SRP | ✅ 应清理 |
| **方案** | 按"先复制后修改"原则，保留 line 68-79 干净版，删除 line 44-66 首组（含误植的 line 65-66） | ✅ KISS |
| **风险** | 纯删除重复代码，功能不变 | ✅ 零风险 |

**结论**: ✅ 删除重复定义，纳入 12.6 P6 代码质量清理

---

## 五、10大编码规范检验

| # | 规范 | 设计是否符合 | 说明 |
|---|------|-------------|------|
| 1 | **SRP 单一职责** | ✅ | 每层职责单一：L1 SQLite、L2 连接池、L3 薄包装、L4 状态隔离、L5 并发控制、L6 心跳 |
| 2 | **DRY 不重复** | ✅ | shared_client 集中管理，不散落在 LLMClient 中；注入/吸收逻辑集中在 B 机制（13.4） |
| 3 | **KISS-DIRECT 简单直接** | ✅ | 无中间层、无注册表、无抽象层，直接创建/直接使用 |
| 4 | **SLAP 同一抽象层** | ✅ | 连接池层不混搭请求层逻辑，状态隔离层不混搭连接池逻辑 |
| 5 | **YAGNI 不过度设计** | ✅ | 不引入 IoC 容器、不引入连接池框架、不引入分布式锁 |
| 6 | **禁止 backward** | ✅ | 不保留旧 `get_service()` 用于请求场景（标注 deprecated） |
| 7 | **OCP 开封闭** | ✅ | 新增 provider 无需修改 shared_client（参数化） |
| 8 | **LSP 里氏替换** | ✅ | snapshot 返回类型与进程级 `get_service()` 相同（BaseAIService），可无缝替换 |
| 9 | **ISP 接口隔离** | ✅ | LLMClient 只暴露 `request_stream()`，不暴露内部 httpx 细节 |
| 10 | **复用优先** | ✅ | 复用现有 `create_service_instance()`、`get_resolver_and_config()` 等 |

---

## 六、5遍交叉验证

### 第1遍：数据流完整性

```
用户请求 → stream_orchestrator 查活跃任务(B机制)
  → 无活跃 → UniversalAgent(llm_client=snapshot 独立服务)
  → register_task(task_id, ai_service, session_id)
  → asyncio.create_task(run_agent_in_background(...))
  → agent.run_react_cycle() → 每轮 LLM 前 drain_inbox 合并 → call_llm_stream()
  → 共享 httpx.AsyncClient.stream() → LLM API
```

**验证**: 每个请求的 `BaseAIService` 独立，`_cancelled`/`_stop_check`/`_current_response` 不共享 ✅

### 第2遍：取消/暂停/恢复安全性

```
cancel_task(task_id):
  → set_cancelled(task_id) → running_tasks[task_id]["cancelled"] = True
  → ai_service.cancel() → 该任务 snapshot 的 _cancelled = True（不影响其他任务）
  → _stop_check() → check_cancelled(task_id) → 读 running_tasks（不影响其他任务）

pause_task(task_id):
  → set_paused(task_id) → running_tasks[task_id]["paused"] = True
  → pause_event.clear() → task_pause_check() 阻塞（不影响其他请求）

resume_task(task_id):
  → set_resumed(task_id) → pause_event.set() → task_pause_check() 恢复
```

**验证**: 每个操作只影响指定 task_id，不影响其他请求 ✅

### 第3遍：SSE 重连兼容性

```
初始连接: POST /chat/stream → create_stream_buffer(task_id) → register_task(task_id, ...)
断线: CancelledError → agent 后台继续 → buffer.done.set()
重连: GET /chat/stream/{task_id}?after_seq=N → get_stream_buffer(task_id)
  → stream_reader(buffer, task_id, after_seq=N) → 重放事件
心跳: stream_reader 超时 → yield `HEARTBEAT_INTERVAL + ": ping\n"`（M3）→ 前端忽略
```

**验证**: 心跳不进入 event_log，不影响 after_seq 重连 ✅

### 第4遍：DB 并发安全性

```
Task A: db.get_conn("chat") → allocate_and_insert_message() → append_execution_step() → conn.close()
Task B: db.get_conn("chat") → allocate_and_insert_message() → append_execution_step() → conn.close()
```

**验证**: SQLite WAL 模式允许并发读，单写串行化，busy_timeout=30s 足够 ✅

### 第5遍：资源泄漏检查

```
httpx.AsyncClient: 模块级管理，进程退出时 close
BaseAIService: 每任务 snapshot() 独立创建，任务结束时无引用→GC 回收
LLMClient: 复用共享连接池，请求结束时无引用→GC 回收
_stream_buffer: call_later(300) 延迟回收
running_tasks: task_cleanup() 正常清理，cancelled 1h 后清理
inbox 队列(B机制): 随任务终态一并清理（13.4.1），无独立生命周期
```

**验证**: 无资源泄漏 ✅（已按 v1.7 B 机制 13.4 更新，废弃 `_session_semaphores`）

---

## 七、设计约束清单

| # | 约束 | 影响 | 缓解 |
|---|------|------|------|
| C1 | **单 provider 架构** | shared httpx.AsyncClient 的 headers/base_url 是 provider 级别的 | 当前安全；多 provider 时需 per-request header |
| C2 | **单 worker 部署** | `_pending_confirmations`、`agent_streams`、`MetricsCollector` 无锁 | 当前安全；多 worker 时需分布式/外部存储 |
| C3 | **SQLite 单写** | 并发写串行化 | WAL + 30s busy_timeout 足够 |
| C4 | **PowerShell 有状态** | 同工作目录 shell 命令串行 | 设计如此，无需修改 |

---

## 八、实施方案（已废弃）

> **v1.8 废弃（小欧 2026-09-20，经北京老陈确认）**：本章原 v1.0「实施方案（Phase 1/2/3）」已被 12.6（落地 Phase P0-P6）与 13.1-13.6（真实代码 Diff）完全取代，无独立参考价值，现留空废弃。落地以 **12.6 P0-P6 + 13.1-13.6** 为准。

---

## 九、风险矩阵

| 问题 | 严重性 | 当前影响 | 实施后影响 | 改动难度 |
|------|--------|----------|-----------|---------|
| C1 LLM Client 竞态 | CRITICAL | Session A 取消误杀 Session B | ✅ 消除(13.2) | 中 |
| C2 config 读改写无锁 | CRITICAL | 静默丢失配置 | ✅ 加锁(P6) | 低 |
| C3 message_count 非原子 | CRITICAL | count 少计 | ✅ 原子递增(2.4) | 低 |
| H1 Session删除不取消任务 | HIGH | 孤儿数据 | ✅ 级联取消(13.5) | 中 |
| H2 get_service_for_model 无锁 | HIGH | 实例泄漏 | ✅ 加锁 | 低 |
| H3 rollback 非幂等 | HIGH | 文件损坏 | ✅ 原子 claim | 中 |
| H4 reset 不清理任务 | HIGH | 僵尸任务 | ✅ 清理 | 中 |
| H5 Session切换回调污染 | HIGH | 幽灵内容 | ✅ session校验 | 中 |
| M1 共享 headers | MEDIUM | 多provider冲突 | ⚠️ 标注约束 | 无 |
| M2 confirmations 无锁 | MEDIUM | 多worker失败 | ⚠️ 标注约束 | 无 |
| M3 SSE 心跳缺失 | MEDIUM | 长操作断连 | ✅ 心跳(M3) | 低 |
| M4 DB 逐条连接 | MEDIUM | 连接开销 | ✅ 批量写入 | 低 |
| M5 agent_streams 无锁 | MEDIUM | 多worker竞态 | ⚠️ 标注约束 | 无 |
| M6 MetricsCollector 无锁 | MEDIUM | 多线程崩溃 | ⚠️ 标注约束 | 无 |
| L1 跨任务文件冲突 | LOW→HIGH | 数据丢失 | ✅ arbiter(13.3) | 高 |
| L2 start步骤不在buffer | LOW | 重连丢start | ⚠️ 已知限制 | 中 |
| L3 _user_message_ids泄漏 | LOW | 内存增长 | ⚠️ 已知限制 | 低 |
| **X1 file_state._state无锁** | HIGH | 跨任务 mtime 缓存污染 | ✅ 加锁(13.3.2) | 低 |
| **X2 跨任务文件冲突无检测** | HIGH | 并发写同文件数据丢失 | ✅ arbiter(13.3) | 中 |
| **X3 task_runtime缺import asyncio** | HIGH | 暂停超时分支崩溃 | ✅ 一行import(13.1) | 无 |
| **X4 win_registry._registry_session_backup无锁** | MEDIUM | 注册表备份竞态 | ✅ 加锁(13.3.3) | 低 |
| **X5 同会话快速发消息无闸门** | MEDIUM | 上下文穿插 | ✅ B机制注入(13.4) | 中 |
| **X6 多轮上下文加载快照竞态** | MEDIUM | 漏读上一轮响应 | ✅ B机制注入(13.4)缓解 | 中 |
| **X7 sequence_number并发重复** | LOW | 回放顺序错乱 | ✅ 事务化(P6) | 低 |
| **X8 ensure_tools_registered缺双调防护** | LOW | 潜在双注册 | ✅ 加guard(1.8) | 无 |
| **X9 chunk_buffer重复方法** | LOW | 代码质量缺陷 | ✅ 清理(1.9) | 无 |

---

## 十、实施状态核查（v1.3 补充，对照本地代码实证）—— 小欧 2026-09-20

> 说明：本核查由小欧于 2026-09-20 对照 `backend/app` 本地最新代码逐项实证标注，非文档作者原判，供北京老陈裁决后续实施范围。

### 10.1 C1-C3 / H1-H5 / M1-M6 状态

| 编号 | 问题 | 状态 | 实证依据（本地代码） |
|------|------|------|---------------------|
| C1 | LLM Client 全局单例竞态 | 🟡 部分缓解（v1.6 更正，**必须修**） | `resolver.py:179` snapshot 仅覆盖「有 sessionModel 覆盖的会话」（判据 `_ov.model or _ov.provider`）；无覆盖会话并行时 `agent.llm_client` 仍指向全局单例，单槽位被多会话轮换覆盖。定案：13.2 **无条件 snapshot + 共享连接池** |
| C2 | config.yaml 读-改-写无锁 | 🔴 未实施 | `config_service.py:82` `write_yaml_config` 裸写，无 Lock |
| C3 | message_count 非原子递增 | 🟢 已实施 | `message_service.py:176` SQL `message_count + 1` 自增 |
| H1 | Session 删除不取消运行任务 | 🔴 未实施 | `session_service.py:194-213` 仅软删 `is_deleted`，不取消任务 |
| H2 | get_service_for_model 无锁 | 🟢 已实施 | `service.py:33/134/159` `_instance_lock` 保护 get/set/cleanup |
| H3 | rollback 非幂等 | ⚪ 未验证 | `task_rollback_service.py:21` 存在，幂等性未验证 |
| H4 | reset() 不清理运行任务 | ⚪ 未验证 | 需核对 main.py 关闭流程 |
| H5 | Session 切换旧 SSE 回调污染 | ⚪ 前端未核查 | 需核对 frontend `useChatCallbacks.ts` |
| M1 | httpx 共享 headers/base_url | 🔴 未实施 | `client_sdk.py:103` LLMClient 无多 provider 共享处理 |
| M2 | _pending_confirmations 无锁 | 🟢 已实施 | `hitl_confirmation.py:61` `_pending_lock`（2026-07-18 #42） |
| M3 | SSE 心跳缺失 | 🟢 已实施 | `stream_orchestrator.py:511/521` `HEARTBEAT_INTERVAL` + `": ping\n"` |
| M4 | DB step 逐条连接 | ⚪ 未验证 | `agent_runner.py` 落库已改 `db.atxn` offload（2026-08-24） |
| M5 | agent_streams 无锁 | 🟢 已实施 | `task_state.py:26` `running_tasks_lock = asyncio.Lock()` |
| M6 | MetricsCollector 无锁 | 🔴 未实施 | `monitoring/collector.py` 无 Lock |
| L1 | 跨任务文件冲突未检测 | 🔴 未实施 | `operation_executor.py` 不存在 |
| L2 | SSE start 步骤不在 buffer | ⚪ 未验证 | 需核对 `openai.py` / SSE 重连 |
| L3 | _user_message_ids 内存泄漏 | 🔴 未实施 | `storage.py:107` 无清理逻辑 |

### 10.2 X1-X9 状态

| 编号 | 问题 | 状态 | 实证依据（本地代码） |
|------|------|------|---------------------|
| X1 | file_state._state 无锁 | 🔴 未实施 | `tools/file/file_state.py` 全程纯 dict 无锁 |
| X2 | 跨任务文件冲突无检测 | 🔴 未实施 | `file_state.py:49` 仅单文件 mtime 检测，无跨任务写入集 |
| X3 | task_runtime 缺 import asyncio | 🔴 **未修复** | `task_runtime.py:32-48` 无 `import asyncio`，但 `200/203` 行用 `asyncio.wait_for`/`asyncio.TimeoutError` → NameError 风险仍在 |
| X4 | win_registry 备份 dict 无锁 | 🔴 未实施 | `registry_read.py:36` `_registry_session_backup = {}` 裸 dict |
| X5 | 同会话快速发消息无闸门 | 🔴 未实施（修复方案已定 = 13.4 B机制注入） | LLM 侧无 per-session Semaphore（全仓仅 sandbox/shell 侧）；且定案已弃 Semaphore，改用 inbox 注入 + 每轮吸收 |
| X6 | 多轮上下文加载快照竞态 | 🔴 未实施（依赖 13.4 落地） | 定案由 B 机制（同会话 1 任务串行 + 注入）保障，不再依赖旧 X5 闸门 |
| X7 | sequence_number 并发重复 | 🟡 部分（等效替代） | `task_db.py:115-129` `SELECT MAX()+1` 同事务原子（文档要求 AUTOINCREMENT/last_insert_rowid，实现为等效替代） |
| X8 | ensure_tools_registered 双调防护 | 🟢 已实施（等效） | `registry.py:413` `if cat_name not in _registered_categories` 分类去重 |
| X9 | chunk_buffer 重复方法 | 🟢 已实施 | `chunk_buffer.py:9` 编辑历史 2026-07-18 小欧#6 删除 |

### 10.3 落地计划对照（v1.7 改写，对应 12.6 P0-P6，非 v1.0 三阶段旧计划）

| 阶段(12.6) | 关键落点 | 状态 |
|------|---------|------|
| P0 | X3 补 `import asyncio`（task_runtime.py） | 🔴 未实施 |
| P1 | C1 无条件 snapshot + 共享连接池（13.2） | 🔴 未实施——resolver 判据未放开、`_ensure_client` 未支持外部注入 |
| P2 | `file_write_arbiter`（X1/X2/X4 落地，13.3） | 🔴 未实施——`operation_executor.py` 不存在 |
| P3 | X5/X6 同会话串行 + B 机制注入（13.4） | 🔴 未实施——无 inbox/inject 代码 |
| P4 | H1 删会话级联取消任务（13.5） | 🔴 未实施——`session_service.py:194-213` 不取消任务 |
| P5 | 全局 LLM 软配额（13.6） | 🔴 未实施——无连接池感知配额 |
| P6 | C2/M1/M6/L3/X7/X8/X9 按需补 | 🔴/🟡/🟢 见 10.1/10.2 |

---

## 十一、最终设计评估（v1.7 重审注记）

> **v1.7 注记（小欧 2026-09-20）**：本表原为 v1.0 初稿评分。北京老陈 2026-09-20 定案后实际打分以下列为准，并入 12.7（最终推荐架构）与第 13 章（真实 Diff）。

| 评估维度 | 评分 | 说明（对应定案章节） |
|---------|------|------|
| **正确性** | 9/10 | 消除主要竞态：C1 无条件 snapshot（13.2）、X1/X2/X4 加锁+arbiter（13.3）、X3 补 import（13.1）、X5/X6 B 机制注入（13.4） |
| **性能** | 9/10 | 共享连接池复用（13.2.1），每任务 snapshot 开销极小；软配额无锁竞争（13.6） |
| **安全性** | 9/10 | HITL cancel-vs-resolve 竞态已补检查；文件写仲裁（13.3）不静默覆盖 |
| **可维护性** | 9/10 | 6 层分层清晰（v1.7 已与 12/13 章对齐）；B 机制 SRP/DRY 集中 |
| **可扩展性** | 8/10 | 单 provider 约束需标注（12.3 M1 等 Zen 多 provider） |
| **总分** | **8.8/10** | **可实施：P0-P6（12.6）+ 13.1-13.6 真实 Diff 可达** |

---

## 十二、多会话并行任务必要性分级与新方案骨架（v1.6 补充）—— 小欧 2026-09-20

> **术语（北京老陈 2026-09-20 定）**：本文档目标 = **「多会话并行任务」**：每个会话各自持有任务，**跨会话并发执行**；同一会话内**维持串行续聊**（linked 链），不放开同会话并发。
> 背景：北京老陈追问「没有实施的这些是多会话并行任务时都必须实施的吗？有没有更好的方案」。本文档按 **「多会话并行任务」** 为目标重新分级，非文档原 C/M/L 分级；结论：不是全部必须，核心是跨任务文件冲突与确定性 bug。

### 12.1 必须修（不做并行必崩/丢数据）

| 项 | 原分级 | 为什么躲不掉 |
|----|--------|-------------|
| **X3** task_runtime 缺 `import asyncio` | HIGH | 确定性 bug，暂停超时分支直接 `NameError`，与并行无关也必修 |
| **X2/L1** 跨任务文件冲突 | LOW | **并行最大的坑**：**会话A任务与会话B任务同时写同一文件** → 数据互相覆盖，无检测无告警 |
| **X1** file_state 无锁 | HIGH | X2 的地基——冲突检测本身要先锁，否则 mtime 缓存先被污染 |
| **X4** registry 备份 dict 无锁 | MEDIUM | 会话A/会话B同时读写注册表备份 → `_registry_session_backup` 竞态覆盖 |
| **C1** LLM 单例竞态 | CRITICAL | **依然是最大定时炸弹**（v1.6 更正）：snapshot 仅覆盖「有 sessionModel 覆盖的会话」（resolver.py:144 判据为 `_ov.model or _ov.provider`），无覆盖会话并行时 `agent.llm_client` 仍指向全局单例（stream_orchestrator.py:328-333），单例 `_cancelled/_current_response/_stop_check`（base_service.py:122-124）单槽位被多会话轮流覆盖，会话A取消会误关会话B正在用的流 |

### 12.2 应该修（并行质量，可排后）

| 项 | 原分级 | 说明 |
|----|--------|------|
| **X5/X6** 同会话串行续聊 | MEDIUM | **保留实施**（v1.4 曾误判「与并行相反」，更正；v1.7 定案 B 机制）：目标维持同会话串行续聊，**同会话 1 任务串行 + 运行中注入 inbox（13.4）** 恰好保障该语义，只限本会话、不碰跨会话 |
| **H1** 删会话取消任务 | HIGH | 任务跑着 session 被删 → 孤儿任务继续写死数据 |
| **全局 LLM 并发总量** | — | 吸收 B 文档（多Session并发系统架构设计方案）SessionQuota 思想，但**不做硬闸 max_concurrent=2**，用「连接池+轻量信号量」软限制——多会话同时调用时防止 LLM 请求总量击穿 |

### 12.3 可暂缓（YAGNI，非并行刚需）

| 项 | 原分级 | 理由 |
|----|--------|------|
| **C2** config 写锁 | CRITICAL | 配置修改是低频管理操作，与任务并发解耦，撞车概率≈0 |
| **M1** httpx 共享 headers | MEDIUM | 当前单 provider，等 Zen 多 provider 真正落地再接 |
| **M6** MetricsCollector 锁 | MEDIUM | 单进程 asyncio 下 GIL 兜底，多 worker 才崩 |
| **L3** 内存小泄漏 | LOW | 一条 dict，进程生命周期内可忽略 |

### 12.4 新方案骨架（多会话并行：会话间并行、会话内串行）

```
共享 httpx.AsyncClient 连接池（唯一一份，max_conn=10）   ← 连接复用
        │
        ● 每任务调用前 snapshot() 无条件构造独立 BaseAIService
        │           （v1.6: 不再仅限「有 sessionModel 覆盖」的会话）
        ▼
会话A任务 ─┐
           ├→ 独立 `_cancelled/_current_response/_stop_check`   ← C1 落地
会话B任务 ─┘       │
                   │  同会话串行（X5/X6 保留，本会话同一时刻 1 任务；
                   │   运行中注入 inbox，每轮 LLM 前合并吸收 = 13.4 B机制）
                   │  跨任务文件写集合（新增，加锁 = 12.1 X1/X2/X4 落地）
                   ▼
            写文件前 CHECK：该文件正被哪个会话/任务写？
                   ├─ 无冲突 → 放行，登记 in-flight
                   └─ 有冲突 → 告警/排队（不静默覆盖）
                   │
                   ▼
            全局 LLM 软配额（连接池感知，非硬闸 = 12.2）
                   │
                   ▼
            删除 session → 级联 cancel 其下任务（12.2 H1）
```

### 12.5 与 [55] 原设计的差异（四点）

1. **跨任务文件冲突检测 X2** 由 L1 低危 **提升为最高优先级**，做成核心模块 `file_write_arbiter`（锁 + 登记 + 排队），不再混在 Phase2 里排后
2. 用**软配额**替代 B 文档的硬 `max_concurrent` 闸，避免多会话任务排队超时降级 Text（丢 LLM 能力）
3. **X5/X6 保留**（v1.4 曾写「改为每任务独立不阻碍并行」，v1.5 更正保留串行语义，v1.7 定 B 机制）——同会话 1 任务串行 + 运行中注入 inbox（13.4）是目标语义，不是要被消除的阻碍，也不是旧"前端禁用+后端拒绝"闸门
4. **C1 回调为必须修**（v1.6 更正）：snapshot 机制改用「**无条件 snapshot（状态分离）+ 共享 httpx 连接池（连接复用）**」，根除单例单槽位竞态，同时不丢连接池收益

### 12.6 落地 Phase（v1.6 重排，P3 于 v1.7 定为 B 机制注入）

| Phase | 内容 | 依赖 |
|-------|------|------|
| P0 | X3 补 `import asyncio`（一行） | 无 |
| P1 | **C1 无条件 snapshot + 共享连接池**（`_ensure_client` 支持注入外部 client；resolver 判据放开） | P0 |
| P2 | `file_write_arbiter`：写前 CHECK + in-flight 登记（含 X1/X4 锁） | P1 |
| P3 | X5/X6 同会话串行 + 运行中注入（**B 机制**：`has_active_task_in_session`/`inject_message_to_task`/inbox，13.4） | P2 |
| P4 | H1 删会话级联取消任务 | P3 |
| P5 | 全局 LLM 软配额（连接池感知） | P4 |
| P6 | C2/M1/M6/L3 按需补（YAGNI，不主动做） | — |

### 12.7 多会话并行**最终推荐架构**（v1.6 新增，北京老陈「最优架构」之问的落笔）

```
┌────────────────────────── session A ──────────────────────────┐
│  chat_stream → snapshot(共享连接池) → 独立A状态(取消/响应/停检) │
│          task_A(react) → file_write_arbiter → 会话内串行+注入   │
└───────────────────────────────────────────────────────────────┘
┌────────────────────────── session B ──────────────────────────┐
│  chat_stream → snapshot(共享连接池) → 独立B状态(取消/响应/停检) │
│          task_B(react) → file_write_arbiter → 会话内串行+注入   │
└───────────────────────────────────────────────────────────────┘
        ▲                        ▲                        ▲
        │ 全部指向同一            │ 全部指向同一            │ 全局级联
        ▼                        ▼                        ▼
共用连接池与进程单例         file_write_arbiter         session销毁→cancel
(httpx 连接复用,             (登记/排队/告警,          (H1, 防孤儿任务)
 单例仅作默认引用)            X1/X2/X4 落地)

设计三原则（对应老陈问的「最合理」）：
 ① 状态不共享（每任务 snapshot）    —— 并行安全的地基，防 cancel 误杀
 ② 资源可共享（连接池/文件登记）    —— 连接复用 + 冲突可见（软配额承载）
 ③ 会话内串行、会话间并行（X5/X6） —— 目标语义：同会话续聊不乱，跨会话尽情并行；
                                     运行中注入（inbox 合并，13.4）不打断、不拒绝、不排队
```

## 十三、多会话并行详细设计与真实代码 Diff（v1.7）—— 小欧 2026-09-20

> 前置铁律：本章所有 diff 的「-行」与「+行」均**逐字对照本地最新源码取证**（取证时间 2026-09-20 04:19 前后）。`File:` 行为源码绝对路径，diff 头含**读取时的真实行号**，实施落地时以实际行号为参考、以「-内容完全一致」为准绳。所有新增代码带署名+日期注释（AGENTS.md 铁规）。

### 13.0 落地项一览（对应 12.6 Phase）

| 小节 | Phase | 内容 | 涉及文件 |
|------|-------|------|----------|
| 13.1 | P0 | X3 补 `import asyncio` | task_runtime.py |
| 13.2 | P1 | C1 无条件 snapshot + 共享连接池 | base_service.py / client_sdk.py / lifecycle/service.py / resolver.py / stream_orchestrator.py / agent_runner.py |
| 13.3 | P2 | `file_write_arbiter` 跨任务文件仲裁 + X1/X4 锁 | 新建 arbiter/`file_write_arbiter.py` / file_state.py / registry_read.py |
| 13.4 | P3 | X5/X6 同会话串行 + 运行中注入（B 机制：inbox 队列，每轮 LLM 前合并） | task_registry.py / react_step.py / stream_orchestrator.py |
| 13.5 | P4 | H1 删会话级联取消任务 | session_service.py / task_registry.py |
| 13.6 | P5 | 全局 LLM 软配额 | base_service.py / client_sdk.py |
| 13.7 | — | 落地顺序 / 验证 / 回滚 | — |

---

### 13.1 P0：X3 补 `import asyncio`（一行，必做）

**现状实证**（`backend/app/services/task/task_runtime.py`）：
- 第 32-34 行 import 段：
  ```python
  import time
  from datetime import datetime
  from typing import Optional, AsyncGenerator
  ```
- 第 198-204 行 `_pause_core` 内**已使用** `asyncio`：
  ```python
      try:
          if timeout:
              await asyncio.wait_for(pause_event.wait(), timeout=timeout)
          else:
              await pause_event.wait()
      except asyncio.TimeoutError:
  ```

**诊断**：`asyncio.wait_for` / `asyncio.TimeoutError` 均已真实调用，但 `asyncio` 从未 import → 一旦 `task_pause_check(task_id, timeout=300)` 进入暂停超时分支（`task_pause_check_and_yield` 传 timeout=300，暂停超 300s 时必然触发）即 **NameError: name 'asyncio' is not defined**。

**Diff**（File: `backend/app/services/task/task_runtime.py`，取证实测第 32 行 `import time` 前插入）

```diff
--- a/backend/app/services/task/task_runtime.py
+++ b/backend/app/services/task/task_runtime.py
@@ -32,6 +32,7 @@
+import asyncio  # 2026-09-20 小欧 X3修复: _pause_core 已用 asyncio.wait_for/TimeoutError 却未import, 暂停超时均NameError — 小欧-2026-09-20
 import time
 from datetime import datetime
 from typing import Optional, AsyncGenerator
```

**验证**：`pytest -k "task or pause"` 后端回归；手工 `python -c "import app.services.task.task_runtime"` 零报错。

---

### 13.2 P1：C1 无条件 snapshot + 共享 httpx 连接池

**问题根（12.1 实证）**：`resolve_session_client`（`backend/app/services/model/resolver.py:132-192`）仅在 `_ov and (_ov.model or _ov.provider)` 即「有 sessionModel 覆盖」时构造快照；无覆盖会话的 `agent.llm_client` 仍是全局单例 `get_service()`（`stream_orchestrator.py:325` → `agent = UniversalAgent(llm_client=ai_service, ...)`），单例 `_cancelled/_current_response/_stop_check`（`base_service.py:122-124`）单槽位 → 多会话并行竞态。

**设计（12.7 ①状态不共享 + ②资源可共享）**：
1. `BaseAIService` 增加**共享连接池注入**：`_llm_sdk` 可由外部传入共享 `LLMClient`（或共享 `httpx.AsyncClient`），构造/`_ensure_client` 不再无条件 new。
2. `resolve_session_client` **无条件 snapshot**：无覆盖时也返回 `ai_service.snapshot()`（状态分离），但该快照**复用全局连接池**（不 new httpx client）。
3. `run_agent_in_background` finally（`agent_runner.py:520-527`）只关闭**独占**快照的连接池，共享池快照静默传递（由单例生命周期统一关闭）。

#### 13.2.1 File: `backend/app/llm/client_sdk.py` —— LLMClient 支持外部注入 httpx 连接池

现状（取证实测）：
- 第 123-136 行 `LLMClient.__init__` 无条件 `self._client = httpx.AsyncClient(...)`
- 第 252-259 行 `create_llm_client` 是**唯一入口**

```diff
--- a/backend/app/llm/client_sdk.py
+++ b/backend/app/llm/client_sdk.py
@@ -108,6 +108,7 @@
     def __init__(
         self,
         llm_model: ModelRef,
         api_key: str,
         base_url: Optional[str] = None,
         timeout: Optional[int] = None,
+        shared_client: Optional[httpx.AsyncClient] = None,  # 2026-09-20 小欧 C1: 共享连接池注入, 快照复用不 new — 小欧-2026-09-20
     ):
         self.llm_model = llm_model   # 前导+model 命名铁律 — 小欧 2026-08-22
         self._api_key = api_key
@@ -121,20 +122,25 @@
         self._default_timeout = read_timeout
-        self._client = httpx.AsyncClient(
-            timeout=httpx.Timeout(
-                connect=DEFAULT_CONNECT_TIMEOUT,
-                read=read_timeout,
-                write=DEFAULT_WRITE_TIMEOUT,
-                pool=DEFAULT_POOL_TIMEOUT,
-            ),
-            limits=httpx.Limits(
-                max_connections=LLM_MAX_CONNECTIONS,
-                max_keepalive_connections=LLM_MAX_KEEPALIVE,
-            ),
-            headers={"Authorization": f"Bearer {api_key}"},
-            base_url=self._base_url,
-        )
+        self._owns_client = shared_client is None   # 真连接池仅全局单例持有, 快照共享不重复建 — 小欧-2026-09-20
+        if shared_client is not None:
+            self._client = shared_client
+        else:
+            self._client = httpx.AsyncClient(
+                timeout=httpx.Timeout(
+                    connect=DEFAULT_CONNECT_TIMEOUT,
+                    read=read_timeout,
+                    write=DEFAULT_WRITE_TIMEOUT,
+                    pool=DEFAULT_POOL_TIMEOUT,
+                ),
+                limits=httpx.Limits(
+                    max_connections=LLM_MAX_CONNECTIONS,
+                    max_keepalive_connections=LLM_MAX_KEEPALIVE,
+                ),
+                headers={"Authorization": f"Bearer {api_key}"},
+                base_url=self._base_url,
+            )
```

> 注：`api_key` 是 `__init__` 首参、用于 `httpx.AsyncClient(headers=...)`。共享 client 时 headers 由**全局单例**构造者写入，快照若跨 provider（api_key 不同）仍需独占 client → 本设计仅「同 provider 无覆盖」会话走共享，跨 provider 覆盖仍走独占（`13.2.2` 判据）。

#### 13.2.2 File: `backend/app/llm/client_sdk.py` —— `create_llm_client` 透传共享 client

```diff
--- a/backend/app/llm/client_sdk.py
+++ b/backend/app/llm/client_sdk.py
@@ -252,6 +256,8 @@
 def create_llm_client(
     llm_model: ModelRef,
     api_key: str,
     base_url: Optional[str] = None,
     timeout: Optional[int] = None,
+    shared_client: Optional[httpx.AsyncClient] = None,  # 2026-09-20 小欧 C1 透传 — 小欧-2026-09-20
 ) -> LLMClient:
     """创建 LLM 客户端 — 唯一入口 - 小沈 2026-06-09; 2026-08-22 小欧 归一: 入参 llm_model: ModelRef"""
-    return LLMClient(llm_model=llm_model, api_key=api_key, base_url=base_url, timeout=timeout)
+    return LLMClient(llm_model=llm_model, api_key=api_key, base_url=base_url, timeout=timeout, shared_client=shared_client)
```

#### 13.2.3 File: `backend/app/llm/base_service.py` —— `_ensure_client` 注入共享池 + `close` 区分所有权

现状（取证实测）：
- 第 126-134 行 `_ensure_client`：`if self._llm_sdk is None: self._llm_sdk = create_llm_client(...)`
- 第 553-555 行 `close`：`if self._llm_sdk: await self._llm_sdk.close()`

```diff
--- a/backend/app/llm/base_service.py
+++ b/backend/app/llm/base_service.py
@@ -126,9 +130,20 @@
     def _ensure_client(self):
         if self._llm_sdk is None:
             # 归一(小欧 2026-08-23): create_llm_client 入参已改 llm_model: ModelRef,
             # 不再接受 provider/model 拆包; base_url 由 LLMClient.__init__ 从 llm_model.api_base 派生
             self._llm_sdk = create_llm_client(
                 llm_model=self.llm_model,
                 api_key=self.api_key,
                 timeout=self.timeout,
+                # 2026-09-20 小欧 C1: 快照优先复用全局共享连接池(同 provider 时), 免除重复建池 —
+                #   global 共享池地址经 snapshot 时注入(见 snapshot diff); 无共享则建独占池 — 小欧-2026-09-20
+                shared_client=getattr(self, "_shared_client", None),
             )
+
+    def set_shared_client(self, client) -> None:
+        """注入全局共享 httpx 连接池(仅 snapshot 构造时调用) — 小欧 2026-09-20 C1"""
+        self._shared_client = client
@@ -553,6 +570,8 @@
     async def close(self):
+        # 2026-09-20 小欧 C1: 共享池 snapshot 不关连接池(全局单例生命周期统一关), 仅独占池才真关 — 小欧-2026-09-20
+        if getattr(self, "_llm_sdk", None) is not None and getattr(self._llm_sdk, "_owns_client", False) is False:
+            return
         if self._llm_sdk:
             await self._llm_sdk.close()
```

#### 13.2.4 File: `backend/app/services/lifecycle/service.py` —— 给全局单例暴露共享客户端引用

现状（取证实测）：
- 第 111-113 行 `create_service_instance` 构造 `BaseAIService(api_key=..., llm_model=..., ...)`，构造时不立即建 sdk（惰性 `_ensure_client`）。
- 第 143 行 `_instance = create_service_instance(...)`。

```diff
--- a/backend/app/services/lifecycle/service.py
+++ b/backend/app/services/lifecycle/service.py
@@ -143,6 +143,13 @@
             provider_config = get_provider_config(ai_config, config_model.provider)
 
             _instance = create_service_instance(provider_config, config_model.provider, config_model.model)
+
+            # 2026-09-20 小欧 C1: 惰性触发单例首次建池(复用原 _ensure_client 路径),
+            #   并暴露共享 client 引用供 snapshot 复用 — 小欧-2026-09-20
+            _instance._ensure_client()
+            _shared_llm_client = getattr(_instance, "_llm_sdk", None)
+            _instance.set_shared_client(_shared_llm_client)
```

#### 13.2.5 File: `backend/app/services/model/resolver.py` —— `resolve_session_client` 无条件快照

现状（取证实测）：
- 第 139 行 `if not session_id: return None`（这是"无会话"合法直接跳过，不是"无覆盖跳过"）
- 第 142-144 行：`_ov = await db.atxn(...)`；`if _ov and (_ov.model or _ov.provider):` —— **只有覆盖才快照**
- 第 190-192 行：`except ... return None` —— 异常放弃

```diff
--- a/backend/app/services/model/resolver.py
+++ b/backend/app/services/model/resolver.py
@@ -141,7 +144,9 @@
     if not session_id:
         return None
     try:
         # 落库 offload 出事件循环(后端卡死修复收尾 小欧 2026-08-24)
         _ov = await db.atxn("chat", lambda conn: get_session_model(conn, session_id))
-        if _ov and (_ov.model or _ov.provider):
+        # 2026-09-20 小欧 C1(v1.7): 无条件快照——无论有无覆盖都构造独立 BaseAIService(状态分离),
+        #   有覆盖按原路径查目标 provider 配置; 无覆盖仅派生全局默认, snapshot 复用全局共享连接池 — 小欧-2026-09-20
+        if _ov and (_ov.model or _ov.provider):
             # 病根修复(小沈 2026-08-29): ...
             _pv_cfg = None
             _pv_key = None
@@ -179,6 +184,20 @@
             session_client = ai_service.snapshot(
                 override_ref,
                 api_key=_pv_key,
                 extra_body_params=_pv_ebp,
                 context_limit=_pv_ctx,
             )
+            # 2026-09-20 小欧 C1: 跨 provider(api_key 不同)时保留独占池; 同 provider 无覆盖时复用全局共享池
+            #   (13.2.1 判据: shared 由全局单例持有, 快照传 own client 引用即复用不 re-create)
+            _shared_llm = getattr(ai_service, "_shared_client", None)
+            if _shared_llm is not None and (not _ov.provider or _ov.provider == ai_service.llm_model.provider):
+                session_client._llm_sdk = _shared_llm
+                logger.info(f"[chat] C1 快照复用共享连接池(session={session_id}, model={session_client.llm_model.model})")
             # 2026-09-01 小欧: 同步 _task_llm_model ...
             logger.info(f"[chat] L2 sessionModel 已生效(独立客户端快照): session={session_id}, "
                         f"provider={session_client.llm_model.provider}, model={session_client.llm_model.model}")
             return session_client
+        # ---- 无条件快照新增分支(无覆盖) ----
+        logger.info(f"[chat] C1 无覆盖会话快照(session={session_id})")
+        _shared_llm = getattr(ai_service, "_shared_client", None)
+        _snap = ai_service.snapshot()
+        if _shared_llm is not None:
+            _snap._llm_sdk = _shared_llm   # 复用全局共享连接池, 不 new
+        return _snap
     except Exception as _ov_e:
         logger.warning(f"[chat] 读会话sessionModel失败(session={session_id}): {_ov_e}")
     return None
```

#### 13.2.6 File: `backend/app/services/agent/agent_runner.py` —— finally 释放区分独占/共享

现状（取证实测）：
- 第 520-527 行 finally：`_snap_client = getattr(agent, "llm_client", None)`；`if _snap_client is not None and getattr(_snap_client, "_is_snapshot", False): await _snap_client.close()`。

```diff
--- a/backend/app/services/agent/agent_runner.py
+++ b/backend/app/services/agent/agent_runner.py
@@ -520,6 +520,9 @@
         # 关闭本任务持有的独立客户端快照(若有), 释放其 httpx 连接池, 防覆盖会话累积泄漏 — 小沈 2026-08-29
         _snap_client = getattr(agent, "llm_client", None) if agent is not None else None
         if _snap_client is not None and getattr(_snap_client, "_is_snapshot", False):
+            # 2026-09-20 小欧 C1: 共享池快照 close 由 13.2.3 base_service.close 判据兜底(共享不真关),
+            #   独占池快照照常释放 — 完全兼容原逻辑 — 小欧-2026-09-20
             try:
                 await _snap_client.close()
                 logger.info(f"[Runner] 会话客户端快照已关闭(task={task_id})")
```

> **13.2 无覆盖快照的前端副作用**：`snapshot()` 自带 `_is_snapshot=True`，运行时模型不变（仍全局默认），仅状态槽位独立——`cancel`/`reset_cancel`/`set_stop_check` 不再跨会话误触（12.1 实证的单槽位竞态根除）。TASK_START 打印与 `_task_llm_model` 均不受影响（值完全一致）。

---

### 13.3 P2：`file_write_arbiter` 跨任务文件仲裁 + X1/X4 锁

#### 13.3.1 新建 `backend/app/tools/file/file_write_arbiter.py`

**设计**（12.7 ②资源可共享/冲突可见）：进程内 `threading.Lock` 保护「in-flight 写登记表」`{resolved_path: {task_id, acquired_at}}`；写文件前 `acquire_write(path, task_id)` 检查是否被他任务占用，冲突则返回占用者（告警/排队决策由调用工具层做，arbiter 只仲裁不覆盖）。

```python
# -*- coding: utf-8 -*-
# 编辑历史:
# 2026-09-20 - 小欧 - 新建: v1.7 12.7② 跨任务文件写仲裁(跨会话并行安全核心, X2 落地)。
#   仅仲裁不强制: 冲突返回占用者信息, 由工具层(TaskFileWriter)决定告警/排队/拒绝, 杜绝静默覆盖。
"""
file_write_arbiter — 跨任务/跨会话文件写仲裁(进程内单例)

职责(X2 落地, [55] 12.1 P1): 记录并检查「正在被谁写入」的文件集合。
- 线程安全: threading.Lock 保护登记表(sync 工具在 asyncio 事件循环线程内以同步调用执行, 线程锁正确)。
- 不强制阻断: 冲突时返回占用者 (task_id, acquired_at), 调用方按策略处理, arbiter 绝不自动覆盖。
小欧 2026-07-05(参考 file_state 既有持久口径) / 2026-09-20 扩展
"""
import threading
import time as _time
from pathlib import Path
from typing import Dict, Optional, Tuple

from app.logger import logger

# {resolved_path_str: {"task_id": str, "acquired_at": float}}
_write_claims: Dict[str, Dict] = {}
_claims_lock = threading.Lock()


def acquire_write(file_path: str, task_id: str) -> Optional[str]:
    """登记文件正被 task_id 写入 → 返回 None=登记成功(无冲突); str=冲突, 返回占用者 task_id — 小欧 2026-09-20"""
    key = str(Path(file_path).resolve())
    with _claims_lock:
        holder = _write_claims.get(key)
        if holder is not None and holder["task_id"] != task_id:
            return holder["task_id"]
        _write_claims[key] = {"task_id": task_id, "acquired_at": _time.time()}
        return None


def release_write(file_path: str, task_id: str) -> None:
    """任务写完释放登记(仅释放本人占用, 防跨任务误删) — 小欧 2026-09-20"""
    key = str(Path(file_path).resolve())
    with _claims_lock:
        holder = _write_claims.get(key)
        if holder is not None and holder["task_id"] == task_id:
            del _write_claims[key]


def who_writes(file_path: str) -> Optional[Tuple[str, float]]:
    """查询当前占用者(task_id, acquired_at), 无占用返回 None — 小欧 2026-09-20"""
    key = str(Path(file_path).resolve())
    with _claims_lock:
        holder = _write_claims.get(key)
        if holder is None:
            return None
        return (holder["task_id"], holder["acquired_at"])


__all__ = ["acquire_write", "release_write", "who_writes"]
```

#### 13.3.2 File: `backend/app/tools/file/file_state.py` —— X1 补线程锁

现状（取证实测）：第 8-15 行 `import hashlib ... _state: Dict[str, Tuple[int, str]] = {}`，全部函数直接读写 `_state` 无锁。

```diff
--- a/backend/app/tools/file/file_state.py
+++ b/backend/app/tools/file/file_state.py
@@ -11,6 +11,7 @@
 import hashlib
+import threading  # 2026-09-20 小欧 X1修复: 多会话并行时 file_state 无锁, 跨任务并发读写污染 mtime 缓存 — 小欧-2026-09-20
 from pathlib import Path
 from typing import Dict, Optional, Tuple
 
 from app.logger import logger
 
 # {resolved_path_str: (mtime_ns, content_hash)}
 _state: Dict[str, Tuple[int, str]] = {}
+_state_lock = threading.Lock()  # 2026-09-20 小欧 X1: 保护 _state 全部读写 — 小欧-2026-09-20
```

四个读改写函数分别加锁（每处仅改函数体首尾）：

```diff
 def record_read(file_path: str, content: str) -> None:
@@
-    _state[key] = (mtime, h)
+    with _state_lock:  # X1
+        _state[key] = (mtime, h)
```

```diff
 def record_write(file_path: str) -> None:
@@
-    if key in _state:
-        old_mtime, old_hash = _state[key]
-        _state[key] = (mtime, old_hash)
-    else:
-        _state[key] = (mtime, "")
+    with _state_lock:  # X1
+        if key in _state:
+            old_mtime, old_hash = _state[key]
+            _state[key] = (mtime, old_hash)
+        else:
+            _state[key] = (mtime, "")
```

```diff
 def check_conflict(file_path: str) -> Optional[str]:
@@
-    recorded = _state.get(key)
-    if recorded is None:
-        return None
-    recorded_mtime, _ = recorded
+    with _state_lock:  # X1
+        recorded = _state.get(key)
+        if recorded is None:
+            return None
+        recorded_mtime, _ = recorded
```

```diff
 def is_unchanged(file_path: str, content: str) -> bool:
@@
-    recorded = _state.get(key)
-    if recorded is None:
-        return False
-    _, recorded_hash = recorded
-    if not recorded_hash:
-        return False
-    return hashlib.md5(content.encode("utf-8")).hexdigest() == recorded_hash
+    with _state_lock:  # X1
+        recorded = _state.get(key)
+        if recorded is None:
+            return False
+        _, recorded_hash = recorded
+        if not recorded_hash:
+            return False
+        return hashlib.md5(content.encode("utf-8")).hexdigest() == recorded_hash
```

```diff
 def check_conflict_strict(file_path: str) -> Optional[str]:
@@
-    recorded = _state.get(key)
-    if recorded is None:
-        return None
-    recorded_mtime, _ = recorded
+    with _state_lock:  # X1
+        recorded = _state.get(key)
+        if recorded is None:
+            return None
+        recorded_mtime, _ = recorded
```

```diff
 def clear_state(file_path: str) -> None:
@@
-    _state.pop(key, None)
+    with _state_lock:  # X1
+        _state.pop(key, None)
```

#### 13.3.3 File: `backend/app/tools/win_registry/registry_read.py` —— X4 备份缓存加锁

现状（取证实测）：第 36 行 `_registry_session_backup = {}`，第 56-84 行 `_backup_registry` 读写无锁。

```diff
--- a/backend/app/tools/win_registry/registry_read.py
+++ b/backend/app/tools/win_registry/registry_read.py
@@ -14,6 +16,8 @@
 import os
+import threading  # 2026-09-20 小欧 X4: 多会话并行时 _registry_session_backup 跨任务读写竞态 — 小欧-2026-09-20
 import subprocess
 import tempfile
@@
 _registry_session_backup = {}
+_registry_backup_lock = threading.Lock()  # 2026-09-20 小欧 X4: 保护备份缓存 — 小欧-2026-09-20
@@
 def _backup_registry(root_key: str, sub_key: str, session_id: str) -> str:
     """备份注册表键到临时文件 — 小健 2026-05-19"""
     backup_key = f"{root_key}\\{sub_key}"
-    if backup_key in _registry_session_backup:
-        return _registry_session_backup[backup_key]
+    with _registry_backup_lock:  # X4
+        if backup_key in _registry_session_backup:
+            return _registry_session_backup[backup_key]
@@
-        if result.returncode == 0 and os.path.exists(backup_file):
-            _registry_session_backup[backup_key] = backup_file
-            logger.info(f"[registry] 备份成功: {backup_key} -> {backup_file}")
+        if result.returncode == 0 and os.path.exists(backup_file):
+            with _registry_backup_lock:  # X4
+                _registry_session_backup[backup_key] = backup_file
+            logger.info(f"[registry] 备份成功: {backup_key} -> {backup_file}")
```

---

### 13.4 P3：X5/X6 同会话串行化闸门 + 运行中注入（B 机制，北京老陈 2026-09-20 定案）

**设计**（12.7 ③会话内串行、会话间并行；**2026-09-20 北京老陈定案：同会话已有活跃任务时，新消息「注入当前任务 loop」（机会②：每轮 LLM 调用前合并），不是排队也不是拒绝**）：

- 同会话**同时只跑一个任务**（L5 语义：1 串行，弃 3 并发）。
- 任务执行中新消息：**不新建任务**，写入当前活跃任务的 **inbox 队列**（可多条）。
- agent 在**每一轮 LLM 调用前**（`react_step.py` `prepare_messages_for_llm()` 之前）把 inbox 积压的 **N 条新 user 消息一并 `add_user_message` 进对话历史**，本轮 LLM 请求自然带上——覆盖**所有轮**（工具轮+answer 轮+重试轮），answer 终态轮也不漏。
- **机制边界**（确认接受）：工具执行期间来的新消息**不能打断当前工具**（文件写一半没法收），等该工具跑完、进入下一轮 LLM 前被吸收。LLM 生成中来的新消息同理，等本轮生成结束、下一轮 LLM 调用前才可见（机会②在 LLM 前合并，天然满足）。
- 无活跃任务时：走正常新建任务路径（不受影响）。
- 跨会话互不等待、并行执行。

> 现成基元（取证实测）：
> - `running_tasks[task_id]` 含 `status/created_at/ai_service`（task_registry.py）但**未存 session_id、无 inbox** → 本小节一并补。
> - `MessageBuilder.add_user_message(content)` 已存在（message_builder.py:119-123），吸收侧直接复用，零新造。
> - `react_step.py:240` `messages = agent.message_builder.prepare_messages_for_llm()` —— 每轮 LLM 调用前统一消息装配点，inbox 合并落在此处。
> - `StreamBuffer`（task_state.py）承担 SSE 事件管道，inbox 是**独立于** StreamBuffer 的纯消息队列（StreamBuffer 管出、inbox 管进，互不干扰）。

#### 13.4.1 File: `backend/app/services/task/task_registry.py` —— register_task 记 session_id + 每任务 inbox 队列 + 注入/查询/吸收接口

现状（取证实测）：
- 第 41-53 行 `register_task(task_id, ai_service)` 无 session_id、无 inbox。
- **import 现状**：`from typing import Any, Optional`（第 16 行）——本小节新增 3 函数用到 `List`，须一并补 import（落地 NameError 预防）。

```diff
 语法区 — 首尾两处补丁:
@@ 16: typing import 段
-from typing import Any, Optional
+from typing import Any, List, Optional   # 2026-09-20 小欧 13.4.1: drain_inbox 返回 List[str] 所需 — 小欧-2026-09-20

@@ -41,7 +41,7 @@
-async def register_task(task_id: str, ai_service: Any) -> None:
-    """注册任务到 running_tasks"""
+async def register_task(task_id: str, ai_service: Any, session_id: Optional[str] = None) -> None:
+    """注册任务到 running_tasks — 小欧 2026-09-20 X5: 增 session_id + 任务级 inbox(运行中注入) — 小欧-2026-09-20"""
     async with running_tasks_lock:
         running_tasks[task_id] = {
             "status": "running",
             "cancelled": False,
             "paused": False,
+            "session_id": session_id,          # X5/X6 同会话判定
+            "_inbox": asyncio.Queue(),         # B机制: 运行中注入消息队列(多条), agent 侧每轮 LLM 调用前合并吸收
             "created_at": datetime.now(),
             "ai_service": ai_service,
             "_task": asyncio.current_task(),
             "_pause_event": asyncio.Event(),
         }
         running_tasks[task_id]["_pause_event"].set()
+
+
+async def has_active_task_in_session(session_id: str) -> Optional[str]:
+    """X5/B: 该会话是否已有活跃任务(running/paused)。返回活跃 task_id(供注入); 无则 None。 — 小欧 2026-09-20"""
+    if not session_id:
+        return None
+    async with running_tasks_lock:
+        for _tid, _t in running_tasks.items():
+            if _t.get("session_id") == session_id and _t.get("status") in ("running", "paused"):
+                return _tid
+    return None
+
+
+async def inject_message_to_task(task_id: str, content: str) -> bool:
+    """B机制: 向运行中任务 inbox 投递一条新用户消息(不打断工具执行, 由 agent 下一轮 LLM 调用前合并吸收)。
+    返回 True=已投递; False=任务不存在/已终态(投递失败, 由编排降级为新任务)。 — 小欧 2026-09-20"""
+    async with running_tasks_lock:
+        _t = running_tasks.get(task_id)
+        if not _t or _t.get("status") not in ("running", "paused"):
+            return False
+        _q = _t.get("_inbox")
+        if _q is None:
+            return False
+        _q.put_nowait(content)
+    return True
+
+
+async def drain_inbox(task_id: str) -> List[str]:
+    """B机制: agent 侧取走 inbox 全部积压消息(每轮 LLM 调用前合并吸收)。返回消息列表(可为空)。 — 小欧 2026-09-20"""
+    async with running_tasks_lock:
+        _t = running_tasks.get(task_id)
+        if not _t:
+            return []
+        _q = _t.get("_inbox")
+        if _q is None:
+            return []
+        _msgs = []
+        while not _q.empty():
+            try:
+                _msgs.append(_q.get_nowait())
+            except asyncio.QueueEmpty:
+                break
+    return _msgs
```

#### 13.4.2 File: `backend/app/services/agent/react_step.py` —— 每轮 LLM 调用前合并 inbox 新消息（机会②）

现状（取证实测）：
- 第 240 行 `messages = agent.message_builder.prepare_messages_for_llm()` —— 每轮唯一消息装配点（工具轮/answer 轮/重试轮都经过它）。

```diff
--- a/backend/app/services/agent/react_step.py
+++ b/backend/app/services/agent/react_step.py
@@ -238,6 +238,14 @@
     _first_token_marked = False           # 11.2-B 首 chunk 只记一次首包时延 — 小欧 2026-08-20
+    # 2026-09-20 小欧 B机制(北京老陈定案, 机会②): 每轮 LLM 调用前合并本任务 inbox 积压的新 user 消息
+    #   ——工具执行期间的新消息不打断工具(安全底线), 于下一轮 LLM 调用前并入本轮上下文, 全轮覆盖(含 answer 轮)不漏
+    #   [KISS] 单一吸收点(所有轮共用 prepare_messages_for_llm 这一处), 无积压则零开销跳过 — 小欧-2026-09-20
+    from app.services.task.task_registry import drain_inbox
+    _injected = await drain_inbox(agent.task_id)
+    for _m in _injected:
+        agent.message_builder.add_user_message(_m)
+        logger.info(f"[B] 每轮LLM前吸入新消息: task={agent.task_id} len={len(_m)}")
     messages = agent.message_builder.prepare_messages_for_llm()
```

> **为什么选①...②（每轮 LLM 前）而非③ (observation 后)**（北京老陈 2026-09-20 定案）：
> ① 全轮覆盖——③只在工具轮触发；若 LLM 下一轮直接 answer 终态，③漏吸用户消息（任务结束后 inbox 清空→丢失），② 在 answer 轮 LLM 前也合并，不漏。
> ② 不牺牲"同框"——对话历史累积：工具结果写入后、② 下一轮合并的新消息同批进历史，两者仍同框给 LLM（与③效果一致）。
> ③ 代码同样简单（drain_inbox+add_user_message 数行），② 位置更通用（单一入口），按 KISS + 增强不退化取 ②。

#### 13.4.3 File: `backend/app/services/chat/stream_orchestrator.py` —— 编排②后、register_task 前改为「有活跃任务则注入、无则新建」

现状（取证实测）：
- 第 162 行 import：`from app.services.task.task_registry import register_task` —— **本小节新增调用 `has_active_task_in_session`/`inject_message_to_task`，须一并补 import（落地 NameError 预防）**。
- 第 269 行 `task_id = generate_task_id()`
- 第 313-315 行：`buffer = create_stream_buffer(task_id)` → `await register_task(task_id, ai_service)`

```diff
--- a/backend/app/services/chat/stream_orchestrator.py
+++ b/backend/app/services/chat/stream_orchestrator.py
@@ 162: task_registry import 段
-from app.services.task.task_registry import register_task
+from app.services.task.task_registry import (
+    register_task,
+    has_active_task_in_session,   # 2026-09-20 小欧 13.4.3: B机制注入入口 — 小欧-2026-09-20
+    inject_message_to_task,
+)

@@ -312,6 +312,26 @@
     bg_task = None  # BUG-32修复: 预初始化, 防 except 块 NameError — 小沈 2026-08-13
     try:
+
+        # 2026-09-20 小欧 B机制(北京老陈定案): 同会话已有活跃任务时, 新消息注入该任务 inbox(可多条),
+        #   待其下一轮 LLM 调用前合并吸收; 无活跃任务时走正常新建任务。工具执行不被打断(安全底线)。 — 小欧-2026-09-20
+        _active_tid = await has_active_task_in_session(session_id)
+        if _active_tid:
+            _injected_ok = await inject_message_to_task(_active_tid, user_input)
+            if _injected_ok:
+                logger.info(f"[chat] 同会话运行中注入(session={session_id}, 目标task={_active_tid}, 新task={task_id}作废)")
+                yield create_error_response(error_type="injected", error_message="消息已注入当前执行中的任务，将在下一轮吸收")
+                return
+            # 注入失败(目标任务恰好终态): 降级新建任务(下述正常路径), 不丢消息
+            logger.warning(f"[chat] 注入失败(目标任务finish), 降级新建任务: session={session_id}, target={_active_tid}")
+
         buffer = create_stream_buffer(task_id)
-        await register_task(task_id, ai_service)
+        await register_task(task_id, ai_service, session_id=session_id)
```

> **前端配合（不落后端代码）**：`has_active_task_in_session` 供前端轮询展示"运行中注入"通道——任务执行期间发送框不禁用、而是改为"注入"模式（提示"将注入当前任务、下一轮吸收"），由 `serverTaskId` + 响应 `injected` error 类型派生。**后端 SSE 契约变化最小**：仅新增 `error_type="injected"` 一种响应形态（前端可静默忽略），主链路新建任务行为零改动。
>
> **排队后备**：若未来想限制注入条数/等待，可回归 13.4 旧 wait_for_session_available（本文档 2026-09-20 早版），当前定案不启用。

---

### 13.5 P4：H1 删会话级联取消任务

**设计**（12.2 H1）：`delete_session` 软删前，先取消该 session 下全部活跃任务，防孤儿任务在会话删除后继续写死数据。

#### 13.5.1 File: `backend/app/services/chat/session_service.py` —— delete_session 级联取消

现状（取证实测）：
- 第 194-213 行 `delete_session(session_id)` 仅软删 + 清 display_name 缓存。

```diff
--- a/backend/app/services/chat/session_service.py
+++ b/backend/app/services/chat/session_service.py
@@ -194,7 +194,27 @@
 def delete_session(session_id: str):
     """删除会话(软删) + 清理 display_name 缓存 — 自 api/v1/sessions.py 迁入, 缓存清理改经 message_service 方法"""
+    # 2026-09-20 小欧 H1: 删会话前级联取消该会话名下活跃任务(防孤儿任务继续写死数据) — 小欧-2026-09-20
+    _cancel_cascade(session_id)
     with db.get_conn("chat") as conn:
@@ -211,6 +231,26 @@
     delete_session_display_names(session_id)
     logger.info(f"删除会话成功: id={session_id}")
     return {"success": True, "message": "会话删除成功"}
+
+
+def _cancel_cascade(session_id: str) -> None:
+    """H1: 取消该会话下全部 running_tasks 中活跃任务(任务侧终态统一由 task_runtime 收尾) — 小欧 2026-09-20"""
+    try:
+        from app.services.task.task_state import running_tasks
+        from app.services.task.task_runtime import cancel_task
+        _ids = []
+        for _tid, _meta in list(running_tasks.items()):
+            if _meta.get("session_id") == session_id and _meta.get("status") in ("running", "paused"):
+                _ids.append(_tid)
+        for _tid in _ids:
+            try:
+                # sync 上下文(delete_session 是同步函数)下剥开协程: cancel_task 内部是 await 协程
+                import asyncio
+                try:
+                    _loop = asyncio.get_running_loop()
+                    _loop.create_task(cancel_task(_tid, session_id, "session_deleted"))
+                except RuntimeError:
+                    asyncio.run(cancel_task(_tid, session_id, "session_deleted"))
+                logger.info(f"[H1] 会话删除级联取消任务: session={session_id}, task={_tid}")
+            except Exception as _ce:
+                logger.warning(f"[H1] 级联取消失败(session={session_id}, task={_tid}): {_ce}")
+    except Exception as _e:
+        logger.warning(f"[H1] 级联取消遍历失败(session={session_id}): {_e}")
```

> 依赖：`register_task` 已记录 `session_id`（13.4.1 提供）。若 13.4 未先落地，`running_tasks[task_id].get("session_id")` 恒 None → 级联空转（不误伤其他会话，安全降级）。此为**唯一强依赖**，实施须 13.4→13.5 顺序。

---

### 13.6 P5：全局 LLM 软配额（连接池感知，非硬闸）

**设计**（12.2）：多会话并行时 LLM 请求集中在共享连接池上，用 `asyncio.Semaphore` 做**软并发上限**：超过水位排队等待（不拒绝、不降级 Text），超时仍进（保底放行，防长时间卡等）。

现状（取证实测）：`LLMClient._client` 由 `httpx.Limits(max_connections=LLM_MAX_CONNECTIONS=10, max_keepalive_connections=LLM_MAX_KEEPALIVE=5)`（client_sdk.py:130-133 实证；constants.py:89-90 实证）。`request/request_stream` 直接 `self._client.post/stream`，无信号量。

#### 13.6.1 File: `backend/app/llm/client_sdk.py` —— 信号量 + 软配额门

```diff
--- a/backend/app/llm/client_sdk.py
+++ b/backend/app/llm/client_sdk.py
@@ -103,6 +103,11 @@
-class LLMClient:
+_soft_pool_semaphore = asyncio.Semaphore(LLM_MAX_CONNECTIONS)  # 2026-09-20 小欧 P5: 全局软配额(共享池水位), 非硬闸 — 小欧-2026-09-20
+
+
+class LLMClient:
@@ -156,6 +165,8 @@
     async def request(
         self,
         messages: List[Dict],
@@ -175,6 +187,9 @@
-        response = await self._client.post("/chat/completions", json=body, timeout=_to)
+        async with _soft_pool_semaphore:  # 软配额: 并发超水位排队, 不拒绝不降级 — 小欧-2026-09-20
+            response = await self._client.post("/chat/completions", json=body, timeout=_to)
@@ -218,7 +233,9 @@
-        async with self._client.stream("POST", "/chat/completions", json=body, timeout=_timeout) as response:
+        async with _soft_pool_semaphore:  # 软配额(流式): 与 request 同门 — 小欧-2026-09-20
+            async with self._client.stream("POST", "/chat/completions", json=body, timeout=_timeout) as response:
+                ...
+                yield from <原块内循环, 缩进+1>
```

> ⚠️ 13.6.1 流式 `yield` 缩进改动范围大（原 218-260 整段循环退格 +1）。**实施时先复制行 218-244 逐字、整体 +4 空格缩进包进 `async with`，业务逻辑零改动**（AGENTS.md 1.4 拆分铁律：能复制就复制，不重写）。若评审倾向最小改动面，可接受变体：信号量仅在 `_build_request_body` 后、`.`stream()` 调用前 `async with` 包住**连接建立**，把 `yield` 保留在锁外——但该变体会让"流式长响应期间持有信号量"的等待语义失真，推荐前案（保真）。

#### 13.6.2 File: `backend/app/llm/client_sdk.py` —— 补 `asyncio` import（本文件 P5 新用）

```diff
--- a/backend/app/llm/client_sdk.py
+++ b/backend/app/llm/client_sdk.py
@@ -…(import 段)…
+import asyncio  # 2026-09-20 小欧 P5: 软配额信号量 — 小欧-2026-09-20
 import os
```

---

### 13.7 落地顺序 / 验证 / 回滚

**顺序（依赖链）**：`13.1 → 13.2 → 13.3 → 13.4 → 13.5 → 13.6`。13.5 强依赖 13.4（session_id 字段）；其余相互独立。13.6 无依赖、可随时插队，但涉及流式 yield 缩进，建议放最后、单独测试。

**每项验证**：
| 落地项 | 验证命令 | 预期 |
|--------|----------|------|
| 13.1 | `python -c "import app.services.task.task_runtime"` + `pytest -k "task or pause"` | 导入零错，暂停超时测试绿 |
| 13.2 | `python -c "from app.services.model.resolver import resolve_session_client"` + 后端 `uvicorn` 双会话并行发消息 | 双会话各自 cancel，互不误杀；snapshot 日志两条 |
| 13.3 | `python -c "from app.tools.file import file_write_arbiter"` + 并发写同文件工具 E2E | acquire 冲突返回占用者、无静默覆盖 |
| 13.4 | 同 session 忙时连发第二条消息（工具执行中）；假设第一条长任务 20s | 第二条不新建任务、注入第一任务 inbox；第一任务下一轮 LLM 调用前吸收该条（约 20s 后同工具结果一起被 LLM 看到）；可与另一 session 任务并行 |
| 13.5 | 跑任务时删会会话 | 任务收到 `session_deleted` source 取消 |
| 13.6 | 并发 15 任务观察信号量 | 超过 10 即排队、无 503 无降级 |

**回滚**：每项都是**纯增量**（新增 import/锁/字段/函数），无删改既有行为（13.6 流式段含缩进，回滚需还原 yield 缩进）。回滚方式：按 diff 反向 `-`/`+` 对调即恢复原文件，无数据迁移、无 schema 变更，可安全逐项撤销。

---

**编写人**: 小欧
**时间**: 2026-09-20 04:22:00
**版本**: v1.7
**状态**: 待北京老陈审核
