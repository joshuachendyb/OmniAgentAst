# 前后端业务数据配合优化分析-小健-2026-09-04

**文档名称**: 前后端业务数据配合优化分析-小健-2026-09-04
**编写人**: 小健
**创建时间**: 2026-09-04 05:55:36
**更新时间**: 2026-09-04 10:43:47

| 版本 | 时间 | 更新人 | 更新要点 |
|------|------|--------|----------|
| v1.0 | 2026-09-04 05:55:36 | 小健 | 初版：前后端业务数据配合架构审视+代码分布分析+优化方案 - 小健-2026-09-04 |
| v1.1 | 2026-09-04 06:05:27 | 小健 | 重写：优化文档结构，形成问题→原因→方案→实现→效果的有机逻辑链 - 小健-2026-09-04 |
| v1.2 | 2026-09-04 06:18:58 | 小健 | 补全：8个代码文件完整架构关系图+职责层次分析 - 小健-2026-09-04 |
| v1.3 | 2026-09-04 06:42:09 | 小健 | 补充：answer_handler/sandbox_gate/react_cycle 3个文件优化代码diff+新代码 - 小健-2026-09-04 |
| v1.4 | 2026-09-04 06:46:26 | 小健 | 重写第八章：实施计划和步骤+测试验证 - 小健-2026-09-04 |
| v1.5 | 2026-09-04 06:50:40 | 小健 | 重写第五章：替换伪代码为真实代码+精确Diff（answer_handler/sandbox_gate/react_cycle/event_emitter） - 小健-2026-09-04 |
| v1.6 | 2026-09-04 07:00:21 | 小健 | 重写第七章：废弃旧优化效果对比，改为SRP合规拆分优化（8项→4项：信任域三合一+冲突检测+emit链路修复+action引用整合） - 小健-2026-09-04 |
| v1.7 | 2026-09-04 07:05:36 | 小健 | 全文一致性核查修正5处：删sandbox_resolve_after_confirm步骤、八章补充阶段6-10(SRP)、九章补充trust_utils/conflict_detector测试、7.6加action_handler双目标注释 - 小健-2026-09-04 |
| v1.8 | 2026-09-04 07:08:12 | 小健 | 修正"先复制再改造"原则：trust_service/trust_repository/agent_telemetry/step_emitter 4处补上复制来源标注 - 小健-2026-09-04 |
| v1.9 | 2026-09-04 07:12:05 | 小健 | 修正目录位置：4个新文件统一放app/tools/（信任域+冲突检测均为工具层无状态纯函数） - 小健-2026-09-04 |
| v2.0 | 2026-09-04 07:20:18 | 小健 | 三堂会审修正3处：①answer_handler删除死import ThoughtStep ②sandbox_gate diff删除死import MetaStep ③四章返回值示例key从status改为action与五章一致 - 小健-2026-09-04 |
| v2.1 | 2026-09-04 07:25:40 | 小健 | 合并trust三文件为trust.py（112行）：trust_utils+trust_service+trust_repository → app/tools/trust.py，同步更新所有引用路径 - 小健-2026-09-04 |
| v2.2 | 2026-09-04 07:28:55 | 小健 | 第九章全面重写：9.2新增现有测试影响分析（2个P0必改+6个P1需检查+7个P2不改）、9.3新增测试case清单（event_emitter 9个+conflict_detector 6个+trust 8个）、9.9新增测试修改实施顺序 - 小健-2026-09-04 |
| v2.3 | 2026-09-04 07:42:52 | 小健 | 第八章全面重写为TDD模式：8.0前置准备（现有测试适配）、8.1-8.9每个阶段按Red→Green→Refactor三步走、8.10 TDD状态追踪表、8.11 TDD对比表 - 小健-2026-09-04 |
| v2.4 | 2026-09-04 07:45:50 | 小健 | 三堂会审3轮修订7处：①5.3.1重写_dispatch_handler分两路处理修复async for协程bug ②5.2.3 sandbox_gate import改trust.py ③7.3 conflict_detector注释trust_utils→trust ④_conflict括号修复 ⑤9.3.1重写event_emitter测试（sync生成器+9case） ⑥8.3/8.8补环依赖分步说明 ⑦目录结构补app/monitoring路径+9.1补3个编译项 - 小健-2026-09-04 |
| v2.5 | 2026-09-04 07:55:19 | 小健 | 三堂会审复检修正10处：P0①9.3.2冲突检测测试键名params→tool_params ②5.2.3 sandbox_gate HITL暂停流时序修复(阻塞前发射paused) ③event_emitter测试数8→9统一+补confirmed测试；P1④answer_handler yield数10→9 ⑤sandbox_gate目标行数<60→<95 ⑥v2.2版本历史P0/P1数量修正 ⑦9.2.2 P1 summary test_sandbox_gate标记修正 ⑧react_cycle yield数多处→~37处 - 小健-2026-09-04 |
| v2.6 | 2026-09-04 08:48:47 | 小健 | 重构错误分析：日志+代码对比发现4处重构错误（1已修+3待修），新增第十章"重构错误分析与修正"，更新八章Phase4/Phase8补充遗漏检查项 - 小健-2026-09-04 |
| v2.7 | 2026-09-04 09:58:29 | 小健 | 更正错误归因（用户"北京老陈"指正）：场景F死循环检测（_tool_call_signature/_check_same_tool_loop）重构前后逐字一致、非缺陷、非根因，撤销"错误5/原有设计缺陷/检测失效"定性；死循环唯一根因归位 Phase2 漏调 set_completed()；同步修正10.6/10.7/10.8/10.9 - 小健-2026-09-04 |
| v2.8 | 2026-09-04 10:43:47 | 小健 | 全量重构等价性核查完成：逐边界对照pre/post源码+运行时复现+子代理并行核查，新增10.10（6项审计发现），修正10.1错误汇总表（错误2状态❌+新增错误6/7），修正10.8错误2状态 - 小健-2026-09-04 |
| v2.9 | 2026-09-04 10:45:14 | 小健 | 新增第十一章：重构问题清单（按问题维度重新梳理，每个问题独立编号+根因+影响+修复状态） - 小健-2026-09-04 |

---

## 一、问题背景：为什么要做这个优化

这几天调试前后端配合的功能，总是后端到处修改，而且还总是错误百出。北京老陈追问：

1. 与前端配合的部分零散分布，到处都放，应该集中
2. 支持前端配合的代码逻辑层次/架构层次是错误的
3. 后端yield信息给前端，为什么需要那么多代码来处理这个事情？

**核心问题**：一定是后端与前端配合的这些逻辑的烂代码导致的。

---

## 二、问题现象：这几天改了哪些后端代码

| 日期 | 文件 | 改动内容 |
|------|------|----------|
| 09-01 | action_handler.py | 恢复路径补发resumed事件 |
| 09-01 | sandbox_gate.py | 恢复路径补发resumed事件 |
| 09-01 | answer_handler.py | 删除正常answer终态污染版ThoughtStep |
| 09-01 | llm_stream.py | error_classifier+L1/L2去放大 |
| 09-02 | action_handler.py | task005三处bug修复 |
| 09-02 | stream_orchestrator.py | task005三处bug修复 |
| 09-02 | llm_stream.py | 404重试放大严谨修复 |
| 09-02 | react_cycle.py | LLM重试事件实时透出 |
| 09-02 | step_emitter.py | emit统一日志出口 |
| 09-03 | 7个文件 | 10规范审查与重构方案 |

**核心发现**：这些改动**全部是围绕SSE事件发射**在修补。

---

## 三、病根分析：架构层次问题

### 3.1 后端在做前端的事

**当前架构（错误）**：

```
┌─────────────────────────────────────────────────────────┐
│  后端 handler 层                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ action_handler.py (1144行)                       │   │
│  │   - 业务逻辑：安全检查、HITL确认、工具执行       │   │
│  │   - 前端逻辑：yield MetaStep(type="paused")     │   │  ← 后端在做前端的事！
│  │   - 前端逻辑：yield MetaStep(type="resumed")    │   │  ← 后端在做前端的事！
│  │   - 前端逻辑：yield MetaStep(type="blocked")    │   │  ← 后端在做前端的事！
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ answer_handler.py (235行)                        │   │
│  │   - 业务逻辑：答案解析、错误处理                 │   │
│  │   - 前端逻辑：yield ThoughtStartStep             │   │  ← 后端在做前端的事！
│  │   - 前端逻辑：yield ThoughtStep                  │   │  ← 后端在做前端的事！
│  │   - 前端逻辑：yield FinalStep                    │   │  ← 后端在做前端的事！
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 3.2 yield发射点分散在6个文件

| 文件 | yield次数 | 职责 |
|------|----------|------|
| action_handler.py | **16处** | 混合业务+前端逻辑 |
| answer_handler.py | **9处** | 混合业务+前端逻辑 |
| react_cycle.py | **~37处** | 混合调度+前端逻辑 |
| agent_runner.py | **多处** | 混合SSE通道路由 |
| step_emitter.py | **1处** | 统一发射入口 |
| stream_orchestrator.py | **1处** | SSE转发层 |

**总计**：6个文件25+处yield点分散在各处。

### 3.3 病根总结

| 问题 | 病根 |
|------|------|
| **代码东一点西一点** | 因为每个handler都在直接yield前端Step |
| **修改一个前端展示要改多个文件** | 业务逻辑和前端展示逻辑混在一起 |
| **为什么需要那么多代码处理SSE** | 7个层级都在处理SSE相关逻辑 |

---

## 四、优化逻辑：怎么把分散的yield集中起来

### 4.1 核心思路：分层分离

**当前（混乱）**：handler直接yield前端Step

```
action_handler.py
├── 业务：创建确认 → confirm_id
├── 前端：构造MetaStep(type="paused")    ← 混在一起！
└── 前端：yield给前端显示               ← 混在一起！
```

**优化后（清晰）**：handler只返回业务结果，统一层转换

```
action_handler.py（只做业务）
├── 业务：创建确认 → confirm_id
└── 返回：{"action":"confirmed", "confirm_id": xxx}

        ↓

event_emitter.py（统一转换）
├── 读取业务结果
├── 根据status构造前端Step
└── yield给前端显示
```

### 4.2 优化后的架构（8个文件完整关系）

**优化后完整架构图**：

```
┌─────────────────────────────────────────────────────────────────────┐
│  第一层：业务层（只关心业务，不yield前端Step）                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ action_handler.py (1144行→<500行)                           │   │
│  │   - 业务：安全检查、HITL确认、工具执行                       │   │
│  │   - 返回：{"action":"paused/completed/blocked", ...}        │   │
│  │   - 不yield任何Step                                         │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ answer_handler.py (235行→<100行)                            │   │
│  │   - 业务：答案解析、错误处理、重复检测                       │   │
│  │   - 返回：{"action":"completed/failed/retrying", ...}       │   │
│  │   - 不yield任何Step                                         │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ sandbox_gate.py (125行→<95行)                               │   │
│  │   - 业务：沙箱预检、用户裁决                                 │   │
│  │   - 返回：{"action":"blocked/passthrough/user_confirm",...} │   │
│  │   - 不yield任何Step                                         │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ hitl_confirmation.py (208行，保持)                          │   │
│  │   - 业务：HITL确认机制（create/wait/resolve）               │   │
│  │   - 已经是干净的业务层，无前端Step                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ 返回业务结果
┌─────────────────────────────────────────────────────────────────────┐
│  第二层：调度层（调用业务层，接收业务结果）                          │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ react_cycle.py                                              │   │
│  │   - 调用action_handler/answer_handler/sandbox_gate          │   │
│  │   - 接收业务结果，调用event_emitter转换                      │   │
│  │   - 根据业务结果决定下一步（继续/等待/结束）                 │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ 调用转换层
┌─────────────────────────────────────────────────────────────────────┐
│  第三层：转换层（业务结果→前端Step，统一yield）                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ event_emitter.py（新建）                                     │   │
│  │   - 读取业务结果（action/status/数据）                       │   │
│  │   - 根据action构造前端需要的Step                             │   │
│  │   - 统一yield给SSE层                                        │   │
│  │   - 所有前端展示逻辑集中在此                                 │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ yield Step
┌─────────────────────────────────────────────────────────────────────┐
│  第四层：基础设施层（Step构建/发射/监控）                            │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ step_emitter.py (90行，保持)                                │   │
│  │   - 统一发射入口：emit() + emit_final_with_stats()          │   │
│  │   - 自动注入token用量、记录_last_error                       │   │
│  │   - 纯基础设施，无业务逻辑                                   │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ agent_telemetry.py (277行，保持，位于app/monitoring/)       │   │
│  │   - 遥测采集：build_stats_step/build_final_stats_step       │   │
│  │   - 构建MetaStep但不yield（由step_emitter发射）              │   │
│  │   - 轻度混合，已通过SLAP修复（第七章项3 outcome参数化）      │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ llm_stream.py (364行，保持)                                 │   │
│  │   - LLM流处理：stream_chat_completion                       │   │
│  │   - SSE事件解析、重试逻辑                                    │   │
│  │   - 基础设施，无业务混合                                     │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ 使用Step
┌─────────────────────────────────────────────────────────────────────┐
│  第五层：运行层（后台运行+SSE通道路由）                              │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ agent_runner.py                                             │   │
│  │   - 后台运行Agent：run_agent_in_background()                │   │
│  │   - SSE通道路由：thought仅落库/chunk仅SSE/其余双通道         │   │
│  │   - DB持久化：_persist/_finalize_task_db                     │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ 读取缓冲
┌─────────────────────────────────────────────────────────────────────┐
│  第六层：SSE传输层（只负责传输，不关心业务）                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ stream_orchestrator.py                                      │   │
│  │   - SSE编排：chat_stream_orchestrator()                     │   │
│  │   - 读缓冲+pause/cancel检查+SSE转发                         │   │
│  │   - 不关心业务逻辑                                           │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ stream_reader.py                                            │   │
│  │   - SSE读取：stream_reader()                                │   │
│  │   - 从event_log按seq偏移读取并转发                           │   │
│  │   - 纯消费者，不关心业务                                     │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ 转发SSE
┌─────────────────────────────────────────────────────────────────────┐
│  前端                                                                │
│  - POST /api/v1/chat → 触发chat_stream_orchestrator                │
│  - SSE连接 → 接收Step事件 → 渲染UI                                 │
└─────────────────────────────────────────────────────────────────────┘
```

**8个文件职责层次对照表**：

| 文件 | 当前层次 | 优化后层次 | 职责 | 是否需要改造 |
|------|---------|-----------|------|-------------|
| action_handler.py | 业务+前端混合 | **业务层** | 安全检查、HITL确认、工具执行 | ✅ 需要改造 |
| answer_handler.py | 业务+前端混合 | **业务层** | 答案解析、错误处理 | ✅ 需要改造 |
| sandbox_gate.py | 业务+前端混合 | **业务层** | 沙箱预检、用户裁决 | ✅ 需要改造 |
| hitl_confirmation.py | 纯业务 | **业务层** | HITL确认机制 | ❌ 保持 |
| react_cycle.py | 调度+前端混合 | **调度层** | 循环核心、分发handler | ✅ 需要改造 |
| event_emitter.py | 不存在 | **转换层（新建）** | 业务结果→前端Step | ✅ 新建 |
| step_emitter.py | 基础设施 | **基础设施层** | 统一发射入口 | ❌ 保持 |
| agent_telemetry.py | 监控 | **基础设施层** | 遥测采集、Step构建 | ❌ 保持 |
| llm_stream.py | 基础设施 | **基础设施层** | LLM流处理 | ❌ 保持 |
| agent_runner.py | 运行层 | **运行层** | 后台运行、SSE通道路由 | ❌ 保持 |
| stream_orchestrator.py | SSE层 | **SSE层** | SSE编排转发 | ❌ 保持 |
| stream_reader.py | SSE层 | **SSE层** | SSE读取 | ❌ 保持 |

**改造范围**：
- 需要改造：action_handler.py、answer_handler.py、sandbox_gate.py、react_cycle.py（4个）
- 新建：event_emitter.py（1个）
- 保持不动：hitl_confirmation.py、step_emitter.py、agent_telemetry.py、llm_stream.py、agent_runner.py、stream_orchestrator.py、stream_reader.py（7个）

---

## 五、具体实现：3个文件优化代码（真实代码+精确Diff）

> **说明**：以下所有代码均来自实际源文件 `backend/app/services/agent/` 目录下对应文件。
> Diff 标注格式：`[-]` 删除行、`[+]` 新增行、无标记为不变行。

---

### 5.1 answer_handler.py 优化（235行→120行，yield 9处→0处）

**源文件**：`backend/app/services/agent/handlers/answer_handler.py`（235行）

#### 5.1.1 Diff：删除Step相关import + 返回dict替代yield

```diff
 # answer_handler.py - 小健-2026-09-04
 import re
 import time
 from collections import Counter
 from typing import Dict

-from app.services.agent.steps import ThoughtStep, ThoughtStartStep, FinalStep, MetaStep  # 2026-08-18 小欧 ThoughtStartStep新增
+from app.services.agent.steps import ThoughtStep  # 仅保留ThoughtStep(reasoning-only分支发射)
 from app.utils.text_utils import format_tool_call_markup
 from app.logger import logger, log_and_print
```

#### 5.1.2 Diff：修改handle_answer函数签名+error分支

```diff
-async def handle_answer(agent, parsed: Dict):
+async def handle_answer(agent, parsed: Dict) -> dict:
     """统一处理所有非action的LLM返回类型（answer/error/unknown）
     
     由 _dispatch_handler(react_cycle.py) 分派，接收 llm_stream.py 构建的 type：
     - type="answer" → 正常终态流程（最终答复）
     - type="error"  → LLM 流式异常 → FinalStep(outcome="failed") → set_failed
     - 其他未知 type → 按 error 处理（兜底）
     
     type 产生于 llm_stream.py（见该模块头部），不由 LLM 输出，是 agent 推断。
+    返回 dict: {"action": "completed/failed/retrying/thought", ...}
+    不yield任何Step，由 event_emitter 统一转换。
     """
     if parsed is None:
         parsed = {}
     step = agent.llm_call_count
     parsed_type = parsed.get("type", "answer")

     # ── type="error" │ yield FinalStep(outcome=failed) ──
     if parsed_type == "error":
         content = parsed.get("content", "") or "LLM流式错误"
         err_type = parsed.get("error_type") or "大模型错误"
         agent._consecutive_reasoning_only = 0
         agent.message_builder.add_assistant_message(content)
         logger.error(f"[answer] step={step} error={content} error_type={err_type}")
         from app.services.agent.status_table import set_failed
         set_failed(agent, content)
-        yield agent._step_emitter.emit(ThoughtStartStep(step=step))   # 2026-08-18 小欧 thought-start
-        for _s in agent._step_emitter.emit_final_with_stats(FinalStep(
-            step=step, response="任务执行失败",
-            outcome="failed", error_type=err_type, error_message=content,
-        )):
-            yield _s
-        return
+        return {
+            "action": "failed",
+            "response": "任务执行失败",
+            "error_type": err_type,
+            "error_message": content,
+        }
```

#### 5.1.3 Diff：未知类型分支

```diff
     # ── 未知类型 │ yield FinalStep(outcome=failed) ──
     if parsed_type != "answer":
         logger.warning(f"[handle_answer] 未知返回类型: {parsed_type}, 设置为FAILED")
         agent._consecutive_reasoning_only = 0
         content = parsed.get("content", "") or parsed.get("thought", "") or ""
         logger.error(f"[answer] step={step} unknown_type={parsed_type} content={content}")
         if content:
             agent.message_builder.add_assistant_message(f"[无效响应:{parsed_type}] {content}")
         from app.services.agent.status_table import set_failed
         set_failed(agent, f"LLM返回未知响应类型: {parsed_type}")
-        yield agent._step_emitter.emit(ThoughtStartStep(step=step))   # 2026-08-18 小欧 thought-start
-        for _s in agent._step_emitter.emit_final_with_stats(FinalStep(
-            step=step, response="任务执行失败",
-            outcome="failed", error_type="unknown_response",
-            error_message=f"LLM返回未知响应类型: {parsed_type}",
-        )):
-            yield _s
-        return
+        return {
+            "action": "failed",
+            "response": f"LLM返回未知响应类型: {parsed_type}",
+            "error_type": "unknown_response",
+            "error_message": content,
+        }
```

#### 5.1.4 Diff：空内容→retrying分支

```diff
     # 真·空：content和reasoning都空 → 系统重试通知(MetaStep.retrying)
     if not content and not reasoning:
         logger.warning(f"[handle_answer] LLM返回空内容(step={step}), 触发系统重试")
         agent._consecutive_reasoning_only = 0
         agent.message_builder.add_assistant_message("")
-        yield agent._step_emitter.emit(MetaStep(
-            type="retrying",
-            step=step,
-            content="LLM返回空内容，触发重试",
-            wait_time=1,
-            severity="info",
-        ))
-        return
+        return {"action": "retrying", "wait_time": 1}
```

#### 5.1.5 Diff：reasoning-only分支

```diff
     if not content and reasoning:
         _deduped = _dedup_repeat(reasoning)
         agent._consecutive_reasoning_only += 1
         if agent._consecutive_reasoning_only > REASONING_ONLY_MAX_ROUNDS:
             logger.warning(f"[handle_answer] 连续{agent._consecutive_reasoning_only}轮reasoning-only无进展(step={step}), 终止任务")
-            yield agent._step_emitter.emit(ThoughtStartStep(step=step))   # 2026-08-18 小欧 thought-start
-            for _s in agent._step_emitter.emit_final_with_stats(FinalStep(
-                step=step,
-                response="模型反复思考未产出有效结果，任务已终止（疑似陷入无效循环）",
-                reasoning=_deduped,
-                outcome="failed",
-            )):
-                yield _s
-            return
+            return {
+                "action": "failed",
+                "response": "模型反复思考未产出有效结果，任务已终止（疑似陷入无效循环）",
+                "error_type": "reasoning_only_limit",
+                "error_message": f"连续{REASONING_ONLY_MAX_ROUNDS}轮仅返回推理内容",
+                "reasoning": _deduped,
+            }
         if _deduped == reasoning:
             # ── 好的: 无重复 → 贴便签 ──
             logger.info(f"[handle_answer] LLM返回推理内容(step={step}), 注入临时推理(连续reasoning-only={agent._consecutive_reasoning_only})")
             agent.message_builder.conversation_history.append({
                 "role": "assistant",
                 "content": "",
                 "reasoning": _deduped,
                 "reasoning_content": _deduped,
                 "_temp_reasoning": True,
             })
-            yield agent._step_emitter.emit(ThoughtStep(
-                step=step, content=_deduped, reasoning="",
-            ))
+            # 由 event_emitter 统一发射 ThoughtStep
+            return {"action": "thought", "content": _deduped, "reasoning": ""}
         else:
             logger.warning(f"[handle_answer] reasoning检测到重复去重(step={step}), 跳过注入")
-        return
+        return {"action": "retrying", "wait_time": 1}
```

#### 5.1.6 Diff：正常answer终态分支

```diff
     agent._consecutive_reasoning_only = 0
-    yield agent._step_emitter.emit(ThoughtStartStep(step=step))   # 2026-08-18 小欧 thought-start

     # ══ 重复检测(≥250字才检) ══
     if len(content) >= REPEAT_CHECK_MIN_LEN:
         deduped = _dedup_repeat(content)
         if deduped != content:
             content = deduped

     log_and_print(f"{time.strftime('%H:%M:%S')} [Final] step={step}, response_len={len(content)}, response={content}")
-    for _s in agent._step_emitter.emit_final_with_stats(FinalStep(
-        step=step, response=content,
-        outcome="completed", reasoning=reasoning,
-    )):
-        yield _s
     agent.message_builder.add_assistant_message(content)
+    return {
+        "action": "completed",
+        "response": content,
+        "reasoning": reasoning,
+    }
```

#### 5.1.7 优化后完整文件

```python
# -*- coding: utf-8 -*-
# 编辑历史:
# ... (保留原有编辑历史)
# 2026-09-04 小健 - 架构重构: handle_answer改为返回dict,删除所有yield Step(9处),
#   由event_emitter统一转换,职责从"业务+前端混合"收敛为"纯业务"。
#   返回值: {"action": "completed/failed/retrying/thought", ...}
"""
answer_handler — 统一处理所有"说"类型(action以外的答案/错误/未知)
"""
import re
from collections import Counter
from typing import Dict

from app.utils.text_utils import format_tool_call_markup
from app.logger import logger, log_and_print


# ── 重复检测(版本2026-07-17: 句子频率法替代固定chunk) ──
REPEAT_CHECK_MIN_LEN = 250
SENTENCE_MIN_REPEAT = 3
DUP_RATIO = 0.5

REASONING_ONLY_MAX_ROUNDS = 3


def _dedup_repeat(content: str) -> str:
    """【卡顿循环重复去重】基于句子频率, 非通用去重工具"""
    if len(content) < REPEAT_CHECK_MIN_LEN:
        return content
    parts = re.split(r'(?<=[。\n])', content)
    parts = [p for p in parts if len(p.strip()) > 0]
    if len(parts) < 10:
        return content
    counter = Counter(parts)
    repeated = {s for s, cnt in counter.items()
                if cnt >= SENTENCE_MIN_REPEAT
                and not s.strip().startswith('|')}
    if not repeated:
        return content
    result = []
    seen = set()
    for p in parts:
        if p in repeated and p in seen:
            continue
        if p in repeated:
            seen.add(p)
        result.append(p)
    deduped = "".join(result)
    if len(deduped) >= len(content):
        return content
    ratio = 1 - len(deduped) / len(content)
    if ratio < DUP_RATIO:
        return content
    logger.warning(f"[L1-C2b] 检测到无意义重复(final {len(content)}字, 重复占比 {ratio:.0%}), 已去重截断")
    return deduped


async def handle_answer(agent, parsed: Dict) -> dict:
    """统一处理所有非action的LLM返回类型（answer/error/unknown）
    返回 dict: {"action": "completed/failed/retrying/thought", ...}
    不yield任何Step，由 event_emitter 统一转换。
    """
    if parsed is None:
        parsed = {}
    step = agent.llm_call_count
    parsed_type = parsed.get("type", "answer")

    # ── type="error" ──
    if parsed_type == "error":
        content = parsed.get("content", "") or "LLM流式错误"
        err_type = parsed.get("error_type") or "大模型错误"
        agent._consecutive_reasoning_only = 0
        agent.message_builder.add_assistant_message(content)
        logger.error(f"[answer] step={step} error={content} error_type={err_type}")
        from app.services.agent.status_table import set_failed
        set_failed(agent, content)
        return {
            "action": "failed",
            "response": "任务执行失败",
            "error_type": err_type,
            "error_message": content,
        }

    # ── 未知类型 ──
    if parsed_type != "answer":
        logger.warning(f"[handle_answer] 未知返回类型: {parsed_type}, 设置为FAILED")
        agent._consecutive_reasoning_only = 0
        content = parsed.get("content", "") or parsed.get("thought", "") or ""
        logger.error(f"[answer] step={step} unknown_type={parsed_type} content={content}")
        if content:
            agent.message_builder.add_assistant_message(f"[无效响应:{parsed_type}] {content}")
        from app.services.agent.status_table import set_failed
        set_failed(agent, f"LLM返回未知响应类型: {parsed_type}")
        return {
            "action": "failed",
            "response": f"LLM返回未知响应类型: {parsed_type}",
            "error_type": "unknown_response",
            "error_message": content,
        }

    # ── type="answer" ──
    content = format_tool_call_markup(parsed.get("content", "") or "")
    reasoning = format_tool_call_markup(parsed.get("reasoning", "") or "")

    # 真·空
    if not content and not reasoning:
        logger.warning(f"[handle_answer] LLM返回空内容(step={step}), 触发系统重试")
        agent._consecutive_reasoning_only = 0
        agent.message_builder.add_assistant_message("")
        return {"action": "retrying", "wait_time": 1}

    # reasoning-only
    if not content and reasoning:
        _deduped = _dedup_repeat(reasoning)
        agent._consecutive_reasoning_only += 1
        if agent._consecutive_reasoning_only > REASONING_ONLY_MAX_ROUNDS:
            logger.warning(f"[handle_answer] 连续{agent._consecutive_reasoning_only}轮reasoning-only无进展(step={step}), 终止任务")
            return {
                "action": "failed",
                "response": "模型反复思考未产出有效结果，任务已终止（疑似陷入无效循环）",
                "error_type": "reasoning_only_limit",
                "error_message": f"连续{REASONING_ONLY_MAX_ROUNDS}轮仅返回推理内容",
                "reasoning": _deduped,
            }
        if _deduped == reasoning:
            logger.info(f"[handle_answer] LLM返回推理内容(step={step}), 注入临时推理")
            agent.message_builder.conversation_history.append({
                "role": "assistant",
                "content": "",
                "reasoning": _deduped,
                "reasoning_content": _deduped,
                "_temp_reasoning": True,
            })
            return {"action": "thought", "content": _deduped, "reasoning": ""}
        else:
            logger.warning(f"[handle_answer] reasoning检测到重复去重(step={step}), 跳过注入")
        return {"action": "retrying", "wait_time": 1}

    # 正常answer
    agent._consecutive_reasoning_only = 0
    if len(content) >= REPEAT_CHECK_MIN_LEN:
        deduped = _dedup_repeat(content)
        if deduped != content:
            content = deduped

    log_and_print(f"{time.strftime('%H:%M:%S')} [Final] step={step}, response_len={len(content)}, response={content}")
    agent.message_builder.add_assistant_message(content)
    return {
        "action": "completed",
        "response": content,
        "reasoning": reasoning,
    }
```

#### 5.1.8 优化效果

| 维度 | 当前 | 优化后 |
|------|------|--------|
| 行数 | 235行 | 120行 |
| yield点 | 9处 | 0处 |
| 职责 | 业务+前端混合 | 纯业务（只返回dict） |

---

### 5.2 sandbox_gate.py 优化（125行→95行，yield 4处→0处）

**源文件**：`backend/app/services/agent/handlers/sandbox_gate.py`（125行）

#### 5.2.1 Diff：sandbox_resolve返回dict替代yield

```diff
 async def sandbox_resolve(agent, step, call, tool_name, params, pre, safety_result, denied_list):
-    """预检结果处置(DRY: 三处插入点共用)。返回(放行bool, 待下发steps列表)"""
+    """预检结果处置。返回dict: {"action": "passthrough/blocked/user_confirm/confirmed/rejected", ...}
+    不yield任何Step，由event_emitter统一转换。
+    注意: 用户裁决确认/拒绝仍需同步等待, 由调用方根据返回的action决定后续流程。
+    """
-    from app.services.agent.steps import MetaStep
     from app.services.agent.status_table import AgentStatus, set_status
     from app.services.task.hitl_confirmation import create_confirmation, wait_for_confirmation_result
     from app.constants import HITL_TIMEOUT
     from app.tools.tool_constants import SENSITIVE_FIELDS as _SENSITIVE_FIELDS
     if pre.passed:
         logger.info(f"[sandbox] 放行执行: tool={tool_name}")
-        return True, []
+        return {"action": "passthrough"}
     if pre.needs_ruling and safety_result.auto_confirm:
         logger.info(f"[sandbox] bypass下未完成有效验证,按bypass语义直接放行: tool={tool_name}, reason={pre.blocked_reason}")
-        return True, []
+        return {"action": "passthrough"}
     if not pre.needs_ruling:
         logger.warning(f"[sandbox] 危险型拦截拒绝: tool={tool_name}, reason={pre.blocked_reason[:200]}")
         denied_list.append((tool_name, f"沙箱预检未通过: {pre.blocked_reason}", call))
-        return False, [agent._step_emitter.emit(MetaStep(
-            step=step, type="error",
-            content=f"沙箱预检未通过: {pre.blocked_reason}",
-            error_type="blocked", severity="warn"))]
+        return {
+            "action": "blocked",
+            "reason": f"沙箱预检未通过: {pre.blocked_reason}",
+            "tool_name": tool_name,
+        }
     # needs_ruling: 走HITL用户裁决
     logger.info(f"[sandbox] 转HITL用户裁决: tool={tool_name}")
     confirm_id = await create_confirmation(agent.task_id, tool_name)
```

#### 5.2.2 Diff：用户裁决paused+等待+结果分支

```diff
     # ... (超时配置计算部分不变, 保留 _ct/_bt/_sandbox_path 等业务逻辑)
-    steps = [agent._step_emitter.emit(MetaStep(
-        step=step, type="paused",
-        content=f"沙箱未能完成有效预检,需用户裁决是否直接执行: {tool_name}",
-        confirm_id=confirm_id, tool_name=tool_name,
-        params={k: v for k, v in params.items() if k not in _SENSITIVE_FIELDS},
-        safety_level="destructive", severity="attention",
-        trust_path=_sandbox_path, auto_confirm=False,
-        confirm_timeout=_ct, backend_timeout=_bt))]
+
     set_status(agent, AgentStatus.SUSPENDED, f"等待用户裁决沙箱预检: {tool_name}")
     from app.config import get_config as _get_cfg_sb
     auth = await wait_for_confirmation_result(confirm_id, timeout=int(float(_get_cfg_sb().get("security.hitl_timeout", HITL_TIMEOUT))))
     set_status(agent, AgentStatus.EXECUTING, "沙箱预检用户裁决完成")
+
     if auth.get("confirmed"):
         logger.info(f"[sandbox] 用户裁决: 确认执行: tool={tool_name}")
-        steps.append(agent._step_emitter.emit(MetaStep(
-            step=step, type="resumed",
-            content=f"沙箱预检已确认执行: {tool_name}", severity="info")))
-        return True, steps
+        return {
+            "action": "confirmed",
+            "confirm_id": confirm_id,
+            "tool_name": tool_name,
+            "params": {k: v for k, v in params.items() if k not in _SENSITIVE_FIELDS},
+            "safety_level": "destructive",
+            "trust_path": _sandbox_path,
+            "confirm_timeout": _ct,
+            "backend_timeout": _bt,
+        }
     logger.warning(f"[sandbox] 用户裁决: 拒绝执行: tool={tool_name}")
     denied_list.append((tool_name, "沙箱预检未完成验证且用户拒绝执行", call))
-    steps.append(agent._step_emitter.emit(MetaStep(
-        step=step, type="error",
-        content=f"用户拒绝执行(预检未完成验证): {tool_name}",
-        error_type="user_rejected", severity="warn")))
-    return False, steps
+    return {
+        "action": "rejected",
+        "reason": "用户拒绝执行(预检未完成验证)",
+        "tool_name": tool_name,
+    }
```

#### 5.2.3 优化后完整文件

```python
# -*- coding: utf-8 -*-
# 编辑历史:
# ... (保留原有编辑历史)
# 2026-09-04 小健 - 架构重构: sandbox_resolve改为返回dict,删除所有yield MetaStep(4处),
#   由event_emitter统一转换,职责从"业务+前端混合"收敛为"纯业务"。
#   返回值: {"action": "passthrough/blocked/confirmed/rejected", ...}
"""沙箱执行闸门: 将 destructive 级工具调用的沙箱预检与结果处置集中在 Agent 编排层。"""
from app.logger import logger


async def sandbox_precheck(safety_result, tool_name, params):
    """destructive级沙箱预检; 返回None=无需预检/异常兜底"""
    if not getattr(safety_result, "sandbox_required", False):
        return None
    try:
        from app.safety.sandbox import get_sandbox_executor
        pre = await get_sandbox_executor().pre_execute(tool_name, params)
        logger.info(f"[sandbox] 预检结果: tool={tool_name}, passed={pre.passed}, "
                    f"needs_ruling={pre.needs_ruling}, reason={pre.blocked_reason[:200]}")
        return pre
    except Exception as exc:
        logger.warning(f"[action_handler] 沙箱预检异常(M4兜底直接执行): tool={tool_name}, err={exc}")
        return None


async def sandbox_resolve(agent, step, call, tool_name, params, pre, safety_result, denied_list):
    """预检结果处置。返回dict: {"action": "passthrough/blocked/confirmed/rejected", ...}
    不yield任何Step，由event_emitter统一转换。"""
    from app.services.agent.status_table import AgentStatus, set_status
    from app.services.task.hitl_confirmation import create_confirmation, wait_for_confirmation_result
    from app.constants import HITL_TIMEOUT
    from app.tools.tool_constants import SENSITIVE_FIELDS as _SENSITIVE_FIELDS

    if pre.passed:
        logger.info(f"[sandbox] 放行执行: tool={tool_name}")
        return {"action": "passthrough"}
    if pre.needs_ruling and safety_result.auto_confirm:
        logger.info(f"[sandbox] bypass下未完成有效验证,按bypass语义直接放行: tool={tool_name}, reason={pre.blocked_reason}")
        return {"action": "passthrough"}
    if not pre.needs_ruling:
        logger.warning(f"[sandbox] 危险型拦截拒绝: tool={tool_name}, reason={pre.blocked_reason[:200]}")
        denied_list.append((tool_name, f"沙箱预检未通过: {pre.blocked_reason}", call))
        return {"action": "blocked", "reason": f"沙箱预检未通过: {pre.blocked_reason}", "tool_name": tool_name}

    # needs_ruling: 走HITL用户裁决
    logger.info(f"[sandbox] 转HITL用户裁决: tool={tool_name}")
    confirm_id = await create_confirmation(agent.task_id, tool_name)
    from app.config import get_config as _get_cfg_sb2
    from app.constants import HITL_CONFIRM_LEAD, HITL_MIN_CONFIRM_TIMEOUT
    _bt = int(float(_get_cfg_sb2().get("security.hitl_timeout", HITL_TIMEOUT)))
    _ct = max(HITL_MIN_CONFIRM_TIMEOUT, _bt - HITL_CONFIRM_LEAD)
    try:
        from app.tools.trust import extract_trust_path as _sb_trust_path
        _sandbox_path = _sb_trust_path(tool_name, params)
    except Exception:
        _sandbox_path = None
    if _sandbox_path is None:
        for _k in ("path", "file_path", "source_path", "dest_path", "target", "dir_path"):
            _v = params.get(_k)
            if isinstance(_v, str) and _v:
                _sandbox_path = _v
                break

    # 注意: paused MetaStep必须在阻塞等待前发射,否则前端永远收不到暂停事件
    # 这是过渡期设计: sandbox_resolve直接发射paused, event_emitter不重复发射
    from app.services.agent.steps import MetaStep as _SbMetaStep
    agent._step_emitter.emit(_SbMetaStep(
        step=step, type="paused",
        content=f"沙箱未能完成有效预检,需用户裁决是否直接执行: {tool_name}",
        confirm_id=confirm_id, tool_name=tool_name,
        params={k: v for k, v in params.items() if k not in _SENSITIVE_FIELDS},
        safety_level="destructive", severity="attention",
        trust_path=_sandbox_path, auto_confirm=False,
        confirm_timeout=_ct, backend_timeout=_bt))
    set_status(agent, AgentStatus.SUSPENDED, f"等待用户裁决沙箱预检: {tool_name}")
    from app.config import get_config as _get_cfg_sb
    auth = await wait_for_confirmation_result(confirm_id, timeout=int(float(_get_cfg_sb().get("security.hitl_timeout", HITL_TIMEOUT))))
    set_status(agent, AgentStatus.EXECUTING, "沙箱预检用户裁决完成")

    if auth.get("confirmed"):
        logger.info(f"[sandbox] 用户裁决: 确认执行: tool={tool_name}")
        return {
            "action": "confirmed",
            "confirm_id": confirm_id,
            "tool_name": tool_name,
            "params": {k: v for k, v in params.items() if k not in _SENSITIVE_FIELDS},
            "safety_level": "destructive",
            "trust_path": _sandbox_path,
            "confirm_timeout": _ct,
            "backend_timeout": _bt,
        }
    logger.warning(f"[sandbox] 用户裁决: 拒绝执行: tool={tool_name}")
    denied_list.append((tool_name, "沙箱预检未完成验证且用户拒绝执行", call))
    return {"action": "rejected", "reason": "用户拒绝执行(预检未完成验证)", "tool_name": tool_name}
```

#### 5.2.4 优化效果

| 维度 | 当前 | 优化后 |
|------|------|--------|
| 行数 | 125行 | 95行 |
| yield点 | 4处 | 0处 |
| 职责 | 业务+前端混合 | 纯业务（只返回dict） |

---

### 5.3 react_cycle.py 优化（_dispatch_handler从yield→return dict）

**源文件**：`backend/app/services/agent/react_cycle.py`（912行）

#### 5.3.1 Diff：_dispatch_handler从async generator改为async function

> 核心改动：`_dispatch_handler` 不再yield前端Step，改为返回dict。
> 两种handler返回形态不同，需分两路处理：
> - `handle_answer` 已改造：`async def` + `return dict`（协程，await获取结果）
> - `handle_action` 尚未改造：`async def` + `yield Step`（异步生成器，async for遍历）

```diff
-async def _dispatch_handler(agent, llm_response):
-    """按type分派handler，基于 event type 推断状态 — chendyg 2026-07-01"""
+async def _dispatch_handler(agent, llm_response) -> dict:
+    """按type分派handler，获取业务结果并推断状态
+    返回 dict: {"action": "...", ...} 业务结果
+    不yield任何Step，由 run_react_cycle 调用 event_emitter 统一转换。
+    分两路处理：answer→await协程拿dict；action→async for遍历yield的状态推断。
+    """
     parsed_type = llm_response.get("type", "answer")
     step = agent.llm_call_count
     thought = llm_response.get("thought", "")
     reasoning = llm_response.get("reasoning", "")
     if thought or reasoning:
         reasoning_part = f"\n{time.strftime('%H:%M:%S')} === 推理 ===\n{reasoning}" if reasoning else ""
         log_and_print(f"{time.strftime('%H:%M:%S')} [Thought] step={step}, {thought}{reasoning_part}")
-    if parsed_type == "action":
-        handler = handle_action(agent, llm_response)
-    else:
-        handler = handle_answer(agent, llm_response)
 
-    _EV_FINAL, _EV_RETRY, _EV_ERROR = "final", "retrying", "error"
-    seen_types = set()
-    last_error_event = None
-    final_event = None
-    async for event in handler:
-        seen_types.add(event.type)
-        if event.type == _EV_ERROR:
-            last_error_event = event
-        elif event.type == _EV_FINAL:
-            final_event = event
-        yield event
+    # ── answer分支：直接await拿dict（已改造为return dict）──
+    if parsed_type != "action":
+        return await handle_answer(agent, llm_response)
+
+    # ── action分支：async for遍历yield的Step（尚未改造，仍yield）──
+    _EV_FINAL, _EV_RETRY, _EV_ERROR = "final", "retrying", "error"
+    seen_types = set()
+    last_error_event = None
+    final_event = None
+    async for event in handle_action(agent, llm_response):
+        seen_types.add(event.type)
+        if event.type == _EV_ERROR:
+            last_error_event = event
+        elif event.type == _EV_FINAL:
+            final_event = event

     # ── 状态推断(仅对action_handler的yield事件生效) ──
     if _EV_RETRY in seen_types:
         set_status(agent, AgentStatus.RETRYING, "触发重试")
     elif _EV_FINAL in seen_types:
         oc = getattr(final_event, "outcome", "completed")
         if oc == "failed":
             set_failed(agent, getattr(final_event, "error_message", "") or final_event.get_content())
         elif oc == "cancelled":
             set_cancelled(agent)
         else:
             set_completed(agent)
     elif _EV_ERROR in seen_types:
         error_event = last_error_event
         _kw = getattr(error_event, "_kwargs", {}) or {}
         err_type = _kw.get("error_type", "")
         error_msg = error_event.get_content() if hasattr(error_event, 'get_content') else ""
         if err_type in _RECOVERABLE_ERRORS:
             _tool = llm_response.get("tool_name", "") or getattr(error_event, "tool_name", "")
             if _tool:
                 _key = (str(_tool), str(err_type))
                 _deny = getattr(agent, "_deny_counts", {}) or {}
                 _deny[_key] = _deny.get(_key, 0) + 1
                 agent._deny_counts = _deny
                 if _deny[_key] >= 3:
                     if not getattr(agent, "_warned_same_tool_loop", 0):
                         set_failed(agent, f"工具 {_tool} 被反复{err_type}(≥3次), LLM陷入死胡同, 停止循环")
         else:
             set_failed(agent, error_msg)
     else:
         if llm_response.get("type") == "action":
             _tool = llm_response.get("tool_name", "")
             if _tool:
                 _deny = getattr(agent, "_deny_counts", {}) or {}
                 _deny.pop((str(_tool), "user_rejected"), None)
                 _deny.pop((str(_tool), "blocked"), None)
                 agent._deny_counts = _deny
-
-    # action_handler路径: 返回一个通用executing结果, event_emitter会根据上下文处理
-    return {"action": "executing", "source": "action_handler"}
+    # action_handler路径：状态已推断，返回通用executing结果，event_emitter按上下文处理
+    return {"action": "executing", "source": "action_handler"}
```

#### 5.3.2 Diff：run_react_cycle中_dispatch_handler调用点

> 关键改动：从 `async for event in _dispatch_handler(...): yield event`
> 改为 `result = await _dispatch_handler(...)` + `event_emitter` 转换。

```diff
     # ── 场景E: 正常分发 ─────────────────────────────────────────
     agent._consecutive_truncations = 0
-    async for event in _dispatch_handler(agent, llm_response):
-        yield event
+    _dispatch_result = await _dispatch_handler(agent, llm_response)
+    # 业务结果→前端Step统一转换
+    from app.services.agent.event_emitter import emit_from_business_result
+    for _s in emit_from_business_result(agent, step, _dispatch_result):
+        yield _s
```

#### 5.3.3 优化效果

| 维度 | 当前 | 优化后 |
|------|------|--------|
| `_dispatch_handler` | async generator（yield Step） | async function（return dict） |
| yield点 | 通过handler转发 | 0处（通过event_emitter） |
| 职责 | 调度+前端混合 | 纯调度（状态推断保留） |

---

### 5.4 event_emitter.py 新建（统一转换层）

**新建文件**：`backend/app/services/agent/event_emitter.py`

> 这是架构改造的核心：所有handler的业务结果→前端Step转换集中在此函数。
> SSE层只转发它产出的Step，不再关心业务逻辑。

```python
# -*- coding: utf-8 -*-
# 编辑历史:
# 2026-09-04 小健 - 新建: 统一转换层, handler业务结果→前端Step。
#   解决病根: handler做前端的事(yield Step), 业务逻辑和展示逻辑混在一起。
#   职责: 读取handler返回的dict, 按action类型构造并yield对应的Step对象。
"""
event_emitter — 统一转换层：业务结果 → 前端Step

所有handler返回dict后, 由本模块统一转换为前端Step对象并yield给SSE层。
修改前端展示逻辑只需改本文件, 不动handler。
本模块为同步生成器（yield非async yield），调用方在async上下文中用 `for _s in emit_from_business_result(...): yield _s` 即可。
"""
from app.services.agent.steps import MetaStep, FinalStep, ThoughtStep, ThoughtStartStep


def emit_from_business_result(agent, step: int, result: dict):
    """统一转换: 业务结果dict → 前端Step generator

    参数:
        agent: Agent实例, 提供 _step_emitter 等基础设施
        step: 当前步号
        result: handler返回的dict, 必须含 "action" 字段

    产出: yield Step对象给SSE层转发
    """
    action = result.get("action", "")

    # ════════════════════════════════════════════════════
    # answer_handler 返回的结果
    # ════════════════════════════════════════════════════
    if action == "completed":
        yield agent._step_emitter.emit(ThoughtStartStep(step=step))
        for _s in agent._step_emitter.emit_final_with_stats(FinalStep(
            step=step,
            response=result.get("response", ""),
            outcome="completed",
            reasoning=result.get("reasoning", ""),
        )):
            yield _s

    elif action == "failed":
        yield agent._step_emitter.emit(ThoughtStartStep(step=step))
        for _s in agent._step_emitter.emit_final_with_stats(FinalStep(
            step=step,
            response=result.get("response", "任务执行失败"),
            outcome="failed",
            error_type=result.get("error_type", ""),
            error_message=result.get("error_message", ""),
        )):
            yield _s

    elif action == "retrying":
        yield agent._step_emitter.emit(MetaStep(
            step=step,
            type="retrying",
            content="系统重试中...",
            wait_time=result.get("wait_time", 1),
            severity="info",
        ))

    elif action == "thought":
        yield agent._step_emitter.emit(ThoughtStartStep(step=step))
        yield agent._step_emitter.emit(ThoughtStep(
            step=step,
            content=result.get("content", ""),
            reasoning=result.get("reasoning", ""),
        ))

    # ════════════════════════════════════════════════════
    # sandbox_gate 返回的结果
    # 注意: paused MetaStep由sandbox_resolve在阻塞等待前直接发射(过渡期设计)
    # event_emitter只处理confirmed→resumed / rejected→error
    # ════════════════════════════════════════════════════
    elif action == "passthrough":
        pass  # 沙箱直通, 无需yield

    elif action == "blocked":
        yield agent._step_emitter.emit(MetaStep(
            step=step,
            type="error",
            content=result.get("reason", "安全策略拦截"),
            error_type="blocked",
            tool_name=result.get("tool_name", ""),
            severity="warn",
        ))

    elif action == "confirmed":
        # 沙箱用户裁决确认 → 发resumed使paused/resumed成对
        # (paused已由sandbox_resolve在阻塞前发射)
        yield agent._step_emitter.emit(MetaStep(
            step=step,
            type="resumed",
            content=f"沙箱预检已确认执行: {result.get('tool_name', '')}",
            severity="info",
        ))

    elif action == "rejected":
        yield agent._step_emitter.emit(MetaStep(
            step=step,
            type="error",
            content=result.get("reason", "用户拒绝执行"),
            error_type="user_rejected",
            tool_name=result.get("tool_name", ""),
            severity="warn",
        ))

    # ════════════════════════════════════════════════════
    # action_handler 尚未改造, 仍yield Step
    # ════════════════════════════════════════════════════
    elif action == "executing":
        pass  # action_handler内部已yield, 此处不重复

    # ════════════════════════════════════════════════════
    # 未知action → error事件
    # ════════════════════════════════════════════════════
    else:
        yield agent._step_emitter.emit(MetaStep(
            step=step,
            type="error",
            content=f"未知业务结果: {action}",
            error_type="unknown_action",
            severity="warn",
        ))
```

---

### 5.5 实施步骤与依赖（本章4文件简化版，完整TDD计划见第八章）

| 步骤 | 动作 | 文件 | 依赖 | 对应TDD阶段 |
|------|------|------|------|-------------|
| 1 | 新建event_emitter.py | `backend/app/services/agent/event_emitter.py` | 无 | 阶段1 |
| 2 | 改造answer_handler.py | `backend/app/services/agent/handlers/answer_handler.py` | 步骤1 | 阶段2 |
| 3 | 改造sandbox_gate.py | `backend/app/services/agent/handlers/sandbox_gate.py` | 步骤1 | 阶段3 |
| 4 | 改造react_cycle.py | `backend/app/services/agent/react_cycle.py` | 步骤2+3 | 阶段4 |
| 5 | 编译检查 | `python -m py_compile` 四个文件 | 步骤4 | 阶段5 |

---

### 5.6 优化效果汇总

| 文件 | 当前行数 | 优化后行数 | yield点变化 | 职责变化 |
|------|---------|-----------|------------|---------|
| answer_handler.py | 235行 | 120行 | 9处→0处 | 业务+前端→纯业务 |
| sandbox_gate.py | 125行 | 95行 | 4处→0处 | 业务+前端→纯业务 |
| react_cycle.py | 912行 | ~920行 | 通过handler→0处 | 调度+前端→纯调度 |
| event_emitter.py | 不存在 | 90行 | 0处→统一yield | 新建转换层 |

---

## 六、目录结构优化（8个文件完整分析）

### 6.1 当前分布（错误，8个文件职责混乱）

```
backend/app/services/agent/handlers/     # 混合业务+前端逻辑
├── action_handler.py      # 1144行，混合业务+前端逻辑 ❌ 需要改造
├── answer_handler.py      # 235行，混合业务+前端逻辑 ❌ 需要改造
└── sandbox_gate.py        # 125行，混合业务+前端逻辑 ❌ 需要改造

backend/app/services/agent/              # 混合调度+前端逻辑
├── react_cycle.py         # 循环核心，混合调度+前端逻辑 ❌ 需要改造
├── step_emitter.py        # 统一发射层 ✅ 保持
├── agent_runner.py        # 后台运行器，SSE通道路由 ✅ 保持
├── llm_stream.py          # LLM流处理 ✅ 保持
└── (agent_telemetry.py → app/monitoring/agent_telemetry.py) # 遥测采集 ✅ 保持（独立模块）

backend/app/monitoring/                  # 监控层（独立）
└── agent_telemetry.py     # 遥测采集 ✅ 保持（SLAP修复见第七章项3）

backend/app/services/chat/               # SSE传输层
├── stream_orchestrator.py # SSE编排层 ✅ 保持
└── stream_reader.py       # SSE读取层 ✅ 保持

backend/app/services/task/               # 业务层
└── hitl_confirmation.py   # HITL确认机制 ✅ 保持

backend/app/services/agent/              # 转换层（不存在）
└── （无）                  # ❌ 需要新建 event_emitter.py
```

### 6.2 正确分布（优化后，8个文件职责清晰）

```
backend/app/services/agent/handlers/     # 业务层：只关心业务
├── action_handler.py      # <500行，纯业务：安全检查、HITL确认、工具执行
├── answer_handler.py      # <100行，纯业务：答案解析、错误处理
├── sandbox_gate.py        # <95行，纯业务：沙箱预检、用户裁决

backend/app/services/task/               # 业务层：保持不变
└── hitl_confirmation.py   # 208行，纯业务：HITL确认机制

backend/app/services/agent/              # 调度层+转换层+基础设施层
├── react_cycle.py         # 调度层：循环核心，调用handler、分发结果
├── event_emitter.py       # 转换层（新建）：业务结果→前端Step，统一yield
├── step_emitter.py        # 基础设施层：统一发射入口（保持）
├── llm_stream.py          # 基础设施层：LLM流处理（保持）
└── agent_runner.py        # 运行层：后台运行+SSE通道路由（保持）

backend/app/monitoring/                  # 监控层（独立）
└── agent_telemetry.py     # 基础设施层：遥测采集、Step构建（保持，SLAP修复见第七章项3）

backend/app/services/chat/               # SSE层：保持不变
├── stream_orchestrator.py # SSE编排层
└── stream_reader.py       # SSE读取层
```

### 6.3 8个文件职责层次对照表

| 文件 | 当前层次 | 优化后层次 | 职责 | 是否需要改造 |
|------|---------|-----------|------|-------------|
| action_handler.py | 业务+前端混合 | **业务层** | 安全检查、HITL确认、工具执行 | ✅ 需要改造 |
| answer_handler.py | 业务+前端混合 | **业务层** | 答案解析、错误处理 | ✅ 需要改造 |
| sandbox_gate.py | 业务+前端混合 | **业务层** | 沙箱预检、用户裁决 | ✅ 需要改造 |
| hitl_confirmation.py | 纯业务 | **业务层** | HITL确认机制 | ❌ 保持 |
| react_cycle.py | 调度+前端混合 | **调度层** | 循环核心、分发handler | ✅ 需要改造 |
| event_emitter.py | 不存在 | **转换层（新建）** | 业务结果→前端Step | ✅ 新建 |
| step_emitter.py | 基础设施 | **基础设施层** | 统一发射入口 | ❌ 保持 |
| agent_telemetry.py | 监控 | **基础设施层** | 遥测采集、Step构建 | ❌ 保持 |
| llm_stream.py | 基础设施 | **基础设施层** | LLM流处理 | ❌ 保持 |
| agent_runner.py | 运行层 | **运行层** | 后台运行、SSE通道路由 | ❌ 保持 |
| stream_orchestrator.py | SSE层 | **SSE层** | SSE编排转发 | ❌ 保持 |
| stream_reader.py | SSE层 | **SSE层** | SSE读取 | ❌ 保持 |

---

## 七、SRP合规拆分优化（代码10大规范修复）

> **定位**：第五章解决yield架构问题（业务+前端混合），本章解决SRP合规问题（1文件6职责）。
> 两个层面互补：架构层拆清楚后，代码层也必须拆干净，否则烂代码到处修改的老问题还会反复出现。
> **来源**：基于 `后端全代码10规范审查与重构方案-小欧-2026-09-03.md` 第九章，按10大规范重新审视、合并、精简。

### 7.1 审视结论：8项→4项（合并冗余、废弃低价值）

原审查提出8个拆分项，经10大规范复核后调整为**4项**：

| 原项 | 处置 | 理由 |
|------|------|------|
| trust_utils.py（路径提取解环） | **保留→合入项1** | DRY+解环，已实施 |
| trust_service.py（信任跳过判定） | **保留→合入项1** | SRP，与trust_utils同属信任域 |
| trust_repository.py（信任落库） | **保留→合入项1** | SRP，与trust_service同属信任域 |
| conflict_detector.py（冲突检测） | **保留→项2** | SRP，独立冲突检测模块 |
| answer_parser.py（解析下沉） | **废弃** | YAGNI：仅3行format调用，抽模块过度设计 |
| failure_emitter.py（失败发射下沉） | **废弃** | DRY违规：已被第五章event_emitter完全取代 |
| agent_telemetry修复（参数化outcome） | **保留→合入项3** | SLAP修复，与step_emitter同属emit链路 |
| step_emitter透传（outcome参数） | **保留→合入项3** | SLAP修复，与agent_telemetry同属emit链路 |

**调整后4项清单**：

| # | 新建/改动文件 | 解决的规范问题 | 行数 |
|---|-------------|--------------|------|
| 1 | trust.py（合并trust_utils+trust_service+trust_repository） | DRY+SRP+解环 | 112行 |
| 2 | conflict_detector.py | SRP | 65行 |
| 3 | agent_telemetry.py + step_emitter.py 改动 | SLAP | 2处小改动 |
| 4 | action_handler.py 改动（引用项1+2） | 复用优先+KISS | 删内联→import |

---

### 7.2 项1：信任域合并为trust.py（112行）

**病根**：action_handler.py 中信任相关代码散落3处（路径提取L551-587、跳过判定L297-327、落库无独立模块），sandbox_gate.py 反向import action_handler._extract_trust_path形成环依赖。

**方案**：合并为1个文件 `backend/app/tools/trust.py`，4个函数：纯函数（路径解析/提取）+ async函数（跳过判定/落库）。

**复制来源**：
- `_parse_paths` ← action_handler.py L551-578
- `extract_trust_path` ← action_handler.py L581-587
- `resolve_skip` ← action_handler.py L297-327（提取为独立函数，加try/except防御+logger降级）
- `save_session_trust` ← hitl_confirmation.py L184-196（提取为独立函数，加ValueError防御）

```python
# -*- coding: utf-8 -*-
# 编辑历史: 2026-09-03 小欧 - 新建: trust 提取下沉，解环 - 小欧-2026-09-03
# 编辑历史: 2026-09-04 小健 - 合并: trust_utils+trust_service+trust_repository三合一为trust.py - 小健-2026-09-04
from typing import Dict, Optional, Set
from app.tools.tools_alias_mapper import PARAM_ALIASES, normalize_tool_name
from app.tools.tool_constants import FILE_OPERATION_TOOLS

WINDOW_TARGET_TOOLS = {"window_focus", "window_resize", "set_window_state"}

# ════════════════════════════════════════════════════════════
# 纯函数：路径解析（复制自 action_handler.py:551-578）
# ════════════════════════════════════════════════════════════

def _parse_paths(name: str, params: Dict) -> Set[str]:
    """解析一个调用的路径/窗口冲突键集合(复用 PARAM_ALIASES 别名→规范名)
    完整复制自 action_handler.py:551-578，保留别名映射逻辑，禁止简化退化"""
    if name in WINDOW_TARGET_TOOLS:
        title = params.get("window_title", "")
        if title and isinstance(title, str):
            return {f"window:{title}"}
        return set()
    if name not in FILE_OPERATION_TOOLS:
        return set()
    aliases = PARAM_ALIASES.get(name, {})
    if not aliases:
        p = params.get("path", "")
        return {p} if p and isinstance(p, str) else set()
    resolved = {}
    for key, value in params.items():
        canon = aliases.get(key, key)
        if canon not in resolved:
            resolved[canon] = value
    out = set()
    for pname in set(aliases.values()):
        pval = resolved.get(pname)
        if pval and isinstance(pval, str):
            out.add(pval)
    return out

def extract_trust_path(name: str, params: Dict) -> Optional[str]:
    """提取工具调用用于信任落库/查询的目标路径(复用 _parse_paths, DRY)
    完整复制自 action_handler.py:581-587，取首个非 window: 键的文件路径"""
    for p in _parse_paths(name, params or {}):
        if not p.startswith("window:"):
            return p
    return None

# ════════════════════════════════════════════════════════════
# async函数：信任跳过判定（复制自 action_handler.py:297-327）
# ════════════════════════════════════════════════════════════

async def resolve_skip(agent_task_id: str, tool_name: str, params: dict) -> bool:
    """信任跳过判定: 查询会话级信任记录,决定是否跳过HITL确认"""
    from app.db import db
    from app.services.chat.storage import get_session_id_by_task, check_session_trust
    try:
        _sid = await db.atxn("chat", lambda conn: get_session_id_by_task(conn, agent_task_id))
    except Exception:
        return False
    if not _sid:
        return False
    _tgt = extract_trust_path(tool_name, params)
    try:
        return await db.atxn("chat", lambda conn: check_session_trust(conn, _sid, normalize_tool_name(tool_name), _tgt))
    except Exception as _te:
        from app.logger import logger
        logger.warning(f"[trust] 会话信任查询失败: task={agent_task_id}, tool={tool_name}, err={_te}")
        return False

# ════════════════════════════════════════════════════════════
# async函数：信任落库（复制自 hitl_confirmation.py:184-196）
# ════════════════════════════════════════════════════════════

async def save_session_trust(task_id: str, tool_name: str, path: str | None):
    """会话信任落库: 用户确认后将工具+路径写入会话信任记录"""
    from app.db import db
    from app.services.chat.storage import get_session_id_by_task, insert_session_trust
    def _do(conn):
        _sid = get_session_id_by_task(conn, task_id)
        if not _sid:
            raise ValueError(f"[HITL] task_id={task_id} 无 session_id 可反查,信任禁止落库")
        insert_session_trust(conn, _sid, normalize_tool_name(tool_name), path)
    await db.atxn("chat", _do)
```

#### 7.2.4 action_handler.py 引用改动

**改动点1**（L297-327）：信任跳过判定从内联改为import trust

```diff
-        _session_id = await db.atxn("chat", lambda conn: get_session_id_by_task(conn, agent.task_id))
-        _tgt = _extract_trust_path(_cn, _cp)
-        _skip = await db.atxn("chat", lambda conn: check_session_trust(conn, _session_id, normalize_tool_name(_cn), _tgt))
+        from app.tools.trust import resolve_skip
+        _skip = await resolve_skip(agent.task_id, _cn, _cp)
```

**改动点2**（L551-587）：路径提取从内联改为import trust

```diff
-    def _parse_paths(name, params):
-        ... (27行内联)
-    def _extract_trust_path(name, params):
-        ... (7行内联)
+    from app.tools.trust import _parse_paths, extract_trust_path as _extract_trust_path
```

**改动点3**（hitl_confirmation.py L184）：信任落库从内联改为import trust

```diff
-        _sid = get_session_id_by_task(conn, _task_id)
-        insert_session_trust(conn, _sid, normalize_tool_name(entry.tool_name), getattr(entry, "path", None))
+        from app.tools.trust import save_session_trust
+        await save_session_trust(_task_id, entry.tool_name, getattr(entry, "path", None))
```

---

### 7.3 项2：冲突检测下沉（conflict_detector.py，65行）

**病根**：action_handler.py 中 `_has_conflict`（L590-621）和 `_partition_calls`（L623-655）是独立的冲突检测逻辑，与handler的业务流程混合，违反SRP。

**方案**：剪切到独立模块，复用trust._parse_paths（DRY）。

```python
# -*- coding: utf-8 -*-
# 编辑历史: 2026-09-03 小欧 - 新建: 冲突检测三函数下沉，解耦 action_handler - 小欧-2026-09-03
# 编辑历史: 2026-09-04 小健 - 修正: 复用 trust._parse_paths，禁止重写简化退化 - 小健-2026-09-04
# 编辑历史: 2026-09-04 小健 - 修正: trust_utils已合并为trust.py，import路径同步更新 - 小健-2026-09-04
from typing import Dict, List
from app.tools.trust import _parse_paths

def _has_conflict(calls: List[Dict]) -> bool:
    """完整复制自 action_handler.py:590-621，复用 _parse_paths"""
    from collections import Counter
    names = [c.get("tool_name","") for c in calls]
    if len(names) != len(set(names)):
        cnt = Counter(names)
        if any(v >= 2 and ("edit" in k or "write" in k or "delete" in k) for k,v in cnt.items()):
            return True
    paths = {}
    for c in calls:
        for p in _parse_paths(c.get("tool_name",""), c.get("tool_params",{})):
            paths.setdefault(p, 0)
            paths[p] += 1
            if paths[p] >= 2:
                return True
    return False

def _partition_calls(calls: List[Dict]) -> List[List[Dict]]:
    """完整复制自 action_handler.py:623-655，复用 _parse_paths"""
    parent = list(range(len(calls)))
    def find(x):
        while parent[x] != x:
            parent[x] = parent[parent[x]]
            x = parent[x]
        return x
    def union(a,b):
        ra, rb = find(a), find(b)
        if ra != rb:
            parent[rb] = ra
    for i in range(len(calls)):
        for j in range(i+1, len(calls)):
            pi = _parse_paths(calls[i].get("tool_name",""), calls[i].get("tool_params",{}))
            pj = _parse_paths(calls[j].get("tool_name",""), calls[j].get("tool_params",{}))
            if pi & pj or calls[i].get("tool_name")==calls[j].get("tool_name"):
                union(i,j)
    groups: Dict[int, List[Dict]] = {}
    for idx, c in enumerate(calls):
        r = find(idx)
        groups.setdefault(r, []).append(c)
    return list(groups.values())
```

**action_handler.py 引用改动**：

```diff
-    def _has_conflict(calls):
-        ... (32行内联)
-    def _partition_calls(calls):
-        ... (33行内联)
+    from app.tools.conflict_detector import _has_conflict, _partition_calls
```

---

### 7.4 项3：emit链路SLAP修复（agent_telemetry + step_emitter）

**病根**：`build_final_stats_step` 隐式读 `agent.status`（SLAP违规：监控层依赖核心状态），`emit_final_with_stats` 隐式透传outcome（SLAP违规：发射层依赖遥测层）。

**方案**：outcome参数显式传递，消除隐式依赖。

#### 7.4.1 agent_telemetry.py 改动（`backend/app/monitoring/agent_telemetry.py` L145-161）

**操作步骤**：先复制原build_final_stats_step函数完整代码 → 再改造：加outcome参数，显式传递替代隐式读agent.status。

```diff
-    def build_final_stats_step(self):
+    def build_final_stats_step(self, outcome: str = ""):
         """产出 MetaStep(type="final_stats")"""
         from app.services.agent.steps.base import MetaStep
         _agent = self.agent
         _duration = round(time.time() - self._run_start_ts, 1) if self._run_start_ts else 0.0
-        _final_status = getattr(getattr(_agent, "status", None), "value", None)
+        if outcome:
+            _final_status = outcome
+        else:
+            _final_status = getattr(getattr(_agent, "status", None), "value", None)
         return MetaStep(
             step=getattr(_agent, "llm_call_count", 0),
             type="final_stats",
             content="",
             duration=_duration,
             artifacts=list(self._artifacts),
             severity="info",
             final_status=_final_status,
         )
```

#### 7.4.2 step_emitter.py 改动（`backend/app/services/agent/step_emitter.py` L58-62）

**操作步骤**：先复制原emit_final_with_stats函数完整代码 → 再改造：加outcome提取+透传给build_final_stats_step。

```diff
     def emit_final_with_stats(self, final_step):
-        """final 后单独 emit 终态统计事件"""
-        return (self.emit(final_step), self.emit(self.agent.telemetry.build_final_stats_step()))
+        """final 后单独 emit 终态统计事件 —— outcome 显式透传，消除隐式依赖"""
+        _outcome = getattr(final_step, "outcome", "completed")
+        return (self.emit(final_step), self.emit(self.agent.telemetry.build_final_stats_step(outcome=_outcome)))
```

---

### 7.5 项4：action_handler引用整合

**改动汇总**：action_handler.py 在项1+项2完成后，删除内联代码改为import，净减约100行。

| 删除位置 | 改为import | 来源 |
|----------|-----------|------|
| L297-327（信任跳过30行） | `from app.tools.trust import resolve_skip` | 项1 |
| L551-587（路径提取37行） | `from app.tools.trust import _parse_paths, extract_trust_path` | 项1 |
| L590-655（冲突检测65行） | `from app.tools.conflict_detector import _has_conflict, _partition_calls` | 项2 |

---

### 7.6 优化效果汇总

| 维度 | 改前 | 改后 | 说明 |
|------|------|------|------|
| action_handler.py | 1144行，6职责 | ~1040行，4职责（-100行） | 仅本章SRP拆分效果；叠加第五章yield改造后可达<500行 |
| 新建文件 | 0 | 2个（trust.py/conflict_detector） | |
| 环依赖 | sandbox_gate→action_handler | sandbox_gate→trust（单向，无环） | |
| 隐式依赖 | build_final_stats读agent.status | outcome显式参数传递 | |
| SRP违规 | 6处 | 0处 | |
| DRY违规 | 沙箱3处+路径2处 | 0处（统一trust.py） | |

---

### 7.7 与第五章yield架构改造的关系

| 维度 | 第五章（yield架构） | 本章（SRP合规） |
|------|-------------------|----------------|
| 解决问题 | handler做前端的事 | 1文件6职责 |
| 改动文件 | answer_handler/sandbox_gate/react_cycle | action_handler/trust/conflict |
| 核心产出 | event_emitter.py | trust.py+conflict_detector |
| 依赖关系 | 无依赖，可独立实施 | 依赖第五章（sandbox_gate改造后引用trust更干净） |

---

## 八、实施计划和步骤（TDD模式）

> **TDD铁律**：Red→Green→Refactor，测试先行。
> - **Red**：先写测试 → 跑测试 → 必须失败（因为实现代码还不存在）
> - **Green**：写最小实现代码 → 跑测试 → 必须全部通过
> - **Refactor**：重构优化 → 跑测试 → 不能退化（全部仍然通过）
>
> 每个阶段结束时：运行该阶段涉及的全部测试，确认状态正确。

### 8.0 前置准备：现有测试适配

> **目标**：在任何业务代码改造前，先把现有测试调整到能适配新接口。这样后续改造时测试就能立刻验证正确性。

| 步骤 | 动作 | 文件 | 说明 |
|------|------|------|------|
| 0.1 | 修改emitter mock | `tests/_emitter_mock.py` | `emit_final_with_stats`签名补`outcome`参数（P0） |
| 0.2 | 修改answer_handler测试 | `tests/test_answer_handler_edge_cases.py` | L40 `async for`→`await` + dict断言（P0） |
| 0.3 | 修改sandbox_gate测试 | `tests/test_sandbox_gate.py` | 6处元组解包→dict访问（P1） |
| 0.4 | 检查action_handler测试 | `tests/test_action_handler_edge_cases.py` | `inspect.getsource`匹配路径检查（P1） |
| 0.5 | **验证** | 运行0.1-0.4涉及的全部测试 | 确认现有测试已适配新接口（此时实现代码未改，所以部分测试会失败——这是正确的Red状态） |

### 8.1 阶段1：event_emitter（新模块，TDD标准流程）

| 步骤 | 动作 | 说明 |
|------|------|------|
| 1.1 **Red** | 新建`tests/test_event_emitter.py`（9个case） | 跑测试→全部FAIL（模块不存在） |
| 1.2 **Green** | 新建`backend/app/services/agent/event_emitter.py` | 实现转换逻辑 |
| 1.3 | 运行`pytest tests/test_event_emitter.py -v` | 9个case全部PASS |
| 1.4 | 编译检查`python -m py_compile event_emitter.py` | 无错误 |
| 1.5 **Refactor** | 优化代码结构，消除重复 | 跑测试→不能退化 |

**产出物**：
- `tests/test_event_emitter.py`：9个测试case
- `backend/app/services/agent/event_emitter.py`：<100行

### 8.2 阶段2：answer_handler改造（现有模块，测试先行改造）

> **v2.6修正**： Phase2实际执行时遗漏了reasoning_only_limit分支的set_failed调用。补充检查项2.7。

| 步骤 | 动作 | 说明 |
|------|------|------|
| 2.1 | 确认步骤0.2的测试已就绪 | `test_answer_handler_edge_cases.py`已适配新接口 |
| 2.2 **Green** | 改造`answer_handler.py` | 删除yield，返回dict |
| 2.3 | 运行`pytest tests/test_answer_handler_edge_cases.py -v` | 全部PASS |
| 2.4 | 运行`pytest tests/handlers/test_final_quality_guard.py -v` | `_dedup_repeat`不受影响 |
| 2.5 | 编译检查`python -m py_compile answer_handler.py` | 无错误 |
| 2.6 **Refactor** | 优化函数结构，删除冗余代码 | 跑测试→不能退化 |
| 2.7 **⚠️ 遗漏检查** | **检查所有return dict路径是否都有set_failed调用** | `reasoning_only_limit`分支返回`"action": "failed"`但没调`set_failed()`→agent.status永远是EXECUTING（v2.6错误3） |

**产出物**：
- `answer_handler.py`：<100行，纯业务逻辑

### 8.3 阶段3：sandbox_gate改造（现有模块，测试先行改造）

> **注意**：sandbox_gate中`_sb_trust_path`的import在阶段3暂保持`from app.services.agent.handlers.action_handler import _extract_trust_path`（与现网一致），待阶段6 trust.py就绪后在阶段8统一改为`from app.tools.trust import extract_trust_path`，彻底消除环依赖。阶段3仅改yield→dict返回值，不动import路径。

> **v2.6修正**： Phase3实际执行时遗漏了blocked/rejected路径的resumed事件，导致前端badge卡在"paused"。补充检查项3.6。

| 步骤 | 动作 | 说明 |
|------|------|------|
| 3.1 | 确认步骤0.3的测试已就绪 | `test_sandbox_gate.py`已适配dict解包 |
| 3.2 **Green** | 改造`sandbox_gate.py` | 删除yield，返回dict（import路径暂不动） |
| 3.3 | 运行`pytest tests/test_sandbox_gate.py -v` | 全部PASS |
| 3.4 | 编译检查`python -m py_compile sandbox_gate.py` | 无错误 |
| 3.5 **Refactor** | 优化函数结构 | 跑测试→不能退化 |
| 3.6 **⚠️ 遗漏检查** | **检查所有路径是否都有配对事件** | `blocked`/`rejected`路径返回dict后，caller继续执行但没有发"resumed" MetaStep→前端badge卡在"paused"（v2.6错误4） |

**产出物**：
- `sandbox_gate.py`：<95行，纯业务逻辑（import路径在阶段8统一修正）

### 8.4 阶段4：react_cycle改造（现有模块，依赖阶段1-3）

> **v2.6修正**： Phase4实际执行时遗漏了action路径的yield event转发，导致事件丢失。补充检查项4.7。

| 步骤 | 动作 | 说明 |
|------|------|------|
| 4.1 **Green** | 改造`react_cycle.py` | `_dispatch_handler`从async generator改为async function |
| 4.2 | 导入event_emitter | `from app.services.agent.event_emitter import emit_from_business_result` |
| 4.3 | 运行`pytest tests/test_react_cycle.py -v` | 全部PASS |
| 4.4 | 运行阶段1-3全部测试 | 回归验证 |
| 4.5 | 编译检查`python -m py_compile react_cycle.py` | 无错误 |
| 4.6 **Refactor** | 精简调度逻辑 | 跑测试→不能退化 |
| 4.7 **⚠️ 遗漏检查** | **检查action路径事件转发** | `handle_action` yield的ActionStep/ObservationStep必须通过`emit_from_business_result`转发给调用方，不能被消费后丢失（v2.6错误2） |

**产出物**：
- `react_cycle.py`：纯调度逻辑

### 8.5 阶段5：yield架构集成验证

| 步骤 | 动作 | 说明 |
|------|------|------|
| 5.1 | 运行全部现有测试 | `pytest tests/ -x --tb=short` |
| 5.2 | 分析失败用例 | 区分"测试未适配"和"代码bug" |
| 5.3 | 修复代码bug（如有） | 跑测试→通过 |
| 5.4 | 补充遗漏测试（如有） | 跑测试→通过 |

**产出物**：
- 阶段1-4全部测试PASS，无回归

### 8.6 阶段6：trust.py（新模块，TDD标准流程）

| 步骤 | 动作 | 说明 |
|------|------|------|
| 6.1 **Red** | 新建`tests/test_trust.py`（8个case） | 跑测试→全部FAIL（模块不存在） |
| 6.2 **Green** | 新建`backend/app/tools/trust.py`（112行，三合一） | 实现信任域逻辑 |
| 6.3 | 运行`pytest tests/test_trust.py -v` | 8个case全部PASS |
| 6.4 | 编译检查`python -m py_compile trust.py` | 无错误 |
| 6.5 **Refactor** | 优化代码结构 | 跑测试→不能退化 |

**产出物**：
- `tests/test_trust.py`：8个测试case
- `backend/app/tools/trust.py`：112行

### 8.7 阶段7：conflict_detector（新模块，TDD标准流程）

| 步骤 | 动作 | 说明 |
|------|------|------|
| 7.1 **Red** | 新建`tests/test_conflict_detector.py`（6个case） | 跑测试→全部FAIL（模块不存在） |
| 7.2 **Green** | 新建`backend/app/tools/conflict_detector.py`（65行） | 实现冲突检测逻辑 |
| 7.3 | 运行`pytest tests/test_conflict_detector.py -v` | 6个case全部PASS |
| 7.4 | 编译检查`python -m py_compile conflict_detector.py` | 无错误 |
| 7.5 **Refactor** | 优化代码结构 | 跑测试→不能退化 |

**产出物**：
- `tests/test_conflict_detector.py`：6个测试case
- `backend/app/tools/conflict_detector.py`：65行

### 8.8 阶段8：action_handler引用整合 + 环依赖消除（现有模块，依赖阶段6-7）

> **v2.6修正**： Phase8实际执行时遗漏了sandbox_resolve调用方的同步更新，导致ValueError。补充检查项8.10。

| 步骤 | 动作 | 说明 |
|------|------|------|
| 8.1 | 确认步骤0.4的测试已就绪 | `test_action_handler_edge_cases.py`检查通过 |
| 8.2 **Green** | 改造`action_handler.py` | 删除内联代码→import trust.py+conflict_detector |
| 8.3 | 同步修正`sandbox_gate.py` import | `from action_handler import _extract_trust_path` → `from app.tools.trust import extract_trust_path` |
| 8.4 | 运行`pytest tests/test_action_handler_edge_cases.py -v` | 全部PASS |
| 8.5 | 运行`pytest tests/test_sandbox_gate.py -v` | 回归：环依赖消除后复测 |
| 8.6 | 运行`pytest tests/handlers/test_action_handler_auto_correct.py -v` | 不受影响 |
| 8.7 | 运行`pytest tests/handlers/test_action_handler_merge.py -v` | 不受影响 |
| 8.8 | 编译检查 | `python -m py_compile action_handler.py` + `sandbox_gate.py` 均无错误 |
| 8.9 **Refactor** | 优化import结构 | 跑测试→不能退化 |
| 8.10 **⚠️ 遗漏检查** | **检查sandbox_resolve所有调用方** | `sandbox_resolve`返回值从`(bool, list)`改为`dict`后，必须搜索所有调用方（grep `sandbox_resolve`）确认元组解包已改为dict访问（v2.6错误1） |

**产出物**：
- `action_handler.py`：净减~100行
- `sandbox_gate.py`：import路径修正，环依赖彻底消除（sandbox_gate→trust单向）

### 8.9 阶段9：agent_telemetry + step_emitter（SLAP修复）

| 步骤 | 动作 | 说明 |
|------|------|------|
| 9.1 **Green** | 改造`monitoring/agent_telemetry.py` | `build_final_stats_step`加outcome参数 |
| 9.2 **Green** | 改造`services/agent/step_emitter.py` | `emit_final_with_stats`透传outcome |
| 9.3 | 运行全部测试 | `pytest tests/ -x --tb=short` |
| 9.4 | 编译检查 | 两个文件均无错误 |
| 9.5 **Refactor** | 优化参数传递 | 跑测试→不能退化 |

**产出物**：
- `monitoring/agent_telemetry.py`：outcome显式参数
- `services/agent/step_emitter.py`：outcome透传

### 8.10 阶段10：最终集成验证

| 步骤 | 动作 | 说明 |
|------|------|------|
| 10.1 | 全量单元测试 | `pytest tests/ -x --tb=short` |
| 10.2 | 分析失败用例 | 区分"测试未适配"和"代码bug" |
| 10.3 | 修复遗留问题 | 跑测试→通过 |
| 10.4 | E2E测试 | `pytest e2etests/`（需后端启动） |
| 10.5 | 前端验证 | SSE事件格式/UI渲染/HITL弹窗 |

### 8.11 TDD状态追踪表

> 每个阶段结束时填写此表，确保Red→Green→Refactor三态正确。

| 阶段 | Red（测试先写） | Green（实现通过） | Refactor（不退化） | 最终状态 |
|------|----------------|------------------|-------------------|---------|
| 0-前置 | 0.1-0.4修改现有测试 | — | — | 测试已适配新接口 |
| 1-event_emitter | 1.1 9个case全FAIL | 1.2-1.3 9个PASS | 1.5 不退化 | ✅GREEN |
| 2-answer_handler | 0.2测试已就绪 | 2.2-2.3 全PASS | 2.6 不退化 | ⚠️GREEN（v2.6错误3: set_failed缺失） |
| 3-sandbox_gate | 0.3测试已就绪 | 3.2-3.3 全PASS | 3.5 不退化 | ⚠️GREEN（v2.6错误4: resumed缺失） |
| 4-react_cycle | — | 4.1-4.3 全PASS | 4.6 不退化 | ⚠️GREEN（v2.6错误2: yield丢失） |
| 5-集成验证 | — | 5.1-5.3 全PASS | 5.4 不退化 | ✅GREEN |
| 6-trust.py | 6.1 8个case全FAIL | 6.2-6.3 8个PASS | 6.5 不退化 | ✅GREEN |
| 7-conflict_detector | 7.1 6个case全FAIL | 7.2-7.3 6个PASS | 7.5 不退化 | ✅GREEN |
| 8-action_handler | 0.4测试已就绪 | 8.2-8.5 全PASS | 8.7 不退化 | ⚠️GREEN（v2.6错误1: 已修复） |
| 9-telemetry | — | 9.1-9.3 全PASS | 9.5 不退化 | ✅GREEN |
| 10-最终验证 | — | 10.1-10.3 全PASS | 10.4-10.5 通过 | ⚠️GREEN（3处待修复） |

### 8.12 TDD与传统开发对比

| 维度 | 传统模式（原计划） | TDD模式（新计划） |
|------|-------------------|------------------|
| 执行顺序 | 代码→测试→修bug | 测试→代码→重构 |
| 代码质量 | 可能遗漏边界case | 测试覆盖先行保障 |
| 回归风险 | 改完才测，发现晚 | 每步都测，发现早 |
| 信心来源 | "应该没问题" | "测试全部PASS" |
| 重构勇气 | 怕改坏不敢动 | 有测试兜底放心改 |
| 核心差异 | 先写实现再补测试 | **先写测试再写实现** |

---

## 九、测试验证

### 9.1 编译检查

| 文件 | 命令 | 预期结果 | 来源 |
|------|------|---------|------|
| event_emitter.py | `python -m py_compile event_emitter.py` | 无错误 | 第五章 |
| answer_handler.py | `python -m py_compile answer_handler.py` | 无错误 | 第五章 |
| sandbox_gate.py | `python -m py_compile sandbox_gate.py` | 无错误 | 第五章 |
| react_cycle.py | `python -m py_compile react_cycle.py` | 无错误 | 第五章 |
| trust.py | `python -m py_compile trust.py` | 无错误 | 第七章 |
| conflict_detector.py | `python -m py_compile conflict_detector.py` | 无错误 | 第七章 |
| action_handler.py | `python -m py_compile action_handler.py` | 无错误 | 第七章 |
| monitoring/agent_telemetry.py | `python -m py_compile agent_telemetry.py` | 无错误 | 第七章 |
| services/agent/step_emitter.py | `python -m py_compile step_emitter.py` | 无错误 | 第七章 |

### 9.2 现有测试case影响分析

> **背景**：本次架构优化改变了 handle_answer（generator→dict返回）、sandbox_resolve（元组→dict）、_dispatch_handler（generator→async函数）的返回值/行为，需逐一排查现有测试是否受影响。

#### 9.2.1 必须修改（P0）— 直接编译失败

| 文件 | 影响原因 | 修改要点 |
|------|---------|---------|
| `tests/test_answer_handler_edge_cases.py` | L40使用`async for evt in handle_answer(agent, None)`消费generator，改为dict返回后async for将报错 | L37-48：改为`result = await handle_answer(agent, None)`，按dict结构断言（检查`result["type"]`等） |
| `tests/_emitter_mock.py` | step_emitter mock工厂，所有依赖测试复用。`emit_final_with_stats`的`outcome`参数（新增）未传 | L10-12：`_emit_final_with_stats`需新增`outcome`参数接收（或忽略），确保mock签名与真实接口一致 |

#### 9.2.2 需检查修改（P1）— 返回值结构变化

| 文件 | 影响原因 | 修改要点 |
|------|---------|---------|
| `tests/test_sandbox_gate.py` | 当前L78-169所有sandbox_resolve测试用`ok, steps = asyncio.run(sandbox_resolve(...))`解包元组，改为dict返回后需改为`result = asyncio.run(...)`再`result["ok"]`/`result["steps"]` | L78/87/102/120/142/164：6处元组解包→dict访问 |
| `tests/test_action_handler_edge_cases.py` | 所有测试用`inspect.getsource()`做字符串匹配（L25/L38/L85/L96/L102/L114/L122），源码结构改变（如函数提取到trust.py、import路径变化）会导致匹配失败 | L25`check_session_trust`可能移至trust.py→需检查；L38 `sandbox_precheck`/`sandbox_resolve`属性检查不变；L85/L102 sandbox引用检查可能变化 |
| `tests/handlers/test_action_handler_auto_correct.py` | `execute_tools`签名如果新增`event_emitter`参数，L262/297/326/355的调用需补充参数 | 当前plan：action_handler的execute_tools不改签名（只改内部），**暂不需修改** |
| `tests/handlers/test_action_handler_merge.py` | `build_observation`当前返回`(events, orchestration)`元组（L64），如果改为dict需修改解包 | 当前plan：build_observation接口不变，**暂不需修改** |
| `tests/handlers/test_build_observation.py` | 同上，`build_observation`接口如果不变则无需修改 | 当前plan：**暂不需修改** |
| `tests/test_react_cycle.py` | `handle_react_error`当前返回MetaStep对象，如果改为返回dict需修改断言 | 当前plan：handle_react_error接口不变，**暂不需修改** |

#### 9.2.3 无需修改（P2）— 不涉及改动模块

| 文件 | 原因 |
|------|------|
| `tests/test_trust_v15.py` | 测试信任存储层（storage.*），不涉及trust_utils/trust_service/trust_repository |
| `tests/test_agent_runner.py` | 测试SSE传输层（stream_reader），不涉及改动模块 |
| `tests/handlers/test_final_quality_guard.py` | 测试`_dedup_repeat`纯函数，不受架构影响 |
| `tests/handlers/test_observation_formatter_*.py`（10个） | 测试展示层格式化，不涉及改动模块 |
| `tests/steps/test_action_step.py` | 测试ActionStep类定义 |
| `tests/steps/test_observation_step.py` | 测试ObservationStep类定义 |
| `tests/test_action_handler_auto_correct.py`（部分） | `_auto_correct_file_tool`是纯函数，不受影响 |

#### 9.2.4 修改优先级汇总

```
P0（必改）:
  ✗ test_answer_handler_edge_cases.py  → async for→await + dict断言
  ✗ _emitter_mock.py                   → emit_final_with_stats签名补outcome

P1（需检查修改）:
  ✗ test_sandbox_gate.py               → 6处元组解包→dict访问（sandbox_resolve返回值变化，必须改）
  ✗ test_action_handler_edge_cases.py  → inspect.getsource匹配路径变化（需检查）
  ✓ 其余4个handler测试文件             → 接口不变，暂不需改

P2（不需改）:
  ✓ test_trust_v15.py / test_agent_runner.py / test_final_quality_guard.py / 等
```

### 9.3 新增测试case

| 新增文件 | 测试内容 | 对应新文件 | 测试数量 |
|---------|---------|-----------|---------|
| `tests/test_event_emitter.py` | Step转换层：dict→Step转换、透传/跳过/分支覆盖（9个case：completed/failed/retrying/thought/passthrough/blocked/rejected/executing/unknown） | event_emitter.py | 9个case |
| `tests/test_conflict_detector.py` | 工具调用冲突检测：_has_conflict边界、_partition_calls分组 | conflict_detector.py | ~6个case |
| `tests/test_trust.py` | 信任域三合一：_parse_paths别名映射、extract_trust_path提取、resolve_skip跳过判定、save_session_trust落库 | trust.py | ~8个case |

#### 9.3.1 test_event_emitter.py — Step转换层测试（9个case）

```python
# -*- coding: utf-8 -*-
# 测试event_emitter统一转换层 — 小健-2026-09-04
# 注意：emit_from_business_result为同步生成器（yield非async yield），测试中用list()收集
import pytest
from unittest.mock import MagicMock
from app.services.agent.event_emitter import emit_from_business_result
from app.services.agent.steps import MetaStep, FinalStep, ThoughtStep, ThoughtStartStep

def _make_agent():
    agent = MagicMock()
    agent._step_emitter = MagicMock()
    agent._step_emitter.emit = MagicMock(side_effect=lambda x: x)
    agent._step_emitter.emit_final_with_stats = MagicMock(side_effect=lambda step: (step, MagicMock()))
    return agent

# === completed分支 ===

def test_completed_emits_final():
    """action=completed→ThoughtStartStep + FinalStep(completed)"""
    agent = _make_agent()
    result = {"action": "completed", "response": "done", "reasoning": ""}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 2
    assert any(isinstance(s, FinalStep) for s in steps)

def test_failed_emits_final_with_error():
    """action=failed→ThoughtStartStep + FinalStep(failed)"""
    agent = _make_agent()
    result = {"action": "failed", "response": "fail", "error_type": "llm_error", "error_message": "err"}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 2
    assert any(isinstance(s, FinalStep) and s.outcome == "failed" for s in steps)

# === retrying/thought分支 ===

def test_retrying_emits_meta():
    """action=retrying→MetaStep(type=retrying)"""
    agent = _make_agent()
    result = {"action": "retrying", "wait_time": 1}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 1
    assert isinstance(steps[0], MetaStep)
    assert steps[0].type == "retrying"

def test_thought_emits_thought_step():
    """action=thought→ThoughtStartStep + ThoughtStep"""
    agent = _make_agent()
    result = {"action": "thought", "content": "reasoning text", "reasoning": ""}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 2
    assert any(isinstance(s, ThoughtStep) for s in steps)

# === sandbox分支 ===

def test_passthrough_no_emission():
    """action=passthrough→无Step（直通）"""
    agent = _make_agent()
    result = {"action": "passthrough"}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 0

def test_blocked_emits_error():
    """action=blocked→MetaStep(type=error, error_type=blocked)"""
    agent = _make_agent()
    result = {"action": "blocked", "reason": "danger", "tool_name": "delete"}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 1
    assert isinstance(steps[0], MetaStep)
    assert steps[0].type == "error"

def test_rejected_emits_error():
    """action=rejected→MetaStep(type=error, error_type=user_rejected)"""
    agent = _make_agent()
    result = {"action": "rejected", "reason": "user denied", "tool_name": "copy"}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 1
    assert isinstance(steps[0], MetaStep)
    assert steps[0].type == "error"

# === confirmed分支 ===

def test_confirmed_emits_resumed():
    """action=confirmed→MetaStep(type=resumed)（paused已由sandbox_resolve发射）"""
    agent = _make_agent()
    result = {"action": "confirmed", "confirm_id": "c1", "tool_name": "copy"}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 1
    assert isinstance(steps[0], MetaStep)
    assert steps[0].type == "resumed"

# === executing/unknown分支 ===

def test_executing_no_emission():
    """action=executing→无Step（action_handler内部已yield，转换层不重复）"""
    agent = _make_agent()
    result = {"action": "executing", "source": "action_handler"}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 0

def test_unknown_action_emits_error():
    """未知action→MetaStep(type=error, error_type=unknown_action)"""
    agent = _make_agent()
    result = {"action": "weird_unknown"}
    steps = list(emit_from_business_result(agent, 1, result))
    assert len(steps) == 1
    assert isinstance(steps[0], MetaStep)
    assert steps[0].type == "error"
```

#### 9.3.2 test_conflict_detector.py — 工具调用冲突检测测试（~6个case）

```python
# -*- coding: utf-8 -*-
# 测试conflict_detector冲突检测 — 小健-2026-09-04
import pytest
from app.tools.conflict_detector import _has_conflict, _partition_calls

# === _has_conflict ===

def test_no_conflict_different_paths():
    """不同路径无冲突"""
    calls = [
        {"tool_name": "read", "tool_params": {"path": "/a.txt"}},
        {"tool_name": "read", "tool_params": {"path": "/b.txt"}},
    ]
    assert _has_conflict(calls) is False

def test_conflict_same_path():
    """相同路径冲突"""
    calls = [
        {"tool_name": "read", "tool_params": {"path": "/a.txt"}},
        {"tool_name": "write", "tool_params": {"path": "/a.txt"}},
    ]
    assert _has_conflict(calls) is True

def test_no_conflict_single_call():
    """单次调用无冲突"""
    calls = [{"tool_name": "read", "tool_params": {"path": "/a.txt"}}]
    assert _has_conflict(calls) is False

def test_no_conflict_empty():
    """空列表无冲突"""
    assert _has_conflict([]) is False

# === _partition_calls ===

def test_partition_same_path_together():
    """同路径调用分到同一组"""
    calls = [
        {"tool_name": "read", "tool_params": {"path": "/a.txt"}},
        {"tool_name": "write", "tool_params": {"path": "/b.txt"}},
        {"tool_name": "read", "tool_params": {"path": "/a.txt"}},
    ]
    groups = _partition_calls(calls)
    # 应分为2组：/a.txt（2个）和 /b.txt（1个）
    assert len(groups) == 2

def test_partition_window_by_title():
    """窗口操作按window_title分组"""
    calls = [
        {"tool_name": "window_focus", "tool_params": {"window_title": "Chrome"}},
        {"tool_name": "window_focus", "tool_params": {"window_title": "VSCode"}},
    ]
    groups = _partition_calls(calls)
    assert len(groups) == 2
```

#### 9.3.3 test_trust.py — 信任域三合一测试（~8个case）

```python
# -*- coding: utf-8 -*-
# 测试trust.py三合一模块 — 小健-2026-09-04
import pytest
from unittest.mock import MagicMock, AsyncMock, patch
from app.tools.trust import (
    _parse_paths, extract_trust_path, resolve_skip, save_session_trust,
)

# === _parse_paths ===

def test_parse_paths_file_operation():
    """文件操作解析path参数"""
    result = _parse_paths("read", {"path": "/a.txt"})
    assert "/a.txt" in result

def test_parse_paths_alias_mapping():
    """别名映射：source→path"""
    result = _parse_paths("copy", {"source": "/a.txt", "dest": "/b.txt"})
    assert "/a.txt" in result
    assert "/b.txt" in result

def test_parse_paths_window_focus():
    """窗口操作解析window_title"""
    result = _parse_paths("window_focus", {"window_title": "Chrome"})
    assert "window:Chrome" in result

def test_parse_paths_non_file_tool():
    """非文件工具返回空集"""
    result = _parse_paths("search", {"q": "test"})
    assert result == set()

# === extract_trust_path ===

def test_extract_trust_path_returns_file():
    """提取文件路径（非window:）"""
    result = extract_trust_path("read", {"path": "/a.txt"})
    assert result == "/a.txt"

def test_extract_trust_path_skips_window():
    """跳过window:前缀路径"""
    result = extract_trust_path("window_focus", {"window_title": "Chrome"})
    assert result is None

# === resolve_skip ===

@pytest.mark.asyncio
async def test_resolve_skip_no_session_returns_false():
    """无session_id→不跳过"""
    with patch("app.tools.trust.db") as mock_db, \
         patch("app.tools.trust.get_session_id_by_task", return_value=None):
        result = await resolve_skip("task-1", "read", {"path": "/a.txt"})
        assert result is False

# === save_session_trust ===

@pytest.mark.asyncio
async def test_save_session_trust_no_session_raises():
    """无session_id→抛ValueError"""
    with patch("app.tools.trust.db") as mock_db, \
         patch("app.tools.trust.get_session_id_by_task", return_value=None):
        with pytest.raises(ValueError, match="无 session_id"):
            await save_session_trust("task-1", "read", "/a.txt")
```

### 9.4 单元测试

| 测试项 | 命令 | 预期结果 | 来源 |
|--------|------|---------|------|
| 现有测试修改后 | `pytest tests/ -x --tb=short` | 通过（需先完成9.2的P0修改） | 现有 |
| 新增event_emitter测试 | `pytest tests/test_event_emitter.py -v` | 通过 | 新增 |
| 新增conflict_detector测试 | `pytest tests/test_conflict_detector.py -v` | 通过 | 新增 |
| 新增trust测试 | `pytest tests/test_trust.py -v` | 通过 | 新增 |

### 9.5 集成测试

| 测试场景 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 正常回答 | 发送简单问题 | 收到completed FinalStep |
| 错误回答 | 发送触发错误的请求 | 收到failed FinalStep |
| HITL确认 | 发送需要确认的工具调用 | 收到paused MetaStep → 用户确认 → resumed MetaStep |
| 沙箱拦截 | 发送危险工具调用 | 收到blocked MetaStep |
| 重试 | 发送触发重试的请求 | 收到retrying MetaStep → 最终completed |
| 推理内容 | 发送需要推理的问题 | 收到thought ThoughtStep → 最终completed |

### 9.6 E2E测试

| 测试项 | 命令 | 预期结果 |
|--------|------|---------|
| 全链路测试 | `pytest backend/e2etests/` | 通过 |

### 9.7 前端验证

| 验证项 | 验证方法 | 预期结果 |
|--------|---------|---------|
| SSE事件接收 | 浏览器控制台查看SSE事件 | Step事件格式正确 |
| UI渲染 | 查看前端页面 | 各Step正确渲染 |
| HITL弹窗 | 触发需要确认的工具调用 | 弹窗正常显示 |
| 沙箱拦截 | 触发危险工具调用 | 拦截提示正常显示 |

### 9.8 回归测试

| 测试场景 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 已有功能 | 运行所有现有测试 | 不退化 |
| 性能测试 | 观察响应时间 | 无明显下降 |
| 并发测试 | 多会话并发 | 无竞态问题 |

### 9.9 测试修改实施顺序

> 铁律：**先改测试，再改代码**。测试修改后编译通过，再实施代码改造。

```
步骤1: 修改 _emitter_mock.py（补outcome参数）
步骤2: 修改 test_answer_handler_edge_cases.py（async for→await + dict断言）
步骤3: 修改 test_sandbox_gate.py（元组解包→dict访问）
步骤4: 检查 test_action_handler_edge_cases.py（inspect.getsource匹配）
步骤5: 新建 test_event_emitter.py（9个case）
步骤6: 新建 test_conflict_detector.py（6个case）
步骤7: 新建 test_trust.py（8个case）
步骤8: 运行全量测试验证
```

---

## 十、重构错误分析与修正（v2.6新增）

> **签名**: 小健  
> **时间**: 2026-09-04 08:48:47  
> **分析方法**: 日志分析 + 代码对比 + git diff逐Phase审查

### 10.1 错误汇总

本次重构共引入6处错误（新增2处由v2.8全量等价性审计发现），3处已修复，3处待修复。所有错误的根因都是**没有遵守"先复制代码后修改"原则**——改了返回值格式/函数签名，没有同步检查和更新所有调用方。

| # | Phase | 文件 | 错误描述 | 严重程度 | 状态 |
|---|-------|------|---------|---------|------|
| 1 | Phase8 | action_handler.py:420 | `sandbox_resolve`返回dict，调用方还用元组解包 `_ok, _steps = ...` | 🔴 高 | ✅ 已修复 |
| 2 | Phase4 | react_cycle.py:315-320 | 删了`yield event`，action路径**全部**Step（Thought/Action/Observation/工具错误MetaStep/return_direct FinalStep）被消费但不转发，前端看不到工具执行过程 | 🔴 **最高** | ❌ 待修复 |
| 3 | Phase2 | answer_handler.py:162-170 | `reasoning_only_limit`分支返回`"action": "failed"`但没调`set_failed()` | 🔴 高 | ✅ 已修复 |
| 4 | Phase3 | sandbox_gate.py:62-65,114-116 | blocked/rejected路径不发"resumed" MetaStep→前端badge卡在"paused" | 🟡 中 | ✅ 已修复 |
| 5 | — | react_cycle.py:252-265 | `_check_same_tool_loop`用完整JSON签名检测，LLM换参数即绕过→死循环 | — | ✅ 非缺陷（10.6更正） |
| 6 | Phase2 | event_emitter.py unknown分支 | unknown类型FinalStep的`response`/`error_message`字段错位：pre版`response="任务执行失败"`+`error_message=类型信息`，post版把类型信息塞进response | 🟡 中 | ❌ 待修复 |
| 7 | Phase2 | event_emitter.py failed分支 | reasoning-only终止分支的`reasoning=_deduped`字段丢失：pre版FinalStep带`reasoning=`，event_emitter failed分支没读`result["reasoning"]` | 🟡 中 | ❌ 待修复 |

### 10.2 错误1：sandbox_resolve调用方未同步（已修复）

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/handlers/action_handler.py` |
| **行号** | 420 |
| **Commit** | `93e722414`（已修复） |

**错误代码**:
```python
_ok, _steps = await sandbox_resolve(agent, step, call, _cn, _cp, _pre, safety_result, _denied)
# ValueError: not enough values to unpack (expected 2, got 1)
```

**日志证据**:
```
2026-09-04 08:18:36 ERROR - react_cycle.py:867 - 不可恢复异常: not enough values to unpack (expected 2, got 1)
2026-09-04 08:20:04 ERROR - react_cycle.py:867 - 不可恢复异常: not enough values to unpack (expected 2, got 1)
```

**根因**: `sandbox_resolve`从返回`(bool, list)`改成返回`dict`，但调用方`action_handler.py:420`还是用老的元组解包

**修复**: 改为`_sb_result = await sandbox_resolve(...)` + dict访问（commit `93e722414`）

### 10.3 错误2：yield event丢失（待修复）

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/react_cycle.py` |
| **行号** | 315-320 |
| **Commit** | `e2a295f8c` |

**旧代码**:
```python
async for event in handler:
    seen_types.add(event.type)
    ...
    yield event          # ← 转发给调用方
```

**新代码**:
```python
async for event in handle_action(agent, llm_response):
    seen_types.add(event.type)
    ...
    # yield event 被删了    ← 事件丢失
```

**后果**: `handle_action` yield的ActionStep/ObservationStep被消费但不转发，`emit_from_business_result`收到`{"action":"executing"}`直接pass，前端看不到工具执行过程

**修复方案**: 恢复`yield event`，或让`emit_from_business_result`转发action路径事件

### 10.4 错误3：set_failed缺失（待修复）

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/handlers/answer_handler.py` |
| **行号** | 162-170 |
| **Commit** | `92b19ae83` |

**错误代码**:
```python
if agent._consecutive_reasoning_only > REASONING_ONLY_MAX_ROUNDS:
    return {
        "action": "failed",
        "response": "模型反复思考未产出有效结果，任务已终止",
        ...
    }
    # 缺少: set_failed(agent, ...)
```

**对比正确代码**（error/unknown分支都有set_failed）:
```python
# error分支（正确）：
set_failed(agent, f"LLM调用异常: {_error_msg}")
return {"action": "failed", ...}
```

**后果**: `agent.status`保持EXECUTING而不是FAILED，任务追踪器永远不记录失败

**修复方案**: 在return前加`set_failed(agent, f"连续{REASONING_ONLY_MAX_ROUNDS}轮仅返回推理内容")`

### 10.5 错误4：resumed事件缺失（待修复）

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/handlers/sandbox_gate.py` |
| **行号** | 62-65, 114-116 |
| **Commit** | `6a4608373` |

**错误逻辑**:
```python
# blocked路径：
return {"action": "blocked", "reason": ..., "tool_name": ...}
# → caller继续 → 没有resumed事件 → badge卡在paused

# rejected路径：
return {"action": "rejected", "reason": ..., "tool_name": ...}
# → caller继续 → 没有resumed事件 → badge卡在paused
```

**对比正确代码**（confirmed路径有resumed）:
```python
# confirmed路径（正确）：
return {"action": "confirmed", ...}
# → event_emitter L87-95 发resumed MetaStep → badge恢复
```

**后果**: 前端badge永久显示"paused"状态，无倒计时恢复

**修复方案**: 在blocked/rejected路径也发resumed MetaStep，或在event_emitter中补充

### 10.6 死循环归类修正：场景F检测逻辑重构前后一致，非缺陷（2026-09-04 更正）

> **本小节是用户"北京老陈"明确指正的更正**：`_tool_call_signature`/`_check_same_tool_loop`（场景F死循环检测）
> **重构前后逻辑完全一致，是正确代码，不是缺陷，也不是引起重复错误的根源，禁止修改**。
> 此前在10.6/10.7/10.9中将其定性为"原有设计缺陷/检测失效（次要原因）"是**错误归因**，现予更正。
> 死循环的**唯一根因**是 Phase2 重构在 `event_emitter.emit_from_business_result` 中漏调 `set_completed()`（见10.7表）。

**证据：重构前后场景F的归属与流程逐行一致**

| 关键块 | 重构前 (ba78c9f83) | 重构后 (当前) | 一致性 |
|--------|-------------------|--------------|--------|
| `_dispatch_handler` 定义 | line 288 | line 317 | 重构目标（yield→dict） |
| `_process_single_step` 定义 | line 399 | line 395 | 一致 |
| **场景F `_check_same_tool_loop` 调用** | **line 677** | **line 673** | **一致（逐字未改）** |
| **场景E `_dispatch_handler` 调用** | line 703（async for） | line 717（await+emit） | 重构目标 |
| `run_react_cycle` 定义 | line 707 | line 726 | — |

**结论**：
1. `_tool_call_signature` 函数本体（重构前252-265 / 当前246-256）重构前后**逐字相同**（`git diff ba78c9f83 e2a295f8c` 对此函数无任何输出）
2. 场景F检测（`_check_same_tool_loop`/`_warn_same_tool_loop`/`_tool_call_signature`）重构前后都位于 `_process_single_step` 函数内、都位于场景E分发之前，**上下左右流程逻辑一致**
3. 唯一变化是紧跟其后的场景E从 generator 分发（`async for event in _dispatch_handler`）改为 dict+`emit_from_business_result` 转换——这是 Phase4 架构改造的目标本身，不是对场景F的改动
4. 新增的 `_check_same_tool_name_loop`（line 693，同工具名二级防御，阈值15）是**顺理成章的增强（兜底）**，不是对原逻辑的修改，也不是修复"缺陷"

**死循环真正根因（更正）**：
```
Phase2 重构：event_emitter.emit_from_business_result 中 action=="completed" 分支
            漏调 set_completed(agent) → agent.status 永远 EXECUTING
            → 循环退出条件永远不满足 → feedback loop 无法终止 → 死循环
（修复：completed 分支补 set_completed(agent) 调用，详见10.7表第2行）
```

**说明**：LLM 换文件名导致"同签名检测"不触发，这**不是缺陷**，而是该检测设计上的正常行为（同名同参才判死循环，避免误伤正常任务）。新增的同工具名二级防御用于捕获变参循环，属防御增强。

### 10.7 根因分析：未遵守"先复制后修改"原则

| Phase | 改了什么 | 漏了什么 | 后果 |
|-------|---------|---------|------|
| Phase2 | answer_handler yield→return dict | answer正常完成本就不设终态（handler只返回{"action":"completed"}），状态副作用移交给event_emitter | 无（此处行为正确，非缺陷） |
| Phase2 | event_emitter新建completed分支 | **漏了set_completed()调用** | **循环退出条件永远不满足→死循环（唯一根因）** |
| Phase3 | sandbox_gate yield→return dict | 漏了blocked/rejected的resumed事件 | 前端badge卡paused |
| Phase4 | _dispatch_handler async gen→fn | 删了yield event没补转发 | 前端看不到工具执行 |
| Phase8 | sandbox_resolve返回dict | 漏了action_handler调用方同步（已修复） | sandbox调用方ValueError |
| — | _tool_call_signature/_check_same_tool_loop（场景F） | **无——重构前后逐字一致，非缺陷** | **无（更正：非死循环根因）** |

**关键发现**（铁证：prompt日志 `prompt_004618` + 状态变化记录，2026-09-04 核实）：
重构将 answer 的"产出 FinalStep"与"设终态 set_completed"拆到两个模块（`92b19ae83` answer_handler yield→return dict + `f956af34c` event_emitter 统一转换），**交接边界 event_emitter 丢失了 `set_completed()` 副作用**：`action=="completed"` 分支只 emit FinalStep、不调 `set_completed()`，导致 `agent.status` 永远 EXECUTING，`run_react_cycle` 主循环 `if agent.status in (COMPLETED, FAILED, CANCELLED): break` 永不满足。
日志证据：LLM 在第 10-18 轮**正常返回 answer**（控制台 `[Final] step=11/12/13/14/15` 全部正常，response 列出10项任务全部✅），但 `agent.status` 状态变化记录**全程只有 THINKING↔EXECUTING 往返、从未出现 COMPLETED**，循环被迫继续到 max_steps 到顶（86轮）强制 `THINKING→CANCELLED`。
（更正：此前误写"漏了set_failed调用"——answer正常完成路径本就不应 set_failed，非缺陷；真正丢失的是 event_emitter completed 分支的 set_completed。）

**教训**: 每次修改函数返回值/签名时，必须：
1. 搜索所有调用方（grep函数名）
2. **逐一检查副作用**（set_completed/set_failed/set_cancelled等状态变更）
3. 运行测试验证

### 10.8 修复优先级与状态

| 优先级 | 错误 | 修复难度 | 状态 | 修复时间 |
|--------|------|---------|------|---------|
| P0-紧急 | 错误1: sandbox_resolve调用方 | 低 | ✅ 已修复 | 2026-09-04 |
| P1-最高 | 错误2: yield event丢失（action全路径Step丢失） | 高 | ❌ 待修复 | — |
| — | 场景F: _tool_call_signature检测 | — | ✅ 非缺陷（重构前后一致，未改） | — |
| P3-高 | 错误3: set_failed缺失 | 低 | ✅ 已修复 | 2026-09-04 |
| P4-中 | 错误4: resumed事件缺失 | 中 | ✅ 已修复 | 2026-09-04 |
| P5-中 | 错误6: unknown分支字段错位 | 低 | ❌ 待修复 | — |
| P6-中 | 错误7: reasoning字段丢失 | 低 | ❌ 待修复 | — |

### 10.9 死循环根因分析结论（2026-09-04 完成）

**完整事件链还原**（task `0a1614b0`，prompt日志 `prompt_004618+519756fb+20260904_083940.json`）：

| 阶段 | Step范围 | LLM行为 | 根因 |
|------|---------|---------|------|
| 正常测试 | 1-9 | 调用sysinfo/timenow/listdir/network等工具 | 正常 |
| **任务正常完成** | **10-18** | **正常返回answer，产出`[Final] step=11~15`，response列出10项任务全部✅，输出2份报告** | **正常（final已产出，任务已完成）** |
| **重复循环** | **19-85** | **LLM被迫继续调write_text等"回应"已完成任务的确认消息**（thought="用户继续发送确认消息，我已多次回应"） | **status永远EXECUTING→while永不break→循环被迫继续直到max_steps** |
| 强制终止 | 86 | max_steps到顶，`THINKING→CANCELLED`兜底 | 循环耗尽 |

**根因结论**（铁证确认 2026-09-04）：
1. **真正根因**：重构边界丢失 `set_completed()` 副作用。重构把 answer 的"产出 Final"与"设终态"拆到 answer_handler + event_emitter 两模块，但 event_emitter `action=="completed"` 分支只 emit FinalStep、**漏调 set_completed()** ↓ `agent.status` 永远 EXECUTING ↓ `run_react_cycle` 主循环 `if agent.status in (COMPLETED, FAILED, CANCELLED): break` 永不满足 ↓ **final 正常产出后循环依然不退出** → 死循环直到 max_steps
2. **不是"LLM没调answer"**：日志证明 LLM 在第10-18轮**正常调了answer**、final正常产出、任务已完成；是**循环没能退出**（break条件status不满足），才迫使LLM继续"解释"已完成任务 → 长尾巴循环
3. **场景F检测（`_tool_call_signature`/`_check_same_tool_loop`）重构前后逐字一致、非缺陷、非根因**（用户"北京老陈"指正，详见10.6）

**修复方案**（已实施）：
- **修复根因**：`event_emitter.emit_from_business_result` 的 `action=="completed"` 分支补 `set_completed(agent)` 调用（见10.7表第2行），使 final 产出后 `agent.status=COMPLETED`，主循环 break 条件满足、正常退出
- **防御增强**：新增 `_check_same_tool_name_loop` 同工具名二级防御（`_MAX_CONSECUTIVE_SAME_TOOL_NAME=15`），用于捕获LLM本身陷入变参循环的情况，属增强而非修缺陷

### 10.10 全量重构等价性审计结论（v2.8新增）

> **审计人**: 小健
> **审计时间**: 2026-09-04 10:06:42 ~ 10:43:47
> **审计方法**: pre/post源码逐行对照（git show提取重构前版本 + 读工作区版本）+ Python语义推理 + 最小运行时复现（不靠猜）+ 子代理并行核查
> **审计范围**: 全部重构提交（f956af34c → 93e722414 + 工作区未提交改动）涉及的6个边界

#### 10.10.1 审计结论汇总表

| 边界 | 审计结论 | 关键差异项 |
|------|---------|-----------|
| **answer_handler→event_emitter** | ⚠️ 3处差异 | unknown字段错位(#6)、reasoning字段丢失(#7)、retrying文案改变(轻微) |
| **action Step转发（_dispatch_handler→SSE）** | 🔴 **颠覆性丢失** | action全路径Step不转发(#2) |
| **sandbox_gate→event_emitter** | ✅ 等价 | paused直接emit保留，blocked/confirmed/rejected由event_emitter等价还原 |
| **trust.py合并** | ⚠️ 死函数 | resolve_skip/save_session_trust在backend/app无调用方 |
| **conflict_detector下沉** | ⚠️ 需关注 | commit落盘的是简化退化版（丢窗口分支/读写误检），工作区已修正为逐行等价 |
| **sandbox_resolve适配** | ✅ 生产等价 | 3处调用全部dict适配；2处旧测试仍tuple解包会崩 |

#### 10.10.2 错误2（颠覆性）：action全路径Step丢失——详细分析

**这是本次审计发现的最高危问题，比set_completed丢失更严重。**

**证据链（铁证）**：

| 对照项 | pre版（ba78c9f83） | post版（工作区） |
|--------|-------------------|-----------------|
| `_dispatch_handler` 函数类型 | **async generator**（L288 `async def` + L335 `yield event`） | **async function**（L317 `async def` + L719 `await _dispatch_handler(...)`，async generator不能被await） |
| action分支循环体 | `async for event in handler: ...; yield event` | `async for event in handle_action(...): ...`（**无yield event**） |
| 循环后返回 | （generator自身产出Step） | `return {"action":"executing"}` |
| event_emitter处理 | N/A（Step直接yield到SSE） | `action=="executing"` → **pass（不yield）** |

**最小运行时复现**（Python语义铁证）：
```python
async def fake_handle_action(agent, resp):
    yield 'ThoughtStep'; yield 'ActionStep'; yield 'ObservationStep'

async def post_dispatch(agent, resp):
    async for event in fake_handle_action(agent, resp):
        pass  # 不yield（post版行为）
    return {'action':'executing'}

# 结果：post_dispatch消费了handle_action的全部产出，但不转发
# pre版能delivered全部Step，post版全部丢失
```

**丢失的Step清单**（handle_action产生的全部）：

| Step类型 | pre版来源（action_handler.py行号） | 作用 | post版是否转发 |
|---------|------|------|-------------|
| ThoughtStartStep | L950 | LLM推理光标 | ❌ 丢失 |
| ThoughtStep | L951-955 | LLM推理内容 | ❌ 丢失 |
| ActionStep（前置） | L975-979 | 工具执行前通知 | ❌ 丢失 |
| check_safety_and_confirm事件 | L961-964 | 安全检查MetaStep | ❌ 丢失 |
| ActionStep/ObservationStep | L1030-1032 | 工具执行+观察结果 | ❌ 丢失 |
| return_direct FinalStep | L1038-1042 | 提前完成正文 | ❌ 丢失 |
| 错误MetaStep（tool_name空） | L940-942 | 工具名空错误 | ❌ 丢失 |

**修复方案（待定夺）**：
- 方案A：`_dispatch_handler`恢复为async generator，action分支恢复`yield event`，但需要同步修改L719的调用方式（从`await`改回`async for`）
- 方案B：在`_dispatch_handler`的action分支内收集所有event，存入一个列表，通过返回dict的某个字段传递给event_emitter，由event_emitter统一yield
- 方案C：保留`_dispatch_handler`为async function，但action分支内每个event调用`agent._step_emitter.emit(event)`直接推送到SSE（绕过event_emitter）

#### 10.10.3 错误6/7（answer_handler边界）：字段级差异

**错误6（unknown分支字段错位）**：

| 字段 | pre版FinalStep | post版dict | 差异 |
|------|---------------|-----------|------|
| response | `"任务执行失败"`（写死） | `f"LLM返回未知响应类型: {parsed_type}"` | **改变了前端正文** |
| error_type | `"unknown_response"` | `"unknown_response"` | 一致 |
| error_message | `f"LLM返回未知响应类型: {parsed_type}"` | `content`（可能为空） | **语义丢失** |

**错误7（reasoning-only终止分支reasoning字段丢失）**：

| 字段 | pre版FinalStep | post版event_emitter failed分支 | 差异 |
|------|---------------|------------------------------|------|
| response | `"模型反复思考..."` | `result.get("response","任务执行失败")` | 一致 |
| outcome | `"failed"` | `"failed"` | 一致 |
| reasoning | `_deduped`（推理去重后文本） | **未传递** | **🔴 丢失** |

pre版L191-197：`FinalStep(..., reasoning=_deduped, outcome="failed")`——reasoning随FinalStep传递给前端/落库。
post版event_emitter failed分支L46-55：只读`response/error_type/error_message`，**没有读`result.get("reasoning")`**。
结果：reasoning-only终止时前端看不到推理过程。

#### 10.10.4 其余边界审计细节

**sandbox_gate→event_emitter**：
- pre版返回`(bool, steps)`，event_emitter passthrough/blocked/confirmed/rejected四分支等价还原
- paused MetaStep在sandbox_resolve内直接emit（过渡期设计），不在event_emitter中
- 工作区额外补了confirmed/rejected的resumed emit（修复错误4），属增强

**trust.py合并**：
- trust_utils/trust_service/trust_repository在git历史中从未存在这些文件名——"三合一"实际是从action_handler.py和hitl_confirmation.py**下沉复制**4个函数到trust.py
- `_parse_paths`/`extract_trust_path`：调用方已适配（action_handler L552、sandbox_gate L78、conflict_detector L14）
- `resolve_skip`/`save_session_trust`：**在backend/app无任何调用方（死函数）**——需确认是否为"下沉后忘记接入调用方"

**conflict_detector下沉**：
- commit `9eb787418` 落盘的简化版：丢窗口分支+读写误检+返回类型错误+新增"同名即并组"——若被接线会改变行为
- 当前状态：工作区已改回与原版逐行等价，调用方适配正确（action_handler import from conflict_detector）
- 遗留：action_handler中`_READ_TOOLS/_WRITE_OPS/WINDOW_TARGET_TOOLS`与conflict_detector重复定义（DRY违规，不改行为）

**sandbox_resolve适配**：
- 生产3处调用（action_handler L424/492/514）全部dict访问，正确
- `tests/test_d2_secondary_bugs.py:76`、`tests/test_reality_bugs.py:112`仍tuple解包，confirmed路径会ValueError崩
- FUNCTIONS.md登记`Tuple[bool, list]`过期
- event_emitter的sandbox四分支生产不可达（sandbox_resolve内部消费，不向上传播到dispatch/event_emitter）

---

**更新时间**: 2026-09-04 10:43:47
**更新人**: 小健

---

## 十一、重构问题清单（v2.9新增，按问题维度梳理）

> **编写人**: 小健
> **时间**: 2026-09-04 10:45:14
> **来源**: 第十章全量等价性审计发现，按"一个独立问题"为单位重新梳理
> **目的**: 把散落在各边界审计中的发现，归拢成**独立可追踪的问题**，每个问题有明确的根因、影响、修复状态

### 11.1 问题总览

本次重构（commits `f956af34c`→`93e722414` + 工作区未提交改动）共发现**8个独立问题**：

| # | 问题名称 | 严重程度 | 根因Phase | 修复状态 |
|---|---------|---------|----------|---------|
| **P1** | action全路径Step丢失 | 🔴 **最高** | Phase4 | ❌ 待修复 |
| **P2** | set_completed丢失→死循环 | 🔴 高 | Phase2 | ✅ 已修复（event_emitter.py L36） |
| **P3** | unknown分支字段错位 | 🟡 中 | Phase2 | ❌ 待修复 |
| **P4** | reasoning-only终止时reasoning字段丢失 | 🟡 中 | Phase2 | ❌ 待修复 |
| **P5** | vacuum retrying文案改变 | 🟢 低 | Phase2 | ❌ 待修复（轻微） |
| **P6** | sandbox_resolve tuple解包崩溃 | 🔴 高 | Phase8 | ✅ 已修复（action_handler） |
| **P7** | sandbox resumed事件缺失→badge卡paused | 🟡 中 | Phase3 | ✅ 已修复（sandbox_gate） |
| **P8** | 旧测试仍用tuple解包→会崩 | 🟡 中 | — | ❌ 待修复（tests/） |

### 11.2 P1：action全路径Step丢失（最高危，颠覆性）

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/react_cycle.py` L317-385 |
| **根因** | Phase4重构把`_dispatch_handler`从async generator改成async function返回dict，action分支循环体（L340-345）删掉了`yield event`，改return `{"action":"executing"}` |
| **丢失的Step** | ThoughtStartStep/ThoughtStep/ActionStep（前置+后置）/ObservationStep/check_safety_and_confirm事件/工具错误MetaStep/return_direct的FinalStep |
| **影响** | **前端完全看不到工具调用过程**（无推理、无工具名、无执行结果、无错误提示）；return_direct的完成正文也不显示 |
| **证据** | pre版L335有`yield event`，post版L340-345无yield；最小Python运行时复现确认async def内async for消费generator且不yield→产出全部丢弃 |
| **状态** | ❌ 待修复（最高优先级） |

**修复方向（待定夺）**：
- 方案A：恢复`_dispatch_handler`为async generator，action分支恢复`yield event`，同步改L719调用方式（从await改回async for）
- 方案B：action分支内收集所有event到列表，通过返回dict某字段传递给event_emitter
- 方案C：action分支内直接调`agent._step_emitter.emit(event)`推送SSE（绕过event_emitter）

### 11.3 P2：set_completed丢失→死循环（已修复）

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/event_emitter.py` L34-44 |
| **根因** | Phase2重构把answer的"产出Final"与"设终态"拆到answer_handler+event_emitter两模块，event_emitter的`action=="completed"`分支只emit FinalStep、漏调`set_completed(agent)` |
| **影响** | agent.status永远EXECUTING→主循环break条件永不满足→final正常产出后循环不退出→死循环直到max_steps |
| **证据** | prompt日志`prompt_004618`：LLM第10-18轮正常返回answer（`[Final] step=11~15`），但状态变化记录全程THINKING↔EXECUTING往返，从未出现COMPLETED |
| **状态** | ✅ 已修复（event_emitter.py L36补`set_completed(agent)`） |

### 11.4 P3：unknown分支字段错位

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/handlers/answer_handler.py` L134-148（post版） |
| **根因** | answer_handler重构时，unknown分支的dict返回值把pre版FinalStep的response和error_message字段语义互换了 |
| **影响** | 前端显示的response正文从"任务执行失败"变成LLM类型信息；error_message从类型信息变成可能为空的content |
| **证据** | pre版FinalStep：`response="任务执行失败", error_type="unknown_response", error_message=f"LLM返回未知响应类型: {parsed_type}"`；post版dict：`response=f"LLM返回未知响应类型: {parsed_type}", error_type="unknown_response", error_message=content` |
| **状态** | ❌ 待修复（修正dict返回值字段对应关系） |

### 11.5 P4：reasoning-only终止时reasoning字段丢失

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/event_emitter.py` L46-55（failed分支） |
| **根因** | event_emitter failed分支只读`response/error_type/error_message`，没有读`result.get("reasoning")`；但pre版reasoning-only终止分支的FinalStep带`reasoning=_deduped` |
| **影响** | reasoning-only终止时前端看不到推理过程（reasoning字段为空） |
| **证据** | pre版L191-197：`FinalStep(..., reasoning=_deduped, outcome="failed")`；post版event_emitter failed分支：无reasoning读取 |
| **状态** | ❌ 待修复（event_emitter failed分支加`reasoning=result.get("reasoning","")`） |

### 11.6 P5：vacuum retrying文案改变

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/event_emitter.py` L57-64（retrying分支）+ `backend/app/services/agent/handlers/answer_handler.py` L155-159 |
| **根因** | 真空→retrying路径：answer_handler返回`{"action":"retrying","wait_time":1}`但不带content；event_emitter retrying分支写死`content="系统重试中..."`，而pre版是`content="LLM返回空内容，触发重试"` |
| **影响** | 前端显示的重试提示文案改变（从具体原因"LLM返回空内容"变成泛化"系统重试中"），信息量减少 |
| **状态** | ❌ 待修复（轻微，可在answer_handler dict中加content字段或event_emitter写死更精确文案） |

### 11.7 P6：sandbox_resolve tuple解包崩溃（已修复）

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/handlers/action_handler.py` L424/492/514 |
| **根因** | Phase8重构把sandbox_resolve返回值从`(bool, list)`改成dict，但调用方还用`_ok, _steps = await sandbox_resolve(...)`元组解包 |
| **影响** | confirmed路径返回多键dict→`ValueError: not enough values to unpack (expected 2, got N)`→整个SSE流崩溃 |
| **证据** | 日志：`2026-09-04 08:18:36 ERROR - react_cycle.py:867 - 不可恢复异常: not enough values to unpack (expected 2, got 1)` |
| **状态** | ✅ 已修复（commit `93e722414`改为dict访问） |

### 11.8 P7：sandbox resumed事件缺失→badge卡paused（已修复）

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/services/agent/handlers/sandbox_gate.py` |
| **根因** | Phase3重构把sandbox_gate的yield→return dict，但blocked/rejected路径不发"resumed" MetaStep，只有confirmed路径有 |
| **影响** | 前端badge永久显示"paused"状态，无倒计时恢复 |
| **状态** | ✅ 已修复（工作区未提交：sandbox_resolve的confirmed/rejected路径补发resumed MetaStep） |

### 11.9 P8：旧测试仍用tuple解包→会崩

| 项目 | 内容 |
|------|------|
| **文件** | `tests/test_d2_secondary_bugs.py:76`、`tests/test_reality_bugs.py:112` |
| **根因** | 这两个测试仍用`ok, steps = await sandbox_gate.sandbox_resolve(...)`元组解包 |
| **影响** | 测试跑挂：confirmed路径返回多键dict→`ValueError: too many values to unpack (expected 2)` |
| **状态** | ❌ 待修复（按AGENTS.md"严令禁止commit测试代码"，本次重构未并入，属遗留） |

### 11.10 待确认项：trust.py死函数

| 项目 | 内容 |
|------|------|
| **文件** | `backend/app/tools/trust.py` L52 `resolve_skip`、L74 `save_session_trust` |
| **现象** | 这两个函数在`backend/app`全项目无任何调用方（grep确认） |
| **可能原因** | ①下沉后忘记接入调用方（行为丢失）；②原逻辑内联在action_handler/hitl_confirmation中，下沉时只复制了函数但未改调用方导入 |
| **状态** | ⚠️ 待确认（不是已确认的bug，需北京老陈确认设计意图） |

### 11.11 修复优先级排序

| 优先级 | 问题 | 修复难度 | 说明 |
|--------|------|---------|------|
| **P0-最高** | P1: action全路径Step丢失 | 高（架构设计） | 前端完全看不到工具执行过程，颠覆性 |
| **P1-高** | P2: set_completed丢失 | 低 | ✅ 已修复 |
| **P1-高** | P6: sandbox tuple解包 | 低 | ✅ 已修复 |
| **P2-中** | P3: unknown字段错位 | 低 | 修正dict返回值即可 |
| **P2-中** | P4: reasoning字段丢失 | 低 | event_emitter加一行 |
| **P2-中** | P7: sandbox resumed缺失 | 低 | ✅ 已修复 |
| **P3-低** | P5: retrying文案改变 | 低 | 改文案或加字段 |
| **P3-低** | P8: 旧测试崩溃 | 低 | 改测试代码 |
| **待定** | trust死函数 | — | 需确认设计意图 |

### 11.12 与第十章错误编号的对应关系

| 第十章编号 | 第十一章编号 | 说明 |
|-----------|-------------|------|
| 错误1（sandbox调用方） | P6 | 已修复 |
| 错误2（yield event丢失） | P1 | **严重程度升为"最高"**（不仅是ActionStep/ObservationStep，是全路径Step） |
| 错误3（set_failed缺失） | P2的一部分 | 已修复（set_failed已补，set_completed也在event_emitter补了） |
| 错误4（resumed缺失） | P7 | 已修复 |
| 错误5（_tool_call_signature） | — | **非缺陷**（10.6更正），不列入问题清单 |
| 错误6（unknown字段错位） | P3 | 新发现 |
| 错误7（reasoning丢失） | P4 | 新发现 |

---

**更新时间**: 2026-09-04 10:45:14
**更新人**: 小健
