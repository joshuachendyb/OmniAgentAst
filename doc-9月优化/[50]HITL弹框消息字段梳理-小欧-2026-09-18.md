# HITL弹框消息字段梳理

> 版本：v1.27 | 创建时间：2026-09-18 09:27:42 | 更新时间：2026-09-18 11:40:22 | 编写人：小欧（v1.26：按用户手工迁移后的实际结构重排章节号——第 3 章：3.1 问题清单 + 3.2~3.5 过宽四方案（原 3.4~3.7 收回编号：3.2 shell 三级处置含 3.2.1/3.2.2、3.3 信任豁免、3.4 同批合并、3.5 writetext）；第 4 章：4.1 问题分析 → 4.2 结论 → 4.3 改后文案 → 4.4 改动点 → 4.5 验收标准（content 问题分析/结论归位第 4 章）；全文交叉引用 20 余处同步修正）；v1.27：基于代码实核的第 3/4 章实施说明修正（10 点，证据行号见各节）——①3.1 P2/3.2.1 白名单系两处定义（:46 常量 + :138 内联字面量，改一处不生效，原"DRY 改一处两处生效"纠偏）；②3.2 HIGH 引改 44-72；③3.2.2 MEDIUM 改 78-99（74-77 为注释非规则项）；④3.3 补 run_sandbox_gate 三调用点（:113/:153/:170）与签名变化，仅 site③ 传 trusted；⑤3.4 组键改 extract_trust_path（SafetyResult 无 trust_path 字段）；⑥3.2 只读短路 bypass 例外回写 2.1.5；⑦4.4 前端 3 处改 6 处（补 TS 类型链 sseParser:150-159 / useChatCallbacks:101-111+:818-828 / useSSE:419-429 / AuthorizationModal 接口），reason 数据源为 paused 帧 content（后端无 reason 字段）；⑧4.3/4.4 截断复用 truncate_text；⑨4.1 第 1 句措辞与行号精修（弹框确认路径 3 处：:269-272/:250-253/:161-163）；⑩4.1 第 3 句补 rejected 拒绝行说明。章节结构未动）

## 1、现状分析

### 1.1 消息字段（线上 13 个）

后端 HITL 网关（`hitl_gateway.py:95-100`）发出的 paused 帧线上一共 13 个字段。基础 4 个（`MetaStep.to_dict` 固定带）：

- `type`（=`paused`）
- `step`（轮号）
- `timestamp`
- `content`（提示文本）

弹窗专用 9 个（构造时传入）：

- `confirm_id`（`task_id:8位随机`，配对关闭弹窗用）
- `tool_name`
- `params`（已脱敏摘要，长值截断，防弹窗超高）
- `safety_level`
- `severity`（固定 `attention`）
- `trust_path`
- `auto_confirm`
- `confirm_timeout`（前端倒计时）
- `backend_timeout`（后端兜底超时）

### 1.2 前端消费链路（8 个进弹窗）

只透传其中 8 个字段进弹窗：`sseParser.ts:873` 取下述 8 个调 `onAuthorizationRequired` → `useChatCallbacks` 经 `authorization_required` 事件原样广播 → `useAuthorization.ts:66` 转驼峰装配 `AuthorizationRequest`（另带布尔归一与超时兜底清洗）→ `AuthorizationModal` 渲染。链路一线图：

```
paused 帧(13 字段) → sseParser 取 8 字段 → authorization_required 事件 → useAuthorization 转驼峰 → AuthorizationModal 渲染
                                                                          ↘ resumed 帧配对 confirm_id → 关框（与 8 字段无关）
```

- `confirm_id` → `confirmId`：发回后端确认兼新旧弹窗配对
- `tool_name` → `toolName`："工具名称"行展示（弹窗标题为"安全确认请求"/bypass"将自动确认"，非工具名）
- `params` → `params`：按 `key=value` 每行一条展示（09-16 定案禁 JSON 式）
- `safety_level` → `safetyLevel`：前端定义五档徽章（只读/安全/破坏性/沙箱危险/系统危险）；但线上出现过的值只有 destructive 一种，5 档中 4 档永不到达（见 1.3 末 level 结论）
- `trust_path` → `trustPath`：仅用于信任勾选框 tooltip（`{toolName} › {trustPath}，含子目录/子键`），无独立路径行；缺失时 tooltip 不展示路径（"未指定路径，仅本次"已随 09-16 改版移除，仅存历史注释）
- `auto_confirm` → `autoConfirm`：为 true 走 bypass 自动确认
- `confirm_timeout` → `confirmTimeout`：倒计时秒数，bypass 显示"将在 Ns 后自动确认（后端 Ns 兜底）"，hitl 显示"未响应将在 Ns 后自动拒绝"
- `backend_timeout` → `backendTimeout`：括号内"后端 Ns 兜底"

没进弹窗的 5 个字段去向（一行一个）：

- `type` → 进执行步骤列表
- `step` → 进执行步骤列表
- `timestamp` → 进执行步骤列表
- `content` → 进任务信息条状态行
- `severity` → paused 链路无任何消费（sseParser 未读取、未转发，弹窗无该字段）

关闭弹窗靠 `resumed` 帧配对 `confirm_id`（`sseParser.ts:889` 派发事件，`useAuthorization.ts:96` 比对后关闭），与 8 字段无关。

### 1.3 弹框触发点（2 处，7 项计数 + 4 项排除）

逻辑流：工具调用 → ① safety_gate 安检（`safety_gate.py:89-104`）：blocked 直接拒绝（rejected，不弹框）；`requires_confirmation` 进网关；通过则 → ② sandbox_gate 沙箱预检+裁决（`sandbox_gate.py:58-129`，预检 58-71、裁决含网关 74-129）：通过或 bypass 免验直接放行（不弹框）；拦截直接拒绝（不弹框）；`needs_ruling` 进网关。两处统一经 `hitl_confirm` 唯一网关暂停发框，网关内 publish paused → 等裁决 → publish resumed（confirmed 必发，bypass+expired 亦发），前端凭 `confirm_id` 配对关框。单 paused 不变量：主路已确认则沙箱跳过二次弹窗（`sandbox_gate.py:87`）。流程示意图：

```
工具调用（每轮 action）
  │
  ├─① safety_gate 工具安检
  │    ├─ blocked → rejected（拒绝，不弹框）
  │    ├─ 通过 → ②
  │    └─ requires_confirmation → [网关]
  │
  ├─② sandbox_gate 沙箱预检
  │    ├─ 通过/放行 → 执行工具（不弹框）
  │    ├─ 拦截 → rejected（不弹框）
  │    └─ needs_ruling → [网关]
  │
  └─[网关] hitl_confirm（唯一暂停源头，唯一 paused 发射）
       ├─ publish paused（13 字段）→ 前端凭 confirm_id 弹框
       └─ 等用户裁决
            ├─ confirmed → publish resumed（配对关框；bypass+expired 亦发）
            └─ 拒绝/超时 → 不发 resumed（rejected 事件留痕）
```

触发点 1：安全闸工具安检（`safety_gate.py:99`）。`tool_name/params` 为实际调用（网关内脱敏加摘要），`safety_level` 取自安检结果（动态），`mode` 按安检 `auto_confirm` 取 `bypass`（前端自动确认倒计时）或 `hitl`（真等待）。

触发点 2：沙箱预检裁决（`sandbox_gate.py:114`）。`mode` 固定 `hitl`，`tool_name/params` 为实际调用，`safety_level` 固定 `destructive`，`auto_confirm` 固定 False。

7 项计数核查（真实代码行，注释已剔除）：①`await hitl_confirm` 全仓 2 处（上两处）；②`await create_confirmation` 与 ③`await wait_for_confirmation_result` 全仓各 1 处（均在网关内）；④`ConfirmSpec(` 构造全仓 2 处（上两处）；⑤`MetaStep(type=paused)` 发射全仓 1 处（网关）；⑥前端 `onAuthorizationRequired` 调用全仓 1 处（`sseParser.ts:873`，且有 `confirm_id` 守卫）；⑦`setAuthorizationPending(新请求)` 全仓 1 处（`useAuthorization.ts:88`，另两处为关框置空）。

排除项（逐一核实非触发点）：①`task_runtime.py:197` 的 paused 是用户手动暂停任务（无 `confirm_id`，被前端守卫过滤，不弹框）；②`react_step.py:60` 的 `task_dir_authorization` 已于 2026-08-10 撤销，仅注释残留；③`authorization_required` 独立事件类型仅见于跳过名单与注释，全仓零发射；④`handle_action.py` 的 S1/bypass 注释均指向经 safety/sandbox 入网关，本文件无直接发射。

level 结论（全仓 25 处赋值加全仓发射点逐条核实）：后端内部产出三档（safe 放行 / destructive 确认 / dangerous 拦截），但序列化到前端的值只有 destructive 一种——paused 帧专属；两处 rejected 发射（安全闸拒绝、沙箱拦截）均不带 `safety_level` 字段；safe 无对应事件。因此前端 5 档徽章实际可达仅"破坏性" 1 档，"只读/安全/沙箱危险/系统危险" 4 档永不到达（有兜底不崩，但属瞎对应）。前版"两档死档""红色只在拦截行"表述有误，特此纠正：拦截行无 level 字段，红色徽章同样到不了前端。

### 1.4 提示信息内容（3 种 content，加参数摘要规则）

- 安全闸 bypass 分支（`safety_gate.py:101`）：`安全策略自动确认工具执行: {工具名}`。
- 安全闸 hitl 分支（`safety_gate.py:102`）：`是否允许执行工具: {工具名}`。
- 沙箱裁决（`sandbox_gate.py:116`）：`沙箱未能完成有效预检,需用户裁决是否直接执行: {工具名}`，`safety_level` 恒为 `destructive`。

以上 3 种 `content` 只进任务信息条状态行，均不进弹窗（见 2.2）。弹窗参数区另有摘要规则（`hitl_gateway.py:42-55`，[43]11.6-T4）：主键 path 优先展示，长值尾部截断 140 字符，截断结果压成单行，脱敏统一收口，保证每参数恒为一行。

## 2、发现的问题及解决方案

### 2.1 确认触发过宽（弹窗多的根因）

**问题是什么：** 确认触发与"危险"脱钩，改挂钩"工具名/兜底"——shell 全弹、沙箱验不出就弹、同批逐个弹。三个倾向即 2.1.3 过宽 3 条，下文按顺序展开：2.1.1 追踪链（看清确认机制）→ 2.1.2 默认清单（影响范围）→ 2.1.3 辨过宽 3 条 → 2.1.4 划合理边界（防误伤）→ 2.1.5 收紧策略。注：bypass 照弹是设计本意（自动确认但提示照出，弹框数不减），不属过宽，前版误判，纠正。

#### 2.1.1 追踪链（以 shell ls 为例）

注册层 `needs_confirmation=True` 写死（`fundamental_register.py:127`，与命令内容无关）→ 安检层 `requires_confirmation=True`（`tool_safety_checker.py:196-201`，信任与开关只能旁路改不了 flag 本身）→ 网关弹框#1（`safety_gate.py:99`，真等待）→ 沙箱预检只读白名单放行（`executor.py:241`，排在弹框之后）。注意：白名单本可免掉的第二次弹框（沙箱 needs_ruling 那一框）已被单 paused 不变量 suppress（主路确认后 `main_confirmed=True` 直放），故当前实现中白名单对减弹零贡献——只读命令的框一次也省不掉。

#### 2.1.2 工具默认需确认清单（字面 flag 共 7 个工具）

- file 类仅 delete（`CONFIRMATION_MAP={"delete": True}`，其余默认 False）；
- fundamental 类仅 shell（恒 True；附带 `action_confirmation={"write": True}` 仅 params 含 action 字段时生效，shell 命令无该字段，实际恒走 flag）；
- system 类 create_task/delete_task、win_registry 类 registry_write/registry_delete（各 `CONFIRM_TOOLS`）、dataanalysis 类 execute_sql；
- 其余分类注册行无 flag，默认 False。动态规则（路径/白名单外授权）在安检层 `_check_known_risks` 实现（白名单外/受保护区，见 1.3 触发链与 2.1.3 过宽 2），不在此重复。

#### 2.1.3 过宽 3 条（现象加证据加判定）

三条共性：确认被问在不需要问的地方——只读命令问（过宽 1）、已信任还问（过宽 2）、同批逐个问（过宽 3）。逐条：

过宽 1：shell 一刀切（工具级 flag，不看命令内容）。现象：`ls/pwd/echo` 等纯查询命令照弹。证据：上链（flag→安检→网关）全程不读命令内容（见 2.1.1）；只读白名单在沙箱内（`executor.py:241`），排在弹框（`safety_gate.py:99`）之后。判定：粒度过粗——沙箱本有能力区分只读与越界（白名单、F-B 意图扫描已建好），应把"只读放行"提到确认链路入口，先放行只读、再谈弹框（见 2.1.5 三级处置与 3.2）。缓解现状：信任可解（shell 无路径参数，信任提取走工具级通配，勾一次管全会话）；bypass 半解（照弹自动过）。

过宽 2：信任被沙箱击穿。信任豁免（勾"信任本次会话"或会话已信任）后，受信 destructive 工具跑到沙箱又弹一次框，前面的信任承诺落空。

分类（哪类"前面的确认"之后沙箱不该再弹，现状三开二）：①手动确认主路：已透传 `main_confirmed=True`，沙箱 `needs_ruling` 直放不再弹（`sandbox_gate.py:87`，单paused）✓；②bypass 自动确认：沙箱 `needs_ruling` 被 `auto_confirm` 直放不再弹（`sandbox_gate.py:90-94`）✓；③信任豁免：豁免只跳过安检弹框，`sandbox_required=True` 原样保留（`tool_safety_checker.py:244/263`），跑到沙箱 `needs_ruling` 又弹 ✗——错就错在这条。

错在哪：豁免结果 `requires_confirmation=False` 却带 `sandbox_required=True`；safety_gate 走 site③（`:170`）调 run_sandbox_gate 未传 `main_confirmed`（缺省 False），豁免又不置 `auto_confirm`，沙箱三个免弹闸（passed / main_confirmed / auto_confirm）全落空，`needs_ruling` 必弹。重灾区：受信 writetext 白名单外每次必弹（`executor.py:291-296` 沙箱不支持该操作，一律转裁决）；受信 shell 在超时/环境失败/越界意图时照弹；受信 flag 高危工具（create_task/delete_task/execute_sql/registry 等）经 skip 豁免同样保留 `sandbox_required`（`tool_safety_checker.py:194`），到沙箱走同一条 unsupported 二次弹。

正确应如何：信任应与手动/自动确认等价——沙箱需识别"该工具+该路径已被用户放行"，区分执行风险与能力缺口：验出真危险（risky）仍弹；验不了（unsupported，能力缺口）配 trusted 直接放行。即 3.3 的 ruling_kind 方案（site③ 透传 `trusted=_skip`），落地后受信 writetext 不再弹，未受信照弹。

过宽 3：同批多调用逐个弹。现象：一轮 3 个同类调用弹 3 次。证据：`safety_gate` 在循环内逐 call `await hitl_confirm`（`safety_gate.py:67/99`），主路无批合并（沙箱侧有单 paused 去重，主路没有）。判定：体验问题，安全语义不变——按批合并（见 3.4）不改变单次裁决的安全水位。

#### 2.1.4 合理的部分（8 条，不过宽）

- delete R3 放行 / R4R5 确认 / R6 硬拦（`delete_safety.py:82-92`），四档分明；
- create_task/delete_task、registry_write/delete、execute_sql 高危 flag，真高危不可逆；
- shell MEDIUM 内容规则（`execute_shell_command_safety.py:216-224`，MEDIUM 特征确认后放行，:1106-1107 仅日志；虽被 flag 全弹盖住而冗余）；
- 白名单外/受保护区首触确认，三重出路：信任勾选（会话持久，前缀递归）、临时授权（任务内有效）、`allowed_dirs` 配永久白名单；
- 系统禁区硬拦（blocked 直接拒，不弹框）；
- M2 沙箱置位精确（仅 destructive 置 `sandbox_required`，safe 零开销直通）；
- 单 paused 不变量（主路已确认跳过沙箱二次裁决）；
- 信任前缀递归加工具级通配（shell 勾一次管全会话，方向正确；但沙箱侧缺豁免消费，即过宽 2，需 3.3 补齐）。

#### 2.1.5 收紧方向（4 条，待立项）

**shell 三级处置总则（allowlist 放行 / blocklist 直拦 / 其余弹框，本章策略、第三章落码）：**

- allowlist 白名单：十类只读前缀直接放行，不弹框。现有五类（`get-`、`ls`、`cat`、`type`、`git status`），新增五类（`echo`、`pwd`、`dir`、`whoami`、`hostname`，逐条过安全评审）；带 `|;&>>` 拼接符一律不进白名单。落点：确认链路入口——`safety_gate.py:89`（`requires_confirmation` 分支）前置只读判定，命中即跳过整个确认链路、直落沙箱白名单放行（代码在 3.2）。
- blocklist 黑名单：HIGH 危险命令（格式化磁盘/关机/rm -rf 根/cipher/dd 直写等）直接硬拦，连确认机会都不给（已存在：工具执行层 `execute_shell_command.py:1098` 调 `check_shell_command_risk`，规则表 `execute_shell_command_safety.py:44-72`；位置说明在 3.2.2）。
- 其余：非白非黑的 shell（非常规只读、写操作、中风险命中）照旧弹框或拦截，安全水位不降。

①shell 只读命令前移免弹（即上述白名单扩展，详见 3.2.1；越界写意图仍弹）；②信任豁免透传沙箱（受信加能力缺口类 ruling 自动过，执行风险类照弹，详见第三章 3.3）；③同批同工具调用合并一次确认；④writetext 白名单外 trusted（并入 3.3 ruling_kind 机制，无独立实施，见第三章 3.5）。bypass 不在收紧范围（自动确认但提示照出是设计本意，非只读命令弹框数不动；只读命令经 3.2 前置短路 bypass 亦免提示——只读无风险、提示无信息量，属唯一例外，见 3.2 影响说明）。

**建议的解决方法是什么：** 按 2.1.5 四向立项实施，落码见第三章 3.2~3.5，本章不动代码。

### 2.2 content 到不了弹窗（确认理由缺失）

**问题是什么：** 后端 `spec.content` 可携带重要原因文本（实证：`sandbox_gate.py:116` 的"沙箱未能完成有效预检，需用户裁决是否直接执行"，用户裁决最需要的正是这句话），但它只进任务信息条状态行，弹窗的 `AuthorizationRequest` 无该字段，用户点开弹窗看不到事由，只能看到工具名和参数。

**建议的解决方法是什么：** `AuthorizationRequest` 加可选字段 `reason`（取 paused 帧 `content`），弹窗在标题下方加一行原因文本展示；`content` 为空时不渲染该行，老 frames 兼容。后端零改动，只动前端两处（`useAuthorization.ts` 装配加一字段，`AuthorizationModal` 加一行展示）。该改动待批准后实施。（注：第四章推翻了本节的"后端零改动"前提——content 文案要改构造行，后端动 2 行、前端动 6 处，以 4.2 注与 4.4 为准。）

### 2.3 severity 发而不用（冗余传输）

**问题是什么：** 后端每帧固定发 `severity=attention`，但 paused 链路自 sseParser 起即丢弃，弹窗用不到，执行步骤也不映射（核实：前端 `severity` 的消费仅存在于错误配置表、截断帧等其他消息类型）。无害，但属冗余传输。

**建议的解决方法是什么：** 保持现状，不删。理由：删减需先全仓确认 paused 无其他隐性消费者，且该字段为未来"按级别着色弹窗"预留了扩展位；当前成本仅为每帧几个字节。若日后做字段瘦身，再与 `content` 进弹窗一并处理。

### 2.4 safety_level 档位错位（前端 5 档瞎对应）

**问题是什么：** 后端内部虽产出三档，但送达前端的值只有 destructive 一种（paused 专属；两处 rejected 发射均不带该字段；safe 无对应事件）；前端 `SAFETY_LEVEL_CONFIG` 却备 5 档，其中 4 档（只读/安全/沙箱危险/系统危险）永不到达。前版称"两档死档"不准确，实际是四档，纠正。新人读前端会误以为后端有 5 种确认级别，属揣测性映射。有兜底回退，不崩，但误导大于实用。

**建议的解决方法是什么：** 删前端四个死档，只留 `destructive`（未知 level 走既有兜底回退显示原文，零风险）。该改动待批准后实施（与第四章无关，独立可先行）。

### 2.5 trust_path 展示浪费（操作对象无独立行）

**问题是什么：** `trust_path` 是操作目标主键（裁决最关键的"动哪个东西"），后端强依赖（信任匹配比对、临时授权范围），但前端仅用于信任勾选框 tooltip（hover 才可见），无独立标注行；虽经参数摘要规则排首行可达，但混在参数堆里需用户自己捞。信息可达但不醒目，裁决效率打折，中度浪费。

**建议的解决方法是什么：** 弹窗加"操作对象"行，显示 `trustPath ?? params 中的 path`；缺失时隐藏（与 reason 行同逻辑）；与第四章 reason 行同属 modal 加行，可合并实施。该改动待批准后实施。

## 3、确认过宽收紧实施方案（2.1 落码）

### 3.1 问题代码与问题点清单（对照 2.1 分析）

本章语义：3.1 把 2.1 的分析落成"要改的问题代码与问题点"清单；3.2~3.5 即按清单的落码实施方案，与本节一一对应；content 传输问题在第 4 章，不在此列。

**过宽 1（shell 一刀切）牵涉 3 个问题点：**

- 问题点 P1（改）：只读判定位置错位——只读白名单判定在沙箱内（`executor.py:241`），排在安检弹框（`safety_gate.py:99`）之后，形成"先弹框后放行"。问题代码：`safety_gate.py:89`（`requires_confirmation` 分支入口未前置只读判定）。对应方案：3.2（分支入口前置 `_is_shell_tool and _is_readonly_whitelisted` 短路）。
- 问题点 P2（改）：免弹范围缺五类——白名单仅五前缀（`executor.py:46` `_READONLY_PREFIXES` 常量 + `:135-140` `_is_readonly_whitelisted` 判定函数），`echo/pwd/dir/whoami/hostname` 等无副作用命令只能进 flag 弹框。问题代码：executor.py 白名单定义两处（:46 常量 + :137-138 判定函数内联字面量，当前 :138 不引用 :46 常量，见 3.2.1 双处同步说明）。对应方案：3.2.1（新增五项两处同步，或顺手重构 :138 改引用常量一次到位）。
- 问题点 P3（不改，保留兜底）：`fundamental_register.py:127` 写死 `needs_confirmation=True`，且 shell 命令无 action 字段分流，恒走 flag。处理：flag 保持——未受信 shell 该弹还弹是合理兜底（2.1.2 清单即现状事实），问题在 P1 的判定位置，不在 flag 本身。

**过宽 2（信任被沙箱击穿）牵涉 3 个问题点：**

- 问题点 P4（改）：豁免与确认不等价——豁免结果 `requires_confirmation=False` 但 `sandbox_required=True` 原样保留（`tool_safety_checker.py:194/244/263`）；safety_gate site③（`:170`）不传 `main_confirmed`（缺省 False）、豁免不置 `auto_confirm`，三免弹闸全落空。问题代码：`safety_gate.py:170`。对应方案：3.3 第 ③ 步（site③ 加 `trusted=_skip` 透传裁决层）。
- 问题点 P5（改）：沙箱裁决不识别"已被用户放行"——`sandbox_resolve` 判据只有 `main_confirmed or auto_confirm`（`sandbox_gate.py:74-129`，bypass 直放 :90-94），信任无消费通道。问题代码：`sandbox_gate.py:74-129`。对应方案：3.3 第 ③ 步（判据改 `main_confirmed or (trusted and pre.ruling_kind=="unsupported")`）。
- 问题点 P6（改）：能力缺口与风险不区分——文件操作"不支持操作类型"分支（`executor.py:291-296`）一律 `needs_ruling=True`，验不了就当追问，受信 writetext/flag 工具必二次弹。问题代码：`executor.py:291-296` 与 `PreCheckResult`。对应方案：3.3 第 ① ② 步（加 `ruling_kind` 字段，该分支置 `unsupported`，其余恒 `risky`）。

**过宽 3（同批逐个弹）牵涉 1 个问题点：**

- 问题点 P7（改）：主路无批合并——safety_gate 循环内逐 call `await hitl_confirm`（`safety_gate.py:67/99`），一轮 N 个同类调用弹 N 次（沙箱侧有单 paused 去重，主路没有）。问题代码：`safety_gate.py:89-104`（确认分支内无分组）。对应方案：3.4（组键 tool+auth/trust_path，组内首 call 确认、其余复用 verdict 与 confirm_id）。

**不改的现状事实（逐条防误改）：** 2.1.2 的 7 个 flag 工具（见 P3）；2.1.4 全部 8 条合理部分（delete 四档、高危 flag、禁区硬拦、白名单外首触三出路、M2 置位、单 paused、MEDIUM 规则、信任递归）；bypass 照弹设计本意；`tool_safety_checker.py` 豁免保留 `sandbox_required` 的语义不改，只在裁决层消费 trusted 透传。

**汇总：** 共 7 个问题点（过宽 1 牵 3、过宽 2 牵 3、过宽 3 牵 1），其中改 6 处、不改 1 处（P3）。改代码落在 3 个文件：`safety_gate.py`（:89 前置只读判定、:170 透传 trusted、:89-104 批分组）、`executor.py`（:46 常量 + :138 内联字面量双处新增白名单五项、PreCheckResult 加 ruling_kind 与 :291 分支置 unsupported）、`sandbox_gate.py`（:74-129 裁决判据、:136-137 签名加 trusted 透传），与本章 3.2/3.2.1/3.3/3.4 一一对应；content 传输问题在第 4 章 4.1 起，与本节无重叠。


### 3.2 shell 三级处置（allowlist 放行 / blocklist 直拦 / 其余弹框）

现状归位：三级中两级已存在——blocklist 直拦层在工具执行层：shell 工具体内 `execute_shell_command.py:1098` 调 `check_shell_command_risk`，HIGH 规则全集（`execute_shell_command_safety.py:44-72`，PS 44-53/CMD 55-58/PS+CMD 60-66/bash 68-72，含格式化/关机/rm -rf 根等）命中直接 blocked 不弹框（安检层未注册 check_fn，此机制不在安检层）；弹框层即 flag 加路径规则；缺的是 allowlist 放行层（只读白名单只在沙箱内，排在弹框之后）。本节只补缺失的一层，不动另两层。

**改法：** `safety_gate.py` requires_confirmation 分支入口（89 行）前置只读判定，命中则整块不进网关，函数内延迟导入（与 sandbox_gate 同款防环写法）：

```python
from app.safety.sandbox.executor import _is_readonly_whitelisted, _is_shell_tool  # 延迟import防环
...
_readonly = _is_shell_tool(_cn) and _is_readonly_whitelisted(str(_cp.get("command") or ""))
if not _readonly and safety_result.requires_confirmation:
    ...既有 requires_confirmation 块原样（网关 await 与 continue 一行不动）...
# 只读短路: 不进网关, 落到下方 site③(170 行 run_sandbox_gate)跑沙箱
```

命中短路的调用不进网关，直接落 site③（170 行 `run_sandbox_gate`）：`sandbox_required=True` 保持（shell 为 destructive 级），沙箱预检仍跑——`_pre_execute_shell` 内 F-B 扫描放行后，只读白名单 `passed=True`（`executor.py:241-243`"只读白名单直通放行真机"），零弹框零新增分支，命令真机直行。

#### 3.2.1 allowlist 名单（现有五前缀加新增五项）

现有（`executor.py:46` `_READONLY_PREFIXES` 常量 + `:135-140` `_is_readonly_whitelisted` 判定函数，一字不改）：`get-`、`ls`、`cat`、`type`、`git status`，且禁 `|;&>>` 五操作符（:140）。新增（逐条过安全评审）：`echo`（纯标准输出；带重定向因操作符禁令进不了短路）、`pwd`、`dir`、`whoami`、`hostname`（纯查询无副作用；拼接符照禁）。新增落点须两处同步：`_READONLY_PREFIXES` 常量（:46）与 `_is_readonly_whitelisted` 内联字面量（:137-138 startswith 元组，当前不引用 :46 常量）——只改一处判定不生效，原"改一处两处生效 DRY"不成立；最简一步到位是顺手把 :138 改引用 `_READONLY_PREFIXES`，消除双源后新增只改常量一处。`git log/diff` 等暂不纳入（pager 与子进程风险，另议）。

#### 3.2.2 blocklist 直拦层（已存在，不新增，只归位说明）

HIGH 模式（格式化磁盘、关机、rm -rf 根、cipher、dd 直写、chmod 777 根等）在工具执行层 `execute_shell_command.py:1098` 调 `check_shell_command_risk` 命中 `blocked=True` 硬拦，连确认机会都不给（规则表 `execute_shell_command_safety.py:44-99`：HIGH 44-72、MEDIUM 78-99（PS 78-82/CMD 84-93/PS+CMD 95-96/bash 98-99；:74-77 为 MEDIUM 块注释非规则项）；MEDIUM 在工具层仅日志放行不拦截，见 :1106-1107；安检层未注册 check_fn，该机制不在安检层）。F-B 写意图扫描不前移：它判的是沙箱语境越界，真机语境下正常项目写入也会命中，前移会误杀，故保留沙箱内加弹框（needs_ruling），维持现状。

**影响：** 十类前缀只读 shell 免弹；其余 shell（含非常规只读、写操作、中风险命中）照旧弹框或拦截，安全水位不降。只读短路对 bypass 同样生效——只读命令 bypass 下连"自动确认提示"也免（只读无风险、提示无信息量，弹框数不白白占一次）；2.1.5"bypass 弹框数不动"相应收窄为"非只读命令 bypass 照弹"。

### 3.3 信任豁免透传沙箱（ruling_kind 区分能力缺口与执行风险）

**改法（3 步）：** ①`executor.py` PreCheckResult 加字段 `ruling_kind: str = "risky"`（dataclass 默认值，既有构造全走 kwargs，后向兼容）；②文件操作"不支持操作类型"分支（`executor.py:291`，:291-296 分支体）改 `ruling_kind="unsupported"`（纯能力缺口，非风险发现；其余 ruling 分支不动，恒 risky）；③`sandbox_gate.py` run_sandbox_gate（现行签名 :136-137，8 参含 main_confirmed=False）加 `trusted: bool = False` 形参变 9 参，透传 sandbox_resolve（现行签名 :74-75，9 参，加后 10 参），ruling 判据改 `main_confirmed or (trusted and pre.ruling_kind == "unsupported")`（插在 :87 主路闸与 :90 bypass 闸之间，bypass 已由 auto_confirm 覆盖），命中即记日志直接放行，否则走既有网关弹框（:106）；调用点对齐：safety_gate.py `:113`（bypass，传 `_bypass_confirmed`）与 `:153`（用户确认，传 True）已分别由 auto_confirm / main_confirmed 闸覆盖，无需 trusted，仅 site③ `:170`（兜底，缺省 False）传 `trusted=_skip`（`_skip` 本轮 :72 已算，safe 直通时为 False 语义不变）。

**安全论证：** 自动过仅发生在"用户已信任该工具加路径"且"沙箱失败原因是能力缺口（验不了）而非风险发现"双条件下；超时/环境/OSError/越界意图/超限等 risky ruling 照弹。信任语义不扩大（不新增信任范围），只是把"验不了"从"问用户"降为"信用户"。

**覆盖：** 本节落地后 2.1.5 之④（writetext 受信裁决）即消失——受信 writetext 白名单外走 unsupported 加 trusted 自动过，未受信照弹（主路 1 次）。

### 3.4 同批同工具调用合并一次确认

**改法：** `safety_gate.py` requires_confirmation 分支内、ConfirmSpec 组装前加分组：组键为 `(tool_name, auth_or_trust_path)`（取 `safety_result.auth_path` or `extract_trust_path(_cn, _cp)`——SafetyResult 无 trust_path 字段（字段：blocked/requires_confirmation/message/safety_level/auto_confirm/auth_path/sandbox_required），信任路径由 trust.py:69 权威函数计算（hitl_gateway.py:91 同款，参数别名归一），两皆空则 tool 单键）；组内首个 call 走既有 hitl_confirm（content 附"另有 N-1 个同类调用同批"计数行），同组其余 call 复用同一 verdict 与同一 confirm_id；`grant_temp_auth` 按组内去重后的 auth_path 逐个执行；用户拒绝则整组 denied。不同 path 不合并（授权范围不同）；bypass 组同样合并（倒计时一次）；单 call 组行为零变化。

**影响：** 一轮 N 个同工具同路径调用 N 弹→1 弹；拒绝语义由逐个变整组（弹窗计数行明示，不藏逻辑）。

### 3.5 writetext 受信裁决（并入 3.3，不独立实施）

原 2.1.5 之④。3.3 落地后：受信 writetext 白名单外 → sandbox unsupported 加 trusted → 自动过；未受信 → 照弹（主路 1 次）。本节无独立代码改动，验收并入 3.3。保留编号占位，防 2.1.5 之④悬空。

## 4、确认理由content -字段完整解决方案:后端+前端联动

### 4.1 问题分析（3 句改前文案逐句剖析）

第 1 句`是否允许执行工具: {工具名}`的问题：只问允不允许，没说为啥要问。实核：弹框确认路径上能产原因的 3 处（白名单外 `tool_safety_checker.py:269-272`、受保护区域写 :250-253、开关绕过 :161-163），`safety_gate.py:101-102` 却一律弃用，换成空话；而 flag 驱动（shell/delete R4R5/execute_sql/registry/create_task 等）走通用路径 :199-201，`message=""` 本就无原因（delete R4/R5 的"删除需确认: {path}"只被取确认布尔，不携带进结果）。另有受保护区删除 :238、豁免 :245/264 带文案但非弹框确认路径（blocked 走 rejected 行 / 豁免 requires_confirmation=False 不进弹框），不入此列。用户只看到"让不让"，看不到"为啥问"——原因并非处处都有，已有的也被构造行弃用了。

第 2 句`安全策略自动确认工具执行: {工具名}`的问题：bypass 用户不决策，缺的不是理由，是人话。"安全策略"四字空洞，原话"提示照出"生僻，用户读完不知道发生了什么。小问题，换纯告知句即好。

第 3 句`沙箱未能完成有效预检，需用户裁决是否直接执行: {工具名}`的问题：起因说了，但"发现了啥"没说。`pre.blocked_reason` 只喂了 LLM 自纠、拒绝名单与 rejected 拒绝行（09-17 改动实证；`sandbox_gate.py:103` 截断80进拒绝行 content），弹框内看不到——用户裁决那一刻缺的正是它。半句，补上原因即完整。

共性问题（三句共有）：纵使上面都写好，也到不了用户眼前——`AuthorizationRequest` 无 `content` 字段（第二章 2.2），弹窗里根本没有理由行。这是传输问题，与文案问题叠加。

### 4.2 结论（两步缺一不可）

核心发现：理由部分场景有（白名单外/受保护区域/开关绕过安检有原话，沙箱有 `blocked_reason`），但两道关都没送到用户眼前——安全闸用空话盖掉了已有的原话，flag 驱动场景本就无原因，弹窗又根本不显示 `content`。因此分两步：第一步重写 3 句 `content` 文案（有原因带原因、无原因走 4.3 兜底通用问句），第二步弹窗加"确认理由"行显示 content（content 进弹窗）。

注：本章方案更新了第二章 2.2 的"后端零改动"前提——content 文案本身要改，后端动 2 行构造，前端动 6 处（类型链 4 处 + 装配 1 处 + 弹窗展示 1 处，详 4.4）。

### 4.3 content 三句改后文案（改前改后对照）

第 1 句（安全闸 hitl，`safety_gate.py:102`）：

- 改前：`是否允许执行工具: {工具名}`（只问允不允许，没说为啥要问，不合格）
- 改后：`{safety_result.message}，是否允许执行？`（原因用安检原话；原话半角标点原样保留，仅追加全角后缀）
- 实例：`该路径超出允许范围(白名单外),需临时授权: /data/x，是否允许执行？`
- 兜底：`safety_result.message` 为空时回退改前通用问句（实核：非空仅路径类 3 处；flag 驱动 shell/delete 等走通用路径为空，回退是常态路径，非纯防御）

第 2 句（安全闸 bypass，`safety_gate.py:101`，用户不决策，属状态告知）：

- 改前：`安全策略自动确认工具执行: {工具名}`（"安全策略"四字多余）
- 改后：`安全开关已绕过，自动确认执行: {工具名}`

第 3 句（沙箱裁决，`sandbox_gate.py:116`）：

- 改前：`沙箱未能完成有效预检，需用户裁决是否直接执行: {工具名}`（起因说了，但没说沙箱发现了啥，半句）
- 改后：`沙箱未能完成有效预检：{blocked_reason截断140}，需用户裁决是否直接执行：{工具名}`（原因取 `pre.blocked_reason`，截断复用 `truncate_text(text,140)` 公用函数——hitl_gateway._summarize_params 同款（app/utils/text_utils.py，:49 延迟导入、:60 调用），口径与参数摘要天然对齐；为空时回退改前模板；新写两处冒号统一全角）

### 4.4 改动点（后端 2 行，前端 6 处）

- 后端第 1 行：`safety_gate.py:101-102`，content 构造换成 4.3 第 1、2 句逻辑。
- 后端第 2 行：`sandbox_gate.py:116`，content 追加 `blocked_reason`（截断复用 `truncate_text(text,140)` 公用函数，口径同 4.3；为空回退改前模板）。
- 前端第 1 处：`sseParser.ts:873` 转发从 `rawData.content` 取值加 `reason`（:858 已解析 content 进 step，此处补转发射出）——后端无 reason 字段，确认理由文本由 paused 帧 content 承载，不是凭空"加一字段"能拿到值的。
- 前端第 2 处：`sseParser.ts:150-159` handlers 类型 `onAuthorizationRequired` 签名加 `reason?: string`。
- 前端第 3 处：`useChatCallbacks.ts:101-111` 接口类型 + :818-828 回调内联参数类型，两处同步加 `reason?: string`。
- 前端第 4 处：`useSSE.ts:419-429` 形参类型加 `reason?: string`（:934/:991 仅透传，类型随签名，无需另改）。
- 前端第 5 处：`useAuthorization.ts:66-84` 装配加 `reason` 可选字段（依赖下方 AuthorizationModal 的 `AuthorizationRequest` 接口加字段，否则装配用不了）。
- 前端第 6 处：`AuthorizationModal` index.tsx:63-72 接口加 `reason?: string` + 标题下方加"确认理由"行，有值展示、无值隐藏（老 frames 兼容）。

理由行无需前端再截断：content 源自后端构造文案，safety_gate.py:101-102 的 message 来源（:269/:250/:162）已在提示幅内，沙箱侧 :116 的 blocked_reason 已按 140 截断，前端直接展示原串即可。

### 4.5 验收标准

- 白名单外路径触发：弹窗理由行显示"该路径超出允许范围(白名单外),需临时授权: {path}，是否允许执行？"（与 4.3 公式输出一字对齐）
- 删除目录触发：message 为空走 4.3 兜底，理由行显示改前通用问句"是否允许执行: {工具名}"（delete R4/R5 的"删除需确认: {path}"只进判定不进 SafetyResult，无原因可带）
- 沙箱裁决触发：理由行显示"沙箱未能完成有效预检：{原因}，需用户裁决是否直接执行：{工具名}"（与 4.3 公式输出一字对齐）
- bypass 触发：理由行显示"安全开关已绕过，自动确认执行: {工具名}"（与 4.3 第 2 句一字对齐；或按倒计时自动过，行为不变）
- 老版本 paused 帧（无 content）：理由行隐藏，弹窗其余 8 字段正常
