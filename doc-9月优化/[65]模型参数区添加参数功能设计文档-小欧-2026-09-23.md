# 设计文档：模型参数区「+ 添加参数」功能

**版本**: v1.11
**创建时间**: 2026-09-23 05:08:18
**编写人**: 小欧
**状态**: 待评审（§二落盘样子已打样 backend/config.yaml.example）

---

## 版本历史

| 版本 | 时间 | 更新人 | 修改简介 |
|------|------|--------|---------|
| v1.0 | 2026-09-23 05:08:18 | 小欧 | 初版：完整设计文档 |
| v1.1 | 2026-09-23 09:48:49 | 小欧 | 核查修正：①§1.4 top_p三参“零引用”结论过期（[64]后已全量消费），预定义表 5→8 并对齐注册表默认值；②P0-1渲染矛盾：ModelParams只遍历defaults导致新行不可见，改遍历params⊔defaults并集（1行）；③P0-2底部import块重复声明改合并到顶部；④§3.6/§6切换语义按BUG-D自动保存重写；⑤输入校验收口（禁空值/枚举必选/布尔严格）；⑥ranges/options落盘通道说明 |
| v1.2 | 2026-09-23 10:02:08 | 小欧 | 预设模式改勾选清单（一行一个：勾选框+参数名+中文说明+默认值+值输入框，替代下拉框）；PARAM_PRESETS加desc字段；确认门禁按行校验 |
| v1.3 | 2026-09-23 10:28:04 | 小欧 | 十遍通读一致性修正：①§2.1方案B残留“下拉选”改勾选清单；②§3.1数据流删defaults注入行（与§3.2决策矛盾）；③表单加existingKeys，已存在key不列出（addParam判重仅作安全网）；④批量提交统一关表单；⑤8标签纯中文+行`({p.key})`出英文 |
| v1.4 | 2026-09-23 10:33:55 | 小欧 | 写读统一性补洞：saveModelGroup 有新键时捎带全量 ranges/paramOptions 落 model_meta（否则刷新回显控件退化）；§4.2.4 落码；§5 同步 |
| v1.5 | 2026-09-23 10:39:39 | 小欧 | 增§3.9回显后编辑链路归一验证（编辑/校验/保存/重置/切换五链路与原生参数零区别）；§2.2补界面位置图；§3.1补配置文件落盘样子 |
| v1.6 | 2026-09-23 11:21:31 | 小欧 | 十遍抽查三修正：①§4.3.3 切 mode 清 checked/values（防旧勾选与旧值残留）；②§六风险表补 env 重名场景；③§2.2 图删 (Pascal) 英文残留 |
| v1.7 | 2026-09-23 11:41:11 | 小欧 | 并入§七 模型能力（多媒体支持）编辑设计（原[66]独立文档收编，[66]删除）：capabilities→model_meta 通道，与§二 model_params 通道并存；P0=仅能力变更必须省略 default_params（空{}会被后端当显式清空）；三决策点定案 Q1 未知值保留合并/Q2 重置不连带/Q3 脏计数+1 |
| v1.8 | 2026-09-23 11:47:49 | 小欧 | 补§7.3.9 配置文件落盘样子（四场景 yaml 实样+写链路锁/备份/校验/原子写/回滚+校验不检 capabilities 实证）+§7.3.10 回显与编辑通路归一验证（通路图+八链路表）；抓出两处实施必修：①ensureModelSaved 只查参数脏，能力脏切模型会静默丢失→扩展；②保存成功须同步 patch providers 内卡片 capabilities，防参数区与卡片同屏不同源；7.4 清单/量同步 |
| v1.9 | 2026-09-23 11:56:44 | 小欧 | 十遍全文交叉核查八修正：①【高】§7.4 body 与 §4.2.4 捎带补叠加形态（原 diff 会丢 range/param_options 捎带→v1.3 形态持久化退化）；②【高】Q1 未知值合并口径统一为"setCapabilities 内合并，state 恒含未知值，提交直接送"（原 7.2/7.3.1/7.4 三处口径互斥且 mergeWithUnknown 悬空）；③§4.3.3 PARAM_PRESETS 补 8 项 desc 字段（渲染 L707 用 p.desc 缺字段会 TS 报错，对齐 §2.3/v1.2）；④§2.2 图 max_tokens [8192]→[16384]（对齐 §2.3 注册表默认值）；⑤7.3.6"按钮文案"→"确认弹窗文案（SettingsPage:270 实文）"；⑥7.4 清单#8 注明 Checkbox 已由 §4.3.0 引入防重复 import；⑦总计 ~70→~73 对齐明细；⑧修正 v1.8 简介"四场景"笔误（正文权威为五场景，历史行不改） |
| v1.10 | 2026-09-23 12:07:32 | 小欧 | 前端规整 + 10 大规范核查九修正：①SRP/组件一文件惯例（settings2 现有 19 组件全独立文件）：AddParamForm 由 SettingsPage 文件底部内嵌 → 独立 `AddParamForm.tsx`（§4.3.0/§4.3.3/清单同步，§四 4→5 文件）；②DRY+复用优先：caps 脏判定 5 处散写 `JSON.stringify` 不等式 → modelUtils 导出 `isCapsDirty`（内部复用既有 `sameValue`），`CAPABILITY_OPTIONS` 收口 modelUtils 单源；③硬编码色 `#d9d9d9/#fafafa` → `Colors.BORDER.DEFAULT/Colors.BG.LIGHT`；④`borderRadius: 4` → `settingsRadius.SM`（[58] v1.12 铁规先例 AboutFiles:8）；⑤散落宽度 120/160/100 → `settingsControl.*` 统一 180 族（2026-09-22/23 控件宽度统一令）；⑥文本 Input 误用 `inputNumberWidth` → `inputWidth`；⑦+添加参数 `marginRight: Spacing.MD` → `Spacing.XL` 对齐「管理选项」同排间距；⑧能力行控件补 `flex:1` 包裹（对齐 ModelParams:155/ProviderConfig 行结构）；⑨AddParamForm `values` 初始表达式两处重复 → `initValues` 函数（DRY）；§4.3.1 当前代码快照对齐真实文件（补 disabled、行号 228-284） |
| v1.11 | 2026-09-23 12:17:41 | 小欧 | §二「配置文件落盘样子」按北京老陈指令落到配置 example 打样：`backend/config.yaml.example`（后端根目录，[64]v1.4 确认的真身）zhipuai 段——model_params 补「+添加参数」新键缀尾（temperature/top_p）+ 自定义键注释样例 + model_meta.range 捎带完整写法（块式 min/max，对齐 §4.2.4 全量捎带）；文件头编辑历史追加行；§3.1 落盘样子处补打样引用（以 example 文件为准） |

---

## 一、问题说明

### 1.1 现象

设置页 → 模型 Tab → ② 参数区（跟随当前模型）：当用户选择一个 **config.yaml 中未配置 `model_params`** 的模型时（如 qiniu provider 的 deepseek-v3.1），参数区**完全空白**，没有任何可操作控件。

### 1.2 根因

`ModelParams.tsx` 渲染逻辑（行 46）：
```tsx
{Object.keys(defaults).map((key) => { ... })}
```

`defaults` 来自后端 `_models_of()` 返回的 `default_params`，而 `default_params` 来自 `config.yaml → ai.{provider}.model_params.{model}`。**config.yaml 中没有 `model_params` 块的模型 → `default_params = {}` → `Object.keys({}) = []` → 零渲染。**

### 1.3 用户需求

在参数区增加 **「+ 添加参数」按钮**，允许用户动态为当前模型添加自定义参数（如 temperature、max_tokens、reasoning_effort 等），无需手动编辑 config.yaml。

### 1.4 项目实际使用的模型参数

从 `config.yaml` 和 `backend/app/llm/` 代码中提取：

| 参数名 | 类型 | 范围/选项 | 用途 | 通用性 |
|--------|------|----------|------|--------|
| `temperature` | number(float) | 0.0 ~ 2.0 | 采样温度 | 通用（所有模型） |
| `max_tokens` | number(int) | 1 ~ 100000 | 最大输出 token | 通用 |
| `top_p` | number(float) | 0.0 ~ 1.0 | 核采样阈值 | 通用（[64]后后端全量消费） |
| `frequency_penalty` | number(float) | -2.0 ~ 2.0 | 频次重复惩罚 | 通用（[64]后后端全量消费） |
| `presence_penalty` | number(float) | -2.0 ~ 2.0 | 话题重复惩罚 | 通用（[64]后后端全量消费） |
| `reasoning_effort` | enum(string) | low / medium / high | 推理深度 | 仅推理模型 |
| `context_limit` | number(int) | 1000 ~ 900000 | 上下文裁剪阈值 | 通用 |
| `seed` | number(int) | 任意整数 | 随机种子 | 通用 |
| `chat_template_kwargs` | object(JSON) | `{"enable_thinking":true}` | 模板开关 | 通用（走自定义模式输入JSON） |

**说明**：v1.0 曾写“`top_p`、`frequency_penalty`、`presence_penalty` 代码零引用”——该结论已过期，[64] 后 `client_sdk._build_request_body` + `base_service` 对三参全量消费；三参值域明确，最适合进预定义表。

---

## 二、修改策略及 UI 形式

### 2.1 策略选择

| 方案 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| A. 纯自由输入 | 用户手动填 key + 选类型 + 填值 | 灵活 | 拼错 key、类型选错、不知道填什么 |
| **B. 预定义 + 自定义混合** | **勾选清单选常用参数（一行一个，自动带默认值/范围/控件），兜底自定义输入** | **安全 + 灵活** | 需维护预定义表 |
| C. 全自动注入 | 后端自动给无 model_params 的模型注入全局默认参数 | 零操作 | 用户无法控制、参数膨胀 |

**选择方案 B**：预定义 8 个常用参数 + 自定义兜底（自定义覆盖 object/JSON 等特殊类型）。

### 2.2 UI 交互流程

```
用户点击标题行右侧「+ 添加参数」（与标题同一行，不另起一行）
        ↓
  标题行下方展开内联表单（行内撑开，非 Modal 弹框；关闭即收回）
        ↓
  ┌─ Radio: [常用参数] [自定义] ─┐
  │                              │
  │  常用参数模式（勾选清单，非下拉框）:   │
  │  8 个预设一行一个，每行 = [勾选框]   │
  │    参数名(key) + 中文说明 + 默认值    │
  │    + 值输入框（number/enum按类型）    │
  │  → 勾选要加的 → 值有效才可点确认     │
  │                              │
  │  自定义模式:                  │
  │  Input 填参数名 + 类型下拉    │
  │  Input 填值                  │
  │  → 同上有效性门禁 → 确认      │
  └──────────────────────────────┘
        ↓
  参数区自动新增一行控件（依赖 §4.4 并集渲染修复）
  （有 range → Slider，有 options → Select，boolean → Switch，
   number → InputNumber，string → Input，object → TextArea/JSON）
        ↓
  新行带 DirtyDot，用户可继续编辑值
  → 点底部「保存本组」→ PUT /models 落盘 config.yaml
```

**新行在界面上的位置**（②参数区内，末尾追加）：

```
┌─ ② 参数区（跟随当前模型）──────────── [+ 添加参数] ─┐
│                                                      │
│  [+ 添加参数]点开后：标题行下方撑出虚线表单            │
│  ┌─ AddParamForm（虚线框） ──────────────────────┐   │
│  │ 常用参数 | 自定义                              │   │
│ │ [x] 温度 (temperature)：采样温度…   [0.7] 默认0.7│
│ │ [ ] 最大Token (max_tokens)：单次…   [16384] …   │   │
│  │ ...（8行勾选清单）                    [确认][取消]│
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  原有行（后端 defaults 顺序）：                        │
│  reasoning_effort  [medium ▾]                        │
│  context_limit     [200000]                          │
│                                                      │
│  新增行（缀在末尾，按添加顺序）：                      │
│  temperature       [0.7]  ●(DirtyDot)                 │
│  top_p             [1.0]  ●                           │
│                                                      │
└──────────────────────────────────────────────────────┘
         ↓ 点底部「保存本组」→ 行留下 ● 熄灭
```

规则：原有行保持后端顺序不动；新行一律缀尾（会话内按确认顺序，落盘后按 yaml 块尾顺序，回显一致）。

### 2.3 预定义参数表

```typescript
const PARAM_PRESETS = [
  { key: 'temperature',       type: 'number' as const, default: 0.7,      range: { min: 0, max: 2 },        label: '温度',     desc: '采样温度：0=完全确定，2=最随机' },
  { key: 'max_tokens',        type: 'number' as const, default: 16384,    range: { min: 1, max: 100000 },   label: '最大Token', desc: '单次最大输出 token 数，超长截断' },
  { key: 'top_p',             type: 'number' as const, default: 1.0,      range: { min: 0, max: 1 },         label: '核采样',    desc: '从概率质量前 p 的词中采样；1.0=不筛选' },
  { key: 'frequency_penalty', type: 'number' as const, default: 0,        range: { min: -2, max: 2 },        label: '频次惩罚', desc: '正值减少重复（更多样），负值增加重复，0=不启用' },
  { key: 'presence_penalty',  type: 'number' as const, default: 0,        range: { min: -2, max: 2 },        label: '存在惩罚',  desc: '正值鼓励新话题，负值鼓励重复，0=不启用' },
  { key: 'reasoning_effort',  type: 'enum' as const,   default: 'medium', options: ['low','medium','high'], label: '推理深度',   desc: '推理深度，仅推理模型有效' },
  { key: 'context_limit',     type: 'number' as const, default: 262144,   range: { min: 1000, max: 900000 }, label: '上下文限制', desc: '上下文窗口上限，超限裁剪旧轮' },
  { key: 'seed',              type: 'number' as const, default: null,     range: { min: 0, max: 999999 },    label: '随机种子',   desc: '固定随机种子，复现输出' },
];
```

**默认值口径**：与注册表/全局缺省对齐（temperature 0.7、max_tokens 16384、context_limit 262144；top_p三参见 `llm.sampling.*`）；seed 无全局缺省，default null 仅作占位，确认时禁空值（见 §4.3.3 校验）。

### 2.4 控件自动映射

| 条件 | 渲染控件 | 来源 |
|------|---------|------|
| 有 range | `Slider` + `InputNumber` | `PARAM_PRESETS[i].range` 注入 `state.model.ranges` |
| 有 options | `Select` | `PARAM_PRESETS[i].options` 注入 `state.model.paramOptions` |
| 值为 number 无 range | `InputNumber` | `typeof value === 'number'` |
| 值为 boolean | `Switch` | `typeof value === 'boolean'`（[62]P4 五分支已支持） |
| 值为 string | `Input` | `typeof value === 'string'` |
| 值为 object | `TextArea`(JSON) | 自定义模式输入 JSON 字符串解析后注入 |

**关键修正（v1.1 P0-1）**：`ModelParams.tsx` 按 `Object.keys(defaults)` 遍历渲染，而 `addParam` 为保脏故意不注入 `defaults`——若不动渲染，新行点确认后**不可见**。修法：渲染改遍历 `params ⊔ defaults` 并集（`ModelParams.tsx:46` 改 1 行，见 §4.4），判脏逻辑不动（新 key 在 defaults 中无 → 天然判脏）。

---

## 三、关联逻辑和参数冲突性分析

### 3.1 数据流完整性分析

```
[添加参数]
addParam(key, value, meta)
  → state.model.params[key] = value      ← 编辑副本
  → state.model.defaults 不注入此 key     ← 保脏（见 §3.2 决策）
  → state.model.ranges[key] = meta.range  ← 控件类型
  → state.model.paramOptions[key] = meta.options  ← 枚举选项
  → ModelParams 并集渲染出新行（依赖 §4.4，否则不可见）
       ↓
[保存]
saveModelGroup()
  → dirty = isDirty(params, defaults, envOverride)
  → changed = {k: params[k] for k in dirty if dirty[k]}
  → PUT /models/{provider}/{model}  body: {default_params: changed}
       ↓
[后端写入]
update_model()  (model_service.py:229-270)
  → old_params = config.yaml 原值
  → old_params.update(changed)  ← 自动合并新 key
  → node["model_params"][model] = old_params
  → merge_nested_patch() → config.yaml
       ↓
[回读]
GET /models → _models_of() → params_block.get(model) → 包含新 key
→ defaults 包含新 key → UI 正常显示
```

**保存链路无需任何改动**——`update_model()` 的 `old_params.update(dp)` 已支持任意新 key。

**配置文件落盘样子**（以 qiniu/deepseek-v3.1 为例，原无 `model_params` 块，勾选 temperature + top_p 后点保存；**打样已落 `backend/config.yaml.example` zhipuai 段——新键缀尾 + 自定义键注释样例 + `model_meta.range` 捎带完整写法，以该文件为准** — 小欧 2026-09-23）：

```yaml
# 保存前：该模型无 model_params 块
  qiniu:
    models:
    - deepseek-v3.1
    - deepseek-v3
    timeout: 90

# 保存后：自动新建 model_params 块，新键缀尾
  qiniu:
    models:
    - deepseek-v3.1
    - deepseek-v3
    model_params:
      deepseek-v3.1:
        temperature: 0.7
        top_p: 1.0
    timeout: 90
```

预设带 range/options 的（如 reasoning_effort 选 medium），`model_meta` 同步多一段（§4.2.4 捎带）：

```yaml
    model_meta:
      deepseek-v3.1:
        param_options:
          reasoning_effort: [low, medium, high]
```

### 3.2 脏态冲突分析

| 场景 | 行为 | 是否冲突 |
|------|------|---------|
| 添加参数后 defaults 同步注入 | 新 key 在 params 和 defaults 中值相同 → `isDirty = false` → 不脏 | ⚠️ 用户未编辑却显示"已保存" |
| **添加参数后 defaults 不同步** | **新 key 在 defaults 中不存在 → `isDirty = true` → 立即脏** | ✅ 正确：必须保存才落盘 |
| 保存后 defaults 同步 | `patchModel({ defaults: { ...params } })` → defaults 更新为 params 快照 | ✅ 正确：新基线 |
| 切换模型后未保存 | `selectModel` 先 `ensureModelSaved` 自动保存（BUG-D），新参数随之落盘 | ✅ 不丢失，无需提示 |

**决策：defaults 不同步注入**。添加参数后 `defaults` 中无此 key → `isDirty` 遍历 `Object.keys(params)` 发现该 key 在 defaults 中无 → `sameValue(params[key], undefined)` → `false` → `isDirty[key] = true` → 立即脏。用户必须点"保存本组"才落盘。

**配套（v1.1 P0-1）**：渲染侧改遍历并集后，新行立即可见 + DirtyDot 正常显示（`dirty[key]` 由本地 `isDirty()` 算出，与 state 无关），与本决策无冲突。

### 3.3 isDirty 函数兼容性验证

```typescript
// modelUtils.ts:35
Object.keys(params).forEach((key) => {
    out[key] = !env[key] && !sameValue(params[key], defaults[key]);
});
```

当 `defaults` 中无新增 key 时：
- `defaults[key]` = `undefined`
- `sameValue(value, undefined)` = `false`（任何值与 undefined 比较都 false）
- `isDirty[key]` = `true` ✅

**无冲突，无需修改 `isDirty` 函数。**

### 3.4 保存链路兼容性验证

```python
# model_service.py:256-257
old_params = dict(ai[provider].get("model_params", {}).get(model, {}) or {})
old_params.update(dp)  # dp = {temperature: 0.7}
```

当 config.yaml 中 model 无 `model_params` 块时：
- `ai[provider].get("model_params", {})` = `{}`（不存在则返回空 dict）
- `{}.get(model, {})` = `{}`
- `old_params = {}`
- `{}.update({temperature: 0.7})` = `{temperature: 0.7}`
- `node["model_params"][model] = {temperature: 0.7}` ✅

**无冲突，后端自动处理。**

### 3.5 后端校验兼容性验证

```python
# model_service.py:252-254
for k, v in dp.items():
    if k in allowed and v not in allowed[k]:
        raise ValueError(f"不支持的配置项值: {k}={v!r}，允许{allowed[k]}")
```

当 `allowed = {}`（空 param_options）时：
- `k in allowed` = `False` → 跳过校验 ✅

**无冲突。**

### 3.6 切换模型/Provider 冲突

| 操作 | 行为 | 影响 |
|------|------|------|
| 切换模型 | `selectModel` 先调 `ensureModelSaved` 自动保存未落库参数（失败才阻止切换，BUG-D 2026-09-21），再重置 params/defaults/ranges/paramOptions | 新参数随自动保存落盘，不丢失；无需 SaveBar 提示 |
| 切换 Provider | 同上（`selectProvider` 同款 ensureModelSaved 门） | 同上 |
| 保存全部 | `saveAll` → 依次调 saveGroup | 新参数随 model 组一起保存 |

**说明（v1.1 修正）**：v1.0 写“未保存切走则丢失”已过期；现行为是切走前自动保存。新参数在切走瞬间即落盘（`PUT /models` 全量 changed 含新 key），`selectModel` 用新模型条目重置三件套，无跨模型泄漏。

### 3.7 envOverride 冲突

新添加的参数 key 不在 `envOverride` 中（envOverride 从后端 `provider.env` 构建，只包含已有参数的 env 接管标记）。

- `envOverride[key]` = `undefined` → `!undefined` = `true` → 不禁改 ✅
- 新参数始终可编辑 ✅

**无冲突。**

### 3.8 param_options 校验冲突

`setParam` 中有枚举拦截：
```typescript
const opts = state.model.paramOptions[key];
if (opts && !opts.includes(value as string)) { ... return; }
```

新添加的预定义参数（如 reasoning_effort）通过 `addParam` 注入 `paramOptions` → `setParam` 编辑时自动校验 ✅

自定义参数无 `paramOptions` → 不校验 → 自由输入 ✅

**落盘边界（v1.1 补充，v1.3 修正）**：`saveModelGroup` 默认只送 `default_params`——新键值能存，但其 `ranges/paramOptions` 不落盘。保存后本会话内形态 intact；一切换模型/整页刷新（`selectModel/refreshModels` 按后端条目重建三件套），新行退化为 InputNumber/Input，违背“回显正常”。

修法（§4.2.4）：保存时若含新键，把全量 `ranges` + `paramOptions` 一并送后端落 `model_meta`（state 里本就是后端全集+注入，全量送无丢失；`update_model` 整块替换+先合并再校验，新枚举值能过）。无新键的保存行为零变化。

**无冲突（保存链不动）。**

### 3.9 回显后编辑链路归一验证

新参数落盘回显后，与原生参数走**同一套**通用链路，无任何特殊分支：

| 链路 | 新参数行为 | 依据 |
|------|-----------|------|
| 编辑 | `setParam` 写 `params[key]`，值变即脏 | `useSettings` setParam；回显后该键已在 `defaults`，判脏规则与原生一致 |
| 校验 | 枚举拦截读 `paramOptions[key]`，range 钳制同理 | 回显的形态来自后端 `model_meta`（§4.2.4 落盘），拦截照样生效 |
| 保存 | 「保存本组」→ `saveModelGroup` 重算 dirty → `PUT /models` → `update_model` 合并覆盖 | 无新键时 body 与原来一字不差 |
| 重置 | `resetParams` 从 `defaults` 恢复，新键一样被重置 | 回显后新键已是基线的一部分 |
| 切换 | `ensureModelSaved` 切前自动保存同样覆盖 | BUG-D 门对所有脏键一视同仁 |

---

## 四、实施代码 diff

### 改动清单

| # | 文件 | 行号 | 改动类型 | 改动量 |
|---|------|------|---------|--------|
| 1 | `types.ts` | 59 | 新增 state 字段 | +1 行 |
| 2 | `useSettings.ts` | 150 | initialModel 补字段 | +1 行 |
| 3 | `useSettings.ts` | 796 附近 | 新增 `addParam` 函数 | +25 行 |
| 4 | `useSettings.ts` | 870 附近 | 暴露 `addParam` | +1 行 |
| 5 | `SettingsPage.tsx` | 顶部 import | 合并追加（Checkbox + settingsRowStyle/settingsLabelStyle + DirtyDot 并入 icons 行 + AddParamForm/CAPABILITY_OPTIONS/isCapsDirty 导入） | +4 行 |
| 6 | `SettingsPage.tsx` | 228-284 | 参数区标题行加按钮 + 条件渲染 | +8 行 |
| 7 | `AddParamForm.tsx` | **新文件** | 独立组件文件（自带顶部 import + `export const AddParamForm`；组件一文件惯例，v1.10） | +95 行 |
| 8 | `ModelParams.tsx` | 46 | 遍历改 params⊔defaults 并集 | +1 行 |
| 9 | `useSettings.ts` | `saveModelGroup` 内 | 有新键时捎带全量 ranges/paramOptions | +12 行 |

**总计：~148 行改动，涉及 5 个前端文件，0 处后端改动。**

---

### 4.1 `frontend/src/features/settings2/types.ts`

**位置**：行 59，`paramOptionsModalOpen` 后面

**diff**：
```diff
   paramOptionsModalOpen: boolean;
+  addParamFormOpen: boolean;
   deleteConfirmOpen: boolean;
```

**原因**：控制「+ 添加参数」内联表单的显示/隐藏。

---

### 4.2 `frontend/src/features/settings2/hooks/useSettings.ts`

#### 4.2.1 initialModel 补字段

**位置**：行 150，`paramOptionsModalOpen: false` 后面

**diff**：
```diff
   paramOptionsModalOpen: false,
+  addParamFormOpen: false,
   deleteConfirmOpen: false,
```

#### 4.2.2 新增 addParam 函数

**位置**：行 796 附近，`setParam` 函数后面

**diff**：
```diff
   const setParam = useCallback(
     (key: string, value: unknown) => {
       // ... 现有代码 ...
     },
     [state.model.envOverride, state.model.ranges, state.model.paramOptions]
   );

+  /** 添加新参数：注入 params + ranges + paramOptions，defaults 不同步（立即判脏） */
+  const addParam = useCallback(
+    (
+      key: string,
+      value: unknown,
+      meta?: { range?: { min: number; max: number }; options?: string[] }
+    ) => {
+      // 1. key 去重
+      if (key in state.model.params) {
+        showMessage(ErrorType.WARNING, `参数 ${key} 已存在`);
+        return;
+      }
+      // 2. 类型校验
+      if (meta?.options && !meta.options.includes(value as string)) {
+        showMessage(ErrorType.WARNING, `参数 ${key} 的值 ${value} 不在选项 ${meta.options.join('/')} 中`);
+        return;
+      }
+      // 3. 注入 state（defaults 不注入 → 立即判脏）
+      setState((s) => {
+        const params = { ...s.model.params, [key]: value };
+        const ranges = meta?.range
+          ? { ...s.model.ranges, [key]: meta.range }
+          : s.model.ranges;
+        const paramOptions = meta?.options
+          ? { ...s.model.paramOptions, [key]: meta.options }
+          : s.model.paramOptions;
+        return {
+          ...s,
+          model: {
+            ...s.model,
+            params,
+            ranges,
+            paramOptions,
+            isDirty: true,
+          },
+        };
+      });
+    },
+    [state.model.params, state.model.ranges, state.model.paramOptions]
+  );
```

#### 4.2.3 暴露 addParam

**位置**：useSettings 函数的 return 对象中（约行 870）

**diff**：
```diff
     setParam,
+    addParam,
     resetParams,
```

#### 4.2.4 saveModelGroup 捎带新键形态（v1.3：否则刷新回显退化）

**位置**：`saveModelGroup` 内，`changed` 算出后、`updateModel` 调用前

**diff**：
```diff
     const changed = Object.fromEntries(
       Object.keys(dirty)
         .filter((k) => dirty[k])
         .map((k) => [k, state.model.params[k]])
     );
     if (!Object.keys(changed).length) return { ok: true as const };
+    // v1.3 新增键形态持久化：新 key 才带全量 ranges/paramOptions 落 model_meta；
+    // 无新键时 body 与原来完全一致（零行为变化）
+    const prevDefaults = state.model.defaults;
+    const newKeys = Object.keys(changed).filter((k) => !(k in prevDefaults));
+    const body: {
+      default_params: Record<string, unknown>;
+      range?: Record<string, { min: number; max: number }>;
+      param_options?: Record<string, string[]>;
+    } = { default_params: changed };
+    if (newKeys.length) {
+      body.range = { ...state.model.ranges };
+      body.param_options = { ...state.model.paramOptions };
+    }
     setSaving(true);
     try {
       // S11：保存前校验 mtime（模型参数与设置同落 YAML），外部已更新则刷新并中止
       const curMtime = await settingsApi.getMtime();
       if (curMtime !== state.mtime) {
         await load({ reset: true });
         showMessage(
           ErrorType.WARNING,
           '配置已被外部更新，已重新加载，请核对后重新保存'
         );
         return { ok: false as const };
       }
       // A1：模型参数写 ai.{provider}.model_params.{model}.{key}（运行时 parse_model_params 消费），
       // 经 PUT /models/{provider}/{model} 的 default_params 通道，不再走 /settings 裸 key（registry 无此 key 会保存失败）
       const r = await modelApi.updateModel(
         state.model.selectedProvider,
         state.model.selectedModel,
-        {
-          default_params: changed,
-        }
+        body
       );
```

**为什么全量送**：`state.model.ranges/paramOptions` 本就是后端全集 + 本次注入；`update_model` 对 `range/param_options` 整块替换，全量送无丢失。`update_model` 先合并选项再校验（v1.6），新枚举值能过校验。

---

### 4.3 `frontend/src/features/settings2/components/SettingsPage.tsx`

#### 4.3.0 顶部 import 合并追加（v1.1 P0-2：同文件禁止底部重复 import；v1.10：AddParamForm 独立成文件自带 import）

```diff
 import React, { useState } from 'react';
-import { Button, Card, Modal, Result, Skeleton, Tabs } from 'antd';
+import { Button, Card, Checkbox, Modal, Result, Skeleton, Tabs } from 'antd';
 import { Colors, FontSize, Spacing, FontWeight } from '@/utils/stepStyles';
-import { settingsSpacing, settingsModalWidth } from '@/theme/settingsTokens';
+import { settingsRowStyle, settingsLabelStyle, settingsSpacing, settingsModalWidth } from '@/theme/settingsTokens';
 ...
-import { SettingIcon } from './icons';
+import { SettingIcon, DirtyDot } from './icons';
+import { AddParamForm } from './AddParamForm';
+import { CAPABILITY_OPTIONS, isCapsDirty } from '../utils/modelUtils';
```

**原因**：能力行复用 `settingsRowStyle/settingsLabelStyle`（[58]DRY，禁止第四套行样式）；`Checkbox` 入 antd 行、`DirtyDot` 并入既有 icons 行（禁止另起重复 import 行）。参数预设表单自 v1.10 起移入独立文件 `AddParamForm.tsx`（组件一文件惯例），SettingsPage 不再需要 Input/InputNumber/Radio/Select/settingsControl——跨文件各自 import 顶部令牌≠重复样式。注意不用 `SettingRow` 组件本身——它是设置项 schema 行（含来源/脏态/env 逻辑），与勾选清单语义不合。

#### 4.3.1 参数区标题行加按钮

**位置**：行 228-284（② 参数区标题行，flex space-between 容器）。**按钮与标题同一行**（标题文本右侧，flex 行内排列），**不另起一行**；下面展开的是表单（见 4.3.2），不是按钮换行。

**当前代码**（v1.10 对齐真实文件）：
```tsx
<div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
    <SectionTitle title="── ② 参数区（跟随当前模型） ──" />
    {/* [62]P8：管理选项入口（仅 param_options 非空显示，与「重置为默认」同排） */}
    {Object.keys(state.model.paramOptions).length > 0 && (
        <Button type="link" style={{ padding: 0, color: Colors.TEXT.SECONDARY, marginRight: Spacing.XL }} ...>
            管理选项
        </Button>
    )}
    {Object.keys(state.model.params).length > 0 && (
        <Button type="link" style={{ padding: 0, color: Colors.TEXT.SECONDARY }}
                disabled={!state.model.isDirty} ...>
            重置为默认
        </Button>
    )}
</div>
```

**diff**：
```diff
 <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
     <SectionTitle title="── ② 参数区（跟随当前模型） ──" />
+    <Button
+      type="link"
+      style={{ padding: 0, color: Colors.TEXT.SECONDARY, marginRight: Spacing.XL }}
+      onClick={() => s.patchModel({ addParamFormOpen: true })}
+    >
+      + 添加参数
+    </Button>
     {Object.keys(state.model.paramOptions).length > 0 && (
         <Button ... onClick={() => s.patchModel({ paramOptionsModalOpen: true })}>
             管理选项
         </Button>
     )}
     {Object.keys(state.model.params).length > 0 && (
         <Button ... onClick={() => { Modal.confirm({ ... onOk: () => s.resetParams() }); }}>
             重置为默认
         </Button>
     )}
 </div>
```

#### 4.3.2 条件渲染 AddParamForm

**位置**：行 284（标题行 div 结束后），`<ModelParams>` 前面

**diff**：
```diff
 </div>
+{state.model.addParamFormOpen && (
+  <AddParamForm
+    existingKeys={Object.keys(state.model.params)}
+    onAdd={(key, value, meta) => {
+      s.addParam(key, value, meta);
+    }}
+    onCancel={() => s.patchModel({ addParamFormOpen: false })}
+  />
+)}
```
说明：勾选多个时表单内循环逐个调 `onAdd`（`addParam` 函数式 setState 可连调）；全部提交完表单调一次 `onCancel` 统一关闭（任一 `key in params` 判重 warning 不阻断其余）。

#### 4.3.3 新增 AddParamForm 组件（v1.10：独立文件 AddParamForm.tsx）

**位置**：新文件 `frontend/src/features/settings2/components/AddParamForm.tsx`（v1.1 曾定 SettingsPage 文件底部内嵌，v1.10 按「组件一文件」现行惯例改独立文件——settings2 现有 19 个组件全部独立成文件，SRP；P0-2「禁止底部 import」照样适用：本文件 import 全部置顶）。`PARAM_PRESETS` 随组件同文件。

**文件顶部 import（全部置顶）**：
```tsx
// 编辑历史: 2026-09-23 小欧 - 新建：模型参数区「+ 添加参数」内联表单（预定义+自定义混合）
// v1.1: 预设 5→8（加 top_p/frequency_penalty/presence_penalty，默认值对齐注册表）；确认按钮有效性门禁（禁空值/枚举必选/布尔严格）
// v1.6: 切 mode 重置 checked/values（防旧勾选与旧值残留）- 小欧-2026-09-23
// v1.10: 独立成 AddParamForm.tsx（组件一文件惯例）；容器色/圆角/控件宽全部令牌化
//   （#d9d9d9→Colors.BORDER.DEFAULT、#fafafa→Colors.BG.LIGHT、borderRadius:4→settingsRadius.SM、
//    宽 120/160/100→settingsControl 统一 180 族、文本 Input 误用 inputNumberWidth→inputWidth）；
//   values 初始表达式两处重复抽 initValues（DRY）- 小欧-2026-09-23
import React, { useState } from 'react';
import { Button, Checkbox, Input, Radio, Select } from 'antd';
import { Colors, Spacing } from '@/utils/stepStyles';
import {
  settingsControl,
  settingsRowStyle,
  settingsLabelStyle,
  settingsRadius,
} from '@/theme/settingsTokens';
```

**完整组件代码**：
```tsx
// 编辑历史: 2026-09-23 小欧 - 新建：模型参数区「+ 添加参数」内联表单（预定义+自定义混合）
// v1.1: 预设 5→8（加 top_p/frequency_penalty/presence_penalty，默认值对齐注册表）；确认按钮有效性门禁（禁空值/枚举必选/布尔严格）
// v1.6: 切 mode 重置 checked/values（防旧勾选与旧值残留）- 小欧-2026-09-23

/** 预定义参数表：从项目实际使用的模型参数中提取（v1.9：补 8 项 desc 字段，对齐 §2.3/v1.2，否则 L707 p.desc 渲染 TS 报错） */
const PARAM_PRESETS = [
  { key: 'temperature',       type: 'number' as const, default: 0.7,    range: { min: 0, max: 2 },       label: '温度',     desc: '采样温度：0=完全确定，2=最随机' },
  { key: 'max_tokens',        type: 'number' as const, default: 16384,    range: { min: 1, max: 100000 },   label: '最大Token', desc: '单次最大输出 token 数，超长截断' },
  { key: 'top_p',             type: 'number' as const, default: 1.0,      range: { min: 0, max: 1 },         label: '核采样',    desc: '从概率质量前 p 的词中采样；1.0=不筛选' },
  { key: 'frequency_penalty', type: 'number' as const, default: 0,        range: { min: -2, max: 2 },        label: '频次惩罚', desc: '正值减少重复（更多样），负值增加重复，0=不启用' },
  { key: 'presence_penalty',  type: 'number' as const, default: 0,        range: { min: -2, max: 2 },        label: '存在惩罚',  desc: '正值鼓励新话题，负值鼓励重复，0=不启用' },
  { key: 'reasoning_effort', type: 'enum' as const,   default: 'medium', options: ['low', 'medium', 'high'], label: '推理深度',   desc: '推理深度，仅推理模型有效' },
  { key: 'context_limit',     type: 'number' as const, default: 262144,   range: { min: 1000, max: 900000 }, label: '上下文限制', desc: '上下文窗口上限，超限裁剪旧轮' },
  { key: 'seed',             type: 'number' as const, default: null,   range: { min: 0, max: 999999 },   label: '随机种子',   desc: '固定随机种子，复现输出' },
];

interface AddParamFormProps {
  onAdd: (key: string, value: unknown, meta?: { range?: { min: number; max: number }; options?: string[] }) => void;
  onCancel: () => void;
  existingKeys: string[];  // v1.3：已存在参数不再列出（addParam 内 key in params 判重仅作安全网）
}

// v1.10 DRY：values 初始表达式原本 useState 与切 mode 两处重复，抽单函数
const initValuesOf = (presets: (typeof PARAM_PRESETS)[number][]) =>
  Object.fromEntries(presets.map((p) => [p.key, p.default !== null ? String(p.default) : '']));

export const AddParamForm: React.FC<AddParamFormProps> = ({ onAdd, onCancel, existingKeys }) => {
  const [mode, setMode] = useState<'preset' | 'custom'>('preset');
  // v1.3：已存在 key 不列出（主防），addParam 内判重为安全网
  const presets = PARAM_PRESETS.filter((p) => !existingKeys.includes(p.key));
  // v1.2 勾选清单：checked 勾选态，values 每行输入值（初始=默认值转字符串，null 填空）
  const [checked, setChecked] = useState<Record<string, boolean>>({});
  const [values, setValues] = useState<Record<string, string>>(() => initValuesOf(presets));
  const [customKey, setCustomKey] = useState('');
  const [customType, setCustomType] = useState<'string' | 'number' | 'boolean'>('number');
  const [value, setValue] = useState<string>('');

  const presetValid = (p: (typeof PARAM_PRESETS)[number], v: string): boolean => {
    if (v === '') return false;
    if (p.type === 'number') {
      const n = Number(v);
      return !isNaN(n) && n >= p.range.min && n <= p.range.max;
    }
    if (p.type === 'enum') return (p.options ?? []).includes(v);
    return true;
  };

  // v1.1 有效性门禁：勾选至少一个且所勾行全有效；自定义空值/非法禁确认（不落 null、不存空串；布尔只认 true/false）
  const canConfirm = (() => {
    if (mode === 'preset') {
      const keys = Object.keys(checked).filter((k) => checked[k]);
      if (!keys.length) return false;
      return keys.every((k) => {
        const p = presets.find((x) => x.key === k);
        return p ? presetValid(p, values[k] ?? '') : false;
      });
    }
    if (!customKey.trim()) return false;
    if (value === '') return false;
    if (customType === 'number') return !isNaN(Number(value));
    if (customType === 'boolean') return value === 'true' || value === 'false';
    return true;
  })();

  const handleAdd = () => {
    if (!canConfirm) return;
    if (mode === 'preset') {
      Object.keys(checked).filter((k) => checked[k]).forEach((k) => {
        const p = presets.find((x) => x.key === k);
        if (!p) return;
        const v: unknown = p.type === 'number' ? Number(values[k]) : values[k];
        onAdd(p.key, v, { range: p.range, options: p.options });
      });
    } else {
      // 自定义模式
      const key = customKey.trim();
      if (!key) return;
      let v: unknown = value;
      if (customType === 'number') v = Number(value);
      if (customType === 'boolean') v = value === 'true';
      onAdd(key, v);
    }
    onCancel();  // 批量提交完统一关表单（不在 onAdd 回调里逐次关）
  };

  const inputStyle = { width: settingsControl.inputWidth }; // v1.10：文本 Input 用 inputWidth（原误用 inputNumberWidth）

  return (
    <div
      style={{
        display: 'flex',
        gap: Spacing.SM,
        alignItems: 'center',
        padding: `${Spacing.SM}px ${Spacing.MD}px`,
        margin: `${Spacing.SM}px 0`,
        border: `1px dashed ${Colors.BORDER.DEFAULT}`, // v1.10 令牌化（原硬编码 #d9d9d9）
        borderRadius: settingsRadius.SM, // v1.10 令牌化（原裸 4，[58]铁规）
        background: Colors.BG.LIGHT, // v1.10 令牌化（原硬编码 #fafafa）
      }}
    >
      <Radio.Group
        value={mode}
        onChange={(e) => { setMode(e.target.value); setValue(''); setCustomKey(''); setChecked({}); setValues(initValuesOf(presets)); }}  // v1.6：切 mode 清勾选与输入，防旧勾选/旧值残留（v1.10 改用 initValuesOf）
        size="small"
      >
        <Radio.Button value="preset">常用参数</Radio.Button>
        <Radio.Button value="custom">自定义</Radio.Button>
      </Radio.Group>

      {mode === 'preset' ? (
        <div style={{ display: 'flex', flexDirection: 'column', gap: Spacing.XS, flex: 1 }}>
          {presets.map((p) => (
            <div key={p.key} style={settingsRowStyle}>
              <Checkbox
                checked={!!checked[p.key]}
                onChange={(e) => setChecked((c) => ({ ...c, [p.key]: e.target.checked }))}
              />
              <span style={settingsLabelStyle}>{p.label} ({p.key})：{p.desc}</span>
              {p.type === 'enum' ? (
                <Select
                  value={values[p.key] || undefined}
                  onChange={(v) => setValues((s) => ({ ...s, [p.key]: v }))}
                  style={{ width: settingsControl.selectWidth }}
                  options={(p.options ?? []).map((o) => ({ value: o, label: o }))}
                />
              ) : (
                <Input
                  value={values[p.key] ?? ''}
                  onChange={(e) => setValues((s) => ({ ...s, [p.key]: e.target.value }))}
                  style={inputStyle}
                />
              )}
              <span style={{ color: Colors.TEXT.SECONDARY }}>默认 {p.default !== null ? String(p.default) : '空'}</span>
            </div>
          ))}
        </div>
      ) : (
        <Input
          placeholder="参数名"
          value={customKey}
          onChange={(e) => setCustomKey(e.target.value)}
          style={{ width: settingsControl.inputWidth }}
        />
      )}


      {mode === 'custom' && (
        <>
          <Select
            value={customType}
            onChange={setCustomType}
            style={{ width: settingsControl.selectWidth }}
            options={[
              { value: 'number', label: '数字' },
              { value: 'string', label: '文本' },
              { value: 'boolean', label: '布尔' },
            ]}
          />
          <Input placeholder="值" value={value} onChange={(e) => setValue(e.target.value)} style={inputStyle} />
        </>
      )}

      <Button type="primary" size="small" onClick={handleAdd} disabled={!canConfirm}>确认</Button>
      <Button size="small" onClick={onCancel}>取消</Button>
    </div>
  );
};
```

#### 4.4 `ModelParams.tsx` 渲染改并集（v1.1 P0-1：否则新行不可见）

**位置**：行 46

**diff**：
```diff
-      {Object.keys(defaults).map((key) => {
+      {[...new Set([...Object.keys(defaults), ...Object.keys(params)])].map((key) => {
```

**原因**：`addParam` 为保脏故意不注入 `defaults`；不改遍历，新 key 点确认后不渲染。改并集后新行立即出现 + DirtyDot 正常（`dirty` 本地重算）；无 addParam 操作时并集 == defaults 键集，行为零变化。

---

## 五、改动影响范围

| 影响项 | 分析 |
|--------|------|
| **后端** | 零改动。`update_model()` 的 `old_params.update(dp)` 已支持任意新 key |
| **ModelParams.tsx** | 1 行。遍历改 params⊔defaults 并集（新行可见性） |
| **modelUtils.ts** | 零改动。`isDirty` 对新 key（defaults 中无）正确判脏 |
| **saveModelGroup** | 有新键时捎带全量 ranges/paramOptions 落 model_meta，无新键行为零变化；回显控件形态不退化 |
| **config.yaml** | 零手动改动。保存时自动写入 `model_params.{model}.{key}` |
| **E2E 测试** | fre2e_11 现有 case 不受影响（不触发 addParam）；需新增 2-3 个 case 覆盖添加参数流程 |
| **兼容性** | 向后兼容。无 addParam 操作时行为完全不变 |

---

## 六、风险点

| 风险 | 等级 | 缓解措施 |
|------|------|---------|
| 用户添加参数后未保存就切走 | 低 | `ensureModelSaved` 切前自动保存（失败才阻止切换，BUG-D），新参数随之落盘，不丢失 |
| 自定义 key 拼写错误导致 LLM 静默忽略 | 中 | 预定义表覆盖 8 个常用参数；自定义布尔只认 true/false，其余禁确认 |
| 添加重复 key | 低 | addParam 检查 `key in params` → 弹 warning |
| 新添加 key 与 env 接管键重名 | 低 | addParam 判重只查 `key in params` 未查 envOverride：注入后该行被 env 禁用 + 保存被拒；缓解：确认前提示用户改名，重名行以 env 禁用态显示 |
| 空值/未选确认 | 低 | 确认按钮有效性门禁（空值、枚举未选一律 disabled，不落 null/空串） |
| config.yaml 中 model_params 块膨胀 | 低 | 只保存用户实际添加的参数，不自动注入 |

---

## 七、模型能力（多媒体支持）编辑设计

> v1.7 并入（原独立[66]文档收编）。与§二「+添加参数」同居②参数区，但走**不同通道**：§二管 `model_params`（default_params），§七管 `model_meta.capabilities`——互不重叠，同次保存可并存。

### 7.1 问题说明

**现象**：设置页无法编辑模型的多模态能力声明。Muse 支持 文本/图片/视频/音频/PDF，有的模型只支持文本+图片——目前只能**添加模型时**随表单带入（`addModel` 已透传），建完想改只能手编 config.yaml。

**现状链路核实（四层，已读源码）**：

| 层 | 位置 | 现状 |
|----|------|------|
| 存储 | `ai.{provider}.model_meta.{model}.capabilities` | 读 `model_service.py:138`（缺省 `[]`）；写 `:240-242`（`fields.capabilities is not None` 即落）；白名单 `:235` **已放行** |
| API | `model.api.ts` | `ModelEntry.capabilities: string[]`（:23）；`updateModel` Pick **已含** `'capabilities'`（:96）——**前端 API 层零改动** |
| 展示 | `CurrentModelRefCard:60-148` / `ModelSelector:66` | 只读 tags，`capabilities.length===0` 隐藏整行 |
| 消费 | chat/LLM 运行时 | **零消费**——纯声明/展示，不门控行为 |

**缺口**：`ModelState` 无 `capabilities` 字段；`saveModelGroup` 只送 `default_params` → 无编辑、无保存、无脏态。

**语义约定**：空 `[]` = 不声明 = 不限制（文本是隐含默认，不勾 `text` 照样发文本）；字段是模型元信息非采样参数。

### 7.2 策略与 UI 形式

**策略**：固定 5 枚举 Checkbox 多选（选 A）——capabilities 是展示声明非采样参数，运行时零消费，词表固定；自由输入会拼错、展示层不可控，违背 KISS。

**UI**：②参数区 `ModelParams` 下方新增一行，复用 `settingsRowStyle`/`settingsLabelStyle` 令牌（[58]DRY，禁止第四套行样式）：

```
  ...现有参数行...
  模型能力  [✓]文本  [✓]图片  [ ]视频  [ ]音频  [ ]PDF   ●(DirtyDot)
```

- 勾选即改 `state.model.capabilities` → 与 baseline 深比较 → DirtyDot / SaveBar / beforeunload 复用现有闸口
- **不占用**标题行「+ 添加参数」按钮（§二管 model_params，本节管 model_meta，两入口并存）

**能力枚举表**（v1.10：收口 `modelUtils.ts` 单源导出，`setCapabilities` 合并（useSettings）与能力行渲染（SettingsPage）共用，杜绝两处词表漂移——DRY+复用优先）：

```typescript
// utils/modelUtils.ts 文件尾追加（定义处，非 SettingsPage 本地）
export const CAPABILITY_OPTIONS = [
  { label: '文本', value: 'text' },
  { label: '图片', value: 'image' },
  { label: '视频', value: 'video' },
  { label: '音频', value: 'audio' },
  { label: 'PDF',  value: 'pdf'  },
];
```

**未知值策略（Q1 定案：保留 + disabled 展示 + 合并提交）**：
1. 四通道回填原样进 state（含未知值如 yaml 手写的 `vision`），baseline 同拷贝；
2. `Checkbox.Group` 只渲染 5 已知枚举；未知值渲染为右侧灰色 `Tag`（不可关、`cursor:not-allowed`）——看得见、本页不提供增删；
3. **合并点收口在 `setCapabilities` 内**（v1.9 统一口径）：antd `Checkbox.Group` 的 onChange 只返回已渲染 5 枚举的勾选值（会丢未知值），故 `setCapabilities(uiValues)` = `uiValues(已知勾选) ∪ state/baseline 中未知原值` 后再入 state——**state 恒含未知值**，提交直接 `body.capabilities = state.model.capabilities`，无二次合并（§7.4 diff 同口径）；
4. 未知值在 baseline/state 同源，不参与误报。

**设计理由**：capabilities 是用户可能手编 yaml 的开放词表，UI 截断=数据丢失（违背"功能只能正确/增强，杜绝退化"）；放开自由编辑又违背选 A 的 KISS——折中=已知可编辑、未知只读透传。

### 7.3 关联逻辑和参数冲突性分析

**7.3.1 数据流完整性**：

```
setCapabilities(caps)
  → state.model.capabilities = caps          ← 编辑副本
  → capabilitiesBaseline 不动                 ← 保脏（§3.2 同套路：不注入基线）
  → isDirty = params脏 OR caps≠baseline       ← 联合置 isDirty，闸口全复用
       ↓ saveModelGroup()
  body = {}                                   ← ★P0 见 7.3.2
  changed 非空 → body.default_params = changed
  capsChanged → body.capabilities = caps（未知值已合并）
       ↓ PUT /models/{p}/{m}
  后端 :235 白名单放行 → :240-242 落 model_meta（[] 也合法=清空能力）
  default_params 缺省(None) → 不碰 model_params 块
       ↓ GET /models 四通道回填 state+baseline
```

**7.3.2 ★P0：空 `default_params` = 显式清空**。`model_service.py:255-263`：`if dp: 合并 else: node.model_params[model] = {}`——仅能力变更若送 `default_params: {}`，该模型采样参数被整块清空。**强制约束：`body.default_params` 仅在 `changed` 非空时赋值，否则不带该字段**（undefined 不进 JSON → `dp=None` → 不碰参数块）。`capabilities: []` 不受影响（`:241` 判 `is not None`，空列表合法）。

**7.3.3 脏态**：baseline 不同步注入（防假保存，§3.2 同款决策）；深比较收口为 `isCapsDirty(caps, baseline)`（v1.10 DRY+复用优先：modelUtils 新导出，内部复用既有 `sameValue`——Object.is 快路径+JSON 深比，不再 5 处散写 `JSON.stringify` 不等式；Checkbox 勾选序=枚举序，基线同源，误报≈0，回填乱序可先 sort）；保存成功后 `capabilitiesBaseline = [...caps]` 新基线。未存切走由 `ensureModelSaved` → 扩展后 `saveModelGroup` 自动覆盖（BUG-D 同门）。

**7.3.4 四通道回填**（与[62]P3 paramOptions 四通道同构，复制不重写）：

| 通道 | 锚点 | 补法 |
|------|------|------|
| `load()` | `:304 paramOptions` 后 | `capabilities: [...(current?.capabilities ?? [])]` + baseline 同 |
| `selectProvider` | `:697` 后 | `first?.capabilities ?? []` |
| `selectModel` | `:741` 后 | `entry.capabilities ?? []` |
| `refreshModels(select)` | `:829` 后 | `m.capabilities ?? []` |

`initialModel()` 补 `capabilities: []` + `capabilitiesBaseline: []`。

**7.3.5 env 接管**：`_raise_if_env_takeover` 拒整次 update → checkbox 禁用口径 = `providerConfig[selectedProvider]?.env === true`（与参数区 S5 同步；不用 `envOverride`——其 keys 来自 default_params，无参数模型会是 `{}` 判不出）。

**7.3.6 重置语义（Q2 定案：不连带）**：`resetParams` 只恢复 `params ← defaults`，不动 capabilities。①确认弹窗文案「将恢复该模型全部参数为默认值，当前修改将丢失。」（`SettingsPage:270` 实文）语义不含元信息，连带=隐藏行为；②yaml 基线一旦被重置清空，恢复成本远高于本地即有的 params defaults；③能力行自带 DirtyDot+保存，单独编辑单独撤销，闭环不需重置兜底。

**7.3.7 脏计数（Q3 定案：计入 +1）**：`dirtyCount`（`useSettings:405`）追加 `isCapsDirty(caps, baseline) ? 1 : 0`（v1.10：走统一 helper，见 7.3.3）。①isDirty 已联合判定，计数不跟上会出现「模型(0 项)却可保存」显示失真；②计 1 项而非按勾选 diff 数——能力行是单一逻辑字段；③`isGroupDirty`/beforeunload/切 Tab 闸已读 isDirty 天然覆盖，计数只影响文案。

**7.3.8 与§二「+添加参数」并存**：两通道字段互斥判断独立，同次保存 `changed` 与 `capsChanged` 同真时 body 两字段都有，后端各自分支处理，零冲突。

**7.3.9 配置文件落盘样子**（北京老陈 v1.8 要求补）

**路径**：`get_config_path()`（`app/config.py:276`）尊重 `OMNIAGENT_CONFIG_PATH` 环境变量，否则 `{代码库根}/config/config.yaml`——本仓即 `F:\OmniAgentAs-repair\config\config.yaml`（[59]B-3 单源化：设置页写的就是运行时读的同一文件）。

**写链路**（`update_model:269 → merge_nested_patch:513 → _merge_region_core:520`，capabilities 与其他配置项同一链路无特殊分支）：

```
SoftFileLock(config.yaml.lock) 持锁（并发写互斥，后写者基于最新文件重读合并）
→ _backup_config 备份(.backup) → 读最新文件 → _set_nested_path 写叶
→ _validate_config_integrity 完整性校验 → _order_for_dump 保序
→ atomic_write(yaml.safe_dump allow_unicode indent=2) → 逐叶重读验证
→ reload_ai_config() 热重载
任一步失败 → 回滚最近备份 → 抛出（前端收到错误，勾选态保留可重试）
```

**校验与 API 层容忍**：
- `_validate_config_integrity:227-276` 只检 `ai.model_ref`/选中 provider 的 `api_base`/`api_key`/`models`——**不检 capabilities 的存在性、键名、值域**；`capabilities: []` 与任意 `string[]` 均放行（实证：`config.yaml.backup.5:21-25` 的 `capabilities: []` 均通过校验落盘）。
- API 层 `model_dump(exclude_none=True)`（`model_routes.py:96`）：`capabilities: []` 非 None → **保留提交**（全不勾不会被丢成"不更新"）；字段缺省 → None → 剔除 → 后端 `fields.get("capabilities") is not None` 为 False → **不碰该叶**。
- 读侧 `_models_of:138`：`meta.get("capabilities") or []`——键缺失、`[]`、`null` 三种都归一 `[]`。

**五种场景 yaml 实样**（PyYAML `safe_dump` 真实格式，缩进 2，块序列与键同缩进）：

| 场景 | 触发 | config.yaml 结果 |
|------|------|------------------|
| A 保存非空（勾文本+图片） | `body.capabilities=["text","image"]` | `model_meta.{model}` 下新增 `capabilities` 叶（样例见下） |
| B 保存空（全部取消勾） | `body.capabilities=[]` | `capabilities: []`（flow 空列表，实证 backup.5:25） |
| C 从未编辑（本仓现状） | 无该键 | 当前 `config/config.yaml` **无 `model_meta` 块**（2026-09-23 实读）→ 读侧归一 `[]` → UI 全不勾，与"无声明=不限制"语义等价 |
| D yaml 手写未知值 | Q1 合并提交 | `- vision` 原样保留在列表尾部，不丢 |
| E 仅能力变更（参数无变） | body 不带 `default_params` | `model_params.{model}` 块**字节不动**（P0 §7.3.2），仅 `model_meta` 叶变 |

**样例 A（保存非空后，以 opencodeZen/big-pickle 为例）**：

```yaml
ai:
  opencodeZen:
    models:
    - big-pickle
    model_params:
      big-pickle:
        context_limit: 10000
    model_meta:
      big-pickle:
        capabilities:
        - text
        - image
```

**样例 B（全不勾保存后，真实格式取自 config.yaml.backup.5:19-25）**：

```yaml
    model_params:
      glm-4.7-flash: {}
    model_meta:
      glm-4.7-flash:
        label: 重复模型
        range: {}
        capabilities: []
```

**7.3.10 回显与编辑通路归一验证（来回闭环）**（北京老陈 v1.8 要求补）

**通路图**：

```
[编辑] UI勾选 → state.model.capabilities（编辑副本；baseline 不动 → 脏）
   ↓
[保存] saveModelGroup：
   S11 mtime 校验（同 config.yaml；外部已改 → load+提示+中止，防覆盖）
   → changed(参数) 与 capsChanged(能力) → body 双通道
     （P0：参数无变不带 default_params；Q1：未知值合并进 capabilities）
   → PUT /models/{p}/{m} → update_model:235 白名单 → :240-242 叶写
   → merge_nested_patch：锁+备份+校验+原子写+逐叶验证+reload（§7.3.9）
   ↓
[保存成功·同会话回显]
   patchModel：capabilitiesBaseline=[...caps] 新基线、defaults 同步、isDirty=false、syncMtime
   ★providers 列表同步 patch 该模型 capabilities（下述必修②）
   → Checkbox 勾选保持 / DirtyDot 灭 / SaveBar 模型(0 项)
   → CurrentModelRefCard 卡片 tags、ModelSelector 标签同屏更新
   ↓
[保存后·再读回显（F5 / 切模型 / refreshModels）]
   GET /models → _models_of:138 读 yaml → 四通道回填 capabilities+baseline
   → 勾选=yaml、卡片=yaml、不脏
   ↓
[再编辑] 勾选改副本 → baseline 不动 → 脏 → 回到[保存]，闭环
```

**八链路验证表**：

| # | 链路 | 行为 | 回显结果 | 结论 |
|---|------|------|---------|------|
| 1 | 勾选/去勾 | 只动 state.caps，baseline 不动 | 即时勾选态 + DirtyDot | 正常 |
| 2 | 保存成功（非空） | 叶写落盘 + baseline 同步 + providers patch | 同会话卡片/勾选/计数三处一致 | 正常 |
| 3 | 保存成功（全不勾） | `capabilities: []` 落盘 | 刷新后全不勾；卡片 tags 整行隐藏（length=0） | 正常 |
| 4 | 保存后 F5 冷读 | load 四通道回填 | 勾选=yaml、卡片=yaml、不脏 | 正常 |
| 5 | 仅能力脏就切模型 | ensureModelSaved 扩展（必修①） | 先落盘再切，源模型能力不丢 | **需扩展** |
| 6 | 未保存关页/切 Tab | isDirty 已 OR caps → beforeunload/确认闸复用 | 提示保留修改 | 正常（复用） |
| 7 | 重置参数 | resetParams 不动 caps（Q2 定案） | 勾选保持 | 正常 |
| 8 | 外部手改 yaml | S11 保存前 mtime 不符 → load+提示+中止 | 重新加载防静默覆盖 | 正常（复用） |

**两处实施必修（已并入 §7.4 清单）**：

① **ensureModelSaved 扩展**（`useSettings:620-626` 现只查参数 dirtyMap）——不扩展则"能力改了没存就切模型/Provider"静默丢失（BUG-D 同类）：

```ts
const dirtyMap = isDirty(state.model.params, state.model.defaults, state.model.envOverride);
if (
  !Object.values(dirtyMap).some(Boolean) &&
  !isCapsDirty(state.model.capabilities, state.model.capabilitiesBaseline) // v1.10：统一 helper
) return true;
// 后续走 saveModelGroup（已扩展双通道），失败阻止切换——文案不变
```

（`selectProvider:680` / `selectModel:718` / `refreshModels:806` 三处都经 ensureModelSaved，扩展一处三链路全覆盖。）

② **保存成功后 providers 同步 patch**——否则参数区勾选已改、通用 Tab 卡片 tags 仍旧值直到 F5，同屏两处不同源=显示失真：

```ts
patchModel({
  defaults: { ...state.model.params },
  capabilitiesBaseline: [...state.model.capabilities],
  providers: state.model.providers.map((p) =>
    p.name !== state.model.selectedProvider ? p : {
      ...p,
      models: p.models.map((m) =>
        m.name !== state.model.selectedModel ? m
          : { ...m, capabilities: [...state.model.capabilities] })
    }
  ),
  isDirty: false,
});
```

**与只读展示同源证明**：卡片 `CurrentModelRefCard:61-64`、`ModelSelector:66` 与参数区 Checkbox 的值同源自 `GET /models.capabilities`（即 yaml `model_meta`）——同一 state 字段、同一次回读；保存经②patch 后三处同源，F5/冷读后端亦同源（`_models_of:138`）。运行时 LLM 对 capabilities 零消费，任何回显差异都不影响会话行为。

### 7.4 实施代码 diff（待批准后落码）

**改动清单**：

| # | 文件 | 位置 | 改动 | 量 |
|---|------|------|------|-----|
| 1 | `types.ts` | ModelState `paramOptions` 后 | +`capabilities`/`capabilitiesBaseline` | +2 |
| 2 | `useSettings.ts` | initialModel | +2 字段 | +2 |
| 3 | `useSettings.ts` | load/selectProvider/selectModel/refreshModels | 四通道回填+baseline | +8 |
| 4 | `useSettings.ts` | setParam 后 | 新增 `setCapabilities`：Q1 合并（uiValues ∪ baseline 未知原值，`CAPABILITY_OPTIONS` 自 modelUtils）+ `isCapsDirty` 联合置脏 | +14 |
| 5 | `useSettings.ts` | dirtyCount/saveModelGroup/return | `isCapsDirty` 联合脏计数、body 双通道（与 §4.2.4 捎带叠加）、暴露；import 并入既有 modelUtils 行 | +12 |
| 6 | `useSettings.ts` | `ensureModelSaved:626` | ★必修①：放行条件补 `isCapsDirty`（防能力脏切换静默丢失） | +5 |
| 7 | `useSettings.ts` | saveModelGroup 成功分支 | ★必修②：patchModel 补 providers 内该模型 capabilities 同步（卡片同屏） | +8 |
| 8 | `SettingsPage.tsx` | import + ModelParams 后 | 能力行（Checkbox 进 antd 行、DirtyDot 并 icons 行、`CAPABILITY_OPTIONS`/`isCapsDirty` 自 modelUtils——禁止重复 import，§4.3.0 同步） | +24 |
| 9 | `modelUtils.ts` | 文件尾 | +`CAPABILITY_OPTIONS` 单源词表 + `isCapsDirty`（复用既有 `sameValue`，DRY/复用优先） | +6 |

**总计 ~81 行 / 4 前端文件（types.ts / useSettings.ts / SettingsPage.tsx / modelUtils.ts）/ 0 API 层 / 0 后端**（types.ts 两处字段已预写，落码时对齐本节为准）。

**modelUtils.ts 落码（清单 #9 对应，复用同文件既有 `sameValue`——v1.10 DRY/复用优先收口）**：

```ts
// 编辑历史: 2026-09-23 小欧 - 新增 isCapsDirty + CAPABILITY_OPTIONS（[65]§七，复用既有 sameValue 与 §7.2 单源词表）
export const isCapsDirty = (caps: string[], baseline: string[]): boolean =>
  !sameValue(caps, baseline);
// CAPABILITY_OPTIONS 完整定义见 §7.2（同文件导出，useSettings 合并与 SettingsPage 渲染共用）
```

**关键 diff（saveModelGroup，P0 落点；★v1.9：与 §4.2.4 捎带叠加的完整形态，range/param_options 逻辑原样保留不可丢）**：

```diff
   const changed = Object.fromEntries(...);
+  const capsChanged = isCapsDirty(state.model.capabilities, state.model.capabilitiesBaseline); // v1.10：modelUtils 统一 helper（复用 sameValue），import 并入既有行
-  if (!Object.keys(changed).length) return { ok: true as const };
+  if (!Object.keys(changed).length && !capsChanged)
+    return { ok: true as const };
+  // ★与 §4.2.4 叠加（必须保留，否则 v1.3 形态持久化退化）：有新键才捎带全量 range/param_options
+  const prevDefaults = state.model.defaults;
+  const newKeys = Object.keys(changed).filter((k) => !(k in prevDefaults));
+  const body: {
+    default_params?: Record<string, unknown>;
+    range?: Record<string, { min: number; max: number }>;
+    param_options?: Record<string, string[]>;
+    capabilities?: string[];
+  } = {};
+  if (Object.keys(changed).length) {
+    body.default_params = changed;          // ★P0：仅参数有变才带，空 {} 会被后端当"显式清空"参数块
+    if (newKeys.length) {                   // §4.2.4 原逻辑原样保留
+      body.range = { ...state.model.ranges };
+      body.param_options = { ...state.model.paramOptions };
+    }
+  }
+  if (capsChanged) body.capabilities = state.model.capabilities; // setCapabilities 已合并未知值（Q1），无需二次合并
   const r = await modelApi.updateModel(
     state.model.selectedProvider, state.model.selectedModel,
-    { default_params: changed }
+    body
   );
   ...
-  patchModel({ defaults: { ...state.model.params }, isDirty: false });
+  patchModel({
+    defaults: { ...state.model.params },
+    capabilitiesBaseline: [...state.model.capabilities],
+    // ★必修②（§7.3.10）：providers 内该模型 capabilities 同步，卡片/参数区同屏同源
+    providers: state.model.providers.map((p) =>
+      p.name !== state.model.selectedProvider ? p : {
+        ...p,
+        models: p.models.map((m) =>
+          m.name !== state.model.selectedModel ? m
+            : { ...m, capabilities: [...state.model.capabilities] })
+      }
+    ),
+    isDirty: false,
+  });
```

**setCapabilities 完整体（★v1.10 补：Q1 合并收口点 + 联合置脏，此前只有描述无代码）**：

```ts
// useSettings.ts：setParam 后新增
const setCapabilities = useCallback((uiValues: string[]) => {
  setState((s) => {
    // Q1（v1.9 统一口径）：onChange 只含已渲染 5 枚举（CAPABILITY_OPTIONS，modelUtils 单源），
    // 合并未知原值 → state 恒含未知值 → 提交直接送，无二次合并
    const known = new Set(CAPABILITY_OPTIONS.map((o) => o.value));
    const unknown = s.model.capabilities.filter((v) => !known.has(v));
    const next = [...uiValues, ...unknown];
    const paramsDirty = Object.values(
      isDirty(s.model.params, s.model.defaults, s.model.envOverride)
    ).some(Boolean);
    return {
      ...s,
      model: {
        ...s.model,
        capabilities: next,
        isDirty: paramsDirty || isCapsDirty(next, s.model.capabilitiesBaseline), // 联合置脏（7.3.1）
      },
    };
  });
}, []);
```

**ensureModelSaved 扩展（★必修①，§7.3.10）**：见 7.3.10 必修①代码块——放行条件由"仅参数脏"改为"参数脏 OR 能力脏"，`selectProvider/selectModel/refreshModels` 三链路经此全覆盖。

**能力行（SettingsPage，ModelParams 后）**：

```tsx
// 编辑历史: 2026-09-23 小欧 - 新建：模型能力多选行（[65]§七 capabilities → model_meta 通道）
// v1.10: 控件补 flex:1 包裹对齐 ModelParams:155/ProviderConfig 行结构；脏判定走 isCapsDirty
<div style={settingsRowStyle}>
  <span style={settingsLabelStyle}>模型能力</span>
  <span style={{ flex: 1 }}>  {/* 行内控件标准包裹：ModelParams:155 同款，缺则不占宽/挤压 DirtyDot */}
    <Checkbox.Group
      value={state.model.capabilities}
      disabled={state.model.providerConfig[state.model.selectedProvider]?.env === true}
      options={CAPABILITY_OPTIONS}
      onChange={(v) => s.setCapabilities(v as string[])}
    />
  </span>
  {!isCapsDirty(state.model.capabilities, state.model.capabilitiesBaseline) ? null : <DirtyDot />}
</div>
```

（import 合并到顶部：`Checkbox` 进 antd 行、`DirtyDot` 并入 icons 行、`CAPABILITY_OPTIONS`/`isCapsDirty` 进 `../utils/modelUtils` 行——全部由 §4.3.0 引入，禁止另起重复行，P0-2 同款约束。）

### 7.5 改动影响范围

| 影响项 | 分析 |
|--------|------|
| 后端 | **零改动**（白名单/读写/`is not None` 分支全就绪） |
| `model.api.ts` | **零改动**（Pick 已含 capabilities，类型可选） |
| saveModelGroup | 扩展双通道；P0=参数无变不带 default_params |
| 运行时 | 零（声明不消费，行为不变） |
| §二 添加参数 | 不受影响，两通道并存 |
| E2E | 现有 case 不触发；建议 +1（勾选→保存→刷新回显→yaml 断言 `model_meta`） |

### 7.6 风险点

| 风险 | 等级 | 缓解措施 |
|------|------|---------|
| 仅能力变更误清空采样参数 | **高** | 7.3.2 强制：changed 空不带 default_params 字段 |
| yaml 手写未知能力值被截断 | 中 | Q1 已定案（7.2）：保留+disabled 展示+合并提交 |
| env 接管 provider 保存被拒 | 中 | 7.3.5 checkbox 按 providerConfig.env 禁用 |
| 回填乱序导致深比较误报脏 | 低 | 回填/比较前 sort，或同源序一致 |
| 重置语义争议 | 低 | Q2 已定案（7.3.6）：重置不连带能力 |

### 7.7 设计决策定案表

| # | 问题 | 定案 | 位置 |
|---|------|------|------|
| Q1 | 未知能力值（非 5 枚举）策略 | **A：保留 + disabled 展示 + 合并提交** | §7.2 |
| Q2 | 「重置为默认」是否连带恢复能力 | **A：不连带** | §7.3.6 |
| Q3 | 能力变更是否计入 SaveBar 脏计数 | **A：计入 +1** | §7.3.7 |

---

**文档结束**
