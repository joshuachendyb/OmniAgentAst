# [59] 问题分析报告 - 设置读取链路 31 个真实 bug（三遍核实 + 修改方案 + 未落码 diff）

创建时间：2026-09-21 19:25:31
编写人：小欧

版本历史：
- v1.0（2026-09-21 19:25:31 - 小欧 - 首次成文）

---

## 一、范围与核实方法

**范围**：后端 `services/settings/settings_service.py`、`services/model/config_service.py`、`services/model/config_helpers.py`、`services/model/model_service.py`、`api/v1/config_routes.py`、`api/v1/settings_routes.py`、`app/config.py`、`app/main.py`；前端 `features/settings2/*`（hooks/components）、`services/api/config.api.ts`、`services/error/handler.ts` —— 全部为"读取设置/配置"链路。

**核实三遍（纯代码，均以最新本地代码为准，v4.20 已校准）**：
- 第 1 遍：源码证据定位（文件:行号 + 原文），每条给出精确位置。
- 第 2 遍：调用链与触发可达性复核（谁能到达、什么状态触发）。
- 第 3 遍：反向证伪（守卫/初始化/契约兜底是否已挡住），挺过才保留。

**撤回与修正（诚实披露，不凑数）**：
1. 初筛候选 `GET /config/full` 的 `model_ref` 幽灵 provider —— **撤回**：v4.20 已修，`is_provider_metadata_field`（config_helpers.py:133-136）已含 `model_ref`，循环已跳过。
2. 初筛候选 `get_setting` 绕过掩码/版本特殊逻辑 —— **撤回**：全仓库 grep 无 `get_setting(` 调用方，为死代码，无真实触发场景。
3. 初筛候选"关于区文件全文为旧快照不刷新" —— **撤回**：`openFile` 每次打开都重新请求后端，非快照。
4. 核实过程新发现 2 条补入：后端 `get_model_list` 忽略 dict 老格式 `models`（B-15）；前端 env 接管键保存后仍清脏态造成"假保存"（F-16）。

**最终保底清单：后端 15 条 + 前端 16 条 = 31 条**，全部为可触发真实 bug，无凑数。每条均给修改方案与可执行 diff（未落码，待分批批准实施）。

---

## 二、后端 15 条

### B-1 ［高］`read_yaml_config` 对坏 YAML 零防护，任意读接口直接 500
- 位置：config_helpers.py:109-114
- 现象：`yaml.load` 遇语法错误抛 `YAMLError` 未捕获。
- 触发：`config/config.yaml` 被手改坏（缩进/引号错误）后，`GET /settings` / `GET /config` / 模型读全部 500（`handle_config_errors` 统一转 500）。
- 代码：`return yaml.load(f, Loader=_make_safe_loader()) or {}`
- 修改方案（KISS/SRP/DIRECT）：读入口统一捕获 `YAMLError` 并显式报错，禁止静默吞（防规则），坏文件必须被看见。
```diff
  def read_yaml_config(config_path: Path) -> dict:
      """读取 YAML 配置文件,文件不存在时返回空 dict"""
      if not config_path.exists():
          return {}
-     with open(config_path, 'r', encoding='utf-8') as f:
-         return yaml.load(f, Loader=_make_safe_loader()) or {}
+     with open(config_path, 'r', encoding='utf-8') as f:
+         text = f.read()
+     try:
+         data = yaml.load(text, Loader=_make_safe_loader())
+     except yaml.YAMLError as e:
+         logger.error(f"配置文件解析失败: {config_path}: {e}")
+         raise HTTPException(status_code=500, detail=f"配置文件解析失败: {e}")
+     return data if isinstance(data, dict) else {}
```

### B-2 ［中］顶层非 dict 一半崩溃一半静默全默认
- 位置：B-1 同源（return 非 dict）；爆炸点 model_service.py:47-50 `_raw_ai` 对 `str` 顶层调 `raw.get` → `AttributeError`→500；settings 侧 `_get_dotted` 安全但全量回默认值。
- 现象：配置被写成字符串/列表时，`GET /models` 崩、`GET /settings` 静默全默认，行为分裂。
- 修改方案（DRY）：B-1 的 `isinstance(dict)` 归一即根治本条目，两现象一根因，一处修复。

### B-3 ［高］`OMNIAGENT_CONFIG_PATH` 造成配置双源分裂
- 位置：config.py:265-267 顶层 `get_config_path`（**无** env 分支） vs config.py:85-90 `Config._get_config_path`（读 env）。
- 现象：`Config` 实例（业务运行）读 env 指定文件；设置页/模型服务（config_helpers.get_config_path，config_helpers.py:105-107 import 顶层函数）读代码库根 `config/config.yaml`。两处文件内容不一致时，**设置页改的不是实际运行中的配置**。
- 代码：`env_path = os.getenv('OMNIAGENT_CONFIG_PATH') ... return Path(env_path)`（仅类方法内）
- 修改方案（DRY/单源）：env 判定收敛到顶层函数，类方法复用同一实现。
```diff
  def get_config_path(filename: str = "config.yaml") -> str:
-     """统一配置路径获取 — 代码库根下 config 目录(非项目根) — 小欧 2026-08-10 ④内部改调"""
-     return str(_get_code_root() / "config" / filename)
+     """统一配置路径获取 — 尊重 OMNIAGENT_CONFIG_PATH 环境变量(与 Config 实例一致)，否则代码库根 config 目录"""
+     env_path = os.getenv('OMNIAGENT_CONFIG_PATH')
+     if env_path:
+         return str(Path(env_path))
+     return str(_get_code_root() / "config" / filename)
```
提示：`Config._get_config_path`（config.py:85-90）建议同步改调此函数消除重复（可选重构，需回归验证，暂不列入本轮）。

### B-4 ［中］掩码契约分叉 + 短密钥显示不一致
- 位置：config_service.py:138-144 `_mask_api_key`（返 `"abc***xy"`） vs config_helpers.py:532+ `mask_secret_value`（返 `{configured, suffix}`）。
- 现象：同一密钥两个接口两套契约：`/config/full` 显字符串掩码，`/settings`、`/models` 显 `{configured,suffix}`（suffix=末4）。≤6 位密钥前者全 `*`、后者 suffix 空——前端 `slice(-4)` 对字符串与 dict 两套解析仍可显示成两样。
- 修改方案（DRY/复用优先）：`_mask_api_key` 内部复用公用 `mask_secret_value`，输出规整为 `"****"+末尾4`，与前端 `slice(-4)` 一致。
```diff
  def _mask_api_key(api_key: str) -> str:
-     """掩码API Key: 非空时保留前3后2, 中间以*替换, 短于等于6位全掩码 — 小欧 2026-08-13 (#7)"""
-     if not api_key:
-         return ""
-     if len(api_key) <= 6:
-         return "*" * len(api_key)
-     return api_key[:3] + "*" * (len(api_key) - 5) + api_key[-2:]
+     """掩码API Key — 2026-09-21 小欧 对齐 mask_secret_value 契约（末4位后缀，与前端 slice(-4) 一致）"""
+     m = mask_secret_value(api_key)
+     return "****" + (m.get("suffix") or "") if m.get("configured") else ""
```
（需确认 `/config/full` 消费方前端对 `"****xxxx"` 的相容性：前端 SettingRow 双态解析兼容字符串，低风险。）

### B-5 ［高］provider 值为非 dict 时读取崩溃
- 位置：config_service.py:150-155 `get_system_config_data`；同源 get_model_list:212-214、get_full_config:245-247 已有守卫。
- 现象：`config.yaml` 写 `ai: { myprov: "oops" }` 且 `ai.model_ref.provider=myprov` → `provider_config.get('api_key')` 对 `str` 调 `.get` → `AttributeError` → 500。
- 触发：手写/迁移遗留配置（v4.20 前扁平模型键），或模型名与 provider 键撞名。
- 修改方案（DRY/复用优先）：新增且复用内部守卫 helper，三处统一。
```diff
+def _provider_conf(ai_config: dict, provider: str) -> dict:
+    """取 provider 配置；非 dict 一律归空，防畸形 YAML 崩溃（同 get_model_list 既有守卫语义）— 2026-09-21 小欧"""
+    p = ai_config.get(provider)
+    return p if isinstance(p, dict) else {}
+
  def get_system_config_data() -> dict:
      ...
      ai_config = config.get('ai', {})
-     provider_config = ai_config.get(resolved_model.provider, {})
+     provider_config = _provider_conf(ai_config, resolved_model.provider)
```
（get_model_list / get_full_config 两处同步换用 `_provider_conf`。）

### B-6 ［高］数字/非字符串 api_key 崩溃
- 位置：config_service.py:154-155、248。
- 现象：`api_key: 12345`（YAML 数字）时 `provider_config.get('api_key') or ''` 返回 `12345`（int），`api_key.strip()` → `AttributeError` 500；`/config/full` 的 `_mask_api_key(12345)` → `len(int)` 同样崩。
- `or ''` 只挡 falsy，数字真值仍穿透。
- 修改方案（KISS）：接层统一字符串化。
```diff
-    api_key = provider_config.get('api_key') or ''
-    api_key_configured = bool(api_key and api_key.strip() != '')
+    api_key = str(provider_config.get('api_key') or '')
+    api_key_configured = bool(api_key.strip() != '')
```
（get_full_config:248 同步 `api_key = str(provider_data.get('api_key') or '')`。）

### B-7 ［中］security 非 dict 透传 → Pydantic ValidationError → 500
- 位置：config_service.py:158-166（只挡 `falsy`）＋ config_routes.py:75-76（`isinstance` 守卫存在但 falsy 才回退）。
- 现象：`security: "x"`（字符串非空）→ `isinstance` 为假，原样塞入 `ConfigResponse(security="x")` → ValidationError → 500。
- 修改方案（KISS/SRP）：service 层规范化——非 dict 一律回默认安全块（默认块提升为模块常量，DRY）。
```diff
+DEFAULT_SECURITY = {
+    "enabled": False, "confirmDangerousOps": True,
+    "auto_confirm_delay": 10, "hitl_timeout": 120,
+}
+
  def get_system_config_data() -> dict:
      ...
      security_config = config.get('security', {})
-     if not security_config:
-         security_config = { ... 默认 ... }
+     if not isinstance(security_config, dict) or not security_config:
+         security_config = dict(DEFAULT_SECURITY)
```

### B-8 ［中］未知分组 ValueError → 500（应为 4xx）
- 位置：settings_service.py:106-108 `get_group`。
- 现象：`GET /settings?group=nosuch` → `ValueError` 被 `handle_config_errors` 转 500。
- 修改方案（KISS）：service 抛 `HTTPException(400)`（`handle_*` 对 `HTTPException` 原样透传；项目已有 service 层抛 HTTPException 先例）。
```diff
  def get_group(group: str) -> Dict[str, Any]:
      if group not in GROUPS:
-         raise ValueError(f"未知分组: {group}")
+         raise HTTPException(status_code=400, detail=f"未知分组: {group}")
```
（`settings_service.py` 顶部补 `from fastapi import HTTPException`。）

### B-9 ［中］`?group=` 空串静默拉全量 + 大小写不归一 500
- 位置：settings_routes.py:37-46。
- 现象：`?group=`（空串）被判 false 走全量（拼写错误静默成功）；`?group=General` → `get_group("General")` → B-8 路径 500。
- 触发：前端固定小写，但手工/第三方调用可触发。
- 修改方案（KISS，与 B-8 合并一个 diff）：service 做 `strip().lower()` 归一 + 未知 400；routes 仅空值分流。
```diff
  def get_group(group: str) -> Dict[str, Any]:
+     group = (group or "").strip().lower()
      if group not in GROUPS:
          raise HTTPException(status_code=400, detail=f"未知分组: {group}")
```
```diff
  async def get_settings(group: Optional[str] = Query(default=None)):
-     if group:
+     if group is not None and group.strip():
          one = svc.get_group(group)
          return SettingsGroupResponse(...)
      all_ = svc.get_all_groups()
```

### B-10 ［中］缺省键仍标 source="yaml"，缺失与显式无法区分
- 位置：settings_service.py:82-89。
- 现象：`config.yaml` 删除 `chat.temperature` 后仍回默认值且 source 标 `yaml`；前端 `SettingsGroup.tsx` 又 `sources[key] ?? 'yaml'` 放大——默认值被当"已落盘"。
- 修改方案（语义补全）：缺省回退时 source 标 `default`（新增第三态，前端同步）。双端 diff：
```diff
-     return raw_val if raw_val is not None else item["default"], "yaml"
+     return raw_val if raw_val is not None else item["default"], ("yaml" if raw_val is not None else "default")
```
```diff
  // frontend settings.api.ts
- export type SettingSource = 'yaml' | 'env' | 'ro';
+ export type SettingSource = 'yaml' | 'env' | 'ro' | 'default';
```
（前端 `SettingRow`/`SettingsGroup` 对 `'default'` 渲染"默认值(未落盘)"角标——建议复用 `EnvTag` 模式新建 `DefaultTag`，见前端部。）

### B-11 ［中］空白环境变量误判 env 接管（truthiness 坑）
- 位置：settings_service.py:84 `is_env`；config.py:104-105 `AI_PROVIDER` 覆写。
- 现象：`AI_PROVIDER="   "`（仅空格）时 `bool(os.environ.get(...))` 为真 → settings 判定 env 接管且 `eff_val` 取空白，config 覆写 `ai.provider` 为空白 → resolver 后续报"缺少 provider"。
- 修改方案（DRY/复用优先）：config.py 新建 `env_nonempty` 公用判定，两处调用。
```diff
  # config.py 顶部（import os 附近）
+def env_nonempty(name: str) -> bool:
+    """环境变量非空（排除纯空白）— 2026-09-21 小欧（env 接管判定的统一语义）"""
+    v = os.environ.get(name)
+    return bool(v and v.strip())
```
```diff
  # settings_service.py:84
-     is_env = bool(item.get("env_key") and os.environ.get(item["env_key"]))
+     is_env = bool(item.get("env_key") and env_nonempty(item["env_key"]))
```
```diff
  # config.py:104
-         if os.getenv('AI_PROVIDER'):
-             ai_config['provider'] = os.getenv('AI_PROVIDER')
+         if env_nonempty('AI_PROVIDER'):
+             ai_config['provider'] = os.getenv('AI_PROVIDER')
```

### B-12 ［低］`mtime` 缺文件返 0.0 + data/mtime 非原子
- 位置：config_helpers.py:524-529 `_config_mtime`；settings_service.py:92-103 先读文件再 stat。
- 现象：脚本删掉 config.yaml → `mtime=0.0` 与全默认数据同返（前端以为是 1970 版本）；并发写者夹在读取与 stat 之间 → 数据旧而 mtime 新，前端误判"已外部更新"整页刷新。
- 修改方案（DRY/复用优先）：新增函数返回"数据+mtime"原子快照（复用已有 filelock），多处改调。
```diff
+def get_config_snapshot() -> dict:
+    """读配置 + mtime 原子快照（filelock 与 merge_region_patch 同一把锁）— 2026-09-21 小欧"""
+    from filelock import FileLock
+    path = get_config_path()
+    with FileLock(str(path) + ".lock"):
+        data = read_yaml_config(path)
+        try:
+            mtime = os.path.getmtime(path)
+        except OSError:
+            mtime = 0.0
+    return {"data": data, "mtime": mtime}
```
（settings_service `get_all_groups`/`get_group`/`get_mtime` 三处改调快照水源；`_config_mtime` 保留供其它已用方。）

### B-13 ［低］`health.version` 为 import 时快照，永不跟随 version.txt
- 位置：main.py:78 `app_version = get_version()`；health.py:61 `version=request.app.version`。
- 现象：不重启进程只改 version.txt → `GET /settings.version`（`app_version()` 每次实时读）已新，`GET /health.version` 仍旧，两处长期不一致。
- 修改方案（KISS）：health 改调实时源 `settings_service.app_version()`（数十字节文件，开销可忽略）。
```diff
  # health.py
-      version=request.app.version,
+      version=svc_app_version(),
```
（health.py 顶部 import `from app.services.settings import settings_service as svc`；注意不可反向 import `app.main` 防循环，故走 settings_service。）

### B-14 ［中］`read_version_file` 透传 BOM、无大小上限
- 位置：config_service.py:420-434。
- 现象：`version.txt` 带 `\ufeff`（真实存在）时全文接口原样返回 BOM，而 `main.get_version` / `settings_service.app_version` 都已剥 BOM——同文件两个通道显示不一致；超大 version.txt 被 `f.read()` 全量装入 JSON 响应。
- 修改方案（KISS/DIRECT）：
```diff
  def read_version_file() -> dict:
      ...
      if not version_path.exists():
          raise HTTPException(status_code=404, detail=f"version 文件不存在: {version_path}")
+     size = version_path.stat().st_size
+     if size > 512 * 1024:
+         raise HTTPException(status_code=400, detail="version 文件过大，拒绝读取")
      with open(version_path, "r", encoding="utf-8") as f:
-         content = f.read()
+         content = f.read().lstrip("\ufeff")
```

### B-15 ［中］`get_model_list` 忽略 dict 老格式 `models`，列表与 `/models` 不一致
- 位置：config_service.py:215-217。
- 现象：老式/手写 YAML 的 `models` 为 dict（`{ "gpt-4.1": {...} }`）→ `isinstance(provider_models, list)` 假 → **整个 provider 静默不出现在 `/config/models`**；而 `/models`（model_service._models_of:73-87）兼容 dict 且已展示 → 两处列表不一致。
- 修改方案（KISS，对齐复用）：
```diff
-             provider_models = provider_data.get('models') or []
-             if isinstance(provider_models, list) and provider_models:
-                 for model_name in provider_models:
+             provider_models = provider_data.get('models') or []
+             if isinstance(provider_models, dict):
+                 provider_models = list(provider_models.keys())
+             if isinstance(provider_models, list) and provider_models:
+                 for model_name in provider_models:
```

---

## 三、前端 16 条

### F-1 ［中］只读项复制不 await 即弹"已复制"
- 位置：SettingRow.tsx:44-47。
- 触发：非 HTTPS/剪贴板权限拒绝时 `writeText` reject，用户仍见"已复制"。
- 修改方案（DRY）：新建 `src/utils/clipboard.ts` 公用，成功才提示。
```diff
+// 编辑历史: 2026-09-21 小强 - 新建：剪贴板工具（复制成功才提示，失败可感知）
+import type { ErrorType } from '@/services/error/handler';
+
+export async function copyTextToClipboard(
+  text: string
+): Promise<{ ok: true } | { ok: false }> {
+  try {
+    await navigator.clipboard.writeText(text);
+    return { ok: true };
+  } catch {
+    return { ok: false };
+  }
+}
```
```diff
  // SettingRow.tsx:44-47
  onClick={() => {
-      void navigator.clipboard.writeText(String(value ?? ''));
-      showSuccess('已复制');
+      void copyTextToClipboard(String(value ?? '')).then((r) => {
+        if (r.ok) showSuccess('已复制');
+        else showMessage(ErrorType.WARNING, '复制失败，请手动复制');
+      });
   }}
```

### F-2 ［中］AboutFiles"复制全文"同样不 await 即弹成功
- 位置：AboutFiles.tsx:76-79。修改同 F-1，换用同一 helper。

### F-3 ［高］外部 mtime 变更触发整页 `load()`，**静默丢弃未保存脏态**
- 位置：useSettings.ts:185-195 `checkMtime`（SettingsPage.tsx:47 切 Tab 时触发）。
- 触发：A 组已改未存，切 Tab → 后端被他处更新 → `load()` 覆盖 values/dirtyKeys → 用户改动无声消失。
- 修改方案（KISS/DIRECT，告知式不丢）。保留"后端是唯一真相源"语义，但必须显式告知：
```diff
   const checkMtime = useCallback(async () => {
     try {
       const mtime = await settingsApi.getMtime();
       if (mtime !== state.mtime) {
+        const hadLocalChanges =
+          Object.keys(state.dirtyKeys).length > 0 || state.model.isDirty;
         await load();
-        showMessage(ErrorType.INFO, '配置已更新，已刷新');
+        showMessage(
+          ErrorType.INFO,
+          hadLocalChanges
+            ? '配置已被外部更新，已重新加载（本地未保存改动已重置）'
+            : '配置已更新，已刷新'
+        );
       }
     } catch (e) {
       handleApiError(e);
     }
-  }, [state.mtime, load]);
+  }, [state.mtime, load, state.dirtyKeys, state.model.isDirty]);
```

### F-4/F-5 ［高］切换 Provider/模型不判 `ok`，校验失败仍切 UI（假成功）
- 位置：useSettings.ts:419-435 / 449-470。
- 触发：`ai.model_ref` 目标模型非法，后端返 `{ok:false, errors:[...]}`，前端仍 `patchModel` 切选且 `syncMtime`——页面看着成功、重启后失效。
- 修改方案（DRY）：抽 `applyModelRef` 成功后置位，两处复用。
```diff
+  const applyModelRef = useCallback(
+    async (payload: { provider: string; model: string }) => {
+      const r = await settingsApi.updateSettings({ 'ai.model_ref': payload });
+      if (!r.ok) {
+        (r.errors || []).forEach((m) =>
+          showMessage(ErrorType.VALIDATE_CONFIG_FAILED, m)
+        );
+        return false;
+      }
+      syncMtime(r.mtime);
+      return true;
+    },
+    [syncMtime]
+  );
```
```diff
   // selectProvider:419-435
-      const r = await settingsApi.updateSettings({
-        'ai.model_ref': { provider: name, model: first.name },
-      });
-      syncMtime(r.mtime);
+      if (!(await applyModelRef({ provider: name, model: first.name }))) return;
       patchModel({ selectedProvider: name, ... });
```
（selectModel 同样改为先 `await applyModelRef(...)` 判断再 `patchModel`。）

### F-6 ［中］切 Tab 确认只看当前组；搜索跳转绕过未保存确认
- 位置：SettingsPage.tsx:42-54。
- 触发：general 脏态、停在干净的 chat，切 appearance 直接走无确认；`jumpTo`（搜索命中）完全绕过 `requestTab`。
- 修改方案（KISS/DIRECT）：统一全局脏判定 + jumpTo 同路径。
```diff
   const requestTab = (tab: TabKey) => {
-    if (tab !== state.activeTab && s.isGroupDirty(state.activeTab)) {
+    if (tab !== state.activeTab && s.dirtyCount > 0) {
       setPendingTab(tab);
     } else {
       s.setActiveTab(tab);
       void s.checkMtime();
     }
   };
 
   const jumpTo = (tab: TabKey, key: string) => {
-    s.setActiveTab(tab);
-    s.setHighlightKey(key);
+    requestTab(tab);
+    setPendingHighlightKey(key); // pending 确认回调里补 setHighlightKey（现有确认弹窗回调内执行）
   };
```
（提示：确认弹窗回调中需在 `setActiveTab` 后补 `setHighlightKey(pendingKey)`，引用 `pendingHighlightKey` 状态。）

### F-7 ［低］模型名含 `::` 删除时截断错目标
- 位置：SettingsPage.tsx:217。
- 触发：模型名形如 `foo::bar`（现分隔符为 `::`），`split('::')` 取前两段 → m 被截断，删除错目标。
- 修改方案（KISS）：provider 名不含 `::`，取首个分隔符切分。
```diff
-      const [p, m] = target.slice('model:'.length).split('::');
+      const body = target.slice('model:'.length);
+      const sep = body.indexOf('::');
+      const p = body.slice(0, sep);
+      const m = body.slice(sep + 2);
```

### F-8 ［中］删除当前 Provider/模型后选中悬空
- 位置：SettingsPage.tsx:225-226（`await s.refreshModels()` 无 select 回落）；useSettings.refreshModels:501-535（无 select 分支不重建选中）。
- 触发：删除当前选中的 provider → provider 列表移除但 `selectedProvider` 仍指向已删名 → 下拉显示空/脏。
- 修改方案（KISS，兜底回落）：
```diff
   // refreshModels 无 select 分支（useSettings.ts ~501）
+      const survive = models.providers.find(
+        (p) => p.name === state.model.selectedProvider
+      );
+      if (!select && !survive && models.providers[0]) {
+        const p = models.providers[0];
+        const m = p.models[0];
+        const defaults = { ...(m?.default_params ?? {}) } as Record<string, unknown>;
+        patchModel({
+          selectedProvider: p.name,
+          selectedModel: m?.name ?? '',
+          params: { ...defaults },
+          defaults,
+          ranges: { ...(m?.range ?? {}) } as Record<string, { min: number; max: number }>,
+          isDirty: false,
+        });
+      }
```

### F-9 ［低］危险确认框 `onOk` 同步返回，连点双发 PUT
- 位置：SaveBar.tsx:36。
- 触发：保存慢时快速双击"确认保存" → 两条 PUT（文件锁串行但冲突/重复保存）。
- 修改方案（antd 支持 onOk 返回 Promise 自动 loading+防抖）：
```diff
  // SaveBar.tsx props 类型
-  onSaveGroup: () => void;
-  onSaveAll: () => void;
+  onSaveGroup: () => Promise<unknown>;
+  onSaveAll: () => Promise<unknown>;
  ...
-    onOk: fn,
+    onOk: fn,
+    // onOk 返回 Promise 后 antd 确认框 OK 按钮进入 loading 并禁止二次触发
```
（SettingsPage 侧 `onSaveGroup={() => s.saveGroup(...)}` 已是 async 返回 Promise，直接匹配；将 `s.saveGroup` 返回值由 `{ok}` 透传即可。）

### F-10 ［低］AboutFiles 打开瞬间闪现上一个文件内容
- 位置：AboutFiles.tsx:54-64。
- 触发：看完 config 紧接着点 version，先 `setOpen(kind)`，await 期间 pre 仍显示旧 config 全文。
- 修改方案（KISS）：
```diff
   const openFile = useCallback(async (kind: FileKind) => {
+    setContent('');
     setLoading(true);
     setOpen(kind);
```

### F-11 ［中］AboutFiles 两按钮并发，后返回覆盖前内容（标题内容错配）
- 位置：AboutFiles.tsx:54-74。
- 触发：快速连点两个查看按钮；`readConfigFile` 迟返回时覆盖已展示的 version 内容。
- 修改方案（请求序号守卫 + 按钮 loading/disabled）：
```diff
+  const reqRef = useRef(0);
   const openFile = useCallback(async (kind: FileKind) => {
+    const reqId = ++reqRef.current;
     setContent('');
     setLoading(true);
     setOpen(kind);
     try {
       if (kind === 'config') {
         const { config_content } = await configApi.readConfigFile();
-        setContent(stripBom(config_content));
+        if (reqId === reqRef.current) setContent(stripBom(config_content));
       } else {
         const { version_content } = await configApi.readVersionFile();
-        setContent(stripBom(version_content));
+        if (reqId === reqRef.current) setContent(stripBom(version_content));
       }
     } catch {
-      setOpen(null);
-      showMessage(ErrorType.LOAD_CONFIG_FAILED, '读取...全文失败');
+      if (reqId === reqRef.current) {
+        setOpen(null);
+        showMessage(ErrorType.LOAD_CONFIG_FAILED, '读取...全文失败');
+      }
     } finally {
-      setLoading(false);
+      if (reqId === reqRef.current) setLoading(false);
     }
   }, []);
```
（另两个 Button 加 `disabled={loading}`。）

### F-12 ［低］搜索英文 label 大小写敏感，漏命中
- 位置：SearchBox.tsx:19-22。
- 触发：搜 `timeout` 不中 `Timeout`。
- 修改方案（KISS）：
```diff
-        (i) => i.key.toLowerCase().includes(text) || i.label.includes(q.trim())
+        (i) =>
+          i.key.toLowerCase().includes(text) ||
+          i.label.toLowerCase().includes(text)
```

### F-13 ［中］同 `ErrorType` 30 秒去重吞掉连续失败提示
- 位置：handler.ts:656-674 + 766-768。
- 触发：保存失败后不改再点保存，30 秒内第二次失败完全无任何提示（形同成功）。
- 修改方案（dedup 仅作用于 info 级，error/warning 必现）：
```diff
-  if (!shouldShowError(errorType)) {
-    return;
-  }
+  if (config.severity === 'info' && !shouldShowError(errorType)) {
+    return;
+  }
```

### F-14 ［低］纯数字/空后空间 warning 撞空文案被静默丢
- 位置：handler.ts:770-780 + 636-642（`WARNING` 的 `message: ''`）。
- 触发：后端 warning 为 `"60000"` 之类纯数字 → `sanitizeDisplayMessage` 消成 undefined → `config.message` 为空 → 静默（用户以为成功无提示）。
- 修改方案（KISS，warning 兜底文案）：
```diff
-  const displayMessage =
-    sanitizeDisplayMessage(customMessage) || config.message;
+  const displayMessage =
+    sanitizeDisplayMessage(customMessage) ||
+    config.message ||
+    (config.severity === 'warning' ? '操作已完成，请留意详情' : '');
```

### F-15 ［低］schema 陈旧时的脏键拼出 `undefined`，PATCH 丢键成"假保存"
- 位置：useSettings.ts:270-277 `saveKeys`。
- 触发：后端已撤掉了某个 key（schema 更新），前端保留脏键仍提交；`state.values[g]?.[ck]` 为 undefined → JSON 序列化丢键 → 后端忽略 → 前端 `ok:true 保存成功`，实际未保存。
- 修改方案（KISS/防误导）：
```diff
-        const patch = Object.fromEntries(
-          keys.map((ck) => {
-            const g = Object.keys(state.schema).find((g) =>
-              state.schema[g]?.items.some((i) => i.key === ck)
-            );
-            return [ck, state.values[g ?? '']?.[ck]];
-          })
-        );
+        const patch: Record<string, unknown> = {};
+        const goneKeys: string[] = [];
+        keys.forEach((ck) => {
+          const g = Object.keys(state.schema).find((g) =>
+            state.schema[g]?.items.some((i) => i.key === ck)
+          );
+          const v = state.values[g ?? '']?.[ck];
+          if (v === undefined) goneKeys.push(ck);
+          else patch[ck] = v;
+        });
+        if (goneKeys.length === keys.length) {
+          showMessage(ErrorType.INFO, '待保存项已在服务端移除，已自动清理');
+          return { ok: false as const };
+        }
```

### F-16 ［高］env 接管键保存后仍清脏态 →"已保存"实未生效
- 位置：useSettings.ts:285-291（成功后对 keys 全量 `delete dirtyKeys`）。
- 触发：`language` 被 `OS_LANGUAGE` env 接管；用户修改语言→保存→后端 warning"被环境变量接管，已跳过"→ 前端仍清脏显示"保存成功"，重启后语言没变。
- 修改方案（KISS，env 源键保留脏角标并明确警告）：
```diff
          setState((s) => {
            const dirtyKeys = { ...s.dirtyKeys };
-            keys.forEach((k) => {
-              delete dirtyKeys[k];
-            });
+            keys.forEach((k) => {
+              const src = Object.values(s.sources)
+                .map((sm) => sm[k])
+                .find((v) => v !== undefined);
+              if (src !== 'env') delete dirtyKeys[k];
+            });
```

---

## 四、修改面与 10 大规范符合性对照

| 规范 | 落实 |
|---|---|
| SRP | 每条 diff 单一职责：读路径统一入口（B-1）、掩码复用（B-4）等均收口单点 |
| DRY | 新建复用 `_provider_conf`（B-5）、`_mask_api_key→mask_secret_value`（B-4）、`env_nonempty`（B-11）、`get_config_snapshot`（B-12）、`copyTextToClipboard`（F-1/F-2）、`applyModelRef`（F-4/F-5） |
| KISS-DIRECT | 全为直线小改，无新抽象/注册表/中间层；B-8/B-9 直接抛 HTTPException 透传 |
| SLAP | 守卫/掩码/快照均放服务层与工具层，路由薄壳不动 |
| YAGNI | 未引入超需求泛化（如不新建通用"映射表"抽象） |
| 禁止 backward | 全部向前修进，无新旧兼容分支；唯一契约变更 B-10 的 `'default'` 为新枚举值（前端 union 同步，属增量非兼容旧态） |
| OCP/LSP/ISP（重构4） | 无继承/接口变更；FUNCTIONS 层新增均在 config_helpers/module 层，可加登记 |
| 复用优先 | B-4/B-11/B-5/B-12/F-1 先查既有公用（mask_secret_value/env 判定/filelock/无前端复制工具则新建 utils/clipboard.ts） |

## 五、风险与待批事项

1. **风险**：B-3、B-5、B-6、B-4 涉及"读路径语义变更"（尤其 B-3 环境变量单源化），需在真实后端回归（probe 侧重读链/settings2 全量/tsc/lint）。
2. **待批**：31 条一未落码。请老陈指示批号与分批（建议：第一批高严重 B-1/B-3/B-5/B-6/F-3/F-4/F-5/F-16；第二批中；第三批低）。
3. **B-10 前端新枚举渲染**（`DefaultTag`）需前端 tokens 对齐；属中等改动，建议单独一批。
4. 本报告为编辑型文档，禁止删除历史版本；后续补条目在文末追加（版本号+签名+时间）。