# 事件总线与HITL网关设计-小健-2026-09-05

**文档名称**: [6]事件总线与HITL网关设计-小健-2026-09-05
**编写人**: 小健
**创建时间**: 2026-09-05 07:30:51
**更新时间**: 2026-09-05 15:58:10

| 版本 | 时间 | 更新人 | 更新要点 |
| v1.2.15 | 2026-09-05 15:58:10 | 小健 | 三章重写为概要设计五问（老陈督办，废方案A/B表）：3.1 为什么用/解决什么问题（对应2.3三宗风险，表格化）、3.2 如何用（签名/动作/时机/注入）、3.3 有什么价值（约束1直保/单写/零侵入/为方案B铺路）、3.4 如何体现（与4.2、7.2~7.7映射）；全文"3.2/3.3/单写标记"引用同步修正 - 小健-2026-09-05 |
| v1.2.13 | 2026-09-05 15:42:46 | 小健 | 七章引言补"小节与正文各章关系及方案A/B归属表"（老陈督办）：7.1/7.2=方案A，7.3/7.4=A+B组合，7.5/7.6/7.7=方案B，7.8=非A非B（[5]范畴）；附归属要点（调网关=A、无yield纯函数化=B） - 小健-2026-09-05 |
| v1.2.12 | 2026-09-05 15:33:26 | 小健 | 全文引用收尾（v1.2.11承诺兑现）：9.1表对应diff列改新节号（七D4→7.1、七D1→7.2、步骤三→7.3、4.4/步骤三四→7.4、步骤四→7.5/7.6、步骤五→7.7/7.8）；第一章1.2.1、4.2、4.4发现C/发现B、七章引言、7.1~7.4小节引言、9.3表、十章风险表全部"七D*"引用改7.x节引用；全文正文D1~D4代号清零（仅版本历史行9/10/12/13保留旧编号记载） - 小健-2026-09-05 |
| v1.2.11 | 2026-09-05 15:10:00 | 小健 | 七章整编重写（老陈督办）：废除D1~D4编号，改为"一个代码文件一个小节"（7.1~7.8），与9.1清单8项一一对应；8个代码文件全部补齐可落盘diff（新补7.5 handle_answer纯函数化/7.6 handle_action纯函数化/7.7 react_dispatch驱动改dict+verdict/7.8 llm_call改yield StreamChunk，并给7.3/7.4补网关收编段；原有D1/D2/D3/D4内容按文件归位保留，已落盘历史（8432c61c0）标注不变）；全文"七D1/七D2/七D3/七D4"引用同步改为7.2/7.4/7.3/7.1节引用 - 小健-2026-09-05 |
| v1.2.10 | 2026-09-05 14:49:00 | 小健 | 十遍核查5修：1.2.1与4.2"十章D4"→"七章D4"漏网2处；9.2/9.3测试编号粒度对齐10.1（T3→T3a+T3b，T4→T4a+T4b）；T2红断言措辞精确化；T3b双重否定改直述 - 小健-2026-09-05 |
| v1.2.9 | 2026-09-05 14:39:11 | 小健 | 九章补全：9.1代码清单8项（hitl_gateway新增+7文件修改，行号实测）/9.2实现顺序双挂表/9.3 TDD五步铁律（TEST红→BACKUP→CODE→TEST绿→COMMIT，红灯未现不许CODE，测试禁入commit）；十章补10.1测试case清单（T1/T2新增+T3a/T3b/T5修改+T4a/T4b沿用保绿+TE2E） - 小健-2026-09-05 |
| v1.2.8 | 2026-09-05 14:34:36 | 小健 | 行号重映射（phase-2+ISS-001/002后工作区实测）：2.1表safety_gate新行号89-102/148-153/196-203/159/115+sandbox新行号106-113/125-128/117；2.3/4.2/4.4/D2/D3/D4注记全量纠正（389删→146/210；411/432/449→156/177/194；377→133；63→66；98→101；281/352→37/108；231-232→14-15）；五章handle_action 7处+llm_call改名+safety_gate前缀；九章llm_call出口；"十D2/D3"→"七D2/D3" - 小健-2026-09-05 |
| v1.2.7 | 2026-09-05 14:15:27 | 小欧 | 15项对盘刷新: ①D2/D3已落盘(8432c61c0)十章标题加注记; ②2.1表action_handler→safety_gate行号; ③2.3嗅探行号; ④4.2 L1修正; ⑤4.4删除清单; ⑥五章表格; ⑦D4注记/脚注; ⑧action_handler.py改名handle_action.py全仓刷新; ⑨行号更新; ⑩import更新; ⑪D4 spec['tool_name']→spec.tool_name(dataclass属性访问); ⑫2.1表safety_gate新行号; ⑬九章两行同步 - 小欧-2026-09-05 |
| v1.1 | 2026-09-05 08:18:21 | 小健 | 一致性4修：publish改async+持锁notify（同步调抛错真bug）；publish注入来源；orchestrator笔误；八5.7按[6]4.3修正 - 小健-2026-09-05 |
| v1.0 | 2026-09-05 07:30:51 | 小健 | 初版：暂停事件审计+总线原语+HITL网关+handler纯函数化+实施顺序（先实施） - 小健-2026-09-05 |

---

## 一、目标与约束

**一句话**：暂停事件只许一个源头（HITL网关），时序由原语实现保证，不由调用方代码位置保证；本阶段先实施。

**三条约束**：
1. paused必须在阻塞等待前到达前端（确认弹窗），resumed在恢复瞬间到达—— functional correctness，不可谈判。
2. event_log append-only + seq语义不动（断线重连/回放依赖，见agent_runner.py行201-205）。
3. agent_runner通道路由表不动（thought仅落库/thought-start与chunk仅SSE/其余双写，见agent_runner.py行307-314）。

### 1.1 方案思路与取舍（v1.2新增 — 小健-2026-09-05）

**核心矛盾**：handler是async generator，"阻塞前先发射paused"依赖yield写在await之前的代码位置——这是三宗时序风险的唯一根。解决方向只有两条路：

| 方向 | 做法 | 解决什么 |
|------|------|---------|
| **方案A：统一暂停源头** | 新增网关模块，所有paused/resumed由publish直写event_log | 位置耦合、双paused、事后补发 |
| **方案B：消除generator** | handler从generator改async函数，不再yield暂停事件 | generator时序脆弱（禁return值、output参数复杂） |

**采纳方案：A+B组合**（本设计文档），理由：

1. **方案A是方案B的前提**：handler要从generator改async函数，前提是暂停事件有其他通道发出——publish提供了这个通道。没有A，B做不了。
2. **方案B是方案A的延伸**：网关落地后handler不再需要yield透传暂停事件，generator→async自然发生，输出参数方案（_verdict/_result_out/_emit_events）作废。
3. **两者不冲突，是先后关系**：步骤一二做A（publish+网关），步骤三四五做B（handler纯函数化）。[6]的实施顺序本身已包含A+B。
4. **改动面可控**：不改agent_runner路由/event_log/DB/SSE/前端，只改handler编排层。

**排除的方案**：

| 方案 | 排除理由 |
|------|---------|
| C：不改架构，加固现有代码 | 治标不治本（位置耦合是架构缺陷），下次重构必重蹈 |
| D：只做B不做A | 没有publish通道，generator→async后暂停事件无处发出，行不通 |

**与[5]的关系**：见1.2概述与八章详述。

### 1.2 与[5]的关系（概述，详见八章）

- [5]v1.6停在设计评审。本设计落地后[5]的5.2/5.3/5.4按网关API重写（输出参数方案作废）；[5]的一/三/四章架构结论不受影响。
- [5]的handler改造方案（输出参数_verdict/_result_out/_emit_events）基于"handler必须是generator"的前提——因为当时没有publish通道。[6]落地后这个前提消失，handler可直接纯函数化（步骤三四五）。


### 1.2.1 待解决问题：ISS-003 / ISS-004（task006 核验入档 — 小欧 2026-09-05）

> 北京老陈督办（2026-09-05）：以下两问题经 task006 问题报告核验确认为真实问题，与本文档网关设计同根。作为待解决问题在此登记，随本设计落地一并解决，不单独硬改。

| 编号 | 问题 | 级别 | 判定 | 与本文档关联 |
|------|------|------|------|--------------|
| ISS-003 | sandbox_gate 门内有门：预检 / 裁决 / HITL等待 / 状态机(SUSPENDED↔EXECUTING) / 事件发射 五职混装于一文件；且外层 check_safety_and_confirm 还有一层完整真HITL（SRP违规） | P2 | 真实（结构性） | 4.x hitl_confirm 网关收编后 sandbox_gate 仅留预检+放行裁定，恢复一件一口 |
| ISS-004 | 暂停事件多源发射：确认通道 4 组（safety_gate paused 1 + resumed 2、sandbox_gate paused/resumed 一对）+ task_runtime 暂停通道 1 对，共 5 组，任一重构挪行即可能违法序（位置耦合） | P1 | 真实（架构） | 7.2 publish总线 + 4.x 网关统一收编确认通道(#1-#4)；task_runtime 按 2.2 两条通道边界保持独立 |

**处置约定**：
1. ISS-003、ISS-004 不复单独立项硬改，随本文档第四章 HITL 网关落地（7.1 骨架+调用方改调）一并消失。
2. ISS-001（bypass 旧块双沙箱调用残留）、ISS-002（沙箱 resumed 缺 confirm_id）已于本日先行落盘修复（commits 8432c61c0），不在本待解决清单内。
3. 本文档七章 7.3/7.4 代码 diff 与上述先行修复两处遥相呼应，网关落地时以七章为准复校。

---
## 二、现状审计：暂停事件全清单与三宗时序风险

### 2.1 全仓paused/resumed发射点（已对盘）

| # | 位置 | 行号 | 事件 | 等待 |
|---|------|------|------|------|
| 1 | safety_gate.check_safety主路 | 89-102 | paused（确认工具执行） | 行159 wait_for_confirmation_result |
| 2 | safety_gate.check_safety bypass | 148-153 | resumed（自动确认恢复） | 行115 S1等待 |
| 3 | safety_gate.check_safety确认后 | 196-203 | resumed（用户确认恢复） | 行159等待返回后 |
| 4 | sandbox_gate.sandbox_resolve裁决 | 106-113 paused / 125-128 resumed | paused+resumed（沙箱裁决） | 行117 wait_for_confirmation_result |
| 5 | task_runtime（用户主动暂停通道） | task_pause_check_and_yield行176 | paused/resumed SSE | 行159 wait_for_resume |

### 2.2 两条暂停通道（必须区分）

- **确认通道**（#1-#4）：confirm_id驱动，前端弹窗裁决；超时/取消语义见hitl_confirmation.py行115-155。
- **任务暂停通道**（#5）：pause_task/resume_task API驱动（task_registry行167/177），与confirm_id无关。
- 本文档网关收编**确认通道**；任务暂停通道保持不动，两者发射同形paused/resumed Step（前端已兼容）。

### 2.3 三宗时序风险（现状即bug温床）

1. **位置耦合**：paused正确性依赖"yield写在await之前"的手工顺序，任一重构挪行即违法序（如[5]v1.4曾误删paused改经_emit_events致弹窗迟到，[5]5.2.6有载）。
2. **嵌套裁决双paused**：主路paused（#1）与沙箱paused（#4）叠加，resumed配对靠嗅探`any(resumed)`（safety_gate行146/210；旧389随ISS-001删除），脆弱。
3. **事后补发诱惑**：任何把paused收进列表事后emit的方案都直接违法序（弹窗迟到）。

---

## 三、StreamBuffer.publish 概要设计

**一句话**：publish是暂停事件进入event_log的唯一入口——"调用它的时刻就是事件落event_log的时刻"，时序由发布动作保证，不由yield代码位置保证。本文档所有暂停事件以此为准。

### 3.1 为什么用 / 解决什么问题（2.3三宗时序风险）

暂停事件（paused/resumed）必须进event_log才能经SSE到达前端。现状发射靠generator `yield`"路过即发射"，yield必须手工写在await之前，代码位置一挪即违法序——这正是2.3三宗时序风险的共同根源。publish把"发射"从隐式路过改成显式调用：

| 问题（2.3） | 现状根因 | publish解决 |
|------|--------|-----------|
| 位置耦合 | 时序靠"yield写在await之前" | 时序靠"调用publish的时刻"，与代码位置无关 |
| 嵌套裁决双paused | 主路/sandbox各自yield各自的 | 统一收编网关，publish单一入口发paused |
| 事后补发诱惑 | 先把paused收进列表、事后才emit | 调用即落event_log，无从延迟补发 |

### 3.2 如何用（接口契约与调用时机）

- **签名**：`async def publish(step_dict: dict) -> int`；入参为完整Step的dict（先`to_dict()`再publish）；返回该事件分配到的seq。
- **动作**：分配seq + append event_log + 持锁`notify_all`，复用agent_runner._append持锁模板（行199-210），不另起队列、不动event_log语义（约束2）。
- **时机**：网关阻塞前`await publish(paused.to_dict())`、恢复瞬间`await publish(resumed.to_dict())`（见4.2行171/177）；普通事件仍走yield透传，仅时序关键事件走publish。
- **注入**：publish由调用方注入`buffer.publish`，与`agent_streams.get(task_id)`同源。

### 3.3 有什么价值

1. **约束1（paused先于等待、resumed恢复瞬间）由发布时刻直保**：是"阻塞前调/恢复瞬间调"这个调用顺序的必然结果，不再依赖手工yield位置。
2. **单写**：所有paused/resumed只从publish一个入口进event_log，位置耦合/双paused/事后补发三种诱惑同时消失（ISS-004同根）。
3. **零侵入**：不动agent_runner路由表（约束3）、不动event_log/SSE/DB，前端零改动。
4. **为方案B铺路**：暂停事件有了独立出口，handler不再靠yield发暂停，可纯函数化（见五章）。

### 3.4 如何体现在本文档

| 本文档位置 | 体现 |
|------|------|
| 二章2.3 | publish要消灭的三宗时序风险（问题清单） |
| 4.2 网关骨架 | `await publish(paused.to_dict())` / `await publish(resumed.to_dict())` 两处调用 |
| 七章7.2 | task_state.py publish方法落地diff（9.1清单#2） |
| 七章7.3/7.4 | sandbox/safety两侧暂停事件收编经publish |
| 七章7.5~7.7 | handler纯函数化后暂停事件经publish、普通事件仍yield |

---

## 四、HITL网关：hitl_confirm（唯一暂停源头）

### 4.1 落点与复用

新模块`app/services/task/hitl_gateway.py`。复用hitl_confirmation三原语（create行87 / wait行115 / resolve行158），不重写等待/超时/取消语义（含2s轮询cancel检查行139-150、expired标记行152、finally清表行154-155）。

### 4.2 原语签名与流程（v1.2重写 — 小健-2026-09-05，示意，实施时落盘为准）

```python
async def hitl_confirm(agent, spec: ConfirmSpec, publish) -> ConfirmVerdict:
    """确认通道唯一入口。
    spec: {mode("hitl"|"bypass"), tool_name, params(原始未脱敏),
           path(可空, 为空网关内算), content, safety_level, auto_confirm(bool, 默认None由mode推导)}。
    网关内统一四件事（原分散在主路/sandbox三处，现收拢，DRY）：
      ①超时计算（点分扁平键，分mode双公式，与三处逐行同源 — 小健-2026-09-05）：
        hitl→security.hitl_timeout兜底HITL_TIMEOUT，confirm=max(HITL_MIN_CONFIRM_TIMEOUT, backend-HITL_CONFIRM_LEAD)；
        bypass→security.auto_confirm_delay兜底10.0，backend=max(MIN+LEAD, delay)，confirm=backend-BYPASS_AUTO_LEAD。
        五常量均在app.constants（落盘实现见7.1 _resolve_timeouts）；
      ②trust_path提取：app.tools.trust.extract_trust_path + 回落键扫描
        （原主路_extract_trust_path与sandbox回落扫描两套，现收拢）；
      ③params脱敏：SENSITIVE_FIELDS过滤（原两处paused各做，现收拢，防调用方漏脱敏）；
      ④set_status：hitl/bypass统一 SUSPENDED→wait→EXECUTING（现状bypass路径agent
        从未变SUSPENDED，badge不翻paused，是隐患；hitl路径行156/177/194翻转（原411/432/449，phase-2搬迁），
        网关接管后调用方不再翻转 — v1.2 L1修正 — 小健-2026-09-05）。
    verdict: {"confirmed": bool, "expired": bool, "trust_session": bool, "confirm_id": str}。
    网关不管授权：grant_temp_auth归调用方按verdict做（见4.4）。
    sandbox②场景：needs_ruling+非bypass时sandbox也调网关（纯函数化），
      不再自己yield paused——"唯一暂停源头"约束要求sandbox②场景也经网关
      （v1.2 L2修正，步骤三sandbox切流覆盖 — 小健-2026-09-05）。
    import约束：本模块顶层只import叶子（steps/status_table/constants/logger）；
      hitl_confirmation/config/trust/SENSITIVE_FIELDS一律函数内延迟import，
      否则重蹈sandbox_gate顶部注释的循环import环（handlers→网关→hitl→task_runtime→task_registry→…）。"""
    from app.services.task.hitl_confirmation import (  # 延迟import防环 — 小健-2026-09-05
        create_confirmation, wait_for_confirmation_result)
    _path = spec.path or _resolve_trust_path(spec.tool_name, spec.params)  # 网关内统一
    confirm_id = await create_confirmation(agent.task_id, spec.tool_name, _path)  # path透传(v1.5 tool+path落库，P0-3)
    _safe_params = _desensitize(spec.params)  # 网关内统一脱敏
    _backend_timeout, _confirm_timeout = _resolve_timeouts(spec.mode)  # 网关内统一
    paused = agent._step_emitter.emit(MetaStep(step=agent.llm_call_count, type="paused",
        content=spec.content, confirm_id=confirm_id, tool_name=spec.tool_name,
        params=_safe_params, safety_level=spec.safety_level, severity="attention",
        trust_path=_path, auto_confirm=(spec.mode == "bypass"),
        confirm_timeout=_confirm_timeout, backend_timeout=_backend_timeout))
    set_status(agent, AgentStatus.SUSPENDED, f"等待用户确认: {spec.tool_name}")  # hitl+bypass统一（L1修正）
    await publish(paused.to_dict())               # 先to_dict再publish（P0-1：emit返回Step对象非dict）
    auth = await wait_for_confirmation_result(confirm_id, timeout=_backend_timeout)
    set_status(agent, AgentStatus.EXECUTING, "用户裁决完成")  # hitl+bypass统一恢复（L1修正）
    if auth.get("confirmed"):
        resumed = agent._step_emitter.emit(MetaStep(step=agent.llm_call_count, type="resumed",
            content=f"已确认执行: {spec.tool_name}", severity="info", confirm_id=confirm_id))
        await publish(resumed.to_dict())          # confirmed才publish（P0-2）
    # 拒绝/超时：不publish resumed（与现状一致，原来拒绝路径只发error不发resumed，
    # 前端badge不闪running）；调用方按verdict发error事件，见4.4。
    return {"confirmed": bool(auth.get("confirmed")), "expired": bool(auth.get("expired")),
            "trust_session": bool(auth.get("trust_session", False)), "confirm_id": confirm_id}
```

### 4.3 语义（从hitl_confirmation原文继承）

- 超时→`{"confirmed": False, expired: True}`；取消→`{"confirmed": False}`；entry丢失→False并告警。
- 幂等：confirm_id一次性（wait finally清表）；网关不重试，重试由调用方按verdict决策。
- bypass（auto_confirm）调用方同样走网关：paused带`auto_confirm=True`照发（前端倒计时照转），S1等待语义保留在调用方。

### 4.4 调用方契约：授权归属 + set_status删除清单 + 双paused策略（v1.2新增 — 小健-2026-09-05）

**授权归属**：grant_temp_auth归调用方按verdict做——confirmed且trust_session为真时调用方grant（现状主路/sandbox各自grant），网关只给verdict、不碰授权。

**set_status删除清单**（网关接管SUSPENDED/EXECUTING后，调用方翻转全删，防双翻；行号为phase-2后safety_gate实测 — 小健-2026-09-05）：
- safety_gate.check_safety主路：行156（SUSPENDED）、行177/194（EXECUTING）删除；
- safety_gate bypass路径：行133 set_status删除（删除后须确认bypass路径badge正常，见4.2 L1修正）；
- sandbox_gate.sandbox_resolve：HITL分支自身paused/resumed侧翻转删除（行114/118，改走网关）。

**发现C（v1.2.2对盘活捉真bug，bypass路径双重沙箱调用；已落盘8432c61c0）**：原action_handler.py bypass路径行382-390（旧`sandbox_precheck`+`sandbox_resolve`）与行396-402（新`run_sandbox_gate` DRY统一入口）**并存**——2026-09-04 DRY重构只把无条件continue防护补在run_sandbox_gate后（注释391-395），**忘记删除旧调用块**。后果：`_pre is not None` 且旧resolve返回ok但无resumed时（放行场景），落进run_sandbox_gate**二次预检/二次裁决**，比2.3风险2"主路+沙箱双弹窗"更严重——**单路径内重复裁决**。处置已落盘（ISS-001：旧块整段删除，现safety_gate行141 run_sandbox_gate为唯一沙箱入口；phase-2后文件为safety_gate.py）。历史diff见7.4（发现C处置归属safety_gate，即7.4.1已落盘段 — 小健-2026-09-05）。此删除同时消除旧行389（旧块内any(resumed)），嗅探删除清单再删新行146/210即可。

**双paused策略**（2.3风险2的处置，v1.2 L2修正 — 小健-2026-09-05）：同工具同时触发主路HITL+沙箱HITL时，sandbox②场景（needs_ruling+非bypass）也调网关，check在调sandbox前已知主路verdict confirmed，将"已确认"上下文传入sandbox，sandbox跳过二次确认（单paused，推荐）；若产品要求双弹窗可接受，则两个confirm_id各弹各的，二选一实施前定案。`any(resumed)`嗅探一律改为读verdict（嗅探代码新行146/210删除；旧389随ISS-001整段删除）。

**发现B（v1.2.2对盘，sandbox resumed缺confirm_id；已落盘8432c61c0）**：sandbox_gate.py原行120-122（现125-128）沙箱确认后resumed**无confirm_id字段**（仅step/type/content/severity），与主路resumed（safety_gate行148-153带confirm_id）不同形——前端关弹窗/回放依赖confirm_id配对。处置已落盘（ISS-002：现128行补confirm_id=confirm_id，与主路同形）。历史diff见7.3（发现B处置归属sandbox_gate，即7.3.1已落盘段 — 小健-2026-09-05）。九章风险表已修正表述。

---

## 五、handler纯函数化对照表（网关落地后）

| handler | 现状 | 网关落地后 | 说明 |
|---------|------|-----------|------|
| handle_answer.handle_answer | generator yield 10处 | **纯函数return dict** | 无阻塞等待，可纯化（[5]5.1八分支照用；模块已git mv改名，见[8]10.3 — 小健-2026-09-05） |
| sandbox_gate.sandbox_resolve | generator（HITL分支阻塞） | **纯函数**：组spec调网关（sandbox②场景needs_ruling+非bypass也调网关），拿verdict回dict | 等待移入网关，本体无yield |
| safety_gate.check_safety_and_confirm | generator（paused 1处+resumed 2处+run_sandbox_gate 3处） | **纯函数**：HITL处组spec调网关；blocked/timeout/rejected记否决表 | 本体无yield，live由网关publish（phase-2后现址，行号见2.1 — 小健-2026-09-05） |
| handle_action.handle_action | generator 7处 | **纯函数return result dict** | Thought/ActionStep/observation/return_direct组dict；generator禁return问题消失（phase-2后handle_action.py 384行单函数，yield行299/309/310/323/337/373/383 — 小健-2026-09-05） |
| llm_call.call_llm_with_fallback | yield元组 | **改yield StreamChunk** | [5]5.5.2照用（含累积搬迁；llm_stream.py已拆改名llm_call.py，函数现居llm_call.py:209 — 小健-2026-09-05） |
| react_dispatch._dispatch_handler | generator驱动+推断 | 驱动纯函数拿dict→转Step→publish/yield；状态推断（拆分自react_cycle.py:288-389）保留 | 推断输入改为dict+verdict，规则不变 |

## 六、react调度与消费侧改造

- `react_dispatch._dispatch_handler`（拆分自react_cycle.py行288-389）：answer分支`await`拿dict；action分支`await handle_action`拿result dict；转换emit后跑状态推断（deny计数/终态规则逐行保留，仅事件源改dict）。
- `react_step._process_single_step`分发处（拆分自react_cycle.py行701-704）保持`async for…yield`。
- agent_runner消费循环（行258-314）**不动**：dict|Step→to_dict→路由表→event_log/DB全保留；网关publish的paused/resumed经同一event_log走SSE——与现状同（paused/resumed本来就仅SSE不落库，见runner行329-330；"落库"措辞修正 — 小健-2026-09-05），零退化。
- orchestrator/SSE/DB：**零改动**（读的还是event_log）。

## 七、可落实代码diff（v1.2.2对盘后补 — 小健-2026-09-05）

> 本章为可直接落盘的diff（git diff格式），按"一个代码文件一个小节"组织，与九章9.1清单8项一一对应：7.1↔#1，7.2↔#2，7.3↔#3，7.4↔#4，7.5↔#5，7.6↔#6，7.7↔#7，7.8↔#8。落盘前一律以本章代码为准、正文示意作废；落盘后按AGENTS.md做三堂会审（合规/合理/关联逻辑）。

**本章小节与正文各章关系及方案A/B归属表**（v1.2.13补充，老陈督办；方案定义见1.1 — 小健-2026-09-05）：

| 小节 | 对应正文章节 | 方案归属 | 说明 |
|------|-------------|---------|------|
| 7.1 hitl_gateway.py | 第4章 HITL网关（4.1/4.2） | **方案A** | 新增网关模块，唯一暂停源头 |
| 7.2 task_state.py publish | 三章3.1/3.2（publish时序与接口） | **方案A** | publish直写event_log，方案A前提 |
| 7.3 sandbox_gate.py | 第4章4.4 + 第5章sandbox行 | **A+B组合** | 组spec调网关（A）纯函数化（B），删114/118翻转 |
| 7.4 safety_gate.py | 第4章4.4 + 第5章safety行 | **A+B组合** | 3处组spec调网关（A）纯函数化（B），删set_status/sniff/steps plumbing |
| 7.5 handle_answer.py | 第5章对照表handle_answer行 | **方案B** | 10处yield→return dict纯函数化 |
| 7.6 handle_action.py | 第5章对照表handle_action行 | **方案B** | 7处yield→return dict纯函数化 |
| 7.7 react_dispatch.py | 第6章 react调度与消费侧 | **方案B** | 驱动改dict+verdict拿结果，状态推断保留 |
| 7.8 llm_call.py | 第5章llm_call行 + [5]5.5.2 | **非A非B** | llm流式出口改yield StreamChunk，[5]范畴随9.1#8并入 |

**方案A/B归属要点**：方案A＝统一暂停源头（7.1+7.2，与7.3/7.4"调网关"那半）；方案B＝消除generator（7.5+7.6+7.7，与7.3/7.4"纯函数化"那半）；7.3/7.4为真实A+B组合（调网关是方案A、本体无yield是方案B，同文件两步落地）；7.8与暂停事件无关，属[5] yield链路改造。

### 7.1 hitl_gateway.py — 新文件骨架（4.1/4.2定稿，落盘后补单测）【对应9.1#1，待落盘】

> 本节为新增文件完整骨架（网关骨架原作为首节保留，按9.1清单归位至7.1 — 小健-2026-09-05）。

```python
# app/services/task/hitl_gateway.py  — 小健-2026-09-05（骨架，按4.2语义实现）
"""HITL确认唯一入口。复用hitl_confirmation三原语，不重写等待/超时/取消。"""
from dataclasses import dataclass
from typing import Optional
from app.services.agent.steps import MetaStep
from app.services.agent.status_table import set_status, AgentStatus  # AgentStatus真实位置status_table行19 — 小健-2026-09-05

@dataclass
class ConfirmSpec:
    """确认规格（与4.2同形，属性访问；auto_confirm默认None由mode推导） — 小健-2026-09-05"""
    mode: str                    # "hitl" | "bypass"
    tool_name: str
    params: Optional[dict] = None
    path: Optional[str] = None
    content: str = ""
    safety_level: str = ""
    auto_confirm: Optional[bool] = None

async def _resolve_trust_path(tool_name, params) -> Optional[str]:
    from app.tools.trust import extract_trust_path        # 延迟import防环（真实位置tools/trust.py行66）
    params = params or {}
    # 网关内统一提取（原主路_extract_trust_path + sandbox回落扫描，收拢；回落键与sandbox_gate行101同源）
    return extract_trust_path(tool_name, params) or next(
        (v for k in ("path", "file_path", "source_path", "dest_path", "target", "dir_path")
         if isinstance((v := params.get(k)), str) and v), None)

def _desensitize(params) -> dict:
    from app.tools.tool_constants import SENSITIVE_FIELDS      # 延迟import防环（真实位置，sandbox_gate行66同源 — 小健-2026-09-05）
    return {k: v for k, v in (params or {}).items() if k not in SENSITIVE_FIELDS}

async def _resolve_timeouts(mode):
    """返回 (backend_timeout, confirm_timeout) 二元组（调用处双解包 — 小健-2026-09-05）。
    点分扁平键（与三处同源，禁嵌套get("security")取法）+ 分mode公式：
    hitl→security.hitl_timeout兜底HITL_TIMEOUT，confirm=max(MIN, backend-HITL_CONFIRM_LEAD)；
    bypass→security.auto_confirm_delay兜底10.0，backend=max(MIN+LEAD, delay)，confirm=backend-BYPASS_AUTO_LEAD。
    （见safety_gate行79/85/87/112、sandbox_gate行90-92；五常量均在app.constants）。"""
    from app.config import get_config                            # 延迟import防环
    from app.constants import HITL_TIMEOUT, HITL_CONFIRM_LEAD, HITL_MIN_CONFIRM_TIMEOUT, BYPASS_AUTO_LEAD
    cfg = get_config()
    if mode == "hitl":
        _bt = int(float(cfg.get("security.hitl_timeout", HITL_TIMEOUT)))
        return _bt, max(HITL_MIN_CONFIRM_TIMEOUT, _bt - HITL_CONFIRM_LEAD)
    _bt = max(HITL_MIN_CONFIRM_TIMEOUT + BYPASS_AUTO_LEAD,
              int(float(cfg.get("security.auto_confirm_delay", 10.0))))
    return _bt, _bt - BYPASS_AUTO_LEAD

async def hitl_confirm(agent, spec: ConfirmSpec, publish):
    """spec属性访问（与4.2同形）→verdict。网关内统一：超时计算/trust_path/脱敏/SUSPENDED→wait→EXECUTING。"""
    from app.services.task.hitl_confirmation import (            # 延迟import防环
        create_confirmation, wait_for_confirmation_result)
    _path = spec.path or await _resolve_trust_path(spec.tool_name, spec.params)
    confirm_id = await create_confirmation(agent.task_id, spec.tool_name, _path)
    _bt, _ct = await _resolve_timeouts(spec.mode)
    _auto = spec.auto_confirm if spec.auto_confirm is not None else (spec.mode == "bypass")
    paused = agent._step_emitter.emit(MetaStep(step=agent.llm_call_count, type="paused",
        content=spec.content, confirm_id=confirm_id, tool_name=spec.tool_name,
        params=_desensitize(spec.params), safety_level=spec.safety_level,
        severity="attention", trust_path=_path, auto_confirm=_auto,
        confirm_timeout=_ct, backend_timeout=_bt))
    set_status(agent, AgentStatus.SUSPENDED, f"等待用户确认: {spec.tool_name}")
    await publish(paused.to_dict())
    auth = await wait_for_confirmation_result(confirm_id, timeout=_bt)
    set_status(agent, AgentStatus.EXECUTING, "用户裁决完成")
    if auth.get("confirmed"):
        resumed = agent._step_emitter.emit(MetaStep(step=agent.llm_call_count, type="resumed",
            content=f"已确认执行: {spec.tool_name}", severity="info", confirm_id=confirm_id))
        await publish(resumed.to_dict())
    return {"confirmed": bool(auth.get("confirmed")), "expired": bool(auth.get("expired")),
            "trust_session": bool(auth.get("trust_session", False)), "confirm_id": confirm_id}
```

- 消费侧（agent_runner行258-314 / react_step分发）`async for…yield`保持不变，paused/resumed经同一event_log走SSE（现状仅SSE不落库，runner行329-330，零退化）。
- 三轮会审结论（2026-09-05 13:11:49 — 小健）：①合规全过，附P2：`_resolve_trust_path`内联回落扫描是第三份拷贝（safety_gate _extract_trust_path / sandbox行97-102 / 本骨架），落盘时抽`app.tools.trust`公用真收拢；②合理附minor：mode非法值静默走bypass，落盘加`ValueError`显式校验；③关联全过：调用方沿用safety_gate行37/108函数级延迟import惯例；顶层两条import边与safety_gate行14-15完全相同，已在线无环；7.3/7.4改造不破坏测试（test_resumed_after_sandbox字符串断言靠safety_gate行51注释仍过）。
- 点分扁平键已对盘（safety_gate行79/85/87/112、sandbox行90），落盘无需再核config键名。

### 7.2 task_state.py — StreamBuffer.publish 生产者直写（对应9.1#2，步骤一）

> 本节内容（task_state.py 发布改造diff）由旧版附录首段归位；已按三章3.2（publish动作与接口）定稿对盘（event_log/cond/done三字段前置已存在，action见三章3.2"动作" — 小健-2026-09-05）。

```diff
+# 插入到 task_state.py StreamBuffer 类内（event_log/cond/done字段之后） — 小健-2026-09-05
+    async def publish(self, step_dict: dict) -> int:
+        """生产者直写：append + seq + 唤醒消费者。返回seq。
+        与 agent_runner._append(行201-210) 同模式：先dict()拷贝防调用方副作用，
+        append 与 notify 均须持锁原子完成；cond.notify_all 未持锁调用抛 RuntimeError。
+        — 小健-2026-09-05"""
+        step_dict = dict(step_dict)
+        step_dict["seq"] = len(self.event_log)
+        self.event_log.append(step_dict)
+        async with self.cond:
+            self.cond.notify_all()
+        return step_dict["seq"]
```

用例（替换三章3.2示意；StreamBuffer现居task_state行25，agent_streams行38，get_stream_buffer行48）：
```python
buffer = get_stream_buffer(task_id)          # 与现有 agent_streams.get 同源
await buffer.publish(step.to_dict())         # 由调用方注入绑定方法，见4.2
```

步骤一落盘：T1红（test_stream_publish.py）→BACKUP→CODE→T1绿→commit（单写价值见三章3.3）。

### 7.3 sandbox_gate.py — 纯函数化调网关（对应9.1#3，步骤三）

> 一段已落盘历史（ISS-002，sandbox resumed缺confirm_id）+ 一段新补diff（sandbox_resolve改纯函数调网关，删114/118翻转）。

**7.3.1 已落盘历史（8432c61c0，ISS-002）：resumed补confirm_id**
```diff
 # 现文件 sandbox_gate.py 行125-128 sandbox_resolve HITL分支确认恢复（8格缩进，逐字节对盘 — 小健-2026-09-05）
        steps.append(agent._step_emitter.emit(MetaStep(
            step=step, type="resumed",
-            content=f"沙箱预检已确认执行: {tool_name}", severity="info")))
+            content=f"沙箱预检已确认执行: {tool_name}", severity="info",
+            confirm_id=confirm_id)))
```
- 行84 create_confirmation 已返回 confirm_id（sandbox_resolve入口，本块作用域内可用；同行119 confirmed判定）。

**7.3.2 新补：sandbox_resolve needs_ruling 分支改组spec调网关（步骤三主体）**
```diff
 # 现文件 sandbox_gate.py 行82-136 sandbox_resolve needs_ruling分支（8格缩进 — 小健-2026-09-05）
-from app.services.task.hitl_confirmation import create_confirmation, wait_for_confirmation_result  # 行64
-from app.services.agent.status_table import AgentStatus, set_status                                # 行63
-    confirm_id = await create_confirmation(agent.task_id, tool_name)          # 行84
-    # 行85-92 时间计算（_bt/_ct）全删，收网关 _resolve_timeouts                      
-    # 行93-105 trust_path扫描全删，收网关 _resolve_trust_path                      
-    steps = [agent._step_emitter.emit(MetaStep(
-        step=step, type="paused", ...))]                                        # 行106-113
-    set_status(agent, AgentStatus.SUSPENDED, f"等待用户裁决沙箱预检: {tool_name}") # 行114
-    auth = await wait_for_confirmation_result(confirm_id, timeout=...)          # 行117
-    set_status(agent, AgentStatus.EXECUTING, "沙箱预检用户裁决完成")             # 行118
-    if auth.get("confirmed"):
-        steps.append(agent._step_emitter.emit(MetaStep(
-            step=step, type="resumed", content=..., confirm_id=confirm_id)))    # 行125-128
-        return True, steps
-    logger.warning(...); denied_list.append(...)                               # 行130-131
-    steps.append(agent._step_emitter.emit(MetaStep(
-        step=step, type="error", ...)))                                         # 行132-135
-    return False, steps
+    # 2026-09-05 小健: needs_ruling 改走网关（唯一暂停源头）。网关内统一：
+    #   paused先于wait到达 / SUSPENDED→wait→EXECUTING / 脱敏 / trust_path / confirm_id回传。
+    from app.services.task.hitl_gateway import ConfirmSpec, hitl_confirm       # 延迟import防环
+    from app.services.task.task_state import get_stream_buffer                 # 延迟import防环
+    logger.info(f"[sandbox] 转HITL用户裁决: tool={tool_name}")
+    publish = get_stream_buffer(agent.task_id).publish
+    spec = ConfirmSpec(
+        mode="hitl", tool_name=tool_name, params=params,
+        content=f"沙箱未能完成有效预检,需用户裁决是否直接执行: {tool_name}",
+        safety_level="destructive", auto_confirm=False)
+    verdict = await hitl_confirm(agent, spec, publish)
+    if verdict["confirmed"]:
+        logger.info(f"[sandbox] 用户裁决: 确认执行: tool={tool_name}")
+        return True, []          # paused/resumed 已由网关publish，此处不再组Step
+    logger.warning(f"[sandbox] 用户裁决: 拒绝执行: tool={tool_name}")
+    denied_list.append((tool_name, "沙箱预检未完成验证且用户拒绝执行", call))
+    return False, [agent._step_emitter.emit(MetaStep(
+        step=step, type="error",
+        content=f"用户拒绝执行(预检未完成验证): {tool_name}",
+        error_type="user_rejected", severity="warn"))]
```
- 返回值仍 (ok, steps)（run_sandbox_gate 契约不变）；steps 只剩 error 类（paused/resumed 由网关 publish）。
- 连接点：run_sandbox_gate 入口（行143-154）零改动，直接复用扩展后的 sandbox_resolve。

### 7.4 safety_gate.py — 3处组spec调网关 + 删set_status/删sniff/删steps plumbing（对应9.1#4，步骤三四）

> 一段已落盘历史（ISS-001，bypass路径双重沙箱调用）+ 一段新补diff（check_safety_and_confirm 纯函数化）。

**7.4.1 已落盘历史（8432c61c0，ISS-001）：bypass路径删除旧sandbox双调用块**
```diff
 # 原 action_handler.py 行378-409（现 safety_gate.py 行134-154区；ISS-001已落盘删除旧块）check_safety_and_confirm bypass路径
 # 删除 行382-390（旧 sandbox_precheck + sandbox_resolve 双调用块），保留行391起新gate（现safety_gate行141唯一gate — 小健-2026-09-05）：
-                    _pre = await sandbox_precheck(safety_result, _cn, _cp)
-                    if _pre is not None:
-                        _ok, _steps = await sandbox_resolve(agent, step, call, _cn, _cp, _pre, safety_result, _denied)
-                        for _st in _steps:
-                            yield _st
-                        if not _ok:
-                            continue
-                        if any(getattr(_s, "type", None) == "resumed" for _s in _steps):
-                            continue
```
```diff
 # 原 action_handler.py 行247 import（现 safety_gate.py 行18，已仅留run_sandbox_gate — 小健-2026-09-05）：
-from app.services.agent.handlers.sandbox_gate import sandbox_precheck, sandbox_resolve, run_sandbox_gate  # 小欧 2026-08-25 沙箱闸门逻辑拆分; 小健 2026-09-04 新增run_sandbox_gate统一入口
+from app.services.agent.handlers.sandbox_gate import run_sandbox_gate  # 小健-2026-09-05：旧双调用删除，precheck/resolve无引用
```

**7.4.2 新补：check_safety_and_confirm 纯函数化（async generator → async 普通函数返回events）**

改造面（以phase-2后safety_gate.py 242行为准，三处run_sandbox_gate在行141/205/226 — 小健-2026-09-05）：
- 签名：`async def check_safety_and_confirm(...) -> list`，内部不再 yield，改收 `_events: list`；入口 `_events = []`，出口 `return _events`（块内所有 `continue`/`yield` 相应调整）。
- `yield agent._step_emitter.emit(MetaStep(...))` → `_events.append(agent._step_emitter.emit(MetaStep(...)))`（error类事件全部入列表）。

```diff
 # 三处 run_sandbox_gate 汇合点（行141/205/226）——steps plumbing 删除 + sniff 删除 — 小健-2026-09-05
-                _ok, _steps = await run_sandbox_gate(agent, step, call, _cn, _cp, safety_result, _denied)
-                for _st in _steps:
-                    yield _st
-                if not _ok:
-                    continue
-                if any(getattr(_s, "type", None) == "resumed" for _s in _steps):  # 行146/210 sniff
-                    continue
+                _ok, _steps = await run_sandbox_gate(agent, step, call, _cn, _cp, safety_result, _denied)
+                _events.extend(_steps)      # 仅error类（paused/resumed已由网关publish，7.3.2后无嗅探必要）
+                if not _ok:
+                    continue
```
```diff
 # 真HITL块（行156-203区）：create_confirmation+paused+wait+resumed+授权 收拢为网关调用 — 小健-2026-09-05
-                set_status(agent, AgentStatus.SUSPENDED, f"等待用户确认工具执行: {_cn}")   # 行156 删
-                auth = await wait_for_confirmation_result(confirm_id, timeout=int(float(_get_cfg_wait().get("security.hitl_timeout", HITL_TIMEOUT))))  # 行159
-                set_status(agent, AgentStatus.EXECUTING, "用户拒绝/超时，恢复执行态")        # 行177 删
-                set_status(agent, AgentStatus.EXECUTING, "用户已确认工具执行")              # 行194 删
+                from app.services.task.hitl_gateway import ConfirmSpec, hitl_confirm
+                from app.services.task.task_state import get_stream_buffer
+                _verdict = await hitl_confirm(agent, ConfirmSpec(
+                    mode="hitl", tool_name=_cn, params=_cp,
+                    content=f"是否允许执行工具: {_cn}",
+                    safety_level=getattr(safety_result, "safety_level", "")), 
+                    get_stream_buffer(agent.task_id).publish)
+                if not _verdict["confirmed"]:
+                    if _verdict["expired"]:
+                        logger.warning(f"[action] step={step} timeout: tool={_cn}")
+                        _events.append(agent._step_emitter.emit(MetaStep(
+                            step=step, type="error", content=f"工具确认超时未响应: {_cn}",
+                            error_type="timeout", severity="warn")))
+                        _denied.append((_cn, "确认超时未响应", call))
+                    else:
+                        logger.warning(f"[action] step={step} rejected: tool={_cn}")
+                        _events.append(agent._step_emitter.emit(MetaStep(
+                            step=step, type="error", content=f"用户拒绝执行工具: {_cn}",
+                            error_type="user_rejected", severity="warn")))
+                        _denied.append((_cn, "被用户拒绝执行", call))
+                    continue
+                # 授权归属（4.4）：verdict.trust_session → grant_temp_auth（若auth_path）；resumed 已由网关publish
+                if _verdict.get("trust_session") and getattr(safety_result, "auth_path", None):
+                    try:
+                        from app.tools.security.temp_auth import grant_temp_auth
+                        grant_temp_auth(safety_result.auth_path, recursive=True)
+                        logger.info(f"[action] step={step} resumed+auth: tool={_cn} path={safety_result.auth_path}")
+                    except Exception as e:
+                        logger.warning(f"[action] 确认后grant_temp_auth失败不阻断: {e!r}")
```
```diff
 # bypass路径（行100-154区）：S1窗口等待 收拢为网关(mode=bypass)调用，badge照转 — 小健-2026-09-05
-                ... create_confirmation + paused(四字段) + S1窗口 wait_for_confirmation_result ...
-                set_status(agent, AgentStatus.EXECUTING, "安全策略自动确认工具执行")   # 行133 删
+                from app.services.task.hitl_gateway import ConfirmSpec, hitl_confirm
+                from app.services.task.task_state import get_stream_buffer
+                _verdict = await hitl_confirm(agent, ConfirmSpec(
+                    mode="bypass", tool_name=_cn, params=_cp,
+                    content=f"安全策略自动确认工具执行: {_cn}",
+                    safety_level=getattr(safety_result, "safety_level", "")),
+                    get_stream_buffer(agent.task_id).publish)
+                _bypass_confirmed = _verdict["confirmed"] or _verdict["expired"]  # S1超时bypass兜底放行
+                if getattr(safety_result, "auth_path", None) and _verdict.get("trust_session"):
+                    try:
+                        from app.tools.security.temp_auth import grant_temp_auth
+                        grant_temp_auth(safety_result.auth_path, recursive=True)
+                    except Exception as e:
+                        logger.warning(f"[action] bypass grant_temp_auth失败仍放行: {e!r}")
+                # 后续 run_sandbox_gate(行141) 汇合点按7.4.2改造执行
```
- 调用方 handle_action 行320-323 同步改为普通await（并入7.6纯函数化）：
```python
_events.append(await check_safety_and_confirm(agent, call_result.all_calls, step,
                                              call_result.fc_context,
                                              _out=_safe_calls, _denied_out=_denied_list))
```
- 测试既有断言不变（test_resumed_after_sandbox 字符串断言靠行51注释仍过）。

### 7.5 handle_answer.py — 纯函数化（对应9.1#5，步骤四）

> 新补diff。10处yield→收集列表→return dict；无等待可安全纯化（[5]5.1八分支照用；模块已git mv改名 — 小健-2026-09-05）。
> yield行：84/88/100/105/117/139/145/157/168/181（与9.1#5一致，已对盘工作区182行文件）。

```diff
 async def handle_answer(agent, parsed: Dict):
+    """纯函数化：10处yield改收集_events，末尾return {"events": _events, "type": parsed_type}。
+    paused/resumed/error类Step由调用方统一发射或落库（三章3.3价值2单写）。 — 小健-2026-09-05"""
+    _events = []
     if parsed is None:
         parsed = {}
     step = agent.llm_call_count
     parsed_type = parsed.get("type", "answer")
-        yield agent._step_emitter.emit(ThoughtStartStep(step=step))   # L84
-        for _s in agent._step_emitter.emit_failed_final(step=step, response="任务执行失败",
-            error_type=err_type, error_message=content):
-            yield _s                                                    # L88
+        _events.append(agent._step_emitter.emit(ThoughtStartStep(step=step)))
+        _events.extend(agent._step_emitter.emit_failed_final(step=step, response="任务执行失败",
+            error_type=err_type, error_message=content))
-        return                                                        # L89 → 副本
+        return {"events": _events, "type": parsed_type}
-        yield agent._step_emitter.emit(ThoughtStartStep(step=step))   # L100
-        for _s in agent._step_emitter.emit_failed_final(step=step, response="任务执行失败",
-            error_type="unknown_response", error_message=...):
-            yield _s                                                    # L105
+        _events.append(agent._step_emitter.emit(ThoughtStartStep(step=step)))
+        _events.extend(agent._step_emitter.emit_failed_final(step=step, response="任务执行失败",
+            error_type="unknown_response", error_message=...))
-        return                                                        # L106
+        return {"events": _events, "type": "unknown"}
-        yield agent._step_emitter.emit(MetaStep(type="retrying", step=step, ...))  # L117-123
-        return                                                        # L124
+        _events.append(agent._step_emitter.emit(MetaStep(type="retrying", step=step, ...)))
+        return {"events": _events, "type": parsed_type}
-        yield agent._step_emitter.emit(ThoughtStartStep(step=step))   # L139
-        for _s in agent._step_emitter.emit_failed_final(step=step, response="模型反复思考未产出有效结果，任务已终止（疑似陷入无效循环）", reasoning=_deduped):
-            yield _s                                                    # L145
+        _events.append(agent._step_emitter.emit(ThoughtStartStep(step=step)))
+        _events.extend(agent._step_emitter.emit_failed_final(step=step, response="模型反复思考未产出有效结果，任务已终止（疑似陷入无效循环）", reasoning=_deduped))
-        return                                                        # L146
+        return {"events": _events, "type": parsed_type}
-        yield agent._step_emitter.emit(ThoughtStep(step=step, content=_deduped, reasoning=""))  # L157-159
+        _events.append(agent._step_emitter.emit(ThoughtStep(step=step, content=_deduped, reasoning="")))
-        return                                                        # L163
+        return {"events": _events, "type": parsed_type}
-    yield agent._step_emitter.emit(ThoughtStartStep(step=step))       # L168
+    _events.append(agent._step_emitter.emit(ThoughtStartStep(step=step)))
-    for _s in agent._step_emitter.emit_completed_final(step=step, response=content, reasoning=reasoning):
-        yield _s                                                        # L181
+    _events.extend(agent._step_emitter.emit_completed_final(step=step, response=content, reasoning=reasoning))
     agent.message_builder.add_assistant_message(content)
+    return {"events": _events, "type": "answer"}
```
- 五个 `return` 全部改 `return {"events": _events, "type": ...}`；函数末尾补return（原隐式结束）。
- 副作用（note_progress / message_builder / logger）保留原位，时序零变。

### 7.6 handle_action.py — 纯函数化（对应9.1#6，步骤四）

> 新补diff。7处yield→收集列表→return dict（yield行299/309/310/323/337/373/383，与9.1#6一致 — 小健-2026-09-05）。

```diff
 async def handle_action(agent, parsed: Dict):
+    """纯函数化：7处yield改收集_events，末尾return {"events": _events, "result": {...}}。
+    check_safety_and_confirm 已纯函数化（7.4.2），此处由 async for 改普通 await。 — 小健-2026-09-05"""
+    _events = []
     set_current_task_id(agent.task_id)
     ...
-        yield agent._step_emitter.emit(MetaStep(step=step, type="error", ...))  # L299-301
-        return                                                               # L302
+        _events.append(agent._step_emitter.emit(MetaStep(step=step, type="error", ...)))
+        return {"events": _events, "result": {}}
-    yield agent._step_emitter.emit(ThoughtStartStep(step=step))               # L309
-    yield agent._step_emitter.emit(ThoughtStep(step=step, content=..., reasoning=...))  # L310-314
+    _events.append(agent._step_emitter.emit(ThoughtStartStep(step=step)))
+    _events.append(agent._step_emitter.emit(ThoughtStep(step=step, content=..., reasoning=...)))
-    async for event in check_safety_and_confirm(agent, call_result.all_calls, step,
-                                                call_result.fc_context,
-                                                _out=_safe_calls, _denied_out=_denied_list):
-        yield event                                                          # L320-323
+    _events.append(await check_safety_and_confirm(agent, call_result.all_calls, step,
+                                                  call_result.fc_context,
+                                                  _out=_safe_calls, _denied_out=_denied_list))
-        yield agent._step_emitter.emit(ActionStep(step=step, exec_type=..., tools=_action_tools))  # L337-341
+        _events.append(agent._step_emitter.emit(ActionStep(step=step, exec_type=..., tools=_action_tools)))
-    for event in events:                                                    # L372-373
-        yield event
+    _events.extend(events)
-        for _s in agent._step_emitter.emit_completed_final(step=step, response=orchestration.get("return_direct_message", "")):
-            yield _s                                                          # L380-383
+        _events.extend(agent._step_emitter.emit_completed_final(step=step, response=orchestration.get("return_direct_message", "")))
+    return {"events": _events, "result": {
+        "return_direct": orchestration.get("return_direct", False),
+        "tool_name": call_result.tool_name,
+        "params": call_result.tool_params}}
```
- 副作用（set_current_task_id / log_and_print / make_fp_callback / telemetry / _add_denial_feedback / note_progress）保留原位，时序零变。
- `check_safety_and_confirm` 现返回list（7.4.2），`_events.append(await ...)` 单层即可（勿用extend，其已返回完整list）。

### 7.7 react_dispatch.py — 驱动改await+dict消费（对应9.1#7，步骤五）

> 新补diff。_dispatch_handler 由 generator 驱动 handler 改普通 await 消费 dict；状态推断块逐行保留（5.7/6.x定稿 — 小健-2026-09-05）。

```diff
 async def _dispatch_handler(agent, llm_response):
     parsed_type = llm_response.get("type", "answer")
     ...
-    if parsed_type == "action":
-        handler = handle_action(agent, llm_response)
-    else:
-        handler = handle_answer(agent, llm_response)
-
     _EV_FINAL, _EV_RETRY, _EV_ERROR = "final", "retrying", "error"
     seen_types = set()
     last_error_event = None
     final_event = None
-    async for event in handler:
+    # 2026-09-05 小健: handler已纯函数化(7.5/7.6)，统一await取dict，逐条yield其Step事件
+    _result = (await handle_action(agent, llm_response)
+               if parsed_type == "action"
+               else await handle_answer(agent, llm_response))
+    for event in (_result.get("events", []) if isinstance(_result, dict) else _result):
         seen_types.add(event.type)
         if event.type == _EV_ERROR:
             last_error_event = event
         elif event.type == _EV_FINAL:
             final_event = event
         yield event
+    # 状态推断块（行71-123）逐行保留：set_status/set_failed/set_cancelled/set_completed + _deny_counts 归零
```
- 注意：被拒/超时的 error 事件已入 events；推断从 `last_error_event`（MetaStep.error）读 error_type/get_content，规则不变。
- `event.type` 字段访问维持Step对象（7.5/7.6返回的events为Step对象），与推断现状遥一致。

### 7.8 llm_call.py — call_llm_with_fallback 改yield StreamChunk（对应9.1#8，步骤五）

> 新补diff。出口由 (`"meta"`/`"chunk"`/`"response"` 元组) 改 yield StreamChunk（字段见llm/core.py行84-112，均已核实存在 — 小健-2026-09-05）。[5]5.5.2消费侧改造照用，现消费在 react_step.py 行197-209。

```diff
 # call_llm_stream（llm_call.py行67-206）：出口改 StreamChunk — 小健-2026-09-05
-                yield ("meta", {
-                    "type": "retrying",
-                    "content": f"LLM请求重试 {chunk.retry_attempt}/{chunk.retry_total}: {chunk.retry_notice}",
-                    "wait_time": None,
-                })                                       # 行90-94
+                yield chunk                             # StreamChunk直透(L1 retry_notice标记携带)
...
-                yield ("chunk", ChunkStep(step=agent.llm_call_count, content=chunk.content, is_reasoning=is_reasoning))  # 行111
+                # 累积(full_content/full_reasoning/tool_calls_result)保留在本函数内(行108-110原样)，
+                # 但不再逐chunk发元组——StreamChunk直透，内容块由消费侧(react_step L211-223)构建ChunkStep
+                yield chunk
...
-    yield _build_tool_calls_response(full_content, tool_calls_result, usage_data, agent, full_reasoning)  # 行152
-    yield _build_answer_response(full_content, full_reasoning, usage_data, agent, _finish_reason)          # 行206
+    yield _build_tool_calls_response(full_content, tool_calls_result, usage_data, agent, full_reasoning)  # 终态response改为单条is_done=True StreamChunk
+    yield _build_answer_response(full_content, full_reasoning, usage_data, agent, _finish_reason)
```
- `_build_tool_calls_response`/`_build_answer_response` 返回dict，出口处包装为 `StreamChunk(content="", chunk_model=agent.llm_client.llm_model, is_done=True, raw_data=json.dumps(resp, ensure_ascii=False))`，消费侧读 `chunk.raw_data` 重建dict（或直接透传dict类型字段，见下方消费侧）。
```diff
 # call_llm_with_fallback（llm_call.py行209-284）：分支判断改 StreamChunk 字段 — 小健-2026-09-05
     for attempt in range(LLM_RESPONSE_RETRIES):
         try:
             async for item in call_llm_stream(agent, messages, openai_tools):
-                if isinstance(item, tuple) and item[0] == "response":
-                    resp = item[1]
+                if isinstance(item, StreamChunk) and item.is_done and not item.tool_calls:
+                    resp = item.response_dict if hasattr(item, "response_dict") else {}
                     if isinstance(resp, dict) and resp.get("type") == "error":
                         err_type = resp.get("error_type", "")
                         if err_type in ("quota_exceeded", "rate_limit", "idle_timeout", "client"):
                             yield item
                             return
                         raise LLMResponseError(message=resp.get("content", "LLM流式错误"))
-                if isinstance(item, tuple) and item[0] == "meta":
-                    _meta = item[1]
-                    if isinstance(_meta, dict) and _meta.get("type") == "retrying":
+                if isinstance(item, StreamChunk) and item.retry_notice:
+                    if getattr(item, "retry_total", LLM_RESPONSE_RETRIES) > 1:
                         _pending_retry_notice = item
                 yield item
                 _pending_retry_notice = None
             return
```
- 消费侧 react_step.py 行197-209 同步改（[5]5.5.2）：
```python
    # 2026-09-05 小健: call_llm_with_fallback已yield StreamChunk，去掉元组拆包 — 更新为StreamChunk消费
    async for chunk in call_llm_with_fallback(agent, messages, openai_tools):
        if getattr(chunk, "stream_error", None):
            chunk_buffer.clear()
            break
        if isinstance(chunk, StreamChunk) and chunk.retry_notice:
            yield agent._step_emitter.emit(MetaStep(
                type="retrying", step=agent.llm_call_count,
                content=f"LLM请求重试 {chunk.retry_attempt}/{chunk.retry_total}: {chunk.retry_notice}",
                severity="info", wait_time=None))
            continue
        if chunk.content:
            chunk_buffer.append(chunk.content)
            if not _first_token_marked:
                agent.telemetry.mark_first_token()
                _first_token_marked = True
            is_reasoning = getattr(chunk, "is_reasoning", False)
            yield agent._step_emitter.emit(ChunkStep(
                step=agent.llm_call_count, content=chunk.content, is_reasoning=is_reasoning))
        if getattr(chunk, "is_done", False):
            if getattr(chunk, "usage", None):
                usage_data = chunk.usage
            _finish_reason = getattr(chunk, "finish_reason", None)
            break
    if tool_calls_result:
        llm_response = ...   # 终态组装复用 llm_response_builder（行53-58 import）
    else:
        llm_response = ...
```
- 累积变量（full_content/full_reasoning/tool_calls_result/usage_data/_finish_reason）与终态组装随消费侧迁移（见[5]5.5.2 行987-1061），`StreamChunk` 定义在 `app.llm.core`。
- 测试既有断言（test模式调用回退`_make_chunk`）不破：chunk直透后类型判别仍走 `isinstance(chunk, StreamChunk)`（行87/61同源）。

---

## 八、与[5]v1.6的关系（8.4/8.6拆分后锚点更新 — 小健-2026-09-05）

- [5]实施暂停（v1.6停在设计评审）。
- 网关落地后：本文一/1.2为关系概述；[5]的5.2/5.3/5.4按网关API重写（输出参数方案作废对应节，以本文五、六为准）；[5]的5.1/5.5/5.6结论保留；5.7按[6]4.2语义修正（历史走loader，转发并orchestrator）。
- [5]的一/三/四章架构结论（3层、consumer关系、66/54口径）不受影响。
- [5]引用的`react_cycle.py:288-389`现居`react_dispatch.py`（_dispatch_handler整段迁入）；`react_cycle.py:439-465`现居`react_step.py`；主循环现居`react_loop.py`；`stream_reader.py`已并入`stream_orchestrator.py`（stream_reader函数）。[5]实施前须先对盘新文件行号。

## 九、实施顺序（先实施，每步独立验证）

```
步骤一：StreamBuffer.publish（三章，1天）
  验证：publish即时可见（seq递增，cond唤醒），无双发
步骤二：hitl_gateway.py + 单测（2天）
  验证：confirm/timeout/cancel三语义；paused先于wait到达（单测断言顺序）
步骤三：sandbox切流（1天）
  sandbox_resolve改纯函数调网关（sandbox②场景needs_ruling+非bypass也调网关，见4.4 L2修正）；check四站点删steps plumbing
  验证：E2E沙箱裁决弹窗时序 + 拒绝/确认路径
步骤四：check/handle_answer纯函数化（2天）
  验证：HITL三处时序 + 467用例回归
步骤五：react _dispatch收编 + llm_call出口（2天；llm_stream.py已改名llm_call.py — 小健-2026-09-05）
  验证：46单测 + E2E全链路
```

### 9.1 修改/新增代码清单（以工作区最新代码为准 — 小健-2026-09-05）

| # | 文件 | 新增/修改 | 内容 | 对应diff |
|---|------|----------|------|---------|
| 1 | backend/app/services/task/hitl_gateway.py | 新增 | ConfirmSpec + hitl_confirm + _resolve_trust_path/_desensitize/_resolve_timeouts | 7.1 |
| 2 | backend/app/services/task/task_state.py | 修改 | StreamBuffer.publish生产者直写入口 | 7.2 |
| 3 | backend/app/services/agent/handlers/sandbox_gate.py | 修改 | sandbox_resolve改纯函数调网关；删114/118翻转 | 7.3 |
| 4 | backend/app/services/agent/handlers/safety_gate.py | 修改 | 3处（141/205/226）组spec调网关；删set_status 133/156/177/194；删sniff 146/210；删steps plumbing | 7.4 |
| 5 | backend/app/services/agent/handlers/handle_answer.py | 修改 | 纯函数化（10处yield→return dict，行84/88/100/105/117/139/145/157/168/181） | 7.5 |
| 6 | backend/app/services/agent/handlers/handle_action.py | 修改 | 纯函数化（7处yield→return dict，行299/309/310/323/337/373/383） | 7.6 |
| 7 | backend/app/services/agent/react_dispatch.py | 修改 | 驱动改dict+verdict拿结果，状态推断保留 | 7.7 |
| 8 | backend/app/services/agent/llm_call.py | 修改 | call_llm_with_fallback（现居209行）改yield StreamChunk | 7.8 |

### 9.2 实现顺序步骤表（代码×测试双挂 — 小健-2026-09-05）

| 步骤 | 代码（9.1编号） | 前置 | 测试门（先红后绿，见10.1） | 验证 |
|------|---------------|------|--------------------------|------|
| 一 publish（1天） | #2 | 无 | T1红→绿 | publish即时可见（seq递增，cond唤醒），无双发 |
| 二 hitl_gateway.py+单测（2天） | #1 | 一 | T2红→绿 | confirm/timeout/cancel三语义；paused先于wait到达 |
| 三 sandbox切流（1天） | #3+#4部分 | 二 | T3a+T3b红→绿+E2E沙箱 | 弹窗时序+拒绝/确认路径 |
| 四 check/handle_answer纯函数化（2天） | #4+#5+#6 | 二 | T4a+T4b红→绿+467回归 | HITL三处时序 |
| 五 react收编+llm_call出口（2天） | #7+#8 | 四 | T5红→绿+46单测+E2E | 全链路 |

注：步骤三原文"check四站点"系旧文，现代码为三处run_sandbox_gate（safety_gate行141/205/226），按现代码为准。

### 9.3 TDD实施步骤（测试先行铁律 — 小健-2026-09-05）

> 体例沿[8]10.3：一步一commit，只含生产文件，测试文件禁入commit；backup/落备份。每步走完五步才进下一步：
> **TEST红→BACKUP→CODE→TEST绿→COMMIT**。TEST红指：先新增/修改10.1对应测试用例，跑出红灯（锁定新地址/新语义），**红灯未现不许CODE**（用户铁律第5条）。

| 步 | TEST红（先行） | BACKUP | CODE | TEST绿 | COMMIT |
|---|--------------|--------|------|--------|--------|
| 一 | tests/test_stream_publish.py新增，publish不存在→红 | task_state.py→backup/ | 7.2落盘 | T1绿+既有用例无回归 | 生产文件单commit |
| 二 | tests/test_hitl_gateway.py新增，三原语import失败→红 | —（新文件） | 7.1落盘 | T2绿 | 生产文件单commit |
| 三 | test_sandbox_gate.py加verdict断言→红；test_action_handler_edge_cases.py加网关调用断言→红 | sandbox_gate.py+safety_gate.py→backup/ | #3+#4部分落盘 | T3a+T3b绿+E2E沙箱 | 生产文件单commit |
| 四 | answer三件回归基线先跑绿存档；加纯函数断言→红 | safety_gate.py+handle_answer.py+handle_action.py→backup/ | #4+#5+#6落盘 | T4a+T4b绿+467回归 | 生产文件单commit |
| 五 | test_react_cycle_split_regression.py加dict断言→红 | react_dispatch.py+llm_call.py→backup/ | #7+#8落盘 | T5绿+46单测+E2E | 生产文件单commit |


## 十、风险与测试

| 风险 | 缓解 |
|------|------|
| publish与yield双发 | 单写（三章3.3价值2）：暂停事件只走publish、普通事件只走yield，同事件绝不双发；回归断言event_log无同seq |
| 网关阻塞卡死事件循环 | 复用wait既有cancel轮询+超时；60s看门狗沿用stream侧 |
| 前端弹窗协议变化 | paused字段全量保留（含四字段Bug-16），key名不变 |
| bypass倒计时错位 | 超时/mode收进网关统一计算（见4.2），调用方只传mode，不再透传三字段 |
| 任务暂停通道干扰 | 网关不管任务暂停通道；两通道同形事件，前端已兼容；_pause_core已对盘（task_runtime:109，emit_sse开关，wait_for_resume纯阻塞），待核销 — 小健-2026-09-05 |
| 拒绝/超时error事件无confirm_id | 现状主路/sandbox拒绝路径error均无confirm_id；前端拒绝关弹窗协议实施前与前端确认（主路resumed带confirm_id；沙箱resumed缺confirm_id属发现B，已落盘ISS-002，见4.4/7.3.1）— 小健-2026-09-05 |
| bypass路径双重沙箱裁决（发现C） | 旧双调用已删（ISS-001落盘，现safety_gate行141唯一gate）；历史diff见7.4.1 — 小健-2026-09-05 |

### 10.1 新增/修改测试case清单（TDD先行，红灯锁定后才许CODE — 小健-2026-09-05）

| 编号 | 用例文件 | 新增/修改 | 覆盖点 | TDD红断言 | 对应步骤 |
|------|---------|----------|--------|----------|---------|
| T1 | tests/test_stream_publish.py | 新增 | publish seq递增/cond唤醒/无双发/dict拷贝隔离/返回值seq | publish不存在→红 | 一 |
| T2 | tests/test_hitl_gateway.py | 新增 | confirm/timeout/cancel三语义；paused先于wait顺序断言；verdict四键；bypass auto_confirm推导；mode非法值ValueError | hitl_gateway不存在→红 | 二 |
| T3a | tests/test_sandbox_gate.py | 修改 | resolve verdict路径；114/118翻转删除不断链；resumed带confirm_id（ISS-002） | 新增verdict断言→红 | 三 |
| T3b | tests/test_action_handler_edge_cases.py | 修改 | 网关调用点verdict驱动；resumed仍sandbox之后（test_resumed_after_sandbox既有断言仍过） | 新增调用断言→红 | 三 |
| T4a | tests/test_answer_handler_edge_cases.py | 沿用保绿 | handle_answer纯函数化无回归（10处yield语义等价） | 既有绿存档 | 四 |
| T4b | tests/test_reasoning_only_defense.py | 沿用保绿 | 计数/防空转语义无回归 | 既有绿存档 | 四 |
| T5 | tests/test_react_cycle_split_regression.py | 修改 | dispatch改dict后状态推断（deny计数/终态规则）不变 | 新增dict断言→红 | 五 |
| TE | E2E bypass/HITL各一 | 新增 | 弹窗时序+拒绝/确认全路径（sandbox②场景双覆盖） | —（TEST绿后跑） | 三/五 |

铁律：以上测试文件只验证用、禁入commit（沿[8]10.3体例）；每步TEST绿含"既有用例无回归"（步骤四467回归/步骤五46单测为硬门）。
**更新时间**: 2026-09-05 14:49:00
**更新人**: 小健-2026-09-05