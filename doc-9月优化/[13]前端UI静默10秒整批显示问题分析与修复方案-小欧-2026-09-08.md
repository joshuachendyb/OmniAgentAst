# 前端UI静默10秒整批显示问题分析与修复方案

- **文档名称**: 前端UI静默10秒整批显示问题分析与修复方案-小欧-2026-09-08
- **编写人**: 小欧
- **创建时间**: 2026-09-08 14:46:24
- **更新时间**: 2026-09-08 16:50:17
- **文档性质**: 分析 + 修复方案
- **版本**: v1.2

> **版本历史**
> | 版本 | 时间 | 作者 | 说明 |
> |------|------|------|------|
> | v1.0 | 2026-09-08 14:46:24 | 小欧 | 初版：北京老陈 2026-09-08 13:27 报告「后端事件实时但前端画面长时冻结后突然整批显示」。前端全链路代码逐行还原，定位 `useTaskInfo.badge` 每次重算从 `idle` 起步、startinfo 门被 `receiving` 压回 `idle`，致 `RightViewer.isCurrentLive` 翻 false → `streaming=false` 停齿轮 + `displaySteps` 切历史视图 → 实时步骤静默压栈，重连后整批回放。给出 KISS 一行修复（待老陈批准后落码）。 |
> | v1.1 | 2026-09-08 16:42:17 | 小欧 | 补充：北京老陈追问“之前分析解决是否彻底、类似问题是否还会出现”。经实码复核，v1.0 修对显示链但触发侧未动，续写 §八（三类复发窗口实码核对）+ §九（结论修正与后续立项建议）。原 §一~§七一字未动。 |
> | v1.2 | 2026-09-08 16:50:17 | 小欧 | 补硬货：北京老陈要求“切实可行的修改方法 + 真实代码 diff”。续写 §十（三个修复的完整 diff，均已按落码标准写死行号与编辑历史注释，批即能落）。原 §一~§九一字未动。 |

---

## 一、背景

1. 北京老陈 2026-09-08 13:27 时段实测：**后端事件实时到达**（日志逐条刷新），但**前端右栏画面长时间冻结（约 10s），随后突然整批显示**多段内容，此前伴随的"工具执行中"齿轮动画未出现。
2. 老陈强令（历史多次）：**必须先查前端实现再下结论，禁止只凭后端 console 判前端无问题**。本报告完全走后端证据 + 前端代码静态追踪双线，前端部分为本次定位主责。
3. 本报告先给结论（根因已证死），再给修复方案；运行期佐证（浏览器 console 断连标记、后端 13:27 日志窗口）留待 §六 补验，不阻塞定性。

---

## 二、前端数据链路还原（action 到达 → 齿轮动画 → 结果渲染）

实时路径（逐环节已验证，均即时、无节流、无延迟）：

```
SSE action 事件
 -> sseParser.ts case action  setExecutionSteps + onStep      // 即时
 -> useChatStreaming.executionSteps 状态更新                  // 即时
 -> RightViewer.tsx  liveSteps={executionSteps}               // useChatPanels.tsx:237
 -> displaySteps = isCurrentLive ? liveSteps : historySteps   // RightViewer.tsx:266
 -> PipelineRenderer steps={splitSteps(displaySteps).business} streaming={isCurrentLive}  // RightViewer.tsx:286-288
 -> tool segment 立即生成                                     // PipelineRenderer.tsx:196-201
 -> ToolCallLine 齿轮动画条件                                 // ToolCallLine.tsx:230
```

关键门一：**齿轮动画条件**（ToolCallLine.tsx:230）

```ts
{!hasResult && tools.length > 0 && !interrupted && !replay && (
  <span className="tool-waiting-cursor" ...>  {/* 齿轮/loader SVG */}
)}
// we 2026-09-08 小欧：replay 由父级传入
```

其中 `replay`（PipelineRenderer.tsx:393 一带）：
```ts
replay={!streaming}
```
> 即 `streaming(false 历史回放)` → `replay=true` → **停齿轮**；`streaming(true 实时)` → `replay=false` → **显示齿轮**。正常实时任务应转齿轮。

关键门二：**实时判定 isCurrentLive**（RightViewer.tsx:125-129）

```ts
const isCurrentLive =
  activeTaskId != null &&
  activeTaskId === serverTaskId &&
  !_hasFinal &&
  (receiving || liveBadge === 'running' || liveBadge === 'paused');
```
> `isCurrentLive=true` 才走 `liveSteps`（实时实时）并传 `streaming=true`（齿轮转）。**只要它翻 false**：
> - `streaming={false}` → 全流水线 `replay=true` → **齿轮停转**；
> - `displaySteps = historySteps`（RightViewer.tsx:266）→ 实时步骤静默压进 `liveSteps` 但 `historySteps` 不长（任务未落库/未完成），右栏切空历史/空态；
> - REST effect 同时触发（RightViewer.tsx:219-230），但任务执行中拉回的步骤不全，气泡此消彼长 = **画面"冻结"**；
> - SSE 恢复 → `isCurrentLive` 转 true → `displaySteps` 切回 `liveSteps`，含缓冲的全部步骤**一次性渲染 = "整批显示"**。

**结论：`isCurrentLive` 在执行间隙翻 false = 症状的直接开关。**

---

## 三、isCurrentLive 为何会翻 false（根因定位）

`isCurrentLive` 的 OR 三态 `(receiving || running || paused)`，任一为真即稳。翻 false 只剩唯一路径：**`receiving=false` 且 `liveBadge` 落回 `idle`**。逐层证死：

### 3.1 badge 每次重算从 idle 起步

useTaskInfo.ts:124
```ts
let badge: TaskBadge = 'idle';
```

### 3.2 thought/action/observation 分支"只救 paused/failed，不救 idle"

useTaskInfo.ts:179-187
```ts
case 'thought':
case 'action':
case 'observation':
  if (badge === 'paused') badge = 'running';
  if (badge === 'failed') { badge = 'running'; _badgeRecovered = true; }
  break;
```
> 任务执行中的业务 step 已存在 `liveSteps` 里，但本分支**从不把 idle 顶成 running**——业务 step 证明执行中这一语义没有落到代码。

### 3.3 startinfo 门每次重算都被 receiving 压回 idle

useTaskInfo.ts:207-208
```ts
if (hasStartInfo && badge === 'idle')
  badge = receiving ? 'running' : 'idle';
```
> 本门每次重算都执行。只要 `badge` 此刻为 idle（含 3.2 从未被顶起的情况）且 `receiving=false`，**badge 恒被压回 idle**，无视 `liveSteps` 里已有多少 thought/action/observation。

### 3.4 receiving=false 的触发源（SSE 空闲断连/请求错误）

useSSE.ts：
- :631 重连路径 `disconnect(..., setReceiving=false)` —— 重连**不落 false**；
- :853 / :879 真错误路径 `setIsReceiving(false)` —— **此路径 receiving=false**；
- :724-745 空闲超时（`IDLE_TIMEOUT`，历史档 60s）→ `reconnect()`；若连接已僵尸，错误分类走 :862 `handleSSEError` → :879 `setIsReceiving(false)`。

> 即：任务执行中的 SSE 静默窗（工具执行 10-60s、网络抖动、proxy 断开）一旦触发真错误/重连，`receiving` 即 false。

### 3.5 三环节串联 = 翻车路径

```
工具执行静默窗 → SSE 真 error/重连 → receiving=false
 → useTaskInfo 重算: badge 从 idle 起步(§3.1) → 业务 step 分支不顶起(§3.2)
 → startinfo 门按 receiving 压回 idle(§3.3)
 → liveBadge='idle'
 → isCurrentLive = ... && (false || false || false) = false   // RightViewer.tsx:129
 → streaming=false(停齿轮) + displaySteps=historySteps(空/不全) → UI 冻结
 → SSE 恢复 receiving=true → badge='running' → isCurrentLive=true
 → displaySteps=liveSteps(含缓冲全体) → 整批显示(齿轮全程未转)
```

---

## 四、为何是"修复被击穿"（历史对照）

RightViewer.tsx 编辑历史 :37-38（2026-09-02 小欧）明确根治过同扇窗：

> 「根治纯网络空闲断连(无paused)60s重连间隙badge=idle致waiting消失的第4窗口」，修复手段：`isCurrentLive` 由单 `receiving` 改三态并集 `(receiving || running || paused)`。

**击穿点**：当时只改了 `isCurrentLive` 的门，未封 `liveBadge` 的派生源。三个 OR 态里：
- `receiving` 会被真错误置 false（useSSE.ts:853/879）；
- `paused` 仅在步骤流含 paused 事件时成立；
- 于是唯一稳定兜底 = `running`，而 `running` 恰恰在 §3.2/§3.3 的挟持下随 `receiving=false` 一起落 `idle`——**三态并集被同时清零**，第二扇门形同虚设。

即：2026-09-02 的修复封了门，没封源（badge 派生），13:27 症状是该历史缺陷在真断连窗口下的复发。

---

## 五、修复方案（KISS，待批准）

### 5.1 目标语义

> 业务 step（thought/action/observation）到达 = 任务执行中的**铁证**，优先级高于连接状态。任务已开始且有业务 step、且未见终态 final 时，badge 不得因 `receiving=false` 落回 idle。

### 5.2 改法（单文件 useTaskInfo.ts，一行 + 同步注释，无第二处改动）

useTaskInfo.ts:179-187 `thought/action/observation` 分支补齐 idle 恢复：

```ts
// 编辑历史: 2026-09-08 小欧 - 「前端UI静默10秒整批显示」修复(北京老陈 2026-09-08 批准实施, 文档:
//   doc-9月优化/前端UI静默10秒整批显示问题分析与修复方案-小欧-2026-09-08.md):
//   业务 step(thought/action/observation)到达即证任务执行中, 补 idle→running 恢复,
//   防 SSE receiving=false 窗内 startinfo 门(207-208)把 badge 压回 idle,
//   致 RightViewer.isCurrentLive 翻 false(停齿轮+切历史视图+整批回放)——三态并集修复(2026-09-02)被击穿根因 — 小欧-2026-09-08
case 'thought':
case 'action':
case 'observation':
  if (badge === 'idle') badge = 'running';
  if (badge === 'paused') badge = 'running';
  if (badge === 'failed') {
    badge = 'running';
    _badgeRecovered = true;
  }
  break;
```

### 5.3 三堂会审（合规 / 合理 / 关联逻辑）

合规：
- SRP：职责仍是"badge 派生"，本次只补一条证明路径，不动其它推导；
- DRY：与既有 `paused→running`/`failed→running` 恢复完全同型，不造新门，不重复逻辑；
- KISS：一行 + 注释，无中间层、无新配置。

合理：
- 逻辑直线：业务 step 到达 → running，比"连接状态证明执行中"更直接更真；
- 无副作用绕路，不引入新状态。

关联逻辑（重点验退）：
- **B33 语义**：`startinfo` 未到且无业务 step 时 badge 仍 `idle`（本分支不起作用），`startinfo` 到但无 step 仍按 `receiving` 决定——执行起点语义不变；
- **终态**：final 为步骤流末尾，case 'final' 覆盖 badge 为 completed/failed/cancelled，本分支在其前已执行完毕无覆盖权；`hasFailedFinal`（useTaskInfo.ts:77-79）单源兜底 failed 不变；
- **切历史任务**：`activeTaskId ≠ serverTaskId` 时 `isCurrentLive` 恒 false（RightViewer.tsx:126-128 硬前提），本改动不改该前提，历史任务跳动不受影响；
- **任务结束沿**：`_hasFinal`（RightViewer.tsx:120）已挡终态 live 卡位，badge 多立在 running 不会导致视图霸屏。

### 5.4 验收口径

修复后 13:27 同类场景期望行为：
1. action 到达即时渲染 ToolCallLine，齿轮（loader）**立即转**，全程不消失；
2. 工具执行静默窗（receiving=false）期间右栏仍显示 liveSteps（action 行 + 齿轮），**不切空历史、不冻结**；
3. SSE 恢复后 observation 到达，齿轮卸载、子行与结果按既有逻辑渲染，**无整批回放跳变**；
4. 全量回归：终态 / 切历史 / HITL 被拒停齿轮三路径无退化。

---

## 六、运行期佐证（待补验，不阻塞定性）

1. **浏览器 console**：13:27 时段是否打印 `[SSE] 空闲超时触发上下文`（useSSE.ts:734，含 `timeSinceLastData`/`receiving`）或 `[SSE] 请求错误`——用于坐实 `receiving=false` 窗口长度（预计 ≥ 60s 空闲或一真 error）。
2. **后端 13:27 日志**：event 流 `seq` 时间戳是否出现 ≥10s 空洞（LLM 工具执行期），与前端冻结窗口对齐。
3. **后端空闲保活**：若确认窗口由 60s IDLE_TIMEOUT 实测触发，后续另有后端心跳/keep-alive 优化项（本次未含，单独立项评估）。

---

## 七、结论

- 根因：`useTaskInfo.badge` 每次重算从 `idle` 起步，`thought/action/observation` 分支不把 idle 顶起，startinfo 门又随 `receiving=false` 压回 idle → `RightViewer.isCurrentLive` 在 SSE 静默/断连窗翻 false → 停齿轮 + 切历史视图 + 实时步骤静默压栈，重连后整批回放。
- 与 2026-09-02「等待圈第 4 窗口」修复同源，属该修复被击穿后的复发。
- 修复方案为单文件一行（useTaskInfo.ts:179-187 补 `idle→running`），已过三堂会审，**待北京老陈批准后落码**（批准后：改码 + 加编辑历史 + 前端 `npm run check`，不 tag）。

---

## 八、复发窗口补充分析（2026-09-08 16:42:17 小欧补）

> 背景：北京老陈 2026-09-08 16:26 追问“之前分析解决是否彻底、类似问题是否还会出现”。以下逐窗实码核对，结论：v1.0 修对显示链，触发侧未动，三类窗口仍可复发同类症状。

### 8.1 窗口一：首个业务 step 到达前的起步间隙（仍冻结）

链路（useTaskInfo.ts:150-227）：该窗口 `steps` 为空（无 thought/action/observation），`for` 循环无分支命中，`badge` 保持初值 `idle`（:133）；随后 startinfo 门（:226-227）`hasStartInfo && badge==='idle'` 按 `receiving` 派生——`receiving=false` 即压回 `idle`。v1.0 的 `idle→running` 补丁位于循环内业务分支，空 steps 时根本不执行，盖不住本窗。

后果（RightViewer.tsx:125-129, 266, 271）：`liveBadge='idle'` + `receiving=false` → `isCurrentLive=false` → `displaySteps=historySteps`（空）→ 右栏 `Empty` + 顶栏徽标 `idle`。若 LLM 首 token 本就慢（10s 级工具首调）又叠加一次真断连，用户看到的仍是“长时间空白后突然出内容”，与 13:27 症状同构，只是发生在任务起步段。

### 8.2 窗口二：SSE 真死不断时的反向掩盖（冻结变空转）

链路（useTaskInfo.ts:200 + PipelineRenderer/ToolCallLine 门）：v1.0 之后，只要有一个业务 step 到过，`badge` 即锁 `running`，不再跟随 `receiving`。若 SSE 实际已死且永不恢复，`isCurrentLive` 恒 true，`streaming=true` → `replay=false` → 齿轮（`tool-waiting-cursor`，ToolCallLine.tsx:230）永久空转。

后果：原来是 fail 到 `idle`（冻结可见），现在是 fail 到 `running`（断连不可见）。且 `receiving` 仍直驱他处：TaskInfoBar 计时停表（TaskInfoBar.tsx:82 `receiving && !detail` 才走表）、B16 结束沿刷新（RightViewer.tsx:252-264）照常触发——同一断连下“齿轮说活着、秒表说死了”，状态自相矛盾。v1.0 把显示开关修成了常开，代价是丢了断连可见性。

### 8.3 窗口三：触发归因本身存疑（60s 空闲对不上 10s 冻结）

v1.0 §3.4 把 `receiving=false` 归于 60s `IDLE_TIMEOUT`（useSSE.ts:439, 727-745）或真错误（:852-853）。但老陈实测冻结“约 10s”，60s 空闲窗对不上 10s 症状；更可能是真错误路径（:852-853）或重连抖动。§六的三项实证（console 空闲打点 `timeSinceLastData`、后端 seq 空洞对齐、心跳评估）至今未补——触发源未坐实，属“显示链定性、触发链待证”。

### 8.4 task_id 错位硬前提（设计如此，不属 bug，列明免误判）

`isCurrentLive` 第一硬前提 `activeTaskId === serverTaskId`（RightViewer.tsx:126-127）不受 badge 影响。切历史任务、或重连后 `serverTaskId` 未及时对齐时切 `historySteps` 是正确语义，不得按“冻结”修，否则串任务。本窗不列入修复范围，特此划界。

---

## 九、结论修正与后续立项建议（2026-09-08 16:42:17 小欧补）

1. **修正结论**：v1.0 根因（badge 派生洞）证死且修对，13:27 原路径不再复发；但属“显示链根治、触发链未动”，§八三类窗口仍可产出同类症状——整体评价：**修对一半**。
2. **建议立项一（取证）**：补 v1.0 §六三项实证，坐实 10s 窗的触发源（重点查真错误 :852-853 而非先入为主 60s 空闲）。
3. **建议立项二（根治触发）**：后端心跳/keep-alive + 前端明确 `reconnecting` 态（徽标/齿轮 distinct 于 running），断连可见而不是用 badge 常开掩盖；首 step 前窗口（§8.1）可按“startinfo 已到 + activeTask 对齐 + 未见终态 + 短时（如 120s）”给 `running`，与 §8.4 划界共存。
4. **验收口径补**：除 v1.0 §5.4 四条外，加两条——起步断连窗不切空历史（或明确 Empty 文案提示重连中）；SSE 永死 60s+ 时齿轮必须停并显错，不得空转。—— 小欧-2026-09-08

---

## 十、切实可行的修改方法 + 真实代码 diff（2026-09-08 16:50:17 小欧补）

> 以下三个 diff 均按落码标准写死：文件、行锚、编辑历史注释（含署名+日期）齐全，北京老陈批准后逐个落码 + `npm run check` + 单文件回归即合入。互不依赖，可分批批。

### 10.1 修复一：起步间隙冻结（§8.1）—— startinfo 门加 120s 新鲜度兜底

文件：`frontend/src/features/chat/hooks/useTaskInfo.ts`，锚点 :224-227。

改前（现状）：

```ts
    // ② startinfo 帧 -> "任务已开始"过程条首行 + 执行中徽标（B33：有帧才亮）
    // startinfo 仅存在于 metaFrames（8.4.3），时间戳取 start 事件的 startTimestamp
    if (hasStartInfo && badge === 'idle')
      badge = receiving ? 'running' : 'idle';
```

改后（diff）：

```diff
     // ② startinfo 帧 -> "任务已开始"过程条首行 + 执行中徽标（B33：有帧才亮）
     // startinfo 仅存在于 metaFrames（8.4.3），时间戳取 start 事件的 startTimestamp
-    if (hasStartInfo && badge === 'idle')
-      badge = receiving ? 'running' : 'idle';
+    if (hasStartInfo && badge === 'idle') {
+      if (receiving) {
+        badge = 'running';
+      } else {
+        // 2026-09-08 小欧 §八窗口一起步间隙修复(文档:
+        //   doc-9月优化/前端UI静默10秒整批显示问题分析与修复方案-小欧-2026-09-08.md §8.1):
+        //   startinfo 已到、无终态、起步 120s 内，即使 receiving=false（断连窗）也判 running，
+        //   防 RightViewer.isCurrentLive 翻 false 切空历史（首 step 前冻结）；
+        //   超 120s 仍无业务 step 则回 idle——真死不断不永久掩盖 — 小欧-2026-09-08
+        const startedAt = frames.startTimestamp || 0;
+        const fresh = startedAt > 0 && Date.now() - startedAt < 120000;
+        const noFinal = !steps.some((s) => s.type === 'final');
+        badge = fresh && noFinal && !hasFailedFinal ? 'running' : 'idle';
+      }
+    }
```

依据（均为实码，不拍脑袋）：
- `frames.startTimestamp` 在 start 事件到达时写入（sseParser case 'start'，`startTimestamp: Date.now()`），起步锚点真实存在；
- `steps`/`frames`/`hasFailedFinal`（:86）均在同一 `useMemo` 作用域内，无需新参数、无跨文件改动；
- B33 不破：仍要求 `hasStartInfo` 先到才亮灯；终态不破：`final`/`hasFailedFinal` 直接判 `idle`，`case 'final'`（:182-186）照常覆盖；
- 120s 界：覆盖正常工具静默窗（10-60s），超限回 `idle` 不替真死不断背书。

回归：`useTaskInfo` 相关单测 + 首 step 前断连手工窗（start 后拔网 20s 再恢复，右栏应保持 live 不切空）。

### 10.2 修复二：真死不断的断连可见（§8.2）—— live 且断流时显“重连中…”

文件：`frontend/src/features/chat/components/taskinfo/TaskInfoBar.tsx`，锚点 :136（`<Badge>` 行）。

改前（现状）：

```tsx
          <Badge status={b.status} text={b.text} />
```

改后（diff）：

```diff
           <Badge status={b.status} text={b.text} />
+          {/* 2026-09-08 小欧 §八窗口二断连可见(文档同上 §8.2):
+              live 且 SSE 已断时显"重连中…"，防 badge 常开掩盖真断连
+              （齿轮说活着、秒表说死了的自相矛盾）；仅提示零逻辑，不碰派生 — 小欧-2026-09-08 */}
+          {!receiving && !detail && info.badge === 'running' && (
+            <span style={{ fontSize: 12, color: Colors.WARNING }}>
+              · 重连中…
+            </span>
+          )}
```

依据：`receiving`/`detail` 均为本组件 props（:66-67），`info.badge` 本文件已算出（:72），`Colors.WARNING` 本文件已用（:152），零新依赖；条件 `!detail` 把历史回放排除在外，只管实时流。

回归：正常任务全程不断流时该提示永不出现（既有快照/单测零改动）；拔网 60s+ 时徽标旁出“重连中…”，恢复后消失。

### 10.3 触发侧（§8.3）：后端心跳已存在，不新增改动，先取证再定点

实码核对结论（2026-09-08 小欧）：后端 `stream_reader` 已有 25s 心跳保活（`stream_orchestrator.py:459-470`，`yield ": ping\n"`，主链路与重连链路共用同一函数），与前端 `IDLE_TIMEOUT=60000`（`useSSE.ts:439`）错开；前端解析器对非 `data: ` 前缀行直接 return（`sseParser.ts:114-118`），心跳零业务副作用，且网络块到达即刷新 `lastDataTimeRef`（`useSSE.ts:747`）。即：**60s 空闲路径已有心跳堵死**，13:27 的 10s 窗最大嫌疑是真错误路径（`useSSE.ts:852-853`）或代理缓冲，而非空闲超时——此时再加新心跳/改超时阈值属扩散性改动，不做。

切实可行的三条取证命令（定位后再定点修）：

```powershell
# ① 后端：13:27 窗内该任务的心跳是否停发（停发=后端/真断连，续发=代理缓冲或前端丢）
Select-String -Path "backend\logs\*.log" -Pattern "心跳#.*task=<13:27任务id>" | Select-Object -Last 20
# ② 前端 F12：空闲超时上下文是否出现、timeSinceLastData 到底是多少（坐实/证伪 60s 路径）
#    过滤 "[SSE] ⏱️ 空闲超时触发上下文"，对照 toast 出现时刻
# ③ 前后端对照：handler.ts "[Toast]" 打点时刻 vs 后端该任务 seq 空洞（LLM 工具执行期），三者对齐即定案
```

取证后分支：若证伪空闲路径且系真错误抖动，定点修只动 `useSSE` 错误分类/重试，不碰 badge 派生；若系代理缓冲吞心跳，修代理层 `X-Accel-Buffering: no` / flush，与本报告显示链无关。—— 小欧-2026-09-08