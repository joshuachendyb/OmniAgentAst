# [52] shell池半死实例探活超时清理链静默卡死根因分析报告

**编写人**: 小欧
**创建时间**: 2026-09-19 05:37:24

## 版本历史

| 版本 | 更新时间 | 更新人 | 更新说明 |
|------|----------|--------|----------|
| v1.0 | 2026-09-19 05:37:24 | 小欧 | 首次创建：定位 task-6dd00f3e617646fe99d6a09826e08a80 卡死630秒病根，锁定为「shell池半死实例探活超时后清理链静默卡死 + 引擎保险丝兜底」，给出探针复现诊断方案与修复方向 |

---

## 一、问题现象描述和证据

### 1.1 现象概述

任务 `task-6dd00f3e617646fe99d6a09826e08a80` 在 step=4 执行两个原生 shell 命令并行（`git diff HEAD~3 HEAD --stat` 与另一端 git 命令）时，自 **2026-09-19 04:58:03** 起进入静默状态，期间无任何工具产物/事件输出，仅 SSE 心跳保活，直至 **04:58:03 + 630 秒 = 05:08:33** 触发 `[Retry][L3] 执行超时:630秒` 才被截断。

### 1.2 关键日志证据时间线（backend/logs/app_2026-09-19.log）

| 时间 | 日志 | 解读 |
|------|------|------|
| 04:58:02 | `[HITL] 用户确认: ...0e6ae1b0` | step=4 第二个 shell 确认完成 |
| 04:58:02 | `[StepEmit] resumed step=4` / `[action_handler] 分组并行执行: tools=['shell','shell','find','find']` | 4 工具并行 |
| 04:58:02 | `[sandbox][exec] shell 预检 run 结束: rc=0` | sandbox 预检正常 |
| 04:58:03 | `[ShellPool] get_all_pids: 2 个活跃PID: {22064, 12628}`（execute_shell_command.py:278 安全检查） | 池中已有 2 个 PS 实例 |
| 04:58:03 | `[Shell] 过滤敏感env(动态): OPENCODE_SERVER_PASSWORD` | 业务命令进入执行流程 |
| 04:58:03 | `[Shell] 三路检测: PS→Bash, cmd=cd ... && git diff HEAD~3 HEAD --stat` / `[Shell] 自动修复bash语法` | git diff 被判定为 Bash 语法 → bash 分支 |
| 04:58:06 | **`shell_engine.py:517 [卡死C8/C14] 命令超时3s未出结果(半死/死循环/锁被hold) → 杀进程树+close`** | **池实例探活超时（3s）**，见第二章分析 |
| 04:58:06 → 05:08:33 | **仅 SSE 心跳（04:58:27 / 04:58:52 / ...），无任何引擎/重试/重建日志** | 清理链静默卡死窗口 |
| 05:08:33 | `tool_retry_engine.py [Retry][L3] 执行超时:630秒` | 引擎保险丝兜底截断 |

### 1.3 时间/超时数值对照

- 保险丝：`max(15, 600) + 30 = 630`（`_compute_fuse`，tool_retry_engine.py:238-259）
- 截断点：`04:58:03 + 630s = 05:08:33`，与实际 `[Retry][L3] 执行超时:630秒` **秒级吻合**。

该吻合证明：卡死窗口 = 保险丝全量耗尽，属「最后一次兜底」，而非业务正常返回。

---

## 二、病根病因挖掘实证分析

### 2.1 三层防线架构回顾（正常应如何演进）

| 层 | 代码位置 | 应有动作 | 本次是否发生 |
|----|----------|----------|--------------|
| ① 探活超时 | shell_engine.py:517（_exec_locked 超时分支） | 打 `[卡死C8/C14] 命令超时` → `_kill_tree()` + `_close()` | ✅ 已打印 |
| ② 探活失败上报 | shell_engine.py:367（_probe） | 打 `[卡死C8] 探活失败(半死)→已销毁` → `_close()` → 返回 False | ❌ **缺位** |
| ③ 池层剔除重试 | shell_engine.py:703（acquire Phase3） | 打 `[卡死C13]` → 销毁剔除实例 → 有界重试 | ❌ **缺位** |
| ④ 重建就绪 | shell_engine.py:427 | 打 `进程就绪` | ❌ **缺位** |
| ⑤ 引擎保险丝 | tool_retry_engine.py `_execute_tool_once` | `asyncio.wait_for(..., 630)` 截断 | ✅ 05:08:33 兜底 |

**结论**：①已执行，②③④全部未执行 → 卡死点位于 **①的清理链本身**（`shell_engine.py:518 _kill_tree()` → `:519 _close()` → `:520 return _ERROR_TIMEOUT` 之间），`_exec_locked` 未返回，导致 `_probe` 的 367 行永远走不到。

### 2.2 "命令超时3s" 是探活超时，非业务超时

- shell_engine.py:163 `_PROBE_TIMEOUT = 3`（探活默认超时）
- shell_engine.py:358-370 `_probe()`：`self._exec(_PROBE_CMD, timeout=timeout or _PROBE_TIMEOUT)` → 探活命令 `Write-Output __OMNI_PROBE__`，3s 未出 `__OMNI_PROBE__` 即判超时
- 业务命令超时是 LLM 传参（示例中 git diff=15s），与本次 3s 不符
- **→ 本次 3s 超时 = acquire 复用池实例时对该半死实例的响应性探活失败**

### 2.3 半死实例来源推断

- 池中两实例 `22064`、`12628` 为此前 step 使用放回的实例
- 其中一实例已进入「假活」状态（进程未死但 stdin 不再响应），探活写 `_PROBE_CMD` 后 `.code` 文件 3s 内未生成 → 触发 517 超时分支
- 半死根因候选（需探针进一步确认）：前序命令残留未消费管道、PS 引擎锁被长命令 hold、进程被系统降级/挂起

### 2.4 为何清理链静默卡死（核心病根）

静态审读 `_kill_tree()`（546-559）与 `_close()`（561-593）：

```python
def _kill_tree(self):
    if self._proc and self._proc.poll() is None:
        try:
            subprocess.run(["taskkill", "/T", "/F", "/PID", str(self._proc.pid)],
                           capture_output=True, timeout=SUBPROCESS_TIMEOUT_SHORT)  # 5s 有界
        except Exception as e:
            logger.warning(f"[卡死C11] taskkill异常 ...")
            ...

def _close(self):
    if self._proc:
        try:
            if self._proc.poll() is None:
                self._proc.kill()
                self._proc.wait(timeout=SUBPROCESS_TIMEOUT_SHORT)  # 5s 有界
        except Exception as e:
            logger.warning(f"[卡死C11] 关闭进程失败 ...")
        ...
    _safe_unlink(self._stderr_path)  # 3 次重试 × 0.05s = 有界
```

- 两函数均带 `timeout=`/重试上限，均有 except 留痕日志
- **但本次 517 打完后，C11/C9 均无留痕，且②③④全部缺位**
- → 卡死原语不在任务层超时范围内，属 **OS 级阻塞**（`taskkill`/`proc.wait()` 被 Windows 内核层挂起，未抛异常、未返回），python 层无法感知，只能靠最外层保险丝截断
- 另：bash 分支的 git diff 自 04:58:03 三路检测后亦无完成日志 → 同波被保险丝一次兜底，二者在同一 to_thread 线程池窗口内被整体截断

### 2.5 病根一句话

> **池中半死 PS 实例被 acquire 复用时探活 3s 超时，进入 `shell_engine.py:517` 超时清理分支后，`_kill_tree()`/`_close()` 在 OS 级原语上静默卡死（无异常、无日志），`_probe` 的失败上报与池层 C13 重试全部不可达，最终由引擎保险丝 630s 硬截断。**

---

## 三、解决方法最佳逻辑的方法

### 3.1 解决原则（KISS-DIRECT + 不退化）

1. **先诊断后修复**：静态代码无法 100% 钉死 OS 级阻塞行，必须先在 517-520 清理链注入 perf 探针日志，复现一次拿到精确卡点行，再对症修，杜绝盲改。
2. **一切清理必须有界且可观测**：超时分支内部每步都要有时间戳留痕，任何一步被系统挂起都能在日志定位。
3. **三防线闭环**：探活失败 → 上报 → 池层剔除重试 → 重建就绪，任一环节缺失即为 bug，必须有日志保证可见。
4. **禁止 backward**：不搞「兼容旧路径」，直接强化现有有界清理链。

### 3.2 第一步：探针复现方案（诊断优先）

在 `_exec_locked` 517-520 超时分支内注入分段时间戳探针：

```python
# 探针（临时诊断，_t0 记录进入超时分支时刻）
import time as _t
_t0 = _t.perf_counter()
logger.warning(f"[卡死C8/C14] 命令超时{timeout}s → 进入清理链 t0={_t0}")
self._kill_tree()
logger.warning(f"[卡死C8/C14] _kill_tree 完成, 耗时={(_t.perf_counter()-_t0)*1000:.1f}ms")
self._close()
logger.warning(f"[卡死C8/C14] _close 完成, 累计耗时={(_t.perf_counter()-_t0)*1000:.1f}ms")
return dict(_ERROR_TIMEOUT)
```

对 `_kill_tree`/`_close` 内部同样分段（taskkill 前后、poll/wait 前后、unlink 前后）。复现用例：制造一个不响应 stdin 的半死 pwsh 实例（如 `Write-Output 'stuck'; $a=...等待` 占住引擎），再并发触发两个 shell 命令，观察探针日志定位精确阻塞行。

### 3.3 第二步：修复方向（探针确认后实施）

| 方向 | 内容 | 落点 |
|------|------|------|
| A. 清理链硬超时守卫 | `_kill_tree`/`_close` 包裹进带 perf 统计的守卫，总耗时超阈值强制 return 并留痕 | shell_engine.py:546-593 |
| B. taskkill 二次确认 | taskkill 后轮询 `poll()` 确认进程已亡，未亡则 `proc.kill()` 兜底 + 留痕 | shell_engine.py:546-559 |
| C. `_probe` 失败上报保序 | 探活失败无论底层是否异常，`_probe` 都必须走 367 上报 + C13 重试，返回前 add 防护 | shell_engine.py:358-370 / 703 |
| D. 保险丝保留为最终防线 | 630s 保险丝是正确最后防线，保留不动，作为探针修好前的兜底 | tool_retry_engine.py |

### 3.4 验证标准

1. 探针日志覆盖：清理链每步耗时可见，无「静默窗口」
2. 半死实例场景下：`[卡死C8] 探活失败` → `[卡死C13] 剔除重试` → `进程就绪` 三链完整打印
3. 复现用例全程 ≤ 有界时间，不再触发 630s 保险丝
4. 回归测试：清理链 6 临时文件不残留（C12），stderr 文件被 `_safe_unlink` 正常回收

---

*文档更新时请保持版本历史追加制，禁止删除历史版本。*