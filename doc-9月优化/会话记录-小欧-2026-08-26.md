# 第七章概要设计 × 本地代码 实证核查记录 — 小欧 — 2026-08-26

> 编辑历史：
> 2026-08-26 小欧 — 创建本报告。北京老陈指令：停止报喜不报忧、停止搜版本历史，必须实读第七章正文(行1574-1663)与本地前端代码逐条对照，找出真实 bug。本报告只列实证确认的真实断点，不凑数、不虚报。

## 一、核查方法
- 实读文档 `[2]会话任务前后端架构设计2.0` 第七章正文（7.1~7.16，行1574-1663）作为设计基准。
- 实读本地代码：`NewChatContainer.tsx`/`useChatStreaming.ts`/`useChatFacade.ts`/`sse.ts`/`TaskInfoBar.tsx`/`useTaskInfo.ts`/`TopbarStats.tsx`/`ChatToolbar.tsx`/`TaskListPanel.tsx`/`RightViewer.tsx`/`TrustPanel.tsx`/`pipeline/*`/`input/*`/`layout/*`/`useModelLayer.ts`/`useChatSend.ts`/`api.ts`/`InputCore.tsx`/`SessionLayout.tsx`/`SubmitBar.tsx`/`PipelineRenderer.tsx`/`steps.ts`。
- 逐条比对 7.x 修改要求与代码实现。

## 二、已正确落地的（不虚报，简要列）
- 7.2 左列(时间+类型徽标+状态+耗时)、guard context_link_mode ✅（api.ts SessionTaskItem 含 duration/context_link_mode）
- 7.3 SessionLayout 六 slot 骨架 + prop 注入 ✅；右侧展开 flex 1 1 75% ✅
- 7.4 PipelineRenderer 三段式/顺序段/废弃 StepRow+ExecutionPanel ✅；action 替换 action_tool ✅；thought-start ✅；start/startinfo ✅
- 7.5 RightViewer C1+C2 并行 + C3 降级 getSessionMessages ✅；静态块 7 项+token 终值 ✅；artifacts ✅
- 7.7 TrustPanel 查询+撤销(D1/D2) ✅；AuthorizationModal 保留 ✅；HITL 高亮经 highlightToolName→ToolCallLine.hitl-border ✅
- 7.10 splitSteps 过滤 META 类型 ✅（steps.ts 正确分流，4.4.4 满足）
- 7.12 InputCore 3-4行+SubmitBar ✅；AttachmentArea 经 SubmitBar 挂 ✅；TaskTypeToggle 默认 independent ✅；CommandPanel 复用 ✅
- 7.13 useModelLayer.saveL2→sessionApi.updateSession(sessionModel) ✅
- 7.14 context_link_mode 端到端透传(sse.ts:850 请求体含) ✅
- 7.15 security_check 清理(五关键词 grep 零命中) ✅

## 三、真实断点（实证确认）

### A 级（功能缺失/严重）
- **A1【7.1⑤ 创建/更新时间悬浮未接线】** `TopbarStats.tsx:17-19` 声明 `createdAt/updatedAt` 弹层，但 `NewChatContainer.tsx:500` 调用 `<TopbarStats taskCount={total} chainTokens={chainTokens} />` 未传这两个 prop → 顶栏时间悬浮永不显示。
- **A2【7.3/7.5 右侧查看区默认收起，核心对话默认不可见】** `NewChatContainer.tsx:187` `useState(false)` 致 `rightOpen` 默认 false；SessionLayout 无 main 槽（"中间无主列"），对话(用户提问+AI 流水线)只活在右侧查看区。默认收起 = 发消息后主屏空白，仅一个展开按钮。严重偏离"对话即查看区"的设计意图。
- **A3【7.6 任务信息条不随左列点击切换】** `TaskInfoBar` 始终绑定实时 `metaFrames`（`NewChatContainer.tsx:553`）；选中历史任务时组件不接收该任务动态信息 → 违反 7.6「任务信息条=当前任务(含点击查看的历史任务)」+ 4.5.1 联动规则。历史任务动态信息条恒空白/滞留实时流。

### B 级（真实偏差）
- **B1【7.4⑦/4.9.3 observation 未读 tool_result 数组】** `ToolCallLine.tsx:80` 与 `ToolResultRenderer` 读 `o.content/o.summary`，未读设计要求的 `obsStep.tool_result` 数组（对应 8.B.3④ 待后端口径，但前端新读法未实现）。
- **B2【7.6② 执行中耗时无实时计时】** `useTaskInfo.ts:111` `elapsedSec = stats?.duration ?? 0`，stats.duration 仅终态下发 → 执行中全程恒显 `耗时 0s`，无 live tick（8.B.3②/③）。
- **B3【7.10/8.10 error 仍进执行步骤列表】** `sse.ts:1595` `case 'error'` 构造 step 并入流；`PipelineRenderer` 把 error 渲染为 StatusLine 段。与"error 不进执行步骤列表、经 onError 独立通道"偏差，且 liveErrorText 横幅 + 查看区 StatusLine 双显。

### C 级（口径不全/已知）
- **C1【7.8 四层分组不全】** 仅顶栏链累计 + 信息条单任务 + 静态块终值；缺"本轮消耗"独立展示位。
- **C2【7.9 上下文概况截断态】** 仅 🔴 图标，无 context_overview.truncated 文字说明；且受 A3 历史切换缺失影响。
- **C3【7.2 左列时间未格式化】** `TaskListPanel.tsx:71` 直接显示 `t.created_at` 原始 ISO 串，未格式化。

## 四、结论
- 第七章 7.1~7.16 绝大部分硬骨头已落地（骨架/流水线/信息条/左列/右侧/信任/HITL/输入区/模型层/context_link_mode/security_check 清理均实证到位）。
- 但存在 A1/A2/A3 三项真实功能缺失（A2 最为严重，会导致用户发消息后主屏空白），以及 B1/B2/B3/C1/C2/C3 口径偏差。
- 本报告未凑足 50 个：实证后确认的硬伤即上述 9 条，余者经核查属已正确实施，不虚报。
- 后续：A2 应改为 rightOpen 默认 true（或骨架默认展开右侧为主视图）；A1 需从 chatSession 取 created/updated 时间传入；A3 需 TaskInfoBar 接收 selectedTask 的动态信息（或历史任务改用静态详情）；B1 需后端确认 tool_result 口径后改读；B2 加前端计时器；B3 让 error 不入 executionSteps。

**编写人**：小欧  **时间**：2026-08-26
