# 前后端业务数据配合优化分析-小健-2026-09-04

**文档名称**: 前后端业务数据配合优化分析-小健-2026-09-04
**编写人**: 小健
**创建时间**: 2026-09-04 05:55:36
**更新时间**: 2026-09-04 21:37:19

| 版本 | 时间 | 更新人 | 更新要点 |
| v2.8 | 2026-09-04 21:37:19 | 小健 | 对照本地代码更新：删除未实施的event_emitter架构方案（原四五六章+八章1-6阶段+九章event_emitter测试）；SRP拆分与action_handler第3阶段拆分标注已实施并更新行数为真实值；第七章trust.py 112→91行、conflict_detector 65→70行；agent_telemetry 277→264行、step_emitter 90→79行；action_handler 1144→564行；3.2节补真实yield给前端点数（action 22/answer 10/react_cycle 23/sandbox 0/step_emitter 0/telemetry 0，排除注释与文档字符串） - 小健-2026-09-04 |

---

## 一、问题背景：为什么要做这个优化

这几天调试前后端配合的功能，总是后端到处修改，而且还总是错误百出。北京老陈追问：

1. 与前端配合的部分零散分布，到处都放，应该集中
2. 支持前端配合的代码逻辑层次/架构层次是错误的
3. 后端yield信息给前端，为什么需要那么多代码来处理这个事情？

**核心问题**：一定是后端与前端配合的这些逻辑的烂代码导致的。

---

## 二、问题现象：这几天改了哪些后端代码

| 日期 | 文件 | 改动内容 |
|------|------|----------|
| 09-01 | action_handler.py | 恢复路径补发resumed事件 |
| 09-01 | sandbox_gate.py | 恢复路径补发resumed事件 |
| 09-01 | answer_handler.py | 删除正常answer终态污染版ThoughtStep |
| 09-01 | llm_stream.py | error_classifier+L1/L2去放大 |
| 09-02 | action_handler.py | task005三处bug修复 |
| 09-02 | stream_orchestrator.py | task005三处bug修复 |
| 09-02 | llm_stream.py | 404重试放大严谨修复 |
| 09-02 | react_cycle.py | LLM重试事件实时透出 |
| 09-02 | step_emitter.py | emit统一日志出口 |
| 09-03 | 7个文件 | 10规范审查与重构方案 |

**核心发现**：这些改动**全部是围绕SSE事件发射**在修补。

---

## 三、病根分析：架构层次问题

### 3.1 后端在做前端的事

**当时架构（2026-09-04 初版分析时）**：

```
┌─────────────────────────────────────────────────────────┐
│  后端 handler 层                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ action_handler.py（当时1144行，现564行已拆分）   │   │
│  │   - 业务逻辑：安全检查、HITL确认、工具执行       │   │
│  │   - 前端逻辑：yield MetaStep(type="paused")     │   │  ← 后端在做前端的事！
│  │   - 前端逻辑：yield MetaStep(type="resumed")    │   │  ← 后端在做前端的事！
│  │   - 前端逻辑：yield MetaStep(type="blocked")    │   │  ← 后端在做前端的事！
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ answer_handler.py（当时235行，现217行）          │   │
│  │   - 业务逻辑：答案解析、错误处理                 │   │
│  │   - 前端逻辑：yield ThoughtStartStep             │   │  ← 后端在做前端的事！
│  │   - 前端逻辑：yield ThoughtStep                  │   │  ← 后端在做前端的事！
│  │   - 前端逻辑：yield FinalStep                    │   │  ← 后端在做前端的事！
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 3.2 yield发射点分布现状（2026-09-04 21:37 实测）

| 文件 | 当前行数 | yield给前端点数 | 职责 | 状态 |
|------|---------|----------------|------|------|
| action_handler.py | **564行** | **22处** | 业务编排+前端yield | ✅ 已SRP拆分3阶段（信任/冲突/观察/执行/解析5模块下沉） |
| answer_handler.py | **217行** | **10处** | 答案解析+前端yield | 保持现状（async generator，未改dict） |
| react_cycle.py | **850行** | **23处** | 调度+前端yield | 保持现状 |
| sandbox_gate.py | **124行** | **0处** | 沙箱预检+用户裁决 | ✅ DRY完成（sandbox_resolve返回元组，无yield，已emit列表交付） |
| step_emitter.py | **79行** | **0处** | 统一发射入口 | ✅ 已SLAP修复（outcome显式透传） |
| agent_telemetry.py | **264行** | **0处** | 遥测采集+Step构建 | ✅ 已SLAP修复（outcome参数化） |
| trust.py（新建） | **91行** | **0处** | 信任域：路径解析/跳过判定/落库（纯函数+async落库，无yield） | ✅ 已实施（action_handler引用） |
| conflict_detector.py（新建） | **70行** | **0处** | 冲突检测：_has_conflict/_partition_calls（纯函数，无yield） | ✅ 已实施（action_handler引用） |
| observation_builder.py（新建） | **135行** | **0处** | build_observation/_add_denial_feedback下沉（返回events列表，无yield） | ✅ 已实施（第3阶段拆分） |
| tool_runner.py（新建） | **141行** | **0处** | execute_tools下沉（async函数，无yield） | ✅ 已实施（第3阶段拆分） |
| action_input_parser.py（新建） | **50行** | **0处** | _build_call_list下沉（纯函数，无yield） | ✅ 已实施（第3阶段拆分） |

### 3.3 病根总结

| 问题 | 病根 | 当前状态 |
|------|------|---------|
| **代码东一点西一点** | 因为每个handler都在直接yield前端Step | 已部分缓解：action_handler经SRP拆分从1144→564行，信任/冲突/观察/执行/解析5大逻辑已下沉独立模块 |
| **修改一个前端展示要改多个文件** | 业务逻辑和前端展示逻辑混在一起 | 仍存在：answer_handler/sandbox_gate/react_cycle/action_handler仍是async generator+yield（event_emitter方案经评估未采用） |
| **为什么需要那么多代码处理SSE** | 7个层级都在处理SSE相关逻辑 | 已部分缓解：agent_runner终态SSE延迟到DB落库后（小欧方案一，消除卡executing竞态） |

---

## 四、优化方案评估结论

### 4.1 event_emitter架构重构方案（原四/五/六章）：经评估未实施，已删除

原v1.0-v2.7版本提出的event_emitter统一转换层方案（handler全部改为返回dict、_dispatch_handler统一await+转换、新建event_emitter.py），经三堂会审评估为**不合理，未实施**。原因见 `[4]前后端配合架构重构-基于最新代码-小健-2026-09-04.md`：

1. HITL时序约束导致handler不能"只返回dict不yield"（paused必须在阻塞等待前发出）
2. 增加转换层复杂度，收益有限
3. split-brain风险（数据过时）
4. 唯一值得做的改动是SLAP修复（已实施，见5.3节）

因此原四、五、六章及八章1-6阶段、九章event_emitter测试内容在本版本中全部删除。

### 4.2 实际实施的优化：SRP合规拆分（已完成）

实际采纳并已实施完成的是SRP合规拆分优化（原第七章），共4项：

| # | 内容 | 状态 |
|---|------|------|
| 1 | trust.py：信任域三合一（路径解析/跳过判定/落库） | ✅ 已实施，已引用 |
| 2 | conflict_detector.py：冲突检测下沉 | ✅ 已实施，已引用 |
| 3 | agent_telemetry.py + step_emitter.py：SLAP修复（outcome显式透传） | ✅ 已实施（2026-09-04 commit） |
| 4 | action_handler.py引用整合 + 第3阶段拆分（观察/执行/解析下沉） | ✅ 已实施（2026-09-04 commit） |

---

## 五、已实施成果：SRP合规拆分详情

### 5.1 项1：信任域合并为trust.py（91行，已实施）

**病根**：action_handler.py中信任相关代码散落3处（路径提取、跳过判定、落库），sandbox_gate.py反向import action_handler._extract_trust_path形成环依赖。

**现状**：`backend/app/tools/trust.py`（91行），4个函数：

- `_parse_paths` ← action_handler.py路径别名映射逻辑（复制不改逻辑）
- `extract_trust_path` ← action_handler.py信任路径提取（复制不改逻辑）
- `resolve_skip` ← action_handler.py会话信任跳过判定（加try/except防御+logger降级）
- `save_session_trust` ← hitl_confirmation.py信任落库（加ValueError防御）

action_handler.py已改为`from app.tools.trust import`引用，sandbox_gate→trust单向无环。

### 5.2 项2：冲突检测下沉（conflict_detector.py，70行，已实施）

**病根**：action_handler.py中`_has_conflict`和`_partition_calls`是独立的冲突检测逻辑，与handler业务流程混合，违反SRP。

**现状**：`backend/app/tools/conflict_detector.py`（70行），2个函数复用trust._parse_paths（DRY）：

- `_has_conflict(calls)`：同名写操作重复≥2次或同路径调用≥2次判冲突
- `_partition_calls(calls)`：并查集合并冲突调用成分组

action_handler.py已改为`from app.tools.conflict_detector import`引用。

### 5.3 项3：emit链路SLAP修复（agent_telemetry + step_emitter，已实施）

**病根**：`build_final_stats_step`隐式读`agent.status`（监控层依赖核心状态），`emit_final_with_stats`隐式透传outcome（发射层隐式依赖）。

**现状**（2026-09-04 小健，commit已提交，三堂会审通过）：

- `build_final_stats_step(self, outcome: str = "")`：outcome显式传入优先，为空时fallback到agent.status.value
- `emit_final_with_stats`：从final_step提取`_outcome = getattr(final_step, "outcome", "completed")`并透传给telemetry
- 19个调用点（react_cycle 7处/action_handler 1处/answer_handler 4处，全部显式传completed/failed/cancelled）
- 修复潜在缺陷：旧代码若emit时status仍为retrying/suspended会误发非终态final_status；新代码final_status必为终态值

### 5.4 项4：action_handler引用整合 + 第3阶段拆分（已实施）

action_handler.py从1144行拆分为564行，分三阶段：

| 阶段 | 下沉内容 | 目标文件 | 状态 |
|------|---------|---------|------|
| trust/conflict引用整合 | 路径提取37行+信任跳过30行+冲突检测65行→import | trust.py/conflict_detector.py | ✅ 已实施（约-100行） |
| 第2阶段 | collect_and_report批量聚合 | agent_telemetry.py | ✅ 已实施 |
| 第3阶段 | _build_call_list（58行）/build_observation+_add_denial_feedback（152行）/execute_tools（169行） | action_input_parser.py/observation_builder.py/tool_runner.py | ✅ 已实施（920→608行） |

**拦截action不emit不落库**（北京老陈裁定，2026-09-04 commit `5857e1007`）：废除记录层兜底`_record_calls=all_calls`，改为`if _exec_calls:`才emit ActionStep，消除"有action无observation"残步。

### 5.5 优化效果汇总

| 维度 | 改前 | 改后 |
|------|------|------|
| action_handler.py | 1144行，6职责 | 564行，信任/冲突/观察/执行/解析已下沉 |
| 新建文件 | 0 | 5个（trust.py/conflict_detector.py/observation_builder.py/tool_runner.py/action_input_parser.py） |
| 环依赖 | sandbox_gate→action_handler | sandbox_gate→trust（单向，无环） |
| 隐式依赖 | build_final_stats读agent.status | outcome显式参数传递 |
| SRP违规 | 6处 | 0处 |
| DRY违规 | 沙箱3处+路径2处 | 0处（统一trust.py） |
| 非终态误发final_status | 可能（retrying/suspended时） | 已消除（final_status必为终态值） |

---

## 六、目录结构与代码分布现状（2026-09-04 21:37 实测）

```
backend/app/services/agent/handlers/     # 业务层（仍含前端yield，未改dict）
├── action_handler.py      # 564行：安全检查、HITL确认、工具执行
├── answer_handler.py      # 217行：答案解析、错误处理
├── sandbox_gate.py        # 124行：沙箱预检、用户裁决（sandbox_resolve返回元组）

backend/app/services/agent/              # 调度层+基础设施层
├── react_cycle.py         # 850行：循环核心，分发handler
├── step_emitter.py        # 79行：统一发射入口（SLAP修复已实施）
├── llm_stream.py          # 325行：LLM流处理
└── agent_runner.py        # 594行：后台运行+SSE通道路由（终态延迟补发已实施）

backend/app/monitoring/                  # 监控层（独立）
└── agent_telemetry.py     # 264行：遥测采集、Step构建（SLAP修复已实施）

backend/app/tools/                       # 工具层（新增独立模块）
├── trust.py               # 91行：信任域（路径解析/跳过判定/落库）
└── conflict_detector.py   # 70行：冲突检测

backend/app/services/agent/              # 第3阶段拆分（新增）
├── action_input_parser.py # 50行：_build_call_list
├── observation_builder.py # 135行：build_observation+_add_denial_feedback
└── tool_runner.py         # 141行：execute_tools

backend/app/services/chat/               # SSE传输层
├── stream_orchestrator.py # 432行：SSE编排转发
└── stream_reader.py       # 319行：SSE读取

backend/app/services/task/               # 业务层
└── hitl_confirmation.py   # 165行：HITL确认机制
```

---

## 七、实施记录

| 阶段 | 内容 | 结果 |
|------|------|------|
| SRP拆分第1批 | trust.py（91行）+ conflict_detector.py（70行）新建，action_handler引用整合 | ✅ 实施完成，467用例通过 |
| SRP拆分第2批 | telemetry collect_and_report下沉 | ✅ 实施完成 |
| action_handler第3阶段拆分 | action_input_parser+observation_builder+tool_runner下沉（920→608行） | ✅ 实施完成 |
| 拦截action不emit不落库 | 废除_record_calls兜底（北京老陈裁定） | ✅ commit `5857e1007`，E2E-P0-03b PASSED |
| agent_telemetry+step_emitter SLAP修复 | outcome显式透传 | ✅ commit已提交，三堂会审通过，46单测通过 |
| agent_runner终态延迟补发 | 小欧方案一（final/final_stats延迟到DB落库后） | ✅ commit已提交，消除卡executing竞态 |

**未实施**：event_emitter统一转换层方案（answer_handler/sandbox_gate/react_cycle/action_handler改dict返回）——经三堂会审评估为不合理，已废弃。

---

## 八、测试验证现状

### 8.1 编译检查

| 文件 | 预期结果 | 实际 |
|------|---------|------|
| trust.py | 无错误 | ✅ |
| conflict_detector.py | 无错误 | ✅ |
| observation_builder.py | 无错误 | ✅ |
| tool_runner.py | 无错误 | ✅ |
| action_input_parser.py | 无错误 | ✅ |
| action_handler.py | 无错误 | ✅ |
| agent_telemetry.py | 无错误 | ✅ |
| step_emitter.py | 无错误 | ✅ |
| agent_runner.py | 无错误 | ✅ |

### 8.2 新增测试

| 新增文件 | 测试内容 | 结果 |
|---------|---------|------|
| `tests/test_conflict_detector.py` | 工具调用冲突检测：_has_conflict边界、_partition_calls分组 | ✅ 通过 |
| `tests/test_trust.py` | 信任域：_parse_paths别名映射、extract_trust_path提取、resolve_skip跳过、save_session_trust落库 | ✅ 通过 |

### 8.3 现有测试回归

| 测试项 | 结果 |
|--------|------|
| 467用例（SRP拆分后） | ✅ 全过 |
| 46单测（SLAP修复后，action/safety/answer相关） | ✅ 全过（除1个为event_emitter预写的无效测试，修改前已失败，与本次无关） |
| E2E-P0-03b（拦截action修复后重跑） | ✅ PASSED（79.89s，无残步） |

### 8.4 前端验证

| 验证项 | 结果 |
|--------|------|
| HITL暂停/确认流 | 正常（终态延迟补发消除卡executing竞态） |
| 拦截action无残步 | ✅（北京老陈裁定修复后，action与observation成对） |
| final_stats终态值 | ✅（必为completed/failed/cancelled终态值） |

---

**更新时间**: 2026-09-04 21:37:19
**更新人**: 小健-2026-09-04
