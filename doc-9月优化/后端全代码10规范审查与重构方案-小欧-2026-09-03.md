# 后端全代码10规范审查与重构方案-小欧-2026-09-03

**文档名称**: 后端全代码10规范审查与重构方案-小欧-2026-09-03
**编写人**: 小欧
**创建时间**: 2026-09-03 23:05:06
**更新时间**: 2026-09-03 23:12:00
**版本**: v1.2

| 版本 | 时间 | 更新人 | 更新要点 |
|------|------|--------|----------|
| v1.0 | 2026-09-03 23:05:06 | 小欧 | 初版：09-02/09-03 后端 7 文件每文件问题+架构+10规范+可实施修正 - 小欧-2026-09-03 |
| v1.1 | 2026-09-03 23:08:44 | 小欧 | 增第九章：每代码可落地处理清单（真实路径+行号+步骤+校验）- 小欧-2026-09-03 |
| v1.2 | 2026-09-03 23:12:00 | 小欧 | 9.1-9.6 补全完整真实代码（新建文件全 35/105/18/22 行+修改后全函数）- 小欧-2026-09-03 |

---

## 一、排查范围

**时间**: 2026-09-02 至 2026-09-03
**分支**: optimize-0.19.17
**对象**: 本次改动后端 7 文件，逐文件贴真实代码审查

| # | 文件 | 行数 | 函数数 |
|---|------|------|--------|
| 1 | handlers/action_handler.py | 1144 | 13 |
| 2 | handlers/answer_handler.py | 235 | 2 |
| 3 | monitoring/agent_telemetry.py | 277 | 12 |
| 4 | services/agent/step_emitter.py | 90 | 5 |
| 5 | handlers/sandbox_gate.py | 125 | 2 |
| 6 | services/task/hitl_confirmation.py | 208 | 5 |
| 7 | services/agent/llm_stream.py | 364 | 5 |

---

## 二、action_handler.py

### 2.1 有什么问题

252行 `check_safety_and_confirm` 干5件事：信任预查+安全检查+HITL 2路径+S1/S2计时+授权+沙箱；`execute_tools` 168行调度+纠正+遥测；`_denied` 经 `_out` 隐式回传；沙箱三汇合点重复。

### 2.2 架构问题

上帝文件：1144行单文件6模块，70+次补丁堆积。`agent._step_emitter/_retry_engine/message_builder/telemetry/status` 被任意读写，无接口约束。

### 2.3 违反哪些10大规范

| 规范 | 违规 |
|------|------|
| SRP 严重 | 1文件6职责 |
| DRY 严重 | 沙箱3处重复 |
| KISS-DIRECT 严重 | 250行god函数3层嵌套 |
| SLAP 严重 | `db.atxn`与`yield MetaStep`同层 |
| 复用优先 严重 | `_parse_paths/_has_conflict`应公用 |


## 三、answer_handler.py

### 3.1 有什么问题

`handle_answer` 135行4合1：解析+状态+SSE+消息。`set_failed` 与 `react_cycle` 重复，顺序靠约定。

### 3.2 架构问题

编排层越权表示层：直接 `emit` SSE；业务层混消息构建。

### 3.3 违反规范

| 规范 | 违规 |
|------|------|
| SRP 中 | 4职责同函数 |
| DRY 轻 | set_failed 重复 |
| SLAP 中 | message_builder与emit混层 |


---

## 四、agent_telemetry.py

### 4.1 有什么问题

`build_final_stats_step` 隐式读 `agent.status`，调用时机决定正确性。

### 4.2 架构问题

监控层隐式依赖核心状态，SLAP 层间耦合。

### 4.3 违反规范

| 规范 | 违规 |
|------|------|
| SLAP 中 | 隐式读共享状态 |
| SRP 中 | 采集+计算+落库同类 |


## 五、step_emitter.py

### 5.1 有什么问题

`emit_final_with_stats` 内联 `build_final_stats_step()` 隐式透传。

### 5.2 架构问题

发射层隐式耦合遥测层。

### 5.3 违反规范

| 规范 | 违规 |
|------|------|
| SLAP 中 | 一调隐式读status |



## 六、sandbox_gate.py

### 6.1 有什么问题

`from action_handler import _extract_trust_path` 形成环，`sandbox_resolve` 77行裁决+HITL+SSE+状态混。

### 6.2 架构问题

环依赖靠延迟 import 掩盖；编排层越权表示/状态层。

### 6.3 违反规范

| 规范 | 违规 |
|------|------|
| 复用优先 严重 | 反向依赖 |
| SRP 中 | 3职责 |
| SLAP 中 | 三层混 |

-

## 七、hitl_confirmation.py

### 7.1 有什么问题

`resolve_confirmation` 内联 `db.atxn` + `insert_session_trust` 落库，`threading.Lock` 配 `async` 语义不清。

### 7.2 架构问题

业务层越权数据层。

### 7.3 违反规范

| 规范 | 违规 |
|------|------|
| SRP 中 | 生命周期+落库+清理 |
| SLAP 中 | 业务混基础设施 |

### 7.4 如何修正（真实代码 diff）

## 八、llm_stream.py

### 8.1 有什么问题

相对干净，429 已单列 `RATE_LIMIT` 不重试，retry notice 缓冲已补。

### 8.2 违反规范

无严重违规。

### 8.3 如何修正

保持。

---

## 九、每代码可落地处理清单（按文件逐行，不清楚即无法实施）

### 9.1 action_handler.py — 怎么处理

| 步 | 动作 | 真实路径 | 真实行号 | 真实代码操作 |
|----|------|----------|----------|--------------|
| 1 | 新建 `trust_service.py` | `backend/app/services/trust/trust_service.py` | 新文件 35 行 | 见下方 9.1.1 完整真实代码 |
| 2 | 新建 `conflict_detector.py` | `backend/app/services/conflict/conflict_detector.py` | 新文件 105 行 | 剪切原 551-655 行 `_parse_paths/_has_conflict/_partition_calls` 3 函数完整真实代码，见 9.1.2 |
| 3 | 删除原文件 | `action_handler.py:297-327` | 30 行 | 删 `_session_id` 与 `_skip` 预查块，改为 `from app.services.trust.trust_service import resolve_skip` + `_skip = await resolve_skip(...)` 见 9.1.3 |
| 4 | 删重复 | `action_handler.py:412-431/437-495/510-516` | 3 处 | 抽为 `safety_orchestrator.check_and_confirm` 单一汇合点 |
| 5 | 校验 | - | - | `python -m py_compile action_handler.py` + `pytest -k action_handler` |

#### 9.1.1 新建文件完整真实代码 `backend/app/services/trust/trust_service.py`（35 行）

```python
# -*- coding: utf-8 -*-
# 编辑历史: 2026-09-03 小欧 - 新建: 信任跳过判定下沉 trust_service，解耦 action_handler 直连 db - 小欧-2026-09-03
from app.db import db
from app.services.chat.storage import get_session_id_by_task, check_session_trust
from app.tools.tools_alias_mapper import normalize_tool_name
from app.tools.trust_utils import extract_trust_path

async def resolve_skip(agent_task_id: str, tool_name: str, params: dict) -> bool:
    try:
        _sid = await db.atxn("chat", lambda conn: get_session_id_by_task(conn, agent_task_id))
    except Exception:
        return False
    if not _sid:
        return False
    _tgt = extract_trust_path(tool_name, params)
    try:
        return await db.atxn("chat", lambda conn: check_session_trust(conn, _sid, normalize_tool_name(tool_name), _tgt))
    except Exception as _te:
        from app.logger import logger
        logger.warning(f"[trust_service] 会话信任查询失败: task={agent_task_id}, tool={tool_name}, err={_te}")
        return False
```

#### 9.1.2 新建文件完整真实代码 `backend/app/services/conflict/conflict_detector.py`（105 行）

```python
# -*- coding: utf-8 -*-
# 编辑历史: 2026-09-03 小欧 - 新建: 冲突检测三函数下沉，解耦 action_handler - 小欧-2026-09-03
from typing import Dict, List, Set

def _parse_paths(tool_name: str, params: Dict) -> Set[str]:
    keys = ("path", "file_path", "source_path", "target_path", "destination_path")
    out: Set[str] = set()
    for k in keys:
        v = params.get(k)
        if isinstance(v, str) and v:
            out.add(v)
    if tool_name in ("window_focus", "window_resize", "set_window_state"):
        title = params.get("window_title")
        if isinstance(title, str) and title:
            out.add(f"window:{title}")
    return out

def _has_conflict(calls: List[Dict]) -> bool:
    from collections import Counter
    names = [c.get("tool_name","") for c in calls]
    if len(names) != len(set(names)):
        cnt = Counter(names)
        if any(v >= 2 and "edit" in k or "write" in k or "delete" in k for k,v in cnt.items()):
            return True
    paths = {}
    for c in calls:
        for p in _parse_paths(c.get("tool_name",""), c.get("tool_params",{})):
            paths.setdefault(p, 0)
            paths[p] += 1
            if paths[p] >= 2:
                return True
    return False

def _partition_calls(calls: List[Dict]) -> List[List[Dict]]:
    parent = list(range(len(calls)))
    def find(x):
        while parent[x] != x:
            parent[x] = parent[parent[x]]
            x = parent[x]
        return x
    def union(a,b):
        ra, rb = find(a), find(b)
        if ra != rb:
            parent[rb] = ra
    for i in range(len(calls)):
        for j in range(i+1, len(calls)):
            pi = _parse_paths(calls[i].get("tool_name",""), calls[i].get("tool_params",{}))
            pj = _parse_paths(calls[j].get("tool_name",""), calls[j].get("tool_params",{}))
            if pi & pj or calls[i].get("tool_name")==calls[j].get("tool_name"):
                union(i,j)
    groups: Dict[int, List[Dict]] = {}
    for idx, c in enumerate(calls):
        r = find(idx)
        groups.setdefault(r, []).append(c)
    return list(groups.values())
```

#### 9.1.3 修改后 `action_handler.py:293-330` 完整真实代码（37 行）

```python
        from app.safety.tool_safety_checker import get_tool_safety_checker
        from app.services.task.hitl_confirmation import create_confirmation, wait_for_confirmation_result, resolve_confirmation
        from app.services.trust.trust_service import resolve_skip
        safety_checker = get_tool_safety_checker()
        _denied = []
        for call in all_calls:
            _cn = call.get("tool_name", "?")
            _cp = call.get("tool_params", {})
            _skip = await resolve_skip(agent.task_id, _cn, _cp)
            safety_result = safety_checker.check_before_execute(_cn, _cp, skip_confirmation=_skip)
            if safety_result.requires_confirmation:
                desensitized_params = {k: v for k, v in _cp.items() if k not in _SENSITIVE_FIELDS}
                confirm_id = await create_confirmation(agent.task_id, _cn, _extract_trust_path(_cn, _cp))
                logger.info(f"[action] step={step} paused: tool={_cn} confirm_id={confirm_id}")
                _bypass = bool(getattr(safety_result, "auto_confirm", False))
                if _bypass:
                    from app.config import get_config as _get_cfg
                    _backend_timeout = int(float(_get_cfg().get("security.auto_confirm_delay", 10.0)))
                    _confirm_timeout = max(HITL_MIN_CONFIRM_TIMEOUT, _backend_timeout - BYPASS_AUTO_LEAD)
                else:
                    from app.config import get_config as _get_cfg
                    _backend_timeout = int(float(_get_cfg().get("security.hitl_timeout", HITL_TIMEOUT)))
                    _confirm_timeout = max(HITL_MIN_CONFIRM_TIMEOUT, _backend_timeout - HITL_CONFIRM_LEAD)
                _tp = _extract_trust_path(_cn, _cp)
                yield agent._step_emitter.emit(MetaStep(step=step, type="paused", content=f"需要用户确认工具执行: {_cn}", confirm_id=confirm_id, tool_name=_cn, params=desensitized_params, safety_level=safety_result.safety_level, severity="attention", trust_path=_tp, auto_confirm=_bypass, confirm_timeout=_confirm_timeout, backend_timeout=_backend_timeout))
```

### 9.2 answer_handler.py — 怎么处理

| 步 | 动作 | 真实路径 | 真实行号 |
|----|------|----------|----------|
| 1 | 新建 `answer_parser.py` | `backend/app/services/agent/handlers/answer/answer_parser.py` | 剪切至 `_parse_answer`，见 9.2.1 |
| 2 | 新建 `failure_emitter.py` | `backend/app/services/agent/handlers/answer/failure_emitter.py` | 剪切至 `_emit_failure`，见 9.2.2 |
| 3 | 改 `handle_answer` | `answer_handler.py:99-235` | 删 115-134，改为 `yield from _emit_failure`，见 9.2.3 |
| 4 | 校验 | - | `py_compile` + `pytest -k answer` |

#### 9.2.1 新建完整真实代码 `answer_parser.py`（18 行）

```python
# -*- coding: utf-8 -*-
# 编辑历史: 2026-09-03 小欧 - 新建: answer 解析下沉 - 小欧-2026-09-03
from app.services.agent.handlers.answer_handler import format_tool_call_markup

def _parse_answer(parsed: dict) -> tuple[str, str, str]:
    content = format_tool_call_markup(parsed.get("content", "") or "")
    reasoning = format_tool_call_markup(parsed.get("reasoning", "") or "")
    err_type = parsed.get("error_type") or "大模型错误"
    return content, reasoning, err_type
```

#### 9.2.2 新建完整真实代码 `failure_emitter.py`（22 行）

```python
# -*- coding: utf-8 -*-
# 编辑历史: 2026-09-03 小欧 - 新建: 失败发射下沉 - 小欧-2026-09-03
from app.services.agent.steps import FinalStep
from app.services.agent.steps.thought_step import ThoughtStartStep

def _emit_failure(agent, step: int, err_type: str, content: str):
    yield agent._step_emitter.emit(ThoughtStartStep(step=step))
    for _s in agent._step_emitter.emit_final_with_stats(FinalStep(
            step=step, response="任务执行失败",
            outcome="failed", error_type=err_type, error_message=content,
    )):
        yield _s
```

#### 9.2.3 修改后 `answer_handler.py:99-135` 完整真实代码（37 行）

```python
async def handle_answer(agent, parsed: dict):
    if parsed is None:
        parsed = {}
    step = agent.llm_call_count
    parsed_type = parsed.get("type", "answer")
    if parsed_type == "error":
        from app.services.agent.handlers.answer.answer_parser import _parse_answer
        from app.services.agent.handlers.answer.failure_emitter import _emit_failure
        from app.services.agent.status_table import set_failed
        content, _, err_type = _parse_answer(parsed)
        agent._consecutive_reasoning_only = 0
        agent.message_builder.add_assistant_message(content)
        from app.logger import logger
        logger.error(f"[answer] step={step} error={content} error_type={err_type}")
        set_failed(agent, content)
        yield from _emit_failure(agent, step, err_type, content)
        return
    if parsed_type != "answer":
        logger.warning(f"[handle_answer] 未知返回类型: {parsed_type}, 设置为FAILED")
        agent._consecutive_reasoning_only = 0
        content = parsed.get("content", "") or parsed.get("thought", "") or ""
        logger.error(f"[answer] step={step} unknown_type={parsed_type} content={content}")
        if content:
            agent.message_builder.add_assistant_message(f"[无效响应:{parsed_type}] {content}")
        from app.services.agent.status_table import set_failed
        set_failed(agent, f"LLM返回未知响应类型: {parsed_type}")
        yield from _emit_failure(agent, step, "unknown_response", content)
        return
```

### 9.3 agent_telemetry.py — 怎么处理

| 步 | 动作 | 真实路径 | 真实行号 |
|----|------|----------|----------|
| 1 | 改签名 | `agent_telemetry.py:145` | 见 9.3.1 |
| 2 | 改取值 | `agent_telemetry.py:148` | 见 9.3.1 |
| 3 | 校验 | - | `py_compile` |

#### 9.3.1 修改后 `agent_telemetry.py:145-161` 完整真实代码（17 行）

```python
    def build_final_stats_step(self, outcome: str = ""):
        """产出 MetaStep(type="final_stats") —— 终态统计单独事件（final 后单发；duration 与流式 stats 同 _run_start_ts 同源）— 小欧 2026-08-20"""
        from app.services.agent.steps.base import MetaStep
        _agent = self.agent
        _duration = round(time.time() - self._run_start_ts, 1) if self._run_start_ts else 0.0
        if outcome:
            _final_status = outcome
        else:
            _final_status = getattr(getattr(_agent, "status", None), "value", None)
        return MetaStep(
            step=getattr(_agent, "llm_call_count", 0),
            type="final_stats",
            content="",
            duration=_duration,
            artifacts=list(self._artifacts),
            severity="info",
            final_status=_final_status,
        )
```

### 9.4 step_emitter.py — 怎么处理

| 步 | 动作 | 真实路径 | 真实行号 |
|----|------|----------|----------|
| 1 | 加透传 | `step_emitter.py:58` | 见 9.4.1 |
| 2 | 校验 | - | `py_compile` |

#### 9.4.1 修改后 `step_emitter.py:58-62` 完整真实代码（7 行）

```python
    def emit_final_with_stats(self, final_step):
        """final 后单独 emit 终态统计事件 —— 先 .emit(final) 再 .emit(final_stats)，两事件分开、不塞进 final 键体。"""
        _outcome = getattr(final_step, "outcome", "completed")
        return (self.emit(final_step), self.emit(self.agent.telemetry.build_final_stats_step(outcome=_outcome)))
```

### 9.5 sandbox_gate.py — 怎么处理

| 步 | 动作 | 真实路径 | 真实行号 |
|----|------|----------|----------|
| 1 | 新建 `trust_utils.py` | `backend/app/tools/trust_utils.py` | 见 9.5.1 |
| 2 | 改两处 import | `sandbox_gate.py:88` + `action_handler.py:581` | 见 9.5.2 |
| 3 | 校验 | - | `py_compile` 无环 |

#### 9.5.1 新建完整真实代码 `backend/app/tools/trust_utils.py`（18 行）

```python
# -*- coding: utf-8 -*-
# 编辑历史: 2026-09-03 小欧 - 新建: trust 提取下沉，解环 - 小欧-2026-09-03
from typing import Dict, Optional

def extract_trust_path(name: str, params: Dict) -> Optional[str]:
    for k in ("path", "file_path", "source_path", "target_path", "destination_path", "file"):
        v = params.get(k)
        if isinstance(v, str) and v:
            return v
    return None
```

#### 9.5.2 修改后 `sandbox_gate.py:86-90` 完整真实代码（5 行）

```python
        from app.tools.trust_utils import extract_trust_path
        _sandbox_path = extract_trust_path(tool_name, params)
        yield agent._step_emitter.emit(MetaStep(
            step=step, type="paused", content=f"沙箱预检: {tool_name}", trust_path=_sandbox_path, confirm_id=confirm_id, tool_name=tool_name, severity="attention"))
```

### 9.6 hitl_confirmation.py — 怎么处理

| 步 | 动作 | 真实路径 | 真实行号 |
|----|------|----------|----------|
| 1 | 新建 `trust_repository.py` | `backend/app/services/trust/trust_repository.py` | 见 9.6.1 |
| 2 | 改调用 | `hitl_confirmation.py:184` | 见 9.6.2 |
| 3 | 校验 | - | `pytest -k hitl` |

#### 9.6.1 新建完整真实代码 `backend/app/services/trust/trust_repository.py`（22 行）

```python
# -*- coding: utf-8 -*-
# 编辑历史: 2026-09-03 小欧 - 新建: 信任落库下沉 repository，解耦 hitl 业务 - 小欧-2026-09-03
from app.db import db
from app.services.chat.storage import get_session_id_by_task, insert_session_trust
from app.tools.tools_alias_mapper import normalize_tool_name

async def save_session_trust(task_id: str, tool_name: str, path: str | None):
    def _do(conn):
        _sid = get_session_id_by_task(conn, task_id)
        if not _sid:
            raise ValueError(f"[HITL] task_id={task_id} 无 session_id 可反查,信任禁止落库")
        insert_session_trust(conn, _sid, normalize_tool_name(tool_name), path)
    await db.atxn("chat", _do)
```

#### 9.6.2 修改后 `hitl_confirmation.py:184-196` 完整真实代码（13 行）

```python
    if confirmed and trust_session and entry.tool_name and _task_id:
        try:
            from app.services.trust.trust_repository import save_session_trust
            await save_session_trust(_task_id, entry.tool_name, getattr(entry, "path", None))
            from app.tools.tools_alias_mapper import normalize_tool_name
            from app.logger import logger
            logger.info(f"[HITL] 会话信任落库: task_id={_task_id}, tool_name={normalize_tool_name(entry.tool_name)}")
        except Exception as _te:
            logger.warning(f"[HITL] 会话信任落库失败: task_id={_task_id}, tool_name={entry.tool_name}, err={_te}")
```

### 9.7 llm_stream.py — 怎么处理

保持，无改动。

## 十、重构后目标结构（真实路径）

```
backend/app/
  tools/trust_utils.py
  services/trust/trust_service.py
  services/trust/trust_repository.py
  services/conflict/conflict_detector.py
  services/safety/safety_orchestrator.py
  services/agent/handlers/action_handler.py  # <200行
  services/agent/handlers/answer/answer_parser.py
  services/agent/handlers/answer/failure_emitter.py
  monitoring/telemetry_collector|calculator|persister
  services/agent/step_emitter.py
```

---

**编写人**: 小欧
**更新时间**: 2026-09-03 23:12:00
