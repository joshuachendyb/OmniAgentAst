# [57]constants.py配置化分组分块设计

创建时间: 2026-09-21 22:05:00
版本历史:
| 版本 | 时间 | 更新人 | 修改简介 |
|------|------|--------|---------|
| v1.0 | 2026-09-21 22:05:00 | 小欧 | 初稿：存放方案结论+第7分组6分块详表+不迁清单+实施步骤，待北京老陈审核 |
| v1.1 | 2026-09-21 22:10:00 | 小欧 | 北京老陈意见：tuning大组下设6个子小组（嵌套块），二章结构改为“组-子组-键”三级 |
| v1.2 | 2026-09-21 22:15:00 | 小欧 | 三思三省自查修正：①llm14键拆为llm(7键)+llm_net(7键)；②删tuning.hhitl_timeout（与security.hitl_timeout重复，单源）；③stream_options标结构风险（registry若不支持dict则留代码）；④参数化加护栏（类型/范围/枚举，前端约束输入）；⑤补二期清单 |
| v1.3 | 2026-09-21 22:25:00 | 小欧 | 北京老陈追问：network子组核查结论——池类已在llm_net（一期），全局代理代码里不存在（仅download_file单次参数），删“二期池/代理预留位”虚话；network=cors_origins单键诚实保留 |
| v1.4 | 2026-09-21 22:30:00 | 小欧 | 10遍熟读核查修正8项：①二章标题6子组→7子组；②三思29→28键；③2.7标题删残留预留位；④tuning.hhitl笔误；⑤三章2.4→2.5编号+stream_options表述；⑥四章otransition笔误+补类型转换说明；⑦五章补全二期清单；⑧tool_choice枚举软化 |
| v1.5 | 2026-09-21 22:35:00 | 小欧 | 北京老陈命题：二期中本次值得加的4键——软配额等待/shell池上限/沙箱并发（新concurrency子组）+历史字符上限（入content）；1组8子组31键；其余9项留二期（理由见三思第6条） |
| v1.6 | 2026-09-21 22:45:00 | 小欧 | 北京老陈重排章节顺序（三不迁/四余9项原因/五实施）+10遍核查修正6项：草图子组顺序对齐2.3/2.4、分7节→8节、三思28→32、老二期清单改指四章、2.8补约束列 |
| v1.7 | 2026-09-21 22:55:00 | 小欧 | 第五章重写：键→文件对照表(grep 21文件实证)、存量文件兼容、校验接线、回归点名4个TDD文件；调用方清单纠错 |
| v1.8 | 2026-09-22 08:30:00 | 小欧 | 实施前阻塞修正：sandbox_max_concurrent已在registry注册（sandbox组），重复注册会拒启，从tuning删除；concurrency剩2键，总数31键；三思第6条同步删沙箱、实施步骤同步 |
| v1.9 | 2026-09-22 08:45:00 | 小欧 | 老实读代码核查修正：3处类型错（2.5/2.7/2.8字符键误标范围、2.7错锚文件）+三思重复/矛盾+第五章遗漏文件与错误行为 |
| v2.0 | 2026-09-22 | 小欧 | 第六章：基于本地最新代码的真实实施diff（21文件，含config/registry/后端调用方/前端/回归验证） |

## 一、存放方案结论（已定）

1. 不开新文件。用现有 `config/config.yaml`，新增第 7 分组 `tuning`（调优）。
2. 理由：复用 [54] 设置体系全套机器（6分组→7分组、merge_region_patch单次落盘、备份/mtime、前端设置页只加一个Tab）；加键向后兼容；新文件要重造加载/校验/备份/UI四套，违背DRY/YAGNI。
3. 本次只迁 `backend/app/constants.py` 31个开关（二期再议compaction/DB/池类，见五章）。

## 二、第7分组`tuning`设计：1组8子组（组-子组-键三级）

结构（yaml嵌套块，读侧点路径`tuning.llm.temperature`）：

```yaml
tuning:
  llm:         # 2.1 大模型语义，7键
    temperature: 0.7
    tool_choice: auto
    # ... 余见2.1表
  llm_net:     # 2.2 大模型网络（超时+池+总闸），7键：三思拆分，llm原14键太胖
    read_timeout: 150
    # ... 余见2.2表
  concurrency: # 2.3 并发配额，2键（v1.8：sandbox已删，剩软配额等待/shell池）
    soft_pool_wait_timeout: 30.0
    # ... 余见2.3表
  agent:       # 2.4 Agent循环，3键
    default_max_steps: 100
    # ... 余见2.4表
  stream_task: # 2.5 流与任务，4键
  hitl:        # 2.6 人工确认，4键（hitl_timeout已删，见下）
  content:     # 2.7 内容裁剪，3键（含历史字符上限，其常量定义在compaction_constants.py，值迁tuning后读配置）
  network:     # 2.8 网络，1键（仅cors_origins；核查结论：池类已在llm_net，
               # 全局代理代码里不存在故无可配项，不写虚的预留位）
```

子组即嵌套dict；settings_registry按子组登记（每子组独立schema段，前端Tab内分8节展示）；
merge_region_patch点路径天然支持嵌套，单次落盘语义不变。
命名规则：键即常量小写原名，缺省值=现常量值；调用方缺键回退常量（向后兼容）。

三思结论（2026-09-21 小欧自查，结论先行）：

1. llm必须拆。14键的子组和大组一样臃肿，语义也分两类（调什么 vs 怎么连），
   拆成llm（语义7键）+llm_net（网络7键）后每组一眼看完。
2. hitl_timeout删。`security.hitl_timeout`已是单源，再配一个缺省源就是双源异味；
   tuning.hitl只留4个真孤儿键。
3. stream_options是风险点。它是dict结构，registry若表达不了就留代码，
   实施时以registry能力为准，不硬配（已在2.1表标注）。
4. 参数化必须加护栏才是最优。32个开关裸露给用户，heartbeat填大了会断流、
   max_tokens填错会爆内存。所以registry每键必须带类型+范围/枚举+说明，
   前端约束输入；危险键（如stream_total_timeout）给推荐区间。这是“可配”成立的前提。
5. 同类参数的取舍已定稿：本次共迁31键（constants.py层+4个二期前置键），
   其余9项不迁，逐条理由见四章。本期先收这一层验证链路。
6. 二期中本次值得加的4键（v1.5决议，标准：用户真会调+配出去安全+零成本）：
   软配额等待30s（PAR实证的并发排队旋钮）+shell池上限3（并行负载运维）+
   沙箱并发3（同上，且已是get_config源头）+历史字符上限50000（token账单相关）。
   前三者立新concurrency子组，历史字符入content。1组8子组31键。
   其余9项不加，理由：DB锁协议是地基（动则事故）、compaction是联动算法、
   推理轮上限因模型而异、SQL/观察字典/标题是边角美容、SSE缓冲与心跳单源、
   清理间隔不影响正确性、产物上限待确认存在。

### 2.1 llm块（大模型语义，7键）

| yaml键 | 现常量值 | 说明 | 输入约束 |
|---|---|---|---|
| temperature | 0.7 | 采样温度 | 0~2 |
| tool_choice | auto | tool_choice模式 | 枚举auto/none（其余值以代码校验为准） |
| max_tokens | 16384 | 单次最大token(None=不限) | ≥1或空 |
| stream_max_retries | 3 | 流式重试次数 | 0~10 |
| response_fallback | true | FC失败回退Text | 布尔 |
| response_retries | 2 | 响应异常重试 | 0~5 |
| stream_options | {include_usage: true} | 流式选项（结构风险：registry若不支持dict则留代码） | 只读/JSON |

### 2.2 llm_net块（大模型网络：超时+池+总闸，7键）

| yaml键 | 现常量值 | 说明 | 输入约束 |
|---|---|---|---|
| read_timeout | 150 | 读超时(秒) | 10~600 |
| connect_timeout | 30.0 | 连接超时(秒) | 5~120 |
| write_timeout | 10.0 | 写超时(秒) | 5~120 |
| pool_timeout | 10.0 | 连接池超时(秒) | 5~120 |
| max_connections | 10 | 连接池最大连接 | 1~50 |
| max_keepalive | 5 | keepalive数 | 0~20 |
| stream_total_timeout | 500 | 流式总硬超时(秒，危险键，改小会断长任务) | 60~3600 |

### 2.3 concurrency块（并发配额，2键；v1.8：sandbox_max_concurrent已在registry，删）

| yaml键 | 现常量值 | 说明 | 输入约束 |
|---|---|---|---|
| soft_pool_wait_timeout | 30.0 | LLM软配额排队等待上限(秒，超时保底放行) | 5~120 |
| shell_pool_max_per_type | 8 | shell池每类槽位数 | 1~10 |

### 2.4 agent块（Agent循环，3键）

| yaml键 | 现常量值 | 说明 | 输入约束 |
|---|---|---|---|
| default_max_steps | 100 | Agent最大步数 | 10~500 |
| max_consecutive_chunks | 5 | 连续chunk上限 | 1~20 |
| max_chunks_without_promote | 50 | 无promote的chunk上限 | 10~200 |

### 2.5 stream_task块（流与任务，4键）

| yaml键 | 现常量值 | 说明 | 输入约束 |
|---|---|---|---|
| heartbeat_interval | 25.0 | SSE心跳(秒，危险键，改大前端会判超时) | 5~60 |
| task_timeout_hours | 1 | 任务过期(小时，存int，读时转timedelta) | 1~24 |
| tool_cache_ttl | 300 | 工具结果缓存TTL(秒) | 60~3600 |
| max_cache_size | 1000 | 会话/任务缓存条目 | 100~10000 |

### 2.6 hitl块（人工确认，4键；hitl_timeout已删，与security.hitl_timeout单源）

| yaml键 | 现常量值 | 说明 | 输入约束 |
|---|---|---|---|
| hitl_confirm_lead | 10 | 前端提前量 | 0~60 |
| bypass_auto_lead | 2 | bypass提前量 | 0~10 |
| hitl_min_confirm_timeout | 3 | 前端倒计时最小值 | 1~30 |
| max_pending_confirmations | 100 | 待确认上限 | 10~1000 |

### 2.7 content块（内容裁剪，3键）

| yaml键 | 现常量值 | 说明 | 输入约束 |
|---|---|---|---|
| project_context_max_chars | 10000 | 项目上下文注入上限(字符) | 1000~50000 |
| action_log_result_max_chars | 5000 | 日志tool_result截断(字符，防MemoryError) | 1000~20000 |
| temp_history_char_limit | 50000 | 临时历史字符上限(字符，token账单相关；常量在compaction_constants.py，迁后读tuning) | 5000~200000 |

### 2.8 network块（网络，1键；仅cors_origins）

| yaml键 | 现常量值 | 说明 | 输入约束 |
|---|---|---|---|
| cors_origins | http://localhost:5173,... | API跨域来源（逗号分隔存yaml，读时split为list） | 字符串，逗号分隔的URL |

## 三、不迁清单（留代码，理由）

1. 正则类（HTML_TAG/SCRIPT/STYLE/空白/UTC偏移）：洗数据规则，配出去无意义。
2. 算法内参与派生量（除已迁入2.6的以外；其余如LEAD钳制中间值）：改了易错。
3. 复杂dict结构键：除stream_options（纯透传保留）外，其余一律不迁。

## 四、余9项不加入的原因说明

能进配置的是“用户该做主的业务旋钮”，这 9 个要么是地基、要么是联动算法、要么配了没用。

DB busy_timeout（30000ms）：SQLite WAL 锁等待底线。调小→并发写直接报 busy 错；调大→卡死级联。动它等于动地基，用户调不出任何好处，只剩事故。
DB 重试（0.5/1/2s 退避）：和 busy_timeout 是一对的锁协议，改一个必须改另一个。拆开配就是给用户递两把互相打架的刀。
compaction 20 个阈值：压缩算法的联动参数（0.75/0.8/0.5 等互相咬合），改一个不改其余直接截断上下文或内存爆炸。这是调参，不是配置。
推理轮上限 3：不同模型行为差一个量级，3 对这个模型是护栏、对那个是枷锁。配成全局值等于用一把尺量所有布。
SQL 超时/limit（15s/50条）：dev 排障工具的护栏，不是业务开关。真卡了是 SQL 写得烂，该修 SQL 不该调参。
观察字典键上限 100：纯显示截断，配大配小都不影响正确性，只影响日志美观。为美观加配置不值。
SSE 缓冲：和心跳 25s 是同一根绳的两头（已配心跳），拆两处配，用户调偏一处就断流。单源原则。
标题长度：纯美容数字，无行为含义。
会话清理间隔：TASK_TIMEOUT（1h，已配）是“什么算过期”，间隔只是“多久扫一次”。扫勤扫懒不影响正确性，不值得占一个键。

## 五、实施步骤（按序执行，键→文件对照为grep实证）

1. config.yaml加`tuning`组（31键，值=现常量；结构见二章草图）。
   存量文件无tuning键不报错：读侧缺键回退常量（步骤3），首次PUT/settings写入时自动补齐整组。
2. settings_registry登记第7组（8子组独立schema段，每键类型/缺省/说明/约束，二章各表输入约束列即schema）。
   校验接线：类型/范围走registry schema；validation.py不管本组（本组无secret、无非空强约束）。
3. settings_service读侧加一行读取`get_setting("tuning.<子组>.<键>", 缺省)`；
   类型转换收口一处：task_timeout_hours读时转timedelta，cors_origins读时按逗号split。
   调用方按子组改（缺键回退`app.constants`，行为零变化）：
   - llm语义：base_service.py（温度/Token/重试/回退/选项）、llm_call.py（tool_choice/回退/重试）
   - llm网络：client_sdk.py（4超时+池连接/保活）、base_service.py（读超时/总硬超时）
   - 并发配额：client_sdk.py（软配额等待）、shell_engine.py（池槽位，构造缺省改读配置）、sandbox executor.py（统一到tuning源头，替代get_config散读）
   - Agent循环：config_schemas.py（缺省步数）、chunk_buffer.py/initialize_run_state.py（chunk上限）
   - 流与任务：stream_orchestrator.py（心跳）、task_registry.py（过期）、base_service.py/tool_facade.py/universal_agent.py（缓存TTL）、message_service.py（缓存条目）
   - 人工确认：hitl_gateway.py/hitl_confirmation.py/handle_action.py/react_step.py/sandbox_gate.py——只迁4孤儿键，HITL_TIMEOUT保持走security单源不动
   - 内容裁剪：project_context.py、handle_action.py/tool_runner.py（日志截断）、message_builder.py（历史字符；常量定义在compaction_constants.py，值迁tuning后该处改读配置）
   - 网络：main.py（CORS）
4. 前端设置页加“调优”Tab（8节，按2.1~2.8顺序；只读+编辑，走现有PUT/settings通道；危险键标红+推荐区间）。
5. 回归：py_compile全量 + settings TDD四件套（test_settings_registry_tdd/test_settings_service_tdd/test_settings_routes_tdd/test_settings_editsave_red）+ 一轮P0 shortest E2E冒烟。
6. constants.py保留作缺省源（transition期后视情况删；禁止当backward载体）
---
编写人：小欧 2026-09-21 22:05:00

## 六、详细实施代码（基于本地最新代码的真实 diff，落盘后 git diff 可复验）

> 本章所有 diff 均取自本地最新工作区文件路径与行号，改前为当前 HEAD 实码，改后为拟落盘实码。
> 实施时按本章逐文件应用，应用后 `git diff` 与本章一一对应；未列出的文件不改。
> 小欧 2026-09-22 编写

### 6.1 config/config.yaml（新增 tuning 组，值=各常量现值）

```diff
--- a/config/config.yaml
+++ b/config/config.yaml
@@ sandbox 末尾(L104-112)
   default_timeout_sec: 60
   max_timeout_sec: 300
+tuning:
+  llm:
+    temperature: 0.7
+    tool_choice: auto
+    max_tokens: 16384
+    stream_max_retries: 3
+    response_fallback: true
+    response_retries: 2
+    stream_options:
+      include_usage: true
+  llm_net:
+    read_timeout: 150
+    connect_timeout: 30.0
+    write_timeout: 10.0
+    pool_timeout: 10.0
+    max_connections: 10
+    max_keepalive: 5
+    stream_total_timeout: 500
+  concurrency:
+    soft_pool_wait_timeout: 30.0
+    shell_pool_max_per_type: 8
+  agent:
+    default_max_steps: 100
+    max_consecutive_chunks: 5
+    max_chunks_without_promote: 50
+  stream_task:
+    heartbeat_interval: 25.0
+    task_timeout_hours: 1
+    tool_cache_ttl: 300
+    max_cache_size: 1000
+  hitl:
+    hitl_confirm_lead: 10
+    bypass_auto_lead: 2
+    hitl_min_confirm_timeout: 3
+    max_pending_confirmations: 100
+  content:
+    project_context_max_chars: 10000
+    action_log_result_max_chars: 5000
+    temp_history_char_limit: 50000
+  network:
+    cors_origins: http://localhost:5173,http://127.0.0.1:5173
```

> 注：shell_pool_max_per_type 默认值取 8（当前运行值，见 shell_engine.py:1077 `shell_pool = ShellPoolManager(max_per_type=8)`）。

### 6.2 backend/app/services/settings/settings_registry.py

**改动1：GROUP_ORDER 加 tuning（L147）**

```diff
--- a/backend/app/services/settings/settings_registry.py
+++ b/backend/app/services/settings/settings_registry.py
@@ L147
-GROUP_ORDER = ["general", "model", "security", "sandbox", "system", "appearance"]
+GROUP_ORDER = ["general", "model", "security", "sandbox", "system", "appearance", "tuning"]
```

**改动2：GROUPS 末尾 appearance 之后新增 tuning 组（L144 之后插入）**

```diff
--- a/backend/app/services/settings/settings_registry.py
+++ b/backend/app/services/settings/settings_registry.py
@@ L144 appearance 闭合括号之后
     ]},
+    # 4.7 调优（tuning，8 子组 31 键，值域来自 constants.py 现值）
+    "tuning": {"label": "调优", "items": [
+        # --- llm: LLM 语义参数（7 键） ---
+        _item("tuning.llm.temperature", "float", "采样温度", 0.7, range_=[0, 2],
+              notice="LLM 采样温度，0=确定性，2=高随机"),
+        _item("tuning.llm.tool_choice", "select", "tool_choice 模式", "auto",
+              options=["auto", "none"], notice="auto=模型自主选择工具，none=纯文本模式"),
+        _item("tuning.llm.max_tokens", "int", "单次最大 token", 16384, range_=[1, 100000],
+              notice="LLM 单次调用最大输出 token 数"),
+        _item("tuning.llm.stream_max_retries", "int", "流式最大重试", 3, range_=[0, 10],
+              notice="LLM 流式调用最大重试次数"),
+        _item("tuning.llm.response_fallback", "bool", "FC 响应回退", True,
+              notice="FC 模式错误时降级为 Text 模式重试"),
+        _item("tuning.llm.response_retries", "int", "响应错误重试", 2, range_=[0, 5],
+              notice="LLM 响应错误（空/无效）最大重试次数"),
+        _item("tuning.llm.stream_options", "textarea", "流式选项(JSON)", '{"include_usage": true}',
+              notice="LLM 流式请求 stream_options 字段，JSON 格式"),
+        # --- llm_net: LLM 网络/超时/连接池（7 键） ---
+        _item("tuning.llm_net.read_timeout", "int", "读超时(秒)", 150, range_=[10, 600],
+              notice="LLM 客户端读超时兜底(秒)"),
+        _item("tuning.llm_net.connect_timeout", "float", "连接超时(秒)", 30.0, range_=[5, 120],
+              notice="LLM 客户端连接超时(秒)"),
+        _item("tuning.llm_net.write_timeout", "float", "写超时(秒)", 10.0, range_=[5, 120],
+              notice="LLM 客户端写超时(秒)"),
+        _item("tuning.llm_net.pool_timeout", "float", "连接池超时(秒)", 10.0, range_=[5, 120],
+              notice="LLM 客户端连接池超时(秒)"),
+        _item("tuning.llm_net.max_connections", "int", "池最大连接", 10, range_=[1, 50],
+              notice="LLM 客户端连接池最大连接数"),
+        _item("tuning.llm_net.max_keepalive", "int", "keepalive 连接", 5, range_=[0, 20],
+              notice="LLM 客户端连接池 keepalive 连接数"),
+        _item("tuning.llm_net.stream_total_timeout", "int", "流总硬超时(秒)", 500, range_=[60, 3600],
+              notice="单次 LLM 流式调用总时长硬超时(秒)"),
+        # --- concurrency: 并发配额（2 键） ---
+        _item("tuning.concurrency.soft_pool_wait_timeout", "float", "软配额等待(秒)", 30.0, range_=[5, 120],
+              notice="LLM 软配额排队等待上限(秒)，超时保底放行"),
+        _item("tuning.concurrency.shell_pool_max_per_type", "int", "Shell 池槽位", 8, range_=[1, 20],
+              notice="同 key Shell 池最大并发实例数"),
+        # --- agent: Agent 循环参数（3 键） ---
+        _item("tuning.agent.default_max_steps", "int", "Agent 最大步数", 100, range_=[10, 500],
+              notice="API 请求体默认 max_steps（config.yaml agent.max_steps 为运行时上限）"),
+        _item("tuning.agent.max_consecutive_chunks", "int", "连续 chunk 上限", 5, range_=[1, 20],
+              notice="Agent 循环连续 chunk 上限，超出触发 promote"),
+        _item("tuning.agent.max_chunks_without_promote", "int", "无 promote 上限", 50, range_=[10, 200],
+              notice="Agent 循环无 promote 的 chunk 上限，超出强制停止"),
+        # --- stream_task: 流/任务/缓存（4 键） ---
+        _item("tuning.stream_task.heartbeat_interval", "float", "SSE 心跳周期(秒)", 25.0, range_=[5, 60],
+              notice="SSE keep-alive 心跳周期(秒)，须 < 前端 IDLE_TIMEOUT=60s"),
+        _item("tuning.stream_task.task_timeout_hours", "int", "任务过期(小时)", 1, range_=[1, 24],
+              notice="创建超过 N 小时的终态任务兜底清理"),
+        _item("tuning.stream_task.tool_cache_ttl", "int", "工具缓存 TTL(秒)", 300, range_=[60, 3600],
+              notice="工具结果缓存 TTL(秒)"),
+        _item("tuning.stream_task.max_cache_size", "int", "缓存条目上限", 1000, range_=[100, 10000],
+              notice="会话/上下文缓存最大条目数"),
+        # --- hitl: 人工确认（4 键） ---
+        _item("tuning.hitl.hitl_confirm_lead", "int", "HITL 倒计时提前量(秒)", 10, range_=[0, 60],
+              notice="前端倒计时比后端 HITL_TIMEOUT 提前的秒数"),
+        _item("tuning.hitl.bypass_auto_lead", "int", "bypass 提前量(秒)", 2, range_=[0, 10],
+              notice="bypass 前端倒计时比后端提前的秒数"),
+        _item("tuning.hitl.hitl_min_confirm_timeout", "int", "倒计时最小值(秒)", 3, range_=[1, 30],
+              notice="前端倒计时最小值，后端窗口-LEAD 钳制下限"),
+        _item("tuning.hitl.max_pending_confirmations", "int", "待确认上限", 100, range_=[10, 1000],
+              notice="HITL 最大待确认请求数"),
+        # --- content: 内容截断（3 键） ---
+        _item("tuning.content.project_context_max_chars", "int", "上下文注入上限(字符)", 10000, range_=[1000, 50000],
+              notice="项目规则文件(OmniAgent.md)注入 Prompt 字符上限"),
+        _item("tuning.content.action_log_result_max_chars", "int", "日志截断(字符)", 5000, range_=[1000, 20000],
+              notice="action_handler 日志 tool_result 截断长度"),
+        _item("tuning.content.temp_history_char_limit", "int", "临时历史字符上限", 50000, range_=[5000, 200000],
+              notice="compaction 临时历史字符上限"),
+        # --- network: 网络（1 键） ---
+        _item("tuning.network.cors_origins", "text", "CORS 允许来源",
+              "http://localhost:5173,http://127.0.0.1:5173",
+              notice="API CORS 允许来源，多个用逗号分隔"),
+    ]},
```

### 6.3 backend/app/llm/base_service.py（L86 import + L108/L111 使用点）

**改动1：import 改为别名回退（L86）**

```diff
--- a/backend/app/llm/base_service.py
+++ b/backend/app/llm/base_service.py
@@ L86
-from app.constants import DEFAULT_READ_TIMEOUT, LLM_TEMPERATURE, LLM_STREAM_MAX_RETRIES, LLM_STREAM_OPTIONS, STREAM_TOTAL_TIMEOUT, LLM_MAX_TOKENS
+from app.constants import DEFAULT_READ_TIMEOUT as _D_READ_TIMEOUT, LLM_TEMPERATURE as _D_TEMPERATURE, LLM_STREAM_MAX_RETRIES as _D_STREAM_MAX_RETRIES, LLM_STREAM_OPTIONS as _D_STREAM_OPTIONS, STREAM_TOTAL_TIMEOUT as _D_STREAM_TOTAL_TIMEOUT, LLM_MAX_TOKENS as _D_MAX_TOKENS
+from app.config import get_config
```

**改动2：`__init__` temperature 默认值改读配置（L107-108）**

```diff
--- a/backend/app/llm/base_service.py
+++ b/backend/app/llm/base_service.py
@@ L107-108
         if temperature is None:
-            temperature = LLM_TEMPERATURE
+            temperature = get_config().get("tuning.llm.temperature", _D_TEMPERATURE)
```

**改动3：`__init__` max_tokens 默认值改读配置（L111）**

```diff
--- a/backend/app/llm/base_service.py
+++ b/backend/app/llm/base_service.py
@@ L111
-        self.max_tokens = max_tokens if max_tokens is not None else LLM_MAX_TOKENS
+        self.max_tokens = max_tokens if max_tokens is not None else get_config().get("tuning.llm.max_tokens", _D_MAX_TOKENS)
```

**改动4：`__init__` timeout 默认值改读配置（L129-131）**

```diff
--- a/backend/app/llm/base_service.py
+++ b/backend/app/llm/base_service.py
@@ L129-131
         try:
-            timeout_value = float(timeout) if timeout else float(DEFAULT_READ_TIMEOUT)
+            timeout_value = float(timeout) if timeout else float(get_config().get("tuning.llm_net.read_timeout", _D_READ_TIMEOUT))
         except (ValueError, TypeError):
-            timeout_value = float(DEFAULT_READ_TIMEOUT)
+            timeout_value = float(get_config().get("tuning.llm_net.read_timeout", _D_READ_TIMEOUT))
```

### 6.4 backend/app/llm/client_sdk.py（L34-41 import + L109-110/117）

**改动1：import 改为别名回退（L34-41）**

```diff
--- a/backend/app/llm/client_sdk.py
+++ b/backend/app/llm/client_sdk.py
@@ L34-41
 from app.constants import (
-    DEFAULT_CONNECT_TIMEOUT,
-    DEFAULT_READ_TIMEOUT,
-    DEFAULT_WRITE_TIMEOUT,
-    DEFAULT_POOL_TIMEOUT,
-    LLM_MAX_CONNECTIONS,
-    LLM_MAX_KEEPALIVE,
+    DEFAULT_CONNECT_TIMEOUT as _D_CONNECT_TIMEOUT,
+    DEFAULT_READ_TIMEOUT as _D_READ_TIMEOUT,
+    DEFAULT_WRITE_TIMEOUT as _D_WRITE_TIMEOUT,
+    DEFAULT_POOL_TIMEOUT as _D_POOL_TIMEOUT,
+    LLM_MAX_CONNECTIONS as _D_MAX_CONNECTIONS,
+    LLM_MAX_KEEPALIVE as _D_MAX_KEEPALIVE,
 )
 from app.config import get_config
```

**改动2：`_SOFT_POOL_WAIT_TIMEOUT` 改读配置（L109-110）**

```diff
--- a/backend/app/llm/client_sdk.py
+++ b/backend/app/llm/client_sdk.py
@@ L109-110
-_soft_pool_semaphore = None  # 2026-09-20 小欧 P5: 延迟初始化, 绑定首次使用时的 event loop — 小欧-2026-09-20
-_SOFT_POOL_WAIT_TIMEOUT = 30.0  # 软配额排队等待上限(秒): 超时保底放行(不拒绝不降级) — 小欧-2026-09-20
+_soft_pool_semaphore = None  # 2026-09-20 小欧 P5: 延迟初始化, 绑定首次使用时的 event loop — 小欧-2026-09-20
+_SOFT_POOL_WAIT_TIMEOUT = get_config().get("tuning.concurrency.soft_pool_wait_timeout", 30.0)  # 软配额排队等待上限(秒): 超时保底放行(不拒绝不降级) — 小欧-2026-09-20
```

**改动3：`_get_soft_pool_semaphore` 信号量改读配置（L117）**

```diff
--- a/backend/app/llm/client_sdk.py
+++ b/backend/app/llm/client_sdk.py
@@ L117
-        _soft_pool_semaphore = asyncio.Semaphore(LLM_MAX_CONNECTIONS)
+        _soft_pool_semaphore = asyncio.Semaphore(get_config().get("tuning.llm_net.max_connections", _D_MAX_CONNECTIONS))
```

**改动4：`LLMClient.__init__` timeout 默认值改读配置（L131）**

```diff
--- a/backend/app/llm/client_sdk.py
+++ b/backend/app/llm/client_sdk.py
@@ L131
-        timeout: Optional[int] = None,
+        timeout: Optional[int] = None,  # 缺省从 tuning 配置读取
```

> 注：`LLMClient.__init__` 的 timeout 默认值 None 在 `request`/`request_stream` 中 fallback 到 `self._default_timeout`，该值在 `__init__` 末尾赋值。改法：在 `__init__` 末尾 `self._default_timeout = timeout or get_config().get("tuning.llm_net.read_timeout", _D_READ_TIMEOUT)` 处改读配置。

**改动5：`request_stream` 连接池参数改读配置（L198 附近 httpx.AsyncClient 构造）**

```diff
--- a/backend/app/llm/client_sdk.py
+++ b/backend/app/llm/client_sdk.py
@@ httpx.AsyncClient 构造处
-            limits=httpx.Limits(max_connections=LLM_MAX_CONNECTIONS, max_keepalive_connections=LLM_MAX_KEEPALIVE),
+            limits=httpx.Limits(max_connections=get_config().get("tuning.llm_net.max_connections", _D_MAX_CONNECTIONS), max_keepalive_connections=get_config().get("tuning.llm_net.max_keepalive", _D_MAX_KEEPALIVE)),
```

### 6.5 backend/app/services/agent/llm_call.py（L73/L87）

**改动1：import 改为别名回退（L73）**

```diff
--- a/backend/app/services/agent/llm_call.py
+++ b/backend/app/services/agent/llm_call.py
@@ L73
-from app.constants import LLM_RESPONSE_FALLBACK, LLM_RESPONSE_RETRIES, LLM_TOOL_CHOICE
+from app.constants import LLM_RESPONSE_FALLBACK as _D_FALLBACK, LLM_RESPONSE_RETRIES as _D_RETRIES, LLM_TOOL_CHOICE as _D_TOOL_CHOICE
+from app.config import get_config
```

**改动2：`tool_choice` 默认值改读配置（L87）**

```diff
--- a/backend/app/services/agent/llm_call.py
+++ b/backend/app/services/agent/llm_call.py
@@ L87
-    tool_choice = LLM_TOOL_CHOICE if openai_tools else None
+    tool_choice = get_config().get("tuning.llm.tool_choice", _D_TOOL_CHOICE) if openai_tools else None
```

**改动3：`call_llm_with_fallback` 中 FALLBACK/RETRIES 改读配置（搜索 LLM_RESPONSE_FALLBACK/LLM_RESPONSE_RETRIES 使用点）**

```diff
--- a/backend/app/services/agent/llm_call.py
+++ b/backend/app/services/agent/llm_call.py
@@ call_llm_with_fallback 内部
-    if LLM_RESPONSE_FALLBACK and ...:
+    if get_config().get("tuning.llm.response_fallback", _D_FALLBACK) and ...:
```

```diff
-        for attempt in range(LLM_RESPONSE_RETRIES):
+        for attempt in range(get_config().get("tuning.llm.response_retries", _D_RETRIES)):
```

### 6.6 backend/app/services/chat/stream_orchestrator.py（L186/L572）

**改动：import + 使用点改读配置（L186, L572）**

```diff
--- a/backend/app/services/chat/stream_orchestrator.py
+++ b/backend/app/services/chat/stream_orchestrator.py
@@ L186
-from app.constants import HEARTBEAT_INTERVAL  # 心跳周期常量(与前端 IDLE_TIMEOUT 的错开关系见 constants.py §6) — 小欧 2026-09-08
+from app.constants import HEARTBEAT_INTERVAL as _D_HEARTBEAT  # 心跳周期常量(与前端 IDLE_TIMEOUT 的错开关系见 constants.py §6) — 小欧 2026-09-08
+from app.config import get_config
```

```diff
@@ L572
-                    await asyncio.wait_for(buffer.cond.wait(), timeout=HEARTBEAT_INTERVAL)  # 心跳周期 HEARTBEAT_INTERVAL(错开关系见 constants.py §6, 与前端 IDLE_TIMEOUT=60s 错开)
+                    _hb = get_config().get("tuning.stream_task.heartbeat_interval", _D_HEARTBEAT)
+                    await asyncio.wait_for(buffer.cond.wait(), timeout=_hb)  # 心跳周期(错开关系见 constants.py §6, 与前端 IDLE_TIMEOUT=60s 错开)
```

> 注：L575/L576/L579 的日志/注释中引用 HEARTBEAT_INTERVAL 的地方同步改为 `_hb`。

### 6.7 backend/app/services/task/task_registry.py（L31/L194）

**改动：import + 使用点改读配置（L31, L194）**

```diff
--- a/backend/app/services/task/task_registry.py
+++ b/backend/app/services/task/task_registry.py
@@ L31
-from app.constants import TASK_TIMEOUT
+from app.constants import TASK_TIMEOUT as _D_TASK_TIMEOUT
+from app.config import get_config
+from datetime import timedelta
```

```diff
@@ L194
-            if t.get("created_at") and now - t["created_at"] > TASK_TIMEOUT
+            _task_timeout = timedelta(hours=get_config().get("tuning.stream_task.task_timeout_hours", 1))
+            if t.get("created_at") and now - t["created_at"] > _task_timeout
```

### 6.8 backend/app/services/tool/tool_facade.py（L50）

**改动：import 改为别名回退（L50）**

```diff
--- a/backend/app/services/tool/tool_facade.py
+++ b/backend/app/services/tool/tool_facade.py
@@ L50
-from app.constants import TOOL_CACHE_TTL as _TOOL_CACHE_TTL
+from app.constants import TOOL_CACHE_TTL as _D_TOOL_CACHE_TTL
+from app.config import get_config
```

> 注：`_TOOL_CACHE_TTL` 在本文件中被 `_FacadeAgent.TOOL_CACHE_TTL` 引用（通过 `_get_tool_dict` 间接），改读配置点在 `UniversalAgent` 类属性处（见 6.10）。

### 6.9 backend/app/services/agent/universal_agent.py（L27/L40）

**改动：import + 类属性改读配置（L27, L40）**

```diff
--- a/backend/app/services/agent/universal_agent.py
+++ b/backend/app/services/agent/universal_agent.py
@@ L27
-from app.constants import TOOL_CACHE_TTL as _TOOL_CACHE_TTL
+from app.constants import TOOL_CACHE_TTL as _D_TOOL_CACHE_TTL
+from app.config import get_config
```

```diff
@@ L40
-    TOOL_CACHE_TTL = _TOOL_CACHE_TTL
+    TOOL_CACHE_TTL = get_config().get("tuning.stream_task.tool_cache_ttl", _D_TOOL_CACHE_TTL)
```

### 6.10 backend/app/services/agent/chunk_buffer.py（L12/L34）

**改动：import + 默认参数改读配置（L12, L34）**

```diff
--- a/backend/app/services/agent/chunk_buffer.py
+++ b/backend/app/services/agent/chunk_buffer.py
@@ L12
-from app.constants import MAX_CONSECUTIVE_CHUNKS, MAX_CHUNKS_WITHOUT_PROMOTE  # noqa: F401 - 作为默认值使用
+from app.constants import MAX_CONSECUTIVE_CHUNKS as _D_CONSECUTIVE, MAX_CHUNKS_WITHOUT_PROMOTE as _D_NO_PROMOTE  # noqa: F401 - 作为默认值使用
+from app.config import get_config
```

```diff
@@ L34
-    def __init__(self, max_consecutive: int = MAX_CONSECUTIVE_CHUNKS, max_chunks_before_stop: int = MAX_CHUNKS_WITHOUT_PROMOTE):
+    def __init__(self, max_consecutive: int = None, max_chunks_before_stop: int = None):
+        if max_consecutive is None:
+            max_consecutive = get_config().get("tuning.agent.max_consecutive_chunks", _D_CONSECUTIVE)
+        if max_chunks_before_stop is None:
+            max_chunks_before_stop = get_config().get("tuning.agent.max_chunks_without_promote", _D_NO_PROMOTE)
```

### 6.11 backend/app/services/agent/initialize_run_state.py（L38）

**改动：import 改为别名回退（L38）**

```diff
--- a/backend/app/services/agent/initialize_run_state.py
+++ b/backend/app/services/agent/initialize_run_state.py
@@ L38
-from app.constants import MAX_CONSECUTIVE_CHUNKS
+from app.constants import MAX_CONSECUTIVE_CHUNKS as _D_CONSECUTIVE
+from app.config import get_config
```

> 注：`MAX_CONSECUTIVE_CHUNKS` 在本文件中仅作为 import 存在（实际使用通过 `ChunkBuffer()` 构造间接消费），import 改别名即可，ChunkBuffer 已在 6.10 改读配置。

### 6.12 backend/app/api/v1/config_schemas.py（L17）

**改动：import 改为别名回退（L17）**

```diff
--- a/backend/app/api/v1/config_schemas.py
+++ b/backend/app/api/v1/config_schemas.py
@@ L17
-from app.constants import DEFAULT_MAX_STEPS
+from app.constants import DEFAULT_MAX_STEPS as _D_MAX_STEPS
+from app.config import get_config
```

> 注：`DEFAULT_MAX_STEPS` 仅用于 `ConfigUpdate.max_steps` 的 Field 默认值描述，实际运行时 max_steps 由 `config.yaml agent.max_steps` 控制。import 改别名即可，不影响 API schema。

### 6.13 backend/app/services/prompts/project_context.py（L15）

**改动：import + 使用点改读配置（L15）**

```diff
--- a/backend/app/services/prompts/project_context.py
+++ b/backend/app/services/prompts/project_context.py
@@ L15
-from app.constants import PROJECT_CONTEXT_MAX_CHARS
+from app.constants import PROJECT_CONTEXT_MAX_CHARS as _D_CONTEXT_CHARS
+from app.config import get_config
```

> 注：`PROJECT_CONTEXT_MAX_CHARS` 在本文件中被 `load_project_context` 使用（截断上限），改读配置点在函数内部使用处。

### 6.14 backend/app/services/agent/tool_runner.py（L12）

**改动：import + 使用点改读配置（L12）**

```diff
--- a/backend/app/services/agent/tool_runner.py
+++ b/backend/app/services/agent/tool_runner.py
@@ L12
-from app.constants import ACTION_LOG_RESULT_MAX_CHARS
+from app.constants import ACTION_LOG_RESULT_MAX_CHARS as _D_LOG_CHARS
+from app.config import get_config
```

> 注：`ACTION_LOG_RESULT_MAX_CHARS` 在 `execute_tools` 内部用于截断 tool_result 日志，改读配置点在使用处。

### 6.15 backend/app/services/agent/handlers/handle_action.py（L272）

**改动：import + 使用点改读配置（L272）**

```diff
--- a/backend/app/services/agent/handlers/handle_action.py
+++ b/backend/app/services/agent/handlers/handle_action.py
@@ L272
-from app.constants import ACTION_LOG_RESULT_MAX_CHARS
+from app.constants import ACTION_LOG_RESULT_MAX_CHARS as _D_LOG_CHARS
+from app.config import get_config
```

> 注：`ACTION_LOG_RESULT_MAX_CHARS` 在本文件中用于 `build_observation` 截断 tool_result 日志，改读配置点在使用处。

### 6.16 backend/app/services/chat/message_service.py（L32/L43）

**改动：import + 使用点改读配置（L32, L43）**

```diff
--- a/backend/app/services/chat/message_service.py
+++ b/backend/app/services/chat/message_service.py
@@ L32
-from app.constants import MAX_CACHE_SIZE
+from app.constants import MAX_CACHE_SIZE as _D_CACHE_SIZE
+from app.config import get_config
```

```diff
@@ L43
-display_name_cache = LRUCache(max_size=MAX_CACHE_SIZE)
+display_name_cache = LRUCache(max_size=get_config().get("tuning.stream_task.max_cache_size", _D_CACHE_SIZE))
```

### 6.17 backend/app/services/task/hitl_confirmation.py（L47）

**改动：import 改为别名回退（L47）**

```diff
--- a/backend/app/services/task/hitl_confirmation.py
+++ b/backend/app/services/task/hitl_confirmation.py
@@ L47
-from app.constants import HITL_TIMEOUT, MAX_PENDING_CONFIRMATIONS
+from app.constants import HITL_TIMEOUT as _D_HITL_TIMEOUT, MAX_PENDING_CONFIRMATIONS as _D_MAX_CONFIRM
+from app.config import get_config
```

> 注：`HITL_TIMEOUT` 在 `_cleanup_stale_confirmations` 中使用（已改读 `security.hitl_timeout`），`MAX_PENDING_CONFIRMATIONS` 在 `create_confirmation` 中使用，改读配置点在各自使用处。

### 6.18 backend/app/services/agent/handlers/hitl_gateway.py（L82）

**改动：import 改为别名回退（L82）**

```diff
--- a/backend/app/services/agent/handlers/hitl_gateway.py
+++ b/backend/app/services/agent/handlers/hitl_gateway.py
@@ L82
-    from app.constants import HITL_TIMEOUT, HITL_CONFIRM_LEAD, HITL_MIN_CONFIRM_TIMEOUT, BYPASS_AUTO_LEAD
+    from app.constants import HITL_TIMEOUT as _D_HITL_TIMEOUT, HITL_CONFIRM_LEAD as _D_CONFIRM_LEAD, HITL_MIN_CONFIRM_TIMEOUT as _D_MIN_TIMEOUT, BYPASS_AUTO_LEAD as _D_BYPASS_LEAD
+    from app.config import get_config
```

> 注：`_resolve_timeouts` 内部已使用 `cfg.get("security.hitl_timeout", HITL_TIMEOUT)` 模式，新增 tuning 键后改为 `cfg.get("tuning.hitl.hitl_min_confirm_timeout", _D_MIN_TIMEOUT)` 等。

### 6.19 backend/app/main.py（L52/L82-86 附近 CORS）

**改动：import + 使用点改读配置（L52）**

```diff
--- a/backend/app/main.py
+++ b/backend/app/main.py
@@ L52
-from app.constants import DEFAULT_CORS_ORIGINS
+from app.constants import DEFAULT_CORS_ORIGINS as _D_CORS
+from app.config import get_config
```

> 注：`DEFAULT_CORS_ORIGINS` 在 `app.add_middleware(CORSMiddleware, allow_origins=...)` 处使用，改为 `get_config().get("tuning.network.cors_origins", _D_CORS).split(",")`。

### 6.20 backend/app/tools/fundamental/shell_engine.py（L1077）

**改动：`shell_pool` 单例构造改读配置（L1077）**

```diff
--- a/backend/app/tools/fundamental/shell_engine.py
+++ b/backend/app/tools/fundamental/shell_engine.py
@@ L1077
-shell_pool = ShellPoolManager(max_per_type=8)
+from app.config import get_config as _get_cfg
+shell_pool = ShellPoolManager(max_per_type=_get_cfg().get("tuning.concurrency.shell_pool_max_per_type", 8))
```

### 6.21 前端（无需新增文件）

- `GROUP_ORDER` 已含 tuning → `useSettings` 动态拉 schema → `SettingsGroup` 8 节自动渲染
- `SettingRow` 已支持 int/float/range/select/bool/textarea/text 类型
- 新增 31 键全部已有类型覆盖，无需新增前端组件

### 6.22 回归验证

```bash
# 1. 编译检查
python -m py_compile backend/app/constants.py
python -m py_compile backend/app/config.py
python -m py_compile backend/app/services/settings/settings_registry.py
python -m py_compile backend/app/services/settings/settings_service.py
python -m py_compile backend/app/llm/base_service.py
python -m py_compile backend/app/llm/client_sdk.py
python -m py_compile backend/app/services/agent/llm_call.py
python -m py_compile backend/app/services/chat/stream_orchestrator.py
python -m py_compile backend/app/services/task/task_registry.py
python -m py_compile backend/app/services/tool/tool_facade.py
python -m py_compile backend/app/services/agent/universal_agent.py
python -m py_compile backend/app/services/agent/chunk_buffer.py
python -m py_compile backend/app/services/agent/initialize_run_state.py
python -m py_compile backend/app/api/v1/config_schemas.py
python -m py_compile backend/app/services/prompts/project_context.py
python -m py_compile backend/app/services/agent/tool_runner.py
python -m py_compile backend/app/services/agent/handlers/handle_action.py
python -m py_compile backend/app/services/chat/message_service.py
python -m py_compile backend/app/services/task/hitl_confirmation.py
python -m py_compile backend/app/services/agent/handlers/hitl_gateway.py
python -m py_compile backend/app/main.py
python -m py_compile backend/app/tools/fundamental/shell_engine.py

# 2. settings TDD 四件套
pytest backend/tests/test_settings_registry_tdd.py backend/tests/test_settings_service_tdd.py backend/tests/test_settings_routes_tdd.py backend/tests/test_settings_editsave_red.py -q

# 3. git diff 与本章一一对应
git diff --stat
```

---
编写人：小欧 2026-09-22
