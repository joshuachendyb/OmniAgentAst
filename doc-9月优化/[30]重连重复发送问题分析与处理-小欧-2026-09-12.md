# 重连重复发送问题分析与处理

创建时间：2026-09-12 09:05:03
编写人：小欧

## 摘要

本文档对「SSE 断线重连后，后端已发过的事件是否/为何会**重复发送**」进行完整分析与处理方案定稿。核心结论：

1. **重连不需要重复发**——只需补发「断点之后」未收到的新帧；**重复发不好**（chunk 增量二次拼接、步骤双份渲染）。
2. 重连机制本质是**传输可靠层**，与「订阅(yield) / publish」两代事件生产机制**无关**，一套代码跨两代共用、行为等价。
3. **三道防线已就位**：后端每帧注入 seq（publish）→ 前端精确续传（after_seq=lastSeqRef+1）→ 前端 S3 seq 守卫去重兜底。
4. 历史「一直没解决」的根因（实证）：①早期重连不带 after_seq 而从 0 全量重播；②seq 守卫早期拿 `step.step`(string) 与 number 比较恒 false，守卫**形同虚设**——09-12 P0-1 已归一化修复。
5. **当前仍旧存活 1 处基准错位**：终态守卫记录 `stepNum`（步骤号）、作废守卫比较 `seq`（事件序号），两套度量衡混比，需修复（§六~七）。
6. **处理分工全景（§六）**：后端 4 项已实施（seq 注入 / 断点续读 / 重连不重播 / 挡 thought）、前端 6 项已实施（lastSeqRef / 精确续传 / S3 守卫 / 终态作废 / 防重复起任务 / 类型归一化 P0-1 今日刚完成）；剩余待实施 2 项——前端终态守卫基准错位（必改）、后端通道路由白名单化（可选，白名单方法已认可待批准实施）。

**实证版本基线**：本文以当前 HEAD（`4bf3ea987`，2026-09-12）前端/后端代码实读为准，均已标注文件+行号；历史面基于代码注释记载（含编辑历史）。

## 一、问题定义

### 1.1 重连成功后需要重复发吗？

**不需要。** 断线只代表「断点之后」的新帧没收到；已收到并处理过的帧（前端已按 seq 推进 `lastSeqRef`）重连后应跳过。重连的职责仅是**续传**，不是**重播**。

### 1.2 重复发好吗？

**不好**，具体危害：

| 重复对象 | 危害 |
|---------|------|
| chunk（正文字符流） | chunk 是逐字增量数据，重复 = 正文二次拼接、run-on 叠加 |
| thought（推理） | 实时 thought + 回放 thought 双通道重复显示（G1 同源） |
| step 类（start/action/observation/final） | 步骤双份渲染、快照错位、计数重复 |
| 终态后帧（final 之后的业务帧） | 污染已完结渲染态（pendingSteps/executionSteps） |

### 1.3 前端要如何配合？

两条腿走路：
1. **精确续传**：重连请求带 `after_seq=lastSeqRef.current+1`，让后端只发断点后的帧（源头不重复）；
2. **seq 幂等去重兜底**：即使后端/网络真发了重复帧、乱序帧，前端按全局事件序号把重复帧**吞掉**（免疫能力）。

## 二、重连机制原貌（跨订阅/publish 两代共有）

重连是**传输可靠层**，不依赖事件如何产生，一套代码贯穿两代：

```
前端 lastSeqRef(已处理最大seq)
  → 重连 GET ?after_seq=lastSeqRef.current+1      (useSSE.ts:789)
  → 后端 stream_reader offset=after_seq             (stream_orchestrator.py:458)
  → 从 event_log 断点续读 → 逐帧 format_agent_sse 转发
  → 前端 sseParser 入口 S3 守卫(seq<=lastSeqRef 拦截) → onSeq 推进 lastSeqRef
```

「重连复用同一函数（传 after_seq 续传），避免重复事件」为北京老陈 2026-07-12 拍定（`stream_orchestrator.py:452-454` 注释原文），早于 4C（09-06）两个多月。

## 三、现状三道防线（实证）

| 防线 | 位置 | 作用 |
|------|------|------|
| 1 后端每帧注入 seq | `task_state.py:44-54`（publish 重写 `seq=len(event_log)`） | 全局递增、连续、全帧携带 |
| 2 前端重连精确续传 | `useSSE.ts:789` `?after_seq=${lastSeqRef.current+1}` → 后端 `stream_orchestrator.py:458` `offset=after_seq` | 只发断点后的帧，源头不重复 |
| 3 前端 seq 去重守卫 | `sseParser.ts:190-197`（`seq<=lastSeqRef` 拦截）+ L199-215 终态后作废 + L218-220 onSeq 推进 | 重复/乱序/终态后帧进不了业务 |

辅助点：
- `lastSeqRef` 初始 -1（`useSSE.ts:514`，首帧 seq=0 不被误拦）；新任务重置 -1、终态标记重置 -1（`useSSE.ts:775-776`）；重连**不断不重置**（保证续传基线连续）。
- 热路径与 done 收尾两路均推进 `lastSeqRef`（`useSSE.ts:894 / 946`）。

## 四、历史「一直没解决」的根因（实证）

两个历史根因横跨订阅/publish 两代，与 4C 改造无关：

1. **早期重连不带 after_seq（或从 0）**：前端重连请求未传断点，后端 stream_reader 从 event_log 头重播**全部事件** → 全量重复。09-10 S3 修复：改为 `after_seq=lastSeqRef.current+1`。
2. **seq 守卫比较基准失效（真凶）**：早期守卫直接拿 `step.step`（后端下发为 string）与 `lastSeqRef`(number) 比较 → **string/number 比较恒 false → 守卫形同虚设**，重复帧一路漏进业务。09-12（本文落盘当日）P0-1 三堂会审修复：统一 `toStepNumber()` 归一化（`sseParser.ts:562` error、L579 final、L562/L645 等，编辑历史见 `sseParser.ts:73`）。

> 结论：代码里一直躺着守卫，但 09-12 之前它从未生效——这就是「感觉一直没解决」的实证。

## 五、订阅时代 vs publish 时代对比（判定：同层机制、行为等价）

| 维度 | 订阅时代（4C 前） | publish 时代（4C 后） | 判定 |
|------|------------------|----------------------|------|
| 重连核心代码 | `stream_orchestrator.py` stream_reader，after_seq 续读 event_log | **同函数未动**（4C 未改该文件） | 相同 |
| 前端三条防线 | after_seq / lastSeqRef / S3 守卫 | 同 | 相同 |
| seq 分配对象 | 仅「应转发事件」占号（thought 不入 event_log） | 全量 publish 事件占号（thought 也占号） | 差异 |
| 对续传精度影响 | — | thought 帧被 `_SSE_EXCLUDE_TYPES` 过滤不转发 → 前端 seq 跳号，但只用「已收最大 seq」续传 → **跳号无碍** | 等价 |
| 重连是否重发 thought | 否（不在 event_log） | 当前 HEAD **否**（`_SSE_EXCLUDE_TYPES` 已挡）；4C~09-11 期间**是**（G1 窗口，未过滤 → 实时+重连双通道重复） | 现已对齐 |

**一句话：重连重复是老问题、跨时代共有，不是 4C 引入，4C 也没修掉。唯一与 publish 相关的传染是 G1 的 5 天窗口（thought 也混进重连流），09-11 已修复。**

## 六、前后端处理方法与实施状态全景

### 6.1 重复问题的完整来源清单（分析）

| 来源 | 问题表现 | 成因 | 状态 |
|------|---------|------|------|
| S1 全量重播 | 重连从 event_log 头重发全部事件 | 早期重连请求不带 after_seq（或从 0） | 已修（09-10 S3：前端 `after_seq=lastSeqRef+1`；后端 07-12 即有 `offset=after_seq` 续读） |
| S2 守卫失效 | 重复帧漏进业务（守卫躺着不干活） | seq 守卫早期拿 `step.step`(string) 与 number 比较恒 false | 已修（09-12 P0-1：`toStepNumber()` 归一化） |
| S3 thought 混入重连流 | 重连补发 thought → 推理双通道重复 | 4C 后 thought publish 进 event_log、stream_reader 当时未过滤 | 已修（09-11 `_SSE_EXCLUDE_TYPES`，实时/重连共用） |
| S4 终态标记基准错位 | 终态后晚到帧漏拦/误伤 | `terminalSeqRef` 记 stepNum、作废守卫比 seq | **待修（§6.4 T1 / §7.1）** |
| S5 final 完整条双通道（G2） | 实时完整条 + chunk 二次渲染重复 | 实时流拿到 publish 完整条（与重连续传的短信号形态不一致） | 方案已定：前端三态消化（见《[28]final三态字段验证与前端渲染契约方案》），后端不剥离 |
| S6 首响应未到断连重复起任务 | 双任务/僵尸任务 | 尚无任务 ID 却再次 POST 起新任务 | 已修（08-30：无任务 ID 不 POST，走统一错误中心判失败） |

### 6.2 后端已实施（4 项）

| # | 措施 | 代码位置 | 实施时点 |
|---|------|---------|---------|
| B1 | publish 每帧注入 seq（全局递增、连续） | `task_state.py:44-54` | 4C 共源设计 |
| B2 | stream_reader 按 `offset=after_seq` 续读（实时/重连同一咽喉点） | `stream_orchestrator.py:458` | 2026-07-12（老陈拍板） |
| B3 | 重连端点只续传：`get_stream_buffer(task_id)` 不存在 → 发 not_found 错误，**不重播** | `chat_routes.py:114-120` + `chat_stream_reconnect_orchestrator`（`stream_orchestrator.py:513-524`） | 07-12 / 08-13 |
| B4 | `_SSE_EXCLUDE_TYPES` 挡 thought（实时+重连续传共用，杜绝 G1 复发） | `stream_orchestrator.py:447` | 2026-09-11 |

### 6.3 前端已实施（6 项）

| # | 措施 | 代码位置 | 实施时点 |
|---|------|---------|---------|
| F1 | `lastSeqRef` 记录已处理最大 seq（初始 -1；新任务重置；**重连不断不重置**保持续传基线） | `useSSE.ts:514`、L775-776 | 09-10 S3 |
| F2 | 重连精确续传 `?after_seq=lastSeqRef.current+1`；热路径与 done 收尾**双路推进** lastSeqRef | `useSSE.ts:789`、L894/L946 | 09-10 S3 |
| F3 | S3 seq 守卫去重（`seq<=lastSeqRef` 拦截，先于 switch/onSeq） | `sseParser.ts:190-197` | 09-10 S3 |
| F4 | 终态后作废守卫（final/error 之后更高 seq 的非豁免帧作废，防渲染态污染；豁免 start/final_stats/usage） | `sseParser.ts:199-215`、L557/L638 | 09-10 C1/C2 |
| F5 | 无任务 ID 不重复 POST 起任务（根治重复起任务） | `useSSE.ts:1030-1035` | 2026-08-30 |
| F6 | seq 类型归一化 `toStepNumber()`（修复 S2 string/number 比较失效，守卫真正生效） | `sseParser.ts:562/579/646` 等 | **2026-09-12 P0-1（今日）** |

### 6.4 待实施（2 项）

| # | 归属 | 措施 | 改动点 | 定级 |
|---|------|------|--------|------|
| T1 | 前端 | 终态守卫统一记 seq（`stepNum` → `rawData.seq`） | `sseParser.ts:557`（final）+ L638（error） | **必改**（行为等价、低风险，详见 §7.1） |
| T2 | 后端 | 通道路由白名单化 `_SSE_FORWARD_TYPES`（显式化实时/重连共用咽喉点的过滤规则）+ 防漂移单测 | `stream_orchestrator.py:447` 集合 + L466 判定；单测断言 `_SSE_FORWARD_TYPES ⊇ 注册实时类型 − {thought}` | 可选加固（白名单方法已认可，待批准实施，详见 4C 核查报告 §7.1） |

> **G2（S5）后端决断**：保持 publish 完整条不动（event_log 完整落库为唯一权威源，任何前置剥离将污染历史回放）；按《[28]final三态字段验证与前端渲染契约方案》在前端快照按 outcome 消化——属独立工作线，不列入本节实施清单。

## 七、处理方案（待批准，动代码先批）

### 7.1 终端守卫统一记 seq（必须改）

final 分支（`sseParser.ts:556-557`）与 error 分支（`sseParser.ts:637-638`）由「记 `stepNum`」改为「记本轮事件真正的 seq」：

```ts
// final 分支（替换 L557）
if (typeof rawData.seq === 'number' && terminalSeqRef) {
  terminalSeqRef.current = rawData.seq;   // 与作废守卫比较基准一致：都是事件序号
}
// error 分支（替换 L638）同构
```

**行为等价性论证**：
- 修复前：终态标记=步骤号；作废条件 `seq > stepNum`（宽松阈值）；
- 修复后：终态标记=终态事件的 seq；作废条件 `seq > finalSeq`（精确阈值）；
- 两类「终态后晚到帧」检测语义完全一致（目的：晚于终态的更高 seq 业务帧不进渲染）；豁免清单（start/final_stats/usage，`sseParser.ts:207-209`）保持不变；
- `rawData.seq` 非 number 时回退不设终态标记（守卫同样非 number 不拦截，行为一致）。

### 7.2 验证（回归）

1. **重连不重复**：真实后端 + 真实 LLM 跑任务，中断网络→重连，确认 `after_seq` 续传帧无重复、无重复渲染（遵循 AGENTS.md E2E 铁律：真实后端/真实 LLM/真实工具/真实 SQLite，禁 Mock）。
2. **S3 守卫兜底**：构造重复帧（`seq <= lastSeqRef`）→ 断言被拦、业务不执行。
3. **终态后作废**：final 后注入更高 seq 的非豁免帧 → 断言被作废（防 pendingSteps 污染）。
4. **跳号续传**：含 thought 占号的会话重连，确认 seq 跳号不破坏断点衔接。
5. **终态标记边界**：final/error 分支 `rawData.seq` 为 number 时终态标记=seq；非 number 时不设标记。

## 八、结论

1. 重连重复发送是**传输可靠层的老问题**，跨订阅/publish 两代同一机制、同一代码、行为等价，并非 4C 引入。
2. 三道防线（后端 seq 注入 / 前端精确续传 / S3 去重守卫）已就位；历史「没解决」的真凶（守卫 string/number 比较失效）已于 09-12 P0-1 修复。
3. 剩余**1 处存活的基准错位**（终态守卫记 stepNum、比 seq）按 §7.1 修复，行为等价、风险低。

## 九、与其他方案的关系

| 方案 | 问题域 | 关系 |
|------|--------|------|
| 4C 通道路由缺口 G2（final 完整条双通道重复） | 渲染层：same final 实时完整条 + chunk 二次渲染 | **不同源**：G2 是渲染重复非重连重复；修复=前端 final 三态消化，与本文 §7.1 互不冲突 |
| G1 thought 实时/重连漏发 | 通道路由：thought 本应仅落库 | 已修（`_SSE_EXCLUDE_TYPES`）；本文 §五 确认重连路径同样不再发 thought |
| 终端守卫基准错位（本文） | 传输可靠层去重兜底 | **独立修复**，仅前端 `sseParser.ts` 两行 |

## 十、版本历史

| 版本 | 时间 | 作者 | 变更说明 |
|------|------|------|---------|
| v1.0 | 2026-09-12 09:05:03 | 小欧 | 创建：重连重复发送问题分析与处理——问题定义（不需重发/重发有害）、机制原貌（传输可靠层两代共有）、三道防线实证、历史根因实证（after_seq 缺失 + seq 守卫 string/number 失效，后者 09-12 P0-1 已修）、订阅 vs publish 时代对比、当前存活基准错位（终态守卫记 stepNum 比 seq）、处理方案（统一记 seq + 回归验证 5 项） |
| v1.1 | 2026-09-12 09:06:54 | 小欧 | 修订：新增 §六「前后端处理方法与实施状态全景」——①重复问题完整来源清单 S1~S6（含成因/状态）；②后端已实施 4 项（seq 注入/断点续读/重连不重播 not_found/挡 thought）实证；③前端已实施 6 项（lastSeqRef/精确续传双路推进/S3 守卫/终态作废/防重复起任务/toStepNumber 归一化）实证；④待实施 2 项（T1 前端终态守卫记 seq 必改、T2 后端白名单化可选已认可）；⑤后端重连端点 buffer 语义实证（`get_stream_buffer` 不存在→not_found 不重播）；原 §六「当前存活问题」内容并入全景 6.1 S4 |