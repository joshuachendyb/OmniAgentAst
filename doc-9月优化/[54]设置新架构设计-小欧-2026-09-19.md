# 前后端统一设置新架构设计（设置项目与后端设置文件一一对应）

**创建时间**: 2026-09-19 20:20:39

**当前位置**: `F:\OmniAgentAs-repair\doc-9月优化\[54]设置新架构设计-小欧-2026-09-19.md`

## 版本历史

| 版本 | 更新时间 | 编写人 | 更新要点 |
|------|---------|--------|---------|
| v4.10 | 2026-09-20 01:24:08 | 小欧 | 【第十章验收排错】逐条对照前9章+真实代码揪出 10 处错误并修正：①漏 GET /settings 全量行（补）；②schema 行"全网唯一"→全局唯一、删"无默认值泄露"、secret 无明文移值接口、readonly 明确三键；③PUT 新 key"走 update_config"与 9.1.2 矛盾→改分区写实；④PUT secret 三态 settings 无 secret 项不可测→改挂 PUT /providers 并给 9.1.3/9.1.6/9.2.2 补 clear:true；⑤POST /providers"返回更新后列表"与 5.2/9.1.3 不一致→改；⑥写保护行"都经 update_config"改分区写实；⑦系统"10 项读写"→8 读写+2 只读；⑧重复模型"前端校验"→后端校验+toast；⑨CRUD 写入行同⑥；⑩?group=X 加 model 组空返回注脚 |
| v4.9 | 2026-09-20 01:17:46 | 小欧（文档）/小沈（后端代码）/小强（前端代码） | 【第九章 代码设计】新建第九章：按第八章每个文件功能要求编写可实施真实代码/diff——9.1 后端6新建（registry/service/model_service/schemas/routes 全量代码）+ 9.2 前端新建（api×2/Settings2/tokens/types/useSettings/modelUtils/icons/13组件 全量代码）+ 9.3 修改类diff（main/file_utils/config_helpers/api-index/handler/App/Layout/FUNCTIONS）；附带补第八章/5.1 config_helpers.py 修改行（write_yaml 改调 atomic_write，修改1→2） |
| v4.8 | 2026-09-20 01:13:00 | 小欧 | 【8.3 代码目录树结构】新增第八章 8.3 小节：后端/前端目录树逐文件标注【新建/修改/已有复用/不动】，与第八章落地清单一一对应；5.1 目录依据补 settings 域归属说明（业务服务域收 services/，不直置 app 顶层） |
| v4.7 | 2026-09-20 01:07:34 | 小欧 | 【三思三省·第八章目录架构修订】实测现有代码：①`services/model/` 已有 config_service/config_helpers/resolver——`model_service.py` 从 `services/settings/` 移入 `services/model/`（模型域聚合）；②`config_schemas.py` 已定义全部模型 DTO——`settings_schemas.py` 只定义 settings 特有 DTO，模型 DTO 复用 config_schemas，不重复定义；③前端 `features/settings2/` 16 文件平铺违反惯例（chat/settings 域均有子目录）——拆 `components/`（13组件+icons）+ `hooks/useSettings.ts` + `utils/modelUtils.ts` + `types.ts`；5.1/6.1/7.9.5/第八章/8.1.1 路径同步 |
| v4.6 | 2026-09-20 01:00:10 | 小欧 | 【第九章 11 条风险逐条对策+代码处理完整化】按"风险→对策→代码处理（文件·函数·逻辑）"逐条写全；实测核实 `_write_system_yaml` 是 `open('w')` 直写覆盖、**无原子重命名**、`update_config` 验证成功即删备份——"原子重命名+编号备份链"现实不存在，v4.6 将实现收敛为：`file_utils.py` 新增 `atomic_write`（临时文件+os.replace）+ `backup_file` 升级编号链 backup.1~5+FIFO；第八章该行、8.1.2-#1 描述同步更新 |
| v4.5 | 2026-09-20 00:58:06 | 小欧 | 【第九章风险逐条对策】11 条风险全部补【对策】+【涉及代码】；仅 1 条需补代码进第八章（风险2 备份编号链 → 新增 file_utils.py 修改项 backup.1~5+FIFO）；8.1 缺口#1/#2 状态同步更新（#1 已补方案、#2 措辞已修正）；一.7 删除"启动校验恢复"（v3.0 已删启动自检），改为"写错回滚最近一个备份" |
| v4.4 | 2026-09-20 00:33:56 | 小欧 | 【8.1 设计要点↔代码文件对应核算】新增第八章 8.1 小节：正向"代码→设计要点"逐文件对应表 + 反向"设计要点→代码"缺口核算；发现 8 个设计要点无对应代码文件（2 真缺口：后端编号备份链、前端 schema 校验；5 缺归属：预览小卡/危险确认/切Tab确认/还原默认/重启Modal；1 文档矛盾：一.7"启动校验"v3.0 已删需删措辞） |
| v4.3 | 2026-09-20 00:10:45 | 小欧 | 【三思三省·7.9重构】settingsTokens 拆为五独立对象继承 stepStyles；新增 pagePadding/rowHeight/labelWidth 三个派生常量不动大表；文件清单精简；6.1/第八章同步；菜单名"新设置"→"设置2版"；目录 settings-new→settings2；页面 SettingsNew→Settings2；路由 /settings-new→/settings2 |
| v4.2 | 2026-09-20 00:09:42 | 小欧 | 【三思三省·推翻7.9】推翻大表设计：settingsTokens 拆为 FontSize/FontWeight/Spacing/Radius/Colors 五独立对象继承 stepStyles；只增 pagePadding/rowHeight/labelWidth 三个派生常量；6.1/第八章同步精简 |
| v4.1 | 2026-09-20 00:07:21 | 小欧 | 【设置页视觉体系】新增 7.9：字体三档/色彩同源/settingsTokens 独立清单/图标映射表/间距留白约束/代码模块化清单；7.2 页 padding 25→16；6.1/第八章文件清单同步模块化文件 |
| v4.0 | 2026-09-19 23:30:00 | 小欧 | 【全面完善·21点改进+模型CRUD】设计目标7条（单一入口/显示即生效/单存储/前门后门/易用性/诚实标注/健壮）；①通用(app.language+project_root)+②模型+③安全+④聊天+⑤外观+⑥系统(内部三小节)；②模型Tab四区域完整CRUD(选择器/参数区/配置区/操作区)+双下拉联动+脏态保持+范围截断+删除级联+自动切换默认；③加搜索框、全局保存条+保存本组并存、全局脏角标；④单useSettings管全组状态+模型管理状态；⑤全量GET一次拉取+getModels；⑥备份链命名+清理策略+启动校验；⑦theme标注深色二期、provider切换来源、未接入展示策略、后端门禁独立；⑧model_service+model_routes后端服务；⑨缺陷加12-19条 |
| v3.0 | 2026-09-19 22:28:42 | 小欧 | 【文本文件务实化】删四层过度防护（校验和/启动自检/内存兜底），保留真正必要的两层：原子重命名（文本文件不会半写）+ 最近 5 个备份链（防写错值）；第九章风险、第十一章第 11 条措辞同步修正 |
| v3.0 | 2026-09-19 22:12:56 | 小欧 | 【架构收敛】存储收敛为单 YAML：删 `user_settings` 表与 merge 逻辑，chat/appearance 全部进 `config.yaml`；`settings_service` 改为调用 `config_service.update_config` 加法扩展（纯 additive，不改旧逻辑）；删 sqlite 备份故事；来源列只留 yaml/env；落地清单删表与 main.py 行；验收表删 DB 断言；第十一章加第 11 条"双存储过度设计" |
| v2.1 | 2026-09-19 21:52:46 | 小欧 | 【北京老陈指令】新设置页菜单名定为"新设置"：Layout 旧"系统设置"项改名+改指 /settings2（单导航入口不变）；同步 6.1/7.0/第八章三处描述 |
| v2.0 | 2026-09-19 21:26:13 | 小欧 | 【北京老陈指令·推翻重写】v1.x 十处重大缺陷推倒重来（清单见第十一章）：①单导航入口接管（旧页文件零改动、旧路由保留可访）；②后端 settings 只做编排，YAML 写操作复用 config_service（验证+备份+回滚不断）；③模型走 ai_model_ref 结构化语义；④env 覆盖透明化（有效值+来源标注，只读）；⑤secret 永不返明文（configured+suffix 契约）；⑥砍掉 PreferenceContext/双 hook/全量双写，页面级单层 state；⑦模型 Tab 直接嵌入复用 ProviderSettings，不重写 CRUD；⑧主题 v1 只读、深色二期；⑨chat.* 诚实标注未接入；⑩第十章改为逐项验收表；路由挂载点纠正为 main.py |
| v1.4 | 2026-09-19 21:10:00 | 小欧 | 【北京老陈指令·画出与现有框架关系】第七章新增 7.0 框架关系总图；7.3 改为 6 分组每页一张内容图；修正 4.5 与 4.1 的 app.language 重复键（key 全局唯一） |
| v1.3 | 2026-09-19 21:08:00 | 小欧 | 【北京老陈指令·删图重画】第七章 UI 全面重画为与现有设置页同骨架（settings-page+Card+Tabs，3 Tab→6 Tab） |
| v1.2 | 2026-09-19 20:57:49 | 小欧 | 【北京老陈指令】第六章/第七章改为落在现有前端框架内；旧页保留不动，新页独立挂载；修正 2.3 为 F 盘真实基线 |
| v1.1 | 2026-09-19 20:23:36 | 小欧 | 文档名称加序号；新增第七章前端设置页 UI 设计方案；原七/八/九章顺延为八/九/十 |
| v1.0 | 2026-09-19 20:20:39 | 小欧 | 首次创建：检索清单 + Schema驱动统一设置架构 + 映射表 |

---

## 一、设计目标（v4.0 收敛为七条）

1. **单一设置入口**：用户视角只有一个"设置"。旧页文件零改动、旧路由保留可访，导航只留一个入口指向新页。
2. **显示即生效**：页面显示的值 = 后端实际生效的值；被环境变量接管的项明确标注来源并只读。
3. **单存储**：所有数据存一个 `config.yaml`，无第二存储。
4. **前门后门**：设置页面服务与后端业务服务读取逻辑完全独立，写入最终都经 `config_service.update_config`（原子重命名+备份链）。时序和信息量完全不同。
5. **易用性**：搜索框快速定位、全局保存条一次保存所有改动、跨Tab脏态可见、初始化全量加载零延迟切Tab。
6. **诚实标注**：没接消费方的配置项不标"生效"；密钥永不明文下发；未接入项明确说明；深色二期标注工作量前提。
7. **健壮**：原子重命名防半写 + 最近 5 个编号备份链（backup.1 最新~backup.5 最旧）+ FIFO 清理 + 启动校验失败自动用备份恢复。

---

## 二、现状基线（F 盘真实源码，2026-09-19 核验）

### 2.1 后端

- 路由挂载：`backend/app/main.py:148-159`，`app.include_router(..., prefix="/api/v1")`；`api/v1/__init__.py` 为空文件（v1.x 曾误写挂载点，v2.0 纠正）。
- 配置装载：`backend/app/config.py`，`class Config` + `get_config()`；环境变量可覆盖 YAML（如 `config.py:105` AI_PROVIDER）。YAML 值不等于生效值的情况真实存在。
- YAML 现状 `config/config.yaml`：`ai.provider/ai.model` 扁平 + 各家 `{p}.api_base/api_key/timeout/max_retries/models[]` + `sensenova.model_params`；`app.*` 7 项（通用2+系统参数5）；`logging.*` 3 项；`security.*` 10 项。
- 现行 API：`api/v1/config_routes.py`（188 行薄壳）+ `services/settings/config_service.py`（业务：验证+备份+写+回滚）+ `config_helpers.py`；DTO 已结构化（`ai_model_ref` / `current_model_ref`，config_routes.py:75-83,119-122）。
- DB：`backend/app/db/models/` 现有 `chat_models.py`、`operation_models.py`；`user_settings` 已删除（v3.0 收敛单 YAML，无第二存储）。

### 2.2 前端

- 框架：`App.tsx`（路由懒加载）→ `AppLayout`（Sider 180px + Header 43px + Content）→ 页面。
- 菜单：`/对话任务`，`/files`·`/knowledge`（disabled 预留），`/history` 历史会话，`/shortcuts` 快捷指令（弹窗），`/settings` 系统设置。
- 旧设置页 `pages/Settings/index.tsx`（124 行）：白底 `.settings-page` + `Card` + `Tabs`（模型配置/ProviderSettings，安全配置/SecuritySettings，系统状态/HealthCheck）。
- API 层 `services/api/`：`client.ts`（axios，baseURL /api/v1，auth token，60s 超时）+ 各域 `.api.ts` + `index.ts` 导出桶。
- 错误：`services/error/handler.ts`（53 类 ErrorType，`handleApiError/showMessage/showSuccess`）+ `lib/antd/bridge.ts` 的 `getMessage()`。
- 硬编码浅色：设置页 `background:'#fff'`（index.tsx:108）、Header `'#fff'`（Layout:639）、Content `#f8fafc`——深色主题无样式基础，v2.0 主题 v1 只读、深色列二期。

### 2.3 缺口

- `theme` 有类型无 UI；`temperature` 等无入口、无消费方；用户偏好无持久化。

---

## 三、新架构总览（编排器模式 + 有效值透明）

### 3.1 一页看懂的数据流

```
【房间：config.yaml —— 同一个配置底文件】

【前门：设置页面服务（settings_service + model_service，完全独立）】
settings_service（读+写独立实现，不依赖 config_service 的读取逻辑）
  读：自己解析 config.yaml → 组装 {groups:{data,sources,mtime}} + env标注 + 脏态
  写：自己校验 patch → 调 config_service.update_config（原子重命名+备份链）→ config.yaml
  API：GET/PUT /api/v1/settings（6组参数）
model_service（读+写独立实现，属于设置页面服务的一部分）
  读：自己解析 config.yaml 的 models/provider_config 区域
  写：自己校验 → 调 config_service.update_config → config.yaml
  API：GET/POST/PUT/DELETE /api/v1/models /api/v1/providers

【后门：后端业务服务（config_service，完全独立）】
config_service（读+写独立实现，不依赖 settings_service）
  读：直接读 config.yaml → get_setting(key, default) 返回有效值（含env覆盖）
  写：直接调 config_service.update_config → config.yaml
  调用：后端任何位置（chat/stream 等）直接调用，无需经过 settings_service

【两者关系】
- 读：完全分开，各自独立实现，互不依赖
- 写：最终都走 config_service.update_config（原子重命名+备份链），这是底层文件操作的唯一入口
- 存储：同一个 config.yaml
- 时序：设置页面（人操作，慢，校验+脏态+确认） vs 后端业务（系统自动，快，实时）
- 信息量：设置页面（groups/sources/mtime/env标注/脏态） vs 后端业务（key/value 直接取）
- mtime 感知：设置页面首次加载时记录 config.yaml mtime；切 Tab/刷新时检查 mtime，若后门修改了文件（mtime 变）则自动重新 GET /settings 刷新

### 3.2 七大铁律

1. **后端唯一真相源**：key、类型、默认值、值域、存储、生效、来源规则，只在 registry 定一次；key 全局唯一，启动自检发现重复直接拒启。**存储位置统一为 YAML（`config/config.yaml`），无第二存储。**
2. **读写前门后门**：设置页面（settings_service+model_service）与后端业务（config_service）完全独立。读：各自实现，互不依赖。写：最终都走 `config_service.update_config`（原子重命名+备份链），这是底层文件操作的唯一入口。时序和信息量完全不同。
3. **显示即生效**：GET 返回有效值 + 每项来源；env 项只读并标注。
4. **密钥与生效诚实**：secret 永不返明文；无消费方的项不标生效；未接入项明确说明。
5. **一次全量 + 零延迟切Tab**：初始化全量拉取所有组值，切 Tab 不重新请求。
6. **全局状态 + 全局保存**：单 hook 管理全部组数据与脏态，底部"保存全部"一次提交所有改动。
7. **模型管理 CRUD 完整**：模型/Provider 支持查看、增加、删除、修改全生命周期操作；属于设置页面服务的一部分，前端四个区域完整覆盖。

---

## 四、设置项目与后端设置文件一一对应映射表（核心）

> 说明：
> - **存储位置**：统一为 `YAML`（`config/config.yaml`）；所有设置项（包括原 v2.0 的 DB 项 chat/appearance）都存这一个文件。无第二存储。
> - **生效方式**：`即时` = 保存立即生效；`重启` = 需重启后端；`未接入` = 已存储但尚无消费方（诚实标注，不承诺）。
> - **来源列**：`yaml` 读 YAML；`env` 被环境变量接管（页面只读+标注）。
> - **分组命名**：①通用（语言/项目根目录）；②模型；③安全；④聊天；⑤外观；⑥系统（系统参数5项 + 运维日志3项 + 关于2项只读）。logging 是运维级参数，归"系统"更合理。

### 4.1 通用（general，2 项，YAML）

| 页面控件 | 设置项 key | 显示名 | 类型/值域 | 默认值 | 存储 | 生效 | 来源 |
|---------|-----------|--------|----------|--------|------|------|------|
| 下拉 | `app.language` | 系统语言 | `zh-CN/en-US` | `zh-CN` | YAML | 重启 | yaml |
| 输入框+浏览 | `app.project_root` | 项目根目录 | 绝对路径 | `E:\test_dir` | YAML | 即时 | yaml |

### 4.2 模型（model，结构化语义 + 完整 CRUD）

| 页面控件 | 设置项 key | 显示名 | 类型/值域 | 默认值 | 存储 | 生效 | 来源 |
|---------|-----------|--------|----------|--------|------|------|------|
| 双下拉 | `ai.model_ref` | 当前模型 | `{provider, model}` 结构 | 当前 YAML 对 | YAML | 即时 | yaml（env 接管时整行只读） |
| Provider配置区 | `ProviderSettings` 组件 | 提供商配置 | 复用旧组件（api_key/base_url/timeout/retries） | — | YAML | 立即生效 | yaml |
| 模型级参数 | `ai.{provider}.{model}.*` | 模型参数 | context_length/reasoning_effort 等 | 各模型默认 | YAML | 即时 | yaml |
| 模型管理 | 新增/删除/修改 | 模型全生命周期 | 增删改查模型和Provider | — | config.yaml | 需重启生效 | model_service |

> v2.0 要点：当前模型不再用扁平双 key，而是与后端 DTO 同形的 `ai.model_ref` 结构；设置页面服务（settings_service）完全独立实现读写，写时调用 `config_service.update_config`（原子重命名+备份链）。模型/Provider 的增删改查由独立的 `model_service` 编排，写入 config.yaml 的 `models`/`provider_config` 区域。

### 4.3 安全（security，10 项，YAML，即时）

| 页面控件 | 设置项 key | 显示名 | 类型/值域 | 默认值 | 存储 | 生效 | 来源 |
|---------|-----------|--------|----------|--------|------|------|------|
| 开关 | `security.enabled` | 安全开关 | bool | `false` | YAML | 即时 | yaml |
| 开关 | `security.strict_mode` | 严格模式 | bool | `false` | YAML | 即时 | yaml |
| 开关 | `security.whitelistEnabled` | 白名单开关 | bool | `false` | YAML | 即时 | yaml |
| 文本域 | `security.commandWhitelist` | 命令白名单 | 多行命令 | `''` | YAML | 即时 | yaml |
| 开关 | `security.blacklistEnabled` | 黑名单开关 | bool | `false` | YAML | 即时 | yaml |
| 文本域 | `security.commandBlacklist` | 命令黑名单 | 多行命令 | `''` | YAML | 即时 | yaml |
| 开关 | `security.confirmDangerousOps` | 危险操作确认 | bool | `true` | YAML | 即时 | yaml |
| 开关 | `security.contentFilterEnabled` | 内容过滤 | bool | `true` | YAML | 即时 | yaml |
| 下拉 | `security.contentFilterLevel` | 过滤级别 | `low/medium/high` | `medium` | YAML | 即时 | yaml |
| 下拉 | `security.maxFileSize` | 最大文件(MB) | `10/50/100/500` | `100` | YAML | 即时 | yaml |

### 4.4 聊天（chat，6 项，YAML，未接入）

| 页面控件 | 设置项 key | 显示名 | 类型/值域 | 默认值 | 存储 | 生效 | 来源 |
|---------|-----------|--------|----------|--------|------|------|------|
| 输入框 | `chat.temperature` | 温度 | 0~2（0.1步进） | `0.7` | YAML | 未接入 | yaml |
| 滑条 | `chat.top_p` | 核采样 | 0~1（0.05步进） | `1.0` | YAML | 未接入 | yaml |
| 输入框 | `chat.max_tokens` | 最大输出 tokens | 正整数/留空=跟随模型 | 空 | YAML | 未接入 | yaml |
| 开关 | `chat.auto_title` | 自动标题 | bool | `true` | YAML | 未接入 | yaml |
| 输入框 | `chat.history_limit` | 历史加载条数 | 1~200 | `50` | YAML | 未接入 | yaml |
| 开关 | `chat.stream` | 流式输出 | bool | `true` | YAML | 未接入 | yaml |

> v2.0 要点：`未接入` = 已存储、尚无消费方，页面如实展示"已存储·待二期接入 LLM"，不承诺即时生效（纠正 v1.x 虚假"热生效"标注）。

### 4.5 外观（appearance；theme 只读，字号/密度 YAML 即时）

| 页面控件 | 设置项 key | 显示名 | 类型/值域 | 默认值 | 存储 | 生效 | 来源 |
|---------|-----------|--------|----------|--------|------|------|------|
| 只读文本 | `app.theme` | 主题 | 当前固定"浅色"，深色二期 | `light` | YAML | —（v1 不切换） | yaml |
| 滑条+预览 | `appearance.fontSize` | 字号(px) | 12~18 | `14` | YAML | 即时（本地预应用+落YAML） | yaml |
| 下拉+预览 | `appearance.density` | 消息密度 | `compact/comfortable` | `comfortable` | YAML | 即时（本地预应用+落YAML） | yaml |

> v3.0 要点：存储统一为单 YAML（`config.yaml`）。设置页面服务（settings_service）完全独立读取 config.yaml 并组装 groups/sources/mtime，写时调用 `config_service.update_config`（原子重命名）。删掉 `user_settings` 表——文本文件天然完整（原子重命名），防写错靠最近 5 个备份链。`app.theme` v1 只读展示（深色缺全站 token 样式基础，诚实做法是二期重做样式体系再开放切换）；`app.language` 只在 4.1 出现（key 全局唯一）。

### 4.6 系统（system，10 项，内部三小节）

> 内部划分：①系统参数（5 项）②运维日志（3 项）③关于（2 项只读）。视觉上用子标题分隔，Tab 内连续滚动。

#### ① 系统参数（5 项）

| 页面控件 | 设置项 key | 显示名 | 类型/值域 | 默认值 | 存储 | 生效 | 来源 |
|---------|-----------|--------|----------|--------|------|------|------|
| 开关 | `app.debug` | 调试模式 | bool | `true` | YAML | 重启 | yaml |
| 输入框 | `app.max_context_tokens` | 上下文上限 | 正整数 | `200000` | YAML | 重启 | yaml |
| 输入框 | `app.max_history_length` | 历史保留条数 | 正整数 | `10` | YAML | 即时 | yaml |
| 输入框 | `app.max_rounds` | 最大轮数 | 正整数 | `100` | YAML | 即时 | yaml |
| 输入框 | `app.max_steps` | 最大步数 | 正整数 | `10000` | YAML | 即时 | yaml |

#### ② 运维日志（3 项）

| 页面控件 | 设置项 key | 显示名 | 类型/值域 | 默认值 | 存储 | 生效 | 来源 |
|---------|-----------|--------|----------|--------|------|------|------|
| 下拉 | `logging.level` | 日志级别 | `DEBUG/INFO/WARNING/ERROR` | `INFO` | YAML | 重启 | yaml |
| 输入框 | `logging.max_file_size` | 日志文件上限(字节) | 正整数 | `10485760` | YAML | 重启 | yaml |
| 输入框 | `logging.backup_count` | 日志备份数 | 正整数 | `5` | YAML | 重启 | yaml |

#### ③ 关于（2 项只读）

| 页面控件 | 设置项 key | 显示名 | 类型/值域 | 默认值 | 存储 | 生效 | 来源 |
|---------|-----------|--------|----------|--------|------|------|------|
| 文本+复制 | `config_path` | 配置文件路径 | 只读 | — | RO | — | — |
| 文本+复制 | `version` | 当前版本 | 只读 | — | RO | — | — |

---

## 五、后端设计（编排器模式）

### 5.1 文件（新增 6，修改 2）

> 目录依据（三思三省 v4.7）：`model_service` 放 `services/model/` 与已有 `config_service/config_helpers/resolver` 同域聚合（模型/配置读写属同一域，避免模型逻辑分裂两处）；`settings_service/settings_registry` 放 `services/settings/`（设置页 6 组专属）；模型管理 DTO 复用 `api/v1/config_schemas.py` 现有类，不在 settings_schemas 重复定义。settings 域归属说明（v4.8）：`services/` 下按业务域分子目录是现有固定惯例（agent/chat/model/task/tool/visualization/…），settings 也是业务服务域，故收 `services/settings/`，与各域同构；不直置 `app/` 顶层——顶层为跨层基础件（config.py 配置装载、constants.py、db/、api/、utils/），且可避免与顶层 `config.py` 职责混淆。

| 文件 | 职责 |
|------|------|
| `backend/app/services/settings/settings_registry.py` | 唯一 Schema 源：模型列表（Provider→Model 层级）+ 6 分组；每项 `key/type/label/default/options/range/validator/storage/restart/secret/readonly/env`；模块加载时自检 key 全局唯一 |
| `backend/app/services/settings/settings_service.py` | **设置页 6 组服务（完全独立）**：读：自己解析 config.yaml → 组装 `{groups:{data,sources,mtime}}` + env标注 + 脏态；写：自己校验 patch → 调 `config_service.update_config`（原子重命名+备份链）→ config.yaml。与后端业务 config_service 完全解耦 |
| `backend/app/services/model/model_service.py` | **模型编排器**（位于 model 域，与 config_service/config_helpers/resolver 同域）：`get_models()` 合并 registry 固定模型 + config.yaml 自定义模型；`add_model()`/`delete_model()`/`update_model()` 写入 config.yaml 的 models/provider_config 区域；`get_provider_config()`/`update_provider_config()` |
| `backend/app/api/v1/settings_schemas.py` | settings 特有 DTO（schema 输出 / GET 响应含 mtime / PUT patch 与响应）；**模型管理 DTO 复用 `config_schemas.py` 现有类（ModelRef/ProviderInfo/ProviderUpdate/ModelAddRequest/ProviderAddRequest），不重复定义** |
| `backend/app/api/v1/settings_routes.py` | 薄壳接口（参数配置，与 `config_routes.py` 同构） |
| `backend/app/api/v1/model_routes.py` | **模型管理路由**：`GET/POST /models`、`GET/PUT/DELETE /models/{provider}/{model}`、`GET/POST/PUT/DELETE /providers`（与 config_routes.py 并列） |
| 修改 `backend/app/main.py` | 加 `app.include_router(settings_router, ...)` + `app.include_router(model_router, ...)` |

### 5.2 接口契约

```
【参数配置接口】
GET  /api/v1/settings
  → {groups:{key:{data,sources,mtime}}, version:"", mtime:""}  # 一次全量返回全部 6 组 + config.yaml mtime
GET  /api/v1/settings?group=X  # 按需刷新单组
  → {data:{...}, sources:{...}, mtime:""}
PUT  /api/v1/settings  body={patch:{...}}
  → {ok:true, updated:[{key, source}], need_restart:[], warnings:[]}

前端 mtime 感知逻辑：
- 首次 GET /settings → 记录返回的 config.yaml mtime
- 切 Tab / 手动刷新 → 检查 config.yaml mtime 是否变化
  → mtime 变了（后门修改了）→ 重新 GET /settings 刷新全部数据
  → mtime 没变（自己修改的）→ 缓存命中，零延迟

【模型管理接口】
GET  /api/v1/models
  → {providers:[{name,label,api_base,models:[{name,label,default_params,range,capabilities}]}], current_model_ref:{provider,model}}
POST /api/v1/models  body={provider,model,label,default_params,range,capabilities}
  → {ok:true, providers:[...], current_model_ref:...}
PUT  /api/v1/models/{provider}/{model}  body={default_params,range,capabilities}
  → {ok:true, model:...}
DELETE /api/v1/models/{provider}/{model}
  → {ok:true, switched_to:"default_model"|null}  # 如果删除的是当前模型，返回切换目标

GET  /api/v1/providers
  → [{name,label,api_base,models:[...]}]
POST /api/v1/providers  body={name,label,api_base}
  → {ok:true, provider:...}
PUT  /api/v1/providers/{name}  body={api_key,base_url,timeout,retry_times}
  → {ok:true, provider:...}  # Provider配置更新，立即生效
DELETE /api/v1/providers/{name}
  → {ok:true, switched_to:"default_provider"|null}  # 级联删除所有子模型
```

要点：
- 单 YAML 存储。设置页面服务（settings_service+model_service）与后端业务服务（config_service）读取逻辑完全独立；但写入最终都经 `config_service.update_config`（原子重命名+备份链），这是底层文件操作的唯一入口。文本文件天然完整：写时原子重命名 + 最近 5 个备份链（FIFO 清理）。
- 旧 `/config/*` 路由保留兼容；新页只调 `/settings/*`，旧页照旧调 `/config/*`（同库同源）。
- 读：设置页面服务（settings_service+model_service）与后端业务服务（config_service）完全独立，各自实现读取逻辑，互不依赖。写：最终都经 `config_service.update_config`（原子重命名+备份链），这是底层文件操作的唯一入口。时序和信息量完全不同。

env 契约：
- GET 对被环境变量接管的项返回有效值 + `sources[key]="env"`；页面整行只读并标注"来自环境变量"。
- PUT 含 env 接管项 → 该项拒绝写入，`warnings` 提示，不影响 patch 内其他项。

模型管理契约：
- 添加模型：校验 provider 存在 + model 名在 Provider 内唯一 → 写入 config.yaml 的 `models.{provider}.models.{model}` → 写入后需重启生效（registry 重建）
- 删除模型：若删除当前模型 → 自动切换回 Provider 的默认模型 → 返回 `switched_to`
- 删除 Provider：级联删除所有子模型 → 若当前 Provider 被删除 → 切换回默认 Provider → 返回 `switched_to`
- Provider 配置（api_key, base_url, timeout）：立即生效（下次 LLM 调用使用新配置），无需重启

secret 契约：
- GET 对 secret 项永不返回明文，只返回 `{configured:true, suffix:"末4位"}`。
- PUT secret 项：带 `value` 非空 = 更新；缺省/空 = 保持原值；`{clear:true}` = 显式清空。

更新返回值 `updated` 结构：
- 返回每项的来源 `{key, source:"yaml"|"env"}`，方便前端标注 env 接管项。

### 5.3 后端读写便利示例

```python
# 后端任意处一行读取有效值 — 小沈 2026-09-19
from app.services.settings.settings_service import get_setting
max_steps = get_setting("app.max_steps", 10000)   # env 覆盖时自动返回生效值
```

---

## 六、前端设计（全局状态 + 搜索 + 全局保存）

> 全部改进落位：模型Tab四区域完整CRUD(选择器/参数区/配置区/操作区)+双下拉联动+脏态保持+范围截断+删除级联+自动切换默认；单useSettings管全组状态+模型管理状态；搜索框、全局保存条+保存本组并存、全局脏角标；初始化全量GET+getModels；备份链命名+清理策略+启动校验；theme标注深色二期、provider切换来源、未接入展示策略、后端门禁独立。

### 6.1 文件（新增 20，修改 4，旧页零改动）

| 层 | 文件 | 动作 | 说明 |
|----|------|------|------|
| API | `services/api/settings.api.ts` | 新增 | `getSchema()` + `getAll()` + `updateSettings(patch)` |
| API | `services/api/model.api.ts` | 新增 | `getModels()` + `addModel()` + `deleteModel()` + `updateModel()` + `getProviders()` + `updateProvider()` |
| API | `services/api/index.ts` | 修改 | 导出桶 +2 行 |
| 错误 | `services/error/handler.ts` | 修改 | +`SETTINGS_SCHEMA_FAILED` / `SETTINGS_SAVE_FAILED` / `MODEL_MANAGEMENT_FAILED` 枚举与映射 |
| 页面 | `pages/Settings2/index.tsx` | 新增 | 同旧页骨架 + 搜索框 + 模型 Tab 四个区域 |
| 主题 | `frontend/src/theme/settingsTokens.ts` | 新增 | `settingsSpacing`（pagePadding/rowHeight/labelWidth）+ `settingsRadius`，从 stepStyles.ts+chatTokens 派生（见 7.9） |
| 组件 | `features/settings2/components/` | 新增 | `SettingsPage.tsx` + `SettingsGroup.tsx` + `SectionTitle.tsx` + `SettingRow.tsx` + `SearchBox.tsx` + `SaveBar.tsx` + `DirtyBadge.tsx` + `ModelSelector.tsx` + `ModelParams.tsx` + `ProviderConfig.tsx` + `ModelActions.tsx` + `ModelModals.tsx` + `icons.tsx`（图标map；域内子目录与 chat/settings 域惯例一致） |
| 状态 | `features/settings2/hooks/useSettings.ts` | 新增 | 全局 state，管全部 6 组 + 模型管理（见 6.2） |
| 纯函数 | `features/settings2/utils/modelUtils.ts` | 新增 | isDirty/clampToRange/validate 纯函数（见 7.5） |
| 类型 | `features/settings2/types.ts` | 新增 | 分组/项/ModelState 类型 |
| 路由 | `App.tsx` | 修改 | +1 懒加载路由 `/settings2` |
| 布局 | `components/Layout/index.tsx` | 修改 | "系统设置"菜单项改名为"设置2版"并改指 `/settings2`（改 1 项） |
| 不动 | `pages/Settings/` + `features/settings/` | — | 原样保留 |

### 6.2 状态设计（全局单层 state）

- **全局 state**：`useSettings()`（唯一 state 层，管理全部 6 组数据 + 脏态 + sources + 模型管理状态）。首次加载 `getAll()` 一次全量拉取（schema + 全部组值）+ `getModels()` 模型列表 → 内存缓存；切 Tab 零延迟。
- **模型管理状态**：
  ```typescript
  interface ModelState {
    providers: Array<{name, label, api_base, models:[{name,label,default_params,range,capabilities}]}>;
    selectedProvider: string;
    selectedModel: string;
    params: Record<string, any>;          // 当前模型的参数值
    defaults: Record<string, any>;        // 当前模型的默认参数
    ranges: Record<string, {min, max}>;   // 当前模型的参数范围
    envOverride: Record<string, boolean>; // env 接管标记
    providerConfig: Record<string, {api_key, base_url, timeout}>;
    isDirty: boolean;                     // 是否有参数被修改
    editingProviderConfig: boolean;       // Provider配置是否在编辑模式
    addModelModalOpen: boolean;           // 添加模型弹窗
    addProviderModalOpen: boolean;        // 添加Provider弹窗
    deleteConfirmOpen: boolean;           // 删除确认弹窗
    deleteTarget: string | null;          // 待删除目标
  }
  ```
- **局部状态**：每 Tab 内 `SettingsGroup` 只管表单渲染 + 局部脏态（该组哪些项改了），汇总到全局 state。
- 仅外观 fontSize/density 做本地预应用：改控件即写 `localStorage["omni.prefs.v1"]` + 预览；保存成功后对齐；后端不可达时外观 Tab 仍可本地试玩（纯本地，不承诺落盘）。
- 其余分组无本地缓存：以后端为准；后端不可达 → 整 Tab 错误态（`Result` + 重试），不展示过期值。
- **模型 Tab 脏态计算**：`isDirty = params[key] !== defaults[key] && !envOverride[key]`；切换模型时未修改的参数恢复为新模型默认值，已修改的参数保留。

### 6.3 页面读写流程

**读（首次加载）**：`getAll()` 一次全量 + `getModels()` 模型列表 → schema + 全部组值 + sources + providers/models 进全局 state；记录 config.yaml mtime；所有 Tab 渲染完毕，切零延迟。

**读（切 Tab / 刷新）**：检查 config.yaml mtime
- mtime 没变（自己修改的，已知状态）→ 缓存命中，零延迟
- mtime 变了（后门修改了）→ 重新 `getAll()` 刷新全部数据

**写（参数配置）**：改控件 → 全局 state 记录脏态 → [保存本组] 或底部 [保存全部] → schema 校验 → `PUT /settings {patch}` → 成功（含重启 Modal）/ 失败 `handleApiError` + 回滚。

**写（模型管理 CRUD）**：
- 添加模型：弹窗填表单 → `POST /models` → 后端写入 config.yaml → 刷新模型列表 → 选中新模型
- 删除模型：弹窗确认 → `DELETE /models/{provider}/{model}` → 若当前模型被删除，自动切换回默认模型 → 刷新列表
- 添加Provider：弹窗填表单 → `POST /providers` → 刷新Provider列表 → 选中新Provider
- 删除Provider：弹窗确认（级联警告）→ `DELETE /providers/{name}` → 级联删除所有子模型 → 刷新列表
- 修改Provider配置：编辑表单 → `PUT /providers/{name}` → 立即生效

### 6.4 搜索与全局脏态

- **搜索框**：页面顶部右上角，Input.Search；按 key（如 `app.language`）和显示名（如"日志级别"）过滤，全部 6 分组参与搜索；匹配项高亮并自动切到对应 Tab 定位。
- **全局脏角标**：顶部 Tab 栏旁显示"有 N 项未保存"（从全局 state 聚合）；切 Tab 时若目标 Tab 有未保存项，弹 Modal 提示。
- **底部保存栏**：`[保存本组]`（当前 Tab 脏时可用）+ `[保存全部]`（全局任意脏时可用，N 项一起提交）。
- **模型 Tab 特殊脏态**：参数区有 `[已修改]` 标记时，`[保存本组]` 可用；Provider 配置区有编辑内容时，`[保存本组]` 也可保存 Provider 配置变更。

### 6.5 模型 Tab 四个区域

模型 Tab 分为四个视觉区域：

| 区域 | 内容 | 操作 |
|------|------|------|
| ① 选择器 | Provider 下拉 + Model 下拉（双联动）+ 能力标签 | 切换 Provider/Model |
| ② 参数区 | 当前模型的所有可配参数（跟随模型）+ 范围 + 默认值 + [已修改] 标记 | 修改参数值、重置为默认 |
| ③ 配置区 | 当前 Provider 的配置（api_key、base_url、timeout、retry_times） | 编辑 Provider 配置 |
| ④ 操作区 | [+]添加模型、[+]添加Provider、[删除此模型]、[删除此Provider] | CRUD 操作 |

### 6.6 模型管理弹窗

- **添加模型弹窗**：Provider 下拉 + 模型名输入 + 默认参数（上下文窗口、温度范围、思考力度）+ 能力标签 → `POST /models`
- **添加Provider弹窗**：名称 + 显示名 + API地址 → `POST /providers`
- **删除确认弹窗**：目标名 + 警告（"删除模型将切换为默认模型" / "删除Provider将级联删除所有子模型"）→ `DELETE /models` 或 `DELETE /providers`

### 6.7 备份链命名统一（前端展示）

- 备份文件统一命名 `config.yaml.backup.1`（最新）~ `config.yaml.backup.5`（最旧），前端若展示备份列表按此编号。
- 旧 `/config/*` 路由保留兼容；新页只调 `/settings/*`，旧页照旧调 `/config/*`（同库同源）。

---

## 七、前端设置页 UI 设计方案

> 总原则：UI 是 API 的可视化；页面骨架与旧设置页一致（白底 + Card + Tabs），用户无感知切换；导航单入口。

### 7.0 与现有框架的整体关系（总图，审查入口）

#### 7.0.1 现有应用框架全貌（只看，不动）

```
App.tsx: BrowserRouter → AppProvider → AntdAppBridge → ErrorBoundary
         → RouterContent → AppLayout (activeKey = location.pathname)

┌──────────┬───────────────────────────────────────────────────┐
│ Sider    │ Header 高43px · 白底                               │
│ 宽180px  │ [动画图标] OmniAgentAst. [模型下拉][检查服务] 用户   │
│ light    ├───────────────────────────────────────────────────┤
│ 可折叠   │ Content · padding 6px · 底色 #f8fafc                │
│ (默认收起)│                                                   │
│          │  skeletonState.visible ? <LayoutSkeleton/>         │
│ Logo 43px│                        : <当前路由的页面/>          │
│ ──────── │                                                   │
│ 对话任务 /│                                                   │
│ 文件管理 │  (disabled 预留)                                   │
│ 知识库   │  (disabled 预留)                                   │
│ ──────── │                                                   │
│ 历史会话 │                                                   │
│ 快捷指令 │  (弹窗，非路由)                                     │
│ ──────── │                                                   │
│ 设置2版   │  → /settings2（旧"系统设置"项改名+改指，改 1 项）│
│ ──────── │                                                   │
│ 版本信息 │                                                   │
└──────────┴───────────────────────────────────────────────────┘
```

#### 7.0.2 路由表（App.tsx，本次只加 1 行）

| path | 页面 | 文件 | 状态 |
|------|------|------|------|
| `/` | 对话页 | `pages/ChatPage.tsx` | 现有，不动 |
| `/history` | 历史页 | `pages/History/index.tsx`（懒加载） | 现有，不动 |
| `/settings` | 旧设置页（3 Tab） | `pages/Settings/index.tsx` | 现有：文件不动，路由保留可访，不在导航出现 |
| `/settings2` | 设置2版（6 Tab + 模型管理） | `pages/Settings2/index.tsx`（懒加载） | 本次新增；菜单名"设置2版"，导航唯一入口；模型 Tab 含 4 区域（选择器/参数区/配置区/操作区）+ model_routes |
| `*` | 兜底进对话页 | — | 现有，不动 |

#### 7.0.3 新旧设置页位置（同一 Content，不同路由）

```
                        ┌─ /settings ──► pages/Settings/index.tsx（不动，深链可访）
                        │                  └ 3 Tab：旧组件原样
AppLayout               │
 Content ───────────────┤
 (同一块内容区)          └─ /settings2 ► pages/Settings2/index.tsx（新增，同骨架）
                                            └ 6 Tab：SettingsGroup 动态渲染；
                                              模型 Tab 四区域（选择器/参数区/配置区/操作区），配置区复用 <ProviderSettings/>
```

#### 7.0.4 共用与隔离对照

| 项目 | 旧页 | 新页 | 关系 |
|------|------|------|------|
| 外壳 AppLayout/Sider/Header/Content | 用 | 用 | 共用，一行不碰 |
| 导航菜单 | — | "设置2版"（旧"系统设置"项改名+改指 → /settings2） | 单入口 |
| 页面/组件 | `pages/Settings/` + `features/settings/` | `pages/Settings2/` + `features/settings2/`（新增；6 Tab + 模型管理四区域） | 新码不碰旧码；只复用、不复制 |
| API | `config.api.ts` | `settings.api.ts` + `model.api.ts`（新增） | 同走 `client.ts` axios |
| 错误/toast | `handleApiError` / `getMessage()` | 同一套（+3 枚举） | 共用 |
| 前端缓存 | 无 | 仅外观两项本地预应用 | 最小化 |
| 后端数据 | `config.yaml`（经 `/config/*`） | `config.yaml`（经 config_service + model_service） | 同源，写链路同一条原子重命名+备份链 |

#### 7.0.5 数据流

```
打开 /settings2
  → useSettings：GET /settings 一次全量（schema + 全部组值 + sources + config.yaml mtime）
                + GET /models 模型列表（providers/models/current_model_ref）
  → 渲染有效值 + 来源标注（env 项只读）
  → 记录 config.yaml mtime
  → 模型 Tab：四个区域渲染
    ① 选择器：双下拉联动（Provider → Model）
    ② 参数区：跟随当前模型，显示 params/defaults/ranges
    ③ 配置区：显示 providerConfig（api_key/base_url/timeout）
    ④ 操作区：CRUD 按钮
改控件 → 全局脏态（仅内存；外观 fontSize/density 同步 localStorage 预览）
模型参数脏态：params[key] !== defaults[key] && !envOverride[key]
[保存本组] → PUT /settings {patch}（参数配置）或 PUT /providers {config}（Provider配置）
  → 成功：success toast；含重启项 → Modal 列出；含 env 拒绝 → warnings 提示
  → 模型 CRUD：POST/PUT/DELETE /models 或 /providers → 写入 config.yaml → 刷新列表
  → 失败：handleApiError + 回滚
切 Tab / 刷新时：检查 config.yaml mtime → 变了则重新 GET /settings 全部刷新

### 7.1 UI 与 API 对应

| UI 层 | API 层 | 说明 |
|-------|--------|------|
| 顶部 6 个 Tab | `GET /settings` 全量 → `groups` | 分组只遍历不写死；首次全量加载，切 Tab 零延迟 |
| 搜索框（右上角） | 客户端过滤（基于已缓存 schema/items） | 按 key/显示名跨分组搜索，匹配项高亮并定位到对应 Tab |
| Tab 内控件 | `items[{key,type,...}]` | 类型/选项/单位来自 schema |
| 控件值 + 来源标注 | `GET /settings` → `groups[key].data` + `sources` | env 项只读 |
| [保存本组]/[保存全部] | `PUT /settings {patch}` | 只提交改动项 |
| 模型选择器 | `GET /models`（首次加载） | providers/models 层级 + current_model_ref |
| 模型参数区 | `GET /models` + `PUT /settings` 或 `PUT /providers` | 参数跟随当前模型；保存时调对应接口 |
| CRUD 按钮 | `POST/PUT/DELETE /models` 或 `/providers` | 添加/删除/修改模型和Provider |

### 7.2 页面整体布局（与旧页同骨架 + 搜索框 + 全局保存条）

```
<div className="settings-page" style={{ padding: 16, background: '#fff' }}>
  <Card>
    <div style={{ display:'flex', justifyContent:'space-between', alignItems:'center', marginBottom: 12 }}>
      <Tabs type="line" items={[...6 个 Tab]} />   {/* 左侧：Tab 栏 */}
      <Input.Search placeholder="搜索设置项 / key..." style={{ width: 260 }} />  {/* 右上角搜索 */}
    </div>
    {/* 有 N 项未保存的全局脏角标显示在 Tab 栏旁 */}
    <div style={{ position:'sticky', bottom:0, marginTop:12, padding:'12px 0 0', background:'#fff', borderTop:'1px solid #f0f0f0' }}>
      <Button>保存本组</Button>   {/* 当前 Tab 脏时可用 */}
      <Button type="primary">保存全部</Button>  {/* 全局任意脏时可用，N 项一起提交 */}
    </div>
  </Card>
</div>
```

- 页 padding 由旧页 25 收敛到 `16`（Content 已有 6px 外留白，双重留白是"空太多"主因）。
- 每个 Tab = `<SettingsGroup>`：小节标题 + antd `Form`（一行一项，行高 44）。
- **模型 Tab = 四个区域**：①选择器（双下拉联动）+ ②参数区（跟随模型）+ ③配置区（Provider配置）+ ④操作区（CRUD按钮）。
- 视觉 100% 沿用旧页（白底 + Card + Tabs + antd 默认样式），无新视觉体系。
- 搜索框基于已缓存的 schema/items 客户端过滤，按 key 和显示名匹配，匹配项高亮并自动切到对应 Tab 定位。
- 全局脏角标：Tab 栏旁显示"有 N 项未保存"；切 Tab 若目标有未保存项，弹 Modal 提示。
- 底部保存栏：`[保存本组]`（当前 Tab 脏可用）+ `[保存全部]`（全局任意脏可用，N 项一起提交）。

### 7.3 六分组页面详细图（每组一张）

> 每行末尾 `[存储·生效]` 即第四章归属；`[来源环境变量]` 行只读。搜索框基于缓存 schema 客户端过滤。

#### 图 7.3.1 Tab"通用"（2 项，YAML）

```
┌ Tab"通用" · <SettingsGroup group="general"> ──────────┐
│ 读: GET /settings?group=general                           │
│ 写: [保存本组] → PUT /settings {patch}                     │
│                                                           │
│ 通用设置                                                   │
│   系统语言     [ 简体中文 ▾ ]             [YAML·重启生效]   │
│   项目根目录   [ E:\test_dir    ] [浏览]   [YAML·即时生效]  │
│                                             [保存本组]    │
└───────────────────────────────────────────────────────────┘
```

#### 图 7.3.2 Tab"模型"（四区域 + 完整 CRUD）

```
┌ Tab"模型" · 模型管理 ──────────────────────────────────────────┐
│ ① 选择器                                                       │
│ [Provider ▾] [Model ▾]  [+]添加模型  [+]添加Provider          │
│   当前模型：deepseek-v4-flash (sensenova)                     │
│   [vision] [function_calling] [streaming]                     │
│                                                                │
│ ② 参数区（跟随当前模型）                                       │
│ 上下文窗口 [128000]  范围64k-200k  默认64000  [已修改]       │
│ 思考力度   [medium]  选项low/med/high  默认low                 │
│ 推理参数   [...]                                               │
│ [重置为默认]                                                  │
│                                                                │
│ ③ Provider配置                                                 │
│ api_key    [••••••••] [显示]                                  │
│ base_url   [https://...]                                       │
│ timeout    [30]                                                │
│                                                                │
│ ④ 操作区                                                       │
│ [删除此模型]  [删除此Provider]                                 │
│ [保存本组]  [保存全部]                                        │
└──────────────────────────────────────────────────────────────┘

┌ 添加模型弹窗 ──────────────────────────────┐
│ Provider [sensenova ▾]                      │
│ 模型名 [____________________]               │
│ 上下文窗口 [____]  范围 [____]-[____]       │
│ 温度范围 [0 ____] - [2 ____]               │
│ 思考力度 [low/med/high ▾]                   │
│ 能力 [vision] [function_calling] [streaming]│
│              [保存]    [取消]                │
└────────────────────────────────────────────┘

┌ 删除确认弹窗 ──────────────────────────────┐
│ 确定要删除 "deepseek-v4-flash"？            │
│ 警告：如果这是当前使用的模型，将自动切换   │
│    为默认模型                               │
│              [删除]    [取消]                │
└────────────────────────────────────────────┘

┌ 删除Provider确认 ──────────────────────────┐
│ 确定要删除 Provider "sensenova"？           │
│ 警告：将级联删除其下所有模型！              │
│              [删除]    [取消]                │
└────────────────────────────────────────────┘
```

要点：
- 模型 Tab 分为四个视觉区域（①选择器 ②参数区 ③配置区 ④操作区），内部用子标题分隔。
- 双下拉联动：切换 Provider → Model 下拉重置为该 Provider 的默认模型；切换 Model → 参数区刷新为该模型的默认参数。
- 脏态保持：切换模型时，用户手动改过的参数保留值并标记 `[已修改]`，未修改的参数恢复为新模型默认值。
- 参数超出范围：切换模型时自动截断到新模型范围，toast 提示。
- 添加/删除模型/Provider：弹窗确认 → 调 `POST/DELETE /models` 或 `/providers` → 写入 config.yaml → 刷新列表。
- 删除当前模型：自动切换回默认模型，参数区刷新。
- 删除 Provider：级联删除所有子模型，自动切换回默认 Provider。
- Provider 配置（api_key/base_url/timeout）：`PUT /providers` 立即生效，无需重启。
- `ProviderSettings` 旧组件嵌入复用（Provider 配置区），不改旧码。

#### 图 7.3.3 Tab"安全"（10 项，YAML，即时）

```
┌ Tab"安全" · <SettingsGroup group="security"> ─────────────┐
│ 读: GET /settings?group=security                          │
│ 写: [保存本组] → PUT /settings {patch}                     │
│                                                           │
│ 安全设置                                                   │
│   安全开关     [ 开 ]                     [YAML·即时生效]  │
│   严格模式     [ 关 ]                     [YAML·即时生效]  │
│   白名单开关   [ 关 ]                     [YAML·即时生效]  │
│   命令白名单   [多行文本域，每行一条命令]   [YAML·即时生效]  │
│   黑名单开关   [ 关 ]                     [YAML·即时生效]  │
│   命令黑名单   [多行文本域，每行一条命令]   [YAML·即时生效]  │
│   危险操作确认 [ 开 ]（保存时二次确认）     [YAML·即时生效]  │
│   内容过滤     [ 开 ]                     [YAML·即时生效]  │
│   过滤级别     [ medium ▾ ]               [YAML·即时生效]  │
│   最大文件MB   [ 100 ▾ ]                  [YAML·即时生效]  │
│                                             [保存本组]    │
└───────────────────────────────────────────────────────────┘
```

#### 图 7.3.4 Tab"聊天"（6 项，DB，未接入）

```
┌ Tab"聊天" · <SettingsGroup group="chat"> ─────────────────┐
│ 读: GET /settings?group=chat                              │
│ 写: [保存本组] → PUT /settings {patch}                     │
│                                                           │
│ 聊天默认参数［已存储·待二期接入 LLM，当前不承诺生效］  │
│   温度 temperature   [━━●━━━ 0.7 ]        [DB·未接入]      │
│   核采样 top_p       [━━━━●━ 1.0 ]        [DB·未接入]      │
│   最大输出 tokens    [ 留空=跟随模型 ]      [DB·未接入]      │
│   自动标题           [ 开 ]                [DB·未接入]      │
│   历史加载条数       [ 50 ]                [DB·未接入]      │
│   流式输出           [ 开 ]                [DB·未接入]      │
│                                             [保存本组]    │
└───────────────────────────────────────────────────────────┘
```

#### 图 7.3.5 Tab"外观"（theme 只读，字号/密度 DB 即时）

```
┌ Tab"外观" · <SettingsGroup group="appearance"> ───────────┐
│ 读: GET /settings?group=appearance                        │
│ 写: [保存本组] → PUT /settings {patch}                     │
│                                                           │
│ 预览小卡（改下面控件，这里实时变）                          │
│   ┌ 示例消息气泡：字号/密度实时预览 ┐                      │
│                                                           │
│   主题   浅色（深色二期：需全站 token 化重做硬编码色值，暂只读） [YAML·只读]     │
│   字号px [━━●━━━━ 14 ]                    [YAML·即时生效]    │
│   消息密度 [ 舒适 ▾ ]                     [YAML·即时生效]    │
│                                             [保存本组]    │
└───────────────────────────────────────────────────────────┘
```

#### 图 7.3.6 Tab"系统"（10 项：5 系统参数 + 3 运维日志 + 2 关于）

```
┌ Tab"系统" · <SettingsGroup group="system"> ───────────────┐
│ 读: GET /settings?group=system                            │
│ 写: [保存本组] → PUT /settings {patch}（只读项不提交）      │
│                                                           │
│   ── 系统参数 ──                                            │
│   调试模式       [ 开 ]                   [YAML·重启生效]  │
│   上下文上限     [ 200000 ]               [YAML·重启生效]  │
│   历史保留条数   [ 10 ]                   [YAML·即时生效]  │
│   最大轮数       [ 100 ]                  [YAML·即时生效]  │
│   最大步数       [ 10000 ]                [YAML·即时生效]  │
│   ── 运维日志 ──                                            │
│   日志级别       [ INFO ▾ ]               [YAML·重启生效]  │
│   日志文件上限   [ 10485760 ]             [YAML·重启生效]  │
│   日志备份数     [ 5 ]                    [YAML·重启生效]  │
│   ── 关于 ──                                                │
│   配置文件路径   [ F:\...\config.yaml ] [复制] [只读]      │
│   当前版本       [ v1.0.1 ]              [复制] [只读]      │
│                                             [保存本组]    │
└───────────────────────────────────────────────────────────┘
```

### 7.4 控件类型 ↔ schema.type ↔ antd 组件

| schema.type | antd 组件 | PUT 值类型 | 备注 |
|------------|-----------|-----------|------|
| `text` | `Input` | `string` | — |
| `secret` | `Input.Password` | 见 7.7 契约 | GET 永不返明文 |
| `textarea` | `Input.TextArea`（等宽） | `string` | — |
| `tags` | `Select mode="tags"` | `string[]` | — |
| `int` / `float` | `InputNumber` | `number` | — |
| `range` | `Slider` + `InputNumber` | `number` | — |
| `bool` | `Switch` | `boolean` | — |
| `select` | `Select` | `string` | — |
| `model_ref` | 双联动 `Select` | `{provider, model}` | 仅当前模型 |
| `readonly` | 文本 + 复制按钮 | 不提交 | — |

### 7.5 保存 / 还原 / 脏状态 / 重启生效

```
【参数配置流】
Tab 内改动 → 脏态 → [保存本组]可用
  → 点[保存本组] → schema 校验
    → 通过 → PUT /settings {仅改动项}
      → 成功：success toast；含重启项 → Modal 列出；含 env 拒绝 → warnings 提示
      → 失败：handleApiError + 回滚
    → 不通过：首个错误控件红框 + 定位
  → [还原默认] → 确认 Modal → 按当前模型 schema.default 重置 → 脏态清空
切 Tab 有未保存 → Modal[保存并切换/放弃修改/取消]

【模型管理流】
模型参数改动 → 脏态(params≠defaults) → [保存本组]可用
  → 范围外值自动截断 + toast
  → PUT /settings {model_ref + 模型级参数} 或 PUT /providers {config}
    → 成功：defaults 更新 → 脏态清空
    → 失败：handleApiError + 回滚 params 到保存前值
添加模型 → 弹窗 → POST /models → 刷新列表
删除模型 → 确认弹窗 → DELETE /models → 若当前模型被删 → 自动切默认模型
删除Provider → 级联确认 → DELETE /providers → 级联删除所有子模型
```

### 7.6 外观本地预应用（仅 fontSize/density）

1. 改控件 → 即写 `localStorage["omni.prefs.v1"]` + 预览实时变；[保存本组] 才落 YAML。
2. 保存前行标注"仅本机生效"，成功后消失。
3. 后端不可达时外观 Tab 仍可本地试玩（纯本地，不承诺落盘）；其余 Tab 整页错误态。

### 7.7 密钥与危险项

- **api_key**：GET 只返 `{configured:true, suffix:"末4位"}`；常态显示"已配置 ····末4位"+[修改]；PUT 带 `value`=更新、缺省/空=保持、`clear:true`=清空。
- **危险操作/黑白名单**：保存二次确认 Modal（UX 层）。注意：后端安全门禁独立于 UI 开关——无论 `confirmDangerousOps` 开关状态如何，后端对危险命令的执行判断都独立生效；UI 开关只影响"是否在提交前弹窗确认"，不影响后端拦截。
- **只读项**：禁用态 + 复制按钮。

### 7.8 加载 / 空 / 错误 / mtime 刷新

| 状态 | UI 表现 |
|------|---------|
| 首次加载 | Tab 内容区 `Skeleton` |
| schema 失败 | 整 Tab `Result` + [重试]；toast 走 `handleApiError(SETTINGS_SCHEMA_FAILED)` |
| 组内无项 | 占位"暂无设置项" |
| 保存失败/后端不可达 | `handleApiError` 统一提示；非外观 Tab 整页错误态（不展过期值），外观 Tab 本地模式 |
| mtime 变化（后门修改） | 自动重新 `GET /settings` 刷新全部数据，toast 提示"配置已更新，已刷新" |

### 7.9 设置页视觉体系（继承 stepStyles 五类结构，只增页级派生）

> 目标：大方得体、不碎、关联聚合。**不复用大表**——直接继承 `utils/stepStyles.ts` 的 `FontSize/FontWeight/Spacing/Radius/Colors` 五对象，只在 `theme/settingsTokens.ts` 中增页级派生常量（pagePadding/rowHeight/labelWidth）。禁止新创大表。**图标全部 SVG（antd icons），禁止 emoji。**

#### 7.9.1 字体（继承 stepStyles.FontSize，不新增字号档）

源文件 `frontend/src/utils/stepStyles.ts`（v1.3 已统一 14/12 二档）：
```ts
export const FontSize = { PRIMARY:14, SECONDARY:12, TERTIARY:12, SMALL:11, CAPTION:10, CODE:12, EMOJI:14 } as const;
export const FontWeight = { BOLD:600, MEDIUM:500, REGULAR:400, LIGHT:300 } as const;
```

设置页直接 import 复用，项行 label 用 `FontSize.PRIMARY(14)/FontWeight.REGULAR(400)`，帮助文用 `FontSize.SECONDARY(12)/FontWeight.REGULAR(400)`，小节头用 `FontSize.PRIMARY(14)/FontWeight.BOLD(600)`。

- 行高 = 字号 + Spacing.XS(4) = 项行 44；小节头行 32。
- 不自定义 font-family；10/11/12/14 四档已够用，不新增。

#### 7.9.2 色彩（零新色相，继承 stepStyles.Colors + chatTokens）

源文件 `frontend/src/utils/stepStyles.ts`：
```ts
export const Colors = { TEXT:{PRIMARY:'#595959',SECONDARY:'#8c8c8c',TERTIARY:'#999',WEAK:'#888',STRONG:'#333'}, BG:{PRIMARY:'#fff',SECONDARY:'#fafafa',TERTIARY:'#f5f5f5'}, BORDER:{LIGHT:'#f0f0f0',VERTICAL:'#e8e8e8'}, PRIMARY:'#1677ff', SUCCESS:'#52c41a', ERROR:'#ff4d4f', WARNING:'#AD6800', ORANGE_RED:'#fa541c', WAIT_ACTION:'#fa8c16', ... } as const;
```

`frontend/src/theme/tokens.ts` 的 `chatTokens` 是会话页薄封装（12 个值），设置页直接复用 `chatTokens` 的 `colorPrimary/colorPrimaryBg/colorText/colorTextSecondary/colorBorder/colorSuccess/colorError/colorWarning`，不重复定义。

设置页特有派生：
- 页白底 `#ffffff`（Content 底 `#f8fafc` 之上）
- 只读行底 `#fafafa`（= `Colors.BG.SECONDARY`）
- 已修改角标：`Colors.PRIMARY` 6px 实心圆点 + 12px 次文 `[已修改]`

- 深色不出 v1；硬编码色值全部收敛进现有 token 文件，禁止行内散写。

#### 7.9.3 间距留白（继承 stepStyles.Spacing + 页级派生）

源文件 `frontend/src/utils/stepStyles.ts`：
```ts
export const Spacing = { XS:4, SM:6, MD:8, LG:12, XL:16 } as const;
export const Radius = { SM:4, DEFAULT:6, LG:8 } as const;
```

设置页 `settingsTokens.ts` 只增三个页级派生常量：
```ts
export const settingsSpacing = {
  ...Spacing,                    // 复用 4/6/8/12/16
  pagePadding: 16,               // 旧页 25→16（Content 外层已有 6）
  rowHeight: 44,                 // 项行高度
  labelWidth: 132,               // label 列宽
};
export const settingsRadius = { ...Radius };  // 直接复用
```

| 位置 | 取值 | 来源 |
|------|------|------|
| 页 padding | `16` | settingsSpacing.pagePadding |
| Card 内边距 | `16` | Spacing.XL |
| 行间距 | `12` | Spacing.LG |
| 行高 | `44` | settingsSpacing.rowHeight |
| label 列宽 | `132` | settingsSpacing.labelWidth |
| 底部保存条 | sticky，`padding 12 0 0`，上边框 `#f0f0f0` | Spacing.LG + Colors.BORDER.LIGHT |

- 关联聚合：模型四区域同页连续滚动；系统三小节同 Tab；Provider 配置与模型选择同 Tab 不跳页。

#### 7.9.4 图标（全部 SVG antd icons，铁律禁 emoji）

| 位置 | 图标 | 规格 |
|------|------|------|
| 通用 Tab | `GlobalOutlined` | 14px，`Colors.TEXT.SECONDARY` |
| 模型 Tab | `ApiOutlined` | 14px，`Colors.TEXT.SECONDARY` |
| 安全 Tab | `SafetyOutlined` | 14px，`Colors.TEXT.SECONDARY` |
| 聊天 Tab | `MessageOutlined` | 14px，`Colors.TEXT.SECONDARY` |
| 外观 Tab | `BgColorsOutlined` | 14px，`Colors.TEXT.SECONDARY` |
| 系统 Tab | `DesktopOutlined` | 14px，`Colors.TEXT.SECONDARY` |
| 已修改 | 6px 实心圆点 `Colors.PRIMARY` + 12px 文 | 不用 emoji |
| 只读/复制 | `CopyOutlined` | 14px |
| 危险删除 | `DeleteOutlined` + 红色按钮 | 二次确认 Modal |
| 能力标签 | 纯 `Tag` 文字（vision/function_calling/streaming） | 无图标 |

- 图标映射收敛进 `features/settings2/components/icons.tsx` 的 `SettingIcon` 单一 map，禁止各文件各自 import 散写。
- **铁律：设置页所有图标必须使用 antd SVG 图标组件，禁止 emoji 字符**。`stepStyles.ts` 的 `stepMeta` 中所有 emoji 标签（思考/开始/暂停/恢复/重试/观察/执行/内容/统计/截断/完成/错误 等）必须替换为 antd SVG 图标组件（如 `BulbOutlined`、`RocketOutlined`、`PauseCircleOutlined`、`PlayCircleOutlined`、`RedoOutlined`、`EyeOutlined`、`SettingOutlined`、`FileTextOutlined`、`BarChartOutlined`、`ScissorOutlined`、`CheckCircleOutlined`、`CloseCircleOutlined`）。此替换属于独立重构任务，不在本次设置页范围，但设计文档必须明确禁令。

#### 7.9.5 代码模块化清单（每个文件只做一件事）

- **Token 层**（不新建大表，扩展现有文件）：
  - `frontend/src/theme/settingsTokens.ts`：只 export `settingsSpacing`（pagePadding/rowHeight/labelWidth）+ `settingsRadius`，import `Spacing/Radius` 自 `stepStyles.ts`、`chatTokens` 自 `tokens.ts`
  - **不动** `frontend/src/utils/stepStyles.ts`（已有五类完整 token）
- **图标层**：`features/settings2/components/icons.tsx`（`SettingIcon` map + `DirtyDot` + `EnvTag`，唯一出口）
- **纯函数层**：`features/settings2/utils/modelUtils.ts`（`isDirty(params,defaults,env)` / `clampToRange(value,range)` / `validate`，无 JSX，可单测）
- **页面层**（域内子目录 `features/settings2/components/`，每个文件只做一件事，页面元素代码化）：
  - `SettingsPage.tsx`（壳：搜索 + Tabs + sticky 保存条）/ `SettingsGroup.tsx`（组渲染 + 外观预览小卡内联）/ `SectionTitle.tsx`（小节头）/ `SettingRow.tsx`（label+控件+帮助一行）/ `SearchBox.tsx` / `SaveBar.tsx`（sticky + 危险项二次确认 + 重启 Modal）/ `DirtyBadge.tsx`
  - 模型：`ModelSelector.tsx`（双下拉）/ `ModelParams.tsx`（参数区 + 还原默认确认）/ `ProviderConfig.tsx`（配置区，内嵌复用旧 `ProviderSettings`）/ `ModelActions.tsx`（CRUD 按钮）/ `ModelModals.tsx`（添加/删除确认弹窗）
- **状态层**：`features/settings2/hooks/useSettings.ts` 只管数据 + 脏态 + mtime 感知，不拼 JSX
  - **不动** `frontend/src/features/settings/components/ProviderSettings.tsx`（旧页零改动）
- **类型层**：`features/settings2/types.ts`

---

## 八、落地文件清单（F 盘真实路径）

| 方向 | 动作 | 文件 |
|------|------|------|
| 后端 | 新建 | `backend/app/services/settings/settings_registry.py` |
| 后端 | 新建 | `backend/app/services/settings/settings_service.py`（设置页 6 组服务，完全独立读+写） |
| 后端 | 新建 | `backend/app/services/model/model_service.py`（模型编排器，读+写独立；位于 model 域与 config_service 同域） |
| 后端 | 新建 | `backend/app/api/v1/settings_schemas.py`（settings 特有 DTO；模型 DTO 复用 config_schemas.py） |
| 后端 | 新建 | `backend/app/api/v1/settings_routes.py` |
| 后端 | 新建 | `backend/app/api/v1/model_routes.py`（模型/Provider CRUD 路由） |
| 后端 | 修改 | `backend/app/main.py`（+2 行 include_router：settings_router + model_router） |
| 后端 | 修改 | `backend/app/utils/file_utils.py`（新增 `atomic_write` 临时文件+`os.replace` 原子写入供 write_yaml_config 使用 + `backup_file` 升级为编号备份链 `config.yaml.backup.1`（最新）~`backup.5`（最旧）+ FIFO 清理；解决 8.1.2-#1 / 9.1-1、9.1-2） |
| 后端 | 修改 | `backend/app/services/model/config_helpers.py`（`write_yaml_config` 改调 `atomic_write`；见 9.3.3） |
| 前端 | 新建 | `frontend/src/services/api/settings.api.ts` |
| 前端 | 新建 | `frontend/src/services/api/model.api.ts`（getModels/addModel/deleteModel/updateModel/getProviders/updateProvider） |
| 前端 | 修改 | `frontend/src/services/api/index.ts`（+2 行导出） |
| 前端 | 修改 | `frontend/src/services/error/handler.ts`（+3 枚举与映射） |
| 前端 | 新建 | `frontend/src/pages/Settings2/index.tsx` |
| 前端 | 新建 | `frontend/src/theme/settingsTokens.ts`（只 export settingsSpacing/pagePadding/rowHeight/labelWidth + settingsRadius，从 stepStyles.ts+chatTokens 派生，见 7.9） |
| 前端 | 新建 | `frontend/src/features/settings2/types.ts` |
| 前端 | 新建 | `frontend/src/features/settings2/hooks/useSettings.ts`（全局 state，见 6.2） |
| 前端 | 新建 | `frontend/src/features/settings2/utils/modelUtils.ts`（isDirty/clampToRange/validate 纯函数） |
| 前端 | 新建 | `frontend/src/features/settings2/components/icons.tsx`（SettingIcon 唯一图标出口 + DirtyDot + EnvTag） |
| 前端 | 新建 | `frontend/src/features/settings2/components/SettingsPage.tsx` + `SettingsGroup.tsx` + `SectionTitle.tsx` + `SettingRow.tsx` + `SearchBox.tsx` + `SaveBar.tsx` + `DirtyBadge.tsx` |
| 前端 | 新建 | `frontend/src/features/settings2/components/ModelSelector.tsx` + `ModelParams.tsx` + `ProviderConfig.tsx` + `ModelActions.tsx` + `ModelModals.tsx` |
| 前端 | 修改 | `frontend/src/App.tsx`（+1 懒加载路由） |
| 前端 | 修改 | `frontend/src/components/Layout/index.tsx`（菜单项改名"设置2版"并改指新页，1 项） |
| 前端 | 不动 | `frontend/src/pages/Settings/` + `features/settings/`（零改动） |
| 文档 | 修改 | `backend/FUNCTIONS.md`（登记 settings_service + model_service 编排函数） |

### 8.1 设计要点与代码文件对应核算（每个代码→设计要点；设计要点→代码缺口）

> 正向：第八章每个代码文件覆盖前 7 章的哪些设计要点；反向：前 7 章哪些设计要点在第 8 章没有对应代码文件。— 小欧 2026-09-20

#### 8.1.1 代码文件 → 覆盖的设计要点（正向对应表）

| 代码文件（第八章清单） | 覆盖的设计要点（出处） |
|------|------|
| `services/settings/settings_registry.py` | 3.2 铁律1 key/默认/值域/存储唯一真相源 + key 全局唯一加载自检拒启；第四章全部设置项定义 |
| `services/settings/settings_service.py` | 3.1 前门读（解析 YAML→groups/sources/mtime + env 标注 + 脏态）与写（校验 patch→update_config）；一.2 显示即生效；一.3 单存储；5.2 env 契约 / secret 契约 / updated 来源结构；5.3 get_setting；4.6-③ config_path/version 只读项组装 |
| `services/model/model_service.py` | 3.2 铁律7 模型 CRUD 读+写；5.1 模型 DTO 复用 config_schemas；5.2 模型管理契约（registry 固定 + config.yaml 自定义合并；删当前模型/Provider 级联 + switched_to；写需重启生效；Provider 配置立即生效） |
| `api/v1/settings_schemas.py` | 5.1 settings 特有 DTO（schema 输出 / GET 含 mtime / PUT patch 与响应）；模型 DTO 复用 config_schemas.py，不重复定义 |
| `api/v1/settings_routes.py` | 5.2 GET/PUT `/settings` 接口 + 返回 config.yaml mtime + updated 结构 |
| `api/v1/model_routes.py` | 5.2 GET/POST/PUT/DELETE `/models` `/providers` 接口 |
| 修改 `main.py` | 5.1 路由挂载（include_router ×2） |
| 修改 `file_utils.py` | 一.7 / 3.2 铁律2 / 6.7 编号备份链 backup.1~backup.5 + FIFO 清理 + `atomic_write` 原子写入 + 写错回滚最近一个（8.1.2-#1；v4.6 实测 `_write_system_yaml` 现为 `open('w')` 直写、无原子重命名，原子写入一并补入本文件） |
| 修改 `services/model/config_helpers.py` | 9.3.3 `write_yaml_config` 改调 `atomic_write`（v4.9 补：8.2.2 已要求但第八章漏列） |
| （复用）`services/model/config_service.update_config` | 3.2 铁律2 原子重命名底层写唯一入口；一.4 前门后门写侧；9.1-1 唯一写入口 |
| `services/api/settings.api.ts` | 6.1 / 7.1 UI↔API（getSchema/getAll/updateSettings） |
| `services/api/model.api.ts` | 6.1 / 7.1 模型 6 函数（getModels/addModel/deleteModel/updateModel/getProviders/updateProvider） |
| `services/api/index.ts`+2 / `services/error/handler.ts`+3 | 6.1 导出桶 + 3 错误枚举映射 |
| `App.tsx`+1 | 7.0.2 `/settings2` 懒加载路由 |
| `components/Layout/index.tsx` | 一.1 单一入口；菜单"设置2版"改名+改指 |
| `pages/Settings2/index.tsx`（懒加载）+ `features/settings2/components/SettingsPage.tsx` | 7.2 布局（白底 Card+Tabs+搜索+sticky 保存条）；7.8 首次加载 Skeleton / group 空占位 / schema 失败 Result+重试；8.1.2-#6 切 Tab 未保存 Modal（保存并切换/放弃/取消）内联 |
| `features/settings2/hooks/useSettings.ts` | 6.2 全局单层 state（6 组+脏态+sources+模型管理）；3.1 mtime 感知（切 Tab/刷新检查）；7.6 外观 localStorage 预应用逻辑；9.10 模型切换脏态合并 |
| `features/settings2/types.ts` | 6.2 ModelState / 各 entities 类型 |
| `features/settings2/components/SettingsGroup.tsx` | 7.3 六分组动态渲染；6.2 局部脏态汇总全局；8.1.2-#4 外观预览小卡（字号/密度实时预览）内联 |
| `features/settings2/components/SectionTitle.tsx` | 4.6 系统三小节视觉分隔；7.9.1 小节头 14/600 |
| `features/settings2/components/SettingRow.tsx` | 7.4 控件↔schema.type↔antd 组件；7.7 secret（configured+suffix+三态）/只读/复制；7.9.1/7.9.2 字号色彩落地 |
| `features/settings2/components/SearchBox.tsx` | 6.4 / 7.1 搜索（key/显示名过滤+高亮+自动切 Tab 定位） |
| `features/settings2/components/SaveBar.tsx` | 6.4 / 7.2 底部保存栏（[保存本组]+[保存全部]）；8.1.2-#5 危险项保存二次确认 Modal 内联；8.1.2-#8 重启生效 Modal（列出重启项）内联 |
| `features/settings2/components/DirtyBadge.tsx` | 6.4 全局脏角标"N 项未保存" |
| `features/settings2/components/ModelSelector.tsx` | 6.5-① / 7.3.2 双下拉联动 + 能力 Tag |
| `features/settings2/components/ModelParams.tsx` | 6.5-② / 7.3.2 参数区跟随模型+范围+[已修改]+[重置为默认]；8.1.2-#7 [还原默认] 确认 Modal 内联 |
| `features/settings2/components/ProviderConfig.tsx` | 6.5-③ / 7.3.2 配置区（内嵌复用旧 ProviderSettings，不改旧码） |
| `features/settings2/components/ModelActions.tsx` | 6.5-④ CRUD 按钮 |
| `features/settings2/components/ModelModals.tsx` | 6.6 添加模型/添加 Provider 弹窗 + 删除确认弹窗（级联警告） |
| `features/settings2/utils/modelUtils.ts` | 6.2 isDirty（params≠defaults && !envOverride）+ 9.10 clampToRange 范围截断 + 8.1.2-#3 validate()（schema 校验，首个错误控件红框+定位） |
| `features/settings2/components/icons.tsx` | 7.9.4 SettingIcon 唯一 SVG 出口 + DirtyDot + EnvTag |
| `theme/settingsTokens.ts` | 7.9.1~7.9.3 settingsSpacing（pagePadding/rowHeight/labelWidth）+ settingsRadius 派生 |
| 不动 `features/settings/components/ProviderSettings.tsx` | 7.0.4 只复用不复制；7.3.2 配置区嵌入复用 |

#### 8.1.2 设计要点缺口核算（前 7 章要点 → 无对应代码，8 个；v4.5 全部给出处理方案）

| # | 设计要点 | 出处 | 处理方案（v4.5） |
|---|---------|------|---------|
| 1 | 最近 5 个编号备份链 backup.1~backup.5 + FIFO 清理，前端按编号展示 | 一.7 / 3.2 铁律2 / 6.7 | **已补方案**：第八章新增 `file_utils.py` 修改项，`backup_file` 升级编号备份链（backup.1 最新~backup.5 最旧）+ FIFO 清理 + `atomic_write`（临时文件+`os.replace`）供 write_yaml_config 使用（v4.6 实测现 `_write_system_yaml` 是 `open('w')` 直写、无原子重命名，一并补入）；写错回滚最近一个（config_service._restore_backup_if_needed 已有） |
| 2 | 启动校验失败自动用备份恢复 | 一.7 | **措辞已修正**：v3.0 已删"启动自检"（版本历史 v3.0 该条），v4.5 删一.7"启动校验恢复"字样，只保留"写错回滚最近一个备份"；无需代码 |
| 3 | 前端保存前 schema 校验（首个错误控件红框+定位） | 7.5 / 6.3 | **宿主已标注**：`modelUtils.ts` 增加 validate() 纯函数（8.1.1 已加职责） |
| 4 | 外观预览小卡（字号/密度实时预览） | 7.3.5 图 / 7.6 | **宿主已标注**：内联 `SettingsGroup.tsx`（8.1.1 已加职责） |
| 5 | 危险项保存二次确认 Modal | 7.5 / 7.7 | **宿主已标注**：内联 `SaveBar.tsx`（8.1.1 已加职责） |
| 6 | 切 Tab 未保存 Modal（保存并切换/放弃/取消） | 6.4 / 7.0.5 | **宿主已标注**：内联 `SettingsPage.tsx`（8.1.1 已加职责） |
| 7 | [还原默认] 确认 Modal（重置为 schema.default） | 7.5 | **宿主已标注**：内联 `ModelParams.tsx`（8.1.1 已加职责） |
| 8 | 重启生效 Modal（列出重启项） | 7.5 / 10.4-2 | **宿主已标注**：内联 `SaveBar.tsx`（8.1.1 已加职责） |

**核算结论（v4.5）**：前 7 章 8 个缺口要点 **全部有处理方案**，不再有"无对应代码"的遗漏。
- 补代码 1 个（#1 第八章新增 `file_utils.py` 修改项）；
- 宿主职责标注 6 个（#3~#8 内联进 modelUtils/SettingsGroup/SaveBar/SettingsPage/ModelParams，不新增文件）；
- 文档措辞修正 1 个（#2 删"启动校验恢复"）。

### 8.2 风险与对策（11 条，v4.6 逐条补处置代码）

> 每条按【风险】→【对策】→【代码处理（文件·函数·逻辑）】；涉及改动均已在 8.1.1 对应代码文件中落地。— 小欧 2026-09-20

#### 8.2.1 唯一写入口 / 并发写

- 【风险】新旧两页同写一个 `config.yaml`，若出现第二个写入口，并发可能损坏文件或静默丢项。
- 【对策】所有写最终收敛到 `config_service.update_config` 唯一底层函数；设置页服务与后端业务读取完全独立、互不干扰；后端按请求串行执行，后写串行胜出。
- 【代码处理】`config_service.update_config()`（已有，不动）：`_backup_config` 先备份 → `read_yaml_config` 读原 → FIELD_HANDLERS 依次改字段 → `_auto_fix_and_validate` 校验（失败返回 fail_result 不改文件）→ `write_yaml_config` 落盘（v4.6 改走 `file_utils.atomic_write` 临时文件+`os.replace`）→ 重读文件验证 → 成功清备份 / 异常 `_restore_backup_if_needed` 回滚。`settings_service.py`、`model_service.py`（第八章新建）只调此函数，杜绝第二写入口。验收断言 10.4-3 并发后写胜出、YAML 不损坏不丢项。
- 【涉及代码】`config_service.py`（已有复用）+ `file_utils.py`（v4.6 改，见 9.1-2）+ settings_service.py / model_service.py（新建）。

### 8.2.2 单存储 / 文本文件健壮性（备份链 + 原子写）

- 【风险】`config.yaml` 写错值或写一半时文件损坏，设置页无兜底可回。
- 【对策】文本文件防损坏靠两层：**原子写入**（临时文件 + `os.replace` 整文件替换，绝不半写）+ **最近 5 个编号备份链**（backup.1 最新 ~ backup.5 最旧 + FIFO 清理），写失败回滚最近一个备份。
- 【代码处理】现实现 `backup_file()` 是时间戳单备份（`config.yaml.backup_时间戳`）且 `update_config` 验证成功即删——无编号链、无原子写，设计承诺与现实不一致。v4.6 收敛：`file_utils.py` 新增 `atomic_write(file_path, writer)`（写临时文件 → `os.replace`）+ 改造 `backup_file` 为编号轮转（删 backup.5，backup.N→backup.N+1 顺移，新备份=backup.1）；`write_yaml_config` 改调 `atomic_write`；回滚沿用 `config_service._restore_backup_if_needed`（已有）。
- 【涉及代码】`file_utils.py`（修改，8.1.2-#1）+ `config_helpers.write_yaml_config`（改调 atomic_write）+ `config_service.py`（回滚已有复用）。

### 8.2.3 env 接管（显示值 ≠ 生效值）

- 【风险】部署环境设了 `AI_PROVIDER` 等变量时，YAML 值与实际生效值不一致，页面展示会骗人。
- 【对策】GET 对被 env 接管的项返回**有效值** + `sources[key]="env"`；前端整行只读并标注"来自环境变量"；PUT 含 env 项 → 该项拒绝写入 + `warnings` 提示，不影响 patch 内其他项。
- 【代码处理】`settings_service.py` 读时逐 key 比较 YAML 值 vs config 生效值（env 覆盖），不一致 → 值返回生效值、`sources` 置 env；写时校验 patch，命中 env 项 → 跳过 + 塞 `warnings`。`settings_routes.py` PUT 返回 `warnings` 透传前端。`SettingRow.tsx` env 项 `disabled` + icons.tsx 的 `EnvTag` 标注。验收 10.1。
- 【涉及代码】settings_service.py / settings_routes.py / SettingRow.tsx / icons.tsx（新建）。

### 8.2.4 secret 明文泄露

- 【风险】api_key 等密钥若 GET 返明文或默认值落库，联调/日志即泄露。
- 【对策】GET 永不返明文，只返 `{configured:bool, suffix:末4位}`；PUT 三态契约：带 `value` = 更新、缺省/空 = 保持原值、`{clear:true}` = 显式清空。
- 【代码处理】`settings_service.py` 序列化时对 `secret` 类型：有值 → `{configured:true, suffix:str[-4:]}`；无值 → `{configured:false}`；写时按三态分支。`SettingRow.tsx` secret 控件用 `Input.Password` + "已配置 ····末4位"+[修改]。验收 10.1 / 10.4 逐项断言 GET 无明文、三态各一次。
- 【涉及代码】settings_service.py / SettingRow.tsx（新建）。

### 8.2.5 主题二期（深色切换）

- 【风险】v1 若开放 `app.theme` 深色，全站大量 `#fff` 硬编码色值（如设置页 index.tsx:108、Layout:639）无 token 基础，样式崩坏。
- 【对策】v1 主题只读展示"浅色"，二期全站 token 化重做硬编码色值后再开放切换；本期不做承诺。
- 【代码处理】`settings_registry.py` 中 `app.theme` 标 `readonly=true`；`SettingRow.tsx` 只读渲染 + 复制按钮；二期仅改 registry 的 readonly 标注即可放开。无需新增代码。
- 【涉及代码】settings_registry.py / SettingRow.tsx（新建）。

### 8.2.6 基线唯一（多副本漂移）

- 【风险】`F:\OmniAgentAs-repair` 与 `G:\OmniAgentAs-desk` 旧副本并存，误以 G 盘为基线导致改动丢失、联调错版。
- 【对策】管理约束：实施、联调、测试一律以 `F:\OmniAgentAs-repair` 为唯一权威；G 盘不得作为开发基线。实施前先与本地最新 commit 对齐。
- 【代码处理】无代码改动；属流程纪律，验收时核对 F 盘代码与本文档一致即可。

### 8.2.7 旧页深链与导航

- 【风险】改旧页会破坏 `/settings` 深链；双导航入口会造成 UX 混乱。
- 【对策】`/settings` 路由保留可访、旧页文件零改动；导航单入口：Layout 旧"系统设置"项改名"设置2版"并改指 `/settings2`；回滚只需摘导航入口（旧码零改动天然可回）。
- 【代码处理】`Layout/index.tsx` 菜单项改名+改指（改 1 项）；`App.tsx` 新增 `/settings2` 懒加载路由（+1 行）；`pages/Settings/` + `features/settings/` 原样不动。验收 10.4-4。
- 【涉及代码】Layout/index.tsx / App.tsx（修改）。

### 8.2.8 模型管理级联（孤儿引用）

- 【风险】删除 Provider 时其子模型成为孤儿引用；删除当前模型后 current_model_ref 悬空，加载即崩。
- 【对策】删除 Provider → 级联删除所有子模型；删除当前模型/Provider → 自动切换回默认模型/默认 Provider，返回 `switched_to` 供前端提示；删除当前 Provider 弹窗级联警告。
- 【代码处理】`model_service.py` DELETE 流程：删 provider 前先删其 `models`，若命中当前 `provider/model` → 解析默认模型 → 写 `current_model_ref` → 返回 `switched_to`；写仍走 `config_service.update_config`。`model_routes.py` 透传。`ModelModals.tsx` 弹窗显示"将级联删除所有子模型"。验收 10.1 / 10.3。
- 【涉及代码】model_service.py / model_routes.py / ModelModals.tsx（新建）。

### 8.2.9 Provider 配置立即生效

- 【风险】api_key/base_url/timeout 改了不生效需重启，用户误以为已生效。
- 【对策】PUT /providers/{name} 写入 config.yaml，LLM 调用侧每次实时解析 config.yaml，无需重启；页面临时标注"立即生效"。
- 【代码处理】`model_routes.py` PUT → `model_service.update_provider_config` → `config_service.update_config` 写入；LLM 侧 `resolver.py` 每次调用 `get_config_instance()` 实时读取（已有），不做缓存过期化。验收 10.3"修改 Provider 配置"。
- 【涉及代码】model_routes.py / model_service.py（新建）+ resolver.py（已有复用）。

### 8.2.10 模型切换边界（脏态 + 越界）

- 【风险】切换模型后：已改参数丢不丢？超出新模型参数范围的值会不会回写报错？
- 【对策】切换模型时 `useSettings` 合并参数：**未修改**的参数恢复为新模型默认值，**已修改**的参数保留值并标 `[已修改]`；超出新模型范围的值自动截断到边界 + toast 提示。
- 【代码处理】`useSettings.ts` 切换模型分支：遍历 params，`modelUtils.isDirty(key)` 判定已/未修改，未修改项取 `defaults`，已修改项经 `modelUtils.clampToRange(value, range)` 截断后保留并标记；写前统一过 `clampToRange`。`ModelParams.tsx` 渲染 `[已修改]` 角标 + toast。验收 10.3 三项（恢复/保留/截断）。
- 【涉及代码】useSettings.ts / modelUtils.ts / ModelParams.tsx（新建）。

#### 8.2.11 mtime 感知（后门修改刷新）

- 【风险】设置页打开后，后端业务（config_service）改写了 config.yaml，页面仍显示旧值。
- 【对策】首次 `GET /settings` 记录返回的 config.yaml mtime；切 Tab / 手动刷新时比对 mtime，变了（后门改过）→ 自动重新 `GET /settings` 全量刷新 + toast"配置已更新，已刷新"；没变（自己改的）→ 缓存命中零延迟。
- 【代码处理】`settings_routes.py` GET 返回体含 `config.yaml` 的 `mtime`；`useSettings.ts` 首次加载记录，切 Tab/刷新时调轻量 mtime 校验（可复用 `getAll` 或独立 `HEAD`/字段），变化 → 重新全量拉取 + 弹出提示。验收 10.4 mtime 场景。
- 【涉及代码】settings_routes.py / useSettings.ts（新建）。

### 8.3 代码目录树结构（与第八章落地清单一一对应）

> 标注：【新建】= 本次新增文件；【修改】= 改现有文件；【已有复用】= 已有文件直接复用；【不动】= 零改动。— 小欧 2026-09-20

后端 `backend/app/`：

```
backend/app/
├── main.py                                           【修改 +2 行：挂 settings_router + model_router】
├── api/v1/
│   ├── settings_schemas.py                           【新建：settings 特有 DTO；模型 DTO 复用 config_schemas.py】
│   ├── settings_routes.py                            【新建：GET/PUT /settings】
│   ├── model_routes.py                               【新建：/models /providers CRUD】
│   ├── config_schemas.py                             【已有复用：ModelRef/ProviderInfo 等模型 DTO】
│   └── config_routes.py                              【已有不动：/config/* 旧页照旧调用】
├── services/
│   ├── settings/                                     【新建目录：设置页业务域】
│   │   ├── settings_registry.py                      【新建：唯一 Schema 源 + key 唯一自检】
│   │   └── settings_service.py                       【新建：6 组独立读+写】
│   └── model/                                        【已有目录：模型/配置域聚合】
│       ├── model_service.py                          【新建：模型编排器，与 config_service 同域】
│       ├── config_service.py                         【已有复用：update_config 唯一写入口】
│       ├── config_helpers.py                         【已有，write_yaml_config 改调 atomic_write】
│       └── resolver.py                               【已有复用：LLM 侧实时读配置】
└── utils/
    └── file_utils.py                                 【修改：新增 atomic_write + backup_file 编号链 backup.1~5 + FIFO】
```

前端 `frontend/src/`：

```
frontend/src/
├── App.tsx                                           【修改 +1 行：/settings2 懒加载路由】
├── pages/
│   ├── Settings2/index.tsx                           【新建：6 Tab + 模型管理四区域】
│   ├── Settings/index.tsx                            【不动：旧页深链可访】
│   ├── ChatPage.tsx                                  【不动】
│   └── History/index.tsx                             【不动】
├── features/
│   ├── settings2/                                    【新建目录：域内分子目录，与 chat/settings 域惯例一致】
│   │   ├── types.ts                                  【新建：分组/项/ModelState 类型】
│   │   ├── hooks/useSettings.ts                      【新建：全局单层 state + mtime 感知】
│   │   ├── utils/modelUtils.ts                       【新建：isDirty/clampToRange/validate 纯函数】
│   │   └── components/                               【新建目录：13 组件 + icons】
│   │       ├── SettingsPage.tsx / SettingsGroup.tsx / SectionTitle.tsx / SettingRow.tsx
│   │       ├── SearchBox.tsx / SaveBar.tsx / DirtyBadge.tsx
│   │       ├── ModelSelector.tsx / ModelParams.tsx / ProviderConfig.tsx
│   │       ├── ModelActions.tsx / ModelModals.tsx
│   │       └── icons.tsx                             【SettingIcon 唯一 SVG 出口 + DirtyDot + EnvTag】
│   └── settings/                                     【不动：旧组件原样保留，ProviderSettings 被复用】
├── services/
│   ├── api/
│   │   ├── settings.api.ts                           【新建：getSchema/getAll/updateSettings】
│   │   ├── model.api.ts                              【新建：模型 6 函数】
│   │   ├── config.api.ts                             【已有不动：旧页照旧调用】
│   │   └── index.ts                                  【修改 +2 行导出】
│   └── error/handler.ts                              【修改 +3 错误枚举与映射】
├── theme/
│   ├── settingsTokens.ts                             【新建：settingsSpacing + settingsRadius 派生】
│   └── tokens.ts                                     【已有：chatTokens 被复用】
└── components/Layout/index.tsx                       【修改 1 项：菜单改名"设置2版"并改指 /settings2】
```

文档：`backend/FUNCTIONS.md` 【修改：登记 settings_service + model_service 编排函数】

## 九、代码设计（按第八章逐文件，可实施真实代码/diff）

> 本章每个文件均可直接落盘实施：新建类给全量代码，修改类给精确 diff。实现依据：第四章 36 项映射表、5.2 接口契约、6.2 状态设计、7.9 视觉体系、8.1 对应核算。— 小沈（后端）/小强（前端） 2026-09-20

### 9.0 落码顺序与依赖

```
1. file_utils.py 改（atomic_write + backup_file 编号链）      # 9.3.2，地基
2. config_helpers.py 改（write_yaml_config 改调 atomic_write） # 9.3.3，地基
3. settings_registry.py 新建                                    # 9.1.1，无依赖
4. settings_service.py 新建                                    # 9.1.2，依赖 1+2+3
5. model_service.py 新建                                       # 9.1.3，依赖 4（复用 _apply_region_patch）
6. settings_schemas.py + settings_routes.py + model_routes.py  # 9.1.4~9.1.6，依赖 4+5
7. main.py 挂载                                                # 9.3.1
8. 前端：settingsTokens/types/modelUtils/icons → api → useSettings → components → Settings2 → App/Layout/handler  # 9.2~9.3
```

### 9.1 后端新建

#### 9.1.1 `backend/app/services/settings/settings_registry.py`（新建，全量）

```python
# -*- coding: utf-8 -*-
"""
settings_registry — 设置页唯一 Schema 源（3.2 铁律1）
key/类型/默认值/值域/存储/生效/来源规则只定一次；key 全局唯一，加载自检重复直接拒启。

编辑历史:
  2026-09-20 - 小沈 - 新建：第四章 36 项映射表代码化（通用2/模型结构/安全10/聊天6/外观3/系统10）
"""
from typing import Any, Dict, List, Optional


def _item(key: str, type_: str, label: str, default: Any = None,
          options: Optional[List[Any]] = None, range_: Optional[List[float]] = None,
          step: Optional[float] = None, restart: bool = False, secret: bool = False,
          readonly: bool = False, notice: str = "") -> Dict[str, Any]:
    """单项构造：storage 统一 YAML（4.0/v3.0 单存储），来源运行时判定 yaml/env。"""
    return {"key": key, "type": type_, "label": label, "default": default,
            "options": options, "range": range_, "step": step, "storage": "YAML",
            "restart": restart, "secret": secret, "readonly": readonly, "notice": notice}


GROUPS: Dict[str, Dict[str, Any]] = {
    # 4.1 通用（general，2 项）
    "general": {"label": "通用", "items": [
        _item("app.language", "select", "系统语言", "zh-CN",
              options=["zh-CN", "en-US"], restart=True),
        _item("app.project_root", "text", "项目根目录", "E:\\test_dir"),
    ]},
    # 4.2 模型（model，结构化语义；CRUD 由 model_service 承接，见 9.1.3）
    "model": {"label": "模型", "items": [
        _item("ai.model_ref", "model_ref", "当前模型", None,
              notice="与后端 DTO 同形的 {provider, model} 结构；env 接管时整行只读"),
    ]},
    # 4.3 安全（security，10 项，YAML，即时）
    "security": {"label": "安全", "items": [
        _item("security.enabled", "bool", "安全开关", False),
        _item("security.strict_mode", "bool", "严格模式", False),
        _item("security.whitelistEnabled", "bool", "白名单开关", False),
        _item("security.commandWhitelist", "textarea", "命令白名单", ""),
        _item("security.blacklistEnabled", "bool", "黑名单开关", False),
        _item("security.commandBlacklist", "textarea", "命令黑名单", ""),
        _item("security.confirmDangerousOps", "bool", "危险操作确认", True,
              notice="保存二次确认 Modal（UX 层）；后端安全门禁独立生效"),
        _item("security.contentFilterEnabled", "bool", "内容过滤", True),
        _item("security.contentFilterLevel", "select", "过滤级别", "medium",
              options=["low", "medium", "high"]),
        _item("security.maxFileSize", "select", "最大文件(MB)", 100,
              options=[10, 50, 100, 500]),
    ]},
    # 4.4 聊天（chat，6 项，未接入：已存储、尚无消费方）
    "chat": {"label": "聊天", "items": [
        _item("chat.temperature", "range", "温度", 0.7, range_=[0, 2], step=0.1,
              notice="已存储·待二期接入 LLM"),
        _item("chat.top_p", "range", "核采样", 1.0, range_=[0, 1], step=0.05,
              notice="已存储·待二期接入 LLM"),
        _item("chat.max_tokens", "int", "最大输出 tokens", None,
              notice="留空=跟随模型；已存储·待二期接入 LLM"),
        _item("chat.auto_title", "bool", "自动标题", True,
              notice="已存储·待二期接入 LLM"),
        _item("chat.history_limit", "int", "历史加载条数", 50, range_=[1, 200],
              notice="已存储·待二期接入 LLM"),
        _item("chat.stream", "bool", "流式输出", True,
              notice="已存储·待二期接入 LLM"),
    ]},
    # 4.5 外观（theme 只读，字号/密度 YAML 即时 + 本地预应用）
    "appearance": {"label": "外观", "items": [
        _item("app.theme", "readonly", "主题", "light", readonly=True,
              notice="当前固定浅色；深色二期（需全站 token 化重做硬编码色值）"),
        _item("appearance.fontSize", "range", "字号(px)", 14, range_=[12, 18], step=1),
        _item("appearance.density", "select", "消息密度", "comfortable",
              options=["compact", "comfortable"]),
    ]},
    # 4.6 系统（system，10 项：5 系统参数 + 3 运维日志 + 2 关于只读）
    "system": {"label": "系统", "items": [
        _item("app.debug", "bool", "调试模式", True, restart=True),
        _item("app.max_context_tokens", "int", "上下文上限", 200000, restart=True),
        _item("app.max_history_length", "int", "历史保留条数", 10),
        _item("app.max_rounds", "int", "最大轮数", 100),
        _item("app.max_steps", "int", "最大步数", 10000),
        _item("logging.level", "select", "日志级别", "INFO",
              options=["DEBUG", "INFO", "WARNING", "ERROR"], restart=True),
        _item("logging.max_file_size", "int", "日志文件上限(字节)", 10485760, restart=True),
        _item("logging.backup_count", "int", "日志备份数", 5, restart=True),
        _item("config_path", "readonly", "配置文件路径", None, readonly=True),
        _item("version", "readonly", "当前版本", None, readonly=True),
    ]},
}

GROUP_ORDER = ["general", "model", "security", "chat", "appearance", "system"]

# registry key → ConfigUpdate 字段映射（旧键走 config_service.update_config，语义不变；
# 未列出的键走通用 region 合并，见 settings_service._apply_region_patch）
OLD_KEY_MAP: Dict[str, str] = {
    "app.language": "language",
    "app.project_root": "project_root",
    "app.theme": "theme",
    "app.max_steps": "max_steps",
    "ai.model_ref": "ai_model_ref",
}
# security.* 中 ConfigUpdate.SecurityConfig 已有字段（enabled/strict_mode/blacklistEnabled
# 三项为新增键，走通用合并；其余 7 项走 security 字典）
SECURITY_KNOWN = {"whitelistEnabled", "commandWhitelist", "commandBlacklist",
                  "confirmDangerousOps", "contentFilterEnabled",
                  "contentFilterLevel", "maxFileSize"}


def _build_index() -> Dict[str, Dict[str, Any]]:
    """模块加载自检：key 全局唯一，重复直接拒启（3.2 铁律1）。"""
    index: Dict[str, Dict[str, Any]] = {}
    for gname, group in GROUPS.items():
        for item in group["items"]:
            key = item["key"]
            if key in index:
                raise RuntimeError(f"[settings_registry] 重复 key 拒启: {key}")
            index[key] = item
    return index


REGISTRY_INDEX = _build_index()


def get_item(key: str) -> Optional[Dict[str, Any]]:
    """按 key 取 schema 项。"""
    return REGISTRY_INDEX.get(key)


def group_items(group: str) -> List[Dict[str, Any]]:
    """取某组全部 schema 项。"""
    return GROUPS[group]["items"]
```

#### 9.1.2 `backend/app/services/settings/settings_service.py`（新建，全量）

```python
# -*- coding: utf-8 -*-
"""
settings_service — 设置页 6 组服务（3.1 前门：读+写完全独立，不依赖 config_service 读取逻辑）
读：自己解析 config.yaml → {groups:{data,sources,mtime}} + env 标注 + secret 掩码
写：旧键(ConfigUpdate 已有字段)→调 config_service.update_config（语义不变）；
    新键(chat/appearance/logging 等)→通用 region 合并（同套备份+原子写+回滚机制）。
只读项 config_path/version 由本服务组装。

编辑历史:
  2026-09-20 - 小沈 - 新建：3.1 前门实现 + 5.2 env/secret 契约 + 5.3 get_setting
"""
import os
from pathlib import Path
from typing import Any, Dict, List, Optional, Tuple

from app.config import get_config, get_code_root
from app.db.models.chat_models import ModelRef
from app.logger import logger
from app.services.model.config_helpers import (
    _backup_config,
    _restore_backup_if_needed,
    get_config_path,
    read_yaml_config,
)
from app.services.model.config_service import update_config as update_config_service
from app.api.v1.config_schemas import ConfigUpdate
from app.utils.file_utils import atomic_write
from app.services.settings.settings_registry import (
    GROUPS, GROUP_ORDER, OLD_KEY_MAP, SECURITY_KNOWN, get_item,
)


# ------------------------------------------------------------------
# 读：有效值 + 来源
# ------------------------------------------------------------------

def _raw_config() -> Dict[str, Any]:
    """读 YAML 原始值（不经 env 覆盖）。"""
    return read_yaml_config(Path(get_config_path()))


def _get_dotted(data: Dict[str, Any], key: str, default: Any = None) -> Any:
    """点号键取值（config_path/version 等虚拟只读键返回 default）。"""
    node: Any = data
    for part in key.split("."):
        if isinstance(node, dict) and part in node:
            node = node[part]
        else:
            return default
    return node


def _resolved(key: str, default: Any = None) -> Any:
    """读后端实际生效值（含 env 覆盖）。"""
    return get_config().get(key, default)


def _app_version() -> str:
    """读 version.txt 首个非空行（不 import main，避免层倒挂）。"""
    try:
        vf = Path(get_code_root()) / "version.txt"
        if vf.exists():
            with open(vf, "r", encoding="utf-8") as f:
                for line in f:
                    v = line.strip().lstrip("﻿")
                    if v:
                        return v
    except Exception as e:
        logger.warning(f"读取 version 失败: {e}")
    return "0.0.0"


def _mask_secret(value: Any) -> Dict[str, Any]:
    """secret 序列化：永不返明文（5.2 secret 契约）。"""
    s = str(value or "")
    if not s.strip():
        return {"configured": False}
    return {"configured": True, "suffix": s[-4:]}


def _item_data(key: str, item: Dict[str, Any], raw: Dict[str, Any]) -> Tuple[Any, str]:
    """单项有效值 + 来源：只读虚拟键特殊组装；其余比较 YAML 原始 vs 生效值判 env。"""
    if key == "config_path":
        return str(get_config_path()), "ro"
    if key == "version":
        return _app_version(), "ro"
    raw_val = _get_dotted(raw, key, item["default"])
    eff_val = _resolved(key, item["default"])
    if item.get("secret"):
        return _mask_secret(eff_val), ("env" if eff_val != raw_val else "yaml")
    if eff_val != raw_val:
        return eff_val, "env"
    return raw_val if raw_val is not None else item["default"], "yaml"


def get_all_groups() -> Dict[str, Any]:
    """GET /settings 全量：6 组 data + sources + config.yaml mtime（5.2 一次全量）。"""
    raw = _raw_config()
    groups: Dict[str, Any] = {}
    for gname in GROUP_ORDER:
        data: Dict[str, Any] = {}
        sources: Dict[str, str] = {}
        for item in GROUPS[gname]["items"]:
            if gname == "model":
                continue  # 模型组由 model_service 承接（9.1.3），此处只给 schema 占位
            val, src = _item_data(item["key"], item, raw)
            data[item["key"]] = val
            sources[item["key"]] = src
        groups[gname] = {"data": data, "sources": sources}
    return {"groups": groups, "version": _app_version(), "mtime": _config_mtime()}


def get_group(group: str) -> Dict[str, Any]:
    """GET /settings?group=X 单组刷新。"""
    raw = _raw_config()
    data: Dict[str, Any] = {}
    sources: Dict[str, str] = {}
    for item in GROUPS[group]["items"]:
        if group == "model":
            continue
        val, src = _item_data(item["key"], item, raw)
        data[item["key"]] = val
        sources[item["key"]] = src
    return {"data": data, "sources": sources, "mtime": _config_mtime()}


def get_schema() -> Dict[str, Any]:
    """GET /settings/schema：6 分组 schema（label + items，10.1 全量断言用）。"""
    return {"groups": {g: {"label": GROUPS[g]["label"], "items": GROUPS[g]["items"]}
                       for g in GROUP_ORDER}}


def _config_mtime() -> float:
    """config.yaml mtime（3.1 mtime 感知）。"""
    try:
        return os.path.getmtime(get_config_path())
    except OSError:
        return 0.0


def get_setting(key: str, default: Any = None) -> Any:
    """后端任意处一行读取有效值（5.3）：env 覆盖时自动返回生效值。"""
    item = get_item(key)
    return _resolved(key, item["default"] if item else default)


# ------------------------------------------------------------------
# 写：校验 + 分区（旧键走 update_config，新键走通用合并）
# ------------------------------------------------------------------

def _validate_value(item: Dict[str, Any], value: Any) -> Optional[str]:
    """单值按 schema 校验，返回错误文案或 None。"""
    if item.get("readonly"):
        return f"{item['key']} 为只读项"
    t = item["type"]
    if value is None:
        return None
    if t == "bool" and not isinstance(value, bool):
        return f"{item['key']} 应为 bool"
    if t in ("int",) and not isinstance(value, int):
        return f"{item['key']} 应为整数"
    if t in ("float", "range") and not isinstance(value, (int, float)):
        return f"{item['key']} 应为数字"
    if item.get("range"):
        lo, hi = item["range"]
        if not (lo <= float(value) <= hi):
            return f"{item['key']} 超出范围 [{lo}, {hi}]"
    if item.get("options") and value not in item["options"]:
        return f"{item['key']} 非法选项"
    if t == "model_ref" and not (isinstance(value, dict) and value.get("provider") and value.get("model")):
        return "ai.model_ref 应为 {provider, model} 结构"
    return None


def _set_dotted(data: Dict[str, Any], key: str, value: Any) -> None:
    """点号键写入嵌套 dict（config_path/version 等虚拟键上游已拦截，此处不处理）。"""
    parts = key.split(".")
    node = data
    for part in parts[:-1]:
        child = node.get(part)
        if not isinstance(child, dict):
            child = {}
            node[part] = child
        node = child
    node[parts[-1]] = value


def _apply_region_patch(region_updates: Dict[str, Any], scope: str) -> str:
    """通用 region 合并写（新键用；model_service 复用本函数，DRY）：
    备份 → 内存合并 → yaml 重载验证 → atomic_write → 重读验证 → reload；
    异常回滚最近一个备份。返回 backup_path。"""
    from app.services.model.config_helpers import reload_ai_config  # 局部 import 防循环
    config_path = Path(get_config_path())
    backup_path = _backup_config(config_path)
    restored = [False]
    try:
        config_data = read_yaml_config(config_path) or {}
        for key, value in region_updates.items():
            _set_dotted(config_data, key, value)
        import yaml
        yaml.safe_load(yaml.safe_dump(config_data))  # 内存结构可序列化校验
        atomic_write(str(config_path), config_data)
        verify = read_yaml_config(config_path)
        for key, value in region_updates.items():
            if _get_dotted(verify, key) != value:
                raise RuntimeError(f"写入验证失败: {key}")
        reload_ai_config()
        logger.info(f"[{scope}] region 合并写入成功: {sorted(region_updates)}")
        return str(backup_path)
    except Exception:
        _restore_backup_if_needed(backup_path, config_path, restored)
        logger.error(f"[{scope}] region 合并写入失败已回滚", exc_info=True)
        raise


def update_settings(patch: Dict[str, Any]) -> Dict[str, Any]:
    """PUT /settings {patch}：只处理改动项；env 接管项拒绝写入进 warnings（5.2）。"""
    raw = _raw_config()
    updated: List[Dict[str, str]] = []
    need_restart: List[str] = []
    warnings: List[str] = []
    errors: List[str] = []
    old_fields: Dict[str, Any] = {}
    security_fields: Dict[str, Any] = {}
    new_region: Dict[str, Any] = {}

    for key, value in patch.items():
        item = get_item(key)
        if item is None:
            errors.append(f"未知 key: {key}")
            continue
        err = _validate_value(item, value)
        if err:
            errors.append(err)
            continue
        # env 接管项：拒绝写入（5.2 env 契约）
        _, src = _item_data(key, item, raw)
        if src == "env":
            warnings.append(f"{key} 被环境变量接管，已跳过")
            continue
        if item.get("restart") and _get_dotted(raw, key, item["default"]) != value:
            need_restart.append(key)
        mapped = OLD_KEY_MAP.get(key)
        if mapped == "ai_model_ref":
            old_fields["ai_model_ref"] = ModelRef(**value)
            updated.append({"key": key, "source": "yaml"})
        elif mapped:
            old_fields[mapped] = value
            updated.append({"key": key, "source": "yaml"})
        elif key.startswith("security.") and key.split(".", 1)[1] in SECURITY_KNOWN:
            security_fields[key.split(".", 1)[1]] = value
            updated.append({"key": key, "source": "yaml"})
        else:
            new_region[key] = value
            updated.append({"key": key, "source": "yaml"})

    if errors:
        return {"ok": False, "updated": [], "need_restart": [], "warnings": warnings, "errors": errors}
    if security_fields:
        old_fields["security"] = security_fields
    if old_fields:
        update_config_service(ConfigUpdate(**old_fields))  # 旧键语义不变（FIELD_HANDLERS）
    if new_region:
        _apply_region_patch(new_region, scope="settings")
    return {"ok": True, "updated": updated, "need_restart": need_restart, "warnings": warnings}
```

#### 9.1.3 `backend/app/services/model/model_service.py`（新建，全量）

```python
# -*- coding: utf-8 -*-
"""
model_service — 模型编排器（位于 model 域，与 config_service/config_helpers/resolver 同域聚合）
读：解析 config.yaml 的 models/provider_config 区域（动态遍历 provider，禁止硬编码 provider 名，
与旧页铁律一致）；写：校验 → settings_service._apply_region_patch → config.yaml。
current_model_ref 与旧扁平 ai.provider/ai.model 双写保持同步（纯加法，不改旧逻辑）。

编辑历史:
  2026-09-20 - 小沈 - 新建：5.2 模型管理契约（合并返回/级联/switched_to/立即生效）
"""
from pathlib import Path
from typing import Any, Dict, List, Optional

from app.logger import logger
from app.services.model.config_helpers import get_config_path, read_yaml_config
from app.services.settings.settings_service import _apply_region_patch

RESERVED_AI_KEYS = {"provider", "model", "model_ref"}


def _raw_ai() -> Dict[str, Any]:
    """读 config.yaml 的 ai 整块。"""
    raw = read_yaml_config(Path(get_config_path())) or {}
    ai = raw.get("ai", {})
    return ai if isinstance(ai, dict) else {}


def _provider_names(ai: Dict[str, Any]) -> List[str]:
    """动态遍历 provider 名（禁止硬编码）。"""
    return [k for k, v in ai.items() if isinstance(v, dict) and k not in RESERVED_AI_KEYS]


def _mask_key(value: Any) -> Dict[str, Any]:
    """provider api_key 掩码（GET 永不返明文）。"""
    s = str(value or "")
    return {"configured": bool(s.strip()), "suffix": s[-4:] if s.strip() else ""}


def get_current_ref(ai: Dict[str, Any], providers: List[str]) -> Dict[str, str]:
    """当前模型：优先 ai.model_ref 结构，回退旧扁平 ai.provider/ai.model。"""
    ref = ai.get("model_ref")
    if isinstance(ref, dict) and ref.get("provider") and ref.get("model"):
        return {"provider": str(ref["provider"]), "model": str(ref["model"])}
    provider = str(ai.get("provider") or (providers[0] if providers else ""))
    models = _models_of(ai, provider)
    model = str(ai.get("model") or (models[0]["name"] if models else ""))
    return {"provider": provider, "model": model}


def _models_of(ai: Dict[str, Any], provider: str) -> List[Dict[str, Any]]:
    """某 provider 下模型列表（models[] 元素可为 str 或 dict）。"""
    p = ai.get(provider, {})
    raw_models = p.get("models", []) if isinstance(p, dict) else []
    out: List[Dict[str, Any]] = []
    for m in raw_models:
        if isinstance(m, str):
            out.append({"name": m, "label": m, "default_params": {},
                        "range": {}, "capabilities": []})
        elif isinstance(m, dict) and m.get("name"):
            out.append({"name": str(m["name"]), "label": str(m.get("label", m["name"])),
                        "default_params": m.get("default_params", {}) or {},
                        "range": m.get("range", {}) or {},
                        "capabilities": m.get("capabilities", []) or []})
    return out


def get_models() -> Dict[str, Any]:
    """GET /models：providers/models 层级 + current_model_ref（11-17 合并返回）。"""
    ai = _raw_ai()
    providers = []
    for name in _provider_names(ai):
        p = ai[name]
        providers.append({"name": name, "label": str(p.get("label", name)),
                          "api_base": str(p.get("api_base", "")),
                          "api_key": _mask_key(p.get("api_key", "")),
                          "timeout": p.get("timeout", 60),
                          "models": _models_of(ai, name)})
    return {"providers": providers,
            "current_model_ref": get_current_ref(ai, [p["name"] for p in providers])}


def get_providers() -> List[Dict[str, Any]]:
    """GET /providers：provider 列表（含掩码 key）。"""
    return get_models()["providers"]


def _sync_current(region: Dict[str, Any], provider: str, model: str) -> None:
    """current 双写同步：新结构 + 旧扁平（加法，不改旧消费方）。"""
    region["ai.model_ref"] = {"provider": provider, "model": model}
    region["ai.provider"] = provider
    region["ai.model"] = model


def add_model(provider: str, model: str, label: str = "",
              default_params: Optional[Dict[str, Any]] = None,
              range_: Optional[Dict[str, Any]] = None,
              capabilities: Optional[List[str]] = None) -> Dict[str, Any]:
    """POST /models：校验 provider 存在 + provider 内 model 名唯一 → 写入。"""
    ai = _raw_ai()
    if provider not in _provider_names(ai):
        raise ValueError(f"Provider 不存在: {provider}")
    if any(m["name"] == model for m in _models_of(ai, provider)):
        raise ValueError("同名模型已存在")
    models = list(ai[provider].get("models", []) or [])
    models.append({"name": model, "label": label or model,
                   "default_params": default_params or {},
                   "range": range_ or {}, "capabilities": capabilities or []})
    _apply_region_patch({f"ai.{provider}.models": models}, scope="model")
    return {**get_models(), "ok": True}


def update_model(provider: str, model: str, fields: Dict[str, Any]) -> Dict[str, Any]:
    """PUT /models/{provider}/{model}：改 default_params/range/capabilities/label。"""
    ai = _raw_ai()
    models = list(ai.get(provider, {}).get("models", []) or [])
    hit = False
    for i, m in enumerate(models):
        name = m if isinstance(m, str) else m.get("name")
        if name == model:
            base = {"name": model} if isinstance(m, str) else dict(m)
            for k in ("label", "default_params", "range", "capabilities"):
                if fields.get(k) is not None:
                    base[k] = fields[k]
            models[i] = base
            hit = True
    if not hit:
        raise ValueError(f"模型不存在: {provider}/{model}")
    _apply_region_patch({f"ai.{provider}.models": models}, scope="model")
    return {"ok": True, "model": model}


def delete_model(provider: str, model: str) -> Dict[str, Any]:
    """DELETE /models：删当前模型 → 自动切回该 provider 默认模型（switched_to）。"""
    ai = _raw_ai()
    models = [m for m in (ai.get(provider, {}).get("models", []) or [])
              if (m if isinstance(m, str) else m.get("name")) != model]
    region: Dict[str, Any] = {f"ai.{provider}.models": models}
    switched_to = None
    cur = get_current_ref(ai, _provider_names(ai))
    if cur["provider"] == provider and cur["model"] == model:
        names = [(m if isinstance(m, str) else m.get("name")) for m in models]
        target = names[0] if names else ""
        _sync_current(region, provider, target)
        switched_to = target or None
    _apply_region_patch(region, scope="model")
    return {"ok": True, "switched_to": switched_to}


def add_provider(name: str, label: str = "", api_base: str = "",
                 api_key: str = "", model: str = "", timeout: int = 60) -> Dict[str, Any]:
    """POST /providers：name 唯一 → 写入 ai.{name} 块。"""
    ai = _raw_ai()
    if name in _provider_names(ai):
        raise ValueError("同名 Provider 已存在")
    _apply_region_patch({f"ai.{name}": {
        "label": label or name, "api_base": api_base, "api_key": api_key,
        "timeout": timeout, "models": ([model] if model else [])}}, scope="model")
    return {"ok": True, "provider": name}


def update_provider_config(name: str, fields: Dict[str, Any]) -> Dict[str, Any]:
    """PUT /providers/{name}：改 api_key/base_url/timeout → 下次 LLM 调用立即生效。"""
    ai = _raw_ai()
    if name not in _provider_names(ai):
        raise ValueError(f"Provider 不存在: {name}")
    region = {}
    key_map = {"api_key": "api_key", "base_url": "api_base", "api_base": "api_base",
               "timeout": "timeout", "retry_times": "max_retries", "max_retries": "max_retries"}
    for k, v in fields.items():
        if k in key_map and v is not None:
            region[f"ai.{name}.{key_map[k]}"] = v
    if fields.get("clear") is True:
        region[f"ai.{name}.api_key"] = ""  # secret 三态之清空（5.2 secret 契约）
    if not region:
        raise ValueError("无有效配置项")
    _apply_region_patch(region, scope="model")
    return {"ok": True, "provider": name}


def delete_provider(name: str) -> Dict[str, Any]:
    """DELETE /providers：级联删除所有子模型；删当前 Provider → 切回默认。"""
    ai = _raw_ai()
    names = _provider_names(ai)
    if name not in names:
        raise ValueError(f"Provider 不存在: {name}")
    raw = read_yaml_config(Path(get_config_path())) or {}
    ai_block = raw.get("ai", {})
    if isinstance(ai_block, dict) and name in ai_block:
        del ai_block[name]
    _apply_region_patch({"ai": ai_block}, scope="model")
    switched_to = None
    cur = get_current_ref(ai, names)
    if cur["provider"] == name:
        rest = [n for n in names if n != name]
        target_p = rest[0] if rest else ""
        target_m = ""
        if target_p:
            ms = _models_of(ai_block, target_p)
            target_m = ms[0]["name"] if ms else ""
        region: Dict[str, Any] = {}
        _sync_current(region, target_p, target_m)
        _apply_region_patch(region, scope="model")
        switched_to = target_p or None
    return {"ok": True, "switched_to": switched_to}
```

#### 9.1.4 `backend/app/api/v1/settings_schemas.py`（新建，全量）

```python
# -*- coding: utf-8 -*-
"""settings 专用 DTO（Pydantic）。模型管理 DTO 复用 config_schemas，不重复定义。

编辑历史:
  2026-09-20 - 小沈 - 新建：GET/PUT /settings 契约 DTO（5.2）
"""
from typing import Any, Dict, List, Optional
from pydantic import BaseModel, Field


class SettingsGetResponse(BaseModel):
    """GET /settings 全量：6 组 data+sources + 版本 + mtime。"""
    groups: Dict[str, Dict[str, Dict[str, Any]]] = Field(..., description="组→{data,sources}")
    version: str = Field("", description="当前版本")
    mtime: float = Field(0.0, description="config.yaml mtime")


class SchemaItem(BaseModel):
    """单个 schema 项（与 registry _item 同形）。"""
    key: str = Field(...)
    type: str = Field(...)
    label: str = Field(...)
    default: Any = Field(default=None)
    options: Optional[List[Any]] = Field(default=None)
    range: Optional[List[float]] = Field(default=None)
    step: Optional[float] = Field(default=None)
    storage: str = Field("YAML")
    restart: bool = Field(False)
    secret: bool = Field(False)
    readonly: bool = Field(False)
    notice: str = Field("")


class SchemaGroup(BaseModel):
    """单组 schema。"""
    label: str = Field(...)
    items: List[SchemaItem] = Field(...)


class SchemaResponse(BaseModel):
    """GET /settings/schema 响应（10.1 全量断言）。"""
    groups: Dict[str, SchemaGroup] = Field(...)


class SettingsGroupResponse(BaseModel):
    """GET /settings?group=X 单组。"""
    data: Dict[str, Any] = Field(...)
    sources: Dict[str, str] = Field(...)
    mtime: float = Field(0.0)


class SettingsUpdateRequest(BaseModel):
    """PUT /settings 请求：只含改动项 patch。"""
    patch: Dict[str, Any] = Field(..., description="改动项 {key: value}")


class UpdatedItem(BaseModel):
    """更新返回值来源标注。"""
    key: str = Field(...)
    source: str = Field(..., description="yaml|env")


class SettingsUpdateResponse(BaseModel):
    """PUT /settings 响应。"""
    ok: bool = Field(...)
    updated: List[UpdatedItem] = Field(default_factory=list)
    need_restart: List[str] = Field(default_factory=list)
    warnings: List[str] = Field(default_factory=list)
    errors: List[str] = Field(default_factory=list)
```

#### 9.1.5 `backend/app/api/v1/settings_routes.py`（新建，全量）

```python
# -*- coding: utf-8 -*-
"""settings 路由薄壳（与 config_routes.py 同构：路由+DTO+调 service+返回）。

编辑历史:
  2026-09-20 - 小沈 - 新建：GET/PUT /settings（5.2 参数配置接口）
"""
from typing import Optional
from fastapi import APIRouter, Query

from app.api.v1.settings_schemas import (
    SchemaResponse,
    SettingsGetResponse,
    SettingsGroupResponse,
    SettingsUpdateRequest,
    SettingsUpdateResponse,
    UpdatedItem,
)
from app.services.model.config_helpers import handle_config_errors
from app.services.settings import settings_service as svc


router = APIRouter()


@router.get("/settings/schema", response_model=SchemaResponse)
@handle_config_errors("获取设置 Schema")
async def get_settings_schema():
    return svc.get_schema()


@router.get("/settings", response_model=SettingsGetResponse)
@handle_config_errors("获取设置")
async def get_settings(group: Optional[str] = Query(default=None)):
    if group:
        one = svc.get_group(group)
        groups = {group: {"data": one["data"], "sources": one["sources"]}}
        return SettingsGetResponse(groups=groups, version=svc._app_version(),
                                   mtime=one["mtime"])
    all_ = svc.get_all_groups()
    return SettingsGetResponse(groups=all_["groups"], version=all_["version"],
                               mtime=all_["mtime"])


@router.put("/settings", response_model=SettingsUpdateResponse)
@handle_config_errors("保存设置")
async def update_settings(req: SettingsUpdateRequest):
    result = svc.update_settings(req.patch or {})
    return SettingsUpdateResponse(
        ok=result["ok"],
        updated=[UpdatedItem(**u) for u in result.get("updated", [])],
        need_restart=result.get("need_restart", []),
        warnings=result.get("warnings", []),
        errors=result.get("errors", []),
    )
```

#### 9.1.6 `backend/app/api/v1/model_routes.py`（新建，全量）

```python
# -*- coding: utf-8 -*-
"""模型管理路由薄壳（与 config_routes.py 并列；DTO 复用 config_schemas）。

编辑历史:
  2026-09-20 - 小沈 - 新建：/models /providers CRUD（5.2 模型管理接口）
"""
from typing import Any, Dict, List, Optional
from fastapi import APIRouter
from pydantic import BaseModel, Field

from app.api.v1.config_schemas import (
    ModelAddRequest,
    ProviderAddRequest,
    ProviderInfo,
    ProviderUpdate,
)
from app.services.model.config_helpers import handle_config_errors
from app.services.model import model_service as svc


router = APIRouter()


class ModelCreateRequest(BaseModel):
    """POST /models 请求。"""
    provider: str = Field(...)
    model: str = Field(...)
    label: str = Field("")
    default_params: Dict[str, Any] = Field(default_factory=dict)
    range: Dict[str, Any] = Field(default_factory=dict)
    capabilities: List[str] = Field(default_factory=list)


class ModelUpdateRequest(BaseModel):
    """PUT /models/{provider}/{model} 请求。"""
    label: Optional[str] = Field(default=None)
    default_params: Optional[Dict[str, Any]] = Field(default=None)
    range: Optional[Dict[str, Any]] = Field(default=None)
    capabilities: Optional[List[str]] = Field(default=None)


class ProviderConfigUpdate(BaseModel):
    """PUT /providers/{name} 请求（立即生效三件套 + secret 清空态）。"""
    api_key: Optional[str] = Field(default=None)
    base_url: Optional[str] = Field(default=None)
    timeout: Optional[int] = Field(default=None)
    retry_times: Optional[int] = Field(default=None)
    clear: Optional[bool] = Field(default=None, description="clear=true 显式清空 api_key")


@router.get("/models")
@handle_config_errors("获取模型列表")
async def get_models():
    return svc.get_models()


@router.post("/models")
@handle_config_errors("添加模型")
async def add_model(req: ModelCreateRequest):
    return svc.add_model(req.provider, req.model, req.label,
                         req.default_params, req.range, req.capabilities)


@router.put("/models/{provider}/{model}")
@handle_config_errors("修改模型")
async def update_model(provider: str, model: str, req: ModelUpdateRequest):
    return svc.update_model(provider, model, req.model_dump(exclude_none=True))


@router.delete("/models/{provider}/{model}")
@handle_config_errors("删除模型")
async def delete_model(provider: str, model: str):
    return svc.delete_model(provider, model)


@router.get("/providers")
@handle_config_errors("获取 Provider 列表")
async def get_providers():
    return svc.get_providers()


@router.post("/providers")
@handle_config_errors("添加 Provider")
async def add_provider(req: ProviderAddRequest):
    return svc.add_provider(req.name, req.name, req.api_base,
                            req.api_key, req.model, req.timeout)


@router.put("/providers/{name}")
@handle_config_errors("修改 Provider 配置")
async def update_provider(name: str, req: ProviderConfigUpdate):
    return svc.update_provider_config(name, req.model_dump(exclude_none=True))


@router.delete("/providers/{name}")
@handle_config_errors("删除 Provider")
async def delete_provider(name: str):
    return svc.delete_provider(name)
```

### 9.2 前端新建

#### 9.2.1 `frontend/src/services/api/settings.api.ts`（新建，全量）

```ts
// 编辑历史: 2026-09-20 小强 - 新建：设置页 API（getSchema/getAll/getGroup/updateSettings，与 config.api.ts 同模式）
import api from './client';

export type SettingSource = 'yaml' | 'env' | 'ro';

export interface SettingSchemaItem {
  key: string;
  type: 'text' | 'secret' | 'textarea' | 'tags' | 'int' | 'float' | 'range' | 'bool' | 'select' | 'model_ref' | 'readonly';
  label: string;
  default: unknown;
  options?: Array<string | number>;
  range?: [number, number];
  step?: number;
  storage: string;
  restart: boolean;
  secret: boolean;
  readonly: boolean;
  notice?: string;
}

export interface SettingsSchema {
  groups: Record<string, { label: string; items: SettingSchemaItem[] }>;
}

export interface SettingsAll {
  groups: Record<string, { data: Record<string, unknown>; sources: Record<string, SettingSource> }>;
  version: string;
  mtime: number;
}

export interface SettingsUpdateResult {
  ok: boolean;
  updated: Array<{ key: string; source: string }>;
  need_restart: string[];
  warnings: string[];
  errors: string[];
}

export const settingsApi = {
  getSchema: async (): Promise<SettingsSchema> => {
    const response = await api.get<SettingsSchema>('/settings/schema');
    return response.data;
  },

  getAll: async (): Promise<SettingsAll> => {
    const response = await api.get<SettingsAll>('/settings');
    return response.data;
  },

  getGroup: async (group: string): Promise<{ data: Record<string, unknown>; sources: Record<string, SettingSource>; mtime: number }> => {
    const response = await api.get('/settings', { params: { group } });
    return response.data;
  },

  updateSettings: async (patch: Record<string, unknown>): Promise<SettingsUpdateResult> => {
    const response = await api.put<SettingsUpdateResult>('/settings', { patch });
    return response.data;
  },
};
```

#### 9.2.2 `frontend/src/services/api/model.api.ts`（新建，全量）

```ts
// 编辑历史: 2026-09-20 小强 - 新建：模型管理 API（与 config.api.ts 同模式；current_model_ref 复用 SessionModelOverride）
import api from './client';
import type { SessionModelOverride } from '@/types/chat';

export interface ModelEntry {
  name: string;
  label: string;
  default_params: Record<string, unknown>;
  range: Record<string, { min: number; max: number }>;
  capabilities: string[];
}

export interface ProviderEntry {
  name: string;
  label: string;
  api_base: string;
  api_key: { configured: boolean; suffix: string } | string;
  timeout: number;
  models: ModelEntry[];
}

export interface ModelsResponse {
  providers: ProviderEntry[];
  current_model_ref: SessionModelOverride;
  ok?: boolean;
  switched_to?: string | null;
}

export interface ProviderConfigPatch {
  api_key?: string;
  base_url?: string;
  timeout?: number;
  retry_times?: number;
  clear?: boolean;
}

const enc = (s: string): string => encodeURIComponent(s);

export const modelApi = {
  getModels: async (): Promise<ModelsResponse> => {
    const response = await api.get<ModelsResponse>('/models');
    return response.data;
  },

  addModel: async (data: { provider: string; model: string; label?: string; default_params?: Record<string, unknown>; range?: Record<string, { min: number; max: number }>; capabilities?: string[] }): Promise<ModelsResponse> => {
    const response = await api.post<ModelsResponse>('/models', data);
    return response.data;
  },

  updateModel: async (provider: string, model: string, data: Partial<Pick<ModelEntry, 'label' | 'default_params' | 'range' | 'capabilities'>>): Promise<{ ok: boolean; model: string }> => {
    const response = await api.put(`/models/${enc(provider)}/${enc(model)}`, data);
    return response.data;
  },

  deleteModel: async (provider: string, model: string): Promise<{ ok: boolean; switched_to: string | null }> => {
    const response = await api.delete(`/models/${enc(provider)}/${enc(model)}`);
    return response.data;
  },

  getProviders: async (): Promise<ProviderEntry[]> => {
    const response = await api.get<ProviderEntry[]>('/providers');
    return response.data;
  },

  addProvider: async (data: { name: string; label?: string; api_base?: string }): Promise<{ ok: boolean; provider: string }> => {
    const response = await api.post('/providers', data);
    return response.data;
  },

  updateProvider: async (name: string, data: ProviderConfigPatch): Promise<{ ok: boolean; provider: string }> => {
    const response = await api.put(`/providers/${enc(name)}`, data);
    return response.data;
  },

  deleteProvider: async (name: string): Promise<{ ok: boolean; switched_to: string | null }> => {
    const response = await api.delete(`/providers/${enc(name)}`);
    return response.data;
  },
};
```

#### 9.2.3 `frontend/src/pages/Settings2/index.tsx`（新建，全量）

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：设置2版页面入口（同旧页骨架，白底 Card + Tabs 由 SettingsPage 承载）
import React from 'react';
import SettingsPage from '@/features/settings2/components/SettingsPage';

const Settings2: React.FC = () => <SettingsPage />;

export default Settings2;
```

#### 9.2.4 `frontend/src/theme/settingsTokens.ts`（新建，全量）

```ts
// 编辑历史: 2026-09-20 小强 - 新建：只增页级派生常量（pagePadding/rowHeight/labelWidth），复用 stepStyles + chatTokens，不建大表（7.9）
import { Spacing, Radius } from '@/utils/stepStyles';

export const settingsSpacing = {
  ...Spacing,
  pagePadding: 16,
  rowHeight: 44,
  labelWidth: 132,
} as const;

export const settingsRadius = { ...Radius } as const;

export type SettingsSpacing = typeof settingsSpacing;
```

#### 9.2.5 `frontend/src/features/settings2/types.ts`（新建，全量）

```ts
// 编辑历史: 2026-09-20 小强 - 新建：设置2版类型层（分组/项/模型管理状态，见 6.2）
import type { SessionModelOverride } from '@/types/chat';
import type { SettingSchemaItem, SettingSource } from '@/services/api/settings.api';
import type { ModelEntry, ProviderEntry } from '@/services/api/model.api';

export type { SettingSchemaItem, SettingSource, ModelEntry, ProviderEntry };

export type TabKey = 'general' | 'model' | 'security' | 'chat' | 'appearance' | 'system';

export interface ModelState {
  providers: ProviderEntry[];
  selectedProvider: string;
  selectedModel: string;
  params: Record<string, unknown>;
  defaults: Record<string, unknown>;
  ranges: Record<string, { min: number; max: number }>;
  envOverride: Record<string, boolean>;
  providerConfig: Record<string, { api_key: unknown; base_url: string; timeout: number }>;
  isDirty: boolean;
  editingProviderConfig: boolean;
  addModelModalOpen: boolean;
  addProviderModalOpen: boolean;
  deleteConfirmOpen: boolean;
  deleteTarget: string | null;
}

export interface SettingsState {
  schema: Record<string, { label: string; items: SettingSchemaItem[] }>;
  values: Record<string, Record<string, unknown>>;
  sources: Record<string, Record<string, SettingSource>>;
  dirtyKeys: Record<string, boolean>;
  mtime: number;
  loading: boolean;
  loadError: string | null;
  activeTab: TabKey;
  model: ModelState;
  currentRef: SessionModelOverride | null;
}
```

#### 9.2.6 `frontend/src/features/settings2/utils/modelUtils.ts`（新建，全量）

```ts
// 编辑历史: 2026-09-20 小强 - 新建：纯函数层（isDirty/clampToRange/validate，无 JSX，可单测，见 6.2/7.5）
import type { SettingSchemaItem } from '@/services/api/settings.api';

/** 模型 Tab 脏态：值与默认不一致且未被 env 接管（6.2）。 */
export function isDirty(
  params: Record<string, unknown>,
  defaults: Record<string, unknown>,
  env: Record<string, boolean>,
): Record<string, boolean> {
  const out: Record<string, boolean> = {};
  Object.keys(params).forEach((key) => {
    out[key] = !env[key] && params[key] !== defaults[key];
  });
  return out;
}

/** 超出新模型范围自动截断（7.5/9.10）。 */
export function clampToRange(value: unknown, range?: { min: number; max: number }): unknown {
  if (typeof value !== 'number' || !range) return value;
  if (value < range.min) return range.min;
  if (value > range.max) return range.max;
  return value;
}

/** 保存前 schema 校验：返回首个错误 {key, message}，通过返回 null（7.5 首个错误控件红框+定位）。 */
export function validate(
  items: SettingSchemaItem[],
  values: Record<string, unknown>,
): { key: string; message: string } | null {
  for (const item of items) {
    if (item.readonly) continue;
    const value = values[item.key];
    if (value === undefined || value === null) continue;
    if ((item.type === 'bool') && typeof value !== 'boolean') {
      return { key: item.key, message: `${item.label}应为开关值` };
    }
    if ((item.type === 'int' || item.type === 'float' || item.type === 'range') && typeof value !== 'number') {
      return { key: item.key, message: `${item.label}应为数字` };
    }
    if (item.range && typeof value === 'number') {
      const [lo, hi] = item.range;
      if (value < lo || value > hi) {
        return { key: item.key, message: `${item.label}超出范围 [${lo}, ${hi}]` };
      }
    }
    if (item.options && !item.options.includes(value as string)) {
      return { key: item.key, message: `${item.label}为非法选项` };
    }
  }
  return null;
}
```

#### 9.2.7 `frontend/src/features/settings2/components/icons.tsx`（新建，全量）

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：SettingIcon 唯一图标出口 + DirtyDot + EnvTag；全部 antd SVG，禁止 emoji（7.9.4）
import React from 'react';
import {
  ApiOutlined,
  BgColorsOutlined,
  CopyOutlined,
  DesktopOutlined,
  GlobalOutlined,
  MessageOutlined,
  SafetyOutlined,
} from '@ant-design/icons';
import { Tag } from 'antd';
import { Colors, FontSize } from '@/utils/stepStyles';
import type { TabKey } from '../types';

const TAB_ICON_STYLE: React.CSSProperties = { fontSize: 14, color: Colors.TEXT.SECONDARY };

export const SettingIcon: Record<TabKey, React.ReactNode> = {
  general: <GlobalOutlined style={TAB_ICON_STYLE} />,
  model: <ApiOutlined style={TAB_ICON_STYLE} />,
  security: <SafetyOutlined style={TAB_ICON_STYLE} />,
  chat: <MessageOutlined style={TAB_ICON_STYLE} />,
  appearance: <BgColorsOutlined style={TAB_ICON_STYLE} />,
  system: <DesktopOutlined style={TAB_ICON_STYLE} />,
};

/** 已修改角标：6px 实心圆点 + 12px 次文（7.9.2，不用 emoji）。 */
export const DirtyDot: React.FC = () => (
  <span style={{ display: 'inline-flex', alignItems: 'center', gap: 4 }}>
    <span style={{ width: 6, height: 6, borderRadius: '50%', background: Colors.PRIMARY, display: 'inline-block' }} />
    <span style={{ fontSize: FontSize.SECONDARY, color: Colors.TEXT.SECONDARY }}>已修改</span>
  </span>
);

/** env 接管标注（9.2.3）。 */
export const EnvTag: React.FC = () => (
  <Tag color="orange" style={{ marginLeft: 8 }}>来自环境变量</Tag>
);

/** 只读复制按钮图标（7.7/7.9.4）。 */
export const CopyIcon: React.FC = () => <CopyOutlined style={{ fontSize: 14 }} />;
```

#### 9.2.8 `frontend/src/features/settings2/hooks/useSettings.ts`（新建，全量）

```ts
// 编辑历史: 2026-09-20 小强 - 新建：全局单层 state（6 组+脏态+sources+模型管理+mtime 感知+外观本地预应用，见 6.2/6.3/7.0.5）
import { useCallback, useEffect, useMemo, useState } from 'react';
import { message } from 'antd';
import { settingsApi, type SettingSchemaItem } from '@/services/api/settings.api';
import { modelApi, type ModelEntry } from '@/services/api/model.api';
import { isDirty, clampToRange, validate } from '../utils/modelUtils';
import type { SettingsState, TabKey } from '../types';
import { handleApiError } from '@/services/error/handler';

const PREF_KEY = 'omni.prefs.v1';
const GROUP_ORDER: TabKey[] = ['general', 'model', 'security', 'chat', 'appearance', 'system'];

const initialModel = () => ({
  providers: [], selectedProvider: '', selectedModel: '',
  params: {}, defaults: {}, ranges: {}, envOverride: {},
  providerConfig: {}, isDirty: false, editingProviderConfig: false,
  addModelModalOpen: false, addProviderModalOpen: false,
  deleteConfirmOpen: false, deleteTarget: null as string | null,
});

function readLocalPrefs(): Record<string, unknown> {
  try {
    return JSON.parse(localStorage.getItem(PREF_KEY) || '{}') as Record<string, unknown>;
  } catch {
    return {};
  }
}

export function useSettings() {
  const [state, setState] = useState<SettingsState>({
    schema: {}, values: {}, sources: {}, dirtyKeys: {}, mtime: 0,
    loading: true, loadError: null, activeTab: 'general',
    model: initialModel(), currentRef: null,
  });
  const [saving, setSaving] = useState(false);
  const [restartKeys, setRestartKeys] = useState<string[]>([]);
  const [highlightKey, setHighlightKey] = useState<string | null>(null);

  const patchState = useCallback((p: Partial<SettingsState>) => {
    setState((s) => ({ ...s, ...p }));
  }, []);

  const patchModel = useCallback((p: Partial<SettingsState['model']>) => {
    setState((s) => ({ ...s, model: { ...s.model, ...p } }));
  }, []);

  /** 首次加载：schema + 全量值 + 模型列表一次拉取（6.3/7.0.5）。 */
  const load = useCallback(async () => {
    patchState({ loading: true, loadError: null });
    try {
      const [schema, all, models] = await Promise.all([
        settingsApi.getSchema(), settingsApi.getAll(), modelApi.getModels(),
      ]);
      const values = all.groups ? Object.fromEntries(
        Object.entries(all.groups).map(([g, v]) => [g, { ...v.data }]),
      ) : {};
      const sources = all.groups ? Object.fromEntries(
        Object.entries(all.groups).map(([g, v]) => [g, { ...v.sources }]),
      ) : {};
      // 7.6 外观本地预应用覆盖
      const prefs = readLocalPrefs();
      if (values.appearance) {
        if (typeof prefs.fontSize === 'number') values.appearance['appearance.fontSize'] = prefs.fontSize;
        if (typeof prefs.density === 'string') values.appearance['appearance.density'] = prefs.density;
      }
      const ref = models.current_model_ref ?? null;
      const provider = models.providers.find((p) => p.name === ref?.provider) ?? models.providers[0];
      const current = provider?.models.find((m) => m.name === (ref?.model ?? '')) ?? provider?.models[0];
      const defaults = Object.fromEntries(
        Object.entries((current?.default_params ?? {}) as Record<string, unknown>),
      );
      patchState({
        schema: Object.fromEntries(Object.entries(schema.groups).map(([g, v]) => [g, v])),
        values, sources, dirtyKeys: {}, mtime: all.mtime, loading: false,
        currentRef: ref,
        model: {
          ...initialModel(),
          providers: models.providers,
          selectedProvider: provider?.name ?? '',
          selectedModel: current?.name ?? '',
          params: { ...defaults }, defaults,
          ranges: { ...((current?.range ?? {}) as Record<string, { min: number; max: number }>) },
          providerConfig: Object.fromEntries(models.providers.map((p) => [p.name, {
            api_key: p.api_key, base_url: p.api_base, timeout: p.timeout,
          }])),
        },
      });
    } catch (e) {
      handleApiError(e);
      patchState({ loading: false, loadError: '设置加载失败' });
    }
  }, [patchState]);

  useEffect(() => { void load(); }, [load]);

  /** 切 Tab/刷新 mtime 检查（3.1/9.11）。 */
  const checkMtime = useCallback(async () => {
    try {
      const all = await settingsApi.getAll();
      if (all.mtime !== state.mtime) {
        await load();
        message.info('配置已更新，已刷新');
      }
    } catch (e) {
      handleApiError(e);
    }
  }, [state.mtime, load]);

  /** 改控件：记脏态；外观两项同步 localStorage 预览（7.6）。 */
  const setValue = useCallback((group: string, key: string, value: unknown) => {
    setState((s) => {
      const values = { ...s.values, [group]: { ...(s.values[group] ?? {}), [key]: value } };
      if (group === 'appearance' && (key === 'appearance.fontSize' || key === 'appearance.density')) {
        try {
          localStorage.setItem(PREF_KEY, JSON.stringify({
            ...readLocalPrefs(),
            [key === 'appearance.fontSize' ? 'fontSize' : 'density']: value,
          }));
        } catch { /* 本地预览失败不阻断 */ }
      }
      return { ...s, values, dirtyKeys: { ...s.dirtyKeys, [`${group}.${key}`]: true } };
    });
  }, []);

  const groupOfKey = useCallback((key: string): string | null => {
    for (const [g, items] of Object.entries(state.schema)) {
      if (items.items.some((i) => i.key === key)) return g;
    }
    return null;
  }, [state.schema]);

  const dirtyCount = useMemo(() => Object.keys(state.dirtyKeys).length, [state.dirtyKeys]);

  const isGroupDirty = useCallback((tab: TabKey) => {
    if (tab === 'model') return state.model.isDirty;
    return Object.keys(state.dirtyKeys).some((k) => k.startsWith(`${tab}.`));
  }, [state.dirtyKeys, state.model.isDirty]);

  /** 写（参数配置）：schema 校验 → PUT /settings（6.3/7.5）。 */
  const saveKeys = useCallback(async (keys: string[]) => {
    const items: SettingSchemaItem[] = [];
    const values: Record<string, unknown> = {};
    keys.forEach((ck) => {
      const dot = ck.indexOf('.');
      const g = ck.slice(0, dot);
      const key = ck.slice(dot + 1);
      const item = state.schema[g]?.items.find((i) => i.key === key);
      if (item) {
        items.push(item);
        values[key] = state.values[g]?.[key];
      }
    });
    const bad = validate(items, values);
    if (bad) {
      setHighlightKey(bad.key);
      message.warning(bad.message);
      return { ok: false as const, firstError: bad.key };
    }
    setSaving(true);
    try {
      const patch = Object.fromEntries(keys.map((ck) => {
        const dot = ck.indexOf('.');
        return [ck.slice(dot + 1), state.values[ck.slice(0, dot)]?.[ck.slice(dot + 1)]];
      }));
      const result = await settingsApi.updateSettings(patch);
      if (!result.ok) {
        result.errors.forEach((m) => message.error(m));
        return { ok: false as const };
      }
      result.warnings.forEach((m) => message.warning(m));
      if (result.need_restart.length) setRestartKeys(result.need_restart);
      else message.success('保存成功');
      setState((s) => {
        const dirtyKeys = { ...s.dirtyKeys };
        keys.forEach((k) => { delete dirtyKeys[k]; });
        return { ...s, dirtyKeys };
      });
      return { ok: true as const };
    } catch (e) {
      handleApiError(e);
      return { ok: false as const };
    } finally {
      setSaving(false);
    }
  }, [state.schema, state.values]);

  const saveGroup = useCallback((tab: TabKey) => {
    if (tab === 'model') return saveModelGroup();
    const keys = Object.keys(state.dirtyKeys).filter((k) => k.startsWith(`${tab}.`));
    return saveKeys(keys);
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [state.dirtyKeys, saveKeys, state.model]);

  const saveAll = useCallback(() => saveKeys(Object.keys(state.dirtyKeys)), [state.dirtyKeys, saveKeys]);

  // ---- 模型 Tab（9.10 脏态合并/截断；6.5 四区域） ----
  const currentModelEntry = useCallback((): ModelEntry | undefined => {
    const p = state.model.providers.find((x) => x.name === state.model.selectedProvider);
    return p?.models.find((m) => m.name === state.model.selectedModel);
  }, [state.model.providers, state.model.selectedProvider, state.model.selectedModel]);

  const selectProvider = useCallback((name: string) => {
    const p = state.model.providers.find((x) => x.name === name);
    const first = p?.models[0];
    const defaults = { ...((first?.default_params ?? {}) as Record<string, unknown>) };
    patchModel({
      selectedProvider: name, selectedModel: first?.name ?? '',
      params: defaults, defaults,
      ranges: { ...((first?.range ?? {}) as Record<string, { min: number; max: number }>) },
      isDirty: false,
    });
  }, [patchModel, state.model.providers]);

  const selectModel = useCallback((name: string) => {
    const entry = state.model.providers
      .find((x) => x.name === state.model.selectedProvider)?.models
      .find((m) => m.name === name);
    if (!entry) return;
    const nextDefaults = { ...entry.default_params } as Record<string, unknown>;
    const nextRanges = { ...(entry.range ?? {}) } as Record<string, { min: number; max: number }>;
    const dirty = isDirty(state.model.params, state.model.defaults, state.model.envOverride);
    const params: Record<string, unknown> = {};
    Object.keys(nextDefaults).forEach((k) => {
      params[k] = dirty[k]
        ? clampToRange(state.model.params[k], nextRanges[k])
        : nextDefaults[k];
    });
    if (dirty && Object.values(dirty).some(Boolean)) message.info('已修改参数已保留并标记');
    patchModel({
      selectedModel: name, params, defaults: nextDefaults, ranges: nextRanges,
      isDirty: Object.values(isDirty(params, nextDefaults, state.model.envOverride)).some(Boolean),
    });
  }, [patchModel, state.model]);

  const setParam = useCallback((key: string, value: unknown) => {
    setState((s) => {
      const params = { ...s.model.params, [key]: clampToRange(value, s.model.ranges[key]) };
      return { ...s, model: { ...s.model, params, isDirty: Object.values(isDirty(params, s.model.defaults, s.model.envOverride)).some(Boolean) } };
    });
  }, []);

  const resetParams = useCallback(() => {
    patchModel({ params: { ...state.model.defaults }, isDirty: false });
  }, [patchModel, state.model.defaults]);

  const saveModelGroup = useCallback(async () => {
    const dirty = isDirty(state.model.params, state.model.defaults, state.model.envOverride);
    const changed = Object.fromEntries(Object.keys(dirty).filter((k) => dirty[k]).map((k) => [k, state.model.params[k]]));
    if (!Object.keys(changed).length) return { ok: true as const };
    setSaving(true);
    try {
      await settingsApi.updateSettings({
        'ai.model_ref': { provider: state.model.selectedProvider, model: state.model.selectedModel },
        ...changed,
      });
      message.success('模型参数已保存');
      patchModel({ defaults: { ...state.model.params }, isDirty: false });
      return { ok: true as const };
    } catch (e) {
      handleApiError(e);
      return { ok: false as const };
    } finally {
      setSaving(false);
    }
  }, [patchModel, state.model]);

  const refreshModels = useCallback(async (select?: { provider: string; model: string }) => {
    try {
      const models = await modelApi.getModels();
      patchModel({ providers: models.providers });
      if (select) {
        const p = models.providers.find((x) => x.name === select.provider);
        const m = p?.models.find((x) => x.name === select.model);
        if (p && m) {
          const defaults = { ...(m.default_params as Record<string, unknown>) };
          patchModel({
            selectedProvider: p.name, selectedModel: m.name,
            params: { ...defaults }, defaults,
            ranges: { ...(m.range as Record<string, { min: number; max: number }>) },
            isDirty: false,
          });
        }
      }
      return models;
    } catch (e) {
      handleApiError(e);
      return null;
    }
  }, [patchModel]);

  return {
    state, saving, restartKeys, setRestartKeys, highlightKey, setHighlightKey,
    dirtyCount, isGroupDirty, groupOfKey, GROUP_ORDER,
    load, checkMtime, setValue, saveGroup, saveAll,
    selectProvider, selectModel, setParam, resetParams, saveModelGroup, refreshModels,
    patchModel, patchState, setActiveTab: (t: TabKey) => patchState({ activeTab: t }),
  };
}

export type UseSettings = ReturnType<typeof useSettings>;
```

#### 9.2.9 `components/` 十三文件（新建，全量）

**`SectionTitle.tsx`**（小节头 14/600，7.9.1）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：小节头（4.6 系统三小节分隔）
import React from 'react';
import { FontSize, FontWeight } from '@/utils/stepStyles';

export const SectionTitle: React.FC<{ title: string }> = ({ title }) => (
  <div style={{ fontSize: FontSize.PRIMARY, fontWeight: FontWeight.BOLD, margin: '12px 0 4px' }}>
    {title}
  </div>
);
```

**`SettingRow.tsx`**（label+控件+帮助一行，7.4/7.7/7.9）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：单行渲染（控件↔schema.type↔antd；secret 三态/只读复制/env 只读，见 7.4/7.7）
import React, { useState } from 'react';
import { Button, Input, InputNumber, Select, Slider, Switch, message } from 'antd';
import { FontSize, FontWeight, Colors } from '@/utils/stepStyles';
import { chatTokens } from '@/theme/tokens';
import { settingsSpacing } from '@/theme/settingsTokens';
import type { SettingSchemaItem, SettingSource } from '@/services/api/settings.api';
import { EnvTag, CopyIcon, DirtyDot } from './icons';

interface Props {
  item: SettingSchemaItem;
  value: unknown;
  source: SettingSource;
  dirty: boolean;
  highlight: boolean;
  onChange: (value: unknown) => void;
}

export const SettingRow: React.FC<Props> = ({ item, value, source, dirty, highlight, onChange }) => {
  const [editingSecret, setEditingSecret] = useState(false);
  const [secretInput, setSecretInput] = useState('');
  const disabled = item.readonly || source === 'env';

  const renderControl = (): React.ReactNode => {
    if (item.readonly) {
      return (
        <span>
          <span style={{ fontSize: FontSize.CODE }}>{String(value ?? '')}</span>
          <Button
            type="link"
            icon={<CopyIcon />}
            onClick={() => { void navigator.clipboard.writeText(String(value ?? '')); message.success('已复制'); }}
          />
        </span>
      );
    }
    if (item.secret) {
      const cfg = value as { configured?: boolean; suffix?: string } | undefined;
      if (!editingSecret) {
        return (
          <span>
            {cfg?.configured ? `已配置 ····${cfg.suffix ?? ''}` : '未配置'}
            <Button type="link" disabled={source === 'env'} onClick={() => { setSecretInput(''); setEditingSecret(true); }}>
              {cfg?.configured ? '修改' : '配置'}
            </Button>
          </span>
        );
      }
      return (
        <span style={{ display: 'inline-flex', gap: 8 }}>
          <Input.Password
            value={secretInput}
            onChange={(e) => setSecretInput(e.target.value)}
            placeholder="留空=保持原值"
            style={{ width: 220 }}
          />
          <Button type="primary" size="small" onClick={() => { onChange(secretInput); setEditingSecret(false); }}>确定</Button>
          <Button size="small" onClick={() => { onChange({ clear: true }); setEditingSecret(false); }}>清空</Button>
          <Button size="small" onClick={() => setEditingSecret(false)}>取消</Button>
        </span>
      );
    }
    switch (item.type) {
      case 'bool':
        return <Switch checked={value as boolean} disabled={disabled} onChange={onChange} />;
      case 'select':
        return (
          <Select value={value as string} disabled={disabled} style={{ width: 200 }} onChange={onChange}>
            {(item.options ?? []).map((o) => <Select.Option key={String(o)} value={o as string}>{String(o)}</Select.Option>)}
          </Select>
        );
      case 'range':
        return (
          <span style={{ display: 'inline-flex', alignItems: 'center', gap: 8 }}>
            <Slider
              min={item.range?.[0]} max={item.range?.[1]} step={item.step ?? 1}
              value={value as number} disabled={disabled} style={{ width: 160 }}
              onChange={onChange}
            />
            <InputNumber
              min={item.range?.[0]} max={item.range?.[1]} step={item.step ?? 1}
              value={value as number} disabled={disabled} onChange={(v) => onChange(v)}
            />
          </span>
        );
      case 'int':
      case 'float':
        return <InputNumber value={value as number} disabled={disabled} onChange={(v) => onChange(v)} />;
      case 'textarea':
        return <Input.TextArea value={value as string} disabled={disabled} rows={3} style={{ width: 320 }} onChange={(e) => onChange(e.target.value)} />;
      case 'model_ref':
        return <span style={{ color: Colors.TEXT.SECONDARY }}>由模型 Tab 选择器管理</span>;
      default:
        return <Input value={value as string} disabled={disabled} style={{ width: 260 }} onChange={(e) => onChange(e.target.value)} />;
    }
  };

  return (
    <div
      style={{
        display: 'flex', alignItems: 'center', minHeight: settingsSpacing.rowHeight,
        borderBottom: `1px solid ${Colors.BORDER.LIGHT}`,
        background: highlight ? chatTokens.colorPrimaryBg : undefined,
      }}
    >
      <span style={{ width: settingsSpacing.labelWidth, fontSize: FontSize.PRIMARY, fontWeight: FontWeight.REGULAR }}>
        {item.label}
      </span>
      <span style={{ flex: 1 }}>{renderControl()}</span>
      {dirty && <DirtyDot />}
      {source === 'env' && <EnvTag />}
      <span style={{ fontSize: FontSize.SECONDARY, color: Colors.TEXT.SECONDARY, marginLeft: 8 }}>
        {item.notice || (item.restart ? '重启生效' : '即时生效')}
      </span>
    </div>
  );
};
```

**`SettingsGroup.tsx`**（组渲染 + 外观预览小卡内联，8.1.2-#4）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：组渲染（7.3 动态渲染；6.2 局部脏态汇总；外观预览小卡内联）
import React from 'react';
import { Card } from 'antd';
import { FontSize } from '@/utils/stepStyles';
import type { SettingSchemaItem, SettingSource } from '@/services/api/settings.api';
import { SettingRow } from './SettingRow';
import { SectionTitle } from './SectionTitle';

interface Props {
  group: string;
  items: SettingSchemaItem[];
  values: Record<string, unknown>;
  sources: Record<string, SettingSource>;
  dirtyKeys: Record<string, boolean>;
  highlightKey: string | null;
  onChange: (group: string, key: string, value: unknown) => void;
}

function sectionOf(group: string, key: string): string | null {
  if (group !== 'system') return null;
  if (key.startsWith('app.')) return '系统参数';
  if (key.startsWith('logging.')) return '运维日志';
  return '关于';
}

export const SettingsGroup: React.FC<Props> = ({ group, items, values, sources, dirtyKeys, highlightKey, onChange }) => {
  let lastSection: string | null = null;
  const fontSize = Number(values['appearance.fontSize'] ?? 14);
  const density = String(values['appearance.density'] ?? 'comfortable');
  return (
    <div>
      {group === 'appearance' && (
        <Card size="small" style={{ marginBottom: 12 }} title="预览小卡（改下面控件，这里实时变）">
          <div style={{ fontSize, padding: density === 'compact' ? 4 : 12, background: '#f5f5f5', borderRadius: 6 }}>
            示例消息气泡：字号 {fontSize}px / {density === 'compact' ? '紧凑' : '舒适'}
          </div>
        </Card>
      )}
      {group === 'chat' && (
        <div style={{ fontSize: FontSize.SECONDARY, color: '#8c8c8c', marginBottom: 8 }}>
          聊天默认参数［已存储·待二期接入 LLM，当前不承诺生效］
        </div>
      )}
      {items.map((item) => {
        const section = sectionOf(group, item.key);
        const header = section && section !== lastSection ? section : null;
        lastSection = section;
        return (
          <React.Fragment key={item.key}>
            {header && <SectionTitle title={`── ${header} ──`} />}
            <SettingRow
              item={item}
              value={values[item.key]}
              source={sources[item.key] ?? 'yaml'}
              dirty={!!dirtyKeys[`${group}.${item.key}`]}
              highlight={highlightKey === item.key}
              onChange={(v) => onChange(group, item.key, v)}
            />
          </React.Fragment>
        );
      })}
    </div>
  );
};
```

**`SearchBox.tsx`**（搜索过滤+高亮+自动切 Tab，6.4）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：搜索框（key/显示名跨分组过滤，高亮并定位到对应 Tab）
import React, { useState } from 'react';
import { Input } from 'antd';
import type { SettingSchemaItem } from '@/services/api/settings.api';
import type { TabKey } from '../types';

interface Props {
  schema: Record<string, { label: string; items: SettingSchemaItem[] }>;
  onJump: (tab: TabKey, key: string) => void;
}

export const SearchBox: React.FC<Props> = ({ schema, onJump }) => {
  const [query, setQuery] = useState('');
  const search = (q: string) => {
    setQuery(q);
    const text = q.trim().toLowerCase();
    if (!text) return;
    for (const [g, grp] of Object.entries(schema)) {
      const hit = grp.items.find(
        (i) => i.key.toLowerCase().includes(text) || i.label.includes(q.trim()),
      );
      if (hit) {
        onJump(g as TabKey, hit.key);
        return;
      }
    }
  };
  return (
    <Input.Search
      placeholder="搜索设置项 / key..."
      style={{ width: 260 }}
      value={query}
      onChange={(e) => search(e.target.value)}
      onSearch={search}
    />
  );
};
```

**`SaveBar.tsx`**（底部保存栏 + 危险二次确认 + 重启 Modal，8.1.2-#5/#8）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：底部保存栏（[保存本组]+[保存全部]；危险项二次确认与重启 Modal 内联）
import React from 'react';
import { Button, Modal } from 'antd';
import { Colors } from '@/utils/stepStyles';

interface Props {
  canSaveGroup: boolean;
  canSaveAll: boolean;
  dirtyCount: number;
  saving: boolean;
  restartKeys: string[];
  hasDangerousDirty: boolean;
  onSaveGroup: () => void;
  onSaveAll: () => void;
  onCloseRestart: () => void;
}

export const SaveBar: React.FC<Props> = ({
  canSaveGroup, canSaveAll, dirtyCount, saving,
  restartKeys, hasDangerousDirty, onSaveGroup, onSaveAll, onCloseRestart,
}) => {
  const confirmThen = (fn: () => void) => {
    if (hasDangerousDirty) {
      Modal.confirm({
        title: '含危险操作相关改动',
        content: '本次保存涉及危险操作/黑白名单配置，确认提交吗？',
        okText: '确认保存',
        cancelText: '取消',
        onOk: fn,
      });
    } else {
      fn();
    }
  };
  return (
    <div style={{ position: 'sticky', bottom: 0, marginTop: 12, padding: '12px 0 0', background: '#fff', borderTop: `1px solid ${Colors.BORDER.LIGHT}` }}>
      <Button disabled={!canSaveGroup || saving} loading={saving} onClick={() => confirmThen(onSaveGroup)}>
        保存本组
      </Button>
      <Button type="primary" disabled={!canSaveAll || saving} loading={saving} style={{ marginLeft: 8 }} onClick={() => confirmThen(onSaveAll)}>
        保存全部{dirtyCount > 0 ? `（${dirtyCount} 项）` : ''}
      </Button>
      <Modal
        open={restartKeys.length > 0}
        title="含重启生效项"
        onOk={onCloseRestart}
        onCancel={onCloseRestart}
        okText="知道了"
        cancelButtonProps={{ style: { display: 'none' } }}
      >
        以下改动需重启后端生效：
        <ul>{restartKeys.map((k) => <li key={k}>{k}</li>)}</ul>
      </Modal>
    </div>
  );
};
```

**`DirtyBadge.tsx`**（全局脏角标，6.4）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：全局脏角标“有 N 项未保存”
import React from 'react';
import { Tag } from 'antd';

export const DirtyBadge: React.FC<{ count: number }> = ({ count }) => {
  if (count <= 0) return null;
  return <Tag color="orange">有 {count} 项未保存</Tag>;
};
```

**`ModelSelector.tsx`**（双下拉联动 + 能力 Tag，6.5-①）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：模型选择器（Provider/Model 双下拉联动 + 能力标签 + CRUD 入口）
import React from 'react';
import { Button, Select, Tag } from 'antd';
import { PlusOutlined } from '@ant-design/icons';
import type { ProviderEntry } from '@/services/api/model.api';

interface Props {
  providers: ProviderEntry[];
  selectedProvider: string;
  selectedModel: string;
  onSelectProvider: (name: string) => void;
  onSelectModel: (name: string) => void;
  onAddModel: () => void;
  onAddProvider: () => void;
}

export const ModelSelector: React.FC<Props> = ({
  providers, selectedProvider, selectedModel,
  onSelectProvider, onSelectModel, onAddModel, onAddProvider,
}) => {
  const provider = providers.find((p) => p.name === selectedProvider);
  const current = provider?.models.find((m) => m.name === selectedModel);
  return (
    <div>
      <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
        <Select value={selectedProvider} style={{ width: 200 }} onChange={onSelectProvider}>
          {providers.map((p) => <Select.Option key={p.name} value={p.name}>{p.label || p.name}</Select.Option>)}
        </Select>
        <Select value={selectedModel} style={{ width: 240 }} onChange={onSelectModel}>
          {(provider?.models ?? []).map((m) => <Select.Option key={m.name} value={m.name}>{m.label || m.name}</Select.Option>)}
        </Select>
        <Button icon={<PlusOutlined />} onClick={onAddModel}>添加模型</Button>
        <Button icon={<PlusOutlined />} onClick={onAddProvider}>添加 Provider</Button>
      </div>
      <div style={{ marginTop: 8 }}>
        当前模型：{selectedModel}（{selectedProvider}）
      </div>
      <div style={{ marginTop: 4 }}>
        {(current?.capabilities ?? []).map((c) => <Tag key={c}>{c}</Tag>)}
      </div>
    </div>
  );
};
```

**`ModelParams.tsx`**（参数区 + 还原默认确认，8.1.2-#7）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：模型参数区（跟随当前模型+范围+[已修改]+[重置为默认]确认内联）
import React from 'react';
import { Button, InputNumber, Modal, Slider } from 'antd';
import { isDirty } from '../utils/modelUtils';
import { DirtyDot } from './icons';

interface Props {
  params: Record<string, unknown>;
  defaults: Record<string, unknown>;
  ranges: Record<string, { min: number; max: number }>;
  envOverride: Record<string, boolean>;
  onChange: (key: string, value: unknown) => void;
  onReset: () => void;
}

export const ModelParams: React.FC<Props> = ({ params, defaults, ranges, envOverride, onChange, onReset }) => {
  const dirty = isDirty(params, defaults, envOverride);
  const confirmReset = () => {
    Modal.confirm({
      title: '重置为默认',
      content: '按当前模型 schema.default 重置全部参数，脏态清空。继续吗？',
      okText: '重置',
      cancelText: '取消',
      onOk: onReset,
    });
  };
  return (
    <div>
      {Object.keys(defaults).map((key) => {
        const range = ranges[key];
        const value = (params[key] ?? defaults[key]) as number;
        return (
          <div key={key} style={{ display: 'flex', alignItems: 'center', gap: 8, minHeight: 44 }}>
            <span style={{ width: 132 }}>{key}</span>
            {range ? (
              <span style={{ display: 'inline-flex', gap: 8, alignItems: 'center' }}>
                <Slider min={range.min} max={range.max} value={value} style={{ width: 160 }} onChange={(v) => onChange(key, v)} />
                <InputNumber min={range.min} max={range.max} value={value} onChange={(v) => onChange(key, v)} />
              </span>
            ) : (
              <InputNumber value={value} onChange={(v) => onChange(key, v)} />
            )}
            <span style={{ color: '#8c8c8c', fontSize: 12 }}>
              范围{range ? `${range.min}-${range.max}` : '不限'} 默认{String(defaults[key])}
            </span>
            {dirty[key] && <DirtyDot />}
          </div>
        );
      })}
      <Button onClick={confirmReset}>重置为默认</Button>
    </div>
  );
};
```

**`ProviderConfig.tsx`**（配置区，内嵌复用旧 ProviderSettings）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：Provider 配置区（内嵌复用旧 ProviderSettings，不改旧码，见 7.3.2）
import React from 'react';
import { ProviderSettings } from '@/features/settings/components/ProviderSettings';

export const ProviderConfig: React.FC = () => (
  <div>
    <ProviderSettings shouldLoad />
  </div>
);
```

**`ModelActions.tsx`**（CRUD 按钮，6.5-④）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：模型操作区（删除模型/Provider 入口；添加入口在选择器）
import React from 'react';
import { Button } from 'antd';
import { DeleteOutlined } from '@ant-design/icons';

interface Props {
  onDeleteModel: () => void;
  onDeleteProvider: () => void;
}

export const ModelActions: React.FC<Props> = ({ onDeleteModel, onDeleteProvider }) => (
  <div style={{ display: 'flex', gap: 8 }}>
    <Button danger icon={<DeleteOutlined />} onClick={onDeleteModel}>删除此模型</Button>
    <Button danger icon={<DeleteOutlined />} onClick={onDeleteProvider}>删除此 Provider</Button>
  </div>
);
```

**`ModelModals.tsx`**（添加/删除确认弹窗，6.6）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：模型管理弹窗（添加模型/Provider + 删除确认含级联警告）
import React, { useState } from 'react';
import { Form, Input, Modal, Select } from 'antd';
import type { ProviderEntry } from '@/services/api/model.api';

interface Props {
  providers: ProviderEntry[];
  addModelOpen: boolean;
  addProviderOpen: boolean;
  deleteOpen: boolean;
  deleteTarget: string | null;
  selectedProvider: string;
  onCloseAddModel: () => void;
  onCloseAddProvider: () => void;
  onCloseDelete: () => void;
  onSubmitAddModel: (data: { provider: string; model: string; label: string }) => void;
  onSubmitAddProvider: (data: { name: string; label: string; api_base: string }) => void;
  onConfirmDelete: () => void;
}

export const ModelModals: React.FC<Props> = (props) => {
  const { providers, addModelOpen, addProviderOpen, deleteOpen, deleteTarget } = props;
  const [mForm] = Form.useForm();
  const [pForm] = Form.useForm();
  const [mProvider, setMProvider] = useState(props.selectedProvider);
  return (
    <>
      <Modal
        open={addModelOpen}
        title="添加模型"
        onCancel={props.onCloseAddModel}
        onOk={() => mForm.validateFields().then((v) => {
          props.onSubmitAddModel({ provider: mProvider, model: v.model, label: v.label ?? v.model });
          mForm.resetFields();
        })}
        okText="保存"
        cancelText="取消"
      >
        <Form form={mForm} layout="vertical">
          <Form.Item label="Provider" required>
            <Select value={mProvider} onChange={setMProvider}>
              {providers.map((p) => <Select.Option key={p.name} value={p.name}>{p.label || p.name}</Select.Option>)}
            </Select>
          </Form.Item>
          <Form.Item name="model" label="模型名" rules={[{ required: true, message: '请输入模型名' }]}>
            <Input />
          </Form.Item>
          <Form.Item name="label" label="显示名">
            <Input />
          </Form.Item>
        </Form>
      </Modal>
      <Modal
        open={addProviderOpen}
        title="添加 Provider"
        onCancel={props.onCloseAddProvider}
        onOk={() => pForm.validateFields().then((v) => {
          props.onSubmitAddProvider(v);
          pForm.resetFields();
        })}
        okText="保存"
        cancelText="取消"
      >
        <Form form={pForm} layout="vertical">
          <Form.Item name="name" label="名称" rules={[{ required: true, message: '请输入名称' }]}>
            <Input />
          </Form.Item>
          <Form.Item name="label" label="显示名">
            <Input />
          </Form.Item>
          <Form.Item name="api_base" label="API 地址">
            <Input />
          </Form.Item>
        </Form>
      </Modal>
      <Modal
        open={deleteOpen}
        title={`确定要删除 "${deleteTarget ?? ''}"？`}
        onCancel={props.onCloseDelete}
        onOk={props.onConfirmDelete}
        okText="删除"
        okButtonProps={{ danger: true }}
        cancelText="取消"
      >
        警告：如果这是当前使用的模型，将自动切换为默认模型；删除 Provider 将级联删除其下所有模型！
      </Modal>
    </>
  );
};
```

**`SettingsPage.tsx`**（壳：搜索 + Tabs + 脏角标 + 切 Tab 确认 + sticky 保存条，8.1.2-#6）：

```tsx
// 编辑历史: 2026-09-20 小强 - 新建：设置页壳（搜索+Tabs+全局脏角标+切 Tab 未保存确认内联+sticky 保存条，见 7.2/6.4）
import React, { useState } from 'react';
import { Button, Card, Modal, Result, Skeleton, Tabs, message } from 'antd';
import { settingsSpacing } from '@/theme/settingsTokens';
import { useSettings } from '../hooks/useSettings';
import { SettingsGroup } from './SettingsGroup';
import { SearchBox } from './SearchBox';
import { SaveBar } from './SaveBar';
import { DirtyBadge } from './DirtyBadge';
import { SettingIcon } from './icons';
import { ModelSelector } from './ModelSelector';
import { ModelParams } from './ModelParams';
import { ProviderConfig } from './ProviderConfig';
import { ModelActions } from './ModelActions';
import { ModelModals } from './ModelModals';
import { modelApi } from '@/services/api/model.api';
import { handleApiError } from '@/services/error/handler';
import type { TabKey } from '../types';

const TAB_TITLES: Record<TabKey, string> = {
  general: '通用', model: '模型', security: '安全',
  chat: '聊天', appearance: '外观', system: '系统',
};

const SettingsPage: React.FC = () => {
  const s = useSettings();
  const { state } = s;
  const [pendingTab, setPendingTab] = useState<TabKey | null>(null);

  const requestTab = (tab: TabKey) => {
    if (tab !== state.activeTab && s.isGroupDirty(state.activeTab)) {
      setPendingTab(tab);
    } else {
      s.setActiveTab(tab);
      void s.checkMtime();
    }
  };

  const jumpTo = (tab: TabKey, key: string) => {
    s.setActiveTab(tab);
    s.setHighlightKey(key);
  };

  const dangerousDirty = Object.keys(state.dirtyKeys).some(
    (k) => k.includes('confirmDangerousOps') || k.includes('Whitelist') || k.includes('Blacklist') || k.includes('blacklist'),
  );

  if (state.loading) {
    return <div className="settings-page" style={{ padding: settingsSpacing.pagePadding, background: '#fff' }}><Skeleton active /></div>;
  }
  if (state.loadError) {
    return (
      <div className="settings-page" style={{ padding: settingsSpacing.pagePadding, background: '#fff' }}>
        <Result status="error" title="设置加载失败" subTitle={state.loadError} extra={<Button onClick={() => { void s.load(); }}>重试</Button>} />
      </div>
    );
  }

  const renderModelTab = () => (
    <div>
      <div style={{ fontWeight: 600, marginBottom: 8 }}>① 选择器</div>
      <ModelSelector
        providers={state.model.providers}
        selectedProvider={state.model.selectedProvider}
        selectedModel={state.model.selectedModel}
        onSelectProvider={s.selectProvider}
        onSelectModel={s.selectModel}
        onAddModel={() => s.patchModel({ addModelModalOpen: true })}
        onAddProvider={() => s.patchModel({ addProviderModalOpen: true })}
      />
      <div style={{ fontWeight: 600, margin: '12px 0 8px' }}>② 参数区（跟随当前模型）</div>
      <ModelParams
        params={state.model.params}
        defaults={state.model.defaults}
        ranges={state.model.ranges}
        envOverride={state.model.envOverride}
        onChange={s.setParam}
        onReset={s.resetParams}
      />
      <div style={{ fontWeight: 600, margin: '12px 0 8px' }}>③ Provider 配置</div>
      <ProviderConfig />
      <div style={{ fontWeight: 600, margin: '12px 0 8px' }}>④ 操作区</div>
      <ModelActions
        onDeleteModel={() => s.patchModel({ deleteConfirmOpen: true, deleteTarget: `${state.model.selectedProvider}/${state.model.selectedModel}` })}
        onDeleteProvider={() => s.patchModel({ deleteConfirmOpen: true, deleteTarget: `provider:${state.model.selectedProvider}` })}
      />
      <ModelModals
        providers={state.model.providers}
        addModelOpen={state.model.addModelModalOpen}
        addProviderOpen={state.model.addProviderModalOpen}
        deleteOpen={state.model.deleteConfirmOpen}
        deleteTarget={state.model.deleteTarget}
        selectedProvider={state.model.selectedProvider}
        onCloseAddModel={() => s.patchModel({ addModelModalOpen: false })}
        onCloseAddProvider={() => s.patchModel({ addProviderModalOpen: false })}
        onCloseDelete={() => s.patchModel({ deleteConfirmOpen: false, deleteTarget: null })}
        onSubmitAddModel={async (d) => {
          const r = await s.refreshModels();
          if (r) {
            try {
              await modelApi.addModel({ provider: d.provider, model: d.model, label: d.label });
              message.success('模型已添加');
              await s.refreshModels({ provider: d.provider, model: d.model });
            } catch (e) {
              handleApiError(e);
            }
          }
          s.patchModel({ addModelModalOpen: false });
        }}
        onSubmitAddProvider={async (d) => {
          try {
            await modelApi.addProvider(d);
            message.success('Provider 已添加');
            await s.refreshModels();
          } catch (e) {
            handleApiError(e);
          }
          s.patchModel({ addProviderModalOpen: false });
        }}
        onConfirmDelete={async () => {
          const target = state.model.deleteTarget ?? '';
          try {
            if (target.startsWith('provider:')) {
              const r = await modelApi.deleteProvider(target.slice('provider:'.length));
              if (r.switched_to) message.info(`已切换回默认 Provider：${r.switched_to}`);
            } else {
              const [p, m] = target.split('/');
              const r = await modelApi.deleteModel(p, m);
              if (r.switched_to) message.info(`已切换回默认模型：${r.switched_to}`);
            }
            await s.refreshModels();
          } catch (e) {
            handleApiError(e);
          }
          s.patchModel({ deleteConfirmOpen: false, deleteTarget: null });
        }}
      />
    </div>
  );

  return (
    <div className="settings-page" style={{ padding: settingsSpacing.pagePadding, background: '#fff' }}>
      <Card>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}>
          <span>
            <Tabs
              type="line"
              activeKey={state.activeTab}
              onChange={(k) => requestTab(k as TabKey)}
              items={s.GROUP_ORDER.map((g) => ({
                key: g,
                label: <span>{SettingIcon[g]} {TAB_TITLES[g]}</span>,
              }))}
            />
            <DirtyBadge count={s.dirtyCount} />
          </span>
          <SearchBox schema={state.schema} onJump={jumpTo} />
        </div>
        {state.activeTab === 'model' ? (
          renderModelTab()
        ) : (
          <SettingsGroup
            group={state.activeTab}
            items={state.schema[state.activeTab]?.items ?? []}
            values={state.values[state.activeTab] ?? {}}
            sources={state.sources[state.activeTab] ?? {}}
            dirtyKeys={state.dirtyKeys}
            highlightKey={state.highlightKey}
            onChange={s.setValue}
          />
        )}
        <SaveBar
          canSaveGroup={s.isGroupDirty(state.activeTab)}
          canSaveAll={s.dirtyCount > 0}
          dirtyCount={s.dirtyCount}
          saving={s.saving}
          restartKeys={s.restartKeys}
          hasDangerousDirty={dangerousDirty}
          onSaveGroup={() => { void s.saveGroup(state.activeTab); }}
          onSaveAll={() => { void s.saveAll(); }}
          onCloseRestart={() => s.setRestartKeys([])}
        />
      </Card>
      <Modal
        open={pendingTab !== null}
        title="有未保存的修改"
        onCancel={() => setPendingTab(null)}
        onOk={() => {
          if (pendingTab) {
            void s.saveGroup(state.activeTab).then((r) => {
              if ((r as { ok: boolean }).ok) s.setActiveTab(pendingTab);
              setPendingTab(null);
            });
          }
        }}
        okText="保存并切换"
        cancelText="取消"
      >
        目标 Tab 存在未保存项：保存并切换 / 取消停留（放弃修改请手动还原后切换）。
      </Modal>
    </div>
  );
};

export default SettingsPage;
```

### 9.3 修改类（精确 diff，可直接实施）

#### 9.3.1 `backend/app/main.py`（修改：import +2，挂载 +2）

```diff
 from app.api.v1 import health, sessions, messages, metrics
 from app.api.v1.config_routes import router as config_router
+from app.api.v1.settings_routes import router as settings_router  # 设置页参数配置路由 — 小沈 2026-09-20
+from app.api.v1.model_routes import router as model_router  # 设置页模型管理路由 — 小沈 2026-09-20
 from app.api.v1.tool_routes import router as tool_routes_router  # A4: 工具测试路由迁出 health.py — 小欧 2026-08-12
```

```diff
 app.include_router(config_router, prefix="/api/v1", tags=["config"])
+app.include_router(settings_router, prefix="/api/v1", tags=["settings"])  # 设置页 — 小沈 2026-09-20
+app.include_router(model_router, prefix="/api/v1", tags=["models"])  # 设置页模型管理 — 小沈 2026-09-20
 app.include_router(sessions.router, prefix="/api/v1", tags=["sessions"])
```

#### 9.3.2 `backend/app/utils/file_utils.py`（修改：新增 `atomic_write` + `backup_file` 改编号链）

```python
# 在文件末尾追加（保留原 backup_file 签名兼容，suffix 透传；2026-09-20 小沈）：
import tempfile


def atomic_write(file_path: str, content: str, encoding: str = "utf-8") -> None:
    """原子写入文本：写同目录临时文件 → fsync → os.replace 整文件替换，绝不半写。

    同目录临时文件保证 replace 不跨文件系统；异常时清临时文件原样抛出。
    """
    abs_path = os.path.abspath(file_path)
    parent = os.path.dirname(abs_path) or "."
    fd, tmp_path = tempfile.mkstemp(dir=parent, prefix=".tmp_", suffix=".yaml")
    try:
        with os.fdopen(fd, "w", encoding=encoding) as f:
            f.write(content)
            f.flush()
            os.fsync(f.fileno())
        os.replace(tmp_path, abs_path)
    except Exception:
        try:
            if os.path.exists(tmp_path):
                os.remove(tmp_path)
        except OSError:
            pass
        raise


def backup_file(file_path: str, backup_dir: Optional[str] = None, suffix: str = ".backup",
                keep: int = 5) -> Dict[str, Any]:
    """编号备份链：backup.1（最新）~ backup.N（最旧）+ FIFO 清理（9.2/一.7）。

    轮转：删最旧 backup.{keep}，backup.{i}→backup.{i+1} 顺移，新备份=backup.1。
    返回保持原形状 {original_path, backup_path, backup_size}，加 backup_index=1。
    注意：旧调用方 update_config 验证成功后会删本次 backup.1（原有行为不变）；
    设置页路径（_apply_region_patch）保留备份，链满 5 个自动 FIFO。
    """
    file_path = os.path.abspath(file_path)
    if not os.path.exists(file_path):
        raise FileNotFoundError(f"文件不存在: {file_path}")
    file_name = os.path.basename(file_path)
    if backup_dir is None:
        backup_dir = os.path.dirname(file_path)
    else:
        backup_dir = os.path.abspath(backup_dir)
        os.makedirs(backup_dir, exist_ok=True)
    for i in range(keep, 1, -1):
        older = os.path.join(backup_dir, f"{file_name}{suffix}.{i}")
        newer_src = os.path.join(backup_dir, f"{file_name}{suffix}.{i - 1}")
        if os.path.exists(older):
            os.remove(older)
        if os.path.exists(newer_src):
            os.rename(newer_src, older)
    backup_path = os.path.join(backup_dir, f"{file_name}{suffix}.1")
    shutil.copy2(file_path, backup_path)
    return {
        "original_path": file_path,
        "backup_path": backup_path,
        "backup_size": os.path.getsize(backup_path),
        "backup_index": 1,
    }
```

> 原 `backup_file` 函数体（时间戳单备份）整体替换为上；`remove_readonly` 不动；文件顶部已有 `os/shutil/typing` import，只需加 `import tempfile`。

#### 9.3.3 `backend/app/services/model/config_helpers.py`（修改：`write_yaml_config` 改调 `atomic_write`）

```diff
 def write_yaml_config(config_path: str, data: dict) -> None:
-    """使用有序 Key 写入 YAML 配置文件"""
-    _write_system_yaml(config_path, data)
+    """使用有序 Key 写入 YAML 配置文件 — 2026-09-20 小沈: 经 atomic_write 原子落盘(9.2)"""
+    import yaml as _yaml
+    from app.utils.file_utils import atomic_write
+    from collections import OrderedDict as _OD  # _write_system_yaml 内 _order 逻辑保持不变
+    ordered = _order_for_dump(data)
+    atomic_write(config_path, _yaml.dump(ordered, allow_unicode=True, default_flow_style=False, indent=2))
```

> 说明：`_write_system_yaml` 内部 `_order(d)` 排序逻辑抽为模块级 `_order_for_dump(d)`（原函数体不动，只把内嵌 `_order` 上提改名），`yaml.add_representer(OrderedDict, ...)` 注册保留在模块 import 时执行一次。

#### 9.3.4 `frontend/src/services/api/index.ts`（修改：+2 行导出）

```diff
 // 编辑历史: 2026-08-28 小欧 - 阶段三api拆分后补充统一导出桶,供测试与业务按目录导入(非backward,纯前向聚合) - 小欧-2026-08-28
 export * from './chat.api';
 export * from './config.api';
+export * from './settings.api';  // 2026-09-20 小强 - 设置页参数配置 API
+export * from './model.api';  // 2026-09-20 小强 - 设置页模型管理 API
 export * from './task.api';
 export * from './session.api';
 export * from './health.api';
```

#### 9.3.5 `frontend/src/services/error/handler.ts`（修改：+3 枚举与映射）

```diff
   // 操作错误
   SAVE_FAILED = 'save_failed',
   LOAD_FAILED = 'load_failed',
+  SETTINGS_SCHEMA_FAILED = 'settings_schema_failed',  // 2026-09-20 小强 - 设置 Schema 拉取失败
+  SETTINGS_SAVE_FAILED = 'settings_save_failed',  // 2026-09-20 小强 - 设置保存失败
+  MODEL_MANAGEMENT_FAILED = 'model_management_failed',  // 2026-09-20 小强 - 模型管理 CRUD 失败
```

```diff
   // 操作错误
   [ErrorType.SAVE_FAILED]: {
     retryable: true,
     maxRetries: 3,
     retryDelay: 1000,
     message: '保存失败，请重试',
     severity: 'warning',
   },
+  [ErrorType.SETTINGS_SCHEMA_FAILED]: {
+    retryable: true,
+    maxRetries: 2,
+    retryDelay: 1000,
+    message: '设置结构加载失败，请重试',
+    severity: 'warning',
+  },
+  [ErrorType.SETTINGS_SAVE_FAILED]: {
+    retryable: true,
+    maxRetries: 2,
+    retryDelay: 1000,
+    message: '设置保存失败，请重试',
+    severity: 'warning',
+  },
+  [ErrorType.MODEL_MANAGEMENT_FAILED]: {
+    retryable: true,
+    maxRetries: 2,
+    retryDelay: 1000,
+    message: '模型管理操作失败，请重试',
+    severity: 'warning',
+  },
```

#### 9.3.6 `frontend/src/App.tsx`（修改：+1 懒加载 +1 路由）

```diff
 const HistoryPage = lazy(() => import('./pages/History'));
 const Settings = lazy(() => import('./pages/Settings'));
+const Settings2 = lazy(() => import('./pages/Settings2'));  // 2026-09-20 小强 - 设置2版（6 Tab + 模型管理）
```

```diff
           <Route path="/" element={<ChatPage />} />
           <Route path="/history" element={<HistoryPage />} />
           <Route path="/settings" element={<Settings />} />
+          <Route path="/settings2" element={<Settings2 />} />
           <Route path="*" element={<ChatPage />} />
```

#### 9.3.7 `frontend/src/components/Layout/index.tsx`（修改：菜单项改名+改指，1 项 +1 case）

```diff
     { type: 'divider' },
     {
-      key: '/settings',
+      key: '/settings2',
       icon: <SettingOutlined />,
-      title: '系统设置',
-      label: '系统设置',
+      title: '设置2版',
+      label: '设置2版',
     },
   ];
```

```diff
       case '/history':
         // 跳转到历史记录
         navigate('/history');
         break;
-      case '/settings':
-        // 跳转到设置
-        navigate('/settings');
+      case '/settings2':
+        // 跳转到设置2版 — 小强 2026-09-20
+        navigate('/settings2');
         break;
```

#### 9.3.8 `backend/FUNCTIONS.md`（修改：登记 2 个编排函数）

```markdown
<!-- 在模型/配置域函数清单追加（2026-09-20 小沈）： -->
- `app.services.settings.settings_service.get_all_groups/get_group/get_setting/update_settings` — 设置页 6 组独立读+写（前门），旧键走 `config_service.update_config`，新键走 `_apply_region_patch`（同套备份+原子写+回滚）
- `app.services.settings.settings_service._apply_region_patch` — 通用 region 合并写（model_service 复用，DRY 单点）
- `app.services.model.model_service.get_models/get_providers/add_model/update_model/delete_model/add_provider/update_provider_config/delete_provider` — 模型编排器（model 域），current 双写同步 + 级联 + switched_to
```

---

## 十、验收表（逐项断言，全过才算完）

### 10.1 后端接口验收

| 接口 | 断言 |
|------|------|
| `GET /settings/schema` | 6 分组齐全；key 全局唯一；readonly 三项标全（app.theme/config_path/version）；secret 项 schema 无值字段 |
| `GET /settings`（全量） | 6 组 data+sources 齐全；secret 值无明文（只返 configured+suffix）；env 接管项 sources=env 且值=生效值；version 非空；mtime > 0；model 组 data 为空（由 GET /models 承接） |
| `GET /settings?group=X` | data 与 config.yaml 生效值一致；env 接管项 sources=env 且值=生效值；group=model 时 data 为空 |
| `PUT 新 key（chat/appearance 等）` | 新键走通用 region 合并（同套备份+原子写+回滚，不走 update_config）；备份文件生成；非法值拒绝且 YAML 原样；回滚可用 |
| `PUT 旧 key` | 旧逻辑不变（config_service 原有路径 update_config） |
| `PUT secret`（`PUT /providers/{name}` api_key） | value 非空更新 / 缺省保持 / clear:true 清空，三态各断言一次；GET 永不返明文 |
| `PUT env 接管项` | 拒绝写入 + warnings 提示，其他项不受影响 |
| 重复 key | registry 加载直接拒启 |
| `GET /models` | 返回完整 providers/models 层级 + current_model_ref |
| `POST /models` | 校验 provider 存在 + model 名唯一 → 写入 config.yaml → 返回更新后列表 |
| `DELETE /models`（删除当前模型） | 自动切换回默认模型 → 返回 `switched_to` |
| `DELETE /models`（删除非当前模型） | 正常删除 → 返回 `ok` |
| `POST /providers` | 校验 name 唯一 → 写入 config.yaml → 返回 ok + provider；前端重新 getProviders 刷新列表 |
| `DELETE /providers`（删除当前 Provider） | 级联删除所有子模型 → 切换回默认 Provider → 返回 `switched_to` |
| `PUT /providers/{name}` | 更新 Provider 配置（api_key/base_url/timeout）→ 返回 ok；立即生效见 10.3 |
| 模型管理写保护 | 旧键写走 `config_service.update_config`，新键（chat/appearance/logging/model 区域）写走通用 region 合并（同套备份+原子写+回滚机制）；读取独立为代码审查项（settings_service/model_service 不 import config_service 读取逻辑） |
| 重复 model 名 | provider 内 model 名唯一校验，后端拒写 + toast"同名模型已存在" |
| 重复 provider 名 | provider 名唯一校验，后端拒写 + toast"同名 Provider 已存在" |

### 10.2 前端逐 Tab 验收

| Tab | 断言 |
|-----|------|
| 通用 | 2 项读写落 YAML；重启项提示 Modal 出现 |
| 模型 | ①选择器：双下拉联动（切Provider→Model重置）；②参数区：跟随当前模型+范围+脏态标记；③配置区：Provider配置编辑保存成功且无重启提示（立即生效见 10.3）；④操作区：添加/删除模型/Provider弹窗+确认；删除当前模型自动切换默认模型；删除Provider级联删除所有子模型 |
| 安全 | 10 项读写；危险项二次确认出现 |
| 聊天 | 6 项落 YAML；页面明确展示"待接入"，无"已生效"承诺语 |
| 外观 | theme 只读；字号/密度本地即时 + 落 YAML 对齐 |
| 系统 | 8 项读写 + 2 只读（5系统参数+3运维日志读写，关于2项只读）；只读项复制可用不提交；内部三小节视觉分隔 |

### 10.3 模型管理验收

| 场景 | 断言 |
|------|------|
| 添加模型 | POST /models → config.yaml 写入 → 列表刷新 → 选中新模型 |
| 删除当前模型 | 自动切换回默认模型 → 参数区刷新 → toast 提示 |
| 删除非当前模型 | 正常删除 → 列表刷新 |
| 添加重复模型 | 后端校验 provider 内唯一，拒写 + toast"同名模型已存在" |
| 删除 Provider | 级联删除所有子模型 → 切换回默认 Provider |
| 删除当前 Provider | 级联删除 → 切换回默认 Provider → toast 提示 |
| 修改 Provider 配置 | PUT /providers → 下次 LLM 调用立即使用新配置 |
| 切换模型（参数保留） | 已修改参数保留值 + [已修改] 标记 |
| 切换模型（参数恢复） | 未修改参数恢复为新模型默认值 |
| 切换模型（范围截断） | 超出新模型范围的值自动截断 + toast 提示 |
| 模型 CRUD 写入 | 通用 region 合并：备份 → 原子重命名落盘 → 验证 → 失败回滚（与 update_config 同套机制） |

### 10.4 联调与回滚

1. 写配置 → `config.yaml` 落盘 + 备份生成；非法值 → 拒写 + 原文件字节一致。
2. 重启后端 → 重启项生效，sources 标注一致。
3. 并发：新旧页同时改同一 provider → 后写串行胜出，无 YAML 损坏、无静默丢项。
4. 回滚：实现有问题 → 摘导航入口即回旧页（旧码零改动，天然可回）。
5. 模型管理回滚：model_service 写入失败 → 写入前备份 → 回滚备份 → 列表不刷新。

---

## 十一、v1.x/v2.0 缺陷审查记录（推翻依据，存档）

---

**编写人：小欧（文档） | 小沈（后端架构） | 小强（前端架构 / 设置页 UI）**
**编写时间：2026-09-20 00:10:45**
**更新时间：2026-09-20 01:24:08（第十章验收排错 v4.10 - 小欧）**
