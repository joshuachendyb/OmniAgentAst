# 全链路端到端集成测试方案（Full-Link E2E Integration Test）

**创建时间**: 2026-05-23 01:00:44  
**版本**: v1.0  
**编写人**: 小健

---

## 版本记录

| 版本 | 时间 | 签名 | 更新内容 |
|------|------|------|---------|
| v1.0 | 2026-05-23 01:00:44 | 小健 | 初始版本 |
| v1.1 | 2026-06-08 18:50:00 | 小欧 | 修复P0章节、统一测试数量、去掉Ollama、补充P7-P10、新增第九章辅助集成测试（Mock LLM/工具/SSE） |

---

## 一、测试定义

### 1.1 测试名称

**全链路端到端集成测试**（Full-Link End-to-End Integration Test）

### 1.2 为什么叫这个名

| 关键词 | 含义 | 在这个测试中的体现 |
|--------|------|-------------------|
| 全链路 | 覆盖系统所有组件 | 前端API→后端→Agent→LLM→工具执行→数据库→SSE响应 |
| 端到端 | 从用户发消息到收到完整结果 | POST /chat → SSE事件流 → final事件 |
| 集成 | 验证各组件对接是否正确 | API路由、SSE封装、Agent循环、LLM调用、工具执行、数据库写入 |

### 1.3 与现有测试的区别

| 测试类型 | 测试什么 | 用什么驱动 | 能发现什么问题 |
|---------|---------|-----------|--------------|
| 单元测试（现有~200个） | 单个工具函数 | Mock/Patch | 函数级bug |
| MessageBuilder测试（43个） | Prompt构建逻辑 | Mock | Prompt构建bug |
| **全链路E2E（本方案）** | **Agent行为+系统集成** | **真实后端（读取config.yaml的LLM配置）+ 真实SQLite** | **死循环、工具选错、参数传错、会话污染、日志异常、数据库异常** |

### 1.4 测试目标

**唯一目标**：用真实LLM（后端从config.yaml读取配置）模拟真实用户操作，把系统中隐藏的问题炸出来。

**具体来说**：
1. Agent在真实LLM下能否正确完成用户请求
2. Agent在异常情况下（LLM返回垃圾/空内容/错误工具）能否容错
3. 系统各组件的串联是否完整（API→Agent→LLM→工具→DB）
4. 日志和数据库记录是否完整准确

### 1.5 核心测试原则（铁律）

**所有测试统一基于真实后端运行，禁止Mock：**

| 环节 | 要求 | 禁止 |
|------|------|------|
| **后端** | 真实uvicorn启动，从config.yaml读取LLM配置 | ❌ 不启动后端、Mock后端 |
| **LLM** | 真实调用config.yaml中配置的provider/model | ❌ Mock LLM返回、假响应 |
| **工具** | 真实执行文件操作、命令、网络请求等 | ❌ Mock工具返回值 |
| **数据库** | 真实读写SQLite（~/.omniagent/chat_history.db） | ❌ Mock数据库、内存数据库 |
| **SSE** | 真实解析后端返回的SSE事件流 | ❌ 假造SSE事件 |

**一句话：测试输入就是用户会说的话，测试过程就是真实系统的完整运行。**

---

## 二、测试架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      测试脚本（pytest）                          │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    测试执行引擎                            │   │
│  │  ① 初始化测试环境                                         │   │
│  │  ② 通过httpx发送POST /chat → 接收SSE事件流               │   │
│  │  ③ 逐事件解析、记录、断言                                  │   │
│  │  ④ 查询日志文件，检查ERROR/WARNING                        │   │
│  │  ⑤ 查询SQLite数据库，检查session/messages/steps          │   │
│  │  ⑥ 清理测试残留数据                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│         ↓ HTTP POST /chat           ↓ SSE event stream          │
│    ┌──────────────────────────────────────────────────┐         │
│    │              后端服务（uvicorn）                   │         │
│    │  main.py → chat_router.py → Agent → LLM → 工具   │         │
│    │        ↑                                         │         │
│    │   从 config.yaml 读取 LLM provider/model        │         │
│    │                          ↓                        │         │
│    │                    SQLite DB / 日志文件             │         │
│    └──────────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、完整测试流程

每个测试用例都严格按照以下步骤执行。

### 3.1 前置条件

| 条件 | 要求 | 如何检查 |
|------|------|---------|
| 后端已启动 | uvicorn运行中，监听8000 | `curl http://localhost:8000/docs` 返回200 |
| LLM配置就绪 | config.yaml中ai.provider已配置有效provider | 后端启动日志无LLM连接错误 |
| 测试数据就绪 | 测试目录、测试文件已创建 | `Test-Path` 检查路径存在 |
| 日志目录就绪 | `backend/logs/prompt-logs/`可写 | `Test-Path` 检查目录 |
| 数据库路径就绪 | `~/.omniagent/chat_history.db`可写 | 确认无权限问题 |

### 3.2 测试执行（核心流程）

```
步骤 1: 记录测试起始状态
─────────────────────────────────────────────────────────────────
  · 记录当前时间 T0
  · 记录数据库当前会话数（base count）
  · 记录日志文件当前大小

步骤 2: 发送用户请求
─────────────────────────────────────────────────────────────────
  · 用 httpx 发送 POST /chat
  · 请求体：{"messages": [{"role": "user", "content": "用户输入"}]}
  · 设置 stream=True 以接收 SSE
  · 记录请求时间 T1

步骤 3: 接收并解析 SSE 事件流
─────────────────────────────────────────────────────────────────
  · 逐行读取 SSE 响应
  · 按 event: xxx 和 data: xxx 解析每个事件
  · 记录每个事件的：
    - 事件类型（start/thought/action/observation/chunk/final/error/interrupted）
    - 时间戳
    - step编号
    - data内容（工具名、参数、结果等）
  · 持续接收直到 final 事件或超时
  · 记录接收完成时间 T2

步骤 4: 验证事件流正确性
─────────────────────────────────────────────────────────────────
  · [必须] 最终收到了 final 事件 → 正常结束
  · [必须] 没有收到 error 事件 → 没有异常中断
  · [必须] 事件总数在合理范围内（如 < 50个） → 没有死循环
  · [警告] 如果事件总数 > 20，可能存在空转/无意义循环
  · [检查] final 事件中是否包含结果内容
  · [检查] 事件顺序是否正确（start→thought→action→observation→...→final）

步骤 5: 验证数据库记录
─────────────────────────────────────────────────────────────────
  · 查询 chat_history.db 的 sessions 表
    - 对应当前测试的 session 记录是否存在
    - session 的开始时间、结束时间、状态是否正常
  · 查询 messages 表
    - 对应的消息记录是否存在
    - user message 和 assistant response 是否都保存了
  · 查询 execution_steps 表
    - 步骤数量与 SSE 事件流中的 action 数量是否一致
    - 每个 step 的 type、tool、status 是否正常

步骤 6: 验证日志文件
─────────────────────────────────────────────────────────────────
  · 扫描后端日志文件
    - 是否有 ERROR 级别日志（有则失败）
    - 是否有异常 traceback（有则失败）
    - 记录 WARNING 级别数量（过多则标记）
  · 扫描 prompt-logs/
    - 检查本次请求的 LLM 调用日志是否已保存
    - 检查 LLM 调用次数（过多则可能死循环）

步骤 7: 记录测试完成状态
─────────────────────────────────────────────────────────────────
  · 记录测试总耗时 T2 - T1
  · 记录 LLM 调用次数
  · 记录 SSE 事件总数
  · 记录最终结论（通过/失败/警告）
```

### 3.3 验证标准总表

| 验证点 | 通过标准 | 失败标准 | 验证来源 |
|--------|---------|---------|---------|
| 最终事件 | 收到 final | 未收到 final（超时或中断） | SSE事件流 |
| 错误事件 | 无 error 事件 | 任意 error 事件 | SSE事件流 |
| step数量 | < 50 | >= 50（可能死循环） | SSE事件流 |
| LLM调用次数 | < 10 | >= 10（可能死循环） | prompt-logs / 事件计数 |
| 数据库记录 | session + messages + steps 完整 | 缺少任意记录 | SQLite DB |
| 日志级别 | 无 ERROR | 有 ERROR | 后端日志文件 |
| 异常traceback | 无 | 有 | 后端日志文件 |
| 响应时间 | 单步请求<120s，多步请求<600s | 超时 | 计时 |
| 执行工具 | 与预期一致 | 调用了不相关的工具 | SSE action事件 |

### 3.4 后置清理

| 操作 | 说明 |
|------|------|
| 清理测试数据库记录 | 删除本次测试产生的session记录 |
| 清理测试文件 | 删除测试过程中创建的文件 |
| 恢复被修改的配置 | 还原 config.yaml 等 |
| 记录测试报告 | 写入测试结果到报告文件 |

### 3.5 测试环境隔离

**数据隔离策略**：
- 测试使用独立的session_id前缀 `e2e_test_`，便于识别和清理
- 测试产生的文件统一放在 `backend/tests/e2e_artifacts/` 目录下
- 测试结束后自动清理该目录下的所有文件

**数据库隔离**：
- 不使用独立数据库，直接操作生产库 `~/.omniagent/chat_history.db`
- 通过session_id前缀区分测试数据和生产数据
- 每轮测试结束后清理 `session_id LIKE 'e2e_test_%'` 的记录

**配置隔离**：
- 不修改 config.yaml，后端使用当前配置运行
- 如需特殊LLM配置，通过环境变量或测试fixture覆盖

**并发安全**：
- E2E-ERR-06（并发3个请求）使用 `asyncio.gather` 模拟协程并发
- 每个并发请求使用独立session_id，避免状态干扰
- 真正的多进程并发不在本方案范围内

---

## 四、测试场景清单（共58个工具全覆盖）

**设计原则：**
1. 每个测试必须至少覆盖1个工具，所有58个工具至少被覆盖1次
2. 单工具任务（P1-P3）验证每个工具在真实LLM下能否被正确识别和调用
3. 复合任务（P4-P5）验证多工具串联、多步推理能力
4. 异常任务（P6）验证系统容错和恢复能力
5. 覆盖率追踪：每节末尾标注已覆盖工具数和累计覆盖率

---

### 4.1 P0：核心链路验证（4个任务）

覆盖完整数据链路 `API → Agent → LLM → 工具 → DB → SSE`，每个环节至少一个测试。

| 编号 | 任务 | 用户输入 | 验证的链路环节 | 预期行为 |
|------|------|---------|--------------|---------|
| E2E-P0-01 | API→SSE通路 | `"hello"` | API层 + SSE封装 | POST /chat返回200，SSE事件流格式正确，收到final事件 |
| E2E-P0-02 | Agent→工具调用 | `"在D盘创建一个test.txt，内容为hello"` | Agent决策 + 工具执行 | Agent识别意图，调用write_text_file，参数正确，返回成功 |
| E2E-P0-03 | LLM→推理链路 | `"读取D盘test.txt的内容，然后告诉我里面写了什么"` | LLM推理 + 多步串联 | LLM正确拆解为两步（读文件→回复内容），两步都执行成功 |
| E2E-P0-04 | DB→持久化 | 发送任意请求后查询数据库 | 数据库写入完整性 | sessions表有记录，messages表有user+assistant，execution_steps表有步骤记录 |

---

### 4.2 P1：FILE工具全覆盖（11个工具 → 8个任务）

| 编号 | 任务 | 用户输入 | 预期调用的工具 | 覆盖工具 |
|------|------|---------|--------------|---------|
| E2E-F-01 | 创建文件 | `"在D盘创建一个test.txt，内容为hello world"` | write_text_file | write_text_file |
| E2E-F-02 | 读取文件 | `"读取test.txt的内容"` | read_file | read_file |
| E2E-F-03 | 搜索内容 | `"在test.txt中搜索hello"` | grep_file_content | grep_file_content |
| E2E-F-04 | 编辑文件 | `"把test.txt中的hello改成你好"` | edit_file | edit_file |
| E2E-F-05 | 重命名+复制 | `"把test.txt改名为test_old.txt，然后复制到backup目录"` | rename_file + file_operation(copy) | rename_file, file_operation |
| E2E-F-06 | 压缩+配置 | `"把test_old.txt压缩成test.zip，再创建一个config.json写入{'ver':'1'}"` | archive_tool + data_file_format | archive_tool, data_file_format |
| E2E-F-07 | 搜索文件+读媒体 | `"搜索当前目录所有.png图片，查看第一个"` | search_files + read_media_file | search_files, read_media_file |
| E2E-F-08 | 列出目录结构 | `"列出当前目录的完整结构，看看有哪些子目录"` | list_directory | list_directory |

**P1覆盖率：FILE 11/11 ✅ 100%**

---

### 4.3 P2：SHELL + NETWORK工具全覆盖（9个工具 → 7个任务）

| 编号 | 任务 | 用户输入 | 预期调用的工具 | 覆盖工具 |
|------|------|---------|--------------|---------|
| E2E-SN-01 | 执行命令 | `"执行dir命令查看当前目录"` | execute_shell_command | execute_shell_command |
| E2E-SN-02 | 查找命令 | `"查找python命令的路径在哪"` | find_command | find_command |
| E2E-SN-03 | 执行代码 | `"用python输出1到10的平方"` | execute_code(python) | execute_code |
| E2E-SN-04 | 后台会话 | `"启动一个后台ping，3秒后查看输出"` | shell_session | shell_session |
| E2E-SN-05 | 搜索网络 | `"帮我搜索2026年世界杯相关信息"` | search_web | search_web |
| E2E-SN-06 | 获取网页 | `"获取百度首页的内容"` | fetch_webpage | fetch_webpage |
| E2E-SN-07 | HTTP请求+诊断 | `"检查百度能否访问，再请求百度首页"` | network_diagnose + http_request | network_diagnose, http_request |

**P2覆盖率：FILE 11/11 + SHELL 4/4 + NETWORK 5/5 = 20/20 ✅ 100%**

---

### 4.4 P3：SYSTEM + DESKTOP工具全覆盖（19个工具 → 13个任务）

| 编号 | 任务 | 用户输入 | 预期调用的工具 | 覆盖工具 |
|------|------|---------|--------------|---------|
| E2E-SY-01 | 系统信息 | `"查看当前系统的CPU和内存使用情况"` | get_system_info | get_system_info |
| E2E-SY-02 | 进程列表 | `"列出当前所有运行的进程"` | list_processes | list_processes |
| E2E-SY-03 | 网络连接 | `"查看当前系统的网络连接状态"` | net_connections | net_connections |
| E2E-SY-04 | 环境变量 | `"查看PATH环境变量的值"` | get_env | get_env |
| E2E-SY-05 | 注册表 | `"读取注册表HKCU\\Control Panel\\Desktop的ScreenSaveTimeOut值"` | registry_control(read) | registry_control |
| E2E-SY-06 | 系统事件 | `"查看最近10条系统日志"` | event_log | event_log |
| E2E-SY-07 | 窗口信息 | `"查看当前打开了哪些窗口"` | window_info | window_info |
| E2E-SY-08 | 截屏 | `"截取当前屏幕"` | screen_capture | screen_capture |
| E2E-SY-09 | 剪贴板 | `"查看剪贴板当前的内容是什么"` | clipboard_control(read) | clipboard_control |
| E2E-SY-10 | 发送通知 | `"发送一条系统通知，内容为'测试完成'"` | send_notification | send_notification |
| E2E-SY-11 | 窗口控制 | `"把记事本窗口最小化"` | window_control | window_control |
| E2E-SY-12 | 鼠标+键盘 | `"在屏幕上(100,200)位置点击鼠标左键"` | mouse_control + keyboard_control | mouse_control, keyboard_control |
| E2E-SY-13 | 截图OCR | `"截取屏幕后识别上面的文字"` | screen_capture + ocr | ocr |

**⚠️ 有副作用的SYSTEM工具（不能直接跑E2E，需Mock或条件跳过）：**
- kill_process → 危险，需限定测试进程
- service_control → 需要管理员权限
- task_control → 需要管理员权限
- set_env → 修改系统环境变量
- screen_record → 依赖mss等外部库
- 以上工具在E2E测试中标记为`skip_if_no_admin`或`skip_on_ci`

**P3覆盖率：FILE 11/11 + SHELL 4/4 + NETWORK 5/5 + SYSTEM 10/10 + DESKTOP 9/9 = 39/39 ✅ 100%**

---

### 4.5 P4：DOCUMENT + META工具全覆盖（19个工具 → 14个任务）

| 编号 | 任务 | 用户输入 | 预期调用的工具 | 覆盖工具 |
|------|------|---------|--------------|---------|
| E2E-DM-01 | 读文档 | `"读取test.docx的内容"` | read_document | read_document |
| E2E-DM-02 | 写文档 | `"创建一个Word文档，内容为'测试报告'"` | write_document | write_document |
| E2E-DM-03 | 查询数据库 | `"查询chat_history.db中的所有表名"` | get_db_schema | get_db_schema |
| E2E-DM-04 | 执行SQL查询 | `"查询sessions表的前5条记录"` | query_sql | query_sql |
| E2E-DM-05 | 分析数据 | `"分析当前目录下data.csv的数据统计信息"` | analyze_data | analyze_data |
| E2E-DM-06 | 筛选数据 | `"从data.csv中筛选出2026年的所有记录"` | filter_data | filter_data |
| E2E-DM-07 | 生成图表 | `"把data.csv的数据生成一张柱状图"` | generate_chart | generate_chart |
| E2E-DM-08 | 当前时间 | `"现在是几点"` | get_time | get_time |
| E2E-DM-09 | 时间加减 | `"三天后是几号"` | time_add | time_add |
| E2E-DM-10 | 日期差 | `"从2026-01-01到今天一共多少天"` | time_diff | time_diff |
| E2E-DM-11 | 日历查询 | `"今天是不是工作日"` | query_calendar | query_calendar |
| E2E-DM-12 | 时区转换 | `"把当前时间转换成UTC时间"` | timezone_convert | timezone_convert |
| E2E-DM-13 | 工具查询 | `"帮我查一下有哪些文件操作工具"` | tool_search + tool_help | tool_search, tool_help |
| E2E-DM-14 | 批量处理 | `"把当前目录所有.txt文件批量复制到backup目录"` | batch_process | batch_process |

**⚠️ 有副作用的DOCUMENT/META工具：**
- execute_sql → DDL/DML操作，需限定只测SELECT
- convert_document → 依赖LibreOffice外部安装
- timer → 异步回调，需特殊处理
- 以上工具在E2E测试中标记特殊处理

**P4覆盖率：P1-P3已覆盖39个 + DOCUMENT 9/9 + META 9/10 = 57/58 ✅ 98.3%**
**未覆盖：timer(异步回调，需专项测试)。execute_sql(写)在P6异常测试中覆盖错误路径，convert_document因依赖外部库标记跳过。**

---

### 4.6 P5：复合多步任务（多tool、多类型、多步骤，12个任务）

这些任务的目的是验证Agent能否在多步推理中正确串联多个工具，以及跨分类调用。

#### 4.6.1 同分类复合任务（6个）

| 编号 | 任务 | 用户输入 | 预期调用链 | 覆盖工具数 |
|------|------|---------|-----------|-----------|
| E2E-MC-01 | 文件全流程 | `"在D盘创建test_report.txt，写入'测试数据'，读取它，然后搜索'test'"` | write_text_file → read_file → grep_file_content | 3 |
| E2E-MC-02 | 文件管理链 | `"把test_report.txt改名为report.txt，复制到backup，压缩成zip"` | rename_file → file_operation(copy) → archive_tool | 3 |
| E2E-MC-03 | Shell管理链 | `"查找notepad路径，启动记事本，查看后台会话状态"` | find_command → execute_shell_command → shell_session | 3 |
| E2E-MC-04 | 网络查询链 | `"搜索今天新闻，打开第一个结果看看内容"` | search_web → fetch_webpage | 2 |
| E2E-MC-05 | 系统诊断链 | `"查看系统信息，列出进程，检查网络连接"` | get_system_info → list_processes → net_connections | 3 |
| E2E-MC-06 | 桌面操作链 | `"截屏，识别文字，复制到剪贴板"` | screen_capture → ocr → clipboard_control(write) | 3 |

#### 4.6.2 跨分类复合任务（6个）

| 编号 | 任务 | 用户输入 | 预期调用链（跨分类） | 覆盖分类 |
|------|------|---------|-------------------|---------|
| E2E-XC-01 | 时间戳文件 | `"获取当前时间，创建一个以时间为文件名的文件，写入系统信息"` | get_time(META) → write_text_file(FILE) | META + FILE |
| E2E-XC-02 | 网页存文件 | `"搜索今天天气，把结果保存到weather.txt"` | search_web(NETWORK) → write_text_file(FILE) | NETWORK + FILE |
| E2E-XC-03 | 系统报告 | `"获取系统信息，生成一份系统报告保存到sys_report.txt"` | get_system_info(SYSTEM) → write_text_file(FILE) | SYSTEM + FILE |
| E2E-XC-04 | 搜索+通知 | `"搜索2026年AI最新进展，发送通知告知结果概要"` | search_web(NETWORK) → send_notification(DESKTOP) | NETWORK + DESKTOP |
| E2E-XC-05 | 数据库查询存文件 | `"查询chat_history.db的sessions表，把结果保存到sessions_export.txt"` | query_sql(DOCUMENT) → write_text_file(FILE) | DOCUMENT + FILE |
| E2E-XC-06 | 综合报告 | `"获取当前时间、系统信息、目录列表，生成一份综合报告保存到report.txt"` | get_time(META) → get_system_info(SYSTEM) → list_directory(FILE) → write_text_file(FILE) | META + SYSTEM + FILE |

**P5覆盖率：多步推理场景，验证工具串联和跨分类调用能力。工具覆盖与P1-P4有重叠，不重复计入。**

---

### 4.7 P6：异常与容错场景（系统健壮性验证，10个任务）

| 编号 | 场景 | 方法 | 预期行为 | 覆盖的风险点 |
|------|------|------|---------|-------------|
| E2E-ERR-01 | 路径不存在 | 请求读`C:\no_such_file_xxx.txt` | Agent报错后finish，不崩溃 | 文件不存在容错 |
| E2E-ERR-02 | 目录不存在 | 请求列出`Z:\nonexistent\` | Agent报错后finish | 目录不存在容错 |
| E2E-ERR-03 | 无效域名 | 请求访问`http://this-domain-does-not-exist-12345.com` | Agent报错后finish | 网络不可达容错 |
| E2E-ERR-04 | 无效SQL | 请求执行`SELECT * FROM non_existent_table` | Agent报错后finish | SQL错误容错 |
| E2E-ERR-05 | 连续5次不同请求 | 按顺序发5个不同请求 | 每次独立完成，不被前一次污染 | session状态隔离 |
| E2E-ERR-06 | 并发3个请求 | 用 `asyncio.gather` 同时发3个不同请求 | 各自正常结束，互不干扰 | 协程并发安全 |
| E2E-ERR-07 | 长输入 | 输入2000字长文本 | Agent能处理，不OOM，正常finish | 输入长度容错 |
| E2E-ERR-08 | 快速连续相同请求 | 连续2次发同样请求 | 第2次结果合理（可缓存可不同） | 重复请求处理 |
| E2E-ERR-09 | 中途关闭LLM服务 | 请求执行中停掉LLM服务（或断网） | Agent检测到连接错误，优雅finish | LLM服务中断 |
| E2E-ERR-10 | 恢复后继续 | LLM服务恢复后重新发请求 | 能正常完成 | 服务恢复能力 |

---

### 4.8 P7：任务生命周期管理（8个任务）

验证任务的取消、暂停、恢复、确认等控制能力。

| 编号 | 任务 | 操作方式 | 预期行为 |
|------|------|---------|---------|
| E2E-LC-01 | 取消正在执行的任务 | 发送 `"执行dir命令并等待10秒"` → 拿到task_id → 调用 `POST /chat/stream/cancel/{task_id}` | 任务被取消，SSE收到cancelled事件，不再继续执行 |
| E2E-LC-02 | 暂停正在执行的任务 | 发送长任务 → 拿到task_id → 调用 `POST /chat/stream/pause/{task_id}` | 任务暂停，SSE收到paused事件，暂停期间不执行下一步 |
| E2E-LC-03 | 恢复暂停的任务 | 执行E2E-LC-02后 → 调用 `POST /chat/stream/resume/{task_id}` | 任务恢复执行，继续之前暂停的步骤 |
| E2E-LC-04 | 取消不存在的任务 | 调用 `POST /chat/stream/cancel/nonexistent-id` | 返回错误提示（任务不存在） |
| E2E-LC-05 | 暂停已取消的任务 | 先取消任务 → 再尝试暂停 | 返回错误提示（任务已取消） |
| E2E-LC-06 | 恢复未暂停的任务 | 发送任务 → 不暂停 → 直接恢复 | 返回错误提示（任务未暂停） |
| E2E-LC-07 | 恢复已取消的任务 | 先取消任务 → 再尝试恢复 | 返回错误提示（任务已取消） |
| E2E-LC-08 | 配置验证 | 调用 `GET /chat/validate` | 返回当前LLM配置是否有效 |

---

### 4.9 P8：会话管理API（8个任务）

验证会话的完整生命周期管理。

| 编号 | 任务 | 操作方式 | 预期行为 |
|------|------|---------|---------|
| E2E-SES-01 | 创建会话 | `POST /sessions` 带 `{"title":"测试会话"}` | 返回session_id，title正确 |
| E2E-SES-02 | 列出会话 | `GET /sessions?page=1&page_size=10` | 返回分页列表，包含刚创建的会话 |
| E2E-SES-03 | 搜索会话 | `GET /sessions?keyword=测试` | 返回title包含"测试"的会话 |
| E2E-SES-04 | 更新会话标题 | `PUT /sessions/{id}` 带新title | 标题更新成功，version递增 |
| E2E-SES-05 | 删除会话（软删除） | `DELETE /sessions/{id}` | 会话标记为已删除，列表中不再显示 |
| E2E-SES-06 | 获取会话消息历史 | `GET /sessions/{id}/messages` | 返回该会话所有消息，包含role和content |
| E2E-SES-07 | 不存在的会话 | `GET /sessions/nonexistent-id/messages` | 返回404 |
| E2E-SES-08 | 并发更新冲突 | 两个请求同时更新同一会话标题（不同version） | 第二个返回409版本冲突 |

---

### 4.10 P9：ReAct循环异常处理（8个任务）

验证Agent在各种异常输入下的容错能力。这些场景通过发送特定用户输入触发。

| 编号 | 任务 | 用户输入 | 预期行为 |
|------|------|---------|---------|
| E2E-RA-01 | 空输入 | `""` 或纯空格 `"   "` | Agent返回错误提示或正常finish，不崩溃 |
| E2E-RA-02 | 无意义乱码 | `"asdfghjkl12345!@#"` | Agent尝试理解，给出无法识别的回复，正常finish |
| E2E-RA-03 | 超长单步任务 | `"读取这个文件然后告诉我内容然后搜索关键词然后统计行数然后排序然后导出然后总结"` | Agent在max_steps内完成或提示步骤过多，不无限循环 |
| E2E-RA-04 | 矛盾指令 | `"删除test.txt然后读取test.txt的内容"` | Agent按顺序执行（先删后读），读取时报文件不存在 |
| E2E-RA-05 | 模糊意图 | `"帮我处理一下那个东西"` | Agent追问具体需求，或给出无法确定的提示 |
| E2E-RA-06 | 不存在的工具 | `"用绘图功能画一个流程图"` | Agent说明没有该工具，建议替代方案 |
| E2E-RA-07 | 中文+英文混合 | `"create一个新文件叫test.txt在D盘"` | Agent能理解混合语言，正确执行write_text_file |
| E2E-RA-08 | 嵌套指令 | `"如果test.txt存在就读取它，如果不存在就创建它"` | Agent先检查文件是否存在，再决定执行哪个操作 |

---

### 4.11 P10：工具执行容错（6个任务）

验证工具调用失败时的重试和容错机制。

| 编号 | 任务 | 用户输入 | 预期行为 |
|------|------|---------|---------|
| E2E-TR-01 | 文件不存在 | `"读取Z:\definitely_not_exist\xxx.txt"` | Agent报错"文件不存在"，正常finish |
| E2E-TR-02 | 权限不足 | `"读取C:\Windows\System32\config\SAM"` | Agent报权限错误，不崩溃 |
| E2E-TR-03 | 命令执行失败 | `"执行命令 xxx_invalid_command_yyy"` | Agent报命令不存在，正常finish |
| E2E-TR-04 | 大文件读取 | 创建一个1MB的测试文件 → `"读取这个大文件"` | Agent能处理，不OOM，正常返回内容或截断提示 |
| E2E-TR-05 | 并发工具调用 | `"列出D盘目录结构，同时查看系统信息"` | Agent能串联或并行调用多个工具，全部返回结果 |
| E2E-TR-06 | 工具返回空结果 | `"在空目录中搜索.txt文件"` | Agent正确报告"未找到"，不报错 |

---

### 4.12 测试场景总计

| 级别 | 场景数 | 覆盖范围 |
|------|--------|---------|
| P0 核心链路 | 4 | API→Agent→LLM→DB→SSE完整链路 |
| P1 FILE工具 | 8 | 文件操作11个工具全覆盖 |
| P2 SHELL+NETWORK | 7 | 命令执行+网络请求9个工具全覆盖 |
| P3 SYSTEM+DESKTOP | 13 | 系统+桌面19个工具全覆盖 |
| P4 DOCUMENT+META | 14 | 文档+元数据19个工具全覆盖 |
| P5 复合多步 | 12 | 多工具串联+跨分类调用 |
| P6 异常容错 | 10 | 输入异常+服务中断+并发 |
| P7 任务生命周期 | 8 | 取消/暂停/恢复/确认 |
| P8 会话管理API | 8 | 会话CRUD+分页+搜索+冲突 |
| P9 ReAct异常处理 | 8 | 空输入/乱码/矛盾/模糊意图 |
| P10 工具执行容错 | 6 | 文件不存在/权限/大文件/空结果 |
| **合计** | **98** | |

---

## 五、实现方式

### 5.1 测试框架选择

使用 **pytest**，与现有测试体系一致。

### 5.2 测试文件结构

```
backend/tests/
  ├── test_e2e_full_link.py       ← 主测试文件（本方案的核心）
  ├── conftest.py                  ← fixtures（backend_client、db检查、日志检查等）
```

### 5.3 测试辅助函数

每个测试都需要以下辅助能力：

```python
async def send_chat_and_collect_events(
    user_input: str,
    base_url: str = "http://localhost:8000"
) -> dict:
    """
    发POST /chat，接收SSE事件流，返回结构化结果。
    后端从config.yaml读取LLM配置，无需单独启动LLM服务。
    
    返回值：
    {
        "events": [...],         # 所有SSE事件列表
        "final_event": {...},    # 最后一个final事件
        "has_error": bool,
        "total_steps": int,
        "tool_calls": [...],     # 调用了哪些工具
        "llm_call_count": int,   # LLM调用次数
        "total_time_ms": int,    # 总耗时
        "response_text": str     # final中的回复文本
    }
    """
```

```python
def check_database(session_id: str) -> dict:
    """
    检查数据库记录，返回完整性报告。
    
    {
        "session_exists": bool,
        "session_status": str,
        "messages_count": int,
        "execution_steps_count": int
    }
    """
```

```python
def check_logs(test_id: str, start_time: datetime) -> dict:
    """
    检查日志文件，返回异常报告。
    
    {
        "errors": [...],
        "warnings": [...],
        "tracebacks": [...]
    }
    """
```

### 5.4 测试用例模板

```python
@pytest.mark.e2e_full_link
@pytest.mark.asyncio
async def test_e2e_p0_01_chat_hello():
    """P0-01: 核心链路验证 - 发送hello，验证API→SSE通路"""
    
    # 1. 记录起始状态
    test_start = datetime.now()
    base_session_count = get_session_count()
    
    # 2. 发送请求并接收SSE事件流
    result = await send_chat_and_collect_events("hello")
    
    # 3. 验证事件流正确性
    assert result["has_error"] == False, "不应有error事件"
    assert result["final_event"] is not None, "必须有final事件"
    
    # 4. 验证SSE事件格式
    assert len(result["events"]) > 0, "应至少有start和final事件"
    assert result["events"][0]["type"] == "start", "第一个事件应为start"
    
    # 5. 验证响应时间
    assert result["total_time_ms"] < 120000, f"不应超时，实际={result['total_time_ms']}ms"
    
    # 6. 验证数据库
    db_check = check_database(result["final_event"]["session_id"])
    assert db_check["session_exists"], "session应已保存"
    
    # 7. 验证日志
    log_check = check_logs("e2e-p0-01", test_start)
    assert len(log_check["errors"]) == 0, f"日志不应有ERROR: {log_check['errors']}"
```

---

## 六、执行计划

### 6.1 前提条件

```bash
# 1. 确保后端已启动（后端从config.yaml读取LLM配置）
cd backend
python -m uvicorn app.main:app --reload

# 2. 运行测试（另一个终端）
cd backend
pytest -m e2e_full_link -v --tb=short
```

### 6.2 测试数量与时间预估

| 级别 | 场景数 | 单个耗时 | 总耗时 | 说明 |
|------|--------|---------|--------|------|
| P0 核心链路 | 4 | 30s-2min | ~5min | 最基本验证，优先跑 |
| P1 FILE工具 | 8 | 30s-3min | ~15min | 单工具验证 |
| P2 SHELL+NETWORK | 7 | 1min-3min | ~15min | 单工具验证 |
| P3 SYSTEM+DESKTOP | 13 | 1min-5min | ~30min | 部分工具有副作用需skip |
| P4 DOCUMENT+META | 14 | 1min-5min | ~35min | 含数据库查询 |
| P5 复合多步 | 12 | 2min-5min | ~40min | 多工具串联 |
| P6 异常容错 | 10 | 1min-3min | ~20min | 含并发、中断等 |
| P7 任务生命周期 | 8 | 1min-3min | ~15min | 需要task_id配合 |
| P8 会话管理API | 8 | 30s-1min | ~5min | REST API直接调用 |
| P9 ReAct异常处理 | 8 | 30s-2min | ~10min | 验证Agent容错 |
| P10 工具执行容错 | 6 | 30s-2min | ~10min | 验证工具重试 |
| **合计** | **98** | | **~4-5h** | **可按级别分批跑** |

> **注意**：总耗时基于串行执行。如需加速，可按P0→P1→...分批运行，或用 `-k` 过滤。

### 6.3 标记与分批执行

所有测试使用 `@pytest.mark.e2e_full_link` 标记。

```bash
# 跑全部全链路测试（约4-5小时）
pytest -m e2e_full_link -v --tb=short

# 只跑P0（核心链路验证，~5分钟）
pytest -m e2e_full_link -k "p0" -v --tb=short

# 只跑P1-P4（单工具验证，~95分钟）
pytest -m e2e_full_link -k "p1 or p2 or p3 or p4" -v --tb=short

# 只跑P5-P6（复合+异常，~60分钟）
pytest -m e2e_full_link -k "p5 or p6" -v --tb=short

# 只跑P7-P10（生命周期+API+容错，~40分钟）
pytest -m e2e_full_link -k "p7 or p8 or p9 or p10" -v --tb=short

# 跑指定单个测试
pytest -m e2e_full_link -k "e2e_p0_01" -v --tb=short
```

---

## 七、测试报告格式

每个测试运行后输出：

```
=== E2E-P0-01: 核心链路验证 ===
  用户输入: "hello"
  状态: ✅ 通过
  耗时: 12.3s
  SSE事件数: 3 (start→thought→final)
  链路验证: API✅ → Agent✅ → LLM✅ → DB✅ → SSE✅
  数据库: ✅ session已保存
  日志: ✅ 无ERROR
```

所有测试跑完后输出汇总：

```
=== 全链路E2E测试汇总 ===
  总测试数: 98
  通过: 92
  失败: 4
  跳过: 2
  
  ❌ 失败详情:
    E2E-P0-01: hello → 死循环（step=47, LLM调用=12）
    E2E-LC-01: 取消任务 → 未收到cancelled事件
    E2E-RA-03: 超长指令 → 超过max_steps未停止
    E2E-TR-04: 大文件读取 → 超时（>120s）
  
  ⚠️ 跳过详情:
    E2E-SY-11: 服务控制 → 需要管理员权限
    E2E-TR-02: 权限不足 → 测试文件不可写
```

---

## 八、与其他测试的关系

```
所有测试体系
│
├── L1: Mock单元测试（~200个, ~10秒）
│   ├── 单工具函数测试（file/shell/network/system/desktop/document/meta）
│   ├── MessageBuilder测试（43个）
│   └── 运行时集成测试（14个）
│
├── L2: 全链路E2E集成测试（98个, ~4-5小时）← 本方案主测
│   ├── P0: 核心链路验证（4个）
│   ├── P1: FILE工具全覆盖（8个）
│   ├── P2: SHELL+NETWORK全覆盖（7个）
│   ├── P3: SYSTEM+DESKTOP全覆盖（13个）
│   ├── P4: DOCUMENT+META全覆盖（14个）
│   ├── P5: 复合多步任务（12个）
│   ├── P6: 异常容错场景（10个）
│   ├── P7: 任务生命周期管理（8个）
│   ├── P8: 会话管理API（8个）
│   ├── P9: ReAct循环异常处理（8个）
│   └── P10: 工具执行容错（6个）
│
└── L3: 辅助集成测试（22个, ~30分钟）← 本方案辅助测
    ├── ReAct循环异常（Mock LLM，8个）
    ├── 工具重试容错（Mock工具，6个）
    └── SSE流管道异常（构造非法请求，8个）
```

**L2覆盖三层验证**：
- **工具层**（P1-P4）：58个工具在真实LLM下能否被正确调用
- **流程层**（P5-P7）：多步推理、任务控制、系统串联是否完整
- **容错层**（P6/P9-P10）：异常输入、工具失败、服务中断时能否优雅处理

---

## 九、辅助集成测试（需Mock，非真实LLM）

**本章与L2的区别**：L2用真实后端+真实LLM，本章用Mock模拟内部异常，验证系统内部机制。

**适用场景**：需要构造正常用户输入无法触发的内部异常状态。

### 9.1 ReAct循环异常（Mock LLM返回）

| 编号 | 场景 | Mock方式 | 预期行为 |
|------|------|---------|---------|
| INT-RA-01 | LLM返回空内容 | Mock LLM返回 `""` | Agent报错"LLM返回为空"，正常finish |
| INT-RA-02 | LLM返回不可解析内容 | Mock LLM返回 `"这不是JSON"` | Agent走parse_error分支，正常finish |
| INT-RA-03 | LLM返回未知type | Mock LLM返回 `{"type":"foo_bar"}` | Agent走unknown handler，正常finish |
| INT-RA-04 | LLM返回空工具名 | Mock LLM返回 `{"tool_name":"","tool_params":{}}` | Agent报错，不死循环 |
| INT-RA-05 | LLM返回不存在的工具 | Mock LLM返回 `{"tool_name":"fake_tool","tool_params":{}}` | Agent报错"工具不存在"，不死循环 |
| INT-RA-06 | LLM返回缺少必要参数 | Mock LLM返回 `{"tool_name":"read_file","tool_params":{}}` | Agent报错"参数缺失"，不死循环 |
| INT-RA-07 | 循环中取消任务 | 发送请求 → 取消 → Mock LLM继续返回action | Agent检测到取消标志，停止循环 |
| INT-RA-08 | 超过max_steps | Mock LLM每次都返回action（永不answer） | Agent在max_steps后强制停止，返回"步骤上限" |

### 9.2 工具执行重试与容错（Mock工具抛异常）

| 编号 | 场景 | Mock方式 | 预期行为 |
|------|------|---------|---------|
| INT-TR-01 | 工具抛可重试异常 | Mock read_file第一次抛ConnectionError，第二次成功 | 重试后成功，Agent继续 |
| INT-TR-02 | 工具抛不可重试异常 | Mock read_file抛ValueError | 立即失败，不重试 |
| INT-TR-03 | 重试耗尽 | Mock read_file每次都抛异常 | 达到最大重试次数后失败，Agent报错 |
| INT-TR-04 | 工具执行超时 | Mock read_file永不返回（sleep 300s） | 超时中断，Agent报错 |
| INT-TR-05 | 工具返回None | Mock read_file返回None | Agent处理空结果，不崩溃 |
| INT-TR-06 | 同步工具通过to_thread执行 | Mock一个同步函数作为工具 | 正常执行，不阻塞事件循环 |

### 9.3 SSE流管道异常（构造非法请求）

| 编号 | 场景 | 构造方式 | 预期行为 |
|------|------|---------|---------|
| INT-SSE-01 | 空messages列表 | `POST /chat` 带 `{"messages":[]}` | 返回错误SSE事件，不崩溃 |
| INT-SSE-02 | 缺少role字段 | `POST /chat` 带 `{"messages":[{"content":"hi"}]}` | 返回参数错误 |
| INT-SSE-03 | 缺少content字段 | `POST /chat` 带 `{"messages":[{"role":"user"}]}` | 返回参数错误 |
| INT-SSE-04 | messages不是数组 | `POST /chat` 带 `{"messages":"hello"}` | 返回类型错误 |
| INT-SSE-05 | 无效session_id格式 | `POST /chat` 带 `{"session_id":"!!!invalid"}` | 返回格式错误或自动清理 |
| INT-SSE-06 | 不存在的session_id | `POST /chat` 带 `{"session_id":"00000000-0000-0000-0000-000000000000"}` | 返回session不存在或自动创建 |
| INT-SSE-07 | 超大messages | `POST /chat` 带100条messages | 正常处理或返回限制提示 |
| INT-SSE-08 | 并发取消同一任务 | 同时发2次cancel同一task_id | 一个成功，一个提示已取消 |

---

**更新时间**: 2026-06-08 18:50:00  
**版本**: v1.1  
**更新人**: 小欧  
**原编写人**: 小健
