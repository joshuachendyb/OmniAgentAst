# 工具参数审计报告

**创建时间**: 2026-06-20 04:35:00
**更新时间**: 2026-07-12 16:00:00
**编写人**: 小欧
**变更人**: 小欧

## 版本历史

| 版本 | 时间 | 修改内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-06-20 04:35 | 初版创建 | 小欧 |
| v1.1 | 2026-06-20 05:15 | 小健审计修正 | 小健 |
| v2.0 | 2026-07-12 16:00 | 全面更新至实际代码：工具数77→63，shell重写(4→2)，desktop重写(16→11)，全类目参数校对 | 小欧 |

**审计范围**: 全部10组63个工具的Schema参数
**审计目标**: 每个参数的名称、含义、必填/可选、是否必要

---

## 一、fundamental（7个工具）

### 1.1 searchtool — 搜索工具

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| query | 搜索关键词，BM25全文检索，支持中英文 | 必填 | ✅ 必要 |

### 1.2 timenow — 获取当前时间

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| （无参数） | — | — | — |

### 1.3 timeadd — 时间偏移计算

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| delta | 偏移量。正数=增加，负数=减少 | 必填 | ✅ 必要 |
| start | 基准时间，默认当前时间 | 可选 | ✅ 必要——多数场景用默认值 |
| unit | 偏移单位：days/hours/minutes/seconds/months | 可选 | ✅ 必要——默认days |

### 1.4 timediff — 计算时间差

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| start | 起始时间 | 必填 | ✅ 必要 |
| end | 结束时间，默认当前时间 | 可选 | ✅ 必要——多数场景用默认值 |

### 1.5 calendar — 日历查询

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| name | 节日名称，如端午节/春节/中秋节 | 必填 | ✅ 必要 |
| year | 查询年份，默认当年 | 可选 | ⚠️ 多数情况用默认值 |

注意：原版本有 date/check_type/n 参数，当前代码已删除。

### 1.6 sysinfo — 获取系统信息

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| info_type | 系统信息类型：basic/cpu/内存/磁盘/网络/all | 可选 | ✅ 必要——默认all |

### 1.7 notify — 发送通知

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| title | 通知标题 | 必填 | ✅ 必要 |
| message | 通知正文 | 必填 | ✅ 必要 |
| duration | 通知显示时长（秒），默认5秒 | 可选 | ⚠️ 多数情况用默认值 |


---

## 二、file（14个工具）

### 2.1 readtext — 读取文本文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件路径（绝对路径） | 必填 | ✅ 必要 |
| offset | 起始行号 | 可选 | ⚠️ 可选，大文件时有用 |
| limit | 最大读取行数 | 可选 | ⚠️ 可选，大文件时有用 |
| tail | 读取后N行 | 可选 | ⚠️ 可选，大文件时有用 |
| encoding | 文件编码，默认utf-8 | 可选 | ✅ 必要——中文文件可能需要gbk |

### 2.2 writetext — 写入文本文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件完整路径（绝对路径） | 必填 | ✅ 必要 |
| content | 要写入的文本内容 | 必填 | ✅ 必要 |
| encoding | 文件编码，默认utf-8 | 可选 | ✅ 必要——中文文件可能需要gbk |
| append | 是否追加，True=追加 False=覆盖 | 可选 | ✅ 必要——日志场景常用 |

### 2.3 readmedia — 读取媒体文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 媒体文件路径。支持图片/音频/视频 | 必填 | ✅ 必要 |

### 2.4 edittext — 编辑文本文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 目标文件绝对路径 | 必填 | ✅ 必要 |
| old_string | 待替换的旧字符串 | 必填 | ✅ 必要 |
| new_string | 替换的新字符串，空字符串表示删除 | 可选 | ✅ 必要——删除时传空 |
| mode | 替换模式：once/all | 可选 | ⚠️ 默认once |
| ignore_case | 是否忽略大小写 | 可选 | ⚠️ 默认False |
| encoding | 文件编码，默认utf-8 | 可选 | ✅ 必要 |

### 2.5 listdir — 列出目录内容

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 目录路径（绝对路径） | 必填 | ✅ 必要 |
| sort_by | 排序方式：name/size/mtime | 可选 | ✅ 必要——默认name |
| include_hidden | 是否显示隐藏文件 | 可选 | ⚠️ 默认False |
| offset | 跳过前N条结果，0=不限 | 可选 | ⚠️ 结果太多时用 |

### 2.6 tree — 树形列出目录

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 目录路径（绝对路径） | 必填 | ✅ 必要 |
| include_hidden | 是否显示隐藏文件 | 可选 | ⚠️ 默认False |
| max_depth | 最大递归深度，默认5，最大20 | 可选 | ✅ 必要——控制展开层级 |
| sort_by | 排序方式：name/mtime | 可选 | ✅ 必要——默认name |

### 2.7 find — 搜索文件名

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| pattern | 文件名匹配模式，支持glob通配符 | 必填 | ✅ 必要 |
| path | 搜索起始目录（绝对路径） | 必填 | ✅ 必要 |
| ignore_case | 是否忽略大小写，默认True | 可选 | ⚠️ 多数情况用默认值 |
| type | 搜索类型过滤：file/directory | 可选 | ⚠️ 多数情况不需要 |
| offset | 跳过前N条结果 | 可选 | ⚠️ 结果太多时用 |

### 2.8 grep — 搜索文件内容

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| pattern | 正则表达式搜索模式 | 必填 | ✅ 必要 |
| path | 搜索路径（绝对路径） | 必填 | ✅ 必要 |
| glob | 文件过滤通配符，如 "*.py" | 可选 | ✅ 必要——限定文件类型时用 |
| ignore_case | 是否忽略大小写，默认True | 可选 | ⚠️ 多数情况用默认值 |
| literal | 是否按纯文本匹配而非正则 | 可选 | ✅ 必要——搜索特殊字符时用 |
| context | 上下各行数，默认0 | 可选 | ⚠️ 可选 |
| output_mode | 输出模式：content/count/only_files | 可选 | ✅ 必要——默认content |

### 2.9 compress — 压缩文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 要压缩的文件/目录路径 | 必填 | ✅ 必要 |
| dest | 输出压缩包路径 | 必填 | ✅ 必要 |
| format | 压缩格式：zip/tar/tar.gz/tar.bz2 | 可选 | ✅ 必要——默认zip |
| password | ZIP加密密码 | 可选 | ⚠️ 特殊场景才用 |
| overwrite | 是否覆盖已存在文件 | 可选 | ⚠️ 默认False |
| exclude_patterns | 排除的文件/目录模式列表 | 可选 | ⚠️ 复杂场景才用 |

### 2.10 extract — 解压文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 压缩包路径 | 必填 | ✅ 必要 |
| dest | 解压目标目录，默认自动创建同名目录 | 可选 | ✅ 必要——多数场景用默认值 |
| password | 解密密码，仅ZIP支持 | 可选 | ⚠️ 特殊场景才用 |
| overwrite | 是否覆盖已存在文件 | 可选 | ⚠️ 默认False |

### 2.11 move — 移动文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 源文件路径（绝对路径） | 必填 | ✅ 必要 |
| dest | 目标路径（绝对路径） | 必填 | ✅ 必要 |
| overwrite | 是否覆盖目标文件 | 可选 | ⚠️ 默认False |

### 2.12 copy — 复制文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 源文件路径（绝对路径） | 必填 | ✅ 必要 |
| dest | 目标路径（绝对路径） | 必填 | ✅ 必要 |
| recursive | 复制目录时需True | 可选 | ✅ 必要——复制目录时必需 |
| overwrite | 是否覆盖目标文件 | 可选 | ⚠️ 默认False |
| preserve_metadata | 是否保留元数据 | 可选 | ✅ 必要——默认True |

### 2.13 delete — 删除文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 要删除的文件/目录路径 | 必填 | ✅ 必要 |
| recursive | 删除非空目录时需True | 可选 | ✅ 必要——删除目录时必需 |
| force | True=跳过回收站永久删除 | 可选 | ⚠️ 默认False |

### 2.14 rename — 重命名文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 原文件/目录路径（绝对路径） | 必填 | ✅ 必要 |
| dest | 新名称（仅文件名，不含目录路径） | 必填 | ✅ 必要 |

注意：原版本的 read_data_file / write_data_file 工具已删除（schema中不再存在）。

---

## 三、shell（2个工具）

本节已全面重写（原版本4个工具：execute_shell_command / find_command / shell_session / execute_code，已全部删除）。

### 3.1 shell — 执行Shell命令

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| command | 要执行的命令 | 必填 | ✅ 必要 |
| shell_type | 执行环境：powershell/cmd | 可选 | ⚠️ 默认powershell，Windows环境几乎不用改 |
| timeout | 超时秒数，默认60，最大600 | 可选 | ⚠️ 多数情况用默认值 |
| cwd | 命令执行的工作目录(绝对路径) | 可选 | ⚠️ 需在特定目录执行命令时设置 |

### 3.2 which — 查找命令路径

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| command | 要查找的命令名称 | 必填 | ✅ 必要 |
| all_paths | False=返回第一个，True=返回全部 | 可选 | ⚠️ 多数情况用默认值 |

---

## 四、network（5个工具）

### 4.1 httprequest — HTTP请求

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| url | 请求目标URL | 必填 | ✅ 必要 |
| method | HTTP方法：GET/POST/PUT/DELETE/PATCH | 可选 | ✅ 必要——默认GET |
| headers | 请求头字典 | 可选 | ✅ 必要——认证等场景用 |
| body | JSON请求体 | 可选 | ✅ 必要——POST时用 |
| timeout | 超时秒数，默认30，最大300 | 可选 | ⚠️ 底层控制，多数用默认值 |
| proxy | 代理地址 | 可选 | ⚠️ 极少用 |

### 4.2 download — 下载文件

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| url | 要下载文件的URL | 必填 | ✅ 必要 |
| dest | 文件保存完整路径，默认自动生成 | 可选 | ✅ 必要——指定保存位置 |
| headers | 自定义请求头 | 可选 | ⚠️ 极少用 |
| timeout | 超时秒数，默认60，最大3600 | 可选 | ⚠️ 底层控制 |
| proxy | 代理地址 | 可选 | ⚠️ 极少用 |

### 4.3 fetchpage — 抓取网页

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| url | 网页URL | 必填 | ✅ 必要 |
| prompt | 提取指令，未指定时返回完整内容 | 可选 | ✅ 必要——精准提取时用 |
| extract_format | 提取格式：markdown/html/text | 可选 | ✅ 必要——默认markdown |
| js_render | 是否执行JavaScript渲染 | 可选 | ⚠️ 默认False |
| timeout | 超时秒数，默认30，最大120 | 可选 | ⚠️ 底层控制 |
| proxy | 代理地址 | 可选 | ⚠️ 极少用 |

### 4.4 searchweb — 搜索网页

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| query | 搜索查询字符串 | 必填 | ✅ 必要 |
| num_results | 返回结果数量，默认10，最大50 | 可选 | ⚠️ 默认10够用 |
| allowed_domains | 限制搜索范围（逗号分隔域名） | 可选 | ⚠️ 极少用 |
| blocked_domains | 排除的域名（逗号分隔） | 可选 | ⚠️ 极少用 |
| proxy | 代理地址 | 可选 | ⚠️ 极少用 |

### 4.5 ping_port — 网络诊断

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| host | 目标主机地址（域名或IP） | 必填 | ✅ 必要 |
| mode | 诊断模式：ping(主机级)/port(服务级) | 可选 | ✅ 必要——默认ping |
| port | 目标端口号（mode=port时必填） | 可选 | ✅ 必要——端口检测时必需 |
| count | ping次数，默认4，最大20 | 可选 | ⚠️ 默认4次够用 |
| timeout | 超时秒数，默认5，最大30 | 可选 | ⚠️ 底层控制 |

---

## 五、document（8个工具）

### 5.1 read_pdf — 读取PDF

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件路径(.pdf) | 必填 | ✅ 必要 |

### 5.2 read_docx — 读取Word

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件路径(.docx/.doc) | 必填 | ✅ 必要 |

### 5.3 read_pptx — 读取PPT

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件路径(.pptx) | 必填 | ✅ 必要 |

### 5.4 read_xlsx — 读取Excel

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件路径(.xlsx/.csv/.xls) | 必填 | ✅ 必要 |
| sheet_name | 指定工作表名 | 可选 | ✅ 必要——多工作表时用 |

### 5.5 write_docx — 写入Word

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件路径(.docx) | 必填 | ✅ 必要 |
| title | 文档标题 | 可选 | ⚠️ 可选 |
| content | 正文文本内容（与table_data互斥） | 可选 | ✅ 必要——写入文本时必需 |
| table_data | 表格数据（与content互斥） | 可选 | ✅ 必要——写入表格时必需 |

### 5.6 write_xlsx — 写入Excel

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件路径(.xlsx) | 必填 | ✅ 必要 |
| data | 写入数据（List[Dict]格式） | 可选 | ✅ 必要——写入内容时必需 |
| sheet_name | 工作表名，默认Sheet1 | 可选 | ⚠️ 默认Sheet1 |

### 5.7 write_pdf — 写入PDF

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件路径(.pdf) | 必填 | ✅ 必要 |
| title | 文档标题 | 可选 | ⚠️ 可选 |
| content | 正文文本内容（与table_data互斥） | 可选 | ✅ 必要——写入文本时必需 |
| table_data | 表格数据（与content互斥） | 可选 | ✅ 必要——写入表格时必需 |

### 5.8 write_pptx — 写入PPT

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 文件路径(.pptx) | 必填 | ✅ 必要 |
| slides | 幻灯片列表（List[Dict]或JSON字符串） | 可选 | ✅ 必要——写入内容时必需 |

---

## 六、dataanalysis（6个工具）

基类：`_DbConnectionMixin`（不公开，被 3 个 SQL 工具继承）

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| connection_type | 数据库类型：sqlite/mysql/postgresql | 可选 | ✅ 必要——默认sqlite |
| connection_string | MySQL/PostgreSQL连接字符串 | 可选 | ✅ 必要——非sqlite时必需 |
| path | SQLite数据库文件路径 | 可选 | ✅ 必要——sqlite时必需 |

验证规则：sqlite 要求 path，禁止 connection_string；mysql/postgresql 相反。

### 6.1 generate_chart — 生成图表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| data | 图表数据（JSON字符串或字典） | 必填 | ✅ 必要 |
| chart_type | 图表类型：bar/line/pie/scatter | 可选 | ✅ 必要——默认bar |
| title | 图表标题 | 可选 | ⚠️ 可选 |
| dest | 输出图片路径 | 可选 | ⚠️ 默认自动生成临时路径 |

### 6.2 analyze_data — 数据分析

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 数据文件路径（与data互斥，二选一必填） | 可选 | ✅ 必要 |
| data | JSON字符串数据（与path互斥，二选一必填） | 可选 | ✅ 必要 |
| operations | 统计分析操作列表 | 可选 | ✅ 必要——默认全部统计 |
| group_by | 分组统计的列名 | 可选 | ✅ 必要——分组分析时用 |
| sort_by | 排序的列名 | 可选 | ⚠️ 可选 |
| top_n | 只返回前N条结果 | 可选 | ✅ 必要——限制结果数 |
| max_rows | 最大处理行数 | 可选 | ⚠️ 底层控制 |

### 6.3 filter_data — 数据筛选

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 数据文件路径（与data互斥，二选一必填） | 可选 | ✅ 必要 |
| data | JSON字符串数据（与path互斥，二选一必填） | 可选 | ✅ 必要 |
| conditions | 筛选条件列表 | 必填 | ✅ 必要 |
| select_columns | 选择返回的列 | 可选 | ✅ 必要——只返回指定列时用 |
| sort_by | 排序的列名 | 可选 | ⚠️ 可选 |
| top_n | 只返回前N条结果 | 可选 | ✅ 必要——限制结果数 |
| max_rows | 最大处理行数 | 可选 | ⚠️ 底层控制 |

### 6.4 query_sql — SQL查询（只读，继承_DbConnectionMixin）

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| connection_type | 数据库类型 | 可选 | ✅ 必要——默认sqlite |
| connection_string | 连接字符串(non-sqlite) | 可选 | ✅ 必要 |
| path | SQLite文件路径 | 可选 | ✅ 必要 |
| sql | SQL查询语句（仅SELECT/SHOW/DESCRIBE） | 必填 | ✅ 必要 |

### 6.5 execute_sql — SQL执行（写入，继承_DbConnectionMixin）

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| connection_type | 数据库类型 | 可选 | ✅ 必要——默认sqlite |
| connection_string | 连接字符串(non-sqlite) | 可选 | ✅ 必要 |
| path | SQLite文件路径 | 可选 | ✅ 必要 |
| sql | SQL写入语句（INSERT/UPDATE/DELETE/DDL） | 必填 | ✅ 必要 |
| dry_run | 预检模式，True=只校验不执行 | 可选 | ⚠️ 可选 |

### 6.6 get_db_schema — 获取数据库结构（继承_DbConnectionMixin）

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| connection_type | 数据库类型 | 可选 | ✅ 必要——默认sqlite |
| connection_string | 连接字符串(non-sqlite) | 可选 | ✅ 必要 |
| path | SQLite文件路径 | 可选 | ✅ 必要 |
| table_name | 指定表名，仅获取该表结构 | 可选 | ✅ 必要——查看指定表时用 |

---

## 七、system（4个工具）

### 7.1 event_log — 获取系统事件日志

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| log_name | 日志名称：System/Application/Security | 可选 | ✅ 必要——默认System |
| max_events | 最大返回事件数，默认50，最大1000 | 可选 | ⚠️ 多数情况用默认值 |
| level | 日志级别过滤：critical/error/warning/info | 可选 | ✅ 必要——默认error |
| source | 事件来源/应用名过滤 | 可选 | ⚠️ 可选 |
| time_range | 时间范围：10m/1h/24h/7d | 可选 | ✅ 必要——默认1h |

### 7.2 create_task — 创建计划任务

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| task_name | 计划任务名称 | 必填 | ✅ 必要 |
| command | 要执行的命令或程序路径 | 必填 | ✅ 必要 |
| schedule | 计划执行时间（HH:MM/HH:MM /day N/HH:MM /monthly DD） | 必填 | ✅ 必要 |
| interval | 重复间隔分钟数 | 可选 | ⚠️ 可选 |

### 7.3 delete_task — 删除计划任务

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| task_name | 要删除的计划任务名称 | 必填 | ✅ 必要 |

### 7.4 list_tasks — 列出计划任务

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| task_name | 按任务名称过滤（模糊匹配） | 可选 | ⚠️ 可选 |
| state | 状态过滤：ready/running/disabled/all | 可选 | ✅ 必要——默认all |

注意：原版本的 list_processes / kill_process / service_control / net_connections / get_env / set_env 已全部删除。

---

## 八、desktop（11个工具）

本节已全面改写。原版本16个工具（window_focus/maximize/minimize/restore/topmost/unpin作为独立工具 + clipboard_read/clipboard_write独立），现合并为11个。

### 8.1 window_info — 获取窗口列表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| include_minimized | 是否包含最小化的窗口 | 可选 | ⚠️ 默认False |
| filter_title | 按窗口标题过滤（大小写不敏感） | 可选 | ✅ 必要——查找特定窗口时用 |

### 8.2 window_focus — 聚焦窗口

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |

### 8.3 window_resize — 调整窗口大小

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |
| width | 窗口宽度（像素），默认800 | 可选 | ✅ 必要——调整大小时必需 |
| height | 窗口高度（像素），默认600 | 可选 | ✅ 必要——调整大小时必需 |

### 8.4 set_window_state — 设置窗口状态（替代原maximize/minimize/restore/topmost/unpin 5个独立工具）

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| window_title | 窗口标题（大小写不敏感） | 必填 | ✅ 必要 |
| action | 操作类型：maximize/minimize/restore/topmost/unpin | 必填 | ✅ 必要 |

### 8.5 mouse_click — 鼠标点击

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| x | X坐标（不传则在当前位置点击） | 可选 | ⚠️ 可选 |
| y | Y坐标（不传则在当前位置点击） | 可选 | ⚠️ 可选 |
| button | 鼠标按钮：left/right/middle | 可选 | ✅ 必要——默认left |

### 8.6 mouse_move — 鼠标移动

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| x | 目标X坐标 | 必填 | ✅ 必要 |
| y | 目标Y坐标 | 必填 | ✅ 必要 |

### 8.7 mouse_scroll — 鼠标滚动

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| direction | 滚动方向：up/down | 可选 | ✅ 必要——默认down |
| amount | 滚动单位，默认3 | 可选 | ✅ 必要——默认3 |

### 8.8 mouse_position — 获取鼠标位置

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| （无参数） | — | — | — |

### 8.9 keyboard_control — 键盘控制

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| action | 键盘操作：type(输入文本)/shortcut(快捷键) | 必填 | ✅ 必要 |
| text_or_keys | 输入内容（type时为文本，shortcut时为ctrl+c） | 必填 | ✅ 必要 |

注意：原版本的 interval 参数已删除；action 的 combo 选项已删除。

### 8.10 screen_capture — 截屏

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| dest | 输出文件路径 | 可选 | ⚠️ 默认自动生成临时路径 |
| region | 截取区域：{x, y, width, height} | 可选 | ⚠️ 可选 |
| display | 显示器编号，1=主显示器（与region/dest互斥） | 可选 | ⚠️ 多显示器时用 |

### 8.11 clipboard_control — 剪贴板控制（替代原clipboard_read + clipboard_write 2个独立工具）

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| action | 操作类型：read/write | 必填 | ✅ 必要 |
| content | 写入内容（action=write时必填） | 可选 | ✅ 必要——写入时必需 |

---

## 九、win_registry（3个工具）

### 9.1 registry_read — 读取注册表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 注册表键路径 | 必填 | ✅ 必要 |
| value_name | 值名称（不填则读取默认值） | 可选 | ✅ 必要——读取特定值时用 |
| hive | 注册表根键：HKCU/HKLM/HKCR/HKU/HKCC | 可选 | ✅ 必要——默认HKCU |
| output_format | 输出格式：auto/hex | 可选 | ⚠️ 默认auto |

### 9.2 registry_write — 写入注册表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 注册表键路径 | 必填 | ✅ 必要 |
| value_name | 值名称 | 必填 | ✅ 必要 |
| value | 值数据（str或int） | 必填 | ✅ 必要 |
| value_type | 值类型：auto_detect/REG_SZ/REG_EXPAND_SZ/REG_DWORD等 | 可选 | ✅ 必要——默认auto_detect |
| hive | 注册表根键 | 可选 | ✅ 必要——默认HKCU |

### 9.3 registry_delete — 删除注册表

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| path | 注册表键路径 | 必填 | ✅ 必要 |
| value_name | 值名称（不填则删除整个键） | 可选 | ✅ 必要——删除特定值时用 |
| hive | 注册表根键 | 可选 | ✅ 必要——默认HKCU |
| recursive | 递归删除子键 | 可选 | ✅ 必要——删除非空键时必需 |

---

## 十、timer（3个工具）

### 10.1 timer_set — 设置定时器

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| delay | 延迟秒数（1~86400即最长24小时） | 必填 | ✅ 必要 |
| callback | 定时器触发内容（文本消息） | 必填 | ✅ 必要 |

### 10.2 timer_clear — 清除定时器

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| timer_id | 定时器ID（由timer_set返回） | 必填 | ✅ 必要 |

### 10.3 timer_list — 列出定时器

| 参数名 | 含义 | 必填/可选 | 是否必要 |
|--------|------|----------|---------|
| （无参数） | — | — | — |

---

## 十一、与前版差异汇总

### 11.1 工具数量变化（77 → 63）

| 类目 | 前版 | 当前 | 差异 |
|------|------|------|------|
| fundamental | 7 | 7 | — |
| file | 15 | 14 | 删除 read_data_file / write_data_file |
| shell | 4 | 2 | 全面重写：删除旧4工具，新增 shell / which |
| network | 5 | 5 | — |
| document | 8 | 8 | — |
| dataanalysis | 6 | 6 | — |
| system | 5 | 4 | 删除 list_processes / kill_process / service_control / net_connections / get_env / set_env |
| desktop | 16 | 11 | 合并窗口操作、合并剪贴板操作 |
| win_registry | 3 | 3 | — |
| timer | 3 | 3 | — |
| **合计** | **77** | **63** | **-14** |

### 11.2 主要参数变更

| 工具 | 参数 | 变更 |
|------|------|------|
| calendar | date/check_type/n | 已删除 |
| edittext | replace_all→mode | mode = once/all |
| listdir | recursive | 已删除（tree工具独立负责递归） |
| grep | context | 恢复（前版声称删除，实际仍存在） |
| grep | output_mode | 恢复（前版声称删除，实际仍存在） |
| grep | literal | 新增 |
| copy | preserve_metadata | 保留（前版声称删除，实际仍存在） |
| compress | compression_level | 保留（前版声称删除，实际仍存在） |
| httprequest | body | 从 json_body 改名 |
| httprequest | timeout/proxy | 保留（前版声称删除，实际仍存在） |
| download | headers/timeout/proxy | 保留（前版声称删除，实际仍存在） |
| fetchpage | js_render/timeout/proxy | 保留（前版声称删除，实际仍存在） |
| searchweb | allowed_domains/blocked_domains/num_results/proxy | 保留（前版声称删除，实际仍存在） |
| ping_port | count/timeout | 保留（前版声称删除，实际仍存在） |
| generate_chart | dest | 从 output_path 改名 |
| analyze_data | operations/max_rows | 保留（前版声称删除，实际仍存在） |
| filter_data | max_rows | 保留（前版声称删除，实际仍存在） |
| query_sql | limit | 保留（前版声称删除，实际仍存在） |
| keyboard_control | interval | 已删除；action删除了combo |
| set_window_state | — | 新增（替代原6个独立窗口操作工具） |
| clipboard_control | — | 新增（替代原 clipboard_read + clipboard_write） |
| registry_read | output_format | 新增 |
| document全部 | file_name→path | 参数名统一改path |
| read_xlsx | sheet_name | 新增 |
| write_docx/write_pdf | paragraphs→content+table_data | 拆分文本和表格 |

---

**报告完成时间**: 2026-06-20 04:50:00
**前版更新时间**: 2026-06-20 05:15:00
**本次更新时间**: 2026-07-12 16:00:00
**编写人**: 小欧
**前版变更人**: 小健
**本次变更人**: 小欧
