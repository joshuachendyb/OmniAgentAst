# 代码命名问题全面扫描报告

**执行时间**: 2026-06-08 12:36:43  
**扫描范围**: backend/app/ 全部Python文件（排除tools/目录）  
**执行人**: 小欧  
**版本**: v1.0

---

## 一、已修复问题（26个）

| # | 文件 | 问题 | 修复方式 | 状态 |
|---|------|------|---------|------|
| 1 | `_ordered_dict.py` | 递归调用命名错误 | 函数名改为 `_ordered_dict` | ✅ |
| 2 | `react_cycle.py:154` | add_chunk vs append | 调用改为 `append()` | ✅ |
| 3 | `react_cycle.py:155` | is_full vs should_promote | 调用改为 `should_promote()` | ✅ |
| 4 | `react_cycle.py:74,90,98...` | _emit_step不存在 | 改为 `_step_emitter.emit()` | ✅ |
| 5 | `validate_config.py:26` | 调用不存在方法 | 改为 `resolve_provider_model()` | ✅ |
| 6 | `data_utils.py` | 文件名不匹配 | 重命名为 `json_utils.py` | ✅ |
| 7 | `common.py` | 文件名不匹配 | 重命名为 `display_utils.py` | ✅ |
| 8 | `time_utils.py` | create_step_counter与时间无关 | 移到 `counter_utils.py` | ✅ |
| 9 | `tool_result_formatter.py` | build_next_actions不匹配 | 移到 `next_actions_builder.py` | ✅ |
| 10 | `error_classifier.py` | extract_api_error_detail不匹配 | 移到 `error_parser.py` | ✅ |
| 11 | `llm_strategies.py` | 只剩1个策略 | 重命名为 `llm_strategy.py` | ✅ |
| 12 | `task_incident_check.py` | 命名不匹配 | 重命名为 `task_interrupt_check.py` | ✅ |
| 13 | `llm/client_sdk.py` | stream方法名易混淆 | 重命名为 `raw_stream()` | ✅ |
| 14 | `llm_core/chat.py` | self参数误导 | 重命名为 `aggregate_chat_response()` | ✅ |
| 15 | `tool_retry_engine.py` | 错误码对调 | 改为 `ERR_INVALID_PARAMS` | ✅ |
| 16 | `chat_stream.py` | 函数名误导 | 重命名为 `get_error_info` | ✅ |
| 17 | `main.py` | chat_router_router笔误 | 改为 `chat_router` | ✅ |
| 18 | `html_report.py` | 文档参数不匹配 | 删除不存在参数文档 | ✅ |
| 19 | `text_report.py` | 文档参数不匹配 | 删除不存在参数文档 | ✅ |
| 20 | `common_patterns.py` | 文档自相矛盾 | 删除矛盾描述 | ✅ |
| 21 | `core_agent/__init__.py` | 文档引用错误 | 改为 `react_cycle.py` | ✅ |
| 22 | `chat/__init__.py` | docstring错误 | 改为 `chat` 模块 | ✅ |
| 23 | `v1/__init__.py` | 注释错误 | 改为 `v1 API 模块` | ✅ |
| 24 | `chat_stream.py` | 死代码和未使用参数 | 删除4个未使用参数 | ✅ |
| 25 | `health.py` | 未使用导入 | 删除4个未使用导入 | ✅ |
| 26 | `_backup_config.py` | 未使用导入 | 删除未使用 `shutil` | ✅ |

---

## 二、新发现问题（5个）— 全部已修复

### 问题27: `error_classifier.py` 中 `extract_api_error_detail` 函数未删除

**严重程度**: P1-高  
**问题描述**: 我在之前修复中创建了 `error_parser.py` 并在 `chat_stream.py` 中更新了导入路径，但忘记删除 `error_classifier.py` 中的旧函数。导致同一函数存在两份代码。  
**位置**: `backend/app/utils/error_classifier.py` 第292-315行  
**状态**: ✅ 已修复（已删除旧函数）

---

### 问题28: 两个同名文件 `tool_result_formatter.py` 造成混淆

**严重程度**: P2-中  
**问题描述**: 
- `app/utils/tool_result_formatter.py` — 公共工具函数（截断、格式化）
- `app/services/agent/tool_result_formatter.py` — 工具结果格式化模块（给LLM看的observation文本）

两个文件名相同，但职责完全不同，容易造成导入混乱。

**状态**: ✅ 已修复 — 将 `app/services/agent/tool_result_formatter.py` 重命名为 `observation_formatter.py`，并更新了 `message_utils.py` 中的导入路径

---

### 问题29: `time_utils.py` 的 `__all__` 导出不存在的函数 `timestamp_for_filename`

**严重程度**: P0-紧急（运行时会报 ImportError）  
**问题描述**: `time_utils.py` 的 `__all__` 中包含 `timestamp_for_filename`，但该函数在文件中**没有定义**。

**状态**: ✅ 已修复 — 在 `time_utils.py` 中添加了 `timestamp_for_filename` 函数定义

---

### 问题30: `error_classifier.py` 中调用不存在的方法 `classify`

**严重程度**: P1-高（运行时会报 AttributeError）  
**问题描述**: `tool_retry_engine.py` 第164行调用 `UnifiedErrorClassifier.classify(e)`，但 `UnifiedErrorClassifier` 类中只有 `classify_error` 方法。

**状态**: ✅ 已修复 — 将 `classify` 改为 `classify_error`

---

### 问题31: `display_utils.py` 中 `__all__` 导出的函数名与导入路径不匹配

**严重程度**: P3-低（不影响运行，但有潜在风险）  
**问题描述**: `display_utils.py` 文档注释中写的是 `from app.utils.common import xxx`，但实际文件名已改为 `display_utils.py`。

**状态**: ✅ 已修复 — 更新了文档注释中的导入路径

---

## 三、问题汇总

| 类别 | 数量 | 说明 |
|------|------|------|
| 已修复 | 26 | 之前的命名问题 |
| 新发现P0 | 1 | timestamp_for_filename函数不存在 → ✅ 已修复 |
| 新发现P1 | 2 | extract_api_error_detail未删除 → ✅ 已修复 |
| 新发现P2 | 1 | 同名文件tool_result_formatter.py → ✅ 已修复 |
| 新发现P3 | 1 | 文档注释导入路径不匹配 → ✅ 已修复 |
| **总计** | **31** | **全部已修复** |

---

## 四、待修复清单

**无** — 所有问题已全部修复。

---

**报告完成时间**: 2026-06-08 12:36:43  
**执行人**: 小欧
